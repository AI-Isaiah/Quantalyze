---
phase: 94-wizard-resumability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/strategies/composite/members/route.ts
  - src/app/api/strategies/composite/members/route.test.ts
autonomous: true
requirements: [WIZ-01]
must_haves:
  truths:
    - "An authenticated owner GETs their composite draft's member keys (seq, api_key_id, exchange, nickname, window, verified) — and the response NEVER contains any of the 5 secret columns: api_key_encrypted, api_secret_encrypted, passphrase_encrypted, dek_encrypted, nonce"
    - "A non-owner (or a nonexistent strategy_id) gets a uniform 403 { code: 'UNKNOWN' } — not-found and not-owned are indistinguishable (no existence oracle)"
    - "A malformed strategy_id gets 400; every response carries NO_STORE_HEADERS"
    - "No migration and no new RLS policy exist — the read rides existing owner RLS on strategy_keys + api_keys"
  artifacts:
    - path: "src/app/api/strategies/composite/members/route.ts"
      provides: "GET owner-scoped composite member read, secretless by construction"
      exports: ["GET"]
      min_lines: 60
    - path: "src/app/api/strategies/composite/members/route.test.ts"
      provides: "Load-bearing no-secret-leak route test + owner-scope + shape pins"
      min_lines: 100
  key_links:
    - from: "src/app/api/strategies/composite/members/route.ts"
      to: "strategy_keys (RLS owner-scoped)"
      via: "supabase embedded select naming ONLY non-secret columns"
      pattern: "seq, api_key_id, window_start, window_end"
    - from: "src/app/api/strategies/composite/members/route.ts"
      to: "withAuth"
      via: "export const GET = withAuth("
      pattern: "withAuth"
---

<objective>
WIZ-01 — the phase's SECURITY foundation, lands first. Add a plain authenticated
GET route that returns a composite draft's existing member keys WITHOUT secrets,
so WIZ-02/WIZ-05 can rehydrate the wizard. NO migration, NO new RLS policy, NO
SECURITY DEFINER RPC (research-verified: existing owner RLS on `strategy_keys` +
`api_keys` fully covers the owner-scoped read; a DEFINER read is exactly the
surface that could leak ciphertext).

Purpose: the wizard currently has no server read of composite membership — any
unmount of MultiKeyConnectStep drops all key state with no way back.
Output: `GET /api/strategies/composite/members?strategy_id=<uuid>` + a
load-bearing route test proving the 5 secret columns can never serialize.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/94-wizard-resumability/94-RESEARCH.md

<interfaces>
<!-- Extracted from the codebase this session. Use directly — no exploration needed. -->

From src/app/api/strategies/composite/set-members/route.ts (the posture to mirror):
```typescript
import { NextRequest, NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { withAuth } from "@/lib/api/withAuth";
import { isUuid } from "@/lib/utils";
import { NO_STORE_HEADERS } from "@/lib/api/headers";
import type { User } from "@supabase/supabase-js";

export const POST = withAuth(async (req: NextRequest, user: User) => { ... });
// uniform error posture: NextResponse.json({ code: "..." }, { status, headers: NO_STORE_HEADERS })
```

Storage (from migrations — verified):
- `strategy_keys` (20260710120000_strategy_keys.sql:30-43): id, strategy_id,
  api_key_id, owner_id, window_start DATE, window_end DATE (null = live), seq,
  created_at. RLS `strategy_keys_owner` USING (owner_id = auth.uid()). NO secret columns.
- `api_keys` (20260405061911_initial_schema.sql:19-32): id, user_id, exchange,
  label, is_active + the 5 SECRET/ENVELOPE columns that must NEVER serialize:
  `api_key_encrypted, api_secret_encrypted, passphrase_encrypted, dek_encrypted, nonce`.
  RLS owner-only (user_id).
- Owner-coherence trigger guarantees owner_id == api_keys.user_id == strategies.user_id.
- "verified" is derivable: every strategy_keys member was read-only-validated at
  add-key time (add-key/route.ts:161-179 runs validateKey BEFORE minting the row),
  so membership ⇒ verified:true by construction. No new column.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/strategies/composite/members — owner-scoped secretless member read</name>
  <files>src/app/api/strategies/composite/members/route.ts</files>
  <read_first>
    - src/app/api/strategies/composite/set-members/route.ts (whole file, 163 lines — the withAuth/isUuid/NO_STORE_HEADERS/createClient/uniform-{code} posture to mirror verbatim)
    - src/lib/api/withAuth.ts (handler signature)
  </read_first>
  <behavior>
    - Owner GET with valid strategy_id → 200 { ok: true, members: [...] } ordered by seq asc
    - Each member: { seq, api_key_id, exchange, nickname, window_start, window_end, verified: true } — nickname = api_keys.label, window_end null = still live
    - strategy_id missing/not a UUID → 400 { code: "UNKNOWN" }
    - strategy_id valid UUID but strategy not found OR not owned by caller → 403 { code: "UNKNOWN" } (identical either way — no existence oracle, mirrors set_wizard_composite_members' WHERE id=? AND user_id=? guard)
    - Owned strategy with zero strategy_keys rows → 200 { ok: true, members: [] }
    - Every response (incl. errors) carries NO_STORE_HEADERS
  </behavior>
  <action>
    Create `src/app/api/strategies/composite/members/route.ts` exporting
    `export const GET = withAuth(async (req: NextRequest, user: User) => ...)`.
    Read `strategy_id` from `req.nextUrl.searchParams`. Guard with `isUuid`
    (set-members/route.ts:61 pattern) → 400. Then `const supabase = await
    createClient()` and TWO reads:

    1. Ownership guard (no existence oracle): `.from("strategies").select("id")
       .eq("id", strategyId).eq("user_id", user.id).maybeSingle()` — no row →
       403 { code: "UNKNOWN" }. This gives the clean 403 the RLS-empty read
       alone can't (Ph91: a non-owner RLS read is silently empty).
    2. Member read — RLS-scoped embedded select that names ONLY non-secret
       columns: `.from("strategy_keys").select("seq, api_key_id, window_start,
       window_end, api_keys ( exchange, label )").eq("strategy_id", strategyId)
       .order("seq", { ascending: true })`.

    Build the response FIELD-BY-FIELD (belt-and-suspenders — NEVER spread a DB
    row, never `SELECT *`): members = data.map(m => ({ seq: m.seq, api_key_id:
    m.api_key_id, exchange: m.api_keys?.exchange ?? null, nickname:
    m.api_keys?.label ?? null, window_start: m.window_start, window_end:
    m.window_end, verified: true })). `verified: true` is correct by
    construction (add-key validates read-only scope before minting the row).
    Note: `api_key_id` is REQUIRED in the response — it is a non-secret row
    UUID and WIZ-02's secretless resubmit (buildSetMembersKeys → set-members)
    has nothing to send without it.

    Error handling: catch → 500 { code: "UNKNOWN" }, `console.error` with the
    error message ONLY (H-0305 posture — never key material; there is none in
    scope to log). NO rate limiter: userActionLimiter buckets are for
    mutations; this read fires on every step re-mount and is RLS-bounded +
    idempotent (document this choice in a comment).

    CRITICAL: do NOT write any of the 5 secret column names anywhere in this
    file — not in code, not in comments (a grep gate in Task 2 asserts 0
    occurrences; keep the file greppably clean).

    NO migration, NO new RLS policy, NO RPC. If you find one IS needed, STOP
    and flag prominently — that contradicts verified research.
  </action>
  <verify>
    <automated>npx tsc --noEmit && ! grep -qE "api_key_encrypted|api_secret_encrypted|passphrase_encrypted|dek_encrypted|nonce" src/app/api/strategies/composite/members/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exports `GET` wrapped in `withAuth`; imports `isUuid`, `NO_STORE_HEADERS`, `createClient` from the same modules set-members uses
    - The `.select()` string is exactly the non-secret enumeration above — grep for the 5 secret column names in the file returns 0 matches
    - Response members are built field-by-field (no object spread of a DB row anywhere in the file)
    - 400 on bad UUID, 403 on not-found/not-owned (same body), NO_STORE_HEADERS on every branch
    - `git status` shows NO new file under supabase/migrations/
  </acceptance_criteria>
  <done>Route compiles, secretless by grep, owner-guarded, no migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Load-bearing no-secret-leak route test</name>
  <files>src/app/api/strategies/composite/members/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/composite/set-members/route.test.ts (whole file — the createClient/withAuth mocking harness to mirror; same mock idioms, same request construction)
  </read_first>
  <behavior>
    - THE LEAK PIN: mock the strategy_keys read to return rows that DELIBERATELY carry planted secret fields (api_key_encrypted: "SENTINEL_CT_1", api_secret_encrypted: "SENTINEL_CT_2", passphrase_encrypted: "SENTINEL_CT_3", dek_encrypted: "SENTINEL_CT_4", nonce: "SENTINEL_CT_5") alongside the legit columns — assert the serialized 200 body (JSON.stringify of the parsed response) contains NONE of the 5 column NAMES and NONE of the 5 SENTINEL values. This proves the field-by-field build strips secrets even if the select were somehow over-broad.
    - Shape pin: 200 body is { ok: true, members: [{ seq, api_key_id, exchange, nickname, window_start, window_end, verified: true }] } with members ordered by seq; a null window_end passes through as null (live member)
    - Owner-scope: ownership guard returns no row → 403 { code: "UNKNOWN" }; the strategy_keys read is never issued (assert the mock not called)
    - Non-existent vs not-owned produce byte-identical responses (no existence oracle)
    - Missing / malformed strategy_id → 400
    - Empty membership (owned strategy, 0 rows) → 200 { ok: true, members: [] }
    - Cache-Control no-store present on the 200 and the 403
  </behavior>
  <action>
    Create `route.test.ts` mirroring set-members/route.test.ts's mock harness
    (vi.mock of @/lib/supabase/server createClient + the withAuth passthrough
    idiom used there). Build a chainable supabase mock where
    from("strategies")→maybeSingle resolves the ownership probe and
    from("strategy_keys")→order resolves the member rows. Write the 7 behavior
    cases above as individual tests. The leak assertion must check BOTH the
    5 column names AND the 5 sentinel values against the full serialized body
    string — this is the phase's load-bearing security pin (94-VALIDATION.md
    "Security pin" row).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/composite/members/route.test.ts && npm run lint -- src/app/api/strategies/composite/members</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains the exact strings "api_key_encrypted", "api_secret_encrypted", "passphrase_encrypted", "dek_encrypted", "nonce" in its leak assertion (the ONLY place in the new directory they may appear)
    - Mutation-honesty spot check performed and reverted: temporarily change the route's response map to spread the DB row (`...m`) → the leak test goes RED; revert, porcelain clean
    - All tests green; tsc clean; lint 0 new errors
  </acceptance_criteria>
  <done>Leak pin RED-provable, owner-scope + shape + error posture pinned, suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/strategies/composite/members | untrusted strategy_id query param from an authenticated but arbitrary user |
| route → strategy_keys/api_keys | ciphertext-bearing tables; only non-secret columns may cross back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Information Disclosure | members/route.ts response | mitigate (block-on: high) | Enumerated non-secret `.select()` + field-by-field response build + Task 2 sentinel leak pin + Task 1 grep gate (5 column names absent from route source) |
| T-94-02 | Elevation/IDOR | members/route.ts ownership | mitigate | withAuth + explicit `strategies WHERE id=? AND user_id=?` guard + strategy_keys owner RLS (defense in depth); non-owner → 403, RLS read empty regardless |
| T-94-03 | Information Disclosure (existence oracle) | 403 branch | mitigate | not-found and not-owned return byte-identical 403 { code: "UNKNOWN" } (pinned by test) |
| T-94-04 | Information Disclosure (logs) | catch branch | mitigate | console.error logs error message only; no key material is in scope; uniform { code } posture never forwards raw messages (H-0305) |
| T-94-05 | Tampering (supply chain) | dependencies | accept | zero new packages installed this phase |

No high-severity threat ships: T-94-01 is closed by construction + test + grep before this plan completes.
</threat_model>

<verification>
- `npx vitest run src/app/api/strategies/composite/members/route.test.ts` green
- `npx tsc --noEmit` clean; `npm run lint` 0 new errors
- `! grep -rqE "api_key_encrypted|api_secret_encrypted|passphrase_encrypted|dek_encrypted|nonce" src/app/api/strategies/composite/members/route.ts` (test file exempt — it holds the leak assertion)
- `git status --porcelain supabase/migrations/` empty (no migration)
</verification>

<success_criteria>
WIZ-01 satisfied: owner-scoped GET returns member metadata (exchange, nickname,
active window, verified, api_key_id) and can never serialize the 5 secret
columns — proven by a leak test that goes RED under a spread-the-row mutation.
Non-owner gets 403/empty. No migration, no new RLS policy, no RPC.
</success_criteria>

<output>
Create `.planning/phases/94-wizard-resumability/94-01-SUMMARY.md` when done
</output>
