---
phase: 94-wizard-resumability
plan: 03
type: execute
wave: 3
depends_on: ["94-02"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx
autonomous: true
requirements: [WIZ-03, WIZ-05]
must_haves:
  truths:
    - "'Review your keys' (composite) is NON-destructive: it navigates back to connect_key only — the draft, its strategy_keys members, and the wizardSessionId all survive; no DELETE fetch fires (D: onReviewKeys = setStep only)"
    - "Single-key 'Try another key' keeps its existing destructive path unchanged (delete draft + fresh session) — no new destructive affordance is added"
    - "Returning to sync_preview with a held snapshot shows the cached stitch snapshot immediately — no strategy_analytics read, no /api/keys/sync POST, no poll (cachedSnapshot short-circuit)"
    - "Durability: ANY COMPLETE composite snapshot short-circuits the crawl kickoff regardless of the 5-minute freshness window (hard-reload case) — a completed crawl is never re-kicked just to display"
    - "Single-key freshness behavior is byte-neutral: fresh skips, stale re-kicks exactly as today"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      provides: "onReviewKeys prop split + cachedSnapshot short-circuit + composite COMPLETE-skip"
      contains: "onReviewKeys"
    - path: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      provides: "non-destructive onReviewKeys callback + cachedSnapshot={syncSnapshot} threading"
      contains: "cachedSnapshot"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      to: "SyncPreviewStep"
      via: "onReviewKeys + cachedSnapshot props at the sync_preview render site"
      pattern: "onReviewKeys=|cachedSnapshot="
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      to: "mount kickoff effect"
      via: "cachedSnapshot early-return BEFORE any supabase read or kickoff POST"
      pattern: "cachedSnapshot"
---

<objective>
WIZ-03 + WIZ-05 — one plan because both live in SyncPreviewStep.tsx +
WizardClient.tsx (file-ownership serialization).

WIZ-03 root cause: all three review/try-again buttons (SyncPreviewStep.tsx
:1043 gate-failed, :1396 composite success, :1470 single-key success) wire
`onClick={onTryAnotherKey}`, and `onTryAnotherKey` (WizardClient.tsx:647-663)
does `setStep("connect_key")` + `setWizardSessionId(newWizardSessionId())` +
`void handleDeleteDraft()` — for a composite this DELETEs the draft and
cascades away all strategy_keys members. Split the callback by isComposite
(server-truth state, :322). RESOLVED decision A2: "Review your keys" becomes
non-destructive setStep only; NO new destructive "start over" affordance;
single-key keeps its destructive path unchanged.

WIZ-05 root cause: the mount effect (:330-434) re-runs on every remount
(WizardClient renders SyncPreviewStep only while step === "sync_preview",
:641-664) and re-POSTs /api/keys/sync unless the analytics row is COMPLETE and
< 5 min fresh (SYNC_FRESHNESS_WINDOW_MS). WizardClient already holds the
completed snapshot (`syncSnapshot`, :144, set at :412) — thread it back as
`cachedSnapshot` and short-circuit. RESOLVED decision 1: ALSO make any COMPLETE
composite skip the kickoff regardless of freshness (hard-reload durability).

Output: non-destructive review + cached-snapshot back-nav, with component tests
that fail without each fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-wizard-resumability/94-RESEARCH.md

<interfaces>
<!-- Extracted from the codebase this session. -->

From steps/SyncPreviewStep.tsx:210-216 (props to extend):
```typescript
export interface SyncPreviewStepProps {
  strategyId: string;
  apiKeyId: string | null;
  wizardSessionId: string;
  onComplete: (snapshot: SyncPreviewSnapshot) => void;
  onTryAnotherKey: () => void;
}
```
`SyncPreviewSnapshot` (:188-208) carries optional `composite?: CompositePreviewData`.
`isComposite` state (:322) is threaded from SERVER TRUTH (kickoff response /
DQ-flags marker) — reuse it, never re-derive client-side.

Mount effect structure (:330-433, deps [strategyId]): async IIFE →
supabase read of strategy_analytics (computation_status, computed_at) →
`isFresh = isComputedAnalytics(status) && Date.now() - computedAtMs < SYNC_FRESHNESS_WINDOW_MS`
→ if fresh: a SECOND read of data_quality_flags decides composite-ness
(fail CLOSED to SYNC_FAILED on error), setPhase("waiting_for_complete"), return
→ else POST /api/keys/sync (:394), thread kickoff.composite → isComposite.

The three button sites, ALL currently `onClick={onTryAnotherKey}`:
- :1041-1047 gate-failed:  label `{isComposite ? "Review your keys" : "Try another key"}`
- :1394-1400 composite success: label "Review your keys" (ghost)
- :1468-1474 single-key success: label "Try another key" (ghost)
All three carry data-testid="wizard-try-another-key".

From WizardClient.tsx:641-664 (render site + the destructive callback to leave intact):
```tsx
{step === "sync_preview" && strategyId && (
  <SyncPreviewStep
    strategyId={strategyId} apiKeyId={apiKeyId} wizardSessionId={wizardSessionId}
    onComplete={handleSyncComplete}
    onTryAnotherKey={() => {  // DESTRUCTIVE — keep byte-identical for single-key
      setStep("connect_key");
      setWizardSessionId(newWizardSessionId());   // F6 fence / LOW-2 — do not touch
      void handleDeleteDraft();
      trackForQuantsEventClient("wizard_try_different_key", { ... });
    }}
  />
)}
```
`persistPointer(nextStep, id)` (:376-393) saves the resume pointer;
`syncSnapshot` state at :144; `handleSyncComplete` sets it at :412.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WIZ-03 — split the review affordance from the destructive one</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx:210-216 (props), :1040-1050, :1390-1401, :1464-1475 (the three button sites)
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx:641-664 (render site + onTryAnotherKey), :462-520 (handleDeleteDraft — read to understand what must NOT fire), :376-393 (persistPointer)
  </read_first>
  <behavior>
    - Composite gate-failed card AND composite success card: clicking "Review your keys" calls onReviewKeys — step becomes connect_key, NO DELETE /api/strategies/draft/* fetch, wizardSessionId UNCHANGED, strategyId retained (MultiKeyConnectStep then rehydrates via WIZ-02)
    - Single-key success card: "Try another key" still calls onTryAnotherKey (destructive path byte-identical — F6 fence semantics preserved, research Pitfall 3)
  </behavior>
  <action>
    SyncPreviewStep.tsx: add `onReviewKeys?: () => void` to SyncPreviewStepProps
    (adjacent to onTryAnotherKey, :215). At ALL THREE button sites change
    `onClick={onTryAnotherKey}` to
    `onClick={isComposite && onReviewKeys ? onReviewKeys : onTryAnotherKey}` —
    BOTH the gate-failed card (:1043) and the composite success card (:1396)
    must be non-destructive for composites (a user fixing a bad window must not
    lose the draft); the single-key site (:1470) resolves to onTryAnotherKey
    (isComposite false there). Labels are already isComposite-branched — do not
    change copy. Keep data-testid as-is.

    WizardClient.tsx: at the sync_preview render site add
    `onReviewKeys={() => { setStep("connect_key"); persistPointer("connect_key", strategyId); }}`
    — setStep + pointer persist ONLY: no handleDeleteDraft, no
    setWizardSessionId, no session regen (resolved decision 3). Leave the
    existing onTryAnotherKey closure byte-identical.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - SyncPreviewStepProps contains `onReviewKeys?: () => void`
    - All three onClick sites read `isComposite && onReviewKeys ? onReviewKeys : onTryAnotherKey`
    - WizardClient's onReviewKeys body contains setStep("connect_key") + persistPointer and does NOT reference handleDeleteDraft or newWizardSessionId
    - git diff of the onTryAnotherKey closure is empty (single-key destructive path untouched)
  </acceptance_criteria>
  <done>Composite review is a pure step transition; single-key destructive semantics intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WIZ-05 — cachedSnapshot short-circuit + COMPLETE-composite durability skip</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx:330-434 (the whole mount effect — freshness skip :360-393, kickoff POST :394-421, fail-closed idioms), :55-60 (SYNC_FRESHNESS_WINDOW_MS)
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx:144 (syncSnapshot), :410-422 (handleSyncComplete)
  </read_first>
  <behavior>
    - Remount with cachedSnapshot present → setSnapshot(cachedSnapshot); if cachedSnapshot.composite exists setIsComposite(true); setPhase("passed"); return — BEFORE the supabase strategy_analytics read and BEFORE any /api/keys/sync POST; no poll starts (regardless of freshness)
    - Mount with NO cachedSnapshot but strategy_analytics COMPLETE + data_quality_flags.composite === true → skip the kickoff POST regardless of computed_at age (durability: hard reload after a finished stitch never re-kicks); phase "waiting_for_complete" so the existing poll materializes the snapshot from persisted rows
    - Single-key with NO cachedSnapshot: COMPLETE + fresh skips (unchanged), COMPLETE + stale still POSTs /api/keys/sync (byte-neutral — the incremental re-sync stays desirable for single keys)
    - Fail-closed idioms preserved: dq-flags read error → SYNC_FAILED (unchanged, :382-386)
  </behavior>
  <action>
    SyncPreviewStep.tsx: add `cachedSnapshot?: SyncPreviewSnapshot | null` to
    SyncPreviewStepProps. In the mount effect:

    1. FIRST LINES of the async IIFE (before createClient): if cachedSnapshot
       is present → `setSnapshot(cachedSnapshot); if (cachedSnapshot.composite)
       setIsComposite(true); setPhase("passed"); return;`. This mirrors the
       existing freshness-skip idiom but keys off the client-held snapshot —
       no DB probe, no kickoff, no poll (phase "passed" never enters the poll
       effect). Do NOT switch to always-mounted-with-CSS-hide (research: that
       changes the render model + poll lifecycle for no benefit).
    2. Durability (resolved decision 1): restructure the post-read branch so
       composite-ness is decided for ANY complete row, not only fresh ones:
       compute `isComplete = isComputedAnalytics(existing?.computation_status)`
       and keep `isFresh` as today. When `isComplete` (fresh OR stale), perform
       the existing data_quality_flags read (keep its fail-CLOSED SYNC_FAILED
       branch verbatim); if `dqFlags.composite === true` → set isComposite,
       setPhase("waiting_for_complete"), return (kickoff SKIPPED regardless of
       freshness — a finished stitch never needs re-running to display). If not
       composite: fresh → skip as today; stale → fall through to the kickoff
       POST exactly as today. Net effect: single-key behavior byte-neutral
       except one extra lightweight dq read on the complete-and-stale
       single-key resume (document in a comment).
    3. Add cachedSnapshot to the effect deps to satisfy react-hooks lint (it
       is stable WizardClient state; the early return keeps re-runs inert).

    WizardClient.tsx: at the sync_preview render site add
    `cachedSnapshot={syncSnapshot}` (the state set by handleSyncComplete:412 —
    the "thread state that already exists" fix; no new persistence layer).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - The cachedSnapshot early-return is the first statement block of the async IIFE (grep order: "cachedSnapshot" appears before "createClient()" within the effect)
    - The dq-flags fail-CLOSED branch (SYNC_FAILED on error/missing row) is byte-identical
    - A COMPLETE composite with computed_at older than SYNC_FRESHNESS_WINDOW_MS does not POST /api/keys/sync (new test-pinned path)
    - A COMPLETE-but-stale single-key row still POSTs /api/keys/sync (byte-neutral pin)
  </acceptance_criteria>
  <done>Back-nav and hard reload both show the cached/persisted snapshot; no re-crawl, no re-stitch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Component tests — non-destructive review + no-re-kick pins</name>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx (harness: how sync_preview is reached, fetch/supabase mock idioms)
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx (render-test harness + existing kickoff mocking)
  </read_first>
  <behavior>
    - WizardClient composite at sync_preview: click "Review your keys" → step renders connect_key, NO fetch with method DELETE was issued to /api/strategies/draft/*, and the wizardSessionId prop passed to the connect-step child is UNCHANGED from before the click (spy/capture the prop or assert no newWizardSessionId effect)
    - WizardClient single-key at sync_preview: "Try another key" still issues the DELETE (destructive pin — proves the split didn't blanket-remove it, research Pitfall 3)
    - SyncPreviewStep mounted with cachedSnapshot (composite variant) → renders the passed snapshot ("passed" UI), NO /api/keys/sync fetch, NO strategy_analytics read
    - SyncPreviewStep mounted without cachedSnapshot, supabase mock returns COMPLETE composite row with computed_at 10 minutes old → NO /api/keys/sync fetch
    - SyncPreviewStep mounted without cachedSnapshot, COMPLETE single-key row 10 minutes old → /api/keys/sync fetch FIRES (byte-neutral pin)
  </behavior>
  <action>
    Extend WizardClient.test.tsx with a "review your keys (WIZ-03)" describe
    and SyncPreviewStep.render.test.tsx with a "cached snapshot (WIZ-05)"
    describe, reusing each file's existing mock idioms. Each test must fail
    without its Task 1/Task 2 change (verify RED by locally reverting the
    relevant edit once; note in summary). Assert the negative space with
    explicit fetch-call inventories (collect mock.calls, filter by
    method/URL), not just positive renders.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard" && npm run lint -- "src/app/(dashboard)/strategies/new/wizard"</automated>
  </verify>
  <acceptance_criteria>
    - A no-DELETE assertion exists for the composite review path and a DELETE-fires assertion for the single-key path
    - A zero-/api/keys/sync-calls assertion exists for both the cachedSnapshot path and the stale-COMPLETE-composite path
    - Full wizard vitest directory green; lint 0 new errors
  </acceptance_criteria>
  <done>WIZ-03 + WIZ-05 pinned offline, RED-provable both ways.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client step transitions | destructive vs non-destructive affordances must not blend |
| cached client state → rendered UI | snapshot is server-derived; no key material inside |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-10 | Tampering (data loss) | onReviewKeys wiring | mitigate | non-destructive callback contains only setStep + persistPointer (acceptance-grepped); destructive closure byte-identical for single-key; test pins both directions |
| T-94-11 | Repudiation/session-fence bypass | wizardSessionId | mitigate | onReviewKeys never regenerates the session id (F6 duplicate-submit fence untouched — the destructive path keeps its optimistic regen, WizardClient.tsx:649-658) |
| T-94-12 | Information Disclosure | cachedSnapshot contents | accept | SyncPreviewSnapshot carries only derived metrics/dates/labels (shape at :188-208); no ciphertext fields exist in the type |
| T-94-13 | DoS (self-inflicted re-crawl) | mount kickoff | mitigate | cachedSnapshot short-circuit + COMPLETE-composite skip close the re-kick window entirely |
| T-94-14 | Tampering (supply chain) | dependencies | accept | zero new packages |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/strategies/new/wizard"` green
- `npx tsc --noEmit` clean; `npm run lint` 0 new errors
- git diff shows onTryAnotherKey closure and the dq-flags fail-closed branch unmodified
</verification>

<success_criteria>
WIZ-03: "Review your keys" navigates back non-destructively — draft + members +
session survive the round-trip (both the success and gate-failed cards).
WIZ-05: returning to the crawled step shows the cached stitch snapshot with
zero re-crawl/re-stitch, including after a hard reload (COMPLETE composite
never re-kicks). Single-key paths byte-neutral.
</success_criteria>

<output>
Create `.planning/phases/94-wizard-resumability/94-03-SUMMARY.md` when done
</output>
