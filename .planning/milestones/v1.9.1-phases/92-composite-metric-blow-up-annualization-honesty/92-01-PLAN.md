---
phase: 92-composite-metric-blow-up-annualization-honesty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/tests/test_native_nav.py
autonomous: true
requirements: [HARD-01]
must_haves:
  truths:
    - "A pure, offline fixture on the REAL services.native_nav.reconstruct_native_nav_and_twr reproduces an un-guarded per-day return |r| ≈ 17/day from a small-but-above-dust prev NAV plus a large same-day native P&L (the live blow-up class), evidenced as a strict-xfail RED"
    - "The branch-selector diagnostic proves whether the reconstructed NAV matches the hand-computed Σ B_c(d)×mark_c(d) valuation (→ fix branch b1, magnitude guard) or diverges from it (→ fix branch b2, valuation fix)"
    - "No shared Supabase test DB is touched — the repro is pure/offline on the real function (research Pitfall 6)"
  artifacts:
    - path: "analytics-service/tests/test_native_nav.py"
      provides: "The _pnl_dominated_blowup_ledger() fixture builder + strict-xfail regression + branch-selector diagnostic"
      contains: "pnl_dominated"
  key_links:
    - from: "analytics-service/tests/test_native_nav.py"
      to: "services.native_nav.reconstruct_native_nav_and_twr"
      via: "direct import of the real production function (never a reimplementation)"
      pattern: "reconstruct_native_nav_and_twr"
---

<objective>
Build the BLOCKING offline reproduction fixture for the composite factsheet metric blow-up (HARD-01): a hand-built `NativeLedger` driven through the REAL `reconstruct_native_nav_and_twr` that reproduces the un-guarded ~1,700%/day return class (small-but-above-dust prev NAV + P&L-dominated day), plus the diagnostic that disambiguates fix branch b1 (return/P&L-magnitude guard at the denominator source) vs b2 (inverse-valuation fix).

Purpose: The phase is repro-gated — it MUST NOT close on reasoning alone (ROADMAP hard gate). This plan produces the RED evidence that Plan 92-02's fix turns GREEN, and the valuation evidence that selects which fix branch 92-02 implements.
Output: Two new tests in `analytics-service/tests/test_native_nav.py` (one strict-xfail regression pinning the bug, one green branch-selector diagnostic) + a shared module-level ledger builder, and a SUMMARY that records the captured exploded return magnitude and the selected fix branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/92-composite-metric-blow-up-annualization-honesty/92-RESEARCH.md
@.planning/phases/92-composite-metric-blow-up-annualization-honesty/92-VALIDATION.md
</context>

<interfaces>
<!-- Key contracts, extracted from the codebase 2026-07-11. Use directly — no exploration needed. -->

From analytics-service/services/native_nav.py (dataclass at :207-243, function at :579-658):
```python
class NativeLedger:  # pure data; currency keys UPPERCASE
    native_pnl: Mapping[str, pd.Series]          # per-ccy daily NATIVE pnl, tz-naive midnight DatetimeIndex
    terminal_native_equity: Mapping[str, float]  # per-ccy NATIVE equity at the anchor
    marks: Mapping[str, pd.Series]               # per-ccy daily USD mark, REQUIRED dense for every valued day
    native_flows: Sequence[ExternalFlow]         # dated flows in NATIVE units: (utc_day_iso, currency, quantity)
    terminal_upnl_native: Mapping[str, float]    # empty mapping = zero wedge
    full_history: bool                           # True ⇒ §5 inception gate reconciles pre-history ≈ 0 per ccy

def reconstruct_native_nav_and_twr(
    ledger: NativeLedger, *, indexable_currencies: frozenset[str], venue: str = ""
) -> tuple[pd.Series, NavTWRMeta]
```

From analytics-service/services/nav_twr.py:
```python
DUST_NAV_FLOOR = 1000.0   # :58
FLOW_DOM_RATIO = 1.0      # :65
# chain_linked_twr per-day return (:392): returns[t] = (cur - prev - flow_t) / prev
# _guard_denominator (:397-420) fires ONLY on: prev<=0, prev<DUST_NAV_FLOOR, |flow|>=FLOW_DOM_RATIO*prev
# — there is NO P&L-magnitude guard (the defect under test).
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pin the inverse-perpetual P&L-dominated blow-up with a strict-xfail regression on the real native core</name>
  <files>analytics-service/tests/test_native_nav.py</files>
  <read_first>
    - .planning/phases/92-composite-metric-blow-up-annualization-honesty/92-RESEARCH.md (§a root cause, §f fixture design — the authoritative spec)
    - analytics-service/services/native_nav.py (NativeLedger :207-243, reconstruct_native_nav_and_twr :579-658 — the §5 inception gate at full_history=True is why the fixture needs a deposit flow)
    - analytics-service/services/nav_twr.py (:377-420 — chain_linked_twr loop + _guard_denominator, the guard gap under test)
    - analytics-service/tests/test_native_nav.py (existing NativeLedger construction conventions — e.g. test_mixed_account_composition_hand_computed at ~:213 and the native_flows shape used by existing fixtures; mirror them exactly)
  </read_first>
  <action>
    Add a module-level fixture builder `_pnl_dominated_blowup_ledger()` to test_native_nav.py returning a `NativeLedger` with a single INDEXED "BTC" bucket over 7 consecutive UTC days (e.g. 2024-01-01..2024-01-07), designed so the reconstruction emits one un-guarded explosive return:
    - `marks`: {"BTC": dense 88000.0 Series covering all 7 days} (density contract, native_nav §3.3).
    - `native_flows`: ONE inception deposit on day 1 of 0.025 BTC (mirror the existing tests' native-flow construction — the core reads (utc_day_iso, currency, quantity)). This seeds the backward roll's pre-history balance to ~0 so the §5 inception gate reconciles under `full_history=True` (without it the gate refuses loud and the fixture never reaches the divide).
    - `native_pnl`: {"BTC": Series} ≈ [d1 +0.002, d2 +0.003, d3 +0.52 (the P&L-DOMINATED day), d4 +0.005, d5 +0.004, d6 +0.006, d7 +0.002]. Hand-check the shape before committing: backward-rolled balance before d3 must be ≈ 0.030 BTC → prev NAV ≈ $2,640 (ABOVE `DUST_NAV_FLOOR=1000` so no existing guard fires; A3 in the research) while d3's P&L ≈ $45,760 → r ≈ 17.3 (the research's 10–100/day class). Days d4–d7 must be small POSITIVE returns so that, post-fix, the retained suffix after the guarded break is a rising ≥4-day window (this is what lets Plan 92-02 assert CAGR > 0 while the curve rises).
    - `terminal_native_equity`: {"BTC": deposit + Σ pnl} (exact float sum of the chosen values); `terminal_upnl_native`: {}; `full_history`: True.
    Then add `test_inverse_perpetual_pnl_dominated_day_is_guarded` decorated `@pytest.mark.xfail(strict=True, reason="Phase 92 HARD-01 pre-fix: a P&L-dominated day on a small-but-above-dust NAV emits an un-guarded ~17x/day return; Plan 92-02 adds the magnitude guard and removes this marker")`. The test body asserts the DESIRED behavior: call `reconstruct_native_nav_and_twr(ledger, indexable_currencies=frozenset({"BTC"}), venue="deribit")` and assert every non-NaN value in the returned Series has `abs(r) < 5.0` (i.e. any P&L-dominated day must be guarded to NaN, never emitted). Pre-fix this assertion FAILS (r≈17 on d3) — strict xfail keeps the suite green while pinning the failure as evidence; post-fix an un-removed marker would XPASS-error, forcing 92-02 to promote it. Day 1 is expected NaN (`negative_nav_guard`, prev0≈0 from the deposit-seeded inception) — that is a leading terminus, not the bug.
    After writing, run once with `--runxfail` and capture the AssertionError output (the actual exploded r value) verbatim into the plan SUMMARY — this is the phase's RED repro-gate evidence.
    Do NOT touch any services/ file, do NOT reimplement the roll in the test, and do NOT use the shared Supabase test project (pure offline only — research Pitfall 6).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_native_nav.py -k pnl_dominated -q 2>&1 | grep -E "1 xfailed"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_native_nav.py -k pnl_dominated -q` reports exactly `1 xfailed` (strict xfail — proves the assertion currently fails, i.e. the bug reproduces).
    - `pytest tests/test_native_nav.py -k pnl_dominated -q --runxfail` FAILS with an AssertionError whose captured |r| is > 5 (expected ≈ 17.3); the value is recorded in the SUMMARY as RED evidence.
    - The test imports `reconstruct_native_nav_and_twr` from `services.native_nav` (grep the new test: `from services.native_nav import` or `services.native_nav.reconstruct_native_nav_and_twr` present; no local reimplementation of the backward roll).
    - The fixture's pre-blow-up NAV is above the dust floor: the hand-computed balance-before-d3 × mark ≥ 1000 (comment in the test states the hand computation).
    - `pytest tests/test_native_nav.py -q` is otherwise fully green (no existing test disturbed) and no test in the file opens a network/DB connection.
  </acceptance_criteria>
  <done>The blow-up reproduces offline on the real function as a strict-xfail with captured RED evidence; full test_native_nav.py otherwise green.</done>
</task>

<task type="auto">
  <name>Task 2: Branch-selector diagnostic — hand-model NAV valuation proves b1 (real small equity) vs b2 (valuation artifact)</name>
  <files>analytics-service/tests/test_native_nav.py</files>
  <read_first>
    - .planning/phases/92-composite-metric-blow-up-annualization-honesty/92-RESEARCH.md (§b fix branches, §h Q1 — what the evidence must disambiguate)
    - analytics-service/services/native_nav.py (:475-540 _value_over_calendar — NAV(d) = Σ_c B_c(d)×mark_c(d), the valuation under audit)
    - analytics-service/tests/test_native_nav.py (Task 1's _pnl_dominated_blowup_ledger builder — reuse it, do not duplicate)
  </read_first>
  <action>
    Add `test_blowup_fixture_nav_valuation_matches_hand_model` (NOT xfailed — must pass now AND stay green after 92-02's fix). Reuse `_pnl_dominated_blowup_ledger()`. Hand-compute, in the test with explicit arithmetic comments, the expected per-day native balances via the backward roll (B(d-1) = B(d) − pnl(d) − flow(d)) and the expected per-day returns under CORRECT valuation (r_t = pnl_t×mark / (B_{t-1}×mark) = pnl_t / B_{t-1}, mark constant at 88000). Assert the emitted returns on the NON-dominated days (d2 and d4..d7 — days that survive both pre- and post-fix) equal the hand-model values with `pytest.approx(rel=1e-9)`. Deliberately do NOT assert on d3 here (post-fix it becomes a guarded NaN; d3's pre-fix magnitude is already captured by Task 1's --runxfail evidence).
    Branch-selection logic to record in the SUMMARY (BLOCKING input to Plan 92-02, per the phase's resolved decision 1):
    - If this test PASSES and Task 1's captured d3 value equals the hand-model pnl_d3/B_d2 (≈17.3): the reconstruction VALUES the equity correctly (Σ B×mark is right) and the tiny denominator is economically real — the defect is the missing magnitude guard → SELECT BRANCH b1 (Plan 92-02 default).
    - If this test FAILS (emitted returns diverge from the hand model): the NAV itself is mis-valued → SELECT BRANCH b2 (valuation fix in native_nav._value_over_calendar / terminal_native_equity sourcing) and say so explicitly in the SUMMARY so 92-02 pivots. Do not guess — the assertion outcome IS the selector.
    Write the selected branch as a dedicated line in the SUMMARY: "Fix branch selected by fixture evidence: b1|b2 — <one-line reason>".
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_native_nav.py -k "valuation_matches_hand_model or pnl_dominated" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_native_nav.py -k valuation_matches_hand_model -q` → 1 passed (expected outcome, selecting b1) — or, if it fails, the SUMMARY explicitly selects b2 with the observed divergence; either way the SUMMARY contains the literal line prefix "Fix branch selected by fixture evidence:".
    - The diagnostic asserts only non-dominated days (d3 is not asserted) — grep the test body: no approx assertion against the d3 index label.
    - The hand-model arithmetic is present as comments in the test (the numbers are auditable without running code).
    - Full file green: `pytest tests/test_native_nav.py -q` → all passed + 1 xfailed, 0 failed.
  </acceptance_criteria>
  <done>Branch b1/b2 is selected by test evidence (not reasoning), recorded in the SUMMARY, and the diagnostic is a durable green pin of valuation correctness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure offline test code on synthetic data; no untrusted input crosses a new boundary; no auth/session/PII surface (ASVS L1 rationale per phase security context). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Information Disclosure | test fixture values | mitigate | Fixture uses SYNTHETIC quantities only (0.025 BTC / $88k marks) — never real account balances; no live-crawl JSON committed (86-04 precedent: per-key raw P&L is account-size-adjacent). |
| T-92-SC | Tampering | package installs | accept | No new packages installed this plan (pytest/pandas already pinned); supply-chain gate not triggered. |

No high-severity threats introduced.
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_native_nav.py -q` → green with exactly 1 xfailed (the strict-xfail repro), 0 failed.
- The --runxfail capture (exploded r > 5) is present in the SUMMARY (RED repro-gate evidence).
- The SUMMARY contains "Fix branch selected by fixture evidence: b1" (or b2 with the divergence evidence).
- No shared Supabase test-DB access: the new tests import no supabase/httpx client (source-scan the diff).
</verification>

<success_criteria>
- The blow-up class (un-guarded |r| ≈ 17/day from above-dust prev NAV + dominating P&L) reproduces on the real `reconstruct_native_nav_and_twr` fully offline.
- RED evidence captured; strict xfail forces Plan 92-02 to flip it GREEN (removing the marker) — the fails-pre-fix / passes-post-fix repro gate is structurally enforced.
- The b1-vs-b2 branch decision is made by fixture evidence and handed to 92-02 in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/92-composite-metric-blow-up-annualization-honesty/92-01-SUMMARY.md` when done. It MUST include: the captured exploded-return value (RED evidence), and the line "Fix branch selected by fixture evidence: …".
</output>
