---
phase: 92-composite-metric-blow-up-annualization-honesty
plan: 02
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - analytics-service/services/nav_twr.py
  - analytics-service/services/analytics_runner.py
  - analytics-service/tests/test_native_nav.py
  - analytics-service/tests/test_nav_twr.py
  - analytics-service/tests/test_metrics.py
  - analytics-service/tests/test_stitch_composite_job.py
autonomous: true
requirements: [HARD-01]
must_haves:
  truths:
    - "The 92-01 repro regression passes WITHOUT its xfail marker: a P&L-dominated day on a small-but-above-dust NAV is guarded (NaN + pnl_dominated_guard), never emitted — fails on the pre-fix commit, passes on the fixed commit (the phase repro gate)"
    - "On the fixture driven through the offline worker harness, every persisted csv_daily_returns value is finite with |r| below the cap, and the geometric compound of the persisted series (the per-key contribution basis) stays within a plausible bound — no per-key contribution in the thousands/millions of %"
    - "compute_all_metrics on the guarded fixture series yields finite, non-zero CAGR and cumulative_return, sign-consistent with the rising retained suffix — no CAGR/Cumulative +0.0% while the curve rises"
    - "Flow-less / non-exploding accounts are byte-identical: SC-4 byte-identity pins, golden/parity fixtures, and the #597 asset-class annualization tests all stay green unchanged"
  artifacts:
    - path: "analytics-service/services/nav_twr.py"
      provides: "PNL_DOM_RATIO constant + pnl_dominated_guard wired in chain_linked_twr, registered in NAV_TWR_GUARD_KEYS, NavTWRMeta, _build_nav_meta"
      contains: "pnl_dominated_guard"
    - path: "analytics-service/services/analytics_runner.py"
      provides: "pnl_dominated_guard in the DataQualityFlags TypedDict (mypy --strict)"
      contains: "pnl_dominated_guard"
    - path: "analytics-service/tests/test_stitch_composite_job.py"
      provides: "Offline worker-level persist assertion: finite csv_daily_returns + plausible metrics_json_by_basis on the blow-up ledger"
      contains: "pnl_dominated"
  key_links:
    - from: "analytics-service/services/nav_twr.py"
      to: "chain_linked_twr per-day loop"
      via: "magnitude check on the candidate return before assignment, mirroring the existing guard_key break-and-flag block"
      pattern: "PNL_DOM_RATIO"
    - from: "analytics-service/services/nav_twr.py NAV_TWR_GUARD_KEYS"
      to: "every lift/promotion registry (transforms._merge_status_meta, analytics_runner lift+promotion, job_worker pre-stamp/member lift)"
      via: "the one-owner tuple — registries iterate it, so registration propagates by construction"
      pattern: "NAV_TWR_GUARD_KEYS"
---

<objective>
Fix the composite metric blow-up AT THE SOURCE (HARD-01): implement the fix branch selected by Plan 92-01's fixture evidence — default b1, a fail-loud P&L-magnitude guard (`pnl_dominated_guard`, the missing sibling of `flow_dominated_guard`) in `nav_twr.chain_linked_twr` — then prove, on the same offline fixture, that the persisted series, the per-key contribution basis, and the headline scalars are all finite and mutually consistent, with zero movement on any existing account.

Purpose: Once the persisted per-day return is finite, the equity chart, the per-key contribution table, and the headline CAGR/cumulative become finite and consistent by construction — no display-layer change needed (research §b/§c; Pitfall 1). The fix self-heals live data on re-stitch (full-delete + overwrite at job_worker.py:3460-3475) — NO data migration.
Output: The guard in `nav_twr.py` + registrations; the 92-01 regression promoted from xfail to enforced; metrics-level and worker-level fixture proofs; SC-4/golden/parity byte-identity evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/92-composite-metric-blow-up-annualization-honesty/92-RESEARCH.md
@.planning/phases/92-composite-metric-blow-up-annualization-honesty/92-01-SUMMARY.md
</context>

<interfaces>
<!-- Extracted from the codebase 2026-07-11. Use directly. -->

From analytics-service/services/nav_twr.py:
```python
DUST_NAV_FLOOR = 1000.0      # :58  — the constants block :55-76 is where the new cap lives
FLOW_DOM_RATIO = 1.0         # :65  — naming/tuning precedent (warning-locked default)
NAV_TWR_GUARD_KEYS: tuple[str, ...] = (  # :166-175 — THE one-owner registry; add here
    "dust_nav_guard", "negative_nav_guard", "flow_dominated_guard",
    "flow_coverage_incomplete", "unrealized_pnl_in_anchor",
    "unrealized_pnl_unreadable", "twr_chain_broken",
    "pre_summary_rollout_option_dailies",
)
# chain_linked_twr loop (:377-394):
#   guard_key = _guard_denominator(prev, flow_t)   # :387
#   if guard_key is not None: flags[guard_key]=True; continue  # break, NEVER substitute
#   returns[t] = (cur - prev - flow_t) / prev      # :392  ← the new check wraps this
# _build_nav_meta (:482-509): explicit per-key assignment for mypy — add the new key
# NavTWRMeta TypedDict (total=False guard keys) — add pnl_dominated_guard annotation
```

From analytics-service/services/analytics_runner.py:
```python
class DataQualityFlags(TypedDict, total=False):  # :128-217 — add pnl_dominated_guard: bool
# Lift + promotion registries iterate NAV_TWR_GUARD_KEYS (74-03/SHOULD-1) — no hand edit needed there.
```

From analytics-service/tests/test_stitch_composite_job.py (offline harness, :97-289):
```python
class _FakeSupabase: ...   # :162 — in-memory table stub, no network
# pattern: patch("services.deribit_ingest.build_deribit_native_ledger", ...) returning a NativeLedger
# existing precedent: test_member_guard_meta_promotes_complete_with_warnings (:517)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pnl_dominated_guard at the source (branch per 92-01 evidence; default b1) and promote the repro to an enforced regression</name>
  <files>analytics-service/services/nav_twr.py, analytics-service/services/analytics_runner.py, analytics-service/tests/test_native_nav.py, analytics-service/tests/test_nav_twr.py</files>
  <read_first>
    - .planning/phases/92-composite-metric-blow-up-annualization-honesty/92-01-SUMMARY.md (the "Fix branch selected by fixture evidence:" line — BLOCKING input; this task implements THAT branch)
    - .planning/phases/92-composite-metric-blow-up-annualization-honesty/92-RESEARCH.md (§b fix design, §h Q3 blast radius)
    - analytics-service/services/nav_twr.py (:24-25 leak discipline, :55-76 constants, NavTWRMeta TypedDict + :166-175 NAV_TWR_GUARD_KEYS + docstring, :377-420 loop + _guard_denominator, :482-509 _build_nav_meta)
    - analytics-service/services/analytics_runner.py (:128-217 DataQualityFlags TypedDict; the NAV_TWR_GUARD_KEYS-driven lift loop and consumer_specific_flags promotion predicate from 74-03 — confirm they iterate the tuple)
    - analytics-service/tests/test_nav_twr.py (existing guard-test conventions — mirror the dust/flow-dominated test shapes)
  </read_first>
  <behavior>
    - A day whose |P&L numerator| (cur − prev − flow_t) ≥ PNL_DOM_RATIO × prev is NaN with flags["pnl_dominated_guard"]=True — break-and-flag, never a substituted/clamped value
    - Boundary is inclusive (>=, mirroring FLOW_DOM_RATIO): exactly-at-cap is guarded; flipping >= to > reddens the boundary test (mutation-honest)
    - A below-cap day is byte-identical to today (exact same r, no flag) — the no-op default for every normal account
    - A fired guard promotes computation_status_hint to complete_with_warnings via the existing _build_nav_meta/NAV_TWR_GUARD_KEYS channel
    - The 92-01 fixture regression passes with its xfail marker REMOVED
  </behavior>
  <action>
    FIRST read 92-01-SUMMARY's selected branch. If b2 was selected: do NOT add the guard blindly — implement the valuation fix in `native_nav._value_over_calendar` / `terminal_native_equity` sourcing instead (research §b2), keep every GREEN criterion below identical (the fixture regression must still pass un-xfailed), and record the pivot in the SUMMARY. The remainder of this action describes the DEFAULT b1 branch (per the phase decision).
    In `nav_twr.py`: add `PNL_DOM_RATIO = 10.0` in the guard-constants block (:55-76), next to `FLOW_DOM_RATIO`, with a comment in the FLOW_DOM_RATIO style: founder-tunable, warning-locked default; |pnl| ≥ 10× prior NAV (≥1,000%/day) is beyond any legitimate daily return (the repro emits ≈17×/day; the observed live class is 10–100×/day, research §a) — a no-op for every normal account so SC-4 byte-identity holds (Pitfall 5). In the `chain_linked_twr` loop (:377-394), AFTER the existing `_guard_denominator` pass, compute the day's numerator `pnl_t = cur - prev - flow_t`; if `abs(pnl_t) >= PNL_DOM_RATIO * prev` set `flags["pnl_dominated_guard"] = True` and `continue` — mirroring the existing guard_key break-and-flag block exactly (NaN, NEVER substitute). Do NOT put this inside `_guard_denominator` (it sees only prev/flow — the researched design is call-site wiring, research §b1). Update the `_guard_denominator`/`chain_linked_twr` docstrings to name `pnl_dominated_guard` as the missing sibling of `flow_dominated_guard`.
    Register the key everywhere the one-owner pattern requires: append `"pnl_dominated_guard"` to `NAV_TWR_GUARD_KEYS` (:166-175 — the tuple docstring says every lift/promotion registry iterates it, so this one line propagates status promotion by construction); add the `pnl_dominated_guard` annotation to the NavTWRMeta TypedDict (boolean only — leak discipline per :24-25 / T-73-02 / T-76-03-LEAK: never emit the raw NAV/P&L magnitude in any flag, message, or log); add the explicit assignment in `_build_nav_meta` (:482-509, mirroring the other guard keys); add `pnl_dominated_guard: bool` to the `DataQualityFlags` TypedDict in analytics_runner.py (:128-217) for mypy --strict. Confirm (read, don't assume) that the analytics_runner lift loop and promotion predicate iterate NAV_TWR_GUARD_KEYS — if any registry hand-restates key literals instead, extend it and note the SHOULD-1 violation in the SUMMARY.
    Remove the `@pytest.mark.xfail` decorator from `test_inverse_perpetual_pnl_dominated_day_is_guarded` in test_native_nav.py (it must now pass as an enforced regression) and extend it to also assert `meta` carries `pnl_dominated_guard is True` and `computation_status_hint == "complete_with_warnings"`. Add unit tests in test_nav_twr.py: (i) boundary — |pnl| exactly PNL_DOM_RATIO×prev is guarded (reddens if >= becomes >); (ii) a below-cap day (e.g. |pnl| = 5×prev) passes through with the exact unguarded r and NO flag (byte-identity partner — reddens if the cap is tightened); (iii) the existing `set(NAV_TWR_GUARD_KEYS) <= set(NavTWRMeta.__annotations__)` pin stays green with the new key.
    The SUMMARY MUST state which branch was implemented and cite the 92-01 evidence line (resolved decision 1).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_native_nav.py tests/test_nav_twr.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_native_nav.py -k pnl_dominated -q` → passed (no xfail marker remains: `grep -c "xfail" analytics-service/tests/test_native_nav.py` counts no occurrence tied to the pnl_dominated test — verify via `grep -B3 "def test_inverse_perpetual_pnl_dominated" tests/test_native_nav.py | grep -v '^#' | grep -c xfail` == 0).
    - `grep -c "pnl_dominated_guard" analytics-service/services/nav_twr.py` ≥ 3 (constant-block wiring, NAV_TWR_GUARD_KEYS entry, _build_nav_meta assignment, TypedDict annotation).
    - `grep -c "pnl_dominated_guard" analytics-service/services/analytics_runner.py` ≥ 1 (DataQualityFlags TypedDict).
    - Boundary test proves >= semantics: temporarily flipping the operator to > makes the boundary test fail (mutation check run locally, then reverted — note the run in the SUMMARY).
    - No raw magnitude leak: the diff adds no f-string/log/exception embedding prev/pnl/nav USD values (`git diff -- analytics-service/services/nav_twr.py | grep -E '^\+' | grep -vE 'PNL_DOM_RATIO = |#' | grep -icE 'f".*(prev|pnl|nav)'` == 0).
    - `cd analytics-service && .venv/bin/python -m mypy services/nav_twr.py services/analytics_runner.py` exits 0 (no NEW errors vs HEAD — pre-existing local-venv drift noise excluded per STATE.md).
    - `pytest tests/test_native_nav.py tests/test_nav_twr.py -q` fully green (0 failed, 0 xfailed).
  </acceptance_criteria>
  <done>The guard (or the evidence-selected b2 valuation fix) is live at the source; the repro regression is enforced GREEN; the key is registered in every one-owner registry; leak discipline holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove headline + persisted-series sanity on the fixture and shared-path byte-identity (blast-radius gate)</name>
  <files>analytics-service/tests/test_metrics.py, analytics-service/tests/test_stitch_composite_job.py</files>
  <read_first>
    - .planning/phases/92-composite-metric-blow-up-annualization-honesty/92-RESEARCH.md (§c headline mechanism, §f layers 2-3)
    - analytics-service/tests/test_stitch_composite_job.py (:97-289 — the _FakeSupabase harness + build_deribit_native_ledger patch pattern; :517 member-guard promotion precedent — reuse both verbatim)
    - analytics-service/tests/test_native_nav.py (the _pnl_dominated_blowup_ledger builder from 92-01 — import it, do not duplicate)
    - analytics-service/services/metrics.py (:545 chart fillna, :621 cumulative_twr_segmented, :645-652 CAGR site — what the layer-2 assertions pin)
    - analytics-service/services/job_worker.py (:3340-3395 _metrics_result_for + cash_metrics_json, :3460-3510 persist ordering — what the layer-3 assertions pin)
  </read_first>
  <behavior>
    - Layer 2 (test_metrics.py): the post-guard fixture-shaped series (rising days + one interior NaN at the guarded day) through the REAL compute_all_metrics (geometric, periods_per_year=365) yields finite cumulative_return > 0 and finite cagr > 0 with |cagr| < 10 — the "+0.0% while the curve rises" symptom is dead
    - Layer 3 (test_stitch_composite_job.py): the worker, fed the blow-up NativeLedger through the offline harness, persists ONLY finite csv_daily_returns with every |r| < PNL_DOM_RATIO; the geometric compound Π(1+r)−1 of the persisted rows (the compositeAttribution per-key contribution basis) is < 10 (i.e. < 1,000%, versus the live +1,489,363.8%); data_quality_flags carries pnl_dominated_guard and computation_status is complete_with_warnings
    - Blast radius: every SC-4/golden/parity/#597 pin is green unchanged
  </behavior>
  <action>
    In test_metrics.py add `test_finite_composite_headline_after_pnl_guard`: derive the guarded series by calling the REAL `reconstruct_native_nav_and_twr` on the imported `_pnl_dominated_blowup_ledger()` (post-fix it returns the fixture series with d3 as NaN), then feed it to the REAL `compute_all_metrics(returns, cumulative_method="geometric"/default, periods_per_year=365)` and assert: `cumulative_return` finite and > 0; `cagr` finite and > 0; `abs(cagr) < 10`; sign-consistency — both scalars positive exactly when the retained suffix compounds positive (the twr_chain_broken segmented-suffix contract, metrics.py:621/645-652). This is the "CAGR non-zero when curve rises" must-have.
    In test_stitch_composite_job.py add `test_blowup_member_persists_finite_series_and_plausible_contribution`: reuse `_FakeSupabase` with one member and `patch("services.deribit_ingest.build_deribit_native_ledger", ...)` returning `_pnl_dominated_blowup_ledger()` (imported from tests.test_native_nav); run `run_stitch_composite_job`; assert on the fake's persisted state: (a) every csv_daily_returns `daily_return` is finite (math.isfinite) and `abs(r) < PNL_DOM_RATIO` (import the constant — the assertion tracks the tunable); (b) the geometric compound of the persisted rows Π(1+r)−1 is < 10 — the per-key-contribution plausibility bound (compositeAttribution.ts computes exactly this compound over these rows, research §a); (c) `metrics_json_by_basis["cash_settlement"]` cumulative_return and cagr are finite; (d) `data_quality_flags` contains `pnl_dominated_guard: True` (the NAV_TWR_GUARD_KEYS-driven member lift) and the stamped computation_status is `complete_with_warnings` (mirror the :517 precedent's assertion mechanics). Keep the harness fully offline — no live DB, no creds (research Pitfall 6).
    Blast-radius gate (roadmap Pitfall #12 / SC-4): run the byte-identity + golden + parity + #597 set and require ZERO diffs — `pytest tests/test_nav_twr.py tests/test_native_nav_sc4_identity.py tests/test_golden_parity.py tests/test_metrics_parity.py tests/test_metrics_minigolden.py tests/test_mt5_golden_fixtures.py tests/test_broker_dailies.py tests/test_transforms.py tests/test_metrics.py -q`. These prove flow-less/non-exploding accounts are byte-identical AND the #597 asset-class annualization behavior (crypto √365 risk / calendar-CAGR) is unmoved. Then run the FULL analytics suite once (wave-merge sampling per 92-VALIDATION.md). Use the pinned 3.12 venv (`analytics-service/.venv`) — local Python 3.14 SIGSEGVs on pandas (STATE.md).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_metrics.py tests/test_stitch_composite_job.py tests/test_nav_twr.py tests/test_native_nav_sc4_identity.py tests/test_golden_parity.py tests/test_metrics_parity.py tests/test_metrics_minigolden.py tests/test_mt5_golden_fixtures.py tests/test_broker_dailies.py tests/test_transforms.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_metrics.py -k finite_composite_headline -q` → 1 passed, asserting cumulative_return > 0 AND cagr > 0 AND abs(cagr) < 10 on the guarded fixture series.
    - `pytest tests/test_stitch_composite_job.py -k blowup -q` → 1 passed, asserting all four (a)-(d) behaviors; the compound bound assertion literally computes Π(1+r)−1 over the persisted rows.
    - The blast-radius command above exits 0 with zero failures (SC-4 + golden + parity + #597 unchanged).
    - Full suite: `cd analytics-service && .venv/bin/python -m pytest -q` green in the pinned 3.12 venv (exit 0).
    - The new worker test imports the ledger builder from tests.test_native_nav (no duplicated fixture: `grep -c "_pnl_dominated_blowup_ledger" analytics-service/tests/test_stitch_composite_job.py` ≥ 1).
  </acceptance_criteria>
  <done>All three repro layers (core, metrics, worker-persist) are green on the fixed code; every shared-path pin is byte-identical; the phase's HARD-01 evidence chain (fails pre-fix at 92-01, passes post-fix here) is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Internal analytics compute on already-ingested ledger data; no new untrusted input crosses a boundary; no auth/session/PII surface (ASVS L1 rationale per phase security context). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02 | Information Disclosure | pnl_dominated_guard flag/exception paths | mitigate | Flag is a boolean ONLY; no raw NAV/P&L/USD magnitude in any message, flag value, or log (nav_twr.py:24-25 leak discipline, T-73-02/T-76-03-LEAK precedent); enforced by the Task 1 diff-grep acceptance criterion. |
| T-92-03 | Denial of Service | over-eager guard NaN-ing legitimate accounts | mitigate | PNL_DOM_RATIO=10.0 no-op default (≥1,000%/day) + byte-identity partner test + full SC-4/golden gate — a normal account cannot be flagged/broken. |
| T-92-SC | Tampering | package installs | accept | No new packages installed this plan; supply-chain gate not triggered. |

No high-severity threats introduced.
</threat_model>

<verification>
- Repro gate: 92-01's strict-xfail flips to enforced-passing here — the fixture fails pre-fix (92-01 evidence) and passes post-fix (this plan). Cite both in the SUMMARY.
- Fixture-level, metrics-level, and worker-persist-level assertions all green offline.
- Blast-radius set (SC-4 pins, golden/parity, #597 annualization, transforms/broker dailies) green with zero changes.
- mypy on the two touched service files clean of NEW errors; full analytics suite green in the pinned 3.12 venv.
</verification>

<success_criteria>
- HARD-01 root cause fixed at the source (`nav_twr.py`), not the display layer; the persisted series can no longer carry an un-guarded explosive return.
- Per-key contribution, equity chart, and headline scalars are finite and consistent by construction on the repro fixture.
- Existing single-key strategies and shipped composites are provably unmoved (byte-identity evidence).
- No data migration: the SUMMARY notes the self-heal-on-re-stitch property (job_worker.py:3460-3475) for the live bad factsheet.
</success_criteria>

<output>
Create `.planning/phases/92-composite-metric-blow-up-annualization-honesty/92-02-SUMMARY.md` when done. It MUST include: which fix branch (b1/b2) was implemented and the 92-01 evidence line it followed; the pre-fix-fails / post-fix-passes repro-gate citation; the blast-radius run results.
</output>
