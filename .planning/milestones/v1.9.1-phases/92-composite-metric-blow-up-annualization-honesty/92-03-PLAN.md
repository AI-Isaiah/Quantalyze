---
phase: 92-composite-metric-blow-up-annualization-honesty
plan: 03
type: execute
wave: 3
depends_on: ["92-02"]
files_modified:
  - analytics-service/services/metrics.py
  - analytics-service/services/job_worker.py
  - analytics-service/services/analytics_runner.py
  - analytics-service/tests/test_metrics.py
  - analytics-service/tests/test_analytics_runner.py
  - analytics-service/tests/test_stitch_composite_job.py
  - src/lib/factsheet/composite-read-path.ts
  - src/lib/factsheet/types.ts
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/lib/factsheet/composite-read-path.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx
autonomous: true
requirements: [HARD-04]
must_haves:
  truths:
    - "A composite or single-key annualization window under MIN_ANNUALIZATION_DAYS calendar days carries insufficient_window in data_quality_flags, and the CAGR value is byte-identical to the pre-change computation (flag is annotation ONLY — HARD-04 hard rule)"
    - "The flag never promotes computation_status and never enters metrics_json (it rides a MetricsResult field + data_quality_flags only): a young-but-clean account stays exact-string complete, and no new key leaks into the strategy_analytics column spread or metrics_json_by_basis"
    - "A window at or above the threshold carries NO key, and a stale composite insufficient_window heals (is dropped) on a re-stitch that has grown past the threshold"
    - "insufficient_window is user-visible as a DQ caveat on the existing surfaces: the wizard composite preview's amber Data quality block and the factsheet hero-strip caveat — no new UI component invented"
  artifacts:
    - path: "analytics-service/services/metrics.py"
      provides: "MIN_ANNUALIZATION_DAYS constant + insufficient_window computed at both CAGR sites, returned via a MetricsResult field"
      contains: "MIN_ANNUALIZATION_DAYS"
    - path: "analytics-service/services/job_worker.py"
      provides: "merged_flags lift with drop-stale semantics (mtm_gated_reason mirror)"
      contains: "insufficient_window"
    - path: "analytics-service/services/analytics_runner.py"
      provides: "DataQualityFlags TypedDict key + single-key lift at both compute sites"
      contains: "insufficient_window"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      provides: "insufficient_window line in the existing amber Data quality role=status block"
      contains: "insufficientWindow"
  key_links:
    - from: "analytics-service/services/metrics.py compute_all_metrics"
      to: "MetricsResult.insufficient_window"
      via: "dataclass field set at construction (NEVER a metrics_json key — the dict is spread into the strategy_analytics upsert as columns)"
      pattern: "insufficient_window"
    - from: "analytics-service/services/job_worker.py run_stitch_composite_job"
      to: "strategy_analytics.data_quality_flags"
      via: "merged_flags read-modify-write block (~:3549) reading cash_metrics_result.insufficient_window"
      pattern: "merged_flags\\[.insufficient_window.\\]"
    - from: "strategy_analytics.data_quality_flags.insufficient_window"
      to: "SyncPreviewStep amber Data quality block + FactsheetView hero caveat"
      via: "wizard dq parse (~:770) / composite-read-path dqf parse → buildOpts.dataQuality"
      pattern: "insufficient_window === true"
---

<objective>
Close HARD-04 (#67): a short-lived / flow-heavy annualization window is honestly FLAGGED (`insufficient_window` in `data_quality_flags`) instead of silently over-annualizing CAGR — stamped at the `metrics.py` CAGR site with the underlying value byte-identical, lifted at BOTH callers (composite `merged_flags`, single-key `DataQualityFlags`), and rendered to the user through the EXISTING DQ-caveat surfaces.

Purpose: `metrics.py:629-636` already self-documents this exact un-flagged class ("a genuine 2-day window still annualizes with exponent 365 … NOT yet flagged"). The flag makes over-annualization visible without changing any number (no-invented-data + Pitfall 4).
Output: `MIN_ANNUALIZATION_DAYS` + flag computation in `metrics.py` via a `MetricsResult` field; lifts in `job_worker.py` + `analytics_runner.py`; frontend caveat lines in the wizard preview + factsheet hero strip; regression tests at every layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/92-composite-metric-blow-up-annualization-honesty/92-RESEARCH.md
@DESIGN.md
</context>

<interfaces>
<!-- Extracted from the codebase 2026-07-11. Use directly. -->

From analytics-service/services/metrics.py:
```python
_CALENDAR_DAYS_PER_YEAR = 365.0   # :55 — the new constant lives beside it
@dataclass
class MetricsResult:              # :246 — ADD: insufficient_window: bool = False
    metrics_json: dict[str, Any] = field(default_factory=dict)
    sibling_kinds: dict[str, Any] = field(default_factory=dict)
# Geometric CAGR site (:645-652): _cagr_index = _last_interior_break_suffix(returns).index
#   _elapsed_days = max((_cagr_index[-1] - _cagr_index[0]).days, 1)      # :649
#   cagr = (1.0 + total_return) ** (_CALENDAR_DAYS_PER_YEAR / _elapsed_days) - 1.0
# Simple-branch CAGR site (:589-594): cagr = mean(stat_returns) * periods_per_year
# Self-documented un-flagged class: comment at :629-636.
```

CRITICAL spread hazard (planner-verified — supersedes the research's "metrics_json key" option):
```python
# analytics_runner.py:1925  →  **metrics_result.metrics_json   spread into the strategy_analytics UPSERT
# analytics_runner.py:2373  →  payload.update(metrics_result.metrics_json)
# job_worker.py:3386/:3506  →  cash_metrics_json copied wholesale into metrics_json_by_basis
# job_worker.py:3595        →  headline_payload.update(cash_metrics_json)
# ⇒ a NEW top-level metrics_json key becomes an UNKNOWN COLUMN in the upsert (PostgREST failure)
#   and pollutes metrics_json_by_basis + every full-dict golden comparison.
# ⇒ the flag MUST ride a MetricsResult FIELD, never a metrics_json key.
```

From analytics-service/services/job_worker.py (merged_flags block ~:3546-3565):
```python
merged_flags: dict[str, Any] = dict(existing_flags)   # read-modify-write, preserves prior flags
# drop-stale precedent to mirror:
#   if not mtm_ok and mtm_reason is not None: merged_flags["mtm_gated_reason"] = mtm_reason
#   else: merged_flags.pop("mtm_gated_reason", None)
```

From src/lib/factsheet/composite-read-path.ts (:50-53, :112):
```typescript
dqf: { composite?: unknown; mtm_gated_reason?: unknown; per_key?: unknown; gap_spans?: unknown } | null | undefined
// buildOpts.dataQuality = { composite: true }   ← extend with insufficientWindow
```
From src/lib/factsheet/types.ts (:500): `dataQuality?: { composite: boolean }`
From src/app/factsheet/[id]/v2/FactsheetView.tsx (~:856-870): the NEW-C20-08 hero-strip caveat — an amber `<p>` gated on `m.n < 252`, `style={{ color: "var(--color-warning, #B45309)" }}` — the render pattern to reuse.
From src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx: `CompositePreviewData` (:157-168, has `mtmGatedReason`/`benchmarkUnavailable`); dq parse (~:770-775, e.g. `benchmarkUnavailable: dq.benchmark_unavailable === true`); `hasDqCaveat` (:1125-1128); the amber `role="status"` "Data quality" block (:1309-1330).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp insufficient_window at both CAGR sites via a MetricsResult field — CAGR value byte-identical</name>
  <files>analytics-service/services/metrics.py, analytics-service/tests/test_metrics.py</files>
  <read_first>
    - .planning/phases/92-composite-metric-blow-up-annualization-honesty/92-RESEARCH.md (§d/§e HARD-04 design; Pitfall 4 value-unchanged)
    - analytics-service/services/metrics.py (:55 constants, :246-305 MetricsResult, :589-594 simple CAGR, :621-652 geometric CAGR + the :629-636 self-documenting comment, the final MetricsResult construction site)
    - analytics-service/tests/test_metrics.py (existing compute_all_metrics test conventions — many existing tests use 5-50-day windows; your change must not alter their metrics_json)
  </read_first>
  <behavior>
    - A geometric series whose retained-suffix calendar span is < MIN_ANNUALIZATION_DAYS (or whose _cagr_index has < 2 days) sets result.insufficient_window True; span ≥ threshold sets False (boundary: exactly 90 → False, 89 → True — strict <)
    - The simple branch mirrors using the (NaN-free by contract) returns index calendar span
    - cagr and every other metrics_json value are BYTE-IDENTICAL with the flag logic present vs absent (value-unchanged pin: cagr equals the exact hand formula on a flagged short window)
    - metrics_json NEVER contains an "insufficient_window" key (spread-hazard pin)
  </behavior>
  <action>
    In `metrics.py`: add `MIN_ANNUALIZATION_DAYS = 90` beside `_CALENDAR_DAYS_PER_YEAR` (:55) with a founder-tunable comment (phase decision: conservative default — an annualization window under ~90 calendar days is flagged; DQ annotation ONLY, the CAGR value is never altered; tune like FLOW_DOM_RATIO). Add `insufficient_window: bool = False` as a field on the `MetricsResult` dataclass (:246) with a docstring line explaining WHY it is a field and not a metrics_json key (the spread hazard quoted in <interfaces> — analytics_runner.py:1925/:2373 and job_worker.py:3595 spread metrics_json into the upsert as columns, and :3386/:3506 copy it into metrics_json_by_basis).
    Geometric branch: immediately after the existing CAGR computation (:645-652), derive `insufficient_window = (total_return is None or len(_cagr_index) < 2) or (_elapsed_days < MIN_ANNUALIZATION_DAYS)` reusing the ALREADY-computed `_elapsed_days` (:649) — do NOT touch the `cagr` expression itself. Note the flow-heavy rationale as a comment: a flow-dominated / P&L-dominated window already breaks the chain upstream (flow_dominated_guard / pnl_dominated_guard), which SHORTENS the retained `_cagr_index` suffix — so the elapsed-days rule fires on the trustworthy window and no separate flow trigger is needed at this site (research §d + resolved decision 2). Simple branch (:589-594): mirror with the calendar span of the returns index ((index[-1] − index[0]).days, guarding len < 2) — the simple path is NaN-free by its own fail-loud contract so the full index IS the annualization window. Thread the boolean into the `MetricsResult(...)` construction at the return site. Handle the degenerate early-return path(s), if any construct MetricsResult before the CAGR sites, by leaving the field at its False default or setting True where the window is trivially degenerate — read the actual return sites and keep it consistent.
    Tests in test_metrics.py (write RED first per tdd): (i) 30-day geometric window → `result.insufficient_window is True` AND `result["cagr"] == pytest.approx((1+total)**(365.0/elapsed)-1)` hand-recomputed in the test (the value-unchanged pin — a fix that clamps/zeroes/NaNs cagr on a flagged window reddens this); (ii) 120-day window → False; (iii) boundary pair: exactly-90-day span → False, 89-day span → True; (iv) simple-branch mirror (short allocated-style window → True, long → False, cagr value equal to mean×periods_per_year hand value); (v) spread-hazard pin: `"insufficient_window" not in result.metrics_json` for both flagged and unflagged runs.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_metrics.py -k insufficient_window -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_metrics.py -k insufficient_window -q` → ≥5 passed covering behaviors (i)-(v).
    - `grep -n "MIN_ANNUALIZATION_DAYS" analytics-service/services/metrics.py` shows the constant defined once near :55 and consumed at both branch sites.
    - Value-unchanged proof: the flagged-window test asserts cagr against the hand formula, and `pytest tests/test_metrics.py tests/test_metrics_parity.py tests/test_metrics_minigolden.py tests/test_golden_parity.py tests/test_mt5_golden_fixtures.py -q` is fully green (many existing tests use <90-day windows — zero of them may change, which the field-not-key design guarantees).
    - Spread-hazard pin green: no `insufficient_window` key in metrics_json (`grep -c 'metrics_json\["insufficient_window"\]' analytics-service/services/metrics.py` == 0).
    - `cd analytics-service && .venv/bin/python -m mypy services/metrics.py` exits 0 (no NEW errors vs HEAD).
  </acceptance_criteria>
  <done>The flag is computed at both CAGR sites, rides the MetricsResult field, provably never changes any value, and never leaks into the column spread.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lift the flag at both callers into data_quality_flags — no status promotion, drop-stale on the composite</name>
  <files>analytics-service/services/job_worker.py, analytics-service/services/analytics_runner.py, analytics-service/tests/test_stitch_composite_job.py, analytics-service/tests/test_analytics_runner.py</files>
  <read_first>
    - analytics-service/services/job_worker.py (:3340-3395 cash_metrics_result, :3546-3565 merged_flags block + the mtm_gated_reason drop-stale precedent)
    - analytics-service/services/analytics_runner.py (:128-217 DataQualityFlags TypedDict; the compute sites at :1653 and :2284 and where each assembles data_quality_flags before its upsert; the consumer_specific_flags promotion predicate — which you must NOT touch)
    - analytics-service/tests/test_stitch_composite_job.py (dq-flags merge precedent test_dq_flags_merge_preserves_existing_key :488 — mirror its mechanics)
    - analytics-service/tests/test_analytics_runner.py (single-key DQ lift test conventions)
  </read_first>
  <behavior>
    - Composite: a short-window stitch persists data_quality_flags.insufficient_window == True; a long-window re-stitch DROPS a pre-existing stale insufficient_window (mtm_gated_reason mirror); other existing flags are preserved by the merge
    - Single-key: both runner paths set data_quality_flags["insufficient_window"] = True only when metrics_result.insufficient_window (present-only additive; fresh dict per run needs no drop)
    - computation_status is NEVER promoted/demoted by this flag: a short-window but otherwise-clean run stays exact-string "complete" (SC-4 status invariant — insufficient_window is deliberately NOT in NAV_TWR_GUARD_KEYS and not in the promotion predicate)
  </behavior>
  <action>
    Composite: in `run_stitch_composite_job`'s merged_flags block (job_worker.py ~:3549-3565), after the coverage-mask keys, add: when `cash_metrics_result.insufficient_window` is True → `merged_flags["insufficient_window"] = True`; else → `merged_flags.pop("insufficient_window", None)` — byte-mirroring the mtm_gated_reason drop-stale pattern with a comment naming the heal-on-re-stitch rationale (a composite that grows past the threshold loses the flag on the next stitch). Read the flag from the CASH headline result only (the canonical compute, :3340-3364) — the mtm second pass shares the same window by construction.
    Single-key: in analytics_runner.py add `insufficient_window: bool` to the `DataQualityFlags` TypedDict (:128-217) with a comment: annotation-only DQ flag, NOT a NAV_TWR_GUARD_KEYS member, never promotes computation_status (deliberate — flagging every young account complete_with_warnings would be factsheet-wide blast radius, roadmap Pitfall #12). At BOTH compute-site DQ assemblies (the run_strategy_analytics path around :1653's downstream flag block and the run_csv_strategy_analytics path around :2284's) set `data_quality_flags["insufficient_window"] = True` only when `metrics_result.insufficient_window` — present-only additive, mirroring the existing metrics-derived flag style. Do NOT touch the consumer_specific_flags promotion predicate or NAV_TWR_GUARD_KEYS.
    Tests (RED first): test_stitch_composite_job.py — (i) a short-window composite (reuse/extend an existing _FakeSupabase fixture with a <90-day stitched span) persists `insufficient_window: True` in data_quality_flags AND the stamped computation_status is NOT changed by this flag alone (assert the status the same fixture produced before this plan — read the existing happy-path test's expectation and pin equality); (ii) seed existing_flags with a stale `insufficient_window: True`, run a ≥90-day stitch, assert the key is ABSENT after (drop-stale) while an unrelated seeded key survives (merge preserved). test_analytics_runner.py — a short-window run lifts the flag; a long-window run has no key; status unaffected in both.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py tests/test_analytics_runner.py -k insufficient_window -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_stitch_composite_job.py -k insufficient_window -q` → ≥2 passed (stamp + drop-stale/merge-preserve).
    - `pytest tests/test_analytics_runner.py -k insufficient_window -q` → ≥1 passed (single-key lift).
    - Status invariant pinned: at least one test asserts computation_status equality against the pre-flag expectation on a flagged run (grep the new tests for a computation_status assertion).
    - `grep -c "insufficient_window" analytics-service/services/job_worker.py` ≥ 2 (set + pop) and `grep -c "insufficient_window" analytics-service/services/analytics_runner.py` ≥ 3 (TypedDict + two lift sites).
    - NAV_TWR_GUARD_KEYS untouched: `git diff -- analytics-service/services/nav_twr.py` is empty for this plan.
    - Full analytics suite green: `cd analytics-service && .venv/bin/python -m pytest -q` exit 0 (pinned 3.12 venv).
  </acceptance_criteria>
  <done>The flag flows metrics → both persistence paths with drop-stale healing, provably without touching computation_status.</done>
</task>

<task type="auto">
  <name>Task 3: Render insufficient_window through the existing DQ-caveat surfaces (no new component)</name>
  <files>src/lib/factsheet/composite-read-path.ts, src/lib/factsheet/types.ts, src/app/factsheet/[id]/v2/FactsheetView.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, src/lib/factsheet/composite-read-path.test.ts, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx, src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx</files>
  <read_first>
    - DESIGN.md (mandatory before any visual/UI decision — match existing tokens; do not deviate)
    - src/lib/factsheet/composite-read-path.ts (:46-119 — the dqf input type and buildOpts.dataQuality construction)
    - src/lib/factsheet/types.ts (:495-505 — FactsheetCommon.dataQuality)
    - src/app/factsheet/[id]/v2/FactsheetView.tsx (~:856-870 — the NEW-C20-08 hero-strip short-track caveat `<p>`: the exact className/style pattern to reuse)
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx (:157-168 CompositePreviewData, ~:770-775 dq parse, :1120-1128 hasDqCaveat, :1309-1330 the amber role="status" Data quality block)
    - src/lib/factsheet/composite-read-path.test.ts and src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx (existing pin conventions)
  </read_first>
  <action>
    Per the phase's resolved decision 3: REUSE the existing DQ-caveat renderers; do NOT invent a new UI component.
    (1) composite-read-path.ts: widen the `dqf` input type (:50-53) with `insufficient_window?: unknown`; change `dataQuality: { composite: true }` (:112) to `dataQuality: { composite: true, insufficientWindow: dqf?.insufficient_window === true }`.
    (2) types.ts: extend `FactsheetCommon.dataQuality` (:500) to `{ composite: boolean; insufficientWindow?: boolean }` (optional — pre-existing cached payloads lack it; readers must treat absent as false).
    (3) FactsheetView.tsx: in the hero-strip caveat area (the NEW-C20-08 `m.n < 252` amber `<p>` at ~:856-870), add a SIBLING `<p>` with the identical className/style pattern (`px-3 sm:px-4 py-2 text-micro font-mono`, borderTop token, `var(--color-warning, #B45309)`), rendered when `payload.dataQuality?.insufficientWindow === true`, copy: "⚠ Track record under 90 days — annualized metrics are flagged as computed on an insufficient window." Keep the existing n<252 caveat untouched (it is a client-count heuristic; this new line is the server-truth flag).
    (4) SyncPreviewStep.tsx: add `insufficientWindow: boolean` to `CompositePreviewData` (:157-168); in the dq parse (~:770-775) add `insufficientWindow: dq.insufficient_window === true` (mirror the `benchmark_unavailable` line); extend `hasDqCaveat` (:1125-1128) to OR in `composite.insufficientWindow`; inside the existing amber `role="status"` "Data quality" block (:1309-1330) add a `<p>` (sibling of the mtm/benchmark lines): "Short track record — annualized metrics are computed on an insufficient window and may not be meaningful."
    Vitest pins (extend the existing files, matching their conventions): composite-read-path.test.ts — dqf with `insufficient_window: true` → `buildOpts.dataQuality.insufficientWindow === true`; absent/malformed (e.g. string "true") → false. SyncPreviewStep.composite.render.test.tsx — flag true → the amber block renders the new line; flag false with no other caveat → the Data quality block absent (hasDqCaveat gating intact). FactsheetView.kpistrip.test.tsx — payload with `dataQuality: { composite: true, insufficientWindow: true }` → the server-truth caveat text renders; absent → not rendered.
  </action>
  <verify>
    <automated>npx vitest run src/lib/factsheet/composite-read-path.test.ts "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx" "src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - The three vitest files pass with the new pins (≥3 new tests total: parse true/false, wizard render, factsheet render).
    - `grep -c "insufficientWindow" src/lib/factsheet/composite-read-path.ts` ≥ 1 with a strict `=== true` coercion (malformed dqf can never render the caveat).
    - `grep -c "insufficientWindow" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"` ≥ 3 (interface, parse, render/gate).
    - No new component file created (`git status --porcelain src/components src/app | grep -c "^??.*\.tsx$"` == 0 for this task's diff).
    - `npm run lint` exits 0 and `npx tsc --noEmit` exits 0.
    - Full frontend suite unaffected: `npm test` (or the repo's vitest run script) green.
  </acceptance_criteria>
  <done>insufficient_window is user-visible on both existing DQ surfaces with server-truth strict parsing; no new UI primitives; lint/tsc/vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client (data_quality_flags JSON) | Existing boundary, already crossed by mtm_gated_reason/gap_spans; the new key is a boolean read with strict `=== true` coercion — no string interpolation of untrusted content into the DOM beyond fixed copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04 | Information Disclosure | insufficient_window flag | mitigate | Boolean only — never the elapsed-days count of a private account nor any raw magnitude (leak-discipline precedent T-73-02/T-76-03-LEAK); fixed caveat copy, no interpolated values. |
| T-92-05 | Tampering | malformed dqf JSON from DB | mitigate | Strict `dqf?.insufficient_window === true` coercion in both parsers (a string/object value renders nothing) — pinned by the malformed-input vitest. |
| T-92-06 | Denial of Service | unknown-column upsert failure | mitigate | The flag rides a MetricsResult FIELD + data_quality_flags JSONB, never the metrics_json column spread (planner-verified hazard; pinned by the Task 1 spread-hazard test). |
| T-92-SC | Tampering | package installs | accept | No new packages installed this plan; supply-chain gate not triggered. |

No high-severity threats introduced (read-only DQ chip; no new untrusted input path, auth surface, or PII — ASVS L1 rationale per phase security context).
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_metrics.py tests/test_stitch_composite_job.py tests/test_analytics_runner.py -q` green, then the full analytics suite green (pinned 3.12 venv).
- Golden/parity set green unchanged (the field-not-key design means zero metrics_json movement).
- Frontend: the three vitest pins green; `npm run lint` + `npx tsc --noEmit` clean; full vitest suite green.
- HARD-04 evidence for the phase record: a flagged short-window run shows insufficient_window in persisted data_quality_flags with byte-identical CAGR, and the caveat renders on the wizard preview + factsheet.
</verification>

<success_criteria>
- HARD-04 closed: short windows are flagged, never silently over-annualized; the CAGR value is untouched (#67).
- The flag is server-truth end to end: metrics site → MetricsResult field → both persistence paths (with composite drop-stale healing) → existing DQ-caveat surfaces.
- Zero blast radius: no status promotion, no metrics_json/column changes, all golden/parity/SC-4 pins green.
</success_criteria>

<output>
Create `.planning/phases/92-composite-metric-blow-up-annualization-honesty/92-03-SUMMARY.md` when done. It MUST cite: the value-unchanged pin, the status-invariant pin, and the two user-visible render surfaces.
</output>
