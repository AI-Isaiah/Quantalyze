---
milestone: v0.16.0.0
milestone_name: Phase 11 — Onboarding & Security Readiness
audited: 2026-04-27
status: passed
scores:
  requirements: 6/6 (ONBOARD-01..06)
  phases: 1/1
  integration: n/a (single-phase milestone)
  flows: 5/5 (success criteria)
phase_results:
  - phase: "11-onboarding-and-security-readiness"
    status: passed
    score:
      goal_achieved: true
      plans_complete: 7/7
      success_criteria: 5/5
      truths_verified: 22/22
    requirements: ["ONBOARD-01", "ONBOARD-02", "ONBOARD-03", "ONBOARD-04", "ONBOARD-05", "ONBOARD-06"]
    human_needed: []  # all 4 items resolved via UAT-AUDIT-2026-04-27.md
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: "11-onboarding-and-security-readiness"
    items:
      - "Resolved 2026-04-27 (commit 841da8a): WR-02 maybeEmitFirstBridgeSurfaced race retired via migration 085 stamp_first_bridge_surfaced SECURITY DEFINER RPC."
      - "IN-02 WidgetState partial pill bg-warning/5 contrast — design-token decision needs explicit user/design approval. Pill border (border border-warning) provides full-strength delineation; deferred to system-wide design-token revision."
      - "S4b inline egress IPs on /security — deferred pending static-IP infrastructure provisioning (Vercel Pro static-IP or Railway egress-allowlist). Email path preserved as canonical IP-disclosure mechanism."
      - "Long-tail 32 WIDGET_REGISTRY widgets — universal <WidgetState> primitive coverage shipped via widget_state_v2 flag (default OFF). Per-state Vitest fixtures for the 32 widgets outside DEFAULT_LAYOUT + Performance + Scenario deferred to Phase 11+1 backlog."
deferred_post_merge:
  - "BLOCK-3 E2E full funnel activation — user-action item: provision test Supabase project + 3 GitHub secrets + 1 repo variable. CI gate uses vars.E2E_TEST_DB_CONFIGURED == 'true' so dormant state is intentional and safe; the gated step silently skips until the variable is added. RISK-2 always-on smoke spec runs on every PR including forks today (3/3 PASS in 15.5s)."
  - "Production smoke on https://quantalyze-rho.vercel.app/security — runs after /ship merges to origin/main; verifies the public, indexable security disclosure surface is not auth-gated. Manual probe or /canary."
  - "PostHog dashboard ingest verification — observable only post-merge in production with real fresh-allocator traffic. Migration 084 trigger + RPC LIVE; helpers + reader paths in production code; content-hash dedupe contract proven via deterministic stamping (WR-02 mitigation, now retired by migration 085 + commit 841da8a)."
nyquist:
  compliant_phases: 1
  partial_phases: 0
  missing_phases: 0
  overall: "Phase 11 has 11-VALIDATION.md on disk; nyquist scaffold present and verified."
---

# v0.16.0.0 — Phase 11 Onboarding & Security Readiness — Milestone Audit

**Audited:** 2026-04-27
**Status:** ✓ PASSED
**Phases in scope:** 11

## Summary

v0.16.0.0 is a single-phase minor-version release that ships Phase 11 (Onboarding + Security Readiness). Phase 11 was spun out of v0.15.0.0 mid-sprint because the onboarding/security work landed independently while v0.15.x had already been shipping incrementally on `main`.

All 6 ONBOARD-XX requirements are satisfied with on-disk evidence in `11-VERIFICATION.md` (5/5 success criteria verified, 22/22 observable truths verified, 7/7 plans complete). The phase ships:

- **OnboardingBanner (S1)** — Connect Exchange nudge above `/allocations` tabs, gated server-side on `apiKeysCount === 0`, sessionStorage dismiss with re-surface until first key connects.
- **MandateQuickSetCard (S2)** — empty input + "Suggested: 15%" helper + Save-disabled-until-typed; Phase 02 D-09 LOCKED honored.
- **`/security` page surfaces** — SOC-2 status banner (S4a), audit-log link (S4c), WithdrawalWarningStrip on every wizard step (S5), WizardIpAllowlistHint persistent (S7), AuditLogSubsection with Download CSV last 90 days (S6).
- **GET /api/me/audit-log/export route** — RFC 4180 + WR-01 formula injection neutralization, RLS isolation test, 36KB CSV with JSON metadata properly quoted.
- **WidgetState 5-mode primitive** — loading/empty/partial/error/success states wired into all 7 DEFAULT_LAYOUT widgets behind `widget_state_v2` feature flag.
- **PostHog onboarding funnel** — 5 single-fire events (`signup` → `first_api_key_added` → `first_sync_success` → `first_bridge_surfaced` → `first_outcome_recorded`) via `auth.users.raw_user_meta_data` markers.
- **Migration 084** — `first_api_key_added_at` AFTER INSERT trigger on `api_keys` + `stamp_first_sync_success` SECURITY DEFINER RPC for Python worker. Self-verifying DO block.
- **Migration 085 (added 2026-04-27)** — `stamp_first_bridge_surfaced` SECURITY DEFINER RPC retiring the WR-02 race via atomic Postgres-level stamping. Replaces deterministic-fallback mitigation.
- **Playwright E2E** — `e2e/onboarding-funnel.spec.ts` (full happy path with 5-marker assertion, BLOCK-3 gated on `vars.E2E_TEST_DB_CONFIGURED`) + `e2e/onboarding-banner-smoke.spec.ts` (RISK-2 always-on, fork-PR safe).

The 2026-04-27 milestone-wrap QA report (`.gstack/qa-reports/qa-report-quantalyze-v0.15-v0.16-milestone-wrap-2026-04-27.md`) explicitly cleared all Phase 11 surfaces (OnboardingBanner gating, MandateQuickSetCard gating, /security S4a + S4c, wizard S5 + S7, /profile?tab=security S6, GET /api/me/audit-log/export 200 + text/csv + 36KB + RFC 4180 + JSON metadata properly quoted).

## Phase Roll-Up

| Phase | Status | Score | REQ Coverage | Notes |
|-------|--------|-------|--------------|-------|
| 11 Onboarding + Security Readiness | ✓ passed | 5/5 SCs, 22/22 truths, 7/7 plans | ONBOARD-01..06 | WR-02 retired today via migration 085 + 841da8a; 4 human-verification items resolved (2 by 2026-04-27 QA report, 2 deferred-with-rationale to post-merge probes). |

## Requirements Coverage

| REQ-ID | Plan | Description | Status | Evidence |
|--------|------|-------------|--------|----------|
| ONBOARD-01 | 11-05 | OnboardingBanner gates on `apiKeysCount === 0`, sessionStorage dismiss + re-surface | satisfied | `AllocationsTabs.tsx`: `showOnboardingBanner = props.apiKeysCount === 0`; 9/9 banner tests green. |
| ONBOARD-02 | 11-05 | MandateQuickSetCard gates on `apiKeysCount === 0 && !mandateIsSet` | satisfied | `AllocationsTabs.tsx`: `props.apiKeysCount === 0 && !props.mandateIsSet`; 16/16 card tests green; 4/4 onboarding integration tests green. |
| ONBOARD-03 | 11-02 + 11-06 | `/security` SOC-2 banner + audit-log link + audit-log CSV export | satisfied | `/security` page surfaces (S4a + S4c) + ProfileTabs Security tab S6 AuditLogSubsection + GET /api/me/audit-log/export 200/text/csv/36KB/RFC4180. |
| ONBOARD-04 | 11-04 | WidgetState 5-mode primitive + 7-widget × 5-state matrix | satisfied | `<WidgetState>` consumed by all 7 DEFAULT_LAYOUT widgets behind `isWidgetStateV2Enabled()` flag; 35-mode matrix test green. |
| ONBOARD-05 | 11-01 + 11-03 | PostHog onboarding funnel events with single-fire markers | satisfied | Migration 084 trigger + stamp_first_sync_success RPC LIVE; 5 helpers in onboarding-funnel.ts; /allocations page.tsx readers in Promise.allSettled; Python worker calls stamp_first_sync_success; scenario-commit + match-decisions/holding stamp first_outcome_at. WR-02 retired in migration 085 (2026-04-27). |
| ONBOARD-06 | 11-07 | Playwright E2E in CI (always-on smoke + gated full funnel) | satisfied | `e2e/onboarding-banner-smoke.spec.ts` (RISK-2 always-on, fork-PR safe, 3/3 PASS in 15.5s) + `e2e/onboarding-funnel.spec.ts` (gated, 5-marker assertion via auth.users.raw_user_meta_data); ci.yml has BLOCK-3 gated step on `vars.E2E_TEST_DB_CONFIGURED == 'true'` (vars.X pattern, NOT secrets.X != ''). |

**Total:** 6/6 milestone REQ-IDs satisfied. No orphaned requirements. No `gaps_found` status.

## Cross-Phase Integration

Phase 11 is a single-phase milestone. Its dependencies are upstream — Phase 10 (E2E covers signup → API key → Performance → Scenario → commit → outcome flow) and Phase 06 (api_keys INSERT trigger fires `first_api_key_added_at`). Both upstream phases are part of v0.15.0.0, audited separately and shipped on `main`.

Cross-phase wiring verified:

- **F-1** Phase 06 api_keys INSERT → Phase 11 first_api_key_added_at trigger ✓ (migration 084 self-verifying DO block + 4 INSERT path coverage in trigger function comment)
- **F-2** Phase 09 first_bridge_surfaced read at /allocations page.tsx → Phase 11 maybeEmitFirstBridgeSurfaced helper ✓ (call site at `src/app/(dashboard)/allocations/page.tsx:55` unchanged across migration 085 retrofit)
- **F-3** Phase 10 scenario-commit → Phase 11 first_outcome_at marker ✓ (scenario-commit emitter in 11-03 plan)
- **F-4** /api/match/decisions/holding → Phase 11 first_outcome_at marker ✓ (match-decisions/holding emitter in 11-03 plan)
- **F-5** Phase 06 + 08 + 09 + 09.1 + 10 surfaces all render correctly under WidgetState 5-mode primitive ✓ (7 DEFAULT_LAYOUT widgets wired behind `widget_state_v2` flag; 35-mode matrix test green)

## Tech Debt (Non-Blocking)

All items already documented in `11-VERIFICATION.md` frontmatter. None block milestone completion.

- **WR-02** (race in `maybeEmitFirstBridgeSurfaced`) — **resolved 2026-04-27** via migration 085 + helper refactor (commit `841da8a`). Replaces deterministic-fallback mitigation with atomic Postgres-level stamping.
- **IN-02** (WidgetState partial pill `bg-warning/5` contrast) — design-token decision needs explicit user/design approval. Pill border (`border border-warning`) provides full-strength delineation. Deferred to system-wide design-token revision.
- **S4b** (inline egress IPs on /security) — deferred pending static-IP infrastructure provisioning. Email path preserved as canonical IP-disclosure mechanism.
- **Long-tail 32 WIDGET_REGISTRY widgets** — universal `<WidgetState>` primitive coverage shipped via `widget_state_v2` flag (default OFF). Per-state Vitest fixtures for the 32 widgets outside DEFAULT_LAYOUT + Performance + Scenario deferred to Phase 11+1 backlog.

## Deferred Post-Merge Probes

Per Phase 11 verification frontmatter:

1. **BLOCK-3 E2E full funnel activation** — user-action item; provision test Supabase project + 3 GitHub secrets + 1 repo variable post-merge.
2. **Production smoke on `https://quantalyze-rho.vercel.app/security`** — runs after `/ship` via /canary or manual probe.
3. **PostHog dashboard ingest verification** — observable only post-merge in production with real fresh-allocator traffic.

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | Action |
|-------|---------------|-------------------|--------|
| 11 | exists | true | none |

## Verdict

**v0.16.0.0 (Phase 11) — PASSED. Ready for `/gsd-complete-milestone v0.16.0.0`.**

All 6 milestone REQ-IDs satisfied. Cross-phase wiring confirmed. Single in-flight tech-debt item (WR-02) retired today. Three deferred items are post-merge probes (production smoke, PostHog ingest, BLOCK-3 GitHub secrets) — none block ship.

Next step: `/gsd-complete-milestone v0.16.0.0`.
