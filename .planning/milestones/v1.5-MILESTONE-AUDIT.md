---
milestone: v1.5
milestone_name: Scenario Coverage-Window Blend
audited: 2026-07-03
status: passed
scores:
  requirements: 28/28 satisfied
  phases: 7/7 complete (verified)
  integration: clean (21/21 REQ rows WIRED; 6/6 seams; 0 orphaned exports)
  flows: 5/5 verified (4 live on authed prod, 1 unit/e2e-only by honest clamp)
gaps: {}  # none — FAIL gate not triggered
nyquist:
  compliant_phases: [58, 59]
  partial_phases: [55]   # VALIDATION.md exists, nyquist_compliant:false — pre-execution contract never re-stamped; compensating evidence: BLEND-07 numpy gate + 7,408-test green suite
  missing_phases: [56, 57, 60, 61]
  overall: partial  # discovery-only; see note below
tech_debt:
  - phase: cross-cutting (integration check)
    items:
      - "WARNING: type-unsafe payload cast at the compare seam — AllocationsTabs.tsx:964 `props as unknown as ScenarioComparePanelProps['payload']`; runtime-proven but a future SSR-payload rename breaks PERSIST-03 silently at compile time. Suggested: derive the panel payload type from the SSR payload type."
      - "WARNING (semantics footnote): union-path member_count = active-set size, not window membership (documented LR-02, scenario.ts:177-181, locked decision) — future consumers must not read it as window membership on own-book callers."
  - phase: 58-coverage-legibility-disclosure
    items:
      - "INFO: from-book gantt rows label as raw 'key <uuid>' (P61 B1) — functional, unfriendly; presentation polish."
  - phase: 60-golden-e2e-rebake
    items:
      - "Deferred: a NEW scenario-blend screenshot golden (new coverage, not a re-bake) — noted in 60-VERIFY-01-EVIDENCE.md."
  - phase: 61-authed-prod-canary
    items:
      - "A5 empty-intersection banner unreachable on prod data (manual dates clamp to data domain) — remains unit/e2e-covered only; documented."
      - "Red-team F3 (PR #570): mixed keys+added shares render added legs renormalized to 100% — add share-page caption. FOLDED into .planning/v1.6-SERIES-SPACE-INPUT.md R4."
      - "Red-team F5 (PR #570, pre-existing): compare merges live book into blank-authored drafts (entryMode ephemeral). FOLDED into v1.6 input R2 (explicit series membership)."
---

# Milestone v1.5 — Scenario Coverage-Window Blend — Audit

**Verdict: PASSED** — 28/28 requirements satisfied, integration clean, no critical
gaps. Accumulated non-critical debt is small and already routed (most items folded
into `.planning/v1.6-SERIES-SPACE-INPUT.md`).

## Definition of done (ROADMAP)

The frozen scenario engine's union/0-fill/every-started-divides convention is
replaced by coverage-window membership (member iff enabled AND span ⊇ window,
constant divisor, renormalized weights), legible in the UI, durable across
save/share/compare, safety nets re-baked, and verified live on authed prod. **Met.**

## Phase verifications (7/7)

| Phase | VERIFICATION status | Disposition |
|-------|--------------------|-------------|
| 55 compute core | passed | 4/4 plans; BLEND-07 numpy parity gate green |
| 56 factsheet parity | passed | PARITY-01 structural guard |
| 57 window control | passed | 3/3 plans |
| 58 coverage legibility | human_needed → **closed** | 2 visual items PASSED live in P61 (B1/B2); addendum 2026-07-03 |
| 59 persisted windows | human_needed → **closed** | 4 authed items: B3 pass; B4-B6 failed → P61-BUG-2 → FIXED PR #570 → re-verified live; addendum 2026-07-03 |
| 60 golden re-bake | passed (✅ VERIFY-01) | No-op bake PROVEN (evidence file); e2e net restored unconditional |
| 61 authed canary | passed (+ addendum) | Core A1-A6 pass; B1-B6 all closed; found bugs fixed + prod-re-verified |

No unverified phases.

## Requirements coverage (3-source cross-reference)

- **Traceability table**: 28/28 rows `Complete`, all checkboxes `[x]`.
- **SUMMARY frontmatter**: 19 REQ-IDs listed across 55-03/04, 56-01, 57-01..03,
  58-01..03, 59-02/03, 60-01, 61-01. Not listed (matrix → "partial, verify
  manually"): BLEND-01..06, PARITY-03 (55-01/02 predate the frontmatter
  convention), WINDOW/PERSIST umbrella rows already covered by sibling plans,
  PERSIST-01 (59-01 lists none — schema plan).
- **Manual verification of the partials**: the integration checker independently
  re-verified every one of these against live source with file:line evidence
  (see Requirements Integration Map below) — BLEND-01..05 engine coverage path,
  BLEND-06 field consumers, PARITY-03 frozen-file zero-diff through #570,
  PERSIST-01 full round-trip. All **satisfied**.
- **Orphans**: none — every REQ-ID appears in ≥1 phase VERIFICATION.

Final: **28/28 satisfied. FAIL gate not triggered.**

## Integration check (gsd-integration-checker, 2026-07-03)

All six seams WIRED with file:line evidence (full report retained in the agent
transcript; key results):

1. Engine → consumers: BlendHeader/chips/timeline/compare-tfoot READ
   member_count/effective window from ComputedMetrics — no re-derivation.
2. Window derivation single-source: composer, share-resolve, compare all derive
   the intersection default via the same `coverageSpanOf → defaultWindowFor`
   chain (PERSIST-02 lock honored).
3. Draft window round-trip: save → JSONB → reopen/share/compare, all three
   decoders through the codec (v2→v3 `upgraded_v2_windowless` honored; no bare casts).
4. Post-#570: `mergeAddedIntoPerKeySet` at BOTH call sites; panel threads the
   per-key channel; 409 mint gate + book-only honest-absence wired end-to-end.
5. E2E flows: 5/5 complete (4 live-proven on prod; WINDOW-06 banner unit/e2e-only —
   unreachable on prod data by honest clamping, not a gap).
6. Frozen-engine discipline: scenario.ts/scenario-window.ts last touched by #565
   (the engine PR itself); scenario-dealias.ts pre-v1.5; zero drift in #566/#567/#570.

0 critical gaps; 3 non-critical items recorded under tech_debt.

## Nyquist note

Discovery-only scan: 58/59 compliant; 55 has a pre-execution VALIDATION contract
never re-stamped (`nyquist_compliant: false`) — its demanded gates (BLEND-07
independent numpy verification, boundary tests) demonstrably exist and pass;
56/57/60/61 have no VALIDATION.md (56/60 are evidence/test phases; 57/61 predate
per-phase validation stamping this milestone). No action required for close;
`/gsd:validate-phase` available retroactively if wanted.

## Ship record

- #565 v0.35.0.26 (P55-57) · #566 v0.35.0.27 (P58-59) · #567 v0.35.0.28 (P60)
- #568 v0.35.0.29 + #569 v0.35.0.30 (CI-speed, adjacent to milestone)
- #570 v0.35.0.31 (P61 found-bug fixes) — all squash-merged, main CI green
  first-try each time, Vercel prod verified at each SHA.
