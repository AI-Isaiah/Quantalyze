---
phase: 84-blend-allocation-asset-class-annualization
plan: 03
type: execute
wave: 2
depends_on: [84-01, 84-02]
files_modified:
  - src/lib/queries.ts
  - src/app/(dashboard)/allocations/widgets/attribution/AlphaBetaDecomposition.tsx
  - src/lib/factsheet/allocator-portfolio-payload.ts
  - src/lib/factsheet/allocator-portfolio-payload.test.ts
  - src/lib/factsheet/allocator.ts
  - src/lib/factsheet/allocator.test.ts
  - src/lib/factsheet/build-payload.ts
autonomous: true
requirements: [BLEND-02]
must_haves:
  truths:
    - "The actual-allocation live-baseline Sharpe (queries.ts per-key blend) annualizes on the blend rule — all legs are exchange keys (crypto) → √365; twr/maxDd/avgRho/equity byte-identical"
    - "The allocator portfolio factsheet (buildAllocatorPortfolioFactsheetPayload) annualizes risk metrics on √365 via assetClass 'crypto'; CAGR unchanged (calendar clock)"
    - "AlphaBetaDecomposition's alpha annualizes ×365 (live crypto book); beta unchanged"
    - "allocator.ts reference panels: 60/40 (SPX/IEF) stays √252 BYTE-IDENTICAL; the BTC-legged all-weather and BTC/ETH blends move to √365 (locked ruling: the reference blend's LEGS decide the basis)"
    - "MyAllocationDashboardPayload strategies[].strategy carries asset_class from the SSR select (the channel the wave-3 composer lookup reads)"
  artifacts:
    - path: "src/lib/queries.ts"
      provides: "asset_class on the dashboard strategy payload + blend-basis threading at the liveBaseline computeScenario call"
      contains: "blendPeriodsPerYear"
    - path: "src/lib/factsheet/allocator.ts"
      provides: "buildAllocatorMetrics(rets, mmRets, periodsPerYear = 252)"
      contains: "periodsPerYear"
    - path: "src/lib/factsheet/allocator-portfolio-payload.ts"
      provides: "assetClass 'crypto' on the portfolio factsheet metadata"
      contains: "assetClass"
  key_links:
    - from: "src/lib/queries.ts"
      to: "src/lib/closed-sets.ts blendPeriodsPerYear"
      via: "computeScenario 4th arg at the live-baseline call"
      pattern: "computeScenario\\(strategies, state, cache, blendPeriodsPerYear"
    - from: "src/lib/factsheet/build-payload.ts"
      to: "src/lib/factsheet/allocator.ts buildAllocatorMetrics"
      via: "per-reference-blend basis (252 / 365 / 365)"
      pattern: "buildAllocatorMetrics\\("
---

<objective>
Make the ACTUAL allocation/portfolio KPI surfaces asset-class-aware (84-CONTEXT.md in-scope
items: allocator-portfolio-payload, allocator.ts reference panels, computeAlphaBeta consumer
AlphaBetaDecomposition, and the queries.ts:2169 live-baseline computeScenario call), and open
the SSR asset_class channel the wave-3 composer lookup consumes.

Purpose: the allocator's real book is a blend of live exchange-key legs — every supported
venue is crypto, so under the locked blend rule the live book annualizes √365. The 60/40
pure-tradfi reference panel is THE 252 case that must stay byte-identical.
Output: threaded call sites + widened dashboard payload + default-252/365 regression pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-blend-allocation-asset-class-annualization-597-part-2-scenar/84-CONTEXT.md
@src/lib/factsheet/build-payload.ts

<interfaces>
From src/lib/closed-sets.ts (created in 84-01):

```typescript
export function blendPeriodsPerYear(legs: ReadonlyArray<{ asset_class?: string | null }>): number; // any 'crypto' → 365, else 252
export function annualizationPeriods(assetClass: string | null | undefined): number; // 'crypto' → 365
```

From src/lib/portfolio-stats.ts (parameterized in 84-02):

```typescript
export function computeAlphaBeta(returns: number[], benchmark: number[], periodsPerYear = 252): AlphaBetaResult;
```

From src/lib/factsheet/build-payload.ts (accepts assetClass since #597):

```typescript
buildFactsheetPayload(strategy: { /* ... */ assetClass?: string | null; /* ... */ }, dailyReturns, ...);
// :141  const periodsPerYear = annualizationPeriods(strategy.assetClass);
```

Reference-panel call sites (build-payload.ts:283-295, the locked 252/365 split):
- `buildAllocatorMetrics(blend([0.6,0.4],[spxRet,iefRet]), stratRet)` → pure tradfi → 252 (default, DO NOT pass an arg — byte-identity)
- `buildAllocatorMetrics(blend([.25,.25,.25,.25],[spxRet,gldRet,iefRet,btcRet]), stratRet)` → BTC leg → 365
- `buildAllocatorMetrics(blend([0.7,0.3],[btcRet,ethRet]), stratRet)` → BTC/ETH → 365
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: queries.ts — asset_class payload channel + live-baseline blend basis</name>
  <files>src/lib/queries.ts</files>
  <read_first>src/lib/queries.ts:1495-1553 (MyAllocationDashboardPayload strategies[].strategy shape), the getMyAllocationDashboard SSR select that projects strategy name/codename/disclosure_tier/strategy_types (grep `organization_name` or `disclosure_tier` inside queries.ts to find it), src/lib/queries.ts:2095-2198 (liveBaselineMetricsFromPerKeyDailies — the per-key unit literal at :2137-2150 and the computeScenario call at :2169), src/app/(dashboard)/allocations/lib/scenario-adapter.ts (the 84-01 per-key asset_class:"crypto" comment to mirror)</read_first>
  <behavior>
    - MyAllocationDashboardPayload strategies[].strategy type carries asset_class?: string | null and the SSR select projects it
    - liveBaselineMetricsFromPerKeyDailies: per-key unit literals carry asset_class "crypto"; computeScenario receives blendPeriodsPerYear over the selected units (all crypto → 365)
    - Value change confined to sharpe: for a fixed per-key fixture, sharpe equals the old value × √(365/252); ytdTwr, maxDd, avgRho, equity, drawdown BYTE-IDENTICAL (basis-invariant)
  </behavior>
  <action>
    (a) Widen the payload type: strategies[].strategy gains `asset_class?: string | null` with a
    JSDoc noting it feeds the composer's addedStrategyMetadataLookup blend-basis derivation
    (#597 part 2); add `asset_class` to the corresponding strategy column list in the
    getMyAllocationDashboard select (find the exact select via grep — do NOT touch any other select).
    (b) liveBaselineMetricsFromPerKeyDailies: add `asset_class: "crypto"` to the per-key
    StrategyForBuilder literal (same rationale comment as the 84-01 adapter edit: id === api_key_id,
    every SUPPORTED_EXCHANGE is crypto), import blendPeriodsPerYear from "@/lib/closed-sets", and
    change :2169 to `computeScenario(strategies, state, cache, blendPeriodsPerYear(strategies))`
    (every unit in this helper is selected=true by construction — comment that).
    (c) Add/extend a unit test for liveBaselineMetricsFromPerKeyDailies (it is exported for testing —
    check existing queries tests via grep before creating a new test file; if a new file is needed,
    add it and list it in the SUMMARY as a deviation) pinning: sharpe scales √(365/252) vs a
    hand-computed 252-basis value; twr/maxDd/avgRho byte-identical to the pre-change values.
    Pin sharpe/twr/maxDd/avgRho FIELD-WISE — do NOT whole-object deep-equal and do NOT pin `cagr`:
    liveBaseline flows through scenario.ts computeScenario, whose CAGR clock 84-06 (wave 3) converts
    to calendar-span this same phase, so a cagr assertion here would go RED once 84-06 lands.
    Do NOT touch any other computeScenario call in queries.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit && _TF=$(grep -rln "liveBaselineMetricsFromPerKeyDailies" src --include="*.test.ts" | head -1); test -n "$_TF" && npx vitest run "$_TF" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "blendPeriodsPerYear" src/lib/queries.ts ≥ 2 (import + call site)
    - The getMyAllocationDashboard strategy select string contains asset_class (grep the select block)
    - A test proves the live-baseline sharpe moved to the 365 basis AND twr/maxDd/avgRho did not move
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>The actual-allocation baseline Sharpe rides √365; every basis-invariant output pinned unchanged; the asset_class SSR channel exists for wave 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attribution widget + allocator portfolio factsheet basis</name>
  <files>src/app/(dashboard)/allocations/widgets/attribution/AlphaBetaDecomposition.tsx, src/lib/factsheet/allocator-portfolio-payload.ts, src/lib/factsheet/allocator-portfolio-payload.test.ts</files>
  <read_first>src/app/(dashboard)/allocations/widgets/attribution/AlphaBetaDecomposition.tsx (the computeAlphaBeta call at :90 and where portfolioReturns comes from — confirm it is the allocator's live book series), src/lib/factsheet/allocator-portfolio-payload.ts (the buildFactsheetPayload metadata literal — the FINDING-6 ingestSource comment style to mirror), src/lib/factsheet/allocator-portfolio-payload.test.ts (existing pins), src/lib/factsheet/build-payload.ts:141 (annualizationPeriods consumption of assetClass)</read_first>
  <behavior>
    - buildAllocatorPortfolioFactsheetPayload output: ann_vol/sharpe/sortino on the 365 basis (scale √/×(365/252) vs before); CAGR byte-identical (calendar clock, invariant)
    - AlphaBetaDecomposition: alpha rendered ×365; beta identical to before
  </behavior>
  <action>
    (a) allocator-portfolio-payload.ts: add `assetClass: "crypto"` to the buildFactsheetPayload
    metadata literal with a FINDING-6-style comment: allocator portfolio blends derive from live
    API equity curves — every supported venue is crypto, so under the blend rule (84-CONTEXT.md,
    any-crypto-leg → 365) the portfolio series is calendar-daily → 365; a future non-crypto venue
    must derive this from the book's key roster instead.
    (b) AlphaBetaDecomposition.tsx: import annualizationPeriods from "@/lib/closed-sets" and pass
    `annualizationPeriods("crypto")` as the 3rd arg to computeAlphaBeta at :90, with the same
    live-book rationale comment (do NOT hardcode a bare 365 literal — keep the mapping greppable
    to the one registry).
    (c) Tests (RED first) in allocator-portfolio-payload.test.ts: pin sharpe/ann_vol moved by the
    expected factor vs a 252-basis hand computation AND cagr unchanged for a fixed equity-curve
    fixture. For the widget, a component test only if one already exists (grep); otherwise the
    payload-level pin + the acceptance grep below suffice.
  </action>
  <verify>
    <automated>npx vitest run src/lib/factsheet/allocator-portfolio-payload.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'assetClass: "crypto"' src/lib/factsheet/allocator-portfolio-payload.ts returns 1
    - grep -c 'annualizationPeriods("crypto")' "src/app/(dashboard)/allocations/widgets/attribution/AlphaBetaDecomposition.tsx" returns 1
    - allocator-portfolio-payload.test.ts pins: risk metrics on 365 basis; cagr byte-identical
  </acceptance_criteria>
  <done>Actual-portfolio factsheet + attribution alpha ride the crypto basis via the closed-set registry; CAGR proven invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: allocator.ts reference panels — per-blend basis</name>
  <files>src/lib/factsheet/allocator.ts, src/lib/factsheet/allocator.test.ts, src/lib/factsheet/build-payload.ts</files>
  <read_first>src/lib/factsheet/allocator.ts (buildAllocatorMetrics — BOTH √252s at :79-80 feed annVol AND mmAnnVol; the sleeve grid-scan consumes them), src/lib/factsheet/build-payload.ts:280-300 (the three reference-panel call sites and their leg composition), 84-CONTEXT.md call-sites block (the locked ruling: "60/40 pure-tradfi=252, any BTC/ETH-legged blend=365")</read_first>
  <behavior>
    - buildAllocatorMetrics(rets, mmRets) deep-equals buildAllocatorMetrics(rets, mmRets, 252) (default identity)
    - At 365: ann_vol and blend_vol scale ×√(365/252); cum_ret, max_dd, corr, tail_* BYTE-IDENTICAL (basis-free); sleeve_pct may legitimately shift (the vol-target grid scan reads annVol) — pin its 365 value from a hand-derived fixture, do not assert invariance
    - build-payload.ts: 60/40 call passes NO basis arg (default 252, byte-identical); all-weather and BTC/ETH calls pass 365
  </behavior>
  <action>
    (a) allocator.ts: `buildAllocatorMetrics(rets: number[], mmRets: number[], periodsPerYear = 252)`;
    replace BOTH `Math.sqrt(252)` occurrences (annVol and mmAnnVol) with Math.sqrt(periodsPerYear).
    JSDoc: the caller derives the basis from the REFERENCE blend's constituent legs per the locked
    #597-part-2 ruling — a BTC/ETH leg makes the joined series calendar-daily.
    (b) build-payload.ts reference panels: leave the 60/40 [spxRet, iefRet] call UNCHANGED (pure
    tradfi → default 252 — the byte-identity case) with a one-line comment; pass 365 to the
    all-weather [spx, gld, ief, btc] and the 70/30 [btc, eth] calls, each with a comment naming the
    crypto leg(s) that trigger the blend rule. Use the literal 365 here ONLY via
    `annualizationPeriods("crypto")` import if build-payload already imports it (it does, :15) —
    prefer `annualizationPeriods("crypto")` for greppability.
    (c) Create src/lib/factsheet/allocator.test.ts (RED first; none exists — verify with ls):
    default-identity deep-equal; the 365 scaling facts; cum_ret/max_dd/corr invariance.
  </action>
  <verify>
    <automated>npx vitest run src/lib/factsheet/allocator.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^\s*//' src/lib/factsheet/allocator.ts | grep -c 'Math.sqrt(252)' returns 0 (both hardcodes parameterized; default in the signature only)
    - build-payload.ts: exactly TWO of the three buildAllocatorMetrics calls carry a basis arg (the BTC-legged ones); the 60/40 call is textually unchanged
    - allocator.test.ts pins default-identity + 365 scaling + invariants and passes
  </acceptance_criteria>
  <done>Reference panels follow the locked leg-decided split: 60/40 byte-identical √252, BTC-legged panels √365.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → SSR payload | asset_class column projected into the dashboard RSC payload (non-sensitive enum, RLS-scoped query unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-03a | Information Disclosure | getMyAllocationDashboard select widening | accept | asset_class is a public classification enum already exposed on public factsheets (#597); no tier redaction applies |
| T-84-03b | Tampering | 60/40 reference panel basis | mitigate | 60/40 call left textually unchanged + default-identity deep-equal pin |
</threat_model>

<verification>
- `npx vitest run src/lib/factsheet/allocator.test.ts src/lib/factsheet/allocator-portfolio-payload.test.ts --no-file-parallelism`
- `npx tsc --noEmit` and `npm run lint` clean
- git diff touches ONLY files_modified
</verification>

<success_criteria>
- Actual allocation KPIs (live baseline sharpe, portfolio factsheet, attribution alpha) ride √365 via the closed-set registry
- 60/40 pure-tradfi reference panel proven byte-identical at √252
- asset_class SSR channel available for the wave-3 composer plan
</success_criteria>

<output>
Create `.planning/phases/84-blend-allocation-asset-class-annualization-597-part-2-scenar/84-03-SUMMARY.md` when done.
</output>
