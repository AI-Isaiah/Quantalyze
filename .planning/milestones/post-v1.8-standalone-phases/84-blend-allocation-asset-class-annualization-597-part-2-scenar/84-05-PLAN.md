---
phase: 84-blend-allocation-asset-class-annualization
plan: 05
type: execute
wave: 3
depends_on: [84-01, 84-02, 84-03]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/app/api/strategies/[id]/returns/route.ts
  - src/lib/scenario-sample-ratios.test.ts
autonomous: true
requirements: [BLEND-01]
must_haves:
  truths:
    - "The composer derives ONE blend basis (blendPeriodsPerYear over the SELECTED engine-set legs) and threads it to computeScenario (:2317), buildBlendPanels (:2647), the own-book sampleBasisRatios leg (:2692), ScenarioFactsheetChart, and ScenarioBenchmarkSection"
    - "A blend with ≥1 crypto leg yields periodsPerYear=365 at the computeScenario call site; an all-unknown-asset_class added-only blend stays 252 byte-identical"
    - "Peer-rank inputs are NOT rescaled — peerSharpe/peerSortino/peerMaxDD remain the engine's raw values; NO √(252/365) factor exists anywhere (rejected in #597: it penalizes 24/7 sleeves ~17%)"
    - "Drawer-added strategies outside the book get their asset_class from the widened /api/strategies/[id]/returns response (published rows only)"
    - "sampleBasisRatios ↔ computeScenario parity holds at 365 (parity-by-construction extended beyond the 252 golden)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "blendBasis memo + threading at all five composer consumption points"
      contains: "blendPeriodsPerYear"
    - path: "src/app/api/strategies/[id]/returns/route.ts"
      provides: "asset_class on the lazy returns response (published probe widened)"
      contains: "asset_class"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/lib/scenario.ts computeScenario"
      via: "4th arg blendBasis"
      pattern: "computeScenario\\(engineSet.strategies, engineState, dateMapCache, blendBasis\\)"
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/lib/sample-basis-ratios.ts sampleBasisRatios"
      via: "own-book leg on the SAME blend basis (like-for-like delta)"
      pattern: "sampleBasisRatios\\(bookReturns, blendBasis\\)"
---

<objective>
Wire the blend basis through the ScenarioComposer — the primary surface of this phase — and
close the metadata gap for drawer-added strategies via the lazy returns route.

Purpose: the composer is where allocators read blended Sharpe/Sortino/vol; per the locked rule
(84-CONTEXT.md) a single crypto leg makes the blended series calendar-daily → √365. The
peer-rank stays on RAW annualized values (frequency-invariant) — the ONE explicitly rejected
change is a basis rescale.
Output: composer threading, widened lazy route, 365 parity extension, no-rescale pin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-blend-allocation-asset-class-annualization-597-part-2-scenar/84-CONTEXT.md
@src/lib/sample-basis-ratios.ts

<interfaces>
From src/lib/closed-sets.ts (84-01):

```typescript
export function blendPeriodsPerYear(legs: ReadonlyArray<{ asset_class?: string | null }>): number;
```

From 84-02 (host props, default 252):
- `<ScenarioFactsheetChart periodsPerYear={...} />`, `<ScenarioBenchmarkSection periodsPerYear={...} />`

From 84-03 (SSR channel): `MyAllocationDashboardPayload strategies[].strategy.asset_class?: string | null`

Already-parameterized engine functions (pass the basis, do not edit the modules):

```typescript
computeScenario(strategies, state, dateMapCache, periodsPerYear = 252)              // src/lib/scenario.ts
buildBlendPanels(portfolioDaily, window, periodsPerYear = 252)                       // src/lib/scenario-blend-panels.ts
sampleBasisRatios(dailyReturns: number[], periodsPerYear = 252): SampleBasisRatios   // src/lib/sample-basis-ratios.ts
```

Composer consumption points (current line anchors):
- :1819 addedStrategyMetadataLookup (disclosure_tier/cagr/sharpe from strategyById)
- :2317 computeScenario, :2647 buildBlendPanels, :2692 sampleBasisRatios(bookReturns)
- :2578-2580 peerSharpe/peerSortino/peerMaxDD = scenarioMetrics.* (raw — MUST stay raw)
- :3749 ScenarioFactsheetChart, :3806 ScenarioBenchmarkSection
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Metadata channel completion — lazy route + composer lookup</name>
  <files>src/app/api/strategies/[id]/returns/route.ts, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>src/app/api/strategies/[id]/returns/route.ts (the published-existence probe select("id") at :134 and the ReturnsResponse body at :178 — plus the T-29-01/T-29-02 comments whose guarantees must hold), src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:1739-1840 (addedReturnsById + fetchAddedReturns + addedStrategyMetadataLookup + handleRemoveAdded purge discipline), src/app/(dashboard)/allocations/lib/scenario-adapter.ts (the widened 4-field Pick from 84-01)</read_first>
  <behavior>
    - GET /api/strategies/[id]/returns response body carries asset_class: string | null for a published strategy (unpublished/absent id still 404s — existence-oracle behavior unchanged)
    - Composer: a book strategy's lookup entry carries asset_class from payload.strategies (84-03 channel); a drawer-added non-book strategy's entry carries the lazily-fetched asset_class; absent everywhere → null
    - handleRemoveAdded purges the fetched asset_class alongside addedReturnsById (a re-add starts clean)
  </behavior>
  <action>
    (a) returns route: widen the published probe to `.select("id, asset_class")` and add
    `asset_class: string | null` to the ReturnsResponse body (`strat.asset_class ?? null`).
    asset_class of a PUBLISHED strategy is public classification data (already rendered on public
    factsheets since #597) — note that in a comment; the 404 existence-oracle and error-redaction
    behavior stay untouched.
    (b) ScenarioComposer.tsx: extend the lazy-fetch bookkeeping — add `addedAssetClassById`
    state (Record<string, string | null>) written by fetchAddedReturns from the widened response
    (tolerate an absent field from a stale deploy → null), purged in handleRemoveAdded exactly like
    addedReturnsById. addedStrategyMetadataLookup entries gain
    `asset_class: found?.strategy.asset_class ?? addedAssetClassById[a.id] ?? null` and the memo
    deps gain addedAssetClassById. The two `Pick<..., "disclosure_tier" | "cagr" | "sharpe">` casts
    at :1923/:1939 widen to include "asset_class" (matching the 84-01 adapter signature).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"id, asset_class"' "src/app/api/strategies/[id]/returns/route.ts" returns 1
    - grep -c "addedAssetClassById" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" ≥ 3 (state, write, purge/lookup)
    - The metadata lookup entry construction references found.strategy.asset_class (grep)
    - tsc clean; existing composer tests green
  </acceptance_criteria>
  <done>Every added strategy — book or drawer — resolves an honest asset_class (or null); route response widened without changing auth/existence behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: blendBasis memo + five-point threading (peer-rank untouched)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:2308-2320 (engineState + computeScenario memo), :2578-2634 (the peer-rank body construction — the DO-NOT-TOUCH surface), :2642-2649 (buildBlendPanels memo), :2669-2720 (PEER-05 own-book delta — the sampleBasisRatios leg and its like-for-like BASIS comment), :3749/:3806 (the two host renders), 84-CONTEXT.md decisions (peer-percentile: NO basis rescale — rejected in #597)</read_first>
  <behavior>
    - blendBasis memo = blendPeriodsPerYear(engineSet.strategies.filter(s => engineState.selected[s.id])) — 365 when any SELECTED leg is crypto, 252 otherwise (unselected crypto legs do not flip it)
    - scenarioMetrics/blendPanels/ownBookDelta/factsheet-preview/benchmark-section all consume the SAME blendBasis value
    - peerSharpe/peerSortino/peerMaxDD remain scenarioMetrics.sharpe/.sortino/.max_drawdown verbatim (raw engine output at the blend basis — annualized Sharpe is frequency-invariant, so the cohort comparison stays honest with NO rescale factor)
  </behavior>
  <action>
    (a) Add the blendBasis useMemo directly after the engineState memo (deps: engineSet, engineState),
    with the selected-legs-only comment. In book mode the per-key legs carry asset_class 'crypto'
    (84-01) so every real book blend derives 365; a pure-CSV-tradfi added-only blend derives 252.
    (b) Thread: computeScenario(engineSet.strategies, engineState, dateMapCache, blendBasis) at :2317
    (memo deps + blendBasis); buildBlendPanels(portfolioDaily, rollingWindow, blendBasis) at :2647;
    sampleBasisRatios(bookReturns, blendBasis) at :2692 — update the PEER-05 comment block from
    "sample/252 basis" wording to "sample basis at the blend's periodsPerYear (like-for-like legs)";
    periodsPerYear={blendBasis} on ScenarioFactsheetChart (:3749) and ScenarioBenchmarkSection (:3806).
    (c) Peer-rank fence: make NO edit to the :2578-2634 peer body/debounce block beyond what the
    scenarioMetrics reference already carries. Add one comment above peerSharpe noting the raw-value
    contract and the #597 rejection of √(252/365).
    Do NOT pin or assert CAGR values in this plan (84-06 converts the CAGR clock within this phase).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "blendBasis" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" ≥ 6 (memo + five consumption points)
    - grep -c "computeScenario(engineSet.strategies, engineState, dateMapCache, blendBasis)" returns 1
    - grep -c "sampleBasisRatios(bookReturns, blendBasis)" returns 1
    - NO basis-rescale factor introduced: `grep -rn "252 / 365\|365 / 252" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" | grep -v '^\s*//' | grep -cv "//"` returns 0 (comments excluded; no operative rescale)
    - peerSharpe/peerSortino/peerMaxDD lines textually unchanged except the added comment (git diff inspection)
  </acceptance_criteria>
  <done>One basis, five consumers, zero rescales; peer-rank inputs verifiably raw.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression pins — crypto-leg 365, unknown-leg 252, 365 parity</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx, src/lib/scenario-sample-ratios.test.ts</files>
  <read_first>src/lib/scenario-sample-ratios.test.ts (the PEER-05 parity golden — how it feeds one series to both sampleBasisRatios and computeScenario and asserts rounded equality), src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (existing render/fixture harness — reuse its payload builders), src/lib/sample-basis-ratios.ts (docstring: the replica must stay in parity "at ANY basis")</read_first>
  <behavior>
    - Parity at 365: for one fixed daily-return series, sampleBasisRatios(series, 365) equals computeScenario's rounded sharpe/sortino/max_drawdown when computeScenario runs with periodsPerYear=365 (extends the existing 252 golden; a drift in either module at the new basis fails it)
    - Composer-level: a rendered blend whose engine set contains ≥1 crypto-tagged leg produces the 365-basis sharpe (assert via the rendered KPI or the engine-call spy, whichever the existing harness supports); an all-null-asset_class added-only blend produces values byte-identical to the pre-phase 252 output
  </behavior>
  <action>
    (a) scenario-sample-ratios.test.ts: add a 365 parity case mirroring the existing 252 golden
    construction exactly (same series, both functions at 365, rounded-equality assertions).
    (b) ScenarioComposer.test.tsx: two cases from the CONTEXT's regression list — crypto-leg → √365
    and pure-unknown → √252 byte-identity. Derive expected values from sampleBasisRatios at the
    respective basis rather than hand-rolled math (single source). Skip CAGR assertions entirely.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario-sample-ratios.test.ts "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - scenario-sample-ratios.test.ts contains a case invoking both functions at 365 and passes
    - ScenarioComposer.test.tsx pins the ≥1-crypto-leg → 365 fact and the all-unknown → 252 byte-identity fact
    - Full suite for the two files green
  </acceptance_criteria>
  <done>The phase's headline regression facts (365-if-any-crypto, 252-identity, replica parity at any basis) are CI-enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/strategies/[id]/returns | response widened with asset_class (published rows only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-05a | Information Disclosure | returns route widening | accept | asset_class of PUBLISHED strategies is public factsheet data (#597); probe stays published-gated + 404 oracle unchanged |
| T-84-05b | Tampering | peer-rank basis | mitigate | raw-value fence + no-rescale grep gate + untouched peer body block |
</threat_model>

<verification>
- `npx vitest run src/lib/scenario-sample-ratios.test.ts "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism`
- `npx tsc --noEmit` + `npm run lint`
- Manual spot-check available post-merge via /qa (blend KPIs on a real book show the √365 basis)
</verification>

<success_criteria>
- Composer blend KPIs, blend panels, own-book delta, preview payload, and benchmark section all ride ONE derived basis
- Peer-rank raw values proven un-rescaled
- 365 parity + both headline regression pins green
</success_criteria>

<output>
Create `.planning/phases/84-blend-allocation-asset-class-annualization-597-part-2-scenar/84-05-SUMMARY.md` when done.
</output>
