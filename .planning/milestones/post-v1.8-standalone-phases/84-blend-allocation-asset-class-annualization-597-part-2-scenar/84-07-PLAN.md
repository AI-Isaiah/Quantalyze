---
phase: 84-blend-allocation-asset-class-annualization
plan: 07
type: execute
wave: 3
depends_on: []
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx
  - src/app/api/strategies/csv-finalize/route.ts
  - src/__tests__/csv-finalize-rpc.test.ts
autonomous: true
requirements: [BLEND-04]
must_haves:
  truths:
    - "The wizard's asset_class picker value reaches the persisted strategies row on the CSV path: CsvSubmitStep forwards metadata.asset_class; csv-finalize validates and writes it"
    - "CSV strategies KEEP the user's picker choice — traditional stays traditional (NO finalize-wizard-style force-derive to 'crypto'; that rule is API-key-only, per the locked decision)"
    - "A present-but-invalid asset_class value 400s (CSV_INVALID_FORMAT, NEW-C14-03 fail-loud pattern); an absent field is omitted from the UPDATE (back-compat, column stays null → 252 default downstream)"
  artifacts:
    - path: "src/app/api/strategies/csv-finalize/route.ts"
      provides: "asset_class on CsvMetadataPayload + parseCsvMetadata validation + buildMetadataUpdatePayload write"
      contains: "asset_class"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx"
      provides: "asset_class forwarded in the finalize metadata body"
      contains: "asset_class"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx"
      to: "src/app/api/strategies/csv-finalize/route.ts"
      via: "POST body metadata.asset_class"
      pattern: "asset_class: metadata.assetClass"
    - from: "src/app/api/strategies/csv-finalize/route.ts"
      to: "strategies.asset_class column"
      via: "buildMetadataUpdatePayload → owner-scoped UPDATE"
      pattern: "updatePayload.asset_class"
---

<objective>
Close the deferred #597 upload-picker gap: the wizard's CSV branch captures an asset_class
picker value (MetadataStep → MetadataDraft.assetClass) but CsvSubmitStep drops it and the
csv-finalize route never persists it — every CSV strategy silently lands with asset_class null.
Forward + validate + persist it (84-CONTEXT.md CSV-finalize decision).

Purpose: a genuinely traditional CSV track record (the phase's one real √252 blend leg) can
only exist if the picker choice survives finalize.
Output: forwarded field, closed-set validation at the route boundary, persistence, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-blend-allocation-asset-class-annualization-597-part-2-scenar/84-CONTEXT.md

<interfaces>
From src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.tsx (already shipped — do not edit):

```typescript
export interface MetadataDraft { /* ... */ assetClass: string; /* 'crypto' | 'traditional'; locked to 'crypto' when a crypto exchange is detected; CSV branch has no exchange → free choice */ }
```

From src/app/api/strategies/csv-finalize/route.ts (current):

```typescript
interface CsvMetadataPayload { description?; category_id?; strategy_types?; subtypes?; markets?; supported_exchanges?; leverage_range?; aum?; max_capacity?; }
export function parseCsvMetadata(raw: unknown): ParseCsvMetadataResult; // ok:false → 400 CSV_INVALID_FORMAT (NEW-C14-03)
function buildMetadataUpdatePayload(metadata: CsvMetadataPayload | null): Record<string, unknown>;
```

The FORCE-DERIVE rule that does NOT apply here (finalize-wizard/route.ts:474):
`.update({ asset_class: apiKeyId ? "crypto" : fields.asset_class })` — API-key strategies force
'crypto'; CSV strategies take the user's value. The csv-finalize path has no apiKeyId; persist
the picker value verbatim after validation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forward asset_class from CsvSubmitStep</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx</files>
  <read_first>src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx (the handleSubmit metadata body at :117-127 and the MetadataDraft prop), src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.tsx:40-115 + :200-215 (assetClass capture + the crypto-lock gate — confirms the CSV branch leaves the picker free), src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx (existing fetch-mock body assertions to extend)</read_first>
  <behavior>
    - The POST /api/strategies/csv-finalize body's metadata object carries asset_class: metadata.assetClass ('crypto' or 'traditional' from the picker)
    - All existing body fields and the idempotent-409 / error-envelope handling are untouched
  </behavior>
  <action>
    Add `asset_class: metadata.assetClass` to the metadata object in handleSubmit (snake_case wire
    key, matching every sibling field), with a one-line comment citing #597 part 2 (the deferred
    upload-picker persistence) and that the route validates the closed set. Extend the existing
    CsvSubmitStep.test.tsx fetch-mock assertion to require metadata.asset_class in the posted body
    (RED first — assert on the current mock, watch it fail, add the field).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "asset_class: metadata.assetClass" "src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx" returns 1
    - CsvSubmitStep.test.tsx asserts the posted metadata carries the picker value and passes
  </acceptance_criteria>
  <done>The picker value leaves the client on every CSV finalize.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate + persist asset_class in csv-finalize</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/__tests__/csv-finalize-rpc.test.ts</files>
  <read_first>src/app/api/strategies/csv-finalize/route.ts:227-443 (CsvMetadataPayload + parseCsvMetadata's ok/false discriminated union + buildMetadataUpdatePayload — mirror the NEW-C14-03 money-field fail-loud pattern for present-but-invalid), src/app/api/strategies/finalize-wizard/route.ts:260-300 (the shipped asset_class validation `=== "crypto" || === "traditional"` to mirror, and :453-485 the force-derive that must NOT be copied), src/__tests__/csv-finalize-rpc.test.ts + src/__tests__/csv-finalize-c14-regression.test.ts (test harness conventions for parseCsvMetadata)</read_first>
  <behavior>
    - parseCsvMetadata: asset_class 'crypto' → payload.asset_class 'crypto'; 'traditional' → 'traditional'
    - asset_class present but not in the closed set (e.g. 'CRYPTO', 'equities', 42) → ok:false with field 'metadata.asset_class' → the route 400s CSV_INVALID_FORMAT
    - asset_class absent or null → key OMITTED from the payload and from buildMetadataUpdatePayload's UPDATE (back-compat: metadata-less finalize unaffected; column stays null → downstream annualizationPeriods default 252)
    - The persisted UPDATE carries asset_class verbatim — NO apiKeyId force-derive on this path (locked decision: CSV strategies keep the user's choice)
  </behavior>
  <action>
    (a) CsvMetadataPayload gains `asset_class?: "crypto" | "traditional"`.
    (b) parseCsvMetadata: after the money-field block, handle obj.asset_class — undefined/null →
    skip; the exact strings 'crypto'/'traditional' → out.asset_class; anything else → return
    `{ ok: false, field: "metadata.asset_class", message: 'asset_class must be "crypto" or "traditional" (got ...)' }`
    (mirror the money-field message style; no case-folding — the DB CHECK/closed set is lowercase).
    (c) buildMetadataUpdatePayload: `if (metadata.asset_class !== undefined) updatePayload.asset_class = metadata.asset_class;`
    with the locked-decision comment (CSV keeps the picker value; the finalize-wizard force-derive
    is API-key-only).
    (d) Tests RED first against the exported parseCsvMetadata + the update-payload path in
    csv-finalize-rpc.test.ts: the four behavior cases above, plus one end-to-end-shaped case if the
    existing harness exercises buildMetadataUpdatePayload through the route (follow whichever level
    the C14 regression tests use).
    Do NOT touch the RPC call, the idempotency branches, or any other metadata field's validation.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/csv-finalize-rpc.test.ts src/__tests__/csv-finalize-c14-regression.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"crypto" | "traditional"' src/app/api/strategies/csv-finalize/route.ts ≥ 1 (typed closed set)
    - grep -v '^\s*//' src/app/api/strategies/csv-finalize/route.ts | grep -c 'apiKeyId ? "crypto"' returns 0 (no force-derive copied onto the CSV path)
    - Tests pin: valid both values persist; invalid → ok:false field metadata.asset_class; absent → no asset_class key in the update payload
    - All pre-existing csv-finalize tests pass unchanged
  </acceptance_criteria>
  <done>The picker choice persists to strategies.asset_class with boundary validation; absent stays absent; the C14 fail-loud contract extended, not forked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → csv-finalize route | attacker-controlled metadata.asset_class crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-07a | Tampering | metadata.asset_class input | mitigate | exact-string closed-set validation in parseCsvMetadata; 400 on anything else (NEW-C14-03 pattern) |
| T-84-07b | Elevation of Privilege | strategies UPDATE | accept | rides the existing owner-scoped RLS UPDATE path csv-finalize already uses for every metadata field |
| T-84-07c | Repudiation | mis-picked class inflating Sharpe (365 vs 252 display) | accept | user-attested classification, same trust level as the shipped finalize-wizard picker (#597); admin review gate unchanged |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx" src/__tests__/csv-finalize-rpc.test.ts src/__tests__/csv-finalize-c14-regression.test.ts --no-file-parallelism`
- `npx tsc --noEmit` + `npm run lint`
</verification>

<success_criteria>
- CSV wizard picker value persists end-to-end (client body → route validation → strategies.asset_class)
- Invalid input fails loud at the boundary; absent input byte-identical to today
- No force-derive on the CSV path
</success_criteria>

<output>
Create `.planning/phases/84-blend-allocation-asset-class-annualization-597-part-2-scenar/84-07-SUMMARY.md` when done.
</output>
