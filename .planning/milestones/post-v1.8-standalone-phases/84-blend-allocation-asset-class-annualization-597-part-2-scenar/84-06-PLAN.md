---
phase: 84-blend-allocation-asset-class-annualization
plan: 06
type: execute
wave: 3
depends_on: []
files_modified:
  - src/lib/scenario.ts
  - src/lib/scenario.test.ts
autonomous: true
requirements: [BLEND-03]
must_haves:
  truths:
    - "computeScenario's CAGR rides the CALENDAR clock: years = calendarYears(first axis date, last axis date), asset-class/periodsPerYear-INVARIANT (the two-clocks ruling)"
    - "At default 252, every NON-CAGR output (twr, volatility, sharpe, sortino, max_drawdown, max_dd_days, correlations, equity_curve, portfolio_daily_returns, member_*) is BYTE-IDENTICAL — existing scenario.test.ts pins pass untouched except deliberately re-derived CAGR expectations"
    - "CAGR is gap-robust: same n, different calendar span → different CAGR (a sparse track no longer over-annualizes)"
    - "computeCompositeCurve completes the locked call-site list: optional periodsPerYear passed through, output proven annualization-INVARIANT (curve only)"
  artifacts:
    - path: "src/lib/scenario.ts"
      provides: "calendar-span CAGR + computeCompositeCurve periodsPerYear pass-through"
      contains: "calendarYears"
  key_links:
    - from: "src/lib/scenario.ts"
      to: "src/lib/closed-sets.ts calendarYears"
      via: "CAGR year-count derivation from the date axis"
      pattern: "calendarYears\\("
---

<objective>
Convert scenario.ts CAGR from the count-based clock (`years = n / periodsPerYear`) to the
calendar-span clock (`days / 365.25` via closed-sets `calendarYears`), and thread the optional
basis through computeCompositeCurve — the last entry in the locked computeScenario call-site
list.

Purpose: the in-file #597 comment block (scenario.ts:513-524) mandates exactly this: "When the
blend follow-up PR flips periodsPerYear toward 365, it MUST also convert this to a real
calendar-span year count derived from the date axis." The Fable CAGR ruling / TWR-05: RETURN
rides the calendar clock, invariant to asset class; risk metrics ride the frequency clock.

REVIEWED-EDIT PROTOCOL: scenario.ts is a frozen-spine carve-out (ADR-001 + #597) — editable as
a deliberate, reviewed math edit, protected by scenario.test.ts's own pins (the phase-52 guard
no longer freezes it). The CAGR shift is a VALUE change; per the [73-02] precedent (metrics.py
TWR-05 landing), any numeric CAGR expectation in tests is RE-DERIVED and re-pinned in the same
commit with a derivation comment — NEVER blind-updated. All other pins must pass UNCHANGED.
Output: calendar CAGR + invariance/gap-robustness pins + composite-curve pass-through.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-blend-allocation-asset-class-annualization-597-part-2-scenar/84-CONTEXT.md
@src/lib/scenario.ts

<interfaces>
From src/lib/closed-sets.ts (already shipped in #597):

```typescript
/** Calendar-year span on the 365.25-day civil clock; returns 0 when span <= 0 / non-finite. */
export function calendarYears(firstDateMs: number, lastDateMs: number): number;
```

The current CAGR block (scenario.ts:508-526) to convert:

```typescript
const twr = cumulative[n - 1] - 1;
// ... #597-blend FOLLOW-UP comment block ...
const years = n / periodsPerYear;
const cagr = years > 0 ? Math.pow(1 + twr, 1 / years) - 1 : null;
```

The return axis available in scope: `commonDates: string[]` (ISO dates, sorted; `n = commonDates.length` on the success path).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Calendar-span CAGR in computeScenario</name>
  <files>src/lib/scenario.ts, src/lib/scenario.test.ts</files>
  <read_first>src/lib/scenario.ts:440-530 (the cumulative/guard/CAGR block and what commonDates holds at that point), src/lib/scenario.test.ts IN FULL (grep every `cagr` expectation first — the grep at planning time found only null-pins on degenerate paths, but verify; every NON-CAGR pin is a byte-identity fence for this edit), src/lib/closed-sets.ts:145-157 (calendarYears semantics — 0 on non-positive span), src/lib/factsheet/compute.ts (the shipped single-strategy calendar-CAGR convention this mirrors)</read_first>
  <behavior>
    - CAGR invariance: one series computed at periodsPerYear 252 and 365 yields the IDENTICAL cagr (calendar clock is basis-free) while sharpe/volatility differ
    - Gap-robustness: two series with identical returns/n but different date spacing (dense daily vs every-other-day) yield DIFFERENT cagr (span-driven) and IDENTICAL sharpe/vol (count-driven)
    - Degenerate: a zero/negative calendar span (calendarYears → 0) yields cagr null (existing `years > 0` guard semantics preserved)
    - Byte-identity fence: for the existing scenario.test.ts fixtures at default 252, twr/volatility/sharpe/sortino/max_drawdown/max_dd_days/correlation_matrix/avg_pairwise_correlation/equity_curve/portfolio_daily_returns/effective_*/member_* are unchanged
  </behavior>
  <action>
    Import calendarYears from "./closed-sets". Replace `const years = n / periodsPerYear;` with
    `const years = calendarYears(Date.parse(commonDates[0]), Date.parse(commonDates[n - 1]));`
    — the ACTUAL return axis (mirrors compute.ts, which derives the span from the series dates;
    do NOT use the window bounds — a window wider than the member coverage would overstate the span).
    Rewrite the :509-524 comment block: delete the "#597-blend FOLLOW-UP" warning (it is now
    discharged) and state the two-clocks contract: RETURN/CAGR = calendar clock (days/365.25,
    asset-class-INVARIANT); vol/sharpe/sortino = frequency clock (√periodsPerYear / ×periodsPerYear).
    Keep the `years > 0 ? ... : null` guard verbatim.
    TEST PROTOCOL (RED first): run scenario.test.ts BEFORE the edit and capture which expectations
    (if any) carry numeric cagr values; add the three behavior pins above; make the edit; every
    non-CAGR expectation must pass with ZERO test-file edits — a numeric cagr expectation (if one
    exists) is re-derived by hand ((1+twr)^(1/calendarYears)−1) and re-pinned with a derivation
    comment citing this plan + the [73-02] precedent. If ANY non-CAGR expectation goes red, STOP
    and fix the source edit, never the pin.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^\s*//' src/lib/scenario.ts | grep -c "n / periodsPerYear" returns 0 (count clock gone)
    - grep -c "calendarYears(Date.parse(commonDates\[0\])" src/lib/scenario.ts returns 1
    - scenario.test.ts contains the CAGR-invariance (252 vs 365 same cagr, different sharpe) and gap-robustness (same n, different span → different cagr) pins, both passing
    - git diff src/lib/scenario.test.ts shows NO modified non-CAGR expectation values
  </acceptance_criteria>
  <done>CAGR rides the calendar clock, invariant across bases, gap-robust; every risk-metric pin untouched and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeCompositeCurve pass-through + invariance pin</name>
  <files>src/lib/scenario.ts, src/lib/scenario.test.ts</files>
  <read_first>src/lib/scenario.ts:731-784 (computeCompositeCurve — returns ONLY the wealth-form equity_curve; the internal computeScenario call at :771), 84-CONTEXT.md call-sites block (scenario.ts:758 is on the locked computeScenario list)</read_first>
  <behavior>
    - computeCompositeCurve gains an optional trailing periodsPerYear (default 252) forwarded to the internal computeScenario call
    - Invariance pin: computeCompositeCurve(..., 365) deep-equals the default output for a fixed fixture (the curve is built from the cumulative product — annualization never enters it), so existing callers (My Allocation composite, favorites overlay) need NO change
  </behavior>
  <action>
    Add `periodsPerYear = 252` as the trailing param of computeCompositeCurve and pass it as the
    4th arg of the internal computeScenario call. JSDoc: this completes the #597-part-2 call-site
    contract; the returned curve is annualization-INVARIANT (only cagr/vol/sharpe/sortino consume
    the basis and none are returned here) — callers may pass a basis for contract uniformity but
    the output cannot differ; the invariance test is the proof. Add the deep-equal invariance pin
    to scenario.test.ts (RED-then-green not applicable to an invariance pin — instead prove it
    falsifiable by noting in the test comment which line would break it: rounding through a
    basis-dependent metric).
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - computeCompositeCurve signature carries periodsPerYear = 252 and forwards it (grep the internal call: `computeScenario(` within the function body has 4 args)
    - The 365-vs-default deep-equal invariance pin exists and passes
    - No caller of computeCompositeCurve was edited (git diff shows only scenario.ts + scenario.test.ts)
  </acceptance_criteria>
  <done>The locked call-site list is complete; curve invariance is CI-pinned rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure in-process math edit inside the reviewed-edit carve-out |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-06 | Tampering | frozen-spine reviewed edit | mitigate | scenario.test.ts pin discipline (no non-CAGR expectation may change; re-derived CAGR pins carry derivation comments per [73-02]) |
</threat_model>

<verification>
- `npx vitest run src/lib/scenario.test.ts --no-file-parallelism`
- `npx tsc --noEmit`
- git diff review: scenario.ts diff limited to the CAGR block, the comment rewrite, the import, and computeCompositeCurve's signature/call
</verification>

<success_criteria>
- CAGR calendar-span, basis-invariant, gap-robust — pinned
- All risk-metric/curve pins byte-identical at default 252
- computeCompositeCurve threaded + invariance-pinned
</success_criteria>

<output>
Create `.planning/phases/84-blend-allocation-asset-class-annualization-597-part-2-scenar/84-06-SUMMARY.md` when done.
</output>
