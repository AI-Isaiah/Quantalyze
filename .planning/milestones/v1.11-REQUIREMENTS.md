# Requirements Archive: v1.11 Scenario Composer v2

**Archived:** 2026-07-18
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements — v1.11 Scenario Composer v2

> ⚠️ **2026-07-16 reconstruction:** the original `REQUIREMENTS.md` was accidentally deleted with the other local ledgers (see `STATE.md` recovery note) and is not in git. This is a faithful reconstruction of the v1.11 requirement IDs + intent, derived from the (verbatim-restored) `ROADMAP.md` phase Success Criteria, which carry the authoritative detailed acceptance criteria. Per-requirement detail: see the matching phase's Success Criteria in `ROADMAP.md` and the phase `*-CONTEXT.md` files. Requirement→phase mapping is authoritative here; treat ROADMAP Success Criteria as the acceptance-criteria source of truth.

**Legend:** ✅ shipped · ⏳ planned/in-milestone · ⚠️ gated

## ROLE — predicate unification + page guards (Phase 109) ✅
- **ROLE-01** ✅ — `profiles.role` is the single marketplace-persona predicate driving nav.
- **ROLE-02** ✅ — role-scoped surfaces: manager sees no allocator workspace/Discovery; allocator sees zero sell-side (Strategies, Portfolios), the scoped "Add a Strategy" entry (P110) the only exception. (⭐ correction: `/portfolios` is ALLOCATOR-owned — original ROLE-02 mis-classified it.)
- **ROLE-03** ✅ — `is_admin` is an ops overlay only (adds the Admin section, grants no persona surface).
- **ROLE-04** ✅ — direct-nav to an unowned route → server-side redirect to the role home; three-branch failure model (transient DB error does NOT redirect); `role × is_admin` matrix redirect-loop-free.
- **ROLE-05** ✅ — `role='both'` staff backfill lands atomically with the `|| isAdmin` drop (pre-merge empty-set SQL assertion).
- **ROLE-06** ✅ — role-denied action shows honest copy naming the role requirement, never an unsatisfiable retry.

## CONTRIB — private-by-default contribution (Phase 110) ✅
- **CONTRIB-01** ✅ — allocator adds an off-catalog strategy via the existing wizard from an allocator-scoped "Add a Strategy" entry; finalizes UNpublished.
- **CONTRIB-02** ✅ — contributed strategy is private by default; never auto-published, never enters the public catalog.
- **CONTRIB-03** ✅ — allocator sees/selects their own private contributions in the composer Browse drawer (`withPublishedOrOwner`).
- **CONTRIB-04** ✅ — cross-owner isolation proven at RLS + request-scoped query-builder layers; lint bans owner-`.or()` on service-role clients; self-publish hole closed by `guard_strategies_publish_transition`.
- **CONTRIB-05** ✅ — Browse drawer "Can't find it? Add your own" CTA.

## DOGFOOD — allocator dogfooding fixes (Phase 110.1, INSERTED) ✅
- **DOGFOOD-1** ✅ — EmptyState distinguishes no-keys vs connected-but-empty; connected signal from canonical `isPerKeyDailiesEligibleKey`; "Manage exchanges →" not "Connect".
- **DOGFOOD-2** ✅ — Exchanges "Active Allocation auto-synced" affirmative gated on real `allocator_holdings` head-count (tri-state, fail-loud on count error); no fabricated/seeded holdings.
- **DOGFOOD-3** ✅ — validate fail-closed probe returns honest `PROBE_FAILED` ("could not verify") across all wizard routes (`validate-and-encrypt`, `create-with-key`, `composite/add-key`); fail-closed rejection preserved.

## CONSTIT — unified constituent presentation (Phase 111) ✅ COMPLETE (parity gate cleared)
- **CONSTIT-01** ✅ (111-03) — every source (api-key/CSV/catalog/composite) is one uniform weightable constituent row in the ONE `CompositionList`; the separate "Data Sources" section is DELETED (per-key rows moved in verbatim; per-coin holdings removed → Holdings tab).
- **CONSTIT-02** ✅ (111-02 token+threading, 111-03 render) — provenance badge per row (api-verified / csv / self-reported / composite). 111-03 renders the badge on the unified rows via `deriveProvenance` (per-key = `api_verified` by construction; added rows from the `addedStrategyMetadataLookup`; null → honest absence / no badge).
- **CONSTIT-03** ✅ (111-03) — book seed collapses to strategy/key-level constituents (per-coin holdings stay on Holdings tab); off-toggle uses the shared `draft.toggleByScopeRef` mechanism via `togglePerKeySource` (weightless refs, never rescales weights). ⭐ Supersedes Phase-66 CF-05: per-key exclusions now persist with the draft + count toward `diffCount`, unifying composer↔compare.
- **CONSTIT-04** ✅ (111-04) — `scenario.ts` byte-frozen vs origin/main (32494ba2, `git diff --exit-code` clean); a PERMANENT whole-repo grep gate (`scenario-backbone-gates.test.ts`, walks src/ + e2e/ + tests/ + scripts/) fails CI if any of the four 111-03-removed Data-Sources identifiers reappears as live code. Self-invalidation-proof (concatenated tokens + self-excluded walk + neutered-gate detection assertion + over-broadening guard pinning the retained `showDataSources`/`allDataSourcesExcluded` live vars OUT of scope). Phase-close sweep green: backbone gates 9 + scenario.test.ts 50 + parity 5; full TS 8309 w/ coverage exit 0; full pytest 3680; tsc+lint 0 errors.
- **CONSTIT-05** ✅ [GATE CLEARED] — independent numpy/pandas re-derivation (`analytics-service/tests/test_constit_blend_parity.py`, plan 111-01) proves per-key daily series == current per-position weighted blend as interpretation **A (fixed-weight-per-key)** within tolerance (curve 1e-9, KPIs at rounding granularity). PARITY VERIFIED — no re-baseline. A-vs-B time-varying divergence (+0.012165 terminal wealth) recorded in 111-01-SUMMARY for any future re-baseline. CONSTIT UI unblocked.

## WEIGHTS — per-constituent weights + leverage + max-DD solver (Phases 112–113) ⏳
- **WEIGHTS-01** ⏳ — per-strategy weights on strategy-level rows; weight-sum validation through the collapse.
- **WEIGHTS-02** ⏳ — per-row leverage; blend re-derives from the levered daily series (Phase-107 `r→L·r`); no symbol-keyed engine path; leverage sanitized on read (never zod `.min/.max`-refined → can't delete the draft).
- **WEIGHTS-03** ⏳ — per-row max-DD target back-solves implied leverage (per-row mode toggle Leverage|Target-max-DD defaulting to Leverage; ruin-clamped grid-scan-then-bisect on the real transform; derived L stays visible).
- **WEIGHTS-04** ✅ — round-trip test (solved L reproduces target DD within tolerance); honest infeasible/degenerate states (never a fabricated value). _Closed at the solver layer by 113-02: two-sided non-tautological round-trip (lever-up L*=2.5 + deleverage L*=0.6, engine re-feed + perturbation teeth); full honest-state matrix (input-gate degenerate no-throw, value-free infeasible branches, all-negative-solves-finite); 'unimplemented' variant deleted grep-gated; eval budget ≤70 (ruin path 28). The em-dash UI rendering of these states is WEIGHTS-03/113-03._

## BACKBONE — E1/E2 absorption (Phases 114–115) ⏳ ⚠️ E2 SCOPE GATE
- **BACKBONE-01** ✅ — E1: weighted portfolio Sharpe/TWR route through `compute_all_metrics` (114-02 re-route); `compute_twr` + `_compute_sharpe_and_vol` deleted under golden-parity gate (114-03); `compute_mwr`/`compute_modified_dietz` (cashflow/IRR) KEPT + import/function-tested; whole-tree caller sweep (114-01 census) + permanent Python delete-gate (`tests/test_e1_delete_gate.py`, hasattr + tree-walk, injection-proven RED). Phase 114 complete 2026-07-17.
- **BACKBONE-02** ⏳ (display half CODE-COMPLETE, LIVE-acceptance pending) — E2: allocator equity DISPLAY derives from the per-key daily-series blend on the backbone; parity verified on `match.py` outputs. — SPLIT: derivation core = Phase 115; frontend display-repoint = Phase 115.1 (worker-side flow-aware $-equity onto a NEW keyed surface + `queries.ts` `equityDailyPoints` repoint). — 2026-07-18: Phase 115.1 SHIPPED the display half CODE-COMPLETE — worker-side compose over the FROZEN P115 core writes `allocator_equity_derived`, `queries.ts` repointed derived-if-trustworthy-ELSE-legacy (safe pre-backfill) + honest provenance; match-golden byte-stable (score path untouched). LIVE acceptance (derived curve reconciles to the current-holdings anchor within same-day tolerance AND `is_trustworthy`) is founder-gated on the key-mode backfill + `E2_GROUND_TRUTH_*` run — NOT a merge blocker (the fallback keeps prod on the legacy basis until then).
- **BACKBONE-03** ⚠️ [GATE, v2/deferred] → else-branch SHIPPED — `allocator_equity_snapshots` store retirement proceeds ONLY if the reader census (matching/LP/GDPR/admin) + cashflow-reality determination clears; else display-repoint ships and the store stays. — 2026-07-17: census did NOT clear → store retirement stays DEFERRED. 2026-07-18: the ELSE-branch SHIPPED in Phase 115.1 — display-repoint onto the NEW `allocator_equity_derived` surface with the legacy store UNTOUCHED (whole-repo two-writers grep gate green: zero new insert/upsert/update/delete to `allocator_equity_snapshots`; legacy producer `equity_reconstruction.py` byte-frozen). Store retirement (STITCH-02) remains DEFERRED.

## STITCH — allocator multi-key = composite strategy (folded into Phase 115) ⏳
- **STITCH-01** ⏳ (display path landed) — route the allocator's multi-key account through the SAME strategy dailies pipeline (windowed `strategy_keys` stitch → dailies → backbone), not the parallel per-key path. — 2026-07-18: Phase 115.1 landed the DISPLAY path on the unified pipeline — the allocator $-equity curve now derives from the per-key dailies backbone (frozen P115 core, segment-wise blend + shared seams + drop-unanchored-first + ISO index) via `allocator_equity_compose.py`, consumed by `queries.ts` `equityDailyPoints`. Full unified-pipeline convergence tracked with the remaining STITCH items.
- **STITCH-02** ⚠️ — retire the per-key first-writer-wins TWR reconstruction (`equity_reconstruction.py`) + the deribit carve-out; KEEP the Dietz/MWR cashflow path (BACKBONE-01 — backbone can't reproduce it). — DEFERRED 2026-07-17 (115 census did NOT clear; residual blockers in `phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-STITCH-02-DEFERRAL.md`). The additive derivation core ships in Phase 115; the store + jobs + deribit carve-out + `compute_twr` METHOD stay.
- **STITCH-03** ✅ — perf-curve (TWR, cashflow-neutral) ≠ equity-curve ($, steps on deposits/withdrawals); equal only with zero cashflows. Derive dailies first, layer the equity curve = return path + cashflow ledger.
- **STITCH-04** ✅ — unknown starting value → derive backward from current equity + known return path.
- **STITCH-05** ✅ — external cashflows (deposits/withdrawals) handled via Modified-Dietz/MWR (the KEPT path).
- **STITCH-06** ✅ — ⭐ stitch-seam rule: a window-boundary equity jump (key N last day → key N+1 first day) is treated as a synthetic deposit/withdrawal through the same Dietz/MWR ledger — TWR stays clean across the seam, equity reflects the real jump. Windowed stitch and cashflow accounting are ONE code path.

## ADDALLOC — context-aware "+ Allocation" (Phase 116) ✅
- **ADDALLOC-01** ✅ — Scenario tab: "+ Strategy" opens strategy picker (others' published + own incl. private contributions) with upload-a-key option. (116-01, `fa507f2b`)
- **ADDALLOC-02** ✅ — Holdings/Overview: "+ Allocation" opens real-data onboarding (CSV or API key) inline. (116-01, `6c8b5e08`)
- **ADDALLOC-03** ✅ — always a deterministic per-tab action (never a silent no-op, never over-corrected into a dropdown-every-time); covers the dynamic-import loading window via a pending-drain. (116-01)
- **ADDALLOC-04** ✅ — zero-portfolio Simulate-Impact disabled affordance deep-links to real allocator onboarding (connect exchange), not the manager-only `/portfolios` dead-end. (116-02, `23b1f4e2`)

## UIFIX — tooltip portal + overflow polish (Phase 117) ⏳
- **UIFIX-01** ✅ 2026-07-18 — portaled tooltip (portal-to-body + horizontal viewport clamp + implemented vertical flip) renders fully in scroll/overflow containers, near the viewport edge, inside an open Dialog (z-[210] > z-[200]), and inside the mobile drawer (open-flag + typeof-document SSR gate; scroll/resize listener add-on-open/remove-on-close+unmount). 117-01 commits cad16b11/645efd6b; 8/8 tests; no new deps/tokens.
- **UIFIX-02** ✅ 2026-07-18 — focus rings not clipped under `overflow-x-auto` (WCAG 2.4.7). Six enumerated sites (factsheet section-nav anchors, allocations tab consts, shared ResponsiveTable scroll region, both heatmap regions, correlation-matrix region, flagged-holdings expand button) repointed off the positive-offset/default outline onto the clip-proof full-opacity inset ring (`focus-visible:ring-2 ring-inset ring-accent` — /20 rejected for WCAG 1.4.11 ≥3:1); ExposureByClass audited focusable-child-free → no change (Rule 2). 117-02 commits 260d756e/bb97d09d; 7 RED→GREEN assertions; no new tokens.
- **UIFIX-03** ⏳ — factsheet CUM RETURN KPI card shows extreme values (high leverage) without truncation/ellipsis; axe route×viewport matrix green.

---

*Deferred to a later milestone (not v1.11): CI-01 (M-C CI/Test-Infra Ratchet — live-DB vitest + import-guard-skipped pytest actually run in CI; pytest coverage past `services/`; Railway red-main deploy-skip alerting; `-n auto` fence parallelization) and M-B cron reliability.*
