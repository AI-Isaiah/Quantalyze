# Roadmap: Quantalyze

## Milestones

- ✅ **v1.0–v1.8** — shipped; see `MILESTONES.md` + `milestones/` archives.
- ✅ **v1.9 — Multi-Key Composite Strategy** — Phases 85–91 + 90.5 (tag `v1.9` @ `044bee50`, 2026-07-11). Archive: `milestones/v1.9-ROADMAP.md`.
- ✅ **v1.9.1 — Composite Onboarding Hardening** — Phases 92–97 (tag `v1.9.1` @ `be215b15`, v0.41.1.0, 2026-07-12; PR #611). Archive: `milestones/v1.9.1-ROADMAP.md`.
- ✅ **v1.10 — Demo-Hero Portfolio Intelligence + Options MTM + Backbone Unification** — Phases 98–108 (tag `v1.10` @ `32494ba2`, v0.43.0.0, 2026-07-15; PR #619 tail). Archive: `milestones/v1.10-ROADMAP.md`. Full detail: `MILESTONES.md`.
- ✅ **v1.11 — Scenario Composer v2 (unified-backbone constituents + allocator/manager role model)** — Phases 109–117 + 110.1/115.1 (tag `v1.11` @ `a42f4bcf`, v0.45.0.0, 2026-07-18; PRs #620 + #622). Archive: `milestones/v1.11-ROADMAP.md`. Full detail: `MILESTONES.md`.
- ✅ **v1.12 — sFOX Verified Integration FOUNDATION (flag-OFF)** — Phases 118–120 shipped + 121–123 code-complete + 122 UI (tag `v1.12` @ `92be47af`, v0.46.0.0, 2026-07-19; PR #623). Ships sFOX **dormant** (`NEXT_PUBLIC_SFOX_ENABLED`/`SFOX_ENABLED` empty). Detail below (kept as the record). **Go-live ops + open defects re-homed → v1.13 Infra:** 121-03 (egress + IP whitelist — now Railway-native), 122-04 (factsheet e2e defect), 123 go-live (FLIP cutover/enqueue/cron + E2 validation), 124 (deribit `correction`).
- 🚧 **v1.13 — Infra: sFOX go-live + worker rebuild** — Phases 125–130 (started 2026-07-19). Dedicated backfill worker + test-DB retention hygiene, factsheet e2e green, E2 ground-truth, deribit `correction`, derived-curve FLIP retry, then GOLIVE = Railway-native static-egress whitelist (founder ops, day-1 parallel — Fly FULLY DROPPED) + sFOX flag flip. Seed: `milestones/v1.13-infra-ROADMAP-SEED.md`. Detail below.

---

<details>
<summary>✅ v1.10 — Demo-Hero Portfolio Intelligence + Options MTM + Backbone Unification (Phases 98–108) — SHIPPED 2026-07-15</summary>

Full phase detail archived at `milestones/v1.10-ROADMAP.md`. Requirements: `milestones/v1.10-REQUIREMENTS.md`. Audit: `milestones/v1.10-MILESTONE-AUDIT.md`.

- [x] Phase 98: Portfolio Data Foundation (3/3) — `[PI-07 + read layer]`
- [x] Phase 99: Exposure & Allocation Widgets (4/4) — `[PI-01/02/03]`
- [x] Phase 100: Optimizer Sleeve + Favorites + Notes + KPI Panel (4/4) — `[PI-04/05/06]`
- [x] Phase 101: Options MTM toggle (analytics/pnl_basis) (2/2) — `[MTM-01 core]`
- [x] Phase 102: Options MTM Factsheet + Composite + Regression (2/2) — `[MTM-01/02/03]`
- [x] Phase 103: MTM daily series → charts follow the toggle (5/5) — `[MTM-04; shared route]`
- [x] Phase 104: Cash daily series onto the backbone (2/2) — `[BB-01]`
- [x] Phase 105: Composite → the one CSV finalize route (5/5) — `[BB-02]`
- [x] Phase 105.1: Onboarding onto the backbone (2/2) — `[BB-02 part]`
- [x] Phase 106: Cutover — flip + delete legacy + janitor (10/10) — `[BB-03]`
- [x] Phase 107: Leverage as a dailies transform (3/3) — `[LEV-BB]`
- [x] Phase 108: Scenario-planner onto the backbone (2/2) — `[SCEN-BB]`

</details>

<details>
<summary>✅ v1.12 sFOX Verified Integration — Phases 118–123 — CLOSED as FOUNDATION (flag-OFF) 2026-07-19 · tag <code>v1.12</code> @ <code>92be47af</code> · full detail archived at <code>milestones/v1.12-ROADMAP.md</code> (expand for the in-line record)</summary>

## ✅ v1.12 sFOX Verified Integration (+ static-IP backend) — Phases 118–124 — CLOSED as FOUNDATION 2026-07-19

> **Closed as Foundation (flag-OFF), tag `v1.12` @ `92be47af` (v0.46.0.0, PR #623).** 118/119/120 shipped; 121–123 code-complete; 122 UI shipped. The remaining work is founder-gated go-live ops + open defects, re-homed to **v1.13 Infra** (`milestones/v1.13-infra-ROADMAP-SEED.md`): 121-03 Fly deploy+whitelist, 122-04 factsheet e2e defect, 123 FLIP go-live (cutover/enqueue/cron + E2 validation), 124 deribit `correction`. The detail below is preserved as the milestone record.

**Milestone goal:** Add a LIVE sFOX exchange adapter that reads the actual account (`api_verified` ground truth — defeating CSV fabrication), backed by a Fly.io static-IP egress so IP-whitelisted sFOX keys work; and burn down the two backbone follow-ups parked at v1.11 close (the rolled-back derived-allocator-equity FLIP, root-caused; the deribit `correction` txn-type classification).

**Why (founder, load-bearing):** CSV track records are `self_reported` — a submitter can fabricate the numbers. A live API read of the real sFOX account is `api_verified` ground truth they can't fake. That trust distinction (the Phase-111 provenance tiers) IS the point of the big adapter over the trivial CSV path. sFOX is NOT in ccxt → a custom non-ccxt adapter. sFOX keys can be IP-whitelisted → the Fly static-IP egress is load-bearing, not optional.

**Governing principles:** live API read = `api_verified` ground truth; dailies-canonical backbone — sFOX balances/trades → daily returns → the ONE `derive_basis_series` backbone (no parallel metrics path); fail-loud, no invented data. flyctl is NOT installed locally → founder runs `fly deploy` + provisions/whitelists the IP (ops gates, not code blockers).

**Build order:** 118 → 119 → 120 → 121 → 122 is the sFOX spine (founder-locked, 5 distinct phases — do not collapse). 123 (FLIPRETRY) and 124 (DERIBITFIX) are independent burn-down with no sFOX dependency and can interleave. Phase numbering continues from v1.11 (Phase 117) — never restart.

### Phases

- [ ] **Phase 118: SFOX Research + adapter contract** — Resolve the real unknown (can daily equity be reconstructed from what sFOX exposes?) + the non-ccxt `SfoxClient` contract + a green sandbox smoke test
- [x] **Phase 119: SFOX Read adapter + key validation + DB constraint-widen** — Read-only balance/trades/txns adapter, `sfox` accepted at worker validate/encrypt + all 3 Vercel key routes, ≥5-CHECK constraint-widening migration (TEST project first) (completed 2026-07-18)
- [x] **Phase 120: SFOX Equity reconstruction + backbone** — sFOX → daily returns → unified backbone with the `api_verified` stamp + live ground-truth parity (completed 2026-07-19)
- [ ] **Phase 121: SFOX Static-IP egress (Fly.io)** — $2/mo dedicated-v4 tinyproxy in `ams`, worker proxy wiring, egress IP verified == dedicated v4 before whitelisting
- [ ] **Phase 122: SFOX Add-key UI + e2e** — Exchange picker + onboarding copy, `api_verified` badge renders, e2e all roles
- [ ] **Phase 123: FLIPRETRY — derived-allocator-equity FLIP retry (root-caused)** — Hard per-crawl timeout + batched/off-hours backfill on its OWN worker + re-scheduled cron + E2_GROUND_TRUTH validation before the data-driven flip
- [ ] **Phase 124: DERIBITFIX — deribit `correction` txn-log classification** — Evidence-based classify so a correction-bearing deribit account ingests; unknown types still fail loud

## Phase Details

### Phase 118: SFOX Research + adapter contract

**Goal**: The genuinely-unknown sFOX questions are answered with evidence and a working `SfoxClient` adapter contract is proven against the sandbox — before any prod wiring exists.
**Depends on**: Nothing (first v1.12 phase)
**Requirements**: SFOX-01
**Success Criteria** (what must be TRUE):

  1. The load-bearing unknown — *can daily equity be reconstructed from what sFOX actually exposes* (balance/trades/transaction endpoints, historical depth, granularity, pagination) — is answered in this phase's RESEARCH.md with an explicit, evidence-cited go / adjust-scope determination; if reconstruction is NOT possible from the exposed data, that surfaces here as a scope decision, never papered over downstream.
  2. A `SfoxClient` adapter contract exists (auth scheme — HTTP Basic / API-token; endpoint set for balances, trades, transactions; rate-limit/pagination behavior; prod `api.sfox.com` vs sandbox `api.staging.sfox.com` base URLs) that coexists with the ccxt-typed `EXCHANGE_CLASSES` seam without pretending sFOX is a ccxt exchange.
  3. A sandbox-key smoke test runs GREEN against `api.staging.sfox.com` — authentication succeeds and at least one read endpoint returns real payload data — proving the contract against the live sandbox, not just docs.

**Plans:** 1/2 plans executed

Plans:
- [x] 118-01-PLAN.md — `SfoxClient` (non-ccxt, aiohttp, Bearer, 4 read endpoints, proxy seam, rate gate) + mocked contract unit tests
- [x] 118-02-PLAN.md — skipIf-gated live sandbox smoke test (SC-3, founder-gated) + phase-gate regression sweep — DONE (commit `b5eddfe5`; skips green in CI, SC-3 human_needed)

### Phase 119: SFOX Read adapter + key validation + DB constraint-widen

**Goal**: A user can connect an sFOX API key end-to-end — the read adapter pulls the account, every validation/encryption chokepoint accepts `sfox`, and the database admits `'sfox'` everywhere an exchange value is constrained.
**Depends on**: Phase 118 (adapter contract + sandbox-proven endpoints)
**Requirements**: SFOX-02, SFOX-03, SFOX-04
**Success Criteria** (what must be TRUE):

  1. `SfoxClient` reads an sFOX account's balances, trades, and transactions, read-only ENFORCED — key-permission scope is asserted at the ingestion boundary and no order/withdraw capability is ever exercised (financial-action safety rule).
  2. A user can connect an sFOX API key through ALL THREE key routes (`api/keys/validate-and-encrypt`, `api/strategies/create-with-key`, `api/strategies/composite/add-key`); the worker `validate_key`/`encrypt_key` path accepts `sfox` + `key_permissions` (credentials trimmed at the shared chokepoint, per the v1.11 dogfood fix).
  3. An invalid or insufficient-permission sFOX key fails CLOSED with honest copy (the shared `classifyKeyValidationError` → `KEY_AUTH_FAILED` pattern) — never a false-verified, never a "trading permissions" mislabel.
  4. The database accepts `'sfox'` at every hardcoded `exchange IN (...)` CHECK (≥5: initial_schema, position_snapshots, strategies-source-csv, funding_fees, terminal-status) via ONE RED-guarded constraint-widening migration, MCP-applied to the TEST project (`qmnijlgmdhviwzwfyzlc`) BEFORE merge (auto-applies to prod on merge, founder-watched); a still-invalid exchange value is still rejected (constraints widened, not dropped).

**Plans**: 4 plans

Plans:
- [x] 119-01-PLAN.md — Constraint-widen migration + 13-site lockstep allowlists + RED SQL guard + [BLOCKING] MCP-apply-to-TEST checkpoint (SFOX-04)
- [x] 119-02-PLAN.md — Worker is_sfox validate branch (non-ccxt, SfoxClient, honest KEY_AUTH_FAILED mapping) (SFOX-03)
- [x] 119-03-PLAN.md — Q1 secret carve-out in the 3 key routes (api_secret optional for sfox ONLY; ccxt unchanged, security-flagged) (SFOX-03)
- [x] 119-04-PLAN.md — Read pull (balances+trades+transactions via SfoxClient, read-only at ingestion boundary) + founder-gated live-read leg (SFOX-02)

### Phase 120: SFOX Equity reconstruction + backbone

**Goal**: An ingested sFOX account becomes an `api_verified` daily-return series on the ONE unified backbone, validated against live account ground truth.
**Depends on**: Phase 119 (a connected sFOX key with readable balances/trades/txns). The SFOX-06 LIVE ground-truth run on an IP-whitelisted key additionally gates on Phase 121 (static egress); a non-whitelisted key or sandbox parity fixture carries the code-complete gate until then.
**Requirements**: SFOX-05, SFOX-06
**Success Criteria** (what must be TRUE):

  1. sFOX balances + trades reconstruct into a daily-return series that flows through the ONE unified backbone (`derive_basis_series`) — no parallel metrics path, no sFOX-special derive chain; scalars/charts/coverage all derive from the same dailies.
  2. The reconstructed sFOX strategy carries the `api_verified` provenance stamp (the Phase-111 trust tier), distinct from `csv`/`self_reported`.
  3. Degenerate input (empty account, <10 days, non-finite returns) renders an honest empty/gated state — never invented data.
  4. ⚠️ The reconstructed equity is validated against a live sFOX account's ground truth (anchor-consistency / parity check, the `deribit_ground_truth.py` pattern); a material divergence FAILS LOUD — the wrong curve is never displayed. _(Live run against a whitelisted prod key is founder-gated on Phase 121's egress; the committed harness + fixture gates carry the phase until that op runs.)_

**Plans**: 4 plans

Plans:
- [x] 120-01-PLAN.md — SfoxAdapter (compute_metrics fail-loud) + lockstep Source/registry registration (resolves the 119 deferral) ✅ 2026-07-18
- [x] 120-02-PLAN.md — combine_sfox_balance_history (chain_linked_twr reuse, P115 hand-derived oracle) + bounded crawls/flows in sfox_read ✅ 2026-07-19
- [x] 120-03-PLAN.md — job_worker `elif venue=="sfox"` branch + sfox preflights/aclose + F2/F7 router seams + CRYPTO_VENUES + api_verified proof ✅ 2026-07-19
- [x] 120-04-PLAN.md — scripts/sfox_ground_truth.py + fixture parity tests + founder-gated live leg (human_needed)

### Phase 121: SFOX Static-IP egress (Fly.io)

**Goal**: All sFOX (and optionally ccxt) worker API traffic egresses from one verified static IPv4, so an IP-whitelisted sFOX key authenticates.
**Depends on**: Phase 119 (the `SfoxClient` aiohttp session + worker ccxt sessions exist to wire the proxy into). Gates the live-whitelisted-key path for Phase 120's SFOX-06 ground-truth run and Phase 122's live e2e.
**Requirements**: SFOX-07
**Success Criteria** (what must be TRUE):

  1. Deploy-ready Fly.io artifacts exist and are founder-runnable: Dockerfile + `fly.toml` + `tinyproxy.conf` for a $2/mo dedicated-IPv4 tinyproxy (CONNECT-443) in `ams` (geo-allowed region matching today's Amsterdam egress — never US).
  2. The actual egress IP, measured FROM the deployed machine (`curl ipinfo.io` through the proxy), is VERIFIED equal to the Fly dedicated v4 BEFORE the founder whitelists anything at sFOX — never whitelist on assumption (Fly shared/v6 egress ≠ the dedicated v4 by default).
  3. Worker wiring routes BOTH transports through the proxy: ccxt exchanges via `aiohttp_proxy`, and `SfoxClient`'s aiohttp session via an EXPLICIT proxy argument (aiohttp ignores `HTTPS_PROXY` without `trust_env` — env-var-only wiring is a silent no-op).
  4. ⚠️ An IP-whitelisted sFOX key authenticates through the proxy path end-to-end (the proof the whole phase exists for).

**Ops gates (founder-run, prerequisites NOT code blockers):** flyctl is not installed locally — the founder runs `fly deploy`, provisions BOTH IPs (dedicated inbound v4 ~$2/mo AND static egress ~$3.60/mo — the 2026-07-19 research correction: the EGRESS ip is what sFOX whitelists, ≈$5.60/mo total), and performs the sFOX IP-whitelist after criterion 2's verification. I generate all artifacts + the verification procedure.
**Plans**: 3 plans

Plans:
- [x] 121-01-PLAN.md — Fly proxy deployable: fly-egress-proxy/ (tinyproxy image, secret-rendered BasicAuth conf, ams fly.toml, corrected dual-IP founder runbook)
- [x] 121-02-PLAN.md — Worker wiring: WORKER_EGRESS_PROXY_URL → make_sfox_client at all 4 sites + opt-in ccxt aiohttp_proxy; probe --expect fail-loud gate; UNSET = byte-identical (tested) — commits cf3fb3fc / 17658668 / dd70d389; full suite 4010 passed
- [ ] 121-03-PLAN.md — Founder ops checkpoint (human-action): deploy, allocate both IPs, verify realized egress == static egress IP, THEN whitelist at sFOX

### Phase 122: SFOX Add-key UI + e2e

**Goal**: sFOX is a first-class exchange in the add-key experience, and the `api_verified` trust distinction is visible and e2e-proven across roles.
**Depends on**: Phase 119 (key routes accept `sfox`), Phase 120 (`api_verified` series exists to badge), Phase 121 (live whitelisted-key flow for any live-path e2e)
**Requirements**: SFOX-08, SFOX-09
**Success Criteria** (what must be TRUE):

  1. The add-key wizard's exchange picker exposes sFOX with correct onboarding / setup-guide copy: how to mint a read-only sFOX token and the static whitelist IP to enter (per DESIGN.md; no dead-end or disabled affordance — blocked states get a clickable remedy).
  2. A connected sFOX strategy renders the `api_verified` provenance badge on its factsheet/discovery surfaces — visually distinct from `csv`/`self_reported` ground truth tiers.
  3. An e2e test across ALL user roles (allocator / strategy-manager / admin / public / demo) asserts the sFOX strategy + its `api_verified` badge render (seeded fixtures owned by the logged-in user; FLOW-01 dual-wired into CI).
  4. An invalid-key attempt in the wizard shows the honest fail-closed copy from Phase 119 (no false-verified path reachable from the UI).

**Plans**: 4 plans

Plans:
- [x] 122-01-PLAN.md — SFOX 3-letter tag (both tag maps, unconditional) + VerifiedBadge sfox api_verified coverage + F6 canonical-lowercase insert (both client sites)
- [x] 122-02-PLAN.md — flag-gated sFOX offer: NEXT_PUBLIC_SFOX_ENABLED in closed-sets (OFF=byte-identical, tested) + wizard picker cards (token-only, F3-honest) + sfox-aware ApiKeyForm
- [x] 122-03-PLAN.md — /security#sfox-readonly setup guide (mint READ-ONLY token, whitelist static egress IP via contact channel, no hardcoded IP)
- [x] 122-04-PLAN.md — seed-gated e2e: SFOX tag + api_verified badge across owner/allocator/admin + axe (public-demo N/A), seedSfoxVerifiedStrategy helper
**UI hint**: yes

### Phase 123: FLIPRETRY — derived-allocator-equity FLIP retry (root-caused)

**Goal**: The v1.11-rolled-back derived-allocator-equity flip retries safely — the root cause (a hanging live exchange crawl blocking the sequential prod worker's event loop) is structurally fixed BEFORE any re-enqueue, and the derived curve only ever displays after ground-truth validation.
**Depends on**: Nothing in the sFOX spine (independent burn-down; builds on the shipped v1.11 Phase 115/115.1 derivation core + `allocator_equity_derived` surface)
**Requirements**: FLIPRETRY-01, FLIPRETRY-02, FLIPRETRY-03, FLIPRETRY-04
**Success Criteria** (what must be TRUE):

  1. Every derived-equity exchange crawl is bounded by a hard `asyncio.wait_for` per-crawl timeout — a slow/hanging live crawl (deribit native ledger ~inception; bybit 19k rows) can NEVER block the worker event loop, proven by a regression test where a deliberately-hung crawl times out and the worker stays live.
  2. The key-mode derived-equity backfill runs batched / off-hours on its OWN worker (never the sequential prod worker's loop), and the cron is re-scheduled (`cron.schedule('derive-allocator-key-dailies', '30 5 * * *')`).
  3. ⚠️ A derived curve is displayed ONLY after it passes `E2_GROUND_TRUTH_*` anchor-consistency validation; `extractTrustworthyDerivedCurve` (`src/lib/queries.ts` — the flip is data-driven, no flag) flips `equityCurveSource` to derived only for a trustworthy curve, else the legacy basis renders unchanged.
  4. Worker health is preserved throughout backfill (healthz never stale past the restart threshold); the enqueue is idempotent/safe to re-run; the rollback is documented and executable (delete flip jobs + empty `allocator_equity_derived` + unschedule cron — the exact v1.11 recovery).

**Ops gates (founder-run, prerequisites NOT code blockers):** the `E2_GROUND_TRUTH_*` env (read-only exchange key in Railway) + the `e2_allocator_ground_truth.py` live acceptance run, and the production backfill enqueue itself, are founder-gated ops. Code criteria 1/2/4 land and are testable without them.
**Plans:** 1/3 plans executed

Plans:
- [x] 123-01-PLAN.md — FLIPRETRY-01: wait_for-bound the deribit cash-pass + ccxt crawls (mirror sfox) + defensive reconstruct wrap; fake-hang transient tests (wave 1)
- [x] 123-02-PLAN.md — FLIPRETRY-02/04: kind-filtered claim RPC migration (TEST-first) + WORKER_CLAIM_ROLE worker + per-job LAST_TICK_AT refresh + fan-out idempotency SQL gate (wave 1)
- [x] 123-03-PLAN.md — FLIPRETRY-03/04: E2 anchor + derived↔legacy flip fixtures, go-live+rollback runbook, founder checkpoint (deploy/cutover/pilot/LIVE E2/enqueue/cron) (wave 2, autonomous: false) — committed gates DONE (be474b9d test, ef8c53a3 runbook); Task 3 founder checkpoint human_needed-OPEN (live E2 + prod backfill + cron reschedule)

### Phase 124: DERIBITFIX — deribit `correction` txn-log classification

**Goal**: A deribit account containing a `correction` txn-log entry ingests cleanly with correct equity — classified on evidence of what deribit `correction` means, while genuinely-unknown types keep failing loud.
**Depends on**: Nothing (independent burn-down; touches the deribit native-ledger ingest shipped in v1.8 Phase 80)
**Requirements**: DERIBITFIX-01, DERIBITFIX-02
**Success Criteria** (what must be TRUE):

  1. The classification of `correction` (CASH_BEARING vs INFORMATIONAL) is made on recorded EVIDENCE — deribit documentation and/or the real Railway key3 ledger entry that surfaced the bug — never a guess; the evidence and determination are written down with the change.
  2. A deribit account containing a `correction` entry (the blocked key3) ingests without raising `LedgerValuationError`, and the resulting equity is correct for the chosen classification.
  3. A regression fixture with a `correction`-bearing ledger proves ingest succeeds with correct equity (fails without the fix).
  4. A genuinely-unknown future txn-log type STILL fails loud (`LedgerValuationError`) — the fix classifies `correction` specifically, it does not silently absorb the unknown class (no corruption path opened).

**Plans**: TBD

## Progress

**Execution Order:** sFOX spine in numeric order 118 → 119 → 120 → 121 → 122 (121's founder ops can start in parallel once 119 lands; 120's live ground-truth closes after 121). 123 and 124 are independent and may interleave anywhere.

| Phase | Milestone | Plans | Status | Completed |
| ----- | --------- | ----- | ------ | --------- |
| 109–117 | v1.11 | 42/42 | ✅ Complete | 2026-07-18 |
| 118. SFOX Research + adapter contract | v1.12 | 2/2 | Plans done — SC-3 human_needed |  |
| 119. SFOX Read adapter + validation + DB widen | v1.12 | 4/4 | Complete   | 2026-07-18 |
| 120. SFOX Equity reconstruction + backbone | v1.12 | 4/4 | Complete   | 2026-07-19 |
| 121. SFOX Static-IP egress (Fly.io) | v1.12 | 1/3 | In progress | 121-01 (fly-egress-proxy/ deployable) |
| 122. SFOX Add-key UI + e2e | v1.12 | 3/4 | In Progress|  |
| 123. FLIPRETRY (root-caused flip retry) | v1.12 | 2/3 | In Progress|  |
| 124. DERIBITFIX (`correction` classify) | v1.12 | 0/TBD | Not started | - |

Next milestone phases continue from **Phase 125** (never restart numbering).

</details>

## 🚧 v1.13 — Infra: sFOX go-live + worker rebuild (Phases 125–130) — ACTIVE (started 2026-07-19)

**Milestone goal:** Take the v1.12 sFOX foundation LIVE (`api_verified` ground truth a submitter can't fabricate) and rebuild the worker architecture the v1.11 FLIP rollback proved is load-bearing — so the derived-allocator-equity flip retries safely and the sFOX flag flips on.

**Requirements:** `.planning/REQUIREMENTS.md` (17 v1 reqs — EGRESS/WORKER/FACTSHEET/E2GT/DERIBITFIX/FLIP/GOLIVE, all mapped). Seed: `milestones/v1.13-infra-ROADMAP-SEED.md`. Runbook: `docs/runbooks/flipretry-derived-equity-go-live.md`.

**⭐ Egress restructure (founder decision 2026-07-19, supersedes the seed's Fly phase):** egress goes through **Railway Pro static-outbound IPs** — Fly is FULLY DROPPED (`fly-egress-proxy/` unused, fallback only; `WORKER_EGRESS_PROXY_URL` stays UNSET — the worker already egresses natively from Amsterdam, covering sFOX AND ccxt with NO code). EGRESS-01/02/03 are therefore a founder-ops gate + a small egress-stability verification with NO build work — they FOLD INTO the GOLIVE phase (130) instead of owning a phase. ⚠️ Railway assigns a FIXED SET OF 3 static outbound IPs (load-balanced) — ALL 3 must be whitelisted at sFOX; the whitelist is a founder action started **day 1 in parallel** while phases 125–129 execute.

**🧱 Groundwork already landed in v1.12 — FINISH/PROVE-LIVE it, never re-plan it:** `asyncio.wait_for` crawl bounds (123-01); the kind-filtered claim RPC migration `20260719073701` (**prod-applied**) + `WORKER_CLAIM_ROLE` (123-02); E2 anchor/flip fixtures + the go-live/rollback runbook (123-03). (The Fly proxy artifacts + worker proxy wiring from 121-01/121-02 remain in-tree but UNUSED — fallback only.)

**Dependency spine:** Phase 125 (WORKER) MUST land before Phase 129 (FLIP) — it is what makes the retry safe. Phases 126 (FACTSHEET), 127 (E2GT), 128 (DERIBITFIX) are parallel go-live gates; 128 is fully independent (deribit ingest, no sFOX dependency). Phase 129 (FLIP) depends on 125 + 127. Phase 130 (GOLIVE) is the culmination — LAST — depending on 126 + 127 + the egress whitelist (founder-gated, kicked off day 1 in parallel so the external sFOX IP-bind long-pole overlaps 125–129).

**Ops/evidence gates (⚠️ founder-run — modeled as `human_needed` legs INSIDE their phases; code criteria land + are testable without them, and a skipped gate is NEVER claimed done):** EGRESS-01/02/03 (Railway dashboard IP-set read, egress probes, sFOX whitelist — inside Phase 130, started day 1), E2GT-01 (Railway `E2_GROUND_TRUTH_*` env + live run), DERIBITFIX-01 (evidence of what deribit `correction` means), FLIP-01 (prod backfill enqueue), GOLIVE-01 (flag flip). The WORKER-03 cron reschedule + the GOLIVE-01 flag flip are founder LIVE ops, NOT migrations (auto-apply + a skipped worker deploy would re-wedge).

### Phases

- [x] **Phase 125: WORKER — dedicated backfill worker + retention hygiene** - Backfill on its OWN worker (claim-role split proven live), hung-crawl bounds proven end-to-end, off-hours batching + cron reschedule, orphaned-job retention purge killing the test-DB CI flake (completed 2026-07-19)
- [x] **Phase 126: FACTSHEET — connected-key api_verified factsheet render + blocking e2e** - Root-cause the factsheet-specific throw, graceful degrade, `sfox-badge.spec.ts` green in the blocking `frontend` gate (completed 2026-07-19)
- [ ] **Phase 127: E2GT — E2 ground-truth live validation + display gate** - `e2_allocator_ground_truth.py` passes live anchor-consistency; a derived curve displays ONLY when trustworthy
- [x] **Phase 128: DERIBITFIX — deribit `correction` classification (evidence-based)** - Classify on evidence so a correction-bearing account ingests with correct equity; unknown types still fail loud ✅ commit `001b00de` (per-row info.reason gate)
- [ ] **Phase 129: FLIP — derived-curve FLIP retry on the hardened worker** - Production backfill enqueue on the dedicated worker after E2 passes, healthz never wedges, rollback executable
- [ ] **Phase 130: GOLIVE — Railway static-egress whitelist + sFOX flag flip + live proof** - All 3 Railway static IPs whitelisted at sFOX (founder ops, day-1 parallel), realized egress verified in-set, gates green → founder flips the flags; a live IP-whitelisted key connects end-to-end and renders `api_verified` across surfaces and roles

## Phase Details (v1.13)

### Phase 125: WORKER — dedicated backfill worker + retention hygiene

**Goal**: A derived-equity backfill can NEVER wedge live analytics — it runs bounded, batched, off-hours, on its OWN worker — and the test-DB compute_jobs pollution flake is killed at its root.
**Depends on**: Nothing (first v1.13 phase). 🧱 Finishes v1.12 groundwork: 123-01 `asyncio.wait_for` crawl bounds + 123-02 kind-filtered claim RPC (`20260719073701`, prod-applied) + `WORKER_CLAIM_ROLE` — this phase deploys/proves the split live, it does not re-implement it. MUST land before Phase 129 (what makes the FLIP retry safe).
**Requirements**: WORKER-01, WORKER-02, WORKER-03, WORKER-04
**Success Criteria** (what must be TRUE):

  1. Derived-equity backfill jobs are claimed ONLY by a dedicated worker (`WORKER_CLAIM_ROLE` + the kind-filtered claim RPC); the sequential prod worker structurally cannot claim them — a slow backfill can never block live analytics' event loop (WORKER-01).
  2. A deliberately-hung exchange crawl times out via the `asyncio.wait_for` bound END-TO-END on the worker (not just unit-level), the worker stays live, and per-job `LAST_TICK_AT` refresh keeps healthz honest through a long backfill (WORKER-02).
  3. The key-mode backfill runs batched / off-hours, and the cron reschedule (`cron.schedule('derive-allocator-key-dailies', '30 5 * * *')`) is executed as a founder LIVE SQL op with a written runbook step — NEVER a migration (auto-apply + a skipped worker deploy would re-wedge prod) (WORKER-03).
  4. Orphaned `running` compute_jobs are purged on a retention schedule (test project nightly + a prod safety sweep) and the recurring `python` fence-test CI flake stops re-firing — verified against the workerless test project the `derive-allocator-key-dailies` cron re-pollutes daily (WORKER-04).

**Plans:** 4/4 plans complete

Plans:
- [x] 125-01-PLAN.md — WORKER-04 code: orphaned-`running` purge cron migration (2h window) + RED-guarded behavioral SQL test (wave 1) ✅ 2026-07-19 (commits 5fda9b86 test, ee79d5f4 migration; NOT yet MCP-applied → 125-03)
- [x] 125-02-PLAN.md — WORKER-02 end-to-end proof: real dispatch_tick + heartbeat + real healthz TCP server; hung crawl → bounded transient, worker stays live; role-payload disjointness (wave 1) ✅ 2026-07-19 (commits 51bbc4dc healthz-TCP, d0c8791a hung-crawl+roles; `tests/test_worker_isolation_e2e.py`, 4 tests, zero prod diff; ⚠️seam finding: anchor-read hang classifies 'unknown' not 'transient' — threat_flag in SUMMARY)
- [x] 125-03-PLAN.md — WORKER-04 runtime: [BLOCKING] MCP-apply migration to TEST + one-time orphan cleanup + fence-test proof + human checkpoint (wave 2, autonomous: false) ✅ 2026-07-19 (migration `20260719120000` applied+version-corrected on TEST `qmnijlgmdhviwzwfyzlc`, cron `15 4 * * *` registered, guard asserts green, one-time cleanup no-op; fence proof CI-deferred; prod auto-applies at merge)
- [x] 125-04-PLAN.md — WORKER-01/03: runbook env-contract/topology/ordering extension + founder LIVE cutover checkpoint (reschedule leg gated on Step 6, may stay OPEN) (wave 2, autonomous: false) ✅ 2026-07-19 (commit 1f50b60b; runbook extended in place — Step 0 topology-confirm + dashboard env contract, Step 1 backfill-service env block + replicas/fail-loud facts, Phase-125 retention/ordering subsection; Step 6 byte-unchanged. ⚠️WORKER-01 cutover + WORKER-03 reschedule recorded human_needed-OPEN, NOT claimed done)

### Phase 126: FACTSHEET — connected-key api_verified factsheet render + blocking e2e

**Goal**: The `/strategy/[id]` factsheet renders (or degrades gracefully) for an `api_verified` + api_key-linked sFOX strategy, and the proof is a BLOCKING e2e gate — a hard prerequisite for the flag flip.
**Depends on**: Nothing (parallel go-live gate; the defect + the seed-gated spec landed at v1.12 122-04 — the spec is currently RED/advisory). Gates Phase 130 (GOLIVE).
**Requirements**: FACTSHEET-01, FACTSHEET-02
**Success Criteria** (what must be TRUE):

  1. The factsheet-specific throw on the connected-key `api_verified` render path is ROOT-CAUSED via a seeded local repro (the same strategy's edit-page tag + browse badge render fine, so seed/tier are correct) and fixed at the source — never suppressed or worked around (FACTSHEET-01).
  2. `/strategy/[id]` renders for the `api_verified` + api_key-linked sFOX strategy — or degrades gracefully with an honest state when the connected-key provenance render fails — with a regression test proving degrade-not-throw (this also hardens prod against a transient analytics outage) (FACTSHEET-01).
  3. `e2e/sfox-badge.spec.ts` is GREEN across all roles (owner / allocator / admin) including axe, and is wired into the BLOCKING `frontend` branch-protection gate — no longer advisory (FACTSHEET-02).

**Plans**: 3 planned + 1 hardening follow-up (126-04)

Plans:
- [x] 126-01-PLAN.md — Wave 0 seeded repro RAN: DISPROVED the SSR-throw premise (200, no throw). Pinned root cause = strategy_verifications RLS owner-only → api_verified badge invisible to non-owner viewers (anon + admin). Fixed at source: service-role `trust_tier`+`status` projection (founder Option B, no RLS widening) + `<main>` landmark + RED-proven regression tests. All 5 sfox-badge legs green vs TEST. (FACTSHEET-01) ✅
- [ ] 126-02-PLAN.md — ⚠️ RE-SCOPE NEEDED: the `unstable_catchError` VerificationBoundary premise is VOID (there is no SSR throw to catch; fail-soft null-tier is the degradation). The Option A `<main>` landmark already landed in 126-01. Remaining candidate work: same-class fix for `getStrategyDetail`/`getFactsheetDetail` if their non-owner surfaces should show the badge. (FACTSHEET-01)
- [x] 126-03-PLAN.md — `e2e-seeded` wired BLOCKING into the `frontend` aggregator (skipped=pass for that row ONLY; all other rows strict) + an anti-tamper grep guard inside e2e-seeded so the gate can't be false-greened by gutting the sfox-badge anti-mask legs (admin + anon non-owner badge-visible + owner axe). Spec unmodified — the anti-mask assertions already landed in 126-01; the stale 126-02 boundary assertions were dropped (no boundary exists). 4-leg green proof CI-DEFERRED to the now-blocking e2e-seeded job (verify via `gh pr checks` before FACTSHEET-02 is marked satisfied); 126-01/126-04 already recorded 5/5 green vs TEST. Commit `f1102ee5`. (FACTSHEET-02) ✅
- [x] 126-04-PLAN.md (hardening) — Replaced the 126-01 app-layer service-role trust_tier projection with a correct-by-construction DB primitive: migration 135 `get_published_trust_signals(uuid[])` (SECURITY DEFINER, published-gated, column-scoped, pinned search_path, anon EXECUTE; strategy_verifications stays owner-locked). `readPublicVerificationSignals` `.rpc()`s it (no createAdminClient); the two flagged allocations members (returns route + watchlist-read) repointed off owner-only RLS embeds → non-owner tier correct. 35 tests RED-proven; tsc+lint clean. Migration NOT applied (orchestrator → TEST post-audit). Commit `3d40d925`. (FACTSHEET-01) ✅
**UI hint**: yes

### Phase 127: E2GT — E2 ground-truth live validation + display gate

**Goal**: The derived allocator curve has its `api_verified` anti-fabrication anchor — it passes live anchor-consistency on real account data, and a curve is DISPLAYED only when trustworthy.
**Depends on**: Nothing to start (🧱 harness + hand-derived-oracle fixtures landed v1.12 123-03 — this is the live acceptance run + the display-gate proof, not a rebuild). Gates Phase 129 (FLIP) and Phase 130 (GOLIVE).
**Requirements**: E2GT-01, E2GT-02
**Success Criteria** (what must be TRUE):

  1. ⚠️ `e2_allocator_ground_truth.py` runs LIVE with the `E2_GROUND_TRUTH_*` env (a read-only exchange key in Railway — founder-provisioned) and PASSES anchor-consistency on real account data (E2GT-01).
  2. A derived curve is displayed ONLY after passing anchor-consistency: `extractTrustworthyDerivedCurve` (`src/lib/queries.ts` — data-driven, no flag) flips `equityCurveSource` to derived only for a trustworthy curve; an untrustworthy or absent curve leaves the legacy basis rendering unchanged, proven by test (E2GT-02).
  3. A failing anchor-consistency check FAILS LOUD and blocks the derived display — the wrong curve is never shown (no-invented-data invariant).

**Plans**: 127-01 ✅ (audit-then-fill + runbook) — coverage audit proved all 3 criteria already covered by v1.12 123-03 fixtures (no new test); runbook Step 4 sharpened to the two-part E2GT-01 gate (exit 0 AND `within_same_day_tolerance===true`). E2GT-02 SATISFIED; ⚠️ E2GT-01 (live run) stays `human_needed` — founder LIVE op, gates Phase 129/130.

### Phase 128: DERIBITFIX — deribit `correction` classification (evidence-based)

**Goal**: A deribit account containing a `correction` txn-log entry ingests cleanly with correct equity — classified on EVIDENCE of what deribit `correction` means — while genuinely-unknown types keep failing loud.
**Depends on**: Nothing (fully independent — deribit native-ledger ingest, no sFOX dependency; re-homes v1.12 Phase 124, never started).
**Requirements**: DERIBITFIX-01, DERIBITFIX-02
**Success Criteria** (what must be TRUE):

  1. ⚠️ The classification of `correction` (CASH_BEARING vs INFORMATIONAL) is made on recorded EVIDENCE — deribit documentation and/or the real Railway key3 ledger entry that surfaced the bug — never a guess; the evidence and the determination are written down with the change (DERIBITFIX-01, evidence-gated).
  2. The blocked deribit account (Railway key3, the one with a `correction` entry) ingests without raising `LedgerValuationError`, and the resulting equity is correct for the chosen classification (DERIBITFIX-01).
  3. A regression fixture with a `correction`-bearing ledger proves ingest succeeds with correct equity — and FAILS without the fix (DERIBITFIX-02).
  4. A genuinely-unknown future txn-log type STILL raises `LedgerValuationError` — the fix classifies `correction` specifically, never silently absorbing the unknown class (no corruption path opened) (DERIBITFIX-02).

**Plans**: 128-01 ✅ (commits `001b00de` per-row gate + `d410d3b7` WR-01) — all 4 criteria satisfied. `correction` gated PER ROW on `info.reason` (founder decision): a funding-calc correction (the key3 recrawl evidence) is realized cash, while a capital/unrecognized/missing-reason correction FAILS LOUD (never miscounted as trading performance). WR-01 (code review): capital-DENYLIST precedence + word-boundary matching + `mark` dropped, so a capital reason containing a trading substring ("transfer to funding account correction") still fails loud. Hand-derived USD+native money oracles + capital-reason/collision/unrecognized/unknown-type fail-loud guards, RED-proven in all neuter directions. Summary: `.planning/phases/128-deribitfix-deribit-correction-classification-evidence-based/128-SUMMARY.md`.

### Phase 129: FLIP — derived-curve FLIP retry on the hardened worker

**Goal**: The v1.11-rolled-back derived-allocator-equity flip is re-attempted SAFELY — the production backfill enqueue runs on the hardened dedicated worker after E2 validation, completes without wedging, and is trivially rollbackable.
**Depends on**: Phase 125 (the dedicated hardened worker — what makes the retry safe) + Phase 127 (E2 ground-truth green — the display gate the flip rides on).
**Requirements**: FLIP-01, FLIP-02
**Success Criteria** (what must be TRUE):

  1. ⚠️ The production backfill enqueue is re-attempted on the dedicated worker AFTER E2GT passes, and completes WITHOUT wedging — worker healthz never goes stale past the restart threshold throughout the backfill (the exact v1.11 failure mode, disproven live) (FLIP-01, founder-run op).
  2. The enqueue is idempotent / safe to re-run — a partial or repeated enqueue never double-processes or corrupts (FLIP-02).
  3. The rollback is documented AND executable per `docs/runbooks/flipretry-derived-equity-go-live.md` — delete flip jobs + empty `allocator_equity_derived` + unschedule the cron (the exact v1.11 recovery) (FLIP-02).
  4. Post-flip, derived curves reach users ONLY through the Phase 127 trustworthy gate — an untrustworthy curve leaves the legacy basis rendering (the flip is data-driven, no flag).

**Plans**: 1 plan

Plans:
- [ ] 129-01-PLAN.md — FLIP-02 audit (idempotent enqueue + Step-8 rollback executability, live pytest/vitest evidence) + display-gate intact check + FLIP-01 modeled as the founder live backfill-enqueue op (E2GT-gated, human_needed; never claimed without healthz-fresh-throughout evidence)

### Phase 130: GOLIVE — Railway static-egress whitelist + sFOX flag flip + live proof

**Goal**: sFOX becomes offerable and LIVE on native Railway egress — the full 3-IP static-outbound set is whitelisted at sFOX (NO Fly, NO proxy), the flags flip only once every gate is green, and a real user connects a live IP-whitelisted key that renders `api_verified` across every surface. Absorbs EGRESS (founder decision 2026-07-19: Railway Pro static outbound, zero build work — `WORKER_EGRESS_PROXY_URL` stays UNSET).
**Depends on**: Phase 126 (factsheet e2e green in the blocking gate) + Phase 127 (E2 ground-truth green) + the egress whitelist (⚠️ founder-gated external long-pole — EGRESS-01/02/03 ops START DAY 1 in parallel so the sFOX IP-bind wait overlaps phases 125–129). The milestone culmination — LAST.
**Requirements**: EGRESS-01, EGRESS-02, EGRESS-03, GOLIVE-01, GOLIVE-02
**Success Criteria** (what must be TRUE):

  1. ⚠️ Railway `quantalyze-analytics` is on Pro with static outbound IPs active, and the FULL 3-IP set (Amsterdam/NL) is read from the dashboard (service → Settings → Networking — redeploy the worker if static outbound activates on next deploy); ALL 3 IPs are whitelisted at sFOX (security@quantalyze.com / sFOX dashboard) — a founder op kicked off DAY 1 in parallel; never whitelist one observed IP, always the whole dashboard set (EGRESS-01, EGRESS-03 handoff).
  2. The worker's realized egress is VERIFIED to stay within the 3-IP static set via repeated `railway ssh … ipinfo.io` probes (country NL) BEFORE relying on the whitelist — a single probe misses load-balanced sibling IPs (EGRESS-02).
  3. ⚠️ An IP-whitelisted sFOX key authenticates end-to-end from the worker's NATIVE Railway egress — no proxy in the path (`WORKER_EGRESS_PROXY_URL` UNSET; native static egress covers BOTH sFOX AND ccxt); this gates the flag flip (EGRESS-03).
  4. ⚠️ The founder flips `SFOX_ENABLED` + `NEXT_PUBLIC_SFOX_ENABLED` on (LIVE env ops in Railway + Vercel, NOT migrations) ONLY after EGRESS (criteria 1–3), FACTSHEET (blocking e2e green), and E2GT are ALL verified green — the gate check is explicit, never assumed (GOLIVE-01).
  5. With the flag on, a user connects a live IP-whitelisted sFOX key end-to-end through the wizard and its `api_verified` strategy renders LIVE across factsheet + discovery + edit, proven E2E across roles (owner / allocator / admin) — the full flow, not one surface; rollback stays trivial (both flags back to empty restores the proven-safe dormant v1.12 state) (GOLIVE-02).

**Plans**: 1 plan

Plans:
- [x] 130-01-PLAN.md ✅ (commit `19a9b690`) — `docs/runbooks/sfox-go-live.md` DELIVERED (Steps 0–5: 3-IP set read → repeated NL probes → sFOX whitelist + native-egress key auth → EXPLICIT GOLIVE-01 gate → flag flip → api_verified live proof → trivial rollback; grep gate RUNBOOK-GATES-GREEN, zero prod touch, zero build work). Both founder checkpoints resolved PRE-DECIDED DEFER → EGRESS-01/02/03 + GOLIVE-01/02 human_needed-OPEN with the runbook as the execution path. Milestone ships FLAG-OFF.

## Progress (v1.13)

**Execution Order:** the Phase 130 EGRESS founder ops (dashboard IP-set read → egress probes → sFOX whitelist handoff) kick off DAY 1 in parallel — the external sFOX IP-bind long-pole ticks while 125–129 execute. 125 (WORKER) starts first as build work. 126 / 127 / 128 run in parallel as go-live gates (128 fully independent). 129 after 125 + 127. 130 LAST, after 126 + 127 + the whitelist.

| Phase | Milestone | Plans | Status | Completed |
| ----- | --------- | ----- | ------ | --------- |
| 118–123 | v1.12 | 18/18 code | ✅ Foundation close | 2026-07-19 |
| 125. WORKER — dedicated backfill worker + hygiene | v1.13 | 4/4 | Complete   | 2026-07-19 |
| 126. FACTSHEET — api_verified render + blocking e2e | v1.13 | 3/3 | Complete   | 2026-07-19 |
| 127. E2GT — ground-truth live validation + gate | v1.13 | 1/1 | ✅ Code done (E2GT-02); ⚠️ E2GT-01 live run human_needed | 2026-07-19 |
| 128. DERIBITFIX — `correction` classification | v1.13 | 1/1 | ✅ Complete | `001b00de`+`d410d3b7` |
| 129. FLIP — derived-curve FLIP retry | v1.13 | 1/1 | ✅ Complete (FLIP-02 verified; FLIP-01 human_needed) | 2026-07-19 |
| 130. GOLIVE — egress whitelist + flag flip + live proof | v1.13 | 1/1 | ✅ Complete (runbook `19a9b690`); EGRESS/GOLIVE human_needed, ships FLAG-OFF | 2026-07-19 |

**⭐v1.13 SHIPPED FLAG-OFF 2026-07-19** — merged PR #624 (v0.47.0.0, main @ `4b79891a`).
Both migrations (20260719120000 retention cron, 20260719140000 SECDEF trust-signal)
verified live on PROD; anon EXECUTE confirmed. Full specialist big-review done
(BL-01 money BLOCKER + observability + drift + comment fixes landed, all RED-proven).
Remaining founder LIVE ops recorded human_needed-OPEN (EGRESS-01/02/03, GOLIVE-01/02,
E2GT-01, FLIP-01, WORKER-01/03), execution path `docs/runbooks/sfox-go-live.md`,
gated on the external sFOX IP-bind long-pole.

## Deferred follow-ups (surfaced at v1.11 close 2026-07-18 — candidates for 118+)

- **Per-key stitch progress in the multi-key connect wizard** (founder dogfood finding 2026-07-18). The "Stitching your composite track record" screen shows one aggregate "Trades are being downloaded… Ns" line with NO per-key breakdown; founder expected per-key progress for a 3-key strategy. NOT an ADDALLOC/UIFIX criterion — new UI work. Wizard lives at `src/app/(dashboard)/strategies/new/wizard/`.
- **117 pixel/axe QA** (from v1.11-MILESTONE-AUDIT tech_debt): seeded /qa pass — tooltip pixel placement (overflow/edge/Dialog/drawer), focus-ring visual at 6 sites, axe route×viewport matrix on the authed/embedded surfaces (seed-gated) + an extreme high-leverage CUM RETURN value. Run at ship-time /qa when seed env is available.
- **Parked non-repro dup-key console warning** on the warming-up allocator Overview — non-functional; not reproducible in jsdom (ResponsiveContainer 0×0) or the current live state; needs the exact key string from a live warm-up-state console (likely EquityChart inner-SVG transient). See v1.11-MILESTONE-AUDIT.
- **Pre-existing lint warning** `react-hooks/exhaustive-deps` at `EquityChart.tsx:1119` (lint exits 0; on the derived-curve render seam).
- **quantstats price-detection Sharpe sign-flip on the strategy-analytics path** (deferred latent bug, founder decision needed — changes displayed numbers). See memory `project_quantstats_price_detection_sharpe_bug`.
- **Blend annualization of unknown-asset_class crypto leg at √252** (deferred latent bug from 109–113). See memory `project_blend_annualization_unknown_assetclass_optimistic`.
- **`allocator_equity_snapshots` legacy store retirement (STITCH-02 / BACKBONE-03)** — unblocked only after the v1.13 FLIP (Phase 129) is live + the derived surface is proven (explicitly out of scope for v1.13; see REQUIREMENTS.md → Out of Scope).
