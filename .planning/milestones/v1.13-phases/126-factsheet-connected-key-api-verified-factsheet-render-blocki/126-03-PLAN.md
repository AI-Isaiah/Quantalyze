---
phase: 126-factsheet-connected-key-api-verified-factsheet-render-blocki
plan: 03
type: execute
wave: 3
depends_on: ["126-01", "126-02"]
files_modified:
  - ".github/workflows/ci.yml"
  - "e2e/sfox-badge.spec.ts"
autonomous: true
requirements: [FACTSHEET-02]
must_haves:
  truths:
    - "On the seeded owner's HEALTHY factsheet render the note card renders and the degraded amber 'verification temporarily unavailable' fallback is NOT shown — so a genuine note bug cannot hide behind the 126-02 boundary and false-green the gate (W1 anti-mask)"
    - "e2e/sfox-badge.spec.ts is GREEN across owner (badge + axe zero-violations), allocator, and admin legs — proven locally with the seeded env OR explicitly CI-deferred to the e2e-seeded job with evidence, never claimed without one of the two"
    - "The frontend aggregator FAILS when e2e-seeded fails, PASSES when e2e-seeded succeeds, and PASSES when e2e-seeded is skipped (fork/unconfigured repos) — skipped tolerance scoped to the e2e-seeded row ONLY"
    - "All pre-existing aggregator rows still demand strict success (no loosening)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "e2e-seeded wired BLOCKING into the frontend aggregator"
      contains: "e2e-seeded"
    - path: "e2e/sfox-badge.spec.ts"
      provides: "Owner happy-path note-render assertion + no-degraded-fallback assertion (W1 anti-mask)"
      contains: "Your note"
  key_links:
    - from: ".github/workflows/ci.yml frontend job"
      to: "e2e-seeded job"
      via: "needs: entry + result-loop row with scoped skipped-as-pass"
      pattern: "needs\\.e2e-seeded\\.result"
---

<objective>
Make the sfox-badge e2e the BLOCKING go-live gate (FACTSHEET-02): wire the
`e2e-seeded` job into the `frontend` aggregator (the single required
branch-protection check — the real e2e gate per the v1.10 lesson) with
`skipped` treated as pass for that row ONLY, and prove all four spec legs green
(owner edit-tag, owner factsheet badge + axe, allocator browse badge, admin
factsheet tier) on top of the 126-01/126-02 fixes.

Purpose: the spec is currently RED/advisory; Phase 130's flag flip is gated on
this being green AND blocking.

Output: ci.yml aggregator edit + recorded green-proof (local seeded run or CI
e2e-seeded evidence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-01-SUMMARY.md
@.planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-02-SUMMARY.md
@.github/workflows/ci.yml
@e2e/sfox-badge.spec.ts
</context>

<interfaces>
<!-- Extracted this session from .github/workflows/ci.yml — exact edit targets. -->

```yaml
# frontend aggregator (ci.yml:615-660): branch protection gates on this single check.
#   needs: frontend-typecheck, frontend-lint, frontend-test, frontend-coverage,
#          frontend-policy, frontend-build          (:633-639)
#   if: always()
#   step "Verify all frontend-* jobs succeeded" (:642-660):
#     for r in "name=${{ needs.<name>.result }}" rows; do
#       if [ "$result" != "success" ]; then fail=1; fi     # ← strict row, keep for existing rows
#     done; [ "$fail" = "1" ] && exit 1
#
# e2e-seeded job (:1269-1303):
#   if: ${{ (github.event_name != 'pull_request' || head.repo == github.repository)
#           && vars.E2E_TEST_DB_CONFIGURED == 'true' }}    # ← resolves to result=skipped when unset
#   needs: [frontend-typecheck]                            # ← do NOT change
```

Failure math (RESEARCH Pitfall 2): with `if: always()` on the aggregator, a
failed `e2e-seeded` yields `needs.e2e-seeded.result == "failure"` → row fails →
exit 1 (BLOCKING, real on the main repo where E2E_TEST_DB_CONFIGURED=true). A
skipped `e2e-seeded` yields `"skipped"` → must NOT fail (fork PRs / unconfigured
repos), else every fork goes red.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire e2e-seeded into the frontend aggregator (skipped=pass for that row only)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml:615-660 — the frontend aggregator (needs list + result loop + the comment block explaining why `if: always()` + explicit exit 1 exist)
    - .github/workflows/ci.yml:1269-1303 — the e2e-seeded job header (its `if:` gate is WHY skipped must be tolerated; its `needs: frontend-typecheck` must not change)
    - .planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-RESEARCH.md — "Pitfall 2: e2e-seeded self-skip breaks the aggregator"
  </read_first>
  <action>
    Two surgical edits to .github/workflows/ci.yml, nothing else:

    (1) Add `- e2e-seeded` to the `frontend` job's `needs:` list (after
    `- frontend-build`, ci.yml:639).

    (2) Extend the "Verify all frontend-* jobs succeeded" loop with an
    `"e2e-seeded=${{ needs.e2e-seeded.result }}"` row, and give THAT ROW ONLY a
    skipped-tolerant check: inside the loop, branch on the row name — when
    `name` is `e2e-seeded`, fail only if result is neither `success` nor
    `skipped`; every other row keeps the existing strict
    `[ "$result" != "success" ]` check unchanged. Echo each row's result as the
    loop already does so the tolerance decision is visible in the log. Update
    the aggregator's leading comment block with one short sentence recording
    that e2e-seeded is now BLOCKING (Phase 126 / FACTSHEET-02) and why skipped
    is tolerated for that row (self-skip on E2E_TEST_DB_CONFIGURED unset —
    fork/unconfigured repos). Do NOT touch the e2e-seeded job's own `if:` or
    `needs:`; do NOT loosen any existing row; do NOT re-add lighthouse-mobile.
  </action>
  <verify>
    <automated>python3 -c "import yaml; wf=yaml.safe_load(open('.github/workflows/ci.yml')); needs=wf['jobs']['frontend']['needs']; assert 'e2e-seeded' in needs, needs; print('frontend.needs OK:', needs)"</automated>
  </verify>
  <acceptance_criteria>
    - The YAML parses and `frontend.needs` includes `e2e-seeded` (the automated python/yaml assertion exits 0).
    - `grep -c 'needs\.e2e-seeded\.result' .github/workflows/ci.yml` outputs 1 (exactly one new result-loop row).
    - The skipped tolerance is scoped: the loop's strict `!= "success"` branch still applies to all six pre-existing rows (read the diff — only the e2e-seeded branch mentions `skipped`), and `grep -c '"skipped"' .github/workflows/ci.yml` in the frontend job region matches only the new branch.
    - `git diff --stat` shows .github/workflows/ci.yml as the ONLY modified file, with no changes inside the e2e-seeded job body (diff hunks confined to the frontend aggregator).
  </acceptance_criteria>
  <done>frontend fails on e2e-seeded failure, passes on success, passes on skipped — with all other rows still strict.</done>
</task>

<task type="auto">
  <name>Task 2: Add the W1 anti-mask note-render assertion, then prove all legs green (local seeded run, else explicit CI-deferral)</name>
  <files>e2e/sfox-badge.spec.ts (add the owner happy-path note assertion — additive only)</files>
  <read_first>
    - e2e/sfox-badge.spec.ts — all four legs + the HAS_SEED_ENV gate (:54-56), the owner factsheet leg (:131-149), and the buildAxe zero-violations assertion (:147-148)
    - src/components/notes/StrategyNoteCard.tsx — the healthy-render anchor is the "Your note" heading (:51-53); this is what the happy-path assertion pins
    - src/app/strategy/[id]/VerificationBoundary.tsx (from 126-02) — the degraded fallback copy "Verification temporarily unavailable" is the NEGATIVE assertion target
    - .planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-01-SUMMARY.md — the Wave 0 outcome + classification (decides which proof path applies AND whether this assertion is the disambiguator per the repro-not-run branch)
    - .planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-02-SUMMARY.md — confirms the exact degraded-fallback copy string as shipped
    - playwright.config.ts — local webServer auto-start + baseURL
  </read_first>
  <action>
    (A) FIRST — add the W1 anti-mask assertion to the owner factsheet leg
    ("owner: the strategy factsheet shows the api_verified badge (+ axe)",
    sfox-badge.spec.ts:131-149). On the seeded owner's HEALTHY render the
    authed note sidecar must actually render, so assert BOTH: (1) the note
    card is present — `expect(page.getByText("Your note")).toBeVisible()` (the
    StrategyNoteCard heading, StrategyNoteCard.tsx:51-53), and (2) the degraded
    boundary fallback is NOT shown on the healthy path —
    `expect(page.getByText(/verification temporarily unavailable/i)).toHaveCount(0)`
    (the exact copy string as shipped in 126-02 — read 126-02-SUMMARY.md to
    confirm it verbatim; if 126-02 chose different copy, match it). This is
    ADDITIVE — do not weaken or remove any existing assertion. WHY this is the
    real safety net: the 126-02 boundary wraps the exact H1 suspect
    (StrategyNoteSection). Without this assertion, a GENUINELY-WRONG note bug
    would be silently degraded to the amber fallback and the badge-only legs
    would still go GREEN — false-greening FACTSHEET-01 while the note feature is
    permanently broken for every authed owner. The healthy-path note assertion
    forces the boundary to prove it is a transient-outage belt, not a bug mask
    (W1). It also RESTORES the disambiguator that the boundary otherwise
    defeats: post-boundary the page no longer throws, so "CI e2e throws again"
    can never fire — this assertion fires instead.

    (B) THEN — prove green. The repro CAN run: test creds live in the macOS
    Keychain (service `quantalyze-test`: alloc/sm/admin@quantalyze.test) and the
    test project is `qmnijlgmdhviwzwfyzlc` (per project memory). This is the
    EXPECTED path. Run the FULL spec `npx playwright test e2e/sfox-badge.spec.ts`
    with the test-project env exported (same recipe as 126-01 Task 1). All legs
    must pass — owner edit-tag, owner factsheet badge PLUS axe zero-violations
    PLUS the new note-render + no-degraded-fallback assertions (Pitfall 4: the
    owner leg must clear ALL of them — a badge-only pass is not green), allocator
    browse badge, admin factsheet tier. If any leg fails, this plan does NOT
    self-certify: capture the failure, and if it is the SSR throw resurfacing or
    the note card degrading on the healthy path, route back per the 126-01 gap
    protocol (a genuinely-wrong note bug that 126-01 classified transient, or
    left unpinned) rather than loosening the spec. Never weaken an assertion to
    pass.

    (C) IF the seeded env genuinely cannot be reached in-session (exception,
    not expectation): record EXPLICITLY in the SUMMARY that the local run did
    not execute and that final green proof — INCLUDING the new note-render
    assertion — is CI-DEFERRED to the now-blocking `e2e-seeded` job on this
    phase's PR (the main repo has E2E_TEST_DB_CONFIGURED=true, so the job runs
    there and the `frontend` aggregator now enforces it). The phase verify-work
    gate must then check the PR's e2e-seeded job result via `gh run view` /
    `gh pr checks` before FACTSHEET-02 is marked satisfied — name that
    follow-through in the SUMMARY. Never claim green without one of the two
    evidence forms (Rule 12). Reference secrets by name only.
  </action>
  <verify>
    <automated>npx playwright test e2e/sfox-badge.spec.ts 2>&1 | tail -15  # env present: 4 passed; env absent: 4 skipped — which triggers the mandatory CI-deferral record instead of a green claim</automated>
  </verify>
  <acceptance_criteria>
    - The owner factsheet leg contains BOTH new assertions: `grep -c "Your note" e2e/sfox-badge.spec.ts` outputs >=1 AND `grep -ci "verification temporarily unavailable" e2e/sfox-badge.spec.ts` outputs >=1 (the healthy-path note-present + no-degraded-fallback checks are wired).
    - The spec change is ADDITIVE ONLY — no existing assertion removed or loosened: `git diff -- e2e/sfox-badge.spec.ts` shows added lines only within the owner leg (no `-` lines deleting/relaxing an `expect`, verified by reading the diff).
    - SUMMARY contains EITHER a local run transcript tail showing all legs passed (including the owner leg's axe zero-violations AND the note-render + no-degraded-fallback assertions), OR an explicit "CI-deferred to e2e-seeded" section naming the missing env and the `gh pr checks` follow-through for verify-work. No third state.
    - No secret values in the SUMMARY (env vars referenced by name only).
  </acceptance_criteria>
  <done>The W1 anti-mask note-render assertion is wired into the owner leg (additive), and all legs are proven green locally, or the CI-deferral (including the new assertion) is honestly recorded with the enforcement path named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fork PR → CI secrets | e2e-seeded's existing composite `if:` withholds the job (and secrets) from fork PRs — must remain untouched |
| branch protection → merge | the frontend aggregator is the single required check; wiring must not create a skipped-satisfies-protection hole |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-21 | Elevation of Privilege (gate bypass) | frontend aggregator loop | mitigate | skipped-as-pass scoped to the e2e-seeded row ONLY; on the main repo E2E_TEST_DB_CONFIGURED=true so the job runs and failure → exit 1 → definite `failure` status branch protection honors |
| T-126-22 | Tampering | e2e-seeded job `if:`/`needs:` | mitigate | diff-confinement acceptance gate — no edits inside the job body; fork-PR secret withholding preserved |
| T-126-23 | Repudiation | green claim | mitigate | two-evidence-forms rule (local transcript OR CI job result); additive-only spec-diff gate blocks assertion-weakening |
| T-126-24 | Tampering (masked bug false-green) | 126-02 boundary wrapping the H1 suspect | mitigate | W1 anti-mask: the owner leg asserts the note card renders AND the degraded amber fallback is absent on the healthy path, so a genuinely-wrong note bug cannot hide behind the boundary and false-green FACTSHEET-01 |
</threat_model>

<verification>
- ci.yml parses; `frontend.needs` includes e2e-seeded; exactly one new result row with row-scoped skipped tolerance; diff confined to the aggregator.
- The W1 anti-mask note-render + no-degraded-fallback assertions wired into the owner leg (additive-only); grep gates pass.
- Four-leg green evidence recorded (local or CI-deferred with follow-through named).
</verification>

<success_criteria>
- FACTSHEET-02 satisfied: sfox-badge green owner/allocator/admin incl. axe, and the e2e-seeded job blocks the `frontend` branch-protection gate — advisory no more — without breaking fork/unconfigured-repo CI.
- W1 closed: the owner leg proves the note card renders on the healthy path (degraded fallback absent), so a masked genuine note bug cannot false-green FACTSHEET-01.
</success_criteria>

<output>
Create `.planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-03-SUMMARY.md` when done.
</output>
