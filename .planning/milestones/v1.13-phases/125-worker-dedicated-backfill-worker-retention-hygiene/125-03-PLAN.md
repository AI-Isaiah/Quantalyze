---
phase: 125-worker-dedicated-backfill-worker-retention-hygiene
plan: 03
type: execute
wave: 2
depends_on: ["125-01"]
files_modified: []
autonomous: false
requirements: [WORKER-04]

must_haves:
  truths:
    - "The purge migration is applied to the TEST project (qmnijlgmdhviwzwfyzlc) BEFORE merge, so the RED-guarded SQL test asserts (not skips) and CI cannot redden on test-DB lag"
    - "The schema_migrations version row on TEST matches the file timestamp 20260719120000 (MCP apply_migration stamps now() — drift must be fixed)"
    - "The TEST project holds ZERO orphaned running compute_jobs rows older than 1 hour after the one-time cleanup"
    - "The fence tests (test_compute_jobs_fencing.py, test_drain_semantics.py) pass against the cleaned TEST DB — the partition-dedupe collision source is gone"
    - "None of these runtime-state ops is claimed done without explicit verification — the human checkpoint confirms them"
  artifacts: []
  key_links:
    - from: "supabase/migrations/20260719120000_retention_orphaned_running_compute_jobs.sql"
      to: "TEST project qmnijlgmdhviwzwfyzlc cron.job"
      via: "Supabase MCP apply_migration"
      pattern: "retention_compute_jobs_orphaned_running"
    - from: "supabase/tests/test_retention_orphaned_running.sql"
      to: "TEST project (post-apply)"
      via: "psql -v ON_ERROR_STOP=1 — must now ASSERT, not NOTICE-skip"
      pattern: "retention_compute_jobs_orphaned_running"
---

<objective>
Apply the WORKER-04 runtime state: [BLOCKING] MCP-apply the purge migration to
the TEST project (mandatory BEFORE merge — the RED SQL guard test otherwise
reddens CI), run the one-time orphan cleanup that greens CI immediately, and
verify the fence-test flake source is gone. Ends with a human checkpoint so no
runtime-state op is ever silently claimed done.

Purpose: WORKER-04 activation on TEST + the one-time cleanup the planning
context mandates ALONGSIDE landing the recurring cron. (PROD gets the cron
automatically when the migration merges to main — founder-watched, standard
auto-apply; the 2h window is prod-safe by design.)
Output: no repo files — runtime state changes on TEST + verification evidence
in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-worker-dedicated-backfill-worker-retention-hygiene/125-RESEARCH.md

<interfaces>
<!-- Runtime facts, verified 2026-07-19 -->
- TEST project ref: qmnijlgmdhviwzwfyzlc (prod: khslejtfbuezsmvmtsdn — do NOT touch prod in this plan)
- Supabase MCP apply_migration stamps the schema_migrations row with now(), NOT the file timestamp — fix the version row after apply (memory: feedback_supabase_apply_migration_drift)
- TEST project pollution found 2026-07-19: ~920 running + ~834 failed_final orphaned compute_jobs (failed_final rows age out via the existing 90-day retention_compute_jobs_failed cron — do not widen this plan's DELETE to chase them)
- One-time cleanup SQL (exact, scoped): DELETE FROM compute_jobs WHERE status='running' AND created_at < now()-interval '1 hour'
- Fence tests MUST run serially (never -n auto) — CI's python job runs them serially against the shared TEST DB
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] MCP-apply the purge migration to TEST + guard test asserts green</name>
  <files>(none — runtime state on TEST project qmnijlgmdhviwzwfyzlc)</files>
  <read_first>
    - supabase/migrations/20260719120000_retention_orphaned_running_compute_jobs.sql (the body to apply — created in 125-01)
    - supabase/tests/test_retention_orphaned_running.sql (the guard that must flip from NOTICE-skip to full assertion)
  </read_first>
  <action>
    Via Supabase MCP against project qmnijlgmdhviwzwfyzlc ONLY:
    (1) apply_migration with name retention_orphaned_running_compute_jobs and
    the exact file body. Note: the migration's own BEGIN/COMMIT envelope and
    terminal self-verify DO block run during apply — an apply that returns
    success has already passed the body's ILIKE self-checks.
    (2) Fix the schema_migrations stamp drift: update the
    supabase_migrations.schema_migrations row just inserted so version =
    '20260719120000' (MCP stamps now(); a drifted version breaks future
    migration ordering — memory feedback_supabase_apply_migration_drift).
    (3) Verify registration: execute SELECT jobname, schedule, command FROM
    cron.job WHERE jobname = 'retention_compute_jobs_orphaned_running' — one
    row, schedule '15 4 * * *', command containing status = 'running' and
    interval '2 hours'.
    (4) Run the guard test: psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1
    -f supabase/tests/test_retention_orphaned_running.sql if the env var is
    available locally; otherwise execute the test's assertion DO blocks via
    MCP execute_sql (same SQL, same oracle). It must now take the ASSERT
    path (no NOTICE-skip) and exit clean.
    Do NOT apply anything to prod (khslejtfbuezsmvmtsdn) — prod receives the
    migration via the standard auto-apply on merge to main, founder-watched.
  </action>
  <verify>
    <automated>psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_retention_orphaned_running.sql</automated>
  </verify>
  <acceptance_criteria>
    - MCP query on TEST returns exactly 1 cron.job row for jobname retention_compute_jobs_orphaned_running with schedule '15 4 * * *'
    - The cron.job.command on TEST ILIKE-matches all three fragments: status = ''running'' / interval ''2 hours'' / public.compute_jobs
    - supabase_migrations.schema_migrations on TEST contains version '20260719120000' (not a now()-derived stamp)
    - The guard test run exits 0 via the ASSERT path — its output contains NO 'SKIP' notice for the purge cron
    - Zero writes to project khslejtfbuezsmvmtsdn in this task
  </acceptance_criteria>
  <done>Migration live on TEST with correct version stamp; guard test asserting green. Merge is now safe for the sql-tests CI gate.</done>
</task>

<task type="auto">
  <name>Task 2: One-time TEST-project orphan cleanup + fence tests prove the flake source gone</name>
  <files>(none — runtime state on TEST project qmnijlgmdhviwzwfyzlc)</files>
  <read_first>
    - supabase/migrations/20260719073701_claim_kind_filter.sql (partition-dedupe arms lines 156-179 — what the orphans collide with; confirms why DELETE is the fix)
  </read_first>
  <action>
    Via Supabase MCP against qmnijlgmdhviwzwfyzlc ONLY:
    (1) Snapshot the before-count: SELECT count(*) FROM compute_jobs WHERE
    status='running' AND created_at < now()-interval '1 hour' (record it in
    the SUMMARY — expected ~920 per the 2026-07-19 inventory).
    (2) Execute the EXACT scoped one-time cleanup: DELETE FROM compute_jobs
    WHERE status='running' AND created_at < now()-interval '1 hour'. Do NOT
    widen it (no failed_final — the existing 90-day retention cron owns
    those; no all-status delete). If the DELETE trips the
    retention_delete_guard 100k cap it would fail loud — rerun in bounded
    batches rather than disabling the guard.
    (3) Verify after-count = 0 for the same predicate.
    (4) Prove the flake source is gone: with the TEST DB env configured, run
    `cd analytics-service && python -m pytest tests/test_compute_jobs_fencing.py
    tests/test_drain_semantics.py -x -q` SERIALLY (never -n auto). If the
    local env lacks the TEST DB credentials, state that explicitly and defer
    the fence proof to the PR's python CI job — never claim it ran.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_compute_jobs_fencing.py tests/test_drain_semantics.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Before/after counts recorded; after-count for status='running' AND created_at < now()-interval '1 hour' on TEST = 0
    - The DELETE touched ONLY status='running' rows (a post-check SELECT count(*) FROM compute_jobs WHERE status='failed_final' is unchanged from its before-snapshot)
    - Fence tests exit 0 when run serially against TEST — or the deferral to CI is stated explicitly in the SUMMARY with the reason (missing local credentials), never silently
    - Zero writes to prod
  </acceptance_criteria>
  <done>TEST project clean of orphaned running rows; fence tests green (or explicitly CI-deferred); recurring cron prevents re-accumulation nightly at 04:15 before the 05:30 derive cron.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the TEST runtime-state ops — never silently claimed done</name>
  <files>(none — human verification of runtime state)</files>
  <read_first>
    - The 125-03-SUMMARY evidence produced by Tasks 1-2 (counts, cron.job row, version stamp, fence-test result)
  </read_first>
  <action>
    Pause and present the Task 1-2 evidence to the user for confirmation. Do
    NOT mark WORKER-04's runtime legs done without an explicit approval —
    a skipped or assumed op is never claimed done.
  </action>
  <what-built>
    Runtime state applied to the TEST project (qmnijlgmdhviwzwfyzlc): the
    retention_compute_jobs_orphaned_running purge cron is registered
    (migration MCP-applied, version stamp fixed to 20260719120000), the
    one-time orphan cleanup deleted all running rows older than 1h, and the
    fence tests were run (or explicitly deferred to CI).
  </what-built>
  <how-to-verify>
    1. Review the SUMMARY evidence: cron.job row (schedule '15 4 * * *'),
       schema_migrations version '20260719120000', before/after orphan counts
       (after = 0), fence-test result or explicit CI deferral.
    2. Optional spot-check via Supabase dashboard or MCP:
       SELECT jobname, schedule FROM cron.job WHERE jobname = 'retention_compute_jobs_orphaned_running';
       SELECT count(*) FROM compute_jobs WHERE status='running' AND created_at < now()-interval '1 hour';
    3. Note for merge time: on merge to main the migration AUTO-APPLIES to
       PROD (safety sweep arm of WORKER-04) — watch the run and verify the
       cron.job row appears on khslejtfbuezsmvmtsdn (standard founder watch).
  </how-to-verify>
  <resume-signal>Type "approved" to close the WORKER-04 runtime legs, or describe what's off.</resume-signal>
  <verify>
    <automated>MISSING — human gate: runtime-state confirmation cannot be automated; the automated legs ran in Tasks 1-2 (guard test + fence tests)</automated>
  </verify>
  <acceptance_criteria>
    - User replied "approved" (or issues were resolved and then approved)
    - The SUMMARY records the approval and the evidence it was based on
  </acceptance_criteria>
  <done>User explicitly approved the TEST runtime-state evidence; WORKER-04 runtime legs recorded done with evidence, or issues captured for follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP → TEST database | Direct SQL execution against the shared CI test project |
| merge → PROD auto-apply | The same migration body lands on prod unattended at merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-08 | Tampering | one-time DELETE over-deletes on TEST (wrong status/window) | mitigate | Exact scoped literal (status='running' AND created_at < now()-interval '1 hour'); before/after counts recorded; failed_final count proven unchanged; retention_delete_guard respected (batch on cap, never disable) |
| T-125-09 | Tampering | wrong project targeted (prod instead of test) | mitigate | Project ref pinned in every task (qmnijlgmdhviwzwfyzlc); explicit zero-writes-to-prod acceptance criterion; prod path is merge-time auto-apply only |
| T-125-10 | Repudiation | runtime ops claimed done without running (skipped-op lie) | mitigate | Blocking human-verify checkpoint with recorded evidence; fence-test deferral must be stated explicitly, never silent |
| T-125-SC | Tampering | npm/pip/cargo installs | accept | N/A — no packages installed |
</threat_model>

<verification>
- Guard test asserts green on TEST (no SKIP path).
- Orphaned-running count on TEST = 0; failed_final untouched.
- Fence tests green serially (or explicit CI deferral recorded).
- Human checkpoint approved.
</verification>

<success_criteria>
- WORKER-04 runtime state live on TEST: recurring purge cron + clean slate; CI fence flake source removed.
- Merge to main is safe: sql-tests gate green (TEST applied first), prod auto-apply is the deliberate safety-sweep arm.
- Every runtime op has recorded evidence; nothing skipped silently.
</success_criteria>

<output>
Create `.planning/phases/125-worker-dedicated-backfill-worker-retention-hygiene/125-03-SUMMARY.md` when done.
</output>
