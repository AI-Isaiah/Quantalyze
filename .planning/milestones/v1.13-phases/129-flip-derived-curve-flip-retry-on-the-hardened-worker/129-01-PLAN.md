---
phase: 129-flip-derived-curve-flip-retry-on-the-hardened-worker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # ⚠️ CONDITIONAL edits only — this is an audit-then-fill plan. These files are
  # touched ONLY if a GENUINE coverage gap is found (Claude's Discretion in
  # 129-CONTEXT.md). The expected outcome is ZERO source edits (audit satisfied).
  - docs/runbooks/flipretry-derived-equity-go-live.md
  - analytics-service/tests/test_phase35_backfill_enqueue.py
autonomous: false
requirements: [FLIP-01, FLIP-02]

must_haves:
  truths:
    - "A re-run of phase35_backfill_enqueue while pending api_key derive_broker_dailies jobs exist enqueues NOTHING (bails 'skipping to avoid duplicates') — proven by the existing pytest suite, not by inspection alone"
    - "A missing duplicate-guard count (count=None) RAISES RuntimeError — the guard is never skipped blind (Rule 12)"
    - "Runbook Step 8 rollback is documented AND executable: delete flip jobs + empty allocator_equity_derived + unschedule the derive-allocator-key-dailies cron (the verbatim v1.11 recovery), reachable via an abort path from every live step"
    - "An untrustworthy / absent / malformed / empty derived payload renders the LEGACY curve unchanged (equityCurveSource='legacy') — the Phase 127 display gate is intact on this branch"
    - "FLIP-01 (the live prod backfill enqueue, healthz-fresh-throughout) is either evidenced by the founder run or explicitly recorded human_needed-OPEN — NEVER silently claimed done"
  artifacts:
    - path: "analytics-service/scripts/phase35_backfill_enqueue.py"
      provides: "Idempotent backfill enqueue (pre-check bail + atomic bulk INSERT + 23505 race rollback + Rule-12 fail-louds)"
      contains: "skipping to avoid duplicates"
    - path: "analytics-service/tests/test_phase35_backfill_enqueue.py"
      provides: "Idempotency + fail-loud suite (pending-skips, count-None-RAISES, malformed-rows-skipped, 23505-race)"
      contains: "test_pending_jobs_present_skips_enqueue"
    - path: "docs/runbooks/flipretry-derived-equity-go-live.md"
      provides: "Executable FLIP runbook: cutover Steps 0-2, E2 two-part gate Step 4, backfill Step 5, cron reschedule Step 6 LAST, Step 8 ROLLBACK"
      contains: "cron.unschedule('derive-allocator-key-dailies')"
    - path: ".planning/phases/129-flip-derived-curve-flip-retry-on-the-hardened-worker/129-01-SUMMARY.md"
      provides: "Per-item audit verdict (FLIP-02) + FLIP-01 disposition (evidence or human_needed-OPEN)"
  key_links:
    - from: "analytics-service/scripts/phase35_backfill_enqueue.py"
      to: "compute_jobs pending pre-check"
      via: "count='exact' select + api_key_id NOT NULL scope"
      pattern: "not_\\.is_\\(\"api_key_id\", \"null\"\\)"
    - from: "docs/runbooks/flipretry-derived-equity-go-live.md Step 8"
      to: "allocator_equity_derived"
      via: "rollback DELETE (TS flip renders legacy on empty)"
      pattern: "DELETE FROM allocator_equity_derived"
    - from: "src/lib/queries.ts extractTrustworthyDerivedCurve"
      to: "equityCurveSource flip site (~L2591)"
      via: "derivedCurve !== null ? 'derived' : 'legacy'"
      pattern: "derivedCurve !== null \\? \"derived\" : \"legacy\""
---

<objective>
Phase 129 FLIP — verify the already-built FLIP-02 groundwork (idempotent enqueue +
executable rollback) with live test evidence, confirm the Phase-127 display gate is
intact (ROADMAP criterion 4), and model FLIP-01 (the founder-run production backfill
enqueue on the hardened dedicated worker, gated on E2GT two-part green) as a
`human_needed` live op that is NEVER claimed done without healthz-fresh-throughout
evidence.

Purpose: this is a VERIFICATION phase, not a rebuild. The v1.11 FLIP rollback root
cause (backfill fan-out wedged the sequential prod worker) was structurally fixed in
Phases 123/125 (crawl bounds + dedicated backfill worker + claim-role split); the
enqueue idempotency and the verbatim-v1.11-recovery rollback were built and tested in
v1.12. Per the ponytail discipline (129-CONTEXT.md Claude's Discretion): fill ONLY a
genuine coverage gap; otherwise record the audit as satisfied — no redundant tests,
no manufactured work over load-bearing tested code.

Output: 129-01-SUMMARY.md carrying (a) a per-item FLIP-02 audit verdict with the
verification commands + their observed results, (b) the display-gate intact proof,
and (c) the FLIP-01 disposition — founder live evidence, or an explicit
human_needed-OPEN record gating Phase 130.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/129-flip-derived-curve-flip-retry-on-the-hardened-worker/129-CONTEXT.md

Key prior-phase facts (do NOT re-derive):
- Phase 125 (WORKER): dedicated backfill worker + `WORKER_CLAIM_ROLE` split landed;
  runbook Steps 0-2 extended in place (commit 1f50b60b). WORKER-01 cutover and
  WORKER-03 cron reschedule are founder live legs, recorded human_needed-OPEN.
- Phase 127 (E2GT): E2GT-02 display gate SATISFIED via audit (all 3 criteria proven
  by existing v1.12 123-03 fixtures — no new test); runbook Step 4 sharpened to the
  TWO-PART gate (exit 0 AND `anchor_consistency.within_same_day_tolerance===true`).
  E2GT-01 (the live run) stays human_needed-OPEN and GATES this phase's FLIP-01.
- The FLIP is data-driven, no flag: `extractTrustworthyDerivedCurve`
  (src/lib/queries.ts ~L2455) returns the derived curve ONLY for
  `is_trustworthy===true` + well-formed dense non-empty curve; else null → legacy
  render byte-unchanged at the flip site (~L2591).
- TWO enqueue paths exist, both idempotent by design: the python script
  `scripts/phase35_backfill_enqueue.py` (pre-check bail + atomic bulk INSERT +
  (api_key_id, kind) in-flight partial unique index) and the runbook Step 5 SQL
  fan-out `enqueue_derive_broker_dailies_for_allocator_keys()` (advisory lock +
  per-(api_key_id, UTC-date) idempotency key), pinned by
  `supabase/tests/test_claim_kind_filter.sql` (double-invoke) +
  `supabase/tests/test_derive_allocator_keys_fanout.sql`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: FLIP-02 audit — enqueue idempotency + rollback executability, with live test evidence</name>
  <read_first>
    analytics-service/scripts/phase35_backfill_enqueue.py,
    analytics-service/tests/test_phase35_backfill_enqueue.py,
    docs/runbooks/flipretry-derived-equity-go-live.md,
    supabase/tests/test_claim_kind_filter.sql (the fan-out double-invoke block ~L183-205),
    supabase/tests/test_derive_allocator_keys_fanout.sql (header + eligibility asserts)
  </read_first>
  <files>docs/runbooks/flipretry-derived-equity-go-live.md, analytics-service/tests/test_phase35_backfill_enqueue.py</files>
  <action>
    Audit-then-fill, per 129-CONTEXT.md (FLIP-02 = ALREADY BUILT — VERIFY). Run the
    checklist below and record a per-item verdict (SATISFIED / GAP) in the SUMMARY.
    Edit files ONLY on a GENUINE gap — the expected outcome is zero edits.

    Checklist (each item names where the evidence should already exist):

    (a) Idempotent re-run bail: `phase35_backfill_enqueue.main` pre-checks pending
    `derive_broker_dailies` jobs with `api_key_id` NOT NULL (count="exact") and
    returns 0 with "skipping to avoid duplicates" when any exist — pinned by
    `test_pending_jobs_present_skips_enqueue` + `test_precheck_scopes_to_api_key_derive_jobs`.

    (b) Rule-12 guard: `existing.count is None` RAISES RuntimeError (never skips the
    duplicate guard blind) — pinned by `test_none_pending_count_raises_not_skips_guard`;
    `rows.data is None` also RAISES — `test_api_keys_none_data_raises`.

    (c) Malformed rows skipped + non-zero exit: `test_malformed_rows_skipped_valid_rows_enqueued`
    + `test_all_rows_malformed_no_insert`.

    (d) Race safety: the single atomic bulk INSERT aborts wholly on the
    (api_key_id, kind) in-flight partial unique index (23505), 0 enqueued, rc!=0 —
    `test_duplicate_race_aborts_atomically_zero_enqueued` + `test_non_race_bulk_failure_returns_nonzero`.

    (e) Runbook Step 5 SQL path idempotency (the FLIP's actual full-backfill route):
    `enqueue_derive_broker_dailies_for_allocator_keys()` advisory lock +
    per-(api_key_id, UTC-date) idempotency key — confirm the double-invoke SQL gate
    exists in `supabase/tests/test_claim_kind_filter.sql` and the fan-out gate in
    `test_derive_allocator_keys_fanout.sql`. (SQL gates run against the DB projects,
    not local CI — verify presence + assertions by reading, do not attempt a local run.)

    (f) Rollback documented AND executable: runbook Step 8 carries the verbatim v1.11
    recovery — DELETE flip jobs (kinds derive_broker_dailies + derive_allocator_equity,
    statuses pending/running), DELETE FROM allocator_equity_derived (the TS flip
    renders legacy on empty — safe), `cron.unschedule('derive-allocator-key-dailies')`
    — plus the `WORKER_CLAIM_ROLE=all` restore note, and an explicit abort path
    pointing at Step 8 from every live step (Steps 1-5 each carry one; Step 0 carries
    the global "Abort at any step → Step 8" line).

    Then run the pytest suite as LIVE evidence (not inspection alone). If any item is
    a genuine GAP, add the MINIMAL test or runbook step closing exactly that gap —
    naming the gap and the minimal fill in the SUMMARY. Do NOT add redundant tests
    over covered behavior, do NOT restructure the runbook, do NOT touch the script
    itself (it is load-bearing, tested, and shipped).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_phase35_backfill_enqueue.py -q</automated>
    <automated>grep -c "DELETE FROM allocator_equity_derived" docs/runbooks/flipretry-derived-equity-go-live.md | grep -qv "^0$" && grep -q "cron.unschedule('derive-allocator-key-dailies')" docs/runbooks/flipretry-derived-equity-go-live.md && grep -q "DELETE FROM compute_jobs" docs/runbooks/flipretry-derived-equity-go-live.md</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_phase35_backfill_enqueue.py -q` exits 0 with all 10 tests passing (no skips).
    - Runbook Step 8 contains all three rollback SQL ops (compute_jobs DELETE scoped to the two flip kinds + pending/running, allocator_equity_derived DELETE, cron.unschedule) — grep gates above pass.
    - SUMMARY records a per-item verdict for (a)-(f) with the observed command output; any GAP names the minimal fill actually made.
    - If verdicts are all SATISFIED: `git status` shows NO modification to phase35_backfill_enqueue.py, its test file, or the runbook from this task.
  </acceptance_criteria>
  <done>FLIP-02 audit recorded with live pytest evidence + runbook grep gates green; zero source edits unless a named genuine gap was minimally filled.</done>
</task>

<task type="auto">
  <name>Task 2: Display-gate intact check — derived curves reach users ONLY via the Phase-127 trustworthy gate</name>
  <read_first>
    src/lib/queries.ts (extractTrustworthyDerivedCurve ~L2455-2485 + the equityCurveSource flip site ~L2581-2594),
    src/lib/queries.test.ts (the FLIPRETRY-03 describe block ~L1144+)
  </read_first>
  <files></files>
  <action>
    ROADMAP Phase-129 criterion 4 (post-flip, an untrustworthy curve leaves the
    legacy basis rendering) rides entirely on the Phase-127-audited gate. This task
    is a REGRESSION CONFIRMATION on the current branch, not new work: run the
    existing vitest fixtures that pin (i) trustworthy→derived (FLIPRETRY-03 PASS
    case), (ii) untrustworthy/absent/malformed/empty→legacy byte-unchanged (the
    115.1 8-case block in queries.my-allocation.test.ts), and confirm by reading
    that `extractTrustworthyDerivedCurve` still enforces is_trustworthy===true +
    strict YYYY-MM-DD dates + finite equity_usd + non-empty curve, and the flip
    site still derives `equityCurveSource` solely from `derivedCurve !== null`.
    NO code edits — if the tests fail on this branch, that is a phase-blocking
    regression to SURFACE loudly (fail the plan), not to silently patch here.
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.test.ts src/lib/queries.my-allocation.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Both vitest files pass on the current branch (use --no-file-parallelism — known local flake carry-forward; a deterministic failure is a REAL regression, stop and surface).
    - SUMMARY cites the FLIPRETRY-03 describe block + the 8-case legacy-fallback block as the criterion-4 evidence, with the run's pass counts.
    - `git status` shows src/lib/queries.ts untouched by this task.
  </acceptance_criteria>
  <done>Criterion 4 evidenced by the existing gate fixtures passing live on this branch; zero edits.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: FLIP-01 — founder live op: production backfill enqueue on the dedicated worker (E2GT-gated)</name>
  <read_first>docs/runbooks/flipretry-derived-equity-go-live.md (Steps 0-5 in order; Step 4 two-part gate; Step 8 abort)</read_first>
  <files></files>
  <action>
    Present the founder live op EXACTLY as the runbook prescribes (Steps 0-5 in
    order; Step 8 abort at any failure; Step 6 cron reschedule excluded — that is
    WORKER-03). Then record the FLIP-01 disposition per the acceptance criteria:
    evidence-satisfied (quote the healthz-fresh-throughout + queue-drained +
    allocator_equity_derived observations in SUMMARY and mark FLIP-01 in
    REQUIREMENTS), or deferred (the expected path while E2GT-01 is open) →
    FLIP-01 recorded human_needed-OPEN in SUMMARY + STATE, explicitly gating
    Phase 130 GOLIVE. No simulation, no CI-derived claim, no partial credit.
  </action>
  <what-built>
    Everything that makes this op safe is landed and verified: the dedicated
    backfill worker + claim-role split (Phase 125, runbook Steps 0-2), bounded
    crawls (123-01), the idempotent enqueue on BOTH paths (Task 1 audit), the
    executable Step 8 rollback (Task 1 audit), and the display gate that keeps an
    unvalidated curve off every screen (Task 2). What remains is the LIVE op only —
    it requires founder-provisioned prod access (Railway + prod SQL) and MUST NOT
    be simulated or claimed from CI.
  </what-built>
  <how-to-verify>
    The op is the runbook, in order — do not improvise:
    1. Step 0 preconditions + topology confirm; Steps 1-2 cutover (backfill service
       up with WORKER_CLAIM_ROLE=backfill + CMD python -m main_worker; prod worker
       → interactive). NOTE: Phase-125 WORKER-01 records this cutover
       human_needed-OPEN — if not yet done, it happens NOW as part of this op.
    2. Step 3 pilot enqueue (ONE heavy key) — prod healthz stays 200/fresh.
    3. Step 4 E2GT-01 LIVE gate (Phase 127 human_needed leg): exit 0 AND
       anchor_consistency.within_same_day_tolerance===true in the evidence JSON.
       ⚠️ NO GREEN, NO FLIP — exit 0 alone is NOT a pass. Never widen
       --same-day-drift-tol to force it.
    4. Step 5 full backfill enqueue (SELECT enqueue_derive_broker_dailies_for_allocator_keys()).
       FLIP-01 evidence contract: the DEDICATED worker burns the queue down while
       the PROD worker's healthz NEVER goes stale past the restart threshold
       throughout the entire backfill (the exact v1.11 failure mode, disproven
       LIVE). Capture: healthz observations spanning the backfill window + the
       drained queue + allocator_equity_derived repopulating.
    5. Any abort → Step 8 ROLLBACK (verbatim v1.11 recovery; system returns to
       dormant-legacy, 0 user impact). Step 6 cron reschedule stays dead LAST and
       belongs to WORKER-03 — NOT required for FLIP-01.
  </how-to-verify>
  <acceptance_criteria>
    - EITHER: founder supplies the live evidence (healthz-fresh-throughout + queue drained + derived rows present) → FLIP-01 marked satisfied in REQUIREMENTS + SUMMARY, with the evidence quoted.
    - OR (expected — E2GT-01 is still human_needed-OPEN): the op is DEFERRED → FLIP-01 recorded human_needed-OPEN in the SUMMARY + STATE, explicitly gating Phase 130 GOLIVE, with the runbook named as the execution path. This is the honest completion of the plan's modeling obligation.
    - NEVER: FLIP-01 claimed done without the live healthz evidence — a skipped gate is never marked complete.
  </acceptance_criteria>
  <resume-signal>Founder types "flip evidence: ..." (with healthz/queue/derived observations) to mark FLIP-01 satisfied, or "defer" to record it human_needed-OPEN and close the plan honestly.</resume-signal>
  <done>FLIP-01 dispositioned honestly: live evidence quoted, or human_needed-OPEN recorded with the runbook (Steps 0-5, Step 8 abort) as the delivered execution path — never silently claimed done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → prod DB | service-role enqueue (python script / Step-5 SQL) writes compute_jobs on PROD |
| backfill worker → prod worker | queue-mediated; backfill load must never starve the prod loop (the v1.11 wedge) |
| DB JSONB → SSR | worker-written derived payload treated as UNTRUSTED at extractTrustworthyDerivedCurve |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-01 | Tampering/DoS | duplicate/racing enqueue floods compute_jobs | mitigate (verify) | pre-check bail + atomic bulk INSERT on the (api_key_id, kind) in-flight partial unique index (Task 1 a/d) + Step-5 advisory lock (Task 1 e) |
| T-129-02 | DoS | backfill wedges the prod worker loop (v1.11 verbatim) | mitigate (verify) | dedicated-worker claim-role isolation (Phase 125) + bounded crawls + runbook ordering with Step-8 abort at every step; FLIP-01 evidence contract = healthz-fresh-throughout (Task 3) |
| T-129-03 | Info disclosure | E2_GROUND_TRUTH_* creds during the Step-4 gate | transfer (runbook) | env-only provisioning (never argv/tracked files), read-only-proven before fetch, rotate after run — already mandated by runbook Step 4 |
| T-129-04 | Tampering | corrupt/untrustworthy derived payload reaching users post-flip | mitigate (verify) | extractTrustworthyDerivedCurve strict-shape + is_trustworthy gate → legacy fallback (Task 2) |

No package-manager installs in this phase → no supply-chain (SC) row.
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_phase35_backfill_enqueue.py -q` → 10 passed, 0 skipped.
- `npx vitest run src/lib/queries.test.ts src/lib/queries.my-allocation.test.ts --no-file-parallelism` → all pass.
- Runbook grep gates: Step 8 contains `DELETE FROM compute_jobs`, `DELETE FROM allocator_equity_derived`, `cron.unschedule('derive-allocator-key-dailies')`.
- `git status` after Tasks 1-2: no source/test/runbook modifications unless a named genuine gap was minimally filled (SUMMARY documents which).
- FLIP-01 disposition recorded (evidence-satisfied OR human_needed-OPEN) — never silently claimed.
</verification>

<success_criteria>
- FLIP-02 audit complete: all six checklist items verdicted with live evidence; idempotent re-run + Rule-12 fail-louds + race safety + Step-8 rollback executability confirmed (or a minimal named fill made).
- Criterion 4 evidenced: the existing display-gate fixtures pass on this branch, no edits.
- FLIP-01 honestly dispositioned: live evidence (healthz never stale throughout the backfill) OR explicitly recorded human_needed-OPEN gating Phase 130 — the runbook (Steps 0-5, Step 8 abort) is the delivered execution path.
- Zero manufactured work: no redundant tests, no restructuring of load-bearing shipped code.
</success_criteria>

<output>
Create `.planning/phases/129-flip-derived-curve-flip-retry-on-the-hardened-worker/129-01-SUMMARY.md` when done, carrying the per-item audit verdicts, the verification command outputs, and the FLIP-01 disposition.
</output>
