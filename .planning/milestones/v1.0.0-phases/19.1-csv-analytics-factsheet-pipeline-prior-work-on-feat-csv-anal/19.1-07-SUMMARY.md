---
phase: 19.1
plan: 07
subsystem: infra
tags: [railway, deploy, analytics-service, csv-analytics, watchdog]

requires:
  - phase: 19.1-06
    provides: PROD-APPLIED migrations (csv_daily_returns + compute_analytics_from_csv kind)
provides:
  - Verified Railway analytics-service deploy at main HEAD (4d9eeaa9)
  - Worker dispatch table includes compute_analytics_from_csv branch in deployed image
  - WATCHDOG_PER_KIND_OVERRIDES for new kind preserves test_every_kind_has_watchdog_headroom
  - Pre-flip state confirmed (0 production CSV jobs claimed by worker)
affects: [19.1-08-vercel-env-flip, 19.1-09-end-to-end-verification]

tech-stack:
  added: []
  patterns:
    - "Railway auto-deploy on main CI green (A6 contract)"
    - "SHA-equality assertion against origin/main HEAD as deploy validity gate"

key-files:
  created:
    - .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-07-SUMMARY.md
  modified: []

key-decisions:
  - "MCP tool mcp__plugin_railway_railway__list_services returned unauthorized — fell back to railway CLI per Plan Task 2 Path B; CLI is authed and returns same JSON shape"
  - "Service name is 'quantalyze-analytics' (not 'analytics-service' as the plan template assumed) — confirmed via railway status --json"
  - "Pre-existing match_engine cron errors observed in deploy logs are unrelated to Phase 19.1 CSV pipeline (allocator_preferences 406, traced to /app/routers/match.py); not a regression introduced by this deploy"

patterns-established:
  - "Convergence loop unnecessary when deploy already converged before plan execution — read status once, assert, proceed"

requirements-completed: []

duration: 6min
completed: 2026-05-22
verdict: DEPLOYED
---

# Plan 19.1-07 — Railway Deploy Verification

**Railway analytics-service is live on main HEAD (4d9eeaa9) with the new compute_analytics_from_csv dispatch branch + handler + TIMEOUT + WATCHDOG override.**

## Performance

- **Duration:** ~6 min (Railway deploy already converged before plan executed)
- **Started:** 2026-05-22 (plan execution)
- **Completed:** 2026-05-22
- **Tasks:** 3 (Task 1 precondition + Task 2 observe + Task 3 four-assert convergence)
- **Files modified:** 0 (this plan is observation + verification only — no source changes)

## Task 1 — Precondition Gate (B5) ✅

Plan 06 SUMMARY canonical verdict line confirmed:

```
$ grep -c '^19\.1-06-VERDICT: PROD-APPLIED$' .planning/phases/19.1-.../19.1-06-SUMMARY.md
1
```

Both migrations present in main HEAD tree:
- `supabase/migrations/20260522120000_csv_daily_returns.sql` ✓
- `supabase/migrations/20260522120100_compute_analytics_from_csv_kind.sql` ✓

PR #276 merged at `2026-05-22T13:51:06Z` (commit `4d9eeaa9`); CI green per `gh run list --branch main --limit 5`.

## Task 2 — Railway Deployment Observation ✅

Railway CLI used as fallback (MCP `mcp__plugin_railway_railway__list_services` returned `Unauthorized — Please run railway login again`). CLI is authed as `Helmut (helmut.mueller1@gmail.com)`.

```
railway status --json | jq '.environments.edges[0].node.serviceInstances.edges[0].node.latestDeployment'
```

Result:

| Field | Value |
|-------|-------|
| Project ID | `dedf7fc5-b3f8-450f-b16c-6734722f5b52` |
| Project name | `quantalyze-analytics` |
| Service name | `quantalyze-analytics` (Dockerfile at `analytics-service/Dockerfile`) |
| Service ID | `26ec84d9-70fc-4e01-8be2-5ca2f5712cd3` |
| Environment | `production` (`76554619-9984-4efd-ba5e-9016ec736e80`) |
| **Latest deployment ID** | `6d18f208-d9c5-4140-b9e6-7fc5a83e1157` |
| **Deployment status** | `SUCCESS` |
| **Deployed commit hash** | `4d9eeaa9e51133bba50e73a3d9972ac286082959` |
| **Deployed branch** | `main` |
| Deployment createdAt | `2026-05-22T13:51:08.648Z` |

Auto-deploy fired on PR #276 merge (PR-Y1-HANDOVER.md A6 contract honored) — no manual `railway redeploy` required.

## Task 3 — Four-Assert Convergence Gate (B6 + W2) ✅

Each of the four asserts evaluated against the live state:

### Assert 1: Deployment SUCCESS within 15 min

`SUCCESS` reached at `2026-05-22T13:51:08.648Z`, well within the 15-min hard ceiling. No `BUILDING` / `DEPLOYING` / `FAILED` / `CRASHED` states observed.

### Assert 2: Deployed SHA == origin/main HEAD (W2)

```
deployed_sha = 4d9eeaa9e51133bba50e73a3d9972ac286082959
main_sha     = 4d9eeaa9e51133bba50e73a3d9972ac286082959
```

Equal ✅ — no stale-image risk.

### Assert 3: Watchdog Headroom Invariant Test

```
$ source analytics-service/.venv/bin/activate
$ pytest tests/test_main_worker.py::TestWatchdogInvariant::test_every_kind_has_watchdog_headroom -v

tests/test_main_worker.py::TestWatchdogInvariant::test_every_kind_has_watchdog_headroom PASSED [100%]
============================== 1 passed in 1.96s ===============================
```

The new `compute_analytics_from_csv` kind has `TIMEOUT_PER_KIND=10min`, `WATCHDOG_PER_KIND_OVERRIDES=15min` — watchdog override > timeout for every kind including the new one (T-19.1-08 mitigated).

### Assert 4: Clean Worker Startup Logs

From `railway logs --service quantalyze-analytics --deployment` (post-2026-05-22T13:56:08 boot):

```
2026-05-22 13:56:09 quantalyze.analytics INFO KEK validation passed (version 1)
2026-05-22 13:56:09 quantalyze.analytics INFO Startup validation complete
2026-05-22 13:56:09 quantalyze.analytics INFO Worker starting as worker-39a4bc6a4067-2 (merged into API)
2026-05-22 13:56:10 INFO:     Application startup complete.
2026-05-22 13:56:10 INFO:     Uvicorn running on http://0.0.0.0:8080
2026-05-22 13:56:10 quantalyze.analytics.worker INFO Daily enqueue: 4 poll_positions jobs created
2026-05-22 13:56:40 quantalyze.analytics.worker INFO Claimed 4 jobs ...
2026-05-22 13:56:41 .. through ..
2026-05-22 13:56:51 quantalyze.analytics.worker INFO Job ffbc92aa-d48e-452f-92ac-cc1a2976905d done (trade_count=None)
```

- KEK validation passed (no `interval-parse error`, no `WATCHDOG_PER_KIND_OVERRIDES` parse failure)
- API + worker started cleanly, no Traceback on boot
- Existing `poll_positions` jobs claimed and marked done normally (4/4 OK)
- T-19.1-18 (worker crashes at boot due to interval-parse error) mitigated — startup is clean

**Pre-existing unrelated noise — NOT a regression introduced by this deploy.**

Match engine cron errors of the form `match_engine cron: allocator <uuid> failed: 'NoneType' object has no attribute 'data'` appear in the logs. These come from `/app/routers/match.py:1047` calling `_load_allocator_context` → `allocator_preferences` returns `HTTP/2 406 Not Acceptable`, then `.data` deref on None. This pre-dates Phase 19.1 (predates the CSV pipeline merge entirely) and is unrelated to `compute_analytics_from_csv` dispatch. Tracked separately; out of scope for Plan 07. The CSV pipeline boot path is clean.

### Pre-Flip Production Check

```
SELECT COUNT(*) AS preflip_csv_jobs FROM compute_jobs WHERE kind = 'compute_analytics_from_csv';
-- 0
```

Production has zero `compute_analytics_from_csv` jobs queued or claimed — expected because Plan 08 has not yet flipped `USE_COMPUTE_JOBS_QUEUE=true`. Worker is idle-looping harmlessly for this kind while continuing to service all other kinds normally.

## Acceptance Criteria — All ✅

- ✅ Railway deployment status = `SUCCESS` within 15 min
- ✅ Deployed SHA == `git rev-parse origin/main` (`4d9eeaa9e51133bba50e73a3d9972ac286082959`)
- ✅ `test_every_kind_has_watchdog_headroom` PASSES locally against main HEAD
- ✅ Service logs show clean startup (no Traceback in CSV-pipeline boot path; no interval-parse error; WATCHDOG override parsed)
- ✅ Pre-flip production check: 0 rows in `compute_jobs` with `kind = 'compute_analytics_from_csv'`
- ✅ Plan 08 (Vercel env flip) is now unblocked

## Plan 08 Handoff

Plan 08 may now proceed:
1. Read Plan 07 SUMMARY canonical verdict line (this file).
2. Flip `USE_COMPUTE_JOBS_QUEUE=true` on Vercel production (if not already set).
3. Redeploy Next.js so the `csv-finalize/route.ts` `after()` block enqueues jobs.
4. Smoke probe `/api/strategies/csv-finalize` (expect 401/405).

19.1-07-VERDICT: DEPLOYED
