---
phase: 19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal
plan: 08
type: execute
wave: 6
depends_on: [07]
files_modified: []
autonomous: false
requirements: []
threat_model_refs: []

must_haves:
  truths:
    - "Vercel production env var USE_COMPUTE_JOBS_QUEUE is set to 'true' (verified via vercel env get OR vercel env pull fallback)"
    - "Next.js production has been redeployed since the env var change so the new value is in the running runtime"
    - "Post-redeploy: a probe POST to /api/strategies/csv-finalize (in Plan 09 verification) successfully enqueues a job — i.e., the after() block reads the new env value"
    - "SUMMARY emits canonical verdict line `19.1-08-VERDICT: FLIPPED` only on full success"
  artifacts:
    - path: "Vercel production environment"
      provides: "USE_COMPUTE_JOBS_QUEUE=true active in runtime"
      contains: "env var set"
  key_links:
    - from: "Vercel env var USE_COMPUTE_JOBS_QUEUE"
      to: "csv-finalize/route.ts after() block + finalize-wizard/route.ts:622 + keys/sync/route.ts:204"
      via: "process.env read inside after() callback"
      pattern: "process.env.USE_COMPUTE_JOBS_QUEUE !== \"true\""
---

<objective>
Set `USE_COMPUTE_JOBS_QUEUE=true` on Vercel production (if not already set) and redeploy Next.js so the new `csv-finalize/route.ts` after() block actually enqueues jobs.

Purpose: The route code is already in production (shipped via Plan 03 + /ship), but the after() block is gated on `process.env.USE_COMPUTE_JOBS_QUEUE === "true"`. Without the flag, CSV uploads succeed silently but no job ever lands in `compute_jobs`. This plan is the single switch that activates the pipeline end-to-end.

Output: Production env var set; Next.js redeployed; canonical verdict line `19.1-08-VERDICT: FLIPPED` written to SUMMARY only on full success. No source changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-CONTEXT.md
@.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-RESEARCH.md
@.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-07-railway-deploy-PLAN.md
@docs/runbooks/compute-queue.md

<interfaces>
<!-- Vercel CLI -->
// vercel env ls production         (read-only, returns the value or absent)
// vercel env get USE_COMPUTE_JOBS_QUEUE production   (may or may not exist depending on CLI version — Task 2 detects)
// vercel env pull <out-file> --environment=production --yes  (W6 fallback path)
// vercel env add USE_COMPUTE_JOBS_QUEUE production    (interactive — enter value)
// vercel env rm USE_COMPUTE_JOBS_QUEUE production     (rollback)
// vercel --prod                    (deploy production)
//
// Auth: VERCEL_TOKEN env var or `vercel login` interactive auth on the user's box.

<!-- Routes that read this flag -->
// src/app/api/strategies/csv-finalize/route.ts   (NEW — Plan 03)
// src/app/api/strategies/finalize-wizard/route.ts:622
// src/app/api/keys/sync/route.ts:204
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [B5] Authorize production env mutation — gated on Plan 07 verdict</name>
  <read_first>
    - .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-RESEARCH.md (Runtime State Inventory — `USE_COMPUTE_JOBS_QUEUE`; Open Question 1 — current value unknown)
    - .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-07-SUMMARY.md (must contain canonical verdict line `19.1-07-VERDICT: DEPLOYED`)
    - docs/runbooks/compute-queue.md (verify the runbook is consistent with flipping)
  </read_first>
  
  <files></files>
<action>
    Plan 07 deployed analytics-service with the new dispatch branch. Plan 03's route code is live (shipped via /ship). Now the missing link is the env var that gates the enqueue side-effect.

    Per RESEARCH.md Open Question 1: it is plausible the flag is already `true` in production (finalize-wizard + keys/sync already gate on it and main is shipping). The first step is **verify**, not flip.

    If verified-true: this plan is a no-op apart from re-confirming and possibly re-deploying to ensure the latest route code is live.

    If verified-false or unset: flip to `true`, then redeploy.
  </action>
  <verify>
    <automated>test -f .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-07-SUMMARY.md && grep -c '^19\.1-07-VERDICT: DEPLOYED$' .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-07-SUMMARY.md | xargs -I{} test {} -eq 1</automated>
  </verify>
  <prompt-text>
    Confirm:
    1. Plan 07 SUMMARY contains exactly one `19.1-07-VERDICT: DEPLOYED` line (canonical machine verdict, not free-text)
    2. Plan 06 SUMMARY shows prod migrations applied (verdict `PROD-APPLIED`)
    3. The user is aware that flipping the flag activates the enqueue side-effect across 3 routes (csv-finalize NEW + finalize-wizard + keys/sync); the existing 2 routes already work with the flag, so flipping has no negative side effect on them.
    4. Rollback procedure: `vercel env rm USE_COMPUTE_JOBS_QUEUE production` + redeploy. Runbook: docs/runbooks/compute-queue.md.
    Precondition gate (B5 revision 2026-05-22): the `<automated>` block above mechanically grep-asserts the canonical verdict line on its own line in Plan 07 SUMMARY before this checkpoint can release. A SUMMARY that lacks the verdict line (because the Railway deploy timed out or SHA-mismatched) cannot pass this gate.
  </prompt-text>
  <done>User authorization captured; subsequent tasks unblocked.</done>
  <resume-signal>Type "authorized to verify-then-flip USE_COMPUTE_JOBS_QUEUE on Vercel production" to unblock Task 2.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [W6] Verify current value of USE_COMPUTE_JOBS_QUEUE on Vercel production (with CLI-feature fallback)</name>
  <files></files>
  <read_first>
    - .vercel/project.json or .vercel/repo.json (confirm Vercel link is correct + on the right project + team) — run `vercel whoami` first
  </read_first>
  <action>
    1. Confirm Vercel auth + project link:
       - `vercel whoami` — confirm logged in as the project owner (expected: helmut.mueller1@gmail.com per session memory)
       - `ls .vercel/` — confirm `project.json` exists (or `repo.json` if monorepo)
       - If not linked: `vercel link` (interactive) before proceeding

    2. **W6 revision 2026-05-22 — CLI subcommand detection + fallback to `vercel env pull`:**

       `vercel env get` may or may not exist depending on the installed Vercel CLI version. Detect at runtime:
       ```bash
       if vercel env --help 2>&1 | grep -q "vercel env get"; then
         USE_GET=true
       else
         USE_GET=false
       fi
       ```

       Then read the value:
       ```bash
       if [ "$USE_GET" = true ]; then
         CURRENT_VALUE=$(vercel env get USE_COMPUTE_JOBS_QUEUE production 2>/dev/null || echo "NOT_SET")
       else
         # Fallback: pull env file then grep
         vercel env pull /tmp/.env.production.19.1 --environment=production --yes >/dev/null 2>&1 || true
         if [ -f /tmp/.env.production.19.1 ]; then
           CURRENT_VALUE=$(grep -E '^USE_COMPUTE_JOBS_QUEUE=' /tmp/.env.production.19.1 | head -1 | cut -d= -f2- | tr -d '"' || echo "NOT_SET")
           [ -z "$CURRENT_VALUE" ] && CURRENT_VALUE="NOT_SET"
           rm -f /tmp/.env.production.19.1  # don't leave secrets on disk
         else
           CURRENT_VALUE="NOT_SET"
         fi
       fi
       echo "CURRENT_VALUE=$CURRENT_VALUE"
       ```

    3. Branch outcomes (driven by `CURRENT_VALUE`):
       - **If `CURRENT_VALUE` is exactly the string `true`:** record the current value, SKIP the flip in Task 3, proceed to Task 4 to verify a redeploy has happened since the most recent route code change is live.
       - **If `CURRENT_VALUE` is anything else (`false`, `NOT_SET`, any other string, or empty):** continue to Task 3 to idempotently set it to `true`.

    4. Capture the captured `CURRENT_VALUE` AND the raw `vercel env ls production | grep -i USE_COMPUTE_JOBS_QUEUE` output (for audit) in the execute-phase summary (redact tokens if visible). Also record `USE_GET` (true|false) so the SUMMARY documents which read path was taken.
  </action>
  <verify>
    <automated>if vercel env --help 2>&1 | grep -q "vercel env get"; then vercel env get USE_COMPUTE_JOBS_QUEUE production 2>/dev/null | head -1; else vercel env pull /tmp/.env.probe.19.1 --environment=production --yes >/dev/null 2>&1 && grep -E '^USE_COMPUTE_JOBS_QUEUE=' /tmp/.env.probe.19.1 | head -1; rm -f /tmp/.env.probe.19.1; fi</automated>
  </verify>
  <acceptance_criteria>
    - `CURRENT_VALUE` of `USE_COMPUTE_JOBS_QUEUE` on Vercel production captured verbatim (one of: `true`, `false`, `NOT_SET`, or a custom string) — recorded in summary
    - Vercel CLI authenticated + linked to the correct project (`vercel whoami` confirms helmut.mueller1@gmail.com; `.vercel/project.json` or `.vercel/repo.json` present)
    - SUMMARY records whether `vercel env get` or `vercel env pull` fallback was used (`USE_GET` boolean)
    - Branch decision (skip flip / proceed to flip) documented in summary based on `CURRENT_VALUE == "true"` test
    - If fallback `vercel env pull` was used, the temp file `/tmp/.env.production.19.1` is deleted before task end (no leftover secret on disk)
  </acceptance_criteria>
  <done>Current state of the flag known via captured `CURRENT_VALUE` string. Task 3 / Task 4 path selected.</done>
</task>

<task type="auto">
  <name>Task 3: Set USE_COMPUTE_JOBS_QUEUE=true on Vercel production (if not already)</name>
  <files></files>
  <read_first>
    - Task 2 outcome
  </read_first>
  <action>
    If Task 2 found the value already `true`, SKIP this task — record "already true, no change" in the summary.

    Otherwise:
    1. Run `vercel env add USE_COMPUTE_JOBS_QUEUE production` and respond with value `true` when prompted.
       - For non-interactive: pipe the value: `printf "true\n" | vercel env add USE_COMPUTE_JOBS_QUEUE production` (verify Vercel CLI supports stdin pipe; otherwise the interactive prompt is fine — the user is in the loop on this plan).
    2. Confirm: `vercel env ls production | grep -i USE_COMPUTE_JOBS_QUEUE` shows the new value.
    3. Note in summary: env var change is NOT live until Vercel redeploys — Task 4 forces that.
  </action>
  <verify>
    <automated>vercel env ls production 2>&1 | grep -i USE_COMPUTE_JOBS_QUEUE</automated>
  </verify>
  <acceptance_criteria>
    - If flip needed: `vercel env ls production` shows `USE_COMPUTE_JOBS_QUEUE` present and (after set) equal to `true` (or marked encrypted with a write timestamp post Task 2)
    - If skip path: documented "already true"
  </acceptance_criteria>
  <done>Env var state correct on Vercel production.</done>
</task>

<task type="auto">
  <name>Task 4: Trigger / verify Vercel Next.js production redeploy + emit verdict line</name>
  <files></files>
  <read_first>
    - Recent main HEAD commit timestamp (`git log -1 --format=%cd main`)
  </read_first>
  <action>
    A Vercel env-var change does NOT automatically redeploy. Force a deploy so:
    (a) the flag value is live in the running runtime, AND
    (b) the post-Plan-03 route code is the active production build.

    Steps:
    1. `vercel --prod` from project root (with auth). This triggers a production deploy of the current main HEAD.
       - If a deploy is already in flight or recently completed for main HEAD AFTER the env change, the redeploy is unnecessary — capture the existing deployment URL and proceed.
    2. Wait for deployment URL to return; capture in summary.
    3. Smoke check (read-only, no real upload):
       - `curl -s -o /dev/null -w "%{http_code}\n" https://quantalyze-rho.vercel.app/api/strategies/csv-finalize` → expect 401 or 405 (route exists; auth/method check kicks in). 404 here means the route is missing — STOP.
       - Confirm `vercel.json` or environment dashboard shows the env var bound to the new deployment.
    4. Confirm production URL is `https://quantalyze-rho.vercel.app` per MEMORY reference_prod_url.md (NOT `quantalyze.com` or `quantalyze.vercel.app`).

    5. Only when all the above pass: emit the canonical verdict line on its OWN line at the END of SUMMARY:
       ```bash
       SUMMARY_PATH=".planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-08-SUMMARY.md"
       printf '\n19.1-08-VERDICT: FLIPPED\n' >> "$SUMMARY_PATH"
       grep -c '^19\.1-08-VERDICT: FLIPPED$' "$SUMMARY_PATH" | xargs -I{} test {} -eq 1
       ```

       If the smoke check returns 404 OR the redeploy never completes, DO NOT emit the verdict line. Plan 09 will grep-fail and block the wave.
  </action>
  <verify>
    <automated>SC=$(curl -s -o /dev/null -w "%{http_code}\n" https://quantalyze-rho.vercel.app/api/strategies/csv-finalize 2>&1 | head -1); test "$SC" != "404" && SUMMARY=".planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-08-SUMMARY.md"; test -f "$SUMMARY" && grep -c '^19\.1-08-VERDICT: FLIPPED$' "$SUMMARY" | xargs -I{} test {} -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Production deployment URL captured + alias pointing to `https://quantalyze-rho.vercel.app` confirmed
    - Smoke check returns 401/405 (NOT 404) for the csv-finalize route — route exists
    - Env var is live in the running runtime (Plan 09 will probe via an actual upload)
    - SUMMARY ends with exactly one `19.1-08-VERDICT: FLIPPED` line on its own line
    - `grep -c '^19\.1-08-VERDICT: FLIPPED$'` against the SUMMARY returns exactly `1`
    - Plan 09 (end-to-end verification) is now unblocked
  </acceptance_criteria>
  <done>Vercel production is on main HEAD with the env var live. Pipeline is now end-to-end activated and awaiting Plan 09's E2E smoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel CLI auth → production env vars | wrong account or wrong env scope corrupts production config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-19 | Tampering | env var set on wrong project/team | mitigate | Task 2 confirms `vercel whoami` + `.vercel/project.json` correctness BEFORE any mutation. |
| T-19.1-20 | DoS / availability | redeploy fails mid-flight, leaving production stale | mitigate | Task 4 waits for deployment URL + smoke probe before emitting the verdict line. Rollback: `vercel env rm USE_COMPUTE_JOBS_QUEUE production` per docs/runbooks/compute-queue.md. |
| T-19.1-28 | Information Disclosure | `vercel env pull` fallback writes the entire production env file to /tmp, including unrelated secrets | mitigate | W6 fallback writes to a uuid-suffixed path under /tmp and explicitly `rm -f` the file at task end (Task 2 step 2 + Task 2 acceptance criterion). |
</threat_model>

<verification>
- `vercel env ls production` shows the flag set to `true`
- Production deployment URL returns 401/405 for `/api/strategies/csv-finalize` (not 404)
- Latest deployment is post-env-mutation
- SUMMARY ends with exactly one `19.1-08-VERDICT: FLIPPED` line
</verification>

<success_criteria>
- USE_COMPUTE_JOBS_QUEUE=true on Vercel production
- Next.js redeployed since the env var change
- Production URL confirmed = `https://quantalyze-rho.vercel.app`
- All three flag-gated routes are now enqueue-active (csv-finalize new, finalize-wizard, keys/sync)
- `grep -c '^19\.1-08-VERDICT: FLIPPED$' 19.1-08-SUMMARY.md` returns exactly `1`
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-08-SUMMARY.md` capturing:
- Pre-flip env state + which read path (`vercel env get` vs `vercel env pull`) was used (W6)
- Flip outcome (changed / no-op)
- Deployment URL + smoke check response code
- Confirmation Plan 09 unblocked
- Final canonical verdict line on its own line at the END of the file: `19.1-08-VERDICT: FLIPPED`
</output>
