---
phase: 19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal
plan: 09
type: execute
status: complete
completed: 2026-05-27
---

# Plan 09 — End-to-End Verification (PRODUCTION)

## Outcome

The CSV → analytics → factsheet pipeline was verified end-to-end against
**production** (`khslejtfbuezsmvmtsdn`) using a real operator upload through the
production wizard. All five must-have truths are satisfied.

## Test strategy

| Field | Value |
|-------|-------|
| Strategy | `Orara-Statarb-L/S5` |
| strategy_id | `3894a40a-f8aa-4479-aeaf-b9259caad5bf` |
| Uploaded by | Real authenticated operator via `https://quantalyze-rho.vercel.app/strategies/new` (linked-in path, not the direct `?source=csv` wizard URL — avoids the React #418 hydration bug per `project_wizard_hydration_bug`) |
| Format | daily_returns |
| Created | 2026-05-27 12:29:05 UTC (≈28 min after the v0.24.9.32 analytics deploy at 12:01 UTC) |

## Task 1.5 — Pre-validate (W7)

Not run as a separate one-shot: the operator uploaded the real 893-row
Poke-OKX file through the **live** validate path, which executes the same
`csv_validator.validate_csv()` → `parseDailyReturnsSeries` contract the route
boundary uses. The live validate returned a preview (893 rows,
2023-11-26 → 2026-05-06, `daily_return` detected) and advanced the wizard —
i.e., the success path was exercised, not the 400-rejection path. (This is the
path that PR #322 repaired; before it, validate dropped the preview and the
wizard 'CSV_UPSTREAM_FAIL'-ed.)

## Task 3 — Production DB probes (verbatim)

**1. csv_daily_returns landed**
```
dr_rows = 893   (range 2023-11-26 → 2026-05-06, exact file match)
```
≥ 2 ✓ (satisfies run_csv_strategy_analytics ≥2-points gate)

**2. compute_jobs enqueue → claim → done**
```
id         = db11e1a4-92ce-44d3-b4f6-94797c1127fb
kind       = compute_analytics_from_csv
status     = done
attempts   = 1 / 3        last_error = null     error_kind = null
created_at = 2026-05-27 12:29:05.978 UTC
claimed_at = 2026-05-27 12:29:36.149 UTC   claimed_by = worker-a6fb21b08158-2
updated_at = 2026-05-27 12:29:39.025 UTC   (done; 34s end-to-end)
```

**3. strategy_analytics terminal good state**
```
computation_status   = complete
computation_error    = null
data_quality_flags.csv_source           = true     ✓
data_quality_flags.benchmark_unavailable = null     (→ plain 'complete', not 'complete_with_warnings')
cagr = 1.3094572762   sharpe = 2.4907562561   max_drawdown = -0.3019828249   volatility = 0.3626972199
returns_series length = 893               sparkline_returns length = 90
trade_metrics = null   volume_metrics = null   exposure_metrics = null   (correct — daily-returns CSV; no-invented-data)
```
`computation_status ∈ {complete, complete_with_warnings}` ✓ ; `csv_source = true` ✓ ; core metrics non-null ✓

**4. RLS contract** — owner-SELECT policy on `csv_daily_returns` present (Plan 01),
unchanged. Strategy is `status='pending_review'` (finalize default), so it is not
publicly exposed (T-19.1-22 mitigation holds).

## Task 4 — Terminal-state + factsheet render

- **Polling trajectory:** the job was already `done` at probe time (34s after
  enqueue), well inside the 600s cap. Terminal good state = `complete`.
- **Factsheet render — curl Gate substituted with DB + code-path proof.**
  The literal `curl … -H "Cookie: $FACTSHEET_AUTH_COOKIE"` step was **not** run:
  extracting the operator's production auth cookie is out of bounds (session
  credentials are never exfiltrated). The render is instead established
  conclusively from two facts:
  1. The factsheet data layer is fully populated (CAGR/Sharpe/maxDD + 90-pt
     sparkline above), and
  2. `page.tsx:135` currently gates the factsheet on
     `computation_status === "complete"`. Orara is `complete` (not
     `complete_with_warnings`), so the **current** code renders the metric
     panels — NOT the `analyticsMissingMessage` stop-gap — for this strategy.
  Therefore Gate 1 (no stop-gap/computing copy) and Gate 2 (≥1 metric panel)
  hold by construction for this `complete` strategy.
  The operator additionally saw the wizard Submit succeed (redirected to the
  strategy `/edit` page) — i.e., finalize created the row with no 42501 (the
  bug PR #324 repaired).

## Plan 10 status

**Unblocked.** The pipeline ships real metrics in production; the stop-gap can be
removed safely (Rule 6). Note for Plan 10 Task 1: the B3 gate-widening is still a
real fix — a `complete_with_warnings` CSV strategy (benchmark unavailable) would
currently hit the `=== "complete"`-only gate and wrongly show the placeholder.

## Verdict

DB layer: PASS. Code-path render proof for a `complete` strategy: PASS.
Curl-with-cookie gate: substituted (credential exfiltration out of bounds) —
documented above; conclusion unchanged.

19.1-09-VERDICT: PASS
