---
phase: 19.1
plan: 06
applied_to: khslejtfbuezsmvmtsdn (PRODUCTION Supabase)
applied_at: 2026-05-22
status: complete
verdict: PROD-APPLIED
---

# Plan 19.1-06 — Apply Migrations to PRODUCTION Supabase

19.1-06-VERDICT: PROD-APPLIED

## Pre-Flight Audits (Tasks 1.5 + 1.6) — ✅

Pre-apply read-only probes against PROD before any DDL:

| Audit | Expected | Observed |
|-------|----------|----------|
| In-flight `compute_jobs` rows (pending/claimed/running) | 0 (no lock-contention risk) | **0** ✅ |
| Existing rows that would violate new strict-superset CHECK | 0 | **0** ✅ |
| `compute_analytics_from_csv` branch already on coherence constraint | false (clean apply, not re-run) | **false** ✅ |
| `process_key_long` branch on coherence constraint (migration 108 baseline) | true | **true** ✅ |
| `csv_daily_returns` table already exists | 0 (clean apply) | **0** ✅ |
| `compute_analytics_from_csv` kind already registered | 0 (clean apply) | **0** ✅ |

No lock contention risk, no in-flight job rows, no constraint violators. Safe to apply.

## Applied (Task 2 + 2.5)

- **Migration 1** — `20260522120000_csv_daily_returns.sql` via Supabase MCP `apply_migration` (name: `csv_daily_returns`). Response: `{"success":true}`. Self-verifying DO block raised no exception.
- **Migration 2** — `20260522120100_compute_analytics_from_csv_kind.sql` via Supabase MCP `apply_migration` (name: `compute_analytics_from_csv_kind`). Response: `{"success":true}`. Self-verifying DO block raised no exception.

## Post-Apply Probes (Task 3) — 7/7 ✅

### Probe 1 — `csv_daily_returns` table shape
| Field | Expected | PROD |
|-------|----------|------|
| columns_found | 5 | **5** ✅ |
| index_count | 1 | **1** ✅ |
| redundant index | 0 | **0** ✅ |

### Probe 2 — `persist_csv_daily_returns` RPC + GRANT shape
| Field | Expected | PROD |
|-------|----------|------|
| prosecdef | true | **true** ✅ |
| grantees | service_role,authenticated,postgres | **service_role,authenticated,postgres** ✅ |
| anon grant | 0 | **0** ✅ |

### Probe 3 — Hardening invariants in RPC body
| Field | Expected | PROD |
|-------|----------|------|
| has `ERRCODE='42501'` | true | **true** ✅ |
| count 42501 raises | 3 | **3** ✅ |
| count 22023 raises | 3 | **3** ✅ |
| jsonb_typeof guard | true | **true** ✅ |
| search_path pinned | true | **true** ✅ |

### Probe 4 — Coherence constraint (strict-superset preserved)
| Kind | PROD |
|------|------|
| compute_portfolio | ✅ |
| rescore_allocator | ✅ |
| poll_allocator_positions | ✅ |
| reconstruct_allocator_history | ✅ |
| refresh_allocator_equity_daily | ✅ |
| process_key_long | ✅ |
| **compute_analytics_from_csv (NEW)** | ✅ |

### Probe 5 — Kinds registry
| Field | PROD |
|-------|------|
| `compute_analytics_from_csv` registered | **1** ✅ |
| `process_key_long` preserved | **1** ✅ |

### Probe 6 — RLS shape
| Field | PROD |
|-------|------|
| RLS enabled on `csv_daily_returns` | **true** ✅ |
| named policies | **3** ✅ |

### Probe 7 — In-flight constraint violators
| Field | PROD |
|-------|------|
| violator_count | **0** ✅ |

## Cross-Project Schema Parity (Task 4 / W9 revision) — 6/6 ✅

Diffs PROD probe payloads against Plan 04 TEST SUMMARY:

| Probe | TEST | PROD | Match |
|-------|------|------|-------|
| 1 (table shape) | 5 cols, 1 index, 0 redundant | 5 cols, 1 index, 0 redundant | ✅ |
| 2 (RPC GRANT shape) | prosecdef=true, anon=0, authenticated+service_role+postgres | identical (grantee order differs but contents identical) | ✅ |
| 3 (hardening) | 42501×3, 22023×3, jsonb_typeof, search_path | identical | ✅ |
| 4 (constraint shape) | new kind admitted, all 7 existing preserved | identical | ✅ |
| 5 (kinds registry) | new=1, process_key_long=1 | identical | ✅ |
| 6 (RLS) | enabled, 3 policies | identical | ✅ |
| 7 (no violators) | 0 | 0 | ✅ |

TEST and PROD schemas are bit-for-bit equivalent on every meaningful invariant.

## Hardening Preserved

- **PR #272** — collapsed 42501 probe-oracle close (Probe 3: 3 raises) ✅
- **PR #272** — no redundant explicit index (Probe 1: 0) ✅
- **PR #272** — GRANT TO authenticated with inline justification (Probe 2 + RPC source) ✅
- **PR #272** — `jsonb_typeof` guard (Probe 3) ✅
- **Migration 108 contract** — `process_key_long` branch preserved (Probes 4, 5) ✅

## Threats Mitigated

- **T-19.1-01** — probe-oracle (verified on PROD: 3 distinct 42501 raises, collapsed missing+wrong-owner)
- **T-19.1-02** — RLS bypass via SECURITY DEFINER misconfig (prosecdef=true, anon grant=0, search_path pinned)

## Deviations

None. Both migrations applied cleanly. Self-verifying DO blocks passed. Cross-project parity verified.

## Rollback Path (Reference)

Not invoked — both migrations applied successfully on first attempt. Documented for future reference:

- **Case A — migration 1 applied + migration 2 failed:** keep migration 1 (table is empty, RPC is harmless); retry migration 2.
- **Case B — migration 2 applied + migration 1 failed:** structurally impossible in MCP-applied order; if MCP partial failure mid-DDL occurs, run `ALTER TABLE compute_jobs DROP CONSTRAINT compute_jobs_kind_target_coherence; ALTER TABLE compute_jobs ADD CONSTRAINT compute_jobs_kind_target_coherence CHECK (...) <old shape from supabase/migrations/20260510175507_process_key_long_compute_job_kinds_repair.sql>;`.
- **Case C — full unwind:** `DROP TABLE public.csv_daily_returns; DROP FUNCTION public.persist_csv_daily_returns(UUID, UUID, JSONB);` then revert constraint per Case B.

## Outputs Consumed By

- **Plan 07** (Railway deploy) — gates on `19.1-06-VERDICT: PROD-APPLIED` in this SUMMARY
- **Plan 09** (E2E verification) — depends on this schema being live on PROD for the test CSV upload

## Verdict Line

`19.1-06-VERDICT: PROD-APPLIED`
