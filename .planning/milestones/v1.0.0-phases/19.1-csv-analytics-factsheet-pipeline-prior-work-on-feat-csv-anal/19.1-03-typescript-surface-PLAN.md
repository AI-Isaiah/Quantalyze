---
phase: 19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/components/strategy/SyncProgress.tsx
  - src/app/api/strategies/csv-finalize/route.ts
  - src/__tests__/csv-validate-route.test.ts
autonomous: true
requirements: []  # Maps to CONTEXT.md Tasks 7, 8, 9, 10, 11
threat_model_refs: [T-19.1-04, T-19.1-05]

must_haves:
  truths:
    - "computation_status TS union admits 'complete_with_warnings' as a first-class DB status"
    - "AnalyticsDataQualityFlags supports optional csv_source?: boolean"
    - "parseDailyReturnsSeries validates array shape, ≤5000 rows, YYYY-MM-DD, finite numbers, and duplicate dates (returns 400 CSV_INVALID_FORMAT on any failure)"
    - "csv-finalize route calls persist_csv_daily_returns RPC after applyCsvMetadataUpdate; persist error → 500 CSV_PERSIST_FAIL"
    - "unifiedCsvFinalizeHandler signature explicitly receives dailyReturnsSeries via param (no closure capture)"
    - "after() block enqueues compute_analytics_from_csv on BOTH legacy + unified-backbone paths, gated on USE_COMPUTE_JOBS_QUEUE"
    - "BOTH enqueue_compute_job call sites carry @audit-skip pragma with PR #275 justification text"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "computation_status union + csv_source flag"
      contains: "complete_with_warnings"
    - path: "src/components/strategy/SyncProgress.tsx"
      provides: "exhaustive switch over computation_status"
      contains: "complete_with_warnings"
    - path: "src/app/api/strategies/csv-finalize/route.ts"
      provides: "parseDailyReturnsSeries + persist RPC + after() enqueue"
      contains: "parseDailyReturnsSeries"
    - path: "src/__tests__/csv-validate-route.test.ts"
      provides: "tests for parse + persist + unified + enqueue paths"
      contains: "parseDailyReturnsSeries"
  key_links:
    - from: "src/app/api/strategies/csv-finalize/route.ts"
      to: "supabase RPC persist_csv_daily_returns"
      via: "supabase.rpc cast-through-unknown pattern"
      pattern: "persist_csv_daily_returns"
    - from: "src/app/api/strategies/csv-finalize/route.ts"
      to: "compute_jobs (enqueue_compute_job RPC)"
      via: "after() block gated on USE_COMPUTE_JOBS_QUEUE"
      pattern: "after\\(async \\(\\) =>"
    - from: "src/components/strategy/SyncProgress.tsx"
      to: "src/lib/types.ts (computation_status)"
      via: "exhaustive switch enforced by tsc"
      pattern: "complete_with_warnings"
---

<objective>
Ship the full TypeScript surface for the CSV → analytics pipeline:

1. `src/lib/types.ts` — extend `computation_status` union with `"complete_with_warnings"` (first-class DB state); add optional `csv_source?: boolean` to `AnalyticsDataQualityFlags`.
2. `src/components/strategy/SyncProgress.tsx` — extend exhaustive switch; refresh jsdoc (PR #274 — no stale "complete_with_warnings has no DB representation" narrative).
3. `src/app/api/strategies/csv-finalize/route.ts` — add `parseDailyReturnsSeries()` route-boundary validator with duplicate-date guard (PR #274 mitigation, T-19.1-04); call `persist_csv_daily_returns` RPC after `applyCsvMetadataUpdate` (legacy path); mirror the persist call on the unified-backbone path via explicit `dailyReturnsSeries` param (no closure capture); `after()` enqueue on BOTH paths with `@audit-skip` pragma carrying PR #275 justification text (T-19.1-05).
4. `src/__tests__/csv-validate-route.test.ts` — extend with parse + persist + unified + enqueue tests using the `vi.mock("next/server")` Promise-storing after-shim pattern (RESEARCH.md Pattern 4).

Purpose: This is where the request validation, persistence, and job-enqueue all converge at the route boundary. Without this, the worker has no data to compute against and the wizard cannot signal "compute me".

Output: Code + tests on disk. The route is live as soon as Vercel redeploys (Plan 08 — env flip + redeploy is gated on Plan 06 + Plan 07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-CONTEXT.md
@.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-RESEARCH.md
@src/lib/types.ts
@src/components/strategy/SyncProgress.tsx
@src/app/api/strategies/csv-finalize/route.ts
@src/__tests__/csv-validate-route.test.ts
@src/app/api/strategies/finalize-wizard/route.ts
@src/app/api/strategies/finalize-wizard/route.test.ts

<interfaces>
<!-- Existing — from src/lib/types.ts current state (line ~267): -->
//   computation_status?: "pending" | "computing" | "complete" | "failed"
// Extension (Task 7): add "complete_with_warnings" as first-class state.

<!-- New: -->
export interface CsvDailyReturnRow {
  date: string;            // YYYY-MM-DD
  daily_return: number;    // finite
}

// Existing supabase.rpc cast-through-unknown shape from csv-finalize/route.ts:341-356 (finalize_csv_strategy)
// Mirror for new RPC:
type PersistCsvDailyReturnsRpc = (
  fn: "persist_csv_daily_returns",
  args: { p_user_id: string; p_strategy_id: string; p_rows: CsvDailyReturnRow[] }
) => Promise<{ data: number | null; error: { code?: string; message?: string } | null }>;

<!-- after() shim — existing pattern from finalize-wizard/route.ts:619-622 -->
// after(async () => { if (process.env.USE_COMPUTE_JOBS_QUEUE !== "true") return; ... admin.rpc("enqueue_compute_job", ...) ... })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend computation_status union + csv_source flag + SyncProgress exhaustive switch</name>
  <files>src/lib/types.ts, src/components/strategy/SyncProgress.tsx</files>
  <read_first>
    - src/lib/types.ts (current `computation_status` union near line 267; current `AnalyticsDataQualityFlags` interface)
    - src/components/strategy/SyncProgress.tsx (existing exhaustive switch + jsdoc — the stale "complete_with_warnings has no DB representation" claim must be removed)
    - .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-CONTEXT.md (decisions block — TypeScript surface, Task 7)
    - .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-RESEARCH.md (Pitfall 3 — extending the union is non-breaking for `=== "complete"` consumers)
    - Prior-art SHA: `git show beba208a:src/lib/types.ts`
    - Prior-art SHA: `git show 3d004118:src/components/strategy/SyncProgress.tsx` (PR #274 jsdoc refresh)
  </read_first>
  <behavior>
    - Behavior 1: `tsc --noEmit` passes after the union extension.
    - Behavior 2: A consumer that switches on `computation_status` with `default: assertNever(x)` either handles `complete_with_warnings` explicitly or the build fails — so the SyncProgress switch MUST add the new branch.
    - Behavior 3: `AnalyticsDataQualityFlags` allows `csv_source?: boolean` without breaking any existing flag.
    - Behavior 4: SyncProgress jsdoc no longer claims "complete_with_warnings has no DB representation".
  </behavior>
  <action>
    `src/lib/types.ts`:
    - Locate the `computation_status` field definition on the `strategy_analytics` row type (current shape: `"pending" | "computing" | "complete" | "failed"`).
    - Extend to: `"pending" | "computing" | "complete" | "complete_with_warnings" | "failed"`.
    - Locate `AnalyticsDataQualityFlags` interface. Add `csv_source?: boolean;` as an optional property. Preserve all existing flags.

    `src/components/strategy/SyncProgress.tsx`:
    - Locate the exhaustive switch on `computation_status` (likely uses `assertNever` in default branch).
    - Add a branch for `case "complete_with_warnings":` — routes to the same UI as `case "complete":` UNLESS the existing code has separate "complete" + "complete_with_warnings" treatment per PR #274; if a separate treatment exists in the prior-art SHA, mirror it. Default behavior: render same as "complete".
    - Update jsdoc above the switch: remove any sentence stating "complete_with_warnings has no DB representation" or equivalent stale narrative. Replace with: "complete_with_warnings is a first-class DB status written by run_strategy_analytics when consumer-specific flags fire; run_csv_strategy_analytics writes plain 'complete'."

    Run `npx tsc --noEmit` to verify the union extension doesn't break any consumer. If a consumer uses `assertNever` on `computation_status` and `tsc` flags it, add the missing branch (defaulting to the "complete" rendering path).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"complete_with_warnings"' src/lib/types.ts` ≥ 1 (union member present)
    - `grep -c "csv_source" src/lib/types.ts` ≥ 1 (new flag present in AnalyticsDataQualityFlags)
    - `grep -c "complete_with_warnings" src/components/strategy/SyncProgress.tsx` ≥ 1 (switch branch added)
    - `grep -c "has no DB representation" src/components/strategy/SyncProgress.tsx` = 0 (stale narrative removed — PR #274)
    - `npx tsc --noEmit` exits 0 (no type errors in the codebase)
    - No production behavior change beyond the new union member rendering as "complete"-equivalent (verify by running the existing SyncProgress vitest suite if one exists)
  </acceptance_criteria>
  <done>Types extended. Switch exhaustive. jsdoc refreshed. tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add parseDailyReturnsSeries + persist RPC + unified-backbone mirror + after() enqueue</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/__tests__/csv-validate-route.test.ts</files>
  <read_first>
    - src/app/api/strategies/csv-finalize/route.ts (CURRENT state on main — read the full file to understand existing finalize_csv_strategy call site at line ~341-356 + applyCsvMetadataUpdate location + isUnifiedBackboneActive branch + unifiedCsvFinalizeHandler signature)
    - src/app/api/strategies/finalize-wizard/route.ts (lines 619-644 — canonical after() + USE_COMPUTE_JOBS_QUEUE pattern to mirror)
    - src/app/api/strategies/finalize-wizard/route.test.ts (lines 229-245 — vi.mock("next/server") Promise-storing shim to copy)
    - src/__tests__/csv-validate-route.test.ts (existing tests + fixtures to extend)
    - src/lib/types.ts (post-Task-1 — for `CsvDailyReturnRow` export)
    - .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-CONTEXT.md (decisions block — TypeScript surface, Tasks 8-11)
    - .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-RESEARCH.md (Code Examples section for parseDailyReturnsSeries — verbatim canonical body; "persist_csv_daily_returns RPC call site"; "after() enqueue with USE_COMPUTE_JOBS_QUEUE gate + @audit-skip pragma" — verbatim block)
    - Prior-art SHAs: `git show 2ed9bcdd:src/app/api/strategies/csv-finalize/route.ts` (Task 8 — parse); `git show d493c802:src/app/api/strategies/csv-finalize/route.ts` (Task 9 — persist); `git show 669a2314:src/app/api/strategies/csv-finalize/route.ts` (Task 10 — unified mirror); `git show 45d74aa0:src/app/api/strategies/csv-finalize/route.ts` (Task 11 — after()); `git show 17705685:src/app/api/strategies/csv-finalize/route.ts` (PR #275 — @audit-skip pragmas final state)
    - **AGENTS.md mandate:** "This is NOT the Next.js you know — read node_modules/next/dist/docs/" — verify `import { after } from "next/server"` shape via `grep -r "import.*after.*from.*next/server" src/` (existing pattern; do NOT switch to `next/cache` `unstable_after`)
  </read_first>
  <behavior>
    - Test 1 (parse — array shape): POST body with `daily_returns_series: "not-an-array"` → 400 `CSV_INVALID_FORMAT`, message mentions "must be an array".
    - Test 2 (parse — size cap): POST with 5001 rows → 400 `CSV_INVALID_FORMAT`, message mentions 5000.
    - Test 3 (parse — date regex): POST with `{date: "2024-1-1", daily_return: 0.01}` → 400 `CSV_INVALID_FORMAT`, message mentions row 0 / date.
    - Test 4 (parse — finite number): POST with `{date: "2024-01-01", daily_return: NaN}` → 400.
    - Test 5 (parse — duplicate date, PR #274 T-19.1-04 mitigation): POST with two rows sharing `date: "2024-01-01"` → 400 `CSV_INVALID_FORMAT`, message mentions "duplicate date" AND the offending row index.
    - Test 6 (persist call on legacy path): mock the supabase RPC chain so `finalize_csv_strategy` returns `newStrategyId`; assert `persist_csv_daily_returns` RPC is invoked with `{p_user_id, p_strategy_id: newStrategyId, p_rows}`.
    - Test 7 (persist failure → 500 CSV_PERSIST_FAIL): mock `persist_csv_daily_returns` to return `{error: {code: "42501", message: "not accessible"}}`; assert response is 500 with `code: "CSV_PERSIST_FAIL"`, human_message mentioning support contact, `strategy_id` in debug_context.
    - Test 8 (unified-backbone path receives dailyReturnsSeries via explicit param, B4 revision 2026-05-22): TWO-PART check — runtime + source:
      (a) Runtime: mock `isUnifiedBackboneActive()` to return true; assert `unifiedCsvFinalizeHandler` is called with an args object whose `dailyReturnsSeries` property === the parsed rows.
      (b) Source-shape: `import fs from "node:fs"; const src = fs.readFileSync("src/app/api/strategies/csv-finalize/route.ts","utf-8"); expect(src).toMatch(/async function unifiedCsvFinalizeHandler\(args: \{[^}]*dailyReturnsSeries:\s*(readonly\s+)?CsvDailyReturnRow\[\]/)` — the handler signature must spell out the typed field, so closure capture is detectable as a regression.
      (c) Arity lock: `expect(unifiedCsvFinalizeHandler.length).toBeGreaterThanOrEqual(<arity_after_change>)` where `<arity_after_change>` = the function's `.length` (number of declared, non-defaulted positional params) after this change lands; record the literal integer in the test the moment the implementation stabilises so a silent param drop fails the test.
    - Test 9 (after() enqueue when flag=true): set `process.env.USE_COMPUTE_JOBS_QUEUE = "true"`; use the Promise-storing `vi.mock("next/server")` shim; after the route returns, await the stored Promise and assert `enqueue_compute_job` was called with `{p_strategy_id: newStrategyId, p_kind: "compute_analytics_from_csv", p_metadata: {source: "csv-finalize", fmt}}`.
    - Test 10 (after() no-op when flag absent): unset/`"false"` `USE_COMPUTE_JOBS_QUEUE`; after the route returns and the Promise resolves, assert `enqueue_compute_job` was NEVER called.
    - Test 11 (after() failure logs but does NOT 500): mock the enqueue to throw; assert response is still 200 and `console.warn` (or whichever logger is wired) received a message containing "non-blocking".
  </behavior>
  <action>
    Tests first (`src/__tests__/csv-validate-route.test.ts` — EXTEND existing file):
    - Add 11 new test cases per the behaviors above. Reuse existing setup/teardown.
    - **Critical: use the Promise-storing `vi.mock("next/server")` shim from `finalize-wizard/route.test.ts:229-245`** (RESEARCH.md Pattern 4 + Pitfall 2). The shim shape:
      ```typescript
      const STATE = { runAfterCallback: false, afterPromise: undefined as Promise<unknown> | undefined };
      vi.mock("next/server", async () => {
        const actual = await vi.importActual<typeof import("next/server")>("next/server");
        return {
          ...actual,
          after: (fn: () => unknown) => {
            if (STATE.runAfterCallback) {
              STATE.afterPromise = Promise.resolve().then(fn).catch(() => {});
            }
          },
        };
      });
      ```
    - For enqueue tests, set `STATE.runAfterCallback = true` and `await STATE.afterPromise` after the route handler returns, to flush the deterministic Promise.
    - Run `npx vitest run src/__tests__/csv-validate-route.test.ts` — expect all 11 new tests failing.

    Implementation (`src/app/api/strategies/csv-finalize/route.ts`):
    Read the current file end-to-end first. Then apply the following changes in order:

    A) **`parseDailyReturnsSeries` helper** (top of file, near imports):
    Define exports verbatim per RESEARCH.md Code Examples "parseDailyReturnsSeries — route boundary validator with duplicate-date guard" (lines ~813-876). Includes:
    - `MAX_DAILY_RETURNS_ROWS = 5000`
    - `DATE_REGEX = /^\d{4}-\d{2}-\d{2}$/`
    - `export interface CsvDailyReturnRow { date: string; daily_return: number; }`
    - `function parseDailyReturnsSeries(raw: unknown): { ok: true; rows: CsvDailyReturnRow[] } | { ok: false; code: string; message: string; debug_context?: Record<string, unknown> }`
    - **Duplicate-date guard (T-19.1-04, PR #274):** track `seen = new Set<string>()`; if `seen.has(r.date)` return 400 `CSV_INVALID_FORMAT` with `duplicate date at row ${i}: ${r.date}` and `debug_context: {row: i, date: r.date}`.

    B) **Call parseDailyReturnsSeries** in the POST handler after body parse:
    - Extract `daily_returns_series` from request body.
    - `const parsed = parseDailyReturnsSeries(daily_returns_series);`
    - If `!parsed.ok`: return `NextResponse.json({ok: false, code: parsed.code, human_message: parsed.message, debug_context: parsed.debug_context ?? {}, correlation_id: null}, {status: 400})`.

    C) **Legacy path: persist_csv_daily_returns RPC** after `applyCsvMetadataUpdate`:
    Use the cast-through-unknown pattern verbatim per RESEARCH.md "persist_csv_daily_returns RPC call site". Body:
    ```typescript
    // database.types.ts hasn't been regenerated; mirror the existing
    // finalize_csv_strategy cast pattern at csv-finalize/route.ts:341-356.
    const { error: persistError } = await (
      supabase.rpc as unknown as (
        fn: "persist_csv_daily_returns",
        args: { p_user_id: string; p_strategy_id: string; p_rows: CsvDailyReturnRow[] },
      ) => Promise<{ data: number | null; error: { code?: string; message?: string } | null }>
    )("persist_csv_daily_returns", {
      p_user_id: user.id,
      p_strategy_id: newStrategyId,
      p_rows: parsed.rows,
    });
    if (persistError) {
      console.error("[strategies/csv-finalize] persist_csv_daily_returns error:", persistError.code, persistError.message);
      return NextResponse.json(
        { ok: false, code: "CSV_PERSIST_FAIL", human_message: "Your strategy was created but the daily-return data could not be saved. Contact support@quantalyze.com with your strategy id so we can recover.", debug_context: { strategy_id: newStrategyId }, correlation_id: null },
        { status: 500 },
      );
    }
    ```

    D) **Unified-backbone path mirror** — locate the `isUnifiedBackboneActive()` branch invoking `unifiedCsvFinalizeHandler(...)`. Extend the args object passed to `unifiedCsvFinalizeHandler`:
    - Add `dailyReturnsSeries: parsed.rows` to the args.
    - **Explicit param** — do NOT rely on closure capture (per CONTEXT.md decision + Anti-patterns). The `unifiedCsvFinalizeHandler` function signature MUST receive `dailyReturnsSeries: CsvDailyReturnRow[]`.
    - If `unifiedCsvFinalizeHandler` is defined in this file, extend its signature to accept the new param and use it inside the handler to invoke `persist_csv_daily_returns` (mirror the legacy path's persist block).
    - If it lives in a sibling file, extend the signature there too (file path determined by reading current source).

    E) **after() enqueue block** — append AFTER the persist call on the legacy path, and inside `unifiedCsvFinalizeHandler` on the unified path. Body verbatim per RESEARCH.md Code Examples "after() enqueue with USE_COMPUTE_JOBS_QUEUE gate + @audit-skip pragma":
    ```typescript
    if (newStrategyId && parsed.rows.length > 0) {
      after(async () => {
        if (process.env.USE_COMPUTE_JOBS_QUEUE !== "true") return;
        try {
          const { createAdminClient } = await import("@/lib/supabase/admin");
          const admin = createAdminClient();
          // @audit-skip: compute_jobs enqueue is internal worker-state
          // scheduling, not a user-visible mutation. User intent is already
          // captured by the finalize_csv_strategy + persist_csv_daily_returns
          // RPCs above. Mirrors finalize-wizard's enqueue (which evades the
          // gate only via incidental multi-line formatting).
          const { error: enqueueErr } = await admin.rpc("enqueue_compute_job", {
            p_strategy_id: newStrategyId,
            p_kind: "compute_analytics_from_csv",
            p_metadata: { source: "csv-finalize", fmt },
          });
          if (enqueueErr) {
            console.warn(`[strategies/csv-finalize] enqueue_compute_analytics_from_csv failed (non-blocking): ${enqueueErr.message}`);
          }
        } catch (err) {
          console.warn(`[strategies/csv-finalize] enqueue side-effect threw (non-blocking): ${err instanceof Error ? err.message : String(err)}`);
        }
      });
    }
    ```
    - The `@audit-skip:` pragma comment is **load-bearing** (T-19.1-05, PR #275) — `src/__tests__/audit-coverage.test.ts` enforces 8-line lookback and requires the pragma to cite `finalize_csv_strategy + persist_csv_daily_returns` as the upstream user-intent RPCs.
    - Apply pragma on BOTH enqueue call sites (legacy + unified). Each occurrence must include the full justification text (verbatim).

    Run all tests after wiring:
    - `npx vitest run src/__tests__/csv-validate-route.test.ts` — all 11 new tests + existing tests pass
    - `npx vitest run src/__tests__/audit-coverage.test.ts` — passes (pragmas detected on both enqueue sites)
    - `npx tsc --noEmit` — clean
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/csv-validate-route.test.ts src/__tests__/audit-coverage.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "parseDailyReturnsSeries" src/app/api/strategies/csv-finalize/route.ts` ≥ 2 (definition + invocation)
    - `grep -c "CSV_INVALID_FORMAT" src/app/api/strategies/csv-finalize/route.ts` ≥ 5 (array shape, size cap, date regex, finite number, duplicate date)
    - `grep -c "duplicate date" src/app/api/strategies/csv-finalize/route.ts` ≥ 1 (T-19.1-04 — PR #274 dup-date guard)
    - `grep -c "persist_csv_daily_returns" src/app/api/strategies/csv-finalize/route.ts` ≥ 2 (legacy + unified — explicit, no closure capture)
    - `grep -c "CSV_PERSIST_FAIL" src/app/api/strategies/csv-finalize/route.ts` ≥ 1
    - `grep -c "after(async" src/app/api/strategies/csv-finalize/route.ts` ≥ 2 (legacy + unified after-blocks)
    - `grep -c "compute_analytics_from_csv" src/app/api/strategies/csv-finalize/route.ts` ≥ 2
    - `grep -c "@audit-skip" src/app/api/strategies/csv-finalize/route.ts` ≥ 2 (T-19.1-05 — PR #275 pragmas on BOTH enqueue sites)
    - `grep -c "finalize_csv_strategy + persist_csv_daily_returns" src/app/api/strategies/csv-finalize/route.ts` ≥ 2 (justification text in each pragma)
    - **(B4 revision 2026-05-22) Strict signature-shape grep — must show `dailyReturnsSeries` in the `unifiedCsvFinalizeHandler` signature, not merely as a closure-captured identifier:**
      `grep -E "async function unifiedCsvFinalizeHandler\(args: \{[^}]*dailyReturnsSeries:\s*(readonly\s+)?CsvDailyReturnRow\[\]" src/app/api/strategies/csv-finalize/route.ts | wc -l | xargs -I{} test {} -ge 1`
      (If `unifiedCsvFinalizeHandler` lives in a sibling file, run the same regex against that file path instead — both are acceptable so long as the handler signature contains the typed `dailyReturnsSeries: CsvDailyReturnRow[]` field in the args object literal.)
    - `npx vitest run src/__tests__/csv-validate-route.test.ts` exits 0; expect ≥ 11 NEW test cases on top of existing
    - `npx vitest run src/__tests__/audit-coverage.test.ts` exits 0 (pragmas detected on both enqueue sites)
    - `npx tsc --noEmit` exits 0
    - `import { after } from "next/server"` (NOT `next/cache`) — `grep "import.*after.*from" src/app/api/strategies/csv-finalize/route.ts` matches `"next/server"`
  </acceptance_criteria>
  <done>Route parses, persists, mirrors to unified path, enqueues with audit pragma. All 11+ tests green. tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard client → /api/strategies/csv-finalize | untrusted `daily_returns_series` JSON crosses into Next.js route |
| route → persist_csv_daily_returns RPC | route-validated rows cross into SECURITY DEFINER body |
| route → enqueue_compute_job RPC (via after) | internal worker-state mutation not user-driven |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-04 | Information Disclosure | UNIQUE (strategy_id, date) leaks 23505 as a 500 | mitigate | PR #274 — parseDailyReturnsSeries duplicate-date guard at route boundary returns clean 400 `CSV_INVALID_FORMAT` with debug_context.row. Test 5 of Task 2 asserts the 400 + message + row index. Defense-in-depth: even if a guarded row slips through, the RPC's ON CONFLICT DO UPDATE makes the upsert idempotent (no 23505 surface). |
| T-19.1-05 | Repudiation | audit-coverage.test.ts false positive on internal compute_jobs enqueue | mitigate | PR #275 — `@audit-skip:` pragma with justification text "compute_jobs enqueue is internal worker-state scheduling... User intent is already captured by the finalize_csv_strategy + persist_csv_daily_returns RPCs above" placed within the 8-line lookback on BOTH enqueue call sites. Acceptance criteria asserts the count ≥ 2 + the justification text present ≥ 2. |
| T-19.1-10 | Tampering | Unified-backbone path receives dailyReturnsSeries via closure capture (type system can't enforce) | mitigate | Decision-locked in CONTEXT.md: `unifiedCsvFinalizeHandler` signature **explicitly** extended with `dailyReturnsSeries: CsvDailyReturnRow[]`. Acceptance criteria + Test 8 of Task 2 enforce. |
| T-19.1-11 | Denial of Service | after() block 500s on enqueue failure, breaking the wizard despite successful persistence | mitigate | The after() block is wrapped in try/catch around the admin.rpc call; failure logs `console.warn(...non-blocking...)` and never propagates. Test 11 of Task 2 asserts response remains 200 when enqueue throws. |
| T-19.1-12 | Information Disclosure | NaN / Infinity in daily_return reaches RPC | mitigate | `Number.isFinite(r.daily_return)` guard in parseDailyReturnsSeries returns 400. Test 4 of Task 2. |
</threat_model>

<verification>
- `npx vitest run src/__tests__/csv-validate-route.test.ts` green with ≥ 11 new test cases
- `npx vitest run src/__tests__/audit-coverage.test.ts` green (PR #275 audit pragmas detected)
- `npx tsc --noEmit` clean
- Full vitest suite passes: `npx vitest run` (no regression to existing 4400+ tests)
- Coverage ≥ 60% (vitest threshold)
</verification>

<success_criteria>
- `parseDailyReturnsSeries` exported helper validates all 5 invariants (array shape, size cap, date regex, finite number, duplicate date)
- `persist_csv_daily_returns` RPC called on BOTH legacy + unified-backbone paths
- `unifiedCsvFinalizeHandler` signature has explicit `dailyReturnsSeries: CsvDailyReturnRow[]` (no closure capture)
- `after()` enqueue block on BOTH paths with `@audit-skip` pragma carrying the verbatim PR #275 justification text
- `computation_status` TS union admits `complete_with_warnings`; `SyncProgress` exhaustive switch updated; stale jsdoc removed (PR #274)
- All four hardening regressions explicitly prevented (PR #272/273/274/275 — Tasks 1+2 above + cross-references in Plans 01+02)
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-03-SUMMARY.md` listing each new export + the test files extended + a checklist that all four hardening fixes (PR #272/273/274/275) are preserved.
</output>
