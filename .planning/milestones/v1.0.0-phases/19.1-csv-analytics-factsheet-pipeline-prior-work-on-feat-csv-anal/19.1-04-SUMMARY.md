---
phase: 19.1
plan: 04
applied_to: qmnijlgmdhviwzwfyzlc (TEST Supabase)
applied_at: 2026-05-22
status: complete
---

# Plan 19.1-04 — Apply Migrations to TEST Supabase

## Executed

- **Migration 1** — `20260522120000_csv_daily_returns.sql` applied via Supabase MCP `apply_migration` (name: `csv_daily_returns`). Response: `{"success":true}`.
- **Migration 2** — `20260522120100_compute_analytics_from_csv_kind.sql` applied via Supabase MCP `apply_migration` (name: `compute_analytics_from_csv_kind`). Response: `{"success":true}`.

Both migrations' self-verifying DO blocks raised no exception → schema landed as designed.

## Probe Results (7/7 ✅)

These probe payloads are the source-of-truth that Plan 06 Task 4 (cross-project schema parity) diffs against the PROD project after that plan applies the same two migrations.

### Probe 1 — `csv_daily_returns` table shape

| Field | Expected | Actual |
|-------|----------|--------|
| columns_found | 5 (`strategy_id`, `date`, `daily_return`, `created_at`, `updated_at`) | **5** |
| index_count | 1 (primary key only) | **1** |
| redundant `csv_daily_returns_strategy_date_idx` | 0 (PR #272 hardening) | **0** ✅ |

### Probe 2 — `persist_csv_daily_returns` RPC + GRANT shape

| Field | Expected | Actual |
|-------|----------|--------|
| `prosecdef` | true (SECURITY DEFINER) | **true** ✅ |
| grantees | `postgres,authenticated,service_role` (no anon) | **postgres,authenticated,service_role** ✅ |
| anon grant | 0 | **0** ✅ |

### Probe 3 — Hardening invariants in RPC body

| Field | Expected | Actual |
|-------|----------|--------|
| has `ERRCODE = '42501'` | true | **true** ✅ |
| count of `42501` raises | ≥ 3 (probe-oracle collapse) | **3** ✅ |
| count of `22023` raises | ≥ 3 (type/length/empty guards) | **3** ✅ |
| `jsonb_typeof` guard | present | **true** ✅ |
| `search_path = public, pg_catalog` | present | **true** ✅ |

### Probe 4 — `compute_jobs_kind_target_coherence` constraint shape (strict superset preserved)

| Existing kind | Preserved |
|---------------|-----------|
| compute_portfolio | ✅ |
| rescore_allocator | ✅ |
| poll_allocator_positions | ✅ |
| reconstruct_allocator_history | ✅ |
| refresh_allocator_equity_daily | ✅ |
| process_key_long (migration 108) | ✅ |
| **compute_analytics_from_csv (NEW)** | ✅ admitted |

### Probe 5 — `compute_job_kinds` registry

| Field | Expected | Actual |
|-------|----------|--------|
| `compute_analytics_from_csv` registered | 1 | **1** ✅ |
| `process_key_long` preserved | 1 | **1** ✅ |

### Probe 6 — RLS shape

| Field | Expected | Actual |
|-------|----------|--------|
| `rls_enabled` on `csv_daily_returns` | true | **true** ✅ |
| named policies (`service_role_all`, `owner_select`, `admin_select`) | 3 | **3** ✅ |

### Probe 7 — In-flight `compute_jobs` rows that would violate the new constraint

| Field | Expected | Actual |
|-------|----------|--------|
| violator_count | 0 | **0** ✅ |

This probe pre-validates the PROD apply: no in-flight rows on TEST violate the new constraint. Plan 06's Task 1.6 (post-revision pre-apply audit) must re-confirm this against PROD before applying.

## Hardening Preserved

- **PR #272** — collapsed 42501 probe-oracle close (Probe 3: 3 raises) ✅
- **PR #272** — no redundant explicit index (Probe 1: redundant index count = 0) ✅
- **PR #272** — GRANT TO authenticated with inline justification (Probe 2 + inline comment) ✅
- **PR #272** — `jsonb_typeof` guard before `jsonb_array_length` (Probe 3) ✅
- **Migration 108 contract preserved** — `process_key_long` branch still in coherence constraint (Probe 4 + 5) ✅

## Deviations

None. Both migrations applied cleanly. Self-verifying DO blocks raised no exception (any failure would have rolled back the transaction).

## Threats Mitigated (T-19.1-01, T-19.1-02)

- **T-19.1-01** (probe-oracle) — verified in Probe 3: 3 distinct `42501` raises including the collapsed missing-strategy + wrong-owner branch.
- **T-19.1-02** (RLS bypass via SECURITY DEFINER misconfig) — verified in Probe 2: prosecdef=true, GRANT to authenticated only (no anon), search_path pinned.

## Outputs Consumed By

- Plan 05 (live-DB regression tests) — runs against this schema
- Plan 06 Task 4 (cross-project parity diff) — re-runs the 7 probes against PROD and diffs against this SUMMARY's payloads

## No Verdict Line

Per the revision spec, Plan 04 does not emit a canonical `19.1-04-VERDICT: X` line — the next downstream checkpoint (Plan 05) gates on this SUMMARY's existence + absence of `FAIL` tokens. Plan 06's W9 schema-parity diff is the next consumer.
