---
phase: 16-diagnostic-spike-observability
plan: 10
type: execute
gate: phase-internal
wave: 1
depends_on: []
files_modified:
  - .planning/phase-16/day-2-decision.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Empty-template Day-2 decision document exists at .planning/phase-16/day-2-decision.md"
    - "Template contains all six required sections (TL;DR / Candidate root causes / Regression test snippet / Refutation table / Falsifiable criteria / Evidence chain table / Decision)"
    - "Frontmatter has the seven required keys per RESEARCH §Day-2 template (gate, status, decided_at, decided_by, deliberation_started_at, deliberation_minutes, correlation_id_evidence_chain)"
    - "Plan-checker can validate the template structure exists BEFORE Phase 18 entry"
    - "Refutation table has exactly 12 BACKBONE/FINGERPRINT REQ rows pre-populated (founder fills the refutation cell at the gate)"
  artifacts:
    - path: ".planning/phase-16/day-2-decision.md"
      provides: "Phase 16 → Phase 18/19 exit gate scaffold (founder fills at the gate; Phase-Internal Gate artifact, NOT an OBSERV requirement — OBSERV-12 is fully covered by Plan 01)"
      contains: "Phase 16 Exit — Day-2 Decision Document"
  key_links:
    - from: ".planning/phase-16/day-2-decision.md"
      to: "Phase 18 entry gate (STATE.md Phase-Internal Gates table)"
      via: "Plan-checker structural validation BEFORE any Phase 18 code lands"
      pattern: "deliberation_minutes"
    - from: "Refutation table"
      to: "Phase 19 BACKBONE-01..10 + FINGERPRINT-01..02 REQs"
      via: "12 rows pre-populated; founder fills refutation cell if SKIP path chosen"
      pattern: "BACKBONE-01"
---

<objective>
Pre-scaffold the Day-2 decision document at `.planning/phase-16/day-2-decision.md` with
all six required sections + frontmatter + 12-row refutation table. The founder fills it at
the Day-2 gate (Day 4 of Phase 16) — Plan 10 ships only the EMPTY template structure so
plan-checker can validate the document exists before Phase 18 entry.

Per CONTEXT.md (decisions §"Day-2 decision document handling"): "Pre-scaffold as Plan 10.
Write `.planning/phase-16/day-2-decision.md` with empty-section template. Founder fills at
the gate. Plan-checker validates template structure exists before Phase 18 entry."

**This plan delivers a Phase-Internal Gate artifact, NOT an OBSERV requirement.** OBSERV-12
(restore-e2e-fixtures presence assertion) is fully covered by Plan 01. The frontmatter
`gate: phase-internal` flag clarifies the artifact's role; `requirements: []` is intentional
to avoid false dual-coverage.

Purpose: Phase-Internal Gate "Day-2 decision gate (Day 4)" per STATE.md requires this file
to land BEFORE any Phase 18/19 code. The scaffold + plan-checker structural validation is
the mechanism — by shipping the empty template now, we ensure the founder never has to
remember the section structure under deliberation pressure.
Output: One markdown file with frontmatter + 6 sections + 12-row refutation table; all
fields are placeholders the founder fills at the gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-diagnostic-spike-observability/16-CONTEXT.md
@.planning/phases/16-diagnostic-spike-observability/16-RESEARCH.md
@.planning/phases/16-diagnostic-spike-observability/16-PATTERNS.md

<interfaces>
<!-- Source-of-truth template: RESEARCH.md L1201-1310 (full Day-2 template with frontmatter + 6 sections) -->
<!-- 12 BACKBONE/FINGERPRINT REQ rows: BACKBONE-01..10 + FINGERPRINT-01..02 (per REQUIREMENTS.md L72-86) -->
<!-- Sibling tone reference: .planning/phase-16/migration-drift-resolution.md (per PATTERNS.md `.planning/phase-16/...` section) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write empty-template Day-2 decision document</name>
  <files>.planning/phase-16/day-2-decision.md</files>
  <read_first>
    - .planning/phases/16-diagnostic-spike-observability/16-RESEARCH.md L1201-1310 (full Day-2 template — copy verbatim with all placeholder markers preserved)
    - .planning/REQUIREMENTS.md L72-86 (12 BACKBONE + FINGERPRINT REQ IDs to enumerate in the refutation table)
    - .planning/STATE.md L101-108 (Phase-Internal Gates row "Day-2 decision gate (Day 4)" — confirms the file path + the SKIP/COMMIT/HOLD criteria match the template)
    - .planning/phase-16/migration-drift-resolution.md (sibling-file tone reference)
  </read_first>
  <action>
Write `.planning/phase-16/day-2-decision.md` with EXACTLY the structure below (verbatim from RESEARCH §Day-2 Decision Document Template L1201-1310 + REQ list cross-checked against REQUIREMENTS.md L72-86). All placeholder markers (`<...>`) are preserved — the founder fills them at the gate.

```markdown
---
gate: phase-16-exit-day-2-decision
status: PENDING
decided_at: ""
decided_by: ""
deliberation_started_at: ""
deliberation_minutes: 0
correlation_id_evidence_chain: []
---

# Phase 16 Exit — Day-2 Decision Document

> **STATUS: TEMPLATE — founder fills at the Day-2 gate (Day 4 of Phase 16).**
>
> Plan-checker validates this document's structural completeness BEFORE Phase 18 entry per
> STATE.md "Phase-Internal Gates" table. Do NOT delete or rename this file — Phase 18 plans
> reference this exact path.
>
> Source template: `.planning/phases/16-diagnostic-spike-observability/16-RESEARCH.md` L1201-1310.
> Required sections (Plan-checker enforces all 6 + frontmatter + 12-row refutation table).

## TL;DR

<One sentence: SKIP / COMMIT / HOLD and the bottom-line reason.>

## Section 1 — Candidate Root Causes (ranked by evidence weight)

| # | Hypothesis | Evidence (correlation_id chain) | Weight | Refuted? |
|---|------------|--------------------------------|--------|----------|
| 1 | <e.g., OKX passphrase encoding> | <list correlation_ids that point here> | <HIGH/MED/LOW> | <yes/no + why> |
| 2 | <hypothesis> | <evidence> | <weight> | <yes/no> |
| 3 | <hypothesis> | <evidence> | <weight> | <yes/no> |

**Top hypothesis (founder commit):** <name>

## Section 2 — Regression Test Snippet for the Chosen Fix

> The test MUST fail without the fix and pass with the fix. Phase 18 entry gate checks this snippet exists and is syntactically valid for the target test framework.

```typescript
// File: <path>
// Run: <command>
test('regression-<short>', async () => {
  // Arrange: reproduce the failing condition
  // Act: invoke the affected code path
  // Assert: behavior currently fails; will pass after Phase 18 fix
});
```

**Why this test gates the Phase 18 fix:** <one paragraph>

## Section 3 — Refutation of Each Phase 19 Task (only required if SKIP path chosen)

> If COMMIT, write "N/A — COMMIT path chosen" in each cell. If SKIP, every Phase 19 task below MUST have an explicit refutation.

| Phase 19 REQ | Why Phase 19 Is NOT Needed Given the Day-2 Finding |
|--------------|---------------------------------------------------|
| BACKBONE-01 (POST /process-key) | <refutation> |
| BACKBONE-02 (IngestionAdapter) | <refutation> |
| BACKBONE-03 (strategy_verifications state machine) | <refutation> |
| BACKBONE-04 (4-PR VIEW-shim) | <refutation> |
| BACKBONE-05 (feature flag) | <refutation> |
| BACKBONE-06 (flag-monitor cron) | <refutation> |
| BACKBONE-07 (wizard_session_id idempotency) | <refutation> |
| BACKBONE-08 (long-fetch worker dispatch) | <refutation> |
| BACKBONE-09 (perp correctness) | <refutation> |
| BACKBONE-10 (route inventory) | <refutation> |
| FINGERPRINT-01 (JSONB fingerprint) | <refutation> |
| FINGERPRINT-02 (compute_similarity SQL) | <refutation> |

## Section 4 — Falsifiable SKIP / COMMIT / HOLD Criteria

**SKIP requires ALL three:**
- [ ] Single correlation_id chain points to ONE config or ONE single-LOC bug
- [ ] Fix has a regression test (Section 2) that fails without it
- [ ] No other failure mode unexplained in the diagnostic output

**COMMIT requires ANY ONE:**
- [ ] 2+ root causes surfaced
- [ ] Fix would touch ≥3 files in divergent paths
- [ ] No clean unit test possible (e.g., needs cross-tier coordination)

**HOLD requires ANY ONE:**
- [ ] Surfaced root cause is unfamiliar to the founder
- [ ] Founder cannot construct a regression test in <2h
- [ ] Founder is fatigued / not in a state to commit (24h re-evaluation)

## Section 5 — correlation_id Evidence Chain (full audit)

> Every correlation_id observed during the diagnostic spike, with the layer it surfaced in. This is the audit trail Phase 18 references.

| correlation_id | Origin (wizard click ts) | Next.js Sentry | Python Sentry | Supabase audit_log | compute_jobs.metadata | Resend webhook | Notes |
|----------------|-----------------------|----------------|---------------|--------------------|--------------------|----------------|-------|
| <uuid> | <ts> | ✓/✗ | ✓/✗ | ✓/✗ | ✓/✗ | ✓/✗/N/A | <observation> |

## Section 6 — Decision

**Final decision:** <SKIP | COMMIT | HOLD>

**Justification (one paragraph):**

<text>

**Next phase:**

- If SKIP: Phase 18 with single-fix scope; Phase 19 explicitly NOT entered. Milestone closes after Phase 18.
- If COMMIT: Phase 17 → Phase 18 → Phase 19 full sequence.
- If HOLD: re-evaluate in 24h; Phase 17 design work continues; Phase 18/19 deferred until HOLD lifts.

**Signed:** <founder name>
**Date:** <ISO8601>
```

Use the Write tool. Never `Bash(cat << 'EOF')`.

**CRITICAL:** the inner triple-backtick fenced block (`typescript ... `) for the regression test snippet is INSIDE this markdown document. When you Write the file, ensure the inner ``` does not break the outer file's parsing — markdown supports nested fences with same backtick count, but the inner fence MUST be balanced. If your editor escapes them differently, use `~~~typescript` for the inner fence to disambiguate.
  </action>
  <acceptance_criteria>
    - File `.planning/phase-16/day-2-decision.md` exists.
    - Frontmatter contains all 7 required keys: `gate`, `status`, `decided_at`, `decided_by`, `deliberation_started_at`, `deliberation_minutes`, `correlation_id_evidence_chain`. Verify: `head -10 .planning/phase-16/day-2-decision.md | grep -cE "^(gate|status|decided_at|decided_by|deliberation_started_at|deliberation_minutes|correlation_id_evidence_chain):"` returns 7.
    - All 6 section headers present: `grep -cE "^## (TL;DR|Section [1-6])" .planning/phase-16/day-2-decision.md` returns 7 (TL;DR + 6 numbered sections).
    - Refutation table contains exactly 12 REQ rows: `grep -cE "^\| (BACKBONE-(0[1-9]|10)|FINGERPRINT-0[12])" .planning/phase-16/day-2-decision.md` returns 12.
    - Falsifiable criteria checklist present: `grep -cE "^- \[ \]" .planning/phase-16/day-2-decision.md` returns >= 9 (3 SKIP + 3 COMMIT + 3 HOLD).
    - `grep -F "Phase 16 Exit" .planning/phase-16/day-2-decision.md` returns 1 match (title).
    - `grep -F "deliberation_minutes" .planning/phase-16/day-2-decision.md` returns >= 1 match (frontmatter key plus optional in-doc reference).
    - `status: PENDING` on the second-or-later line of frontmatter (initial value before founder fills).
    - `correlation_id_evidence_chain: []` (empty list initial value).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phase-16/day-2-decision.md && head -10 .planning/phase-16/day-2-decision.md | grep -cE "^(gate|status|decided_at|decided_by|deliberation_started_at|deliberation_minutes|correlation_id_evidence_chain):" | grep -E "^7$" && grep -cE "^\| (BACKBONE-(0[1-9]|10)|FINGERPRINT-0[12])" .planning/phase-16/day-2-decision.md | grep -E "^12$"</automated>
  </verify>
  <done>Template scaffold lands with all required sections, frontmatter, and 12-row refutation table. Founder fills at the Day-2 gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning tree (markdown only) | No code execution; doc is read by founder + plan-checker only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10-01 | Repudiation | Founder fills the doc but Phase 18 entry gate proceeds without verifying minimum 2-hour deliberation floor | mitigate | `deliberation_minutes` is a required frontmatter key; STATE.md gate language requires `>= 120`. Plan-checker can grep for the value at Phase 18 entry. |
| T-16-10-02 | Tampering | A future plan deletes or renames this file silently | mitigate | Phase 18 entry gate explicitly references this exact path; STATE.md "Phase-Internal Gates" table cites it. Deletion would block Phase 18. |
| T-16-10-03 | Information Disclosure | Founder pastes raw broker credentials into Section 5 evidence chain | accept | The doc lives in `.planning/` (committed to repo). Founder is the only filler; standard PII discipline applies. correlation_id is UUID v4 (no PII by design). |
| T-16-10-04 | Repudiation | Refutation table left empty (cells say "<refutation>") even when SKIP path chosen | mitigate | Plan-checker can grep for `<refutation>` placeholder strings post-fill — any remaining placeholder fails the Phase 18 entry gate. |
</threat_model>

<verification>
- File exists with correct structure.
- All 6 sections + frontmatter + 12-row refutation table present.
- Initial `status: PENDING` and empty placeholder values throughout.
</verification>

<success_criteria>
- Day-2 decision template scaffold lands; plan-checker can validate structure before Phase 18 entry.
- Founder has the exact section skeleton ready to fill at the gate.
- 12 BACKBONE/FINGERPRINT REQ rows pre-populated.
</success_criteria>

<output>
After completion, create `.planning/phases/16-diagnostic-spike-observability/16-10-SUMMARY.md`
documenting: file path, line count, frontmatter key list, REQ rows confirmed against
REQUIREMENTS.md.
</output>
</content>
</invoke>