---
phase: 18-root-cause-fix-founder-lp-skeleton
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
# Plan-checker fix 2026-05-06: warning 2 — .gitignore line 50 (`.planning/`) gitignores the directory.
# Verified via `git ls-files`: REQUIREMENTS.md, ROADMAP.md, STATE.md ARE tracked (committed before the rule
# was added; git keeps tracking already-indexed files even after the dir is gitignored).
# phase-18/dogfood-commitment.md and phase-16/day-2-decision.md are NOT tracked and live under local_artifacts.
files_modified:
  - .planning/REQUIREMENTS.md       # tracked (verified via git ls-files)
  - .planning/ROADMAP.md            # tracked (verified via git ls-files)
  - .planning/STATE.md              # tracked (verified via git ls-files)
local_artifacts:
  - .planning/phase-18/dogfood-commitment.md       # gitignored — never committed under phase-18/
  - .planning/phase-16/day-2-decision.md           # gitignored — never committed under phase-16/
autonomous: true
requirements:
  - LP-03
tags:
  - documentation
  - phase-18
  - dogfood
  - backbone-push
must_haves:
  truths:
    - "`.planning/phase-18/dogfood-commitment.md` exists as a stub with frontmatter `gate: phase-18-exit-dogfood-commitment`, `status: PENDING`, and a `<TODO: founder fills in at /ship time>` literal marker — Claude does NOT auto-fill the verbal-in-writing commitment text"
    - "REQUIREMENTS.md Phase mapping table attributes BACKBONE-06 + BACKBONE-07 to Phase 19 (NOT Phase 18)"
    - "ROADMAP.md Phase 18 + Phase 19 sections reflect the BACKBONE-06/-07 push (rationale: pairs with `IngestionAdapter.reconstruct_positions` + equity-curve refactor in Phase 19)"
    - "STATE.md accumulated context note records the Phase 18 → Phase 19 push for BACKBONE-06/-07"
    - "Day-2 decision document Section 5 has BOTH a one-line `> **REVISED ...** ` header explaining the supersession AND inline row updates marking BACKBONE-06/-07 as 'IN (Phase 18) — SUPERSEDED, see 18-CONTEXT.md L22-23 — pushed to Phase 19' (Adversarial revision 2026-05-06: W6 — preserve historical context, but inline-mark the supersession on the rows themselves)"
    - "After all edits, `grep -E \"BACKBONE-0[67]\" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` shows EVERY occurrence reads Phase 19 (not Phase 18)"
    - "scripts/verify-phase18-artifacts.ts (from Plan 1 Task 4) is dry-run executed at the end of this plan and exits non-zero (expected: <TODO: still present in dogfood-commitment.md)"
  artifacts:
    - path: ".planning/phase-18/dogfood-commitment.md"
      provides: "LP-03 commitment stub (founder fills at /ship time)"
      contains: "status: PENDING, <TODO: founder fills"
  key_links:
    - from: ".planning/REQUIREMENTS.md (Phase mapping table)"
      to: "Phase 19 (conditional)"
      via: "row attribution for BACKBONE-06 + BACKBONE-07"
      pattern: "BACKBONE-06.*Phase 19"
    - from: ".planning/ROADMAP.md Phase 18 success criteria"
      to: ".planning/ROADMAP.md Phase 19 BACKBONE-06/-07 listing"
      via: "scope move with rationale"
      pattern: "BACKBONE-06"
    - from: ".planning/phase-16/day-2-decision.md Section 5"
      to: ".planning/phases/18-root-cause-fix-founder-lp-skeleton/18-CONTEXT.md L22-23"
      via: "REVISED header pointer + inline row supersede annotation (Adversarial revision: W6)"
      pattern: "REVISED.*supersed.*CONTEXT"
---

<objective>
Ship LP-03 + cross-cutting Phase 18 doc updates: an empty dogfood-commitment stub for the founder to fill at /ship time, plus the planning-doc edits that push BACKBONE-06 + BACKBONE-07 from Phase 18 to Phase 19 across REQUIREMENTS.md / ROADMAP.md / STATE.md / Day-2 decision doc. Also dry-runs the Plan 1 verify-phase18-artifacts.ts gate.

Purpose: LP-03 ("Phase 18 exit interview captures founder verbal-in-writing commitment to send the unedited cron PDF to a real LP within 14 days of milestone close") cannot be auto-filled by Claude — the commitment text MUST come from the founder. This plan ships the stub with `status: PENDING` + the literal `<TODO:` marker; founder edits at /ship time. Pitfall 10 in 18-RESEARCH.md spells this out: auto-fill defeats the verbal-in-writing accountability mechanism.

The BACKBONE-06/-07 doc-push is the second half of this plan. CONTEXT.md L22-23 supersedes Day-2 doc Section 5's "BACKBONE-06/-07 IN (Phase 18)" attribution. Pitfall 9 in 18-RESEARCH.md flags the inconsistency risk: 3 source-of-truth docs + 1 supporting doc all carry the phase boundary; missing one creates contradiction. Plan 4 closes all four in one pass — and per Adversarial revision 2026-05-06 (W6), the Day-2 doc gets BOTH a header callout AND inline row annotations so the supersession is visible at the row level (not just at the section-header level).

Output:
- `.planning/phase-18/dogfood-commitment.md` (NEW; stub with PENDING + TODO)
- `.planning/REQUIREMENTS.md` (EDIT; Phase mapping table — BACKBONE-06 + BACKBONE-07 attributed to Phase 19)
- `.planning/ROADMAP.md` (EDIT; Phase 18 success criteria removes BACKBONE-06/-07 references; Phase 19 BACKBONE-06/-07 explicit listing + rationale)
- `.planning/STATE.md` (EDIT; accumulated context note recording the push)
- `.planning/phase-16/day-2-decision.md` Section 5 (EDIT; one-line REVISED header AND inline row updates per W6 — preserves history while making the supersession unmissable)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-root-cause-fix-founder-lp-skeleton/18-CONTEXT.md
@.planning/phases/18-root-cause-fix-founder-lp-skeleton/18-RESEARCH.md
@.planning/phase-16/day-2-decision.md
@.planning/phase-18/metaworld-commitment.md

<interfaces>
<!-- Locked stub format from 18-CONTEXT.md L77-90 -->

```markdown
---
gate: phase-18-exit-dogfood-commitment
status: PENDING
---
# Founder LP Dogfood Commitment

> Required: founder pastes the unedited verbal-in-writing commitment to send the cron-generated LP PDF to a real LP within 14 days of v1.0.0 milestone close.

## Commitment Text (verbatim)

<TODO: founder fills in at /ship time>
```

<!-- REQUIREMENTS.md Phase mapping table — current (lines around 216-217) -->

```markdown
| BACKBONE-06 | Phase 19 (conditional) | Pending |
| BACKBONE-07 | Phase 19 (conditional) | Pending |
```

(NOTE: the REQUIREMENTS.md mapping table ALREADY attributes BACKBONE-06/-07 to Phase 19 — the work here is to verify + ROADMAP.md sync + STATE.md note + Day-2 doc REVISED header + inline row updates.)

<!-- ROADMAP.md Phase 18 success criteria (lines around 162-176) — currently lists ONLY FIX/LP REQs in success criteria; BACKBONE-06/-07 are NOT listed under Phase 18 currently. Verify during read_first. -->

<!-- ROADMAP.md Phase 19 — currently lists BACKBONE-06 + BACKBONE-07 in `**Requirements:**` line at L183. Verify the rationale annotation is present. -->

<!-- Day-2 decision Section 5 (line ~115-117) — currently says BACKBONE-06/-07 "IN (Phase 18)". This is the section that needs a REVISED header AND inline supersede markers (W6). -->

```markdown
| BACKBONE-06 | Open-perp correctness fix | IN (Phase 18) | Independent v1 work, ships ahead of unification. |
| BACKBONE-07 | TWR ≠ YTD reconciliation at equity-curve layer | IN (Phase 18) | Independent v1 work, ships ahead of unification. |
```

After Adversarial revision 2026-05-06 (W6), these rows become:
```markdown
| BACKBONE-06 | Open-perp correctness fix | ~~IN (Phase 18)~~ → SUPERSEDED, see 18-CONTEXT.md L22-23 — pushed to Phase 19 | Independent v1 work, ships ahead of unification. |
| BACKBONE-07 | TWR ≠ YTD reconciliation at equity-curve layer | ~~IN (Phase 18)~~ → SUPERSEDED, see 18-CONTEXT.md L22-23 — pushed to Phase 19 | Independent v1 work, ships ahead of unification. |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship dogfood-commitment.md stub (LP-03)</name>
  <files>.planning/phase-18/dogfood-commitment.md</files>
  <read_first>
    - .planning/phases/18-root-cause-fix-founder-lp-skeleton/18-CONTEXT.md L77-90 (locked stub format — verbatim)
    - .planning/phase-18/metaworld-commitment.md (precedent — frontmatter + section heading style)
    - .planning/phases/18-root-cause-fix-founder-lp-skeleton/18-RESEARCH.md "Pitfall 10" (Claude MUST NOT auto-fill the commitment text)
  </read_first>
  <action>
    Write `.planning/phase-18/dogfood-commitment.md` VERBATIM to this content (no expansion, no Claude-authored prose):

    ```markdown
    ---
    gate: phase-18-exit-dogfood-commitment
    status: PENDING
    captured_at: "<TODO: founder fills at /ship time, ISO-8601>"
    captured_by: "<TODO: founder fills at /ship time>"
    requirement: LP-03
    ---

    # Founder LP Dogfood Commitment

    > **Required by:** REQUIREMENTS.md LP-03 — "Phase 18 exit interview captures founder verbal-in-writing commitment to send the unedited cron PDF to a real LP within 14 days of milestone close (dogfood loop closure per Pitfall 10); commitment text logged in `.planning/phase-18/dogfood-commitment.md`."

    > **CRITICAL — Pitfall 10 from 18-RESEARCH.md:** Claude MUST NOT auto-fill the commitment text. The whole point of LP-03 is FOUNDER-OWNED text. Auto-fill defeats the verbal-in-writing accountability mechanism. The stub frontmatter `status: PENDING` + literal `<TODO: founder fills in at /ship time>` is the canary; gate-checker before /ship asserts the stub still has both. After founder fills, status flips to SATISFIED and the literal is gone — that is the gate transition.

    ## Commitment Text (verbatim)

    <TODO: founder fills in at /ship time>

    ## Notes

    <TODO: any conditions or qualifications the founder wants on record (target LP, expected send date, fallback if cron fails)>
    ```

    DO NOT add any other body content. DO NOT replace `<TODO:` markers with placeholder text or paraphrases of "what the founder might say". The plan-checker greps for the literal `<TODO: founder fills` AND `status: PENDING` — both must be present and identical to the spec above.
  </action>
  <acceptance_criteria>
    - File exists at `.planning/phase-18/dogfood-commitment.md`
    - File contains frontmatter line `gate: phase-18-exit-dogfood-commitment`
    - File contains frontmatter line `status: PENDING`
    - File contains frontmatter line `requirement: LP-03`
    - File contains the literal string `<TODO: founder fills in at /ship time>` (in Commitment Text section body)
    - File does NOT contain any Claude-authored commitment text (e.g., placeholder sentences like "I commit to..."). The grep gate `! grep -qE "I (commit|will|promise|agree)" .planning/phase-18/dogfood-commitment.md` (case-sensitive — the `> **Required by:**` quote line is fine because it begins with `>`) returns success
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phase-18/dogfood-commitment.md && grep -q "status: PENDING" .planning/phase-18/dogfood-commitment.md && grep -q "<TODO: founder fills in at /ship time>" .planning/phase-18/dogfood-commitment.md && grep -q "gate: phase-18-exit-dogfood-commitment" .planning/phase-18/dogfood-commitment.md</automated>
  </verify>
  <done>LP-03 stub exists with PENDING + TODO marker; founder fills verbatim at /ship time; Pitfall 10 auto-fill anti-pattern avoided.</done>
</task>

<task type="auto">
  <name>Task 2: Sync BACKBONE-06/-07 phase attribution across REQUIREMENTS.md / ROADMAP.md / STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — find Traceability table around L182-227 + BACKBONE descriptions L72-81)
    - .planning/ROADMAP.md (full file — find Phase 18 section L162-176 + Phase 19 section L177-194 + Wave Structure table L82-90)
    - .planning/STATE.md (Accumulated Context section L146-160 — find natural insertion point for the push note)
    - .planning/phases/18-root-cause-fix-founder-lp-skeleton/18-CONTEXT.md L22-26 + L40 (locked rationale: "Phase 19's `IngestionAdapter.reconstruct_positions` + equity-curve unification absorbs the same call sites; Phase 18 is already heavy")
  </read_first>
  <action>
    Step A — `.planning/REQUIREMENTS.md` audit:

    1. Confirm Traceability table rows for BACKBONE-06 + BACKBONE-07 already read `Phase 19 (conditional)` (per L216-217 in repo's current state). If already correct → no change needed; record in summary that this row is already aligned.
    2. Check L72-81 BACKBONE description block — verify BACKBONE-06 + BACKBONE-07 description text says "Phase 19" or is phase-agnostic. If text says "Phase 18" anywhere, edit to "Phase 19". (Per L80: "BACKBONE-06: Open-perp position correctness — `reconstruct_positions()` adapter method..." — this text is phase-agnostic, no edit needed. Verify same for BACKBONE-07.)
    3. If any prose section in REQUIREMENTS.md mentions "BACKBONE-06" or "BACKBONE-07" with a "Phase 18" attribution, fix to "Phase 19".

    Use the Edit tool for each individual edit (NOT Write — preserve all other content).

    Step B — `.planning/ROADMAP.md` edits:

    1. Phase 18 section (around L162-176): the current Success Criteria list does NOT mention BACKBONE-06 or BACKBONE-07 (success criteria 1-5 are all FIX/LP). Verify no BACKBONE-06/-07 mention in Phase 18 success criteria. If found, remove it.
    2. Phase 19 section (around L177-194): Confirm BACKBONE-06 + BACKBONE-07 are present in `**Requirements:**` line (L183). Add a one-line annotation under Phase 19's success criteria explaining the late inclusion:

       Append a new bullet to Phase 19 success criteria (after success criterion 6, OR add as a new success criterion 7):

       ```markdown
         7. Open-perp position valuation correctness (BACKBONE-06) + TWR ≠ YTD reconciliation at the equity-curve layer (BACKBONE-07) ship as part of Phase 19 — pushed from Phase 18 (per `.planning/phases/18-root-cause-fix-founder-lp-skeleton/18-CONTEXT.md` L22-23) because they pair naturally with `IngestionAdapter.reconstruct_positions` and the equity-curve unification.
       ```

       (If success criterion 7 already exists, insert as 7a or a sub-bullet under the matching item — preserve existing numbering.)

    3. Wave Structure table (around L82-90) does NOT need editing because the wave-level dependencies remain — Phase 18 → Phase 19 ordering is unchanged.

    Use Edit tool for each ROADMAP.md change.

    Step C — `.planning/STATE.md` edit:

    Locate the `## Accumulated Context` section (around L146). Find the `### Roadmap Evolution` subsection. Append a new bullet to that list:

    ```markdown
    - 2026-05-06 — Phase 18 plan-phase records that BACKBONE-06 (open-perp correctness) + BACKBONE-07 (TWR ≠ YTD reconciliation) push from Phase 18 to Phase 19 per `.planning/phases/18-root-cause-fix-founder-lp-skeleton/18-CONTEXT.md` L22-23. Rationale: Phase 19's `IngestionAdapter.reconstruct_positions` + equity-curve refactor absorbs the same call sites; Phase 18 already heavy with FIX-04 redact mirror + LP cron + 10-team verification. Day-2 doc Section 5's "IN (Phase 18)" rows are now superseded — see `.planning/phase-16/day-2-decision.md` Section 5 REVISED header AND inline row supersede annotations (Adversarial revision 2026-05-06: W6).
    ```

    Use the Edit tool to insert this bullet at the END of the `### Roadmap Evolution` bullet list (preserve existing bullets).

    Step D — Verification grep gate (run after edits):

    ```bash
    # Every BACKBONE-06/-07 occurrence in REQUIREMENTS/ROADMAP/STATE should be in a Phase 19 context.
    # Find any line that mentions BACKBONE-0[67] AND Phase 18 (excluding lines that explicitly explain the push, which mention both):
    grep -nE "BACKBONE-0[67]" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md
    ```

    Manually inspect output: every row that pairs BACKBONE-06/-07 with a phase MUST say "Phase 19" — except for explanatory text describing the push (e.g., "pushed from Phase 18 to Phase 19" is acceptable; "BACKBONE-06: Phase 18: pending" is NOT).
  </action>
  <acceptance_criteria>
    - REQUIREMENTS.md Traceability table rows for BACKBONE-06 + BACKBONE-07 read `Phase 19 (conditional)` (or equivalent containing "Phase 19")
    - ROADMAP.md Phase 18 success criteria do NOT reference BACKBONE-06 or BACKBONE-07
    - ROADMAP.md Phase 19 section explicitly mentions BACKBONE-06 + BACKBONE-07 ship in Phase 19 with rationale referencing 18-CONTEXT.md L22-23
    - STATE.md `### Roadmap Evolution` section has a new bullet dated 2026-05-06 recording the push with rationale
    - `grep -E "BACKBONE-0[67]" .planning/REQUIREMENTS.md` returns lines, ALL of which are either (a) prose descriptions, (b) attribute to Phase 19, or (c) mention "pushed from Phase 18 to Phase 19" as historical record
    - No file is overwritten by Write tool — every edit uses Edit tool to preserve surrounding content
  </acceptance_criteria>
  <verify>
    <automated>grep -q "BACKBONE-06.*Phase 19\|Phase 19.*BACKBONE-06" .planning/REQUIREMENTS.md && grep -q "BACKBONE-07.*Phase 19\|Phase 19.*BACKBONE-07" .planning/REQUIREMENTS.md && grep -q "BACKBONE-06" .planning/ROADMAP.md && grep -q "BACKBONE-07" .planning/ROADMAP.md && grep -q "2026-05-06" .planning/STATE.md && grep -q "BACKBONE-06\|BACKBONE-07" .planning/STATE.md</automated>
  </verify>
  <done>BACKBONE-06/-07 phase attribution synced across 3 source-of-truth docs; STATE.md records the push rationale; grep gate clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add REVISED header AND inline row supersede annotations to Day-2 decision Section 5 (Adversarial revision: W6)</name>
  <files>.planning/phase-16/day-2-decision.md</files>
  <read_first>
    - .planning/phase-16/day-2-decision.md Section 5 (around L108-119) — the `## Section 5 — Phase 18 Scope (under COMMIT)` heading + the BACKBONE-06/-07 IN (Phase 18) rows
    - .planning/phases/18-root-cause-fix-founder-lp-skeleton/18-CONTEXT.md L22-23 (canonical supersession source)
    - .planning/phases/18-root-cause-fix-founder-lp-skeleton/18-RESEARCH.md "Pitfall 9" + "Open Question 4" (recommendation: header callout + inline row annotation per W6)
  </read_first>
  <action>
    Adversarial revision 2026-05-06: W6 — apply BOTH a REVISED header AND inline row updates so the supersession is visible at the row level (a header-only callout was Claude+Grok agreement insufficient).

    Step 1 — Insert the REVISED header IMMEDIATELY AFTER the `## Section 5 — Phase 18 Scope (under COMMIT)` heading line. Use Edit tool. Insert verbatim:

    ```markdown
    > **REVISED 2026-05-06** — BACKBONE-06 + BACKBONE-07 rows below are SUPERSEDED. Per `.planning/phases/18-root-cause-fix-founder-lp-skeleton/18-CONTEXT.md` L22-23, both pushed to Phase 19 (rationale: pairs with `IngestionAdapter.reconstruct_positions` + equity-curve refactor). Section preserved for historical record; canonical phase attribution lives in REQUIREMENTS.md + ROADMAP.md.
    ```

    Edit tool invocation 1:
    - `old_string`: the exact heading line `## Section 5 — Phase 18 Scope (under COMMIT)`
    - `new_string`: same heading + blank line + the REVISED blockquote + blank line

    Verify the blockquote starts with `> **REVISED 2026-05-06**` and references the CONTEXT.md path verbatim.

    Step 2 (Adversarial revision: W6) — Inline row updates. Use Edit tool TWICE (once per row) to mark BACKBONE-06 and BACKBONE-07 as superseded inline. The historical text stays (strikethrough); the supersession is appended to the same status cell.

    Edit tool invocation 2 — BACKBONE-06 row:
    - `old_string`:
      ```
      | BACKBONE-06 | Open-perp correctness fix | IN (Phase 18) | Independent v1 work, ships ahead of unification. |
      ```
    - `new_string`:
      ```
      | BACKBONE-06 | Open-perp correctness fix | ~~IN (Phase 18)~~ → SUPERSEDED, see 18-CONTEXT.md L22-23 — pushed to Phase 19 | Independent v1 work, ships ahead of unification. |
      ```

    Edit tool invocation 3 — BACKBONE-07 row:
    - `old_string`:
      ```
      | BACKBONE-07 | TWR ≠ YTD reconciliation at equity-curve layer | IN (Phase 18) | Independent v1 work, ships ahead of unification. |
      ```
    - `new_string`:
      ```
      | BACKBONE-07 | TWR ≠ YTD reconciliation at equity-curve layer | ~~IN (Phase 18)~~ → SUPERSEDED, see 18-CONTEXT.md L22-23 — pushed to Phase 19 | Independent v1 work, ships ahead of unification. |
      ```

    Important: if the table rows in the actual file do not match the `old_string` above byte-for-byte, the executor must Read the file first and use the EXACT text from the file (preserve any extra whitespace, typo, or variation). Do NOT rewrite the file with Write — only Edit the two rows surgically.

    DO NOT delete the existing Section 5 body. The historical record is preserved exactly via strikethrough; readers see the original Day-2 verdict text WITH the supersede arrow + canonical-source pointer.
  </action>
  <acceptance_criteria>
    - `.planning/phase-16/day-2-decision.md` contains the literal string `**REVISED 2026-05-06**`
    - The REVISED header appears AFTER `## Section 5 — Phase 18 Scope (under COMMIT)` heading and BEFORE the existing table
    - Section 5 BACKBONE-06 row contains `~~IN (Phase 18)~~` AND `SUPERSEDED, see 18-CONTEXT.md L22-23 — pushed to Phase 19` (Adversarial revision: W6)
    - Section 5 BACKBONE-07 row contains `~~IN (Phase 18)~~` AND `SUPERSEDED, see 18-CONTEXT.md L22-23 — pushed to Phase 19` (Adversarial revision: W6)
    - The trailing rationale column ("Independent v1 work, ships ahead of unification.") is PRESERVED on both rows
    - The header references `.planning/phases/18-root-cause-fix-founder-lp-skeleton/18-CONTEXT.md` verbatim
    - File is not rewritten (Edit tool used, not Write)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "REVISED 2026-05-06" .planning/phase-16/day-2-decision.md && grep -q "Section 5 — Phase 18 Scope (under COMMIT)" .planning/phase-16/day-2-decision.md && grep -q "18-CONTEXT.md" .planning/phase-16/day-2-decision.md && grep -q "BACKBONE-06.*~~IN (Phase 18)~~.*SUPERSEDED" .planning/phase-16/day-2-decision.md && grep -q "BACKBONE-07.*~~IN (Phase 18)~~.*SUPERSEDED" .planning/phase-16/day-2-decision.md</automated>
  </verify>
  <done>Day-2 doc Section 5 carries a REVISED header pointer to canonical source AND inline row supersede annotations on BACKBONE-06/-07; historical record preserved via strikethrough; Pitfall 9 inconsistency closed at both header-level and row-level (Adversarial revision: W6).</done>
</task>

<task type="auto">
  <name>Task 4: Dry-run scripts/verify-phase18-artifacts.ts gate</name>
  <!-- Adversarial revision 2026-05-06: Grok B4 — Plan 4 acceptance criterion runs the script in dry-run. -->
  <files></files>
  <read_first>
    - scripts/verify-phase18-artifacts.ts (built in Plan 1 Task 4 — verify it greps dogfood-commitment.md)
    - .planning/phase-18/dogfood-commitment.md (Task 1 output — should still have <TODO: literal so the dry-run fails)
  </read_first>
  <action>
    Run `npx tsx scripts/verify-phase18-artifacts.ts` and capture exit code. The expected outcome at this point in the pipeline is FAIL (exit code 1) — because the founder has not yet filled in dogfood-commitment.md or founder-okx-smoke.md. This is the desired pre-/ship state; the gate's job is to keep blocking until /ship time.

    If the dry-run UNEXPECTEDLY exits 0 (script claims pass), it means either:
      (a) Plan 1 Task 4 omitted the dogfood-commitment.md check — fix Plan 1 first.
      (b) Founder already filled the artefacts — verify by grep.

    Document the dry-run exit code in the SUMMARY.md output.

    Notes:
    - DO NOT auto-fill the `<TODO:` markers to pass the gate. The gate is supposed to fail until founder fills.
    - DO NOT modify the script unless Plan 1 left it broken.
  </action>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase18-artifacts.ts` exits non-zero (expected pre-/ship state — Adversarial revision: Grok B4 acceptance criterion)
    - Stderr output includes `UNFILLED <TODO:` mentioning `.planning/phase-18/dogfood-commitment.md` and `.planning/phase-18/founder-okx-smoke.md`
    - SUMMARY.md (Task 5 output) records the exit code
  </acceptance_criteria>
  <verify>
    <automated>(npx tsx scripts/verify-phase18-artifacts.ts; test $? -ne 0)</automated>
  </verify>
  <done>Plan 1's verify-phase18-artifacts.ts gate runs from Plan 4 in dry-run; exits non-zero as expected (the gate is the actual /ship-time blocker, not this plan's job to satisfy).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Founder text → planning doc | Founder pastes verbal-in-writing commitment text at /ship time; integrity of the captured wording is the audit trail for LP-03 |
| Plan-phase output → cross-doc consistency | 4 source-of-truth docs (REQUIREMENTS / ROADMAP / STATE / Day-2) all reference the same phase boundaries; missing a sync creates contradiction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-17 | T (Tampering) | dogfood-commitment.md auto-fill by Claude (Pitfall 10) | mitigate | Stub frontmatter `status: PENDING` + literal `<TODO: founder fills in at /ship time>` is the canary; verify task asserts both exist + asserts no Claude-authored commit-shaped prose ("I commit", "I will", "I promise", "I agree") |
| T-18-18 | I (Information disclosure) | LP commitment text contents | accept | Founder authors at /ship time; commitment is internal accountability, no PII concern |
| T-18-19 | T (Tampering) | BACKBONE-06/-07 phase drift across 4 docs (Pitfall 9) | mitigate | Plan 4 closes all 4 in one pass; verify task greps ALL 3 source-of-truth docs + Day-2 doc to confirm consistency; Adversarial revision W6 adds inline row supersede markers in Day-2 doc |
| T-18-20 | R (Repudiation) | Day-2 doc historical record | mitigate | Section 5 BODY preserved; strikethrough on the original "IN (Phase 18)" with arrow → SUPERSEDED + canonical-source pointer (Adversarial revision: W6); both header callout AND row annotations make the supersession unmissable |
| T-18-21 | A (Availability) | Plan 1's verify-phase18-artifacts.ts gate not exercised | mitigate | Adversarial revision Grok B4 acceptance — Plan 4 Task 4 dry-runs the gate; non-zero exit confirms gate is functional pre-/ship |
</threat_model>

<verification>
**Plan-level checks (run after all 4 tasks complete):**

```bash
# LP-03 stub: PENDING + TODO + correct gate id
test -f .planning/phase-18/dogfood-commitment.md
grep -q "status: PENDING" .planning/phase-18/dogfood-commitment.md
grep -q "<TODO: founder fills in at /ship time>" .planning/phase-18/dogfood-commitment.md
grep -q "gate: phase-18-exit-dogfood-commitment" .planning/phase-18/dogfood-commitment.md

# No auto-filled commitment text (Pitfall 10 anti-pattern)
! grep -qE "^I (commit|will|promise|agree)" .planning/phase-18/dogfood-commitment.md

# BACKBONE-06/-07 phase consistency (Pitfall 9)
grep -q "BACKBONE-06.*Phase 19" .planning/REQUIREMENTS.md
grep -q "BACKBONE-07.*Phase 19" .planning/REQUIREMENTS.md
grep -q "BACKBONE-06" .planning/ROADMAP.md
grep -q "BACKBONE-07" .planning/ROADMAP.md
grep -q "BACKBONE-06\|BACKBONE-07" .planning/STATE.md

# Day-2 doc REVISED header AND inline row updates (Adversarial revision: W6)
grep -q "REVISED 2026-05-06" .planning/phase-16/day-2-decision.md
grep -q "Section 5 — Phase 18 Scope" .planning/phase-16/day-2-decision.md
grep -qE "BACKBONE-06.*~~IN \(Phase 18\)~~.*SUPERSEDED" .planning/phase-16/day-2-decision.md
grep -qE "BACKBONE-07.*~~IN \(Phase 18\)~~.*SUPERSEDED" .planning/phase-16/day-2-decision.md

# Plan 1 verify-phase18-artifacts.ts gate dry-run (Adversarial revision: Grok B4)
(npx tsx scripts/verify-phase18-artifacts.ts; test $? -ne 0)  # expected: non-zero (TODO: still present)
```
</verification>

<success_criteria>
- LP-03 stub at `.planning/phase-18/dogfood-commitment.md` has frontmatter (gate, status: PENDING, requirement: LP-03) + body with `<TODO:` markers + Pitfall 10 warning quote
- Claude does NOT auto-fill any commitment-shaped text (verified by grep of "I commit/will/promise/agree" anti-pattern)
- BACKBONE-06 + BACKBONE-07 attribute to Phase 19 in REQUIREMENTS.md Traceability table
- ROADMAP.md Phase 19 section explicitly lists BACKBONE-06/-07 with push rationale
- STATE.md `### Roadmap Evolution` records the 2026-05-06 push
- Day-2 decision doc Section 5 has BOTH a one-line REVISED header pointing to 18-CONTEXT.md AND inline row supersede annotations (Adversarial revision: W6) — body preserved via strikethrough
- All 4 source-of-truth docs are now consistent — Pitfall 9 inconsistency closed at both section-header and row level
- All edits use Edit tool — no surrounding-content overwrites
- scripts/verify-phase18-artifacts.ts dry-run exits non-zero (expected pre-/ship — Adversarial revision: Grok B4)
</success_criteria>

<output>
After completion, create `.planning/phases/18-root-cause-fix-founder-lp-skeleton/18-04-SUMMARY.md` with:
- LP-03 stub status (PENDING — founder fills at /ship)
- BACKBONE-06/-07 cross-doc sync status (verified across REQUIREMENTS / ROADMAP / STATE)
- Day-2 doc Section 5 REVISED header line + inline row supersede annotations on BACKBONE-06 + BACKBONE-07 (Adversarial revision: W6)
- verify-phase18-artifacts.ts dry-run exit code (expected: 1 — gate is functional)
- Pointer to /ship-time gate-checker (asserts dogfood-commitment.md flips PENDING → SATISFIED before merge)
</output>
</content>
</invoke>