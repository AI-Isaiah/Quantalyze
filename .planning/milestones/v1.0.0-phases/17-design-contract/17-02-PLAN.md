---
phase: 17-design-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DESIGN.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05
must_haves:
  truths:
    - "DESIGN.md gains 5 new sub-sections — Trust-Tier Badges, Error Envelope, Broker Selector Grid, CSV Escape-Hatch Card, 9-State Matrix — inserted between 'Component Patterns' and 'Data density principle'"
    - "DESIGN.md Decisions Log gains 5 new rows dated 2026-05-01 (one per DESIGN REQ)"
    - "REQUIREMENTS.md DESIGN-01 row is corrected from #D97706 to #B45309 (system-canon alignment)"
    - "Phase 17 zero-TBD hard exit gate passes: `grep -E 'TBD|TODO|TKTK' DESIGN.md` returns no matches inside the new sub-sections"
  artifacts:
    - path: "DESIGN.md"
      provides: "Trust-Tier Badges, Error Envelope, Broker Selector Grid, CSV Escape-Hatch Card, 9-State Matrix sub-sections + 5 Decisions Log rows"
      contains: "## Trust-Tier Badges"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DESIGN-01 row using #B45309 (was #D97706)"
      contains: "#B45309"
  key_links:
    - from: "DESIGN.md"
      to: "src/lib/design-tokens/trust-tier.ts"
      via: "verbatim hex values + variant labels"
      pattern: "#1B6B5A.*#4A5568.*#B45309"
    - from: ".planning/REQUIREMENTS.md DESIGN-01"
      to: "DESIGN.md self-reported variant hex"
      via: "shared #B45309 token"
      pattern: "#B45309"
---

<objective>
Land all DESIGN.md additions for DESIGN-01..05 in a single, PR-friendly file edit. Correct REQUIREMENTS.md DESIGN-01 from the stale `#D97706` literal to the canonical `#B45309`.

Purpose: Lock the design contract before Phase 19 backend rewrite. This plan is the documentation half of the design contract — Plans 17-01 (tokens), 17-04 (ErrorEnvelope), 17-05 (TrustTierLabel internals), 17-06 (a11y tests) cover the code half. Per CONTEXT.md decisions DESIGN-01..05 + UI-SPEC.md §18 verbatim prose snippets, this is a verbatim-paste exercise — the prose is already authored by gsd-ui-researcher.

Output: One DESIGN.md edit (~150 added lines: 5 sub-sections + 5 Decisions Log rows). One REQUIREMENTS.md edit (~1 line: hex correction). Hard exit gate verified: zero TBD cells in the new sub-sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<branch_constraint>
**DO NOT run any of the following commands (under any circumstance):**
- `git checkout`, `git switch`, `git pull`, `git fetch`, `git branch`, `git rebase`, `git merge`, `git reset --hard`
- Any command that changes the current branch
- Any command that fetches or pulls from a remote

The current branch is `v1.0.0-api-key-rewrite-15-16` and MUST remain so. Verify with `git rev-parse --abbrev-ref HEAD` at start and end.
</branch_constraint>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-design-contract/17-CONTEXT.md
@.planning/phases/17-design-contract/17-UI-SPEC.md
@DESIGN.md

<verbatim_source>
This plan is a transcription job. The five DESIGN.md sub-section bodies and the
five Decisions Log entries are pre-authored in `17-UI-SPEC.md` §18.1 through
§18.6. The executor's job is to copy those snippets verbatim into DESIGN.md
at the correct insertion points. DO NOT paraphrase, condense, or "improve" the
prose — gsd-ui-checker will validate verbatim presence on Phase 17 exit.
</verbatim_source>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert 5 new DESIGN.md sub-sections between line ~100 and ~102 (between 'Component Patterns' and 'Data density principle')</name>
  <files>DESIGN.md</files>

  <read_first>
    - DESIGN.md lines 94-110 (the existing "Component Patterns" section through the "Data density principle" heading) — to lock the insertion anchor
    - .planning/phases/17-design-contract/17-UI-SPEC.md §18.1 (lines 991-1010 — Trust-Tier Badges)
    - .planning/phases/17-design-contract/17-UI-SPEC.md §18.2 (lines 1012-1035 — Error Envelope)
    - .planning/phases/17-design-contract/17-UI-SPEC.md §18.3 (lines 1037-1052 — Broker Selector Grid)
    - .planning/phases/17-design-contract/17-UI-SPEC.md §18.4 (lines 1054-1071 — CSV Escape-Hatch Card)
    - .planning/phases/17-design-contract/17-UI-SPEC.md §18.5 (lines 1073-1105 — 9-State Matrix)
  </read_first>

  <action>
    Per D-01..D-22 (the locked decisions in 17-CONTEXT.md `<decisions>` map directly to UI-SPEC.md §18 prose blocks).

    Step 1 — Anchor identification. The current DESIGN.md has the section structure:
    - line ~94: `## Component Patterns`
    - lines 95-100: 6 bullets (Cards / Tables / Buttons / Badges / Inputs / Modals)
    - line 101: blank line
    - line 102: `## Data density principle`

    Insertion anchor: AFTER the last bullet of Component Patterns (currently `- **Modals:** White surface, subtle shadow, slide-out panels from right edge`), BEFORE `## Data density principle`. Five new H2 sub-sections go in this gap, separated by blank lines per existing DESIGN.md style.

    Step 2 — Insert verbatim prose blocks from UI-SPEC.md §18.1..§18.5. Each block IS the H2 sub-section content (the `## Title` line is included in the snippet — DO NOT add an extra heading).

    Block A — Trust-Tier Badges (verbatim from UI-SPEC §18.1, the markdown content inside the ```md fence):

    ```md
    ## Trust-Tier Badges
    Three-variant pill component lives at `src/components/strategy/TrustTierLabel.tsx`.
    Tokens live at `src/lib/design-tokens/trust-tier.ts` (single nested
    `TRUST_TIER_TOKENS as const`). DESIGN.md ↔ token consistency asserted by
    `tests/a11y/trust-tier-tokens.test.ts`.

    | Variant | Fill | Text | Border | Label |
    |---------|------|------|--------|-------|
    | `api_verified` | #1B6B5A | #FFFFFF | #1B6B5A | API verified |
    | `csv_uploaded` | #FFFFFF | #4A5568 | #4A5568 | CSV uploaded — verification pending |
    | `self_reported` | #FFFFFF | #B45309 | #B45309 | Self-reported |

    Visual: `inline-flex items-center rounded-sm border px-2 py-0.5 text-xs font-medium`.
    4px radius (`rounded-sm` per badge ladder), 1px border, 12px DM Sans regular.
    Inserted next to the strategy name on factsheet H1, marketplace tile, admin
    CSV-status row. No icons; identity carried by border + text colour only.
    ```

    Block B — Error Envelope (verbatim from UI-SPEC §18.2):

    ```md
    ## Error Envelope
    Canonical error renderer lives at `src/components/error/ErrorEnvelope.tsx`.
    Used by every error surface (wizard, CSV upload, factsheet load failure, admin
    status page, future `error.tsx` route boundaries). Sources human copy from
    `src/lib/wizardErrors.ts` via `buildEnvelope()` in `src/lib/envelope.ts`.

    Visual contract:
    - Shell: `role="alert"` + `rounded-md border border-negative/30 bg-negative/5 px-4 py-3`.
    - Title: `text-base font-semibold text-text-primary` — 16px DM Sans semibold #1A1A2E (REQ DESIGN-02).
    - Body: optional numbered `<ul>` of `debug_context` lines (12px DM Sans regular #4A5568).
    - Retry CTA: `Button size="sm"`, BELOW the body and ABOVE the `<details>` accordion.
      Renders iff `envelope.recoverable && onRetry`.
    - Diagnostics: always-collapsed `<details>` with `code` + `correlation_id` (Geist
      Mono 12px) + ghost-button "Copy diagnostics".
    - Copy-diagnostics payload: newline-delimited text block prefixed `QUANTALYZE_DIAG`
      + code + correlation_id + ISO timestamp + user_agent + debug_context lines +
      trailer `--- pii-scrubbed ---`. Runs through `pii-scrub.ts` before clipboard write.

    Authoring rule: every error path MUST call `buildEnvelope(code, correlation_id, ctx)`;
    no inline-string error envelopes. Code-review block enforced by grep at PR-time.
    ```

    Block C — Broker Selector Grid (verbatim from UI-SPEC §18.3):

    ```md
    ## Broker Selector Grid
    3-cols × 1-row card grid at the top of the API path on `/strategies/new/wizard`.
    3 active cards (Binance, OKX, Bybit) per UC-B v1 source scope. Drops the literal
    "2×3" interpretation from REQ DESIGN-03; v2 may revisit when MT5/IBKR ship.

    Visual: `<button>` cards with white surface, 1px `#E2E8F0` border, 8px radius.
    Active state: `border-accent bg-accent/5`. Hover (inactive): `hover:border-accent/50`.
    Card copy: 14px DM Sans semibold (name) + 12px DM Sans regular (caption).

    Per-source field schema lives in UI-SPEC.md §per-source-fields (DESIGN.md stays
    narrow on tokens + visual contracts). OKX requires a passphrase field; Binance
    and Bybit do not. IP-allowlist hint copy is per-source.
    ```

    Block D — CSV Escape-Hatch Card (verbatim from UI-SPEC §18.4):

    ```md
    ## CSV Escape-Hatch Card
    Full-width card BELOW the broker selector grid. White surface, 1px `#E2E8F0`
    border, 8px radius. Same visual weight as the broker cards — directs users
    without an API key into the CSV branch (`?source=csv`).

    Title (verbatim): "Don't have an API key? Upload CSV instead". Body:
    "Upload daily returns, NAV, or trades. We validate every row before creating
    your strategy. Max 10 MB." CTA: `Button variant="secondary"` "Upload CSV →"
    (secondary variant intentionally avoids competing with the API path's
    `bg-accent` primary CTA on the same screen).

    3 accepted formats live as a segmented control on the CSV branch landing step:
    `daily_returns` / `daily_nav` / `trades`. Format selector is part of Phase 15
    CSV branch — see Phase 15 UI-SPEC §8.3.
    ```

    Block E — 9-State Matrix (verbatim from UI-SPEC §18.5):

    ```md
    ## 9-State Matrix
    Every API-key-flow surface declares behavior across 9 states: loading, empty,
    error, partial, success, retry-in-flight, stale, optimistic, offline. Concrete
    DOM/copy specs per cell live in UI-SPEC.md §9-state-matrix. Hard exit gate
    before Phase 19: `gsd-sdk validate phase-17-exit` greps for `TBD | TODO | TKTK`
    in the matrix; FAILS on any unresolved cell.

    A11y minimums (DESIGN-05):
    - Trust-tier pill text ≥ 4.5:1 against rendering context (page bg #F8F9FA or
      surface bg #FFFFFF). Asserted by `tests/a11y/wizard-contrast.test.ts`.
    - ARIA live regions: `role="alert"` on blocking errors; `role="status"` +
      `aria-live="polite"` on non-blocking state changes.
    - Keyboard navigation: stepper Tab/Shift+Tab in DOM order; Enter activates;
      `aria-current="step"` on active step.
    - Focus management: on step transition, focus moves to the first interactive
      control of the new step.

    axe-core CI scans `/strategies/new/wizard` (both `?source=` branches) and
    `/admin/csv-status` with `wcag2a + wcag2aa + best-practice` rule sets. Zero
    violations required for green CI.

    Mobile fallback (DESIGN-04): deferred to v2. Trigger condition: PostHog
    `wizard_start` event with `device_type='mobile'` count > 0 over a rolling
    7-day window in production. Audit cron logs the count weekly to
    `.planning/audits/wizard-mobile-count.md`. When trigger fires, build the
    read-only review state spec (single column, 16px base, no chrome reflow,
    copy-only — full mobile-responsive polish remains v2 scope per PROJECT.md).
    v1 ships the 640px `DesktopGate.tsx` as-is per the Phase 16 OBSERV-11 audit
    (mobile-start count = 0).
    ```

    Step 3 — Use the Edit tool with `old_string` set to the boundary text:
    ```
    - **Modals:** White surface, subtle shadow, slide-out panels from right edge

    ## Data density principle
    ```
    and `new_string` set to the same boundary plus all five blocks inserted between, separated by single blank lines:
    ```
    - **Modals:** White surface, subtle shadow, slide-out panels from right edge

    ## Trust-Tier Badges
    [block A body]

    ## Error Envelope
    [block B body]

    ## Broker Selector Grid
    [block C body]

    ## CSV Escape-Hatch Card
    [block D body]

    ## 9-State Matrix
    [block E body]

    ## Data density principle
    ```

    PRESERVE the EM DASH `—` (U+2014) characters in the variant labels (`CSV uploaded — verification pending`). PRESERVE the multiplication sign `×` (U+00D7) in `3-cols × 1-row`. PRESERVE the `≥` (U+2265) character in the `≥ 4.5:1` lines. These are the bytes the Vitest consistency test (Plan 17-01 Task 2) and gsd-ui-checker will look for verbatim.

    Step 4 — DO NOT add HTML comments or section dividers around the new content. DO NOT renumber existing Decisions Log entries. DO NOT touch lines outside the Component Patterns → Data density principle gap.
  </action>

  <verify>
    <automated>grep -c "^## Trust-Tier Badges$\|^## Error Envelope$\|^## Broker Selector Grid$\|^## CSV Escape-Hatch Card$\|^## 9-State Matrix$" DESIGN.md</automated>
  </verify>

  <acceptance_criteria>
    - All five sub-sections present: `grep -c "^## \(Trust-Tier Badges\|Error Envelope\|Broker Selector Grid\|CSV Escape-Hatch Card\|9-State Matrix\)$" DESIGN.md` returns `5`
    - All three trust-tier hex literals present in the new section: `grep -c "#1B6B5A" DESIGN.md` returns `>= 3` (was 2 — chart strategy + accent), one new occurrence in trust-tier table; `grep -c "#4A5568" DESIGN.md` returns `>= 2` (was 1 — text-secondary), one new in trust-tier; `grep -c "#B45309" DESIGN.md` returns `>= 2` (was 1 in 2026-04-30 entry, plus warning row in legend)
    - All three trust-tier labels present verbatim: `grep -F "API verified" DESIGN.md`, `grep -F "CSV uploaded — verification pending" DESIGN.md`, `grep -F "Self-reported" DESIGN.md` each return `>= 1`
    - Verbatim CSV title: `grep -F "Don't have an API key? Upload CSV instead" DESIGN.md` returns `1`
    - Zero TBD/TODO/TKTK in the new sub-sections: extract lines from `^## Trust-Tier Badges` through `^## Data density principle` and verify `grep -E 'TBD|TODO|TKTK'` returns no matches. Use:
      ```
      sed -n '/^## Trust-Tier Badges$/,/^## Data density principle$/p' DESIGN.md | grep -cE 'TBD|TODO|TKTK'
      ```
      Expect `0`.
    - The pre-existing `## Data density principle` section is still present (was not deleted): `grep -c '^## Data density principle$' DESIGN.md` returns `1`
    - QUANTALYZE_DIAG appears once (in Error Envelope section): `grep -c "QUANTALYZE_DIAG" DESIGN.md` returns `1`
  </acceptance_criteria>

  <done>
    DESIGN.md contains the five new H2 sub-sections in the correct anchor position with verbatim prose from UI-SPEC.md §18.1-§18.5. The Phase 17 hard exit gate (zero TBD in new sub-sections) passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append 5 new Decisions Log rows dated 2026-05-01 to DESIGN.md</name>
  <files>DESIGN.md</files>

  <read_first>
    - DESIGN.md current Decisions Log table (lines 126-141 in the pre-task-1 file; line numbers shift after Task 1 — re-read the file)
    - .planning/phases/17-design-contract/17-UI-SPEC.md §18.6 (lines 1107-1115) — verbatim source for the 5 new rows
  </read_first>

  <action>
    Per D-01..D-22 + UI-SPEC.md §18.6 verbatim text.

    Append five new rows at the END of the Decisions Log table (after the 2026-04-30 Recharts row, currently line 141). Each row is a single Markdown table row with three pipe-delimited cells: `Date | Decision | Rationale`. The rows are pre-authored in UI-SPEC §18.6.

    Verbatim rows to append (DO NOT paraphrase — gsd-ui-checker validates verbatim presence):

    ```
    | 2026-05-01 | DESIGN-01 — Trust-tier badge variants + token file landed | Three pill variants (`api_verified` filled accent, `csv_uploaded` neutral grey outline, `self_reported` warning amber outline) shipped as `src/lib/design-tokens/trust-tier.ts` (`TRUST_TIER_TOKENS as const`). Self-reported hex aligned with canonical `--color-warning` (#B45309 — REQUIREMENTS.md DESIGN-01 row corrected from #D97706, which was retired 2026-04-30 for AA failure). Consistency Vitest test `tests/a11y/trust-tier-tokens.test.ts` reads DESIGN.md and asserts each hex appears verbatim. CI gate against drift. |
    | 2026-05-01 | DESIGN-02 — Error envelope wireframe codified + canonical component | Live `WizardErrorEnvelope.tsx` shape adopted as the wireframe (Retry CTA below body, above `<details>`; always-collapsed). Component rebranded → `src/components/error/ErrorEnvelope.tsx` (file move + 1-line re-export shim at old wizard path; zero call-site churn). Title typography upgraded `text-sm text-negative` → `text-base font-semibold text-text-primary` per REQ. Copy-diagnostics payload changed JSON.stringify → newline-delimited prefixed text block (`QUANTALYZE_DIAG\n{code}\n{correlation_id}\n{ISO ts}\n{user_agent}\n{debug_context lines}\n--- pii-scrubbed ---`) with `pii-scrub.ts` pass before clipboard. Surface scope: ALL error renderers (wizard, CSV upload, factsheet, admin status, future `error.tsx`). |
    | 2026-05-01 | DESIGN-03 — Broker selector grid + CSV escape-hatch card | 3-cols × 1-row grid with 3 active cards (OKX, Binance, Bybit) per UC-B; drops the literal "2×3" wording from REQ. Visual: white surface, 1px `#E2E8F0` border, 8px radius (matches `Card` primitive). Per-source field schema (passphrase required for OKX; IP-allowlist hint per source) lives in UI-SPEC.md §per-source-fields, NOT DESIGN.md (keeps DESIGN.md narrow on tokens). Full-width CSV escape-hatch card BELOW the grid with title "Don't have an API key? Upload CSV instead" — same border/radius/surface, secondary-variant CTA, no accent fill (avoids competing with `api_verified` accent identity). |
    | 2026-05-01 | DESIGN-04 — Mobile-readable wizard fallback deferred | Phase 16 / OBSERV-11 PostHog `wizard_start` mobile-device count audit returned 0 (with credential-gap caveat — `posthog-js` short-circuits in production). REQ DESIGN-04 conditional on count > 0; gate honored. Ship 640px `DesktopGate.tsx` as-is for v1. Trigger condition for v2 build: PostHog `wizard_start` event with `device_type='mobile'` count > 0 over a rolling 7-day window in production. Audit cron logs weekly to `.planning/audits/wizard-mobile-count.md`. When trigger fires, future phase builds read-only review state (single column, 16px base, no chrome reflow). Full mobile-responsive polish on strategy pages remains v2 per PROJECT.md. |
    | 2026-05-01 | DESIGN-05 — 9-state matrix + a11y minimums + wizardErrors.ts source-of-truth | 9 surfaces × 9 states (loading / empty / error / partial / success / retry-in-flight / stale / optimistic / offline) with concrete DOM/copy specs in UI-SPEC.md §9-state-matrix. Hard exit gate before Phase 19 — plan-checker rejects entry on any TBD cell. A11y: 4.5:1 contrast asserted by `tests/a11y/wizard-contrast.test.ts`; axe-core CI extended to `/strategies/new/wizard` + `/admin/csv-status`; ARIA live regions on state transitions; keyboard-nav stepper with `aria-current="step"`. `wizardErrors.ts` declared canonical source of `human_message` strings; envelope's `human_message` = wizardErrors `title`, `debug_context` = wizardErrors `fix[]`. Phase 17 absorbs the 19 CSV-branch literal strings Phase 15 left as `// TODO(phase-17): hoist into wizardErrors` (17 new error codes + 3 heading-constant exports + 1 rule-labels constant). |
    ```

    Use the Edit tool to append. Find the `old_string` = the last existing Decisions Log row (the 2026-04-30 Recharts row — confirm the exact text by reading DESIGN.md after Task 1):
    ```
    | 2026-04-30 | Recharts 3.x `accessibilityLayer` opt-out across the codebase | …pinned by `tests/visual/chart-accessibility-layer.test.ts` (whole-codebase grep). |
    ```
    Set `new_string` to the same row PLUS a newline + the five new rows.

    The five rows MUST be added in the order DESIGN-01 → 02 → 03 → 04 → 05 (chronological-by-decision, not chronological-by-creation; matches UI-SPEC §18.6 ordering). All five rows are dated `2026-05-01`.

    PRESERVE backticks around all code references (`api_verified`, `--color-warning`, etc.). PRESERVE the multiplication sign × and EM DASH —. PRESERVE the inline code-block with newline literals `\n` in row 2 (these are LITERAL backslash-n characters in the source, not actual newlines — Markdown table cells can't contain real newlines).

    DO NOT add an HTML comment, section divider, or extra heading. The five rows just append to the existing table.
  </action>

  <verify>
    <automated>grep -c "^| 2026-05-01 |" DESIGN.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^| 2026-05-01 |" DESIGN.md` returns `5` (exactly 5 new rows)
    - Each row mentions its REQ ID: `grep -c "DESIGN-01 —" DESIGN.md` returns `1`, same for `DESIGN-02 —`, `DESIGN-03 —`, `DESIGN-04 —`, `DESIGN-05 —` — total 5
    - `grep -c "TRUST_TIER_TOKENS as const" DESIGN.md` returns `>= 1`
    - `grep -F "QUANTALYZE_DIAG\n{code}" DESIGN.md` returns `>= 1` (literal-newline payload preserved)
    - `grep -F ".planning/audits/wizard-mobile-count.md" DESIGN.md` returns `>= 1`
    - `grep -F "9 surfaces × 9 states" DESIGN.md` returns `>= 1`
    - `grep -F "17 new error codes + 3 heading-constant exports + 1 rule-labels constant" DESIGN.md` returns `1`
    - The decisions log table is still parseable Markdown (visual check via `grep -c '^| ' DESIGN.md` increases by exactly 5 vs pre-task)
  </acceptance_criteria>

  <done>
    Five new Decisions Log rows present, all dated 2026-05-01, in DESIGN-01..05 order, verbatim from UI-SPEC §18.6.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct REQUIREMENTS.md DESIGN-01 from #D97706 to #B45309</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md line 51 (the DESIGN-01 row containing `self_reported warning amber #D97706 outline pill`)
    - .planning/phases/17-design-contract/17-CONTEXT.md `<decisions>` DESIGN-01 first sub-bullet (the rationale: REQ literal #D97706 retired 2026-04-30 for AA failure; aligning to canonical #B45309)
  </read_first>

  <action>
    Per D-01 (REQUIREMENTS.md hex correction `#D97706` → `#B45309`).

    Use the Edit tool to perform a targeted single-line replacement in `.planning/REQUIREMENTS.md`:

    `old_string` = the exact line 51 content (verify by reading the file first):
    ```
    - [ ] **DESIGN-01**: DESIGN.md gains trust-tier badge component variants — `api_verified` filled accent #1B6B5A pill, `csv_uploaded` neutral #4A5568 outline pill, `self_reported` warning amber #D97706 outline pill — with typed token constants in `src/lib/design-tokens/trust-tier.ts`; regex test asserts DESIGN.md ↔ token file consistency
    ```

    `new_string` = same line, two changes:
    1. Replace `#D97706` with `#B45309`.
    2. Append a parenthetical note at end of the line for traceability:
       ` (corrected 2026-05-01: REQ literal was #D97706, retired 2026-04-30 for AA failure; #B45309 aligns with canonical --color-warning).`

    Final new_string:
    ```
    - [ ] **DESIGN-01**: DESIGN.md gains trust-tier badge component variants — `api_verified` filled accent #1B6B5A pill, `csv_uploaded` neutral #4A5568 outline pill, `self_reported` warning amber #B45309 outline pill — with typed token constants in `src/lib/design-tokens/trust-tier.ts`; regex test asserts DESIGN.md ↔ token file consistency (corrected 2026-05-01: REQ literal was #D97706, retired 2026-04-30 for AA failure; #B45309 aligns with canonical --color-warning).
    ```

    DO NOT touch any other line in REQUIREMENTS.md. DO NOT modify the Traceability table at the bottom (the DESIGN-01 row there has no hex value). DO NOT modify the "*Last updated*" footer line (that gets touched in a separate doc-housekeeping pass, NOT this plan).
  </action>

  <verify>
    <automated>grep -c "self_reported warning amber #B45309 outline pill" .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "self_reported warning amber #B45309 outline pill" .planning/REQUIREMENTS.md` returns `1`
    - `grep -c "self_reported warning amber #D97706 outline pill" .planning/REQUIREMENTS.md` returns `0` (retired hex removed from DESIGN-01 row)
    - `grep -c "corrected 2026-05-01" .planning/REQUIREMENTS.md` returns `1` (traceability annotation present)
    - The total file line count changes by exactly 0 (single-line edit; no insertions or deletions)
    - REQUIREMENTS.md remains valid Markdown (the `- [ ] **DESIGN-01**:` bullet still parses as a list item)
  </acceptance_criteria>

  <done>
    REQUIREMENTS.md DESIGN-01 row uses `#B45309` with the traceability annotation. The retired `#D97706` no longer appears in the DESIGN-01 row.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation files (DESIGN.md + REQUIREMENTS.md) | Repo-internal markdown; no runtime impact; consumed by humans and the gsd-ui-checker grep gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Tampering | DESIGN.md additions | mitigate | gsd-ui-checker validates verbatim presence of UI-SPEC §18 prose; Vitest `trust-tier-tokens.test.ts` (Plan 17-01) catches token-vs-DESIGN.md drift |
| T-17-02-02 | Information Disclosure | DESIGN.md / REQUIREMENTS.md | accept | Public design system data — zero PII, zero secrets |
| T-17-02-03 | Repudiation | REQUIREMENTS.md hex correction | mitigate | Inline traceability annotation `(corrected 2026-05-01: …)` documents the correction reason; full rationale in DESIGN.md 2026-04-30 amber-700 Decisions Log row |
</threat_model>

<verification>
End-of-plan checks:
- All five DESIGN.md sub-sections present (Trust-Tier Badges, Error Envelope, Broker Selector Grid, CSV Escape-Hatch Card, 9-State Matrix)
- Five new Decisions Log rows dated 2026-05-01
- REQUIREMENTS.md DESIGN-01 row uses `#B45309`, NOT `#D97706`
- Hard exit gate: `sed -n '/^## Trust-Tier Badges$/,/^## Data density principle$/p' DESIGN.md | grep -cE 'TBD|TODO|TKTK'` returns `0`
- `git rev-parse --abbrev-ref HEAD` returns `v1.0.0-api-key-rewrite-15-16`
- After both Plan 17-01 and Plan 17-02 are complete, `npx vitest run tests/a11y/trust-tier-tokens.test.ts` exits 0 (consistency test passes — token hexes appear verbatim in DESIGN.md)
</verification>

<success_criteria>
- DESIGN.md gains five new sub-sections in the correct anchor position
- DESIGN.md gains five new Decisions Log rows
- REQUIREMENTS.md DESIGN-01 corrected from `#D97706` to `#B45309`
- Zero TBD/TODO/TKTK strings in the new DESIGN.md sub-sections
- All verbatim labels and hex values present
- Branch unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/17-design-contract/17-02-SUMMARY.md` documenting:
- Insertion location chosen (the line numbers between Component Patterns and Data density principle)
- Confirmation that all 5 sub-sections + 5 Decisions Log rows landed
- Confirmation that REQUIREMENTS.md DESIGN-01 corrected
- Hard exit gate verification command + result (zero TBDs in new sub-sections)
- Confirmation that the Plan 17-01 Vitest consistency test now passes (run `npx vitest run tests/a11y/trust-tier-tokens.test.ts` and capture exit code)
- Confirmation that branch remained `v1.0.0-api-key-rewrite-15-16`
</output>
