# Requirements — v1.14 Smoothed options MTM (third factsheet basis) — ✅ COMPLETE (2026-07-23)

> **Outcome:** all 4 v1 requirements shipped (SMTM-01..04), landed to main+prod
> (#633 d60e6fed + #635 0adde939), flipped LIVE. Live acceptance on a real
> Deribit options key stays deferred/out-of-scope (no such key in prod). See
> `v1.14-ROADMAP.md`.

> **Structure (founder 2026-07-22):** this is milestone-sized — three phase-sized layers, each an independent verify+review boundary: **Phase 131** smoothed-MTM analytics core (plans 131-01a + 131-01b), **Phase 132** worker persistence (was plan 131-02), **Phase 133** factsheet third toggle (was plan 131-03).

**Defined:** 2026-07-22
**Core Value:** Options-trading Deribit strategies get an HONEST daily factsheet. Today an un-smoothed options book lumps a whole session's P&L (weeks of accrual) onto the single settlement day (live: 94%/day, +3305%/month, total correct) and the `mark_to_market` basis is honestly gated OFF for it. `smoothed_mtm` — a THIRD selectable basis — spreads each lump across the days it accrued via daily option marks (`Book[d]−Book[d−1]`), total-preserving, so the daily worth is real. Standalone phases 82/83 brought forward; the pure attribution (Phase 82) already ships on `cash_settlement`, this milestone adds the smoothed daily-MTM basis (Phase 83).

**Legend:** ⏳ planned · ✅ shipped · ⚠️ gated (live acceptance)

## v1 Requirements

- [x] **SMTM-01** ✅ — (satisfied by SMTM-02/03/04 across Phases 131/132/133; the additive third basis exists end-to-end, existing two bases byte-identical.) `smoothed_mtm` is a THIRD selectable factsheet `pnl_basis` alongside `cash_settlement` (default/headline/peer-rank, UNCHANGED) and `mark_to_market` (UNCHANGED). Additive: the existing two bases are BYTE-IDENTICAL on every fixture (SC-4), config/compute enums agree (`allocated_capital._VALID_PNL_BASES` + `deribit_txn._PNL_BASES` both carry it). *(Phase 131 — plans 131-01/02/03)*
- [x] **SMTM-02** ✅ — (Phase 131 done 2026-07-22: verifier goal-ACHIEVED 9/9 + reviewer PASS after 2 fix rounds closing 11 money-path findings; suite 4183, SC-4 byte-identity preserved.) The pure smoothed-MTM core: `fetch_deribit_option_daily_marks` (verified source `get_tradingview_chart_data` 1D, works for expired options), pure `replay_option_positions` + `option_mtm_daily` (Book[d]−Book[d−1], bar-tick-day grid), adapter ΔMTM merge (smoothed- AND option-classification-gated). Total-preserving: smoothed total == cash total on a flat terminal book; telescopes to Σchange + Book(last settlement) on a non-flat book. Sparse marks inside a listed instrument's life FAIL LOUD (LedgerValuationError instrument+day; NO interpolation, NO session-lump fallback); pre-retention (whole life predates ~2.5yr chart retention) → cash-basis + `pre_mark_retention_option_days` warning. deribit_txn.py stays pandas/async-free (AST purity). *(Phase 131 — plan 131-01)*
- [x] **SMTM-03** ✅ — (Phase 132 done 2026-07-22: verifier ACHIEVED 9/9 + reviewer PASS after 1 fix round closing HIGH-01 budget-starvation + HIGH-02 Pitfall-5 stale-series + MED/LOW; MTM gate byte-unchanged, suite 4208.) The worker persists the `smoothed_mtm` daily series + scalars in BOTH routes (single-key + composite), keyed `KIND_SMOOTHED_MTM` (no DDL — kind is TEXT), exposed as `metrics_json_by_basis.smoothed_mtm`. A SEPARATE smoothed-availability predicate opens the `unsmoothed_options_book` gate for smoothed WITHOUT mutating the `mark_to_market_available` gate / `MTM_REASON_OPTIONS` (MTM decision byte-identical). *(Phase 131 — plan 131-02)*
- [x] **SMTM-04** ✅ — (Phase 133 done 2026-07-22: verifier ACHIEVED 9/9 + reviewer PASS after 1 fix round closing WR-01 two-surface divergence [root-caused via shared helper] + WR-02 wiring-guard gap + 3 LOW; 770 tests green, cash/MTM byte-identical.) The factsheet renders `smoothed_mtm` across ALL basis-dependent surfaces — SegmentedControl third option, series swap (metrics AND charts, single-key AND composite), basis eyebrow label, chart caption honesty three-state, `suppressRelative`, leverage eligibility (no Phase-107 L=1↔L≠1 jump), peer-rank "computed on cash basis" note — honest-disabled-with-reason where marks are missing. No surface renders smoothed numbers under a wrong basis label. *(Phase 131 — plan 131-03)*

## Out of scope (this milestone)
- Live acceptance on Phoenix (key `95089958`, deleted from test DB → re-onboard) + Zav2 — deferred to post-131-03 live verification (83-PLAN Task 10).
- Non-Deribit venues (smoothing is a Deribit-options-book fix; perp/USD-native books converge with existing bases).

## Traceability
| Req | Phase | Plan(s) | Status |
|-----|-------|---------|--------|
| SMTM-01 | 131 / 132 / 133 | 131-01a/b, 132-01, 133-01 | ✅ all 3 layers done + review-clean |
| SMTM-02 | 131 | 131-01a, 131-01b | ✅ done (verified + review-clean, 2 fix rounds) |
| SMTM-03 | 132 | 132-01 (was 131-02) | ✅ done (verified + review-clean, 1 fix round) |
| SMTM-04 | 133 | 133-01 (was 131-03) | ✅ done (verified + review-clean, 1 fix round) |
