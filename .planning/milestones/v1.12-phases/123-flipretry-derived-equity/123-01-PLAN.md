---
phase: 123-flipretry-derived-equity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/services/equity_reconstruction.py
  - analytics-service/tests/test_job_worker_flipretry.py
autonomous: true
requirements: [FLIPRETRY-01]
tags: [asyncio, worker, timeout, deribit, ccxt]
must_haves:
  truths:
    - "A deribit cash-pass crawl that hangs is cut at the per-crawl bound and returns a transient FAILED — the event loop is never blocked on an unbounded await"
    - "A ccxt transfers/price-index crawl that hangs is cut at the per-crawl bound and returns a transient FAILED"
    - "A reconstruct-history window crawl that hangs is cut at its bound and returns a transient FAILED (defensive, CONTEXT-named path)"
    - "The sfox branch (:2662) and the deribit MTM second pass (:2472) remain byte-identical — they are already bounded"
  artifacts:
    - path: "analytics-service/tests/test_job_worker_flipretry.py"
      provides: "Fake-hang regression tests proving each bound fires transient (FLIPRETRY-01)"
      min_lines: 80
    - path: "analytics-service/services/job_worker.py"
      provides: "wait_for-bounded deribit cash pass + ccxt transfers/price-index crawls"
      contains: "_BROKER_CRAWL_TIMEOUT_S"
    - path: "analytics-service/services/equity_reconstruction.py"
      provides: "wait_for-bounded _fetch_and_price_window orchestrating crawl"
      contains: "wait_for"
  key_links:
    - from: "analytics-service/services/job_worker.py deribit branch"
      to: "asyncio.wait_for"
      via: "bounded await around build_deribit_native_ledger cash pass"
      pattern: "wait_for\\(\\s*build_deribit_native_ledger"
    - from: "analytics-service/services/job_worker.py ccxt branch"
      to: "asyncio.wait_for"
      via: "bounded awaits around fetch_ccxt_transfers + _resolve_ccxt_flow_price_index"
      pattern: "wait_for\\(\\s*fetch_ccxt_transfers"
    - from: "asyncio.TimeoutError"
      to: "DispatchResult transient"
      via: "explicit except arm mirroring the sfox FLIPRETRY-01 block"
      pattern: "error_kind=\"transient\""
---

<objective>
FLIPRETRY-01: bound every unwrapped derived-equity exchange crawl with a hard `asyncio.wait_for`, mirroring the phase-120 sFOX pattern EXACTLY, so a slow/hanging live crawl (the v1.11 FLIP wedge root cause — deribit native ledger ~inception, bybit 19k rows) becomes a classified, retryable transient failure instead of an unbounded await that freezes the single-loop worker.

Purpose: this is 1 of the 3 legs that make the FLIP retry safe (the other two are plan 02's dedicated worker and plan 03's ground-truth gate). Per RESEARCH §1, the load-bearing sites are in `run_derive_broker_dailies_job` (the kind the `derive-allocator-key-dailies` cron fans out), NOT the CONTEXT-originally-named reconstruct path — but both get wrapped (SCOPE CORRECTION in 123-CONTEXT.md, mitigating research assumption A4).

Output: 3 bounded crawl sites + a fake-hang regression test per site, all transient-classified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-flipretry-derived-equity/123-CONTEXT.md
@.planning/phases/123-flipretry-derived-equity/123-RESEARCH.md
</context>

<interfaces>
Verified in-repo contracts the executor uses directly (no exploration needed):

From analytics-service/services/job_worker.py:
- `_SFOX_CRAWL_TIMEOUT_S: Final[float] = float(os.getenv("SFOX_CRAWL_TIMEOUT_S", "300"))` at :195 — the constant pattern to mirror (env-overridable, 300s default, rationale comment at :186-197 explains the 300s×2=600s under the 900s outer budget).
- The sFOX mirror block at :2661-2698: `await asyncio.wait_for(crawl_..., timeout=_SFOX_CRAWL_TIMEOUT_S)` inside a try, then `except asyncio.TimeoutError:` → `logger.warning(...)` (NEVER `logger.exception` — H-3 HMAC-in-URL leak class) → `return DispatchResult(outcome=DispatchOutcome.FAILED, error_message="...(FLIPRETRY-01)", error_kind="transient")`.
- `classify_exception` at :351: `asyncio.TimeoutError → ("transient", ...)` at :364 — defense in depth if a TimeoutError ever escapes to `dispatch()`'s generic handler.
- Deribit cash pass: `native_ledger, _completeness = await build_deribit_native_ledger(ctx.exchange, account_state=..., pnl_basis=..., exclude_spot_extraction=...)` at :2319 — UNWRAPPED, inside an existing `try:` at :2309.
- Deribit MTM second pass at :2472: ALREADY `asyncio.wait_for(..., timeout=_mtm_pass_timeout)` with its own DEGRADE-LOUD TimeoutError arm (:2491-2499) — do NOT touch.
- ccxt branch: `_deposits = await fetch_ccxt_transfers(ctx.exchange, "deposits", ...)` at :2880, `_withdrawals = await fetch_ccxt_transfers(...)` at :2883, `_price_index = await _resolve_ccxt_flow_price_index(...)` at :2893 — all UNWRAPPED.
- `TIMEOUT_PER_KIND["derive_broker_dailies"] = 15 * 60` at :294 — the OUTER watchdog budget; it does NOT protect the event loop (the exact v1.11 error, Pitfall 3).

From analytics-service/services/equity_reconstruction.py:
- `run_reconstruct_allocator_history_job` (:2070) awaits the single orchestrating crawl `_fetch_and_price_window(...)` at :2131 (internally: up to 500 trade pages, transfers, OHLCV gather, CoinGecko fallback, `_fetch_current_equity`). Cleanest wrap point per RESEARCH §1.
- `TIMEOUT_PER_KIND["reconstruct_allocator_history"] = 30 * 60` — outer budget for the bound headroom calc.
- `run_refresh_allocator_equity_daily_job` (:2367) reads DB holdings only (no live crawl) — out of scope.

From analytics-service/tests/test_sfox_reconstruct.py:
- :727-1000 — the T-120-10 / FLIPRETRY-01 fake-hang test pattern: monkeypatch the crawl coroutine to `asyncio.sleep`-forever, monkeypatch the timeout constant to a sub-second value, run the handler, assert FAILED + `error_kind == "transient"` within wall-clock seconds. Mirror its job/ctx fixture construction.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wait_for-bound the deribit cash pass and the ccxt transfers/price-index crawls in run_derive_broker_dailies_job (fake-hang tests first)</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_job_worker_flipretry.py</files>
  <read_first>analytics-service/services/job_worker.py :186-213 (the sfox constant + comment), :2290-2360 (deribit cash pass + surrounding try/except arms — read the FULL except chain so the new TimeoutError arm is ordered before any broader catch), :2440-2500 (the already-bounded MTM second pass — reference, untouched), :2638-2698 (the sfox mirror block, copy its shape verbatim), :2850-2930 (the ccxt branch incl. the WR-04 transient-bubble comment); analytics-service/tests/test_sfox_reconstruct.py :727-1000 (the fake-hang test pattern + fixtures); 123-RESEARCH.md §1 (crawl-site table C1-C3)</read_first>
  <behavior>
    - Test 1 (deribit hang): monkeypatch `build_deribit_native_ledger` to a coroutine that awaits `asyncio.sleep(9999)`; monkeypatch the new bound constant to ~0.05s; drive `run_derive_broker_dailies_job` down the deribit venue branch (mirror the test_sfox_reconstruct fixture approach for job/ctx); assert the handler returns within ~2s wall-clock with `DispatchResult(FAILED)`, `error_kind == "transient"`, and an error_message referencing the per-crawl bound + FLIPRETRY-01 — NEVER a terminal/permanent stamp, NEVER a hang.
    - Test 2 (ccxt hang): same shape with `fetch_ccxt_transfers` mocked to hang on the ccxt venue branch (e.g. bybit); assert transient FAILED within the bound.
    - Test 3 (ccxt price-index hang): `_resolve_ccxt_flow_price_index` mocked to hang; assert transient FAILED within the bound.
    - Test 4 (bound not hit): a fast mocked crawl passes through unchanged — the wrap is behavior-neutral on the happy path (assert the branch proceeds past the crawl, e.g. the next honesty gate fires as before).
  </behavior>
  <action>
    RED first: create `analytics-service/tests/test_job_worker_flipretry.py` (the filename 123-VALIDATION.md names) with the 4 behaviors above; run them and confirm Tests 1-3 FAIL on current code (the hang is only cut by the 900s outer budget, so with the constant monkeypatched the handler still does not return transient at the crawl bound). Then GREEN:

    1. Add a module constant `_BROKER_CRAWL_TIMEOUT_S` next to `_SFOX_CRAWL_TIMEOUT_S` (:195): env-overridable via `BROKER_CRAWL_TIMEOUT_S`, default 300 — mirroring the sfox constant per D "mirror the sfox wait_for exactly" and the Claude's-discretion note in 123-CONTEXT.md (300s per crawl leaves headroom under the 900s `derive_broker_dailies` outer budget; document assumption A1 — the founder verifies the bound against a real deribit-inception/bybit-19k crawl in the plan-03 runbook pilot). Carry a comment naming the v1.11 wedge root cause, like the sfox comment at :186-197.
    2. Deribit cash pass (:2319): wrap the `build_deribit_native_ledger` await in `asyncio.wait_for(..., timeout=_BROKER_CRAWL_TIMEOUT_S)` and add an `except asyncio.TimeoutError:` arm that mirrors the sfox block at :2680-2698 — `logger.warning` (scrubbed static text only, never `logger.exception`, never interpolated exception content from the crawl) then return `DispatchResult(FAILED, error_message=... "(FLIPRETRY-01)", error_kind="transient")`. Order the arm so it is caught BEFORE any existing broader except in the :2309 try (read the full arm chain first). Do NOT stamp `_stamp_strategy_analytics_failed` on timeout — a transient retries; a terminal stamp is the sfox-truncation (permanent) path only. The MTM second pass at :2472 stays byte-identical.
    3. ccxt branch: wrap BOTH `fetch_ccxt_transfers` awaits (:2880, :2883) and the `_resolve_ccxt_flow_price_index` await (:2893, research C3 — it may hit venue OHLCV I/O) each in `asyncio.wait_for(..., timeout=_BROKER_CRAWL_TIMEOUT_S)`, with one shared `except asyncio.TimeoutError:` transient return of the same shape. Preserve the WR-04 contract: non-timeout fetch errors keep bubbling to the dispatcher classifier exactly as today, and the HIGH-1 `NavReconstructionError` permanent disposition of the pure valuer (:2908-2919) is untouched — the wait_for wraps ONLY the awaits, not the pure `ccxt_rows_to_dated_flows` call.
    4. Confirm `finally: aclose_exchange` (or the branch's equivalent cleanup) still runs on the timeout paths — the sfox comment at :2683 documents this requirement.

    Run the new file plus the neighboring suites (`tests/test_job_worker.py`, `tests/test_sfox_reconstruct.py`) to prove no regression; mypy if configured for the service.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_job_worker_flipretry.py tests/test_job_worker.py tests/test_sfox_reconstruct.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Fake-hang tests exist and pass for deribit cash pass, ccxt transfers, and ccxt price-index; each proves transient FAILED within the (monkeypatched) bound, never a permanent stamp, never an unbounded await.
    - ⚠️ (plan-checker warning) In Python 3.11+ `asyncio.TimeoutError IS builtins.TimeoutError` (an `OSError` subclass), so the new `except asyncio.TimeoutError` arm MUST be verified to precede EVERY broader `except (Exception | OSError | ...)` in the `:2309` chain (whose first arm stamps `error_kind="permanent"`, the honesty gate) — otherwise a timeout mis-disposes as PERMANENT and defeats the transient-retry intent. The deribit fake-hang test explicitly asserts `error_kind == "transient"` (NOT `"permanent"`) to catch this ordering regression.
    - `grep -c "wait_for" analytics-service/services/job_worker.py` increases by exactly the new bounded sites; `grep -n "logger.exception" ` shows NO new occurrence at the new catch sites.
    - sfox block :2661-2698 and MTM pass :2472-2499 byte-identical (git diff shows no hunk there).
    - Happy-path test proves behavior-neutrality when the crawl is fast.
  </acceptance_criteria>
  <done>All three previously-unwrapped crawls in run_derive_broker_dailies_job are wait_for-bounded with transient classification, proven by failing-first fake-hang tests; existing suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: defensively wait_for-bound the reconstruct-history orchestrating crawl (CONTEXT-named path)</name>
  <files>analytics-service/services/equity_reconstruction.py, analytics-service/tests/test_job_worker_flipretry.py</files>
  <read_first>analytics-service/services/equity_reconstruction.py :2070-2160 (run_reconstruct_allocator_history_job through the `_fetch_and_price_window` await at :2131 + its except arms); 123-RESEARCH.md §1 "Legacy path" subsection; the sfox constant comment job_worker.py:186-197 (headroom rationale to adapt)</read_first>
  <behavior>
    - Test 5 (reconstruct hang): monkeypatch `_fetch_and_price_window` to hang; monkeypatch the new reconstruct bound to ~0.05s; drive `run_reconstruct_allocator_history_job`; assert transient FAILED (or the handler's established transient-disposition equivalent — match how that handler classifies retryable errors today) within ~2s wall-clock.
  </behavior>
  <action>
    RED then GREEN. The original 123-CONTEXT locked decision named this path; the SCOPE CORRECTION moved the load-bearing work to Task 1 but research A4's mitigation is to wrap BOTH pipelines — this task is the defensive half.

    Add a module constant in equity_reconstruction.py, env-overridable via `RECONSTRUCT_CRAWL_TIMEOUT_S`, defaulting to a bound that leaves persist headroom under the 30-min `reconstruct_allocator_history` outer budget (recommend 1500s = 25 min; a full 500-page window crawl is legitimately long — comment the rationale the way job_worker.py:186-197 does). Wrap the single `_fetch_and_price_window(...)` await at :2131 in `asyncio.wait_for` with an `except asyncio.TimeoutError:` arm returning the handler's transient/retryable disposition (read how this handler returns errors — it may use DispatchResult like job_worker or its own envelope; mirror its existing retryable arm, scrubbed static log text, no `logger.exception`). `run_refresh_allocator_equity_daily_job` (:2367) is DB-only — do not touch it.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_job_worker_flipretry.py -q && cd .. && git diff --stat analytics-service/services/equity_reconstruction.py</automated>
  </verify>
  <acceptance_criteria>
    - Test 5 passes; fails when the wait_for is removed (prove once by reverting the wrap locally, then restore — the test must pin the wiring, not just the helper).
    - The diff touches ONLY the constant + the :2131 call site + the except arm (surgical; refresh path untouched).
  </acceptance_criteria>
  <done>The reconstruct orchestrating crawl is bounded and transient-classified; test proves the bound fires; refresh job untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| worker → live exchange APIs | untrusted latency/hangs and error payloads cross here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | DoS | run_derive_broker_dailies_job crawls | mitigate | hard asyncio.wait_for per crawl → transient FAILED (this plan) |
| T-123-02 | Info disclosure | new TimeoutError log lines | mitigate | static scrubbed messages only; never logger.exception at catch sites (H-3 class); grep-gated in acceptance |
| T-123-03 | Tampering | none — no package installs in this phase | accept | no npm/pip/cargo install tasks exist |
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_job_worker_flipretry.py -q` green (5 behaviors).
- `git diff` shows no hunks in the sfox block or the MTM second pass.
- Full analytics suite green: `cd analytics-service && .venv/bin/python -m pytest -q`.
</verification>

<success_criteria>
FLIPRETRY-01 code leg complete: every derived-equity crawl (deribit cash pass, ccxt transfers ×2, ccxt price-index, reconstruct window crawl) is wait_for-bounded and transient-classified, mirroring the sfox pattern; fake-hang regression tests prove each bound fires; already-bounded sites byte-identical.
</success_criteria>

<output>
Create `.planning/phases/123-flipretry-derived-equity/123-01-SUMMARY.md` when done.
</output>
