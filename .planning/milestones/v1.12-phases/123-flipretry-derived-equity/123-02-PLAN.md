---
phase: 123-flipretry-derived-equity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/2026XXXXXXXXXX_claim_kind_filter.sql
  - supabase/tests/test_claim_kind_filter.sql
  - analytics-service/main_worker.py
  - analytics-service/tests/test_main_worker.py
autonomous: true
requirements: [FLIPRETRY-02, FLIPRETRY-04]
tags: [worker, pg-cron, claim-rpc, healthz, railway]
must_haves:
  truths:
    - "A worker in backfill role claims ONLY the backfill kinds (derive_broker_dailies, derive_allocator_equity); a worker in interactive role NEVER claims them — the backfill runs structurally OFF the sequential prod loop"
    - "Default role ('all') produces the byte-identical 3-arg claim RPC call — merging this plan changes NO prod behavior until the founder flips env"
    - "LAST_TICK_AT refreshes between jobs inside a claimed batch, so a batch of bounded-slow backfill jobs never falsely stales healthz — while a genuinely frozen loop still goes stale (no refresh can run)"
    - "Re-running the enqueue fan-out never creates a duplicate in-flight derive_broker_dailies per key (idempotent, safe to re-run)"
  artifacts:
    - path: "supabase/migrations/2026XXXXXXXXXX_claim_kind_filter.sql"
      provides: "claim_compute_jobs_with_priority re-based with p_kind_include/p_kind_exclude (NULL = today's behavior)"
      contains: "p_kind_exclude"
    - path: "supabase/tests/test_claim_kind_filter.sql"
      provides: "SQL gates: kind filter include/exclude/NULL + fan-out idempotency re-run"
      min_lines: 40
    - path: "analytics-service/main_worker.py"
      provides: "WORKER_CLAIM_ROLE env (all|interactive|backfill), BACKFILL_KINDS, per-job LAST_TICK_AT refresh"
      contains: "WORKER_CLAIM_ROLE"
  key_links:
    - from: "analytics-service/main_worker.py _claim_priority"
      to: "claim_compute_jobs_with_priority"
      via: "role-derived p_kind_include/p_kind_exclude RPC args"
      pattern: "p_kind_(include|exclude)"
    - from: "main_worker.py dispatch_tick for-job loop"
      to: "main_worker_healthz.LAST_TICK_AT"
      via: "per-job refresh between dispatches"
      pattern: "LAST_TICK_AT"
---

<objective>
FLIPRETRY-02 (the STRUCTURAL fix) + the FLIPRETRY-04 worker-side health guarantees: give the claim RPC a kind filter so a DEDICATED backfill worker claims ONLY the backfill kinds and the prod worker EXCLUDES them — because per-crawl wait_for alone cannot keep the shared loop's healthz fresh (5 sequential 300s-bounded jobs = 25 min with LAST_TICK_AT frozen; RESEARCH §2, the KEY INSIGHT in 123-CONTEXT.md). Plus per-job LAST_TICK_AT refresh so the batched worker's own healthz stays honest mid-batch, and a SQL gate proving the enqueue fan-out is idempotent.

Purpose: the v1.11 wedge recurred structurally, not incidentally — isolation of the backfill blast radius is the only guarantee that protects prod healthz even against a non-yielding (wait_for-uncancellable) crawl. The sFOX-F5 active-account crawl rides this worker for free (same derive_broker_dailies kind — note in SUMMARY, build nothing sFOX-specific).

Output: kind-filtered claim RPC migration (TEST-applied), role-aware worker, per-job healthz refresh, SQL idempotency + filter gates. The actual Railway deploy + cron re-schedule are founder ops in plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-flipretry-derived-equity/123-CONTEXT.md
@.planning/phases/123-flipretry-derived-equity/123-RESEARCH.md
</context>

<interfaces>
Verified in-repo contracts:

From supabase/migrations/20260603120000_compute_jobs_rpc_clear_error_and_gin_fanin.sql (:170 — the LATEST claim RPC definition; re-verify with a repo-wide grep before writing, per the re-base rule):
- Signature: `claim_compute_jobs_with_priority(p_batch_size INTEGER, p_worker_id TEXT, p_unified_backbone_active BOOLEAN DEFAULT NULL) RETURNS SETOF compute_jobs, SECURITY DEFINER, SET search_path = public, pg_temp`.
- Body has a `v_high_pending` short-circuit probe (any normal/high pending → exclude priority='low' this tick) + the FOR UPDATE SKIP LOCKED claim SELECT + claim_token stamping + the metadata backbone stamp. COMMENT + REVOKE at :334/:341 name the `(INTEGER, TEXT, BOOLEAN)` signature.

From analytics-service/main_worker.py:
- `_claim_priority()` at :393-401 calls the RPC with exactly `{p_batch_size: 5, p_worker_id, p_unified_backbone_active: flag_active}`.
- `_claim_legacy()` at :403-413 — the 42883 fallback (2-arg `claim_compute_jobs`), latched via `_FALLBACK_CLAIM_RPC`/`_FALLBACK_LATCHED_AT` with re-probe (:415-485).
- `jobs: list[ClaimedJob] = claim_result.data or []` :490; `main_worker_healthz.LAST_TICK_AT = time.time()` at :498 (ONCE per tick, at claim); sequential `for job in jobs:` at :505 with `await dispatch(...)` at :523.
- `main()` gathers dispatch_loop + watchdog_loop + daily_enqueue_loop + start_healthz_server on ONE loop (:849-854).

From analytics-service/main_worker_healthz.py: `STALE_THRESHOLD = 90.0`; healthz 503 when `now - LAST_TICK_AT > 90`.

From supabase/migrations/20260717233529_allocator_equity_derived_surface.sql STEP 4 (:218-260): `enqueue_derive_broker_dailies_for_allocator_keys()` — advisory lock `hashtext('derive_broker_dailies_key_fanout')`, per-key `idempotency_key = 'derive-dailies-<key>-<UTC-date>'`, `EXCEPTION WHEN unique_violation THEN NULL`, one-inflight via `compute_jobs_one_inflight_per_kind_api_key`. STEP 5 (:274-289): the cron (live-UNSCHEDULED at v1.11 recovery — repo/live diverge; do NOT re-schedule in this plan).

From analytics-service/tests/test_main_worker.py: existing dispatch_tick/watchdog tests incl. `test_every_kind_has_watchdog_headroom` — extend, do not fork patterns.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: migration — kind-filtered claim RPC (re-based on the latest def) + SQL gates for filter and fan-out idempotency</name>
  <files>supabase/migrations/2026XXXXXXXXXX_claim_kind_filter.sql, supabase/tests/test_claim_kind_filter.sql, supabase/tests/test_claim_compute_jobs_dedupe_partition.sql, supabase/tests/test_compute_jobs_rpc_error_clear_and_fanin.sql</files>
  <read_first>ALL migrations touching the claim RPC — run a repo-wide grep for claim_compute_jobs_with_priority and read the LATEST full definition end-to-end (currently 20260603120000_compute_jobs_rpc_clear_error_and_gin_fanin.sql:170-341) plus one earlier one to see the re-base convention; supabase/migrations/20260717233529_...sql STEP 4 (the fan-out fn under test); one existing supabase/tests/test_*.sql file for the SQL-test harness conventions (pgTAP or DO-block style — match exactly); 123-RESEARCH.md §2 + Open Question 2</read_first>
  <action>
    Create a new timestamped migration (real `date +%Y%m%d%H%M%S` stamp; the frontmatter placeholder is not literal) that re-bases the FULL latest body of `claim_compute_jobs_with_priority` and adds two args: `p_kind_include TEXT[] DEFAULT NULL, p_kind_exclude TEXT[] DEFAULT NULL`.

    Hard constraints:
    - `DROP FUNCTION IF EXISTS claim_compute_jobs_with_priority(INTEGER, TEXT, BOOLEAN);` BEFORE the CREATE — CREATE OR REPLACE with a different arg list creates an OVERLOAD, and the worker's 3-named-arg PostgREST rpc call would then be ambiguous. The drop/create window is deploy-safe: main_worker's structured-42883 fallback latch (:415-485) degrades to the legacy claim for that tick and self-heals on re-probe — state this in the migration header comment.
    - Filter semantics: `p_kind_include IS NULL OR kind = ANY(p_kind_include)` AND `p_kind_exclude IS NULL OR NOT (kind = ANY(p_kind_exclude))`. Apply the SAME predicate to EVERY kind-touching read in the body — the claim SELECT AND the v_high_pending short-circuit probe — so a filtered worker's low-priority throttle decision is scoped to the kinds it can actually claim (an include-filtered backfill worker must not defer on unrelated prod normal/high pending). Both NULL → byte-identical behavior to the current definition (prove in the SQL test).
    - Re-state COMMENT ON FUNCTION and the REVOKE ... FROM PUBLIC, anon, authenticated with the NEW 5-arg signature (the old COMMENT/REVOKE die with the DROP). Preserve SECURITY DEFINER + search_path exactly.
    - Keep the fencing/priority/dedupe/metadata-stamp contract of the latest body UNCHANGED — this migration adds ONLY the filter predicates (re-base rule: never regress an intermediate definition).

    Create `supabase/tests/test_claim_kind_filter.sql` (CI-run location per the db-test wiring rule) asserting:
    1. Seed pending jobs of a backfill kind + a non-backfill kind; claim with `p_kind_exclude := ARRAY['derive_broker_dailies','derive_allocator_equity']` returns NO backfill rows; claim with `p_kind_include := ` the same array returns ONLY backfill rows; claim with both NULL returns kinds as today.
    2. Fan-out idempotency (FLIPRETRY-04, the 123-VALIDATION Wave-0 gap): call `enqueue_derive_broker_dailies_for_allocator_keys()` twice against a seeded eligible api_key; assert exactly ONE in-flight `derive_broker_dailies` row for that key (the unique_violation swallow + one-inflight index + advisory lock hold).

    ⚠️ BLOCKER FIX (plan-checker): two committed CI SQL gates pin the OLD 3-arg signature via
    `pg_get_functiondef('claim_compute_jobs_with_priority(integer,text,boolean)'::regprocedure)` —
    `supabase/tests/test_claim_compute_jobs_dedupe_partition.sql:39` and
    `supabase/tests/test_compute_jobs_rpc_error_clear_and_fanin.sql:28`. After the DROP+5-arg CREATE,
    that `::regprocedure` cast raises `function does not exist` → both DO-blocks error under
    `psql -v ON_ERROR_STOP=1` → the branch-protection db-test CI job goes RED. UPDATE the signature
    reference in BOTH files to the new 5-arg form
    `claim_compute_jobs_with_priority(integer,text,boolean,text[],text[])`, preserving every existing
    body assertion (rn_s=1, PARTITION BY kind/strategy_id, last-error-clear/fan-in) unchanged. Grep
    `grep -rn "with_priority(integer,text,boolean)" supabase/tests/` to confirm no other file pins it.

    Then MCP-apply the migration to the TEST project `qmnijlgmdhviwzwfyzlc` (standing rule — BEFORE merge, or the RED-guarded SQL tests fail in CI) and fix the schema_migrations stamped timestamp to the file's timestamp (the MCP apply_migration drift rule). Do NOT apply to prod (`khslejtfbuezsmvmtsdn`) — merge auto-applies. After MCP-apply, re-run the two updated signature-pinned tests against TEST to confirm they resolve the 5-arg function and their body assertions still pass.
  </action>
  <verify>
    <automated>bash -c 'f=$(ls supabase/migrations/*claim_kind_filter.sql); grep -c "p_kind_include" "$f" | grep -qv "^0$" && grep -q "DROP FUNCTION IF EXISTS claim_compute_jobs_with_priority(INTEGER, TEXT, BOOLEAN)" "$f" && grep -q "REVOKE" "$f" && test -f supabase/tests/test_claim_kind_filter.sql'</automated>
    <human-check>Supabase MCP against the TEST project: run the two test scenarios' SQL and confirm filter + idempotency behavior (the *_live SQL never runs locally)</human-check>
  </verify>
  <acceptance_criteria>
    - Migration drops the exact old 3-arg signature before creating the 5-arg one; NULL/NULL is proven behavior-identical by the SQL test.
    - The v_high_pending probe carries the same kind filter as the claim SELECT.
    - COMMENT + REVOKE re-stated for the new signature; SECURITY DEFINER + search_path preserved.
    - SQL test file covers include / exclude / NULL-passthrough / double-fan-out-single-inflight.
    - Migration MCP-applied to TEST project with corrected schema_migrations timestamp; prod untouched.
    - The 2 signature-pinned CI tests (test_claim_compute_jobs_dedupe_partition.sql,
      test_compute_jobs_rpc_error_clear_and_fanin.sql) updated to the 5-arg `::regprocedure` cast,
      body assertions unchanged, and re-verified GREEN against TEST after the MCP-apply — CI db-test
      job stays green.
  </acceptance_criteria>
  <done>Kind-filtered claim RPC exists on TEST, defaults byte-identical, gated by supabase/tests SQL; fan-out idempotency pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: role-aware worker claim + per-job LAST_TICK_AT refresh</name>
  <files>analytics-service/main_worker.py, analytics-service/tests/test_main_worker.py</files>
  <read_first>analytics-service/main_worker.py :360-560 (dispatch_tick end-to-end: both claim closures, the fallback latch, :498 LAST_TICK_AT, the for-job loop) and :821-860 (main); analytics-service/main_worker_healthz.py (STALE_THRESHOLD mechanics); analytics-service/tests/test_main_worker.py (existing dispatch_tick test fixtures — reuse their supabase/dispatch mocking); 123-RESEARCH.md §2 Options table + §4</read_first>
  <behavior>
    - Role mapping: WORKER_CLAIM_ROLE unset/"all" → _claim_priority sends EXACTLY today's 3 args (assert the rpc payload dict has no p_kind_* keys — byte-identical merge safety); "interactive" → sends p_kind_exclude == list(BACKFILL_KINDS) and no include; "backfill" → sends p_kind_include == list(BACKFILL_KINDS) and no exclude; any other value → startup ValueError (fail loud, no silent default).
    - Legacy-fallback safety: with role "backfill" and the 42883 fallback path triggered, dispatch_tick claims NOTHING and logs an error (the 2-arg legacy RPC cannot filter — a backfill worker must never claim interactive jobs); with role "interactive" the fallback still claims (today's behavior) but the warning names the degraded exclusion.
    - Per-job refresh: with 2 mocked slow-ish jobs in one claimed batch, LAST_TICK_AT strictly advances between the two dispatch calls (refresh at the TOP of each for-job iteration — between jobs the loop is provably alive, so the signal is honest; a frozen loop gets no refresh and still goes stale past 90s — assert no refresh happens DURING a single mocked dispatch await).
  </behavior>
  <action>
    RED first in tests/test_main_worker.py (new test class, reusing existing fixtures), then GREEN in main_worker.py:

    1. Module constants: `BACKFILL_KINDS: Final = ("derive_broker_dailies", "derive_allocator_equity")` (per 123-CONTEXT SCOPE CORRECTION — the cron fans out derive_broker_dailies which follow-on-enqueues derive_allocator_equity; both leave the prod loop together so the whole chain is isolated) and `WORKER_CLAIM_ROLE` read once at import/startup from env, validated against {"all","interactive","backfill"} with a loud ValueError otherwise. Comment WHY "all" is the default: merge must change nothing until the founder cuts over BOTH workers in the plan-03 runbook — the same structural-gate discipline as SFOX_ENABLED (Phase 122). Note in a comment that "interactive" also moves NEW-key broker-dailies ingestion (deribit/sfox key connects) onto the dedicated worker — intended (those are exactly the long-crawl jobs), and the runbook makes the dedicated worker prod-critical from cutover.
    2. `_claim_priority()`: build the args dict from the role — role "all" adds NO new keys (the RPC's NULL defaults preserve behavior and the payload stays byte-identical for prod today).
    3. Fallback guard: in the 42883 fallback branches, if role == "backfill", skip claiming entirely (return after an error log naming the missing migration) — fail loud rather than claim out-of-role jobs.
    4. Per-job healthz refresh: at the top of the `for job in jobs:` loop, set `main_worker_healthz.LAST_TICK_AT = time.time()` (keep the :498 at-claim write). This is the FLIPRETRY-04 defense-in-depth from RESEARCH §4: the DEDICATED worker's own healthz stays green across a batch of bounded 300s crawls, while a genuinely frozen loop still cannot refresh and goes stale — liveness detection is not weakened.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_main_worker.py tests/test_health.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests above pass; the role="all" byte-identical-payload assertion would FAIL if anyone later adds an unconditional p_kind_* arg (wiring pinned).
    - `test_every_kind_has_watchdog_headroom` still green (no kind table changes).
    - The per-job refresh test proves refresh BETWEEN jobs and NO refresh DURING a dispatch await.
    - No new env is required for today's prod deploy (default "all" documented).
  </acceptance_criteria>
  <done>Worker role mechanism + per-job healthz refresh landed and pinned; prod behavior unchanged until founder cutover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| PostgREST rpc → claim RPC | SECURITY DEFINER hot path; args cross from worker env |
| env → worker role | misconfigured role changes claim scope |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-04 | Elevation | claim_compute_jobs_with_priority | mitigate | DROP+CREATE re-states REVOKE FROM PUBLIC/anon/authenticated; SECURITY DEFINER + pinned search_path preserved |
| T-123-05 | DoS | prod loop claiming backfill kinds | mitigate | kind-exclude on interactive role; backfill role refuses legacy-fallback claiming |
| T-123-06 | Repudiation/health masking | per-job LAST_TICK_AT refresh | mitigate | refresh only BETWEEN jobs (loop provably alive); test asserts no refresh during a frozen dispatch |
| T-123-07 | Tampering | none — no package installs | accept | no install tasks in this plan |
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_main_worker.py tests/test_health.py -q` green.
- Migration + SQL test committed; TEST project carries the new RPC (verify objects via Supabase MCP on qmnijlgmdhviwzwfyzlc).
- `git diff` of main_worker.py shows the role plumbing + one-line-per-iteration refresh only (surgical).
</verification>

<success_criteria>
FLIPRETRY-02 mechanism complete in code+SQL: a dedicated worker can claim only backfill kinds, prod can exclude them, defaults change nothing, the fan-out enqueue is pinned idempotent, and healthz semantics are honest per-job. Founder deploy/cutover/cron are plan 03.
</success_criteria>

<output>
Create `.planning/phases/123-flipretry-derived-equity/123-02-SUMMARY.md` when done.
</output>
