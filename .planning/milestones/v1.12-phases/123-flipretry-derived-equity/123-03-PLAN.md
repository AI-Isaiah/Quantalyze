---
phase: 123-flipretry-derived-equity
plan: 03
type: execute
wave: 2
depends_on: [123-01, 123-02]
files_modified:
  - analytics-service/tests/test_e2_ground_truth_harness.py
  - src/lib/queries.test.ts
  - docs/runbooks/flipretry-derived-equity-go-live.md
autonomous: false
requirements: [FLIPRETRY-03, FLIPRETRY-04]
tags: [ground-truth, e2, runbook, founder-ops, healthz]
user_setup:
  - service: railway
    why: "Live E2 anchor-consistency run + dedicated backfill worker"
    env_vars:
      - name: E2_GROUND_TRUTH_API_KEY
        source: "read-only exchange key minted by founder; set on the Railway analytics service"
      - name: E2_GROUND_TRUTH_API_SECRET
        source: "same key's secret"
      - name: E2_GROUND_TRUTH_PASSPHRASE
        source: "if the venue requires one"
      - name: WORKER_CLAIM_ROLE
        source: "set 'backfill' on the NEW dedicated Railway worker service; 'interactive' on the existing prod worker at cutover"
    dashboard_config:
      - task: "Create the second Railway worker service (same repo/image) with WORKER_CLAIM_ROLE=backfill"
        location: "Railway project -> New Service"
must_haves:
  truths:
    - "The E2 verdict is FALSE when anchor drift exceeds tolerance OR the curve carries a blocking degradation (is_trustworthy false) — pinned by hand-derived economic fixtures, never by re-calling the compose formula (P115)"
    - "extractTrustworthyDerivedCurve → equityCurveSource wiring: a trustworthy well-formed payload renders derived; the SAME curve with is_trustworthy false renders legacy — proven at the derivePhase07Fields wiring level"
    - "A written, executable go-live runbook sequences: verify deploys → deploy dedicated worker → flip prod role → pilot enqueue with healthz watch → LIVE E2 exit 0 → full enqueue → cron re-schedule; and carries the verbatim v1.11 rollback"
    - "No derived curve is shown to users before the founder-gated live E2 run passes — the staged gate is documented and the founder checkpoint enforces it"
  artifacts:
    - path: "analytics-service/tests/test_e2_ground_truth_harness.py"
      provides: "anchor pass/fail fixture pair binding drift + trustworthiness to the verdict"
      contains: "compute_anchor_consistency"
    - path: "src/lib/queries.test.ts"
      provides: "derived↔legacy flip fixtures at the derivePhase07Fields wiring level"
      contains: "equityCurveSource"
    - path: "docs/runbooks/flipretry-derived-equity-go-live.md"
      provides: "go-live sequence + verbatim v1.11 rollback + bound-verification pilot"
      min_lines: 60
  key_links:
    - from: "allocator_equity_derived.payload.is_trustworthy"
      to: "equityCurveSource"
      via: "extractTrustworthyDerivedCurve inside derivePhase07Fields"
      pattern: "extractTrustworthyDerivedCurve"
    - from: "scripts/e2_allocator_ground_truth.py"
      to: "go-live gate"
      via: "exit 0 required before full enqueue + cron (runbook step; exit 3 SKIP is NOT a pass)"
      pattern: "within_same_day_tolerance"
---

<objective>
FLIPRETRY-03 + the FLIPRETRY-04 documentation/ops leg: pin the ground-truth gate with CI fixtures (the E2 anchor verdict and the TS display flip), write the go-live runbook that sequences the founder ops so no unvalidated curve can ever show and the prod worker can never wedge, and gate the live legs behind a founder checkpoint (never faked).

Purpose: the flip is DATA-DRIVEN (no flag) — `extractTrustworthyDerivedCurve` already gates on the persisted `is_trustworthy` (queries.ts:2379, VERIFIED: no new TS wiring needed, per the 123-CONTEXT SCOPE CORRECTION). The safety model is the RESEARCH §3 staged gate: (1) backfill writes self-assessed `is_trustworthy` curves; (2) the founder runs the INDEPENDENT E2 harness (P115 — it consumes the persisted anchors + a live read via the deribit_ground_truth sanitize contract, and its verdict itself gates on `is_trustworthy`); (3) ONLY on E2 exit 0 does the founder run the full enqueue + cron re-schedule; (4) the SSR flip reads `is_trustworthy`. CI carries the committed harness + fixtures; the live run is human_needed.

Output: fixture tests (pytest + vitest), the runbook, and the blocking founder checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-flipretry-derived-equity/123-CONTEXT.md
@.planning/phases/123-flipretry-derived-equity/123-RESEARCH.md
@.planning/phases/123-flipretry-derived-equity/123-01-SUMMARY.md
@.planning/phases/123-flipretry-derived-equity/123-02-SUMMARY.md
</context>

<interfaces>
Verified in-repo contracts:

From analytics-service/scripts/e2_allocator_ground_truth.py:
- `compute_anchor_consistency(derived_terminal: float, live_equity: float, tol: float, *, trustworthy: bool = True) -> dict` (:144) — returns `drift_pct`, `drift_within_tol`, `trustworthy`, `within_same_day_tolerance` (= within_tol AND trustworthy); raises `GroundTruthSkip` on non-positive derived terminal. C3/LOW-7: gates on `is_trustworthy` (benign `window_truncated` stays clean; blocking degradation does not).
- `derive_terminal_equity(series_by_key, anchors_by_key) -> (derived_terminal, flags, is_trustworthy)` (:181) — uses ONLY the pure allocator_equity_derive core (backward $-replay + common-window sum), fails loud on honest-empty.
- Exit codes: 0 pass, non-zero fail, 3 SKIP (missing `E2_GROUND_TRUTH_*` env/spec). Evidence scrubbed via `scripts.deribit_ground_truth.assert_sanitized`/`sanitize_evidence` — the P115 independence contract.

From src/lib/queries.ts:
- `extractTrustworthyDerivedCurve(payload: unknown): DailyPoint[] | null` (:2379) — null unless `is_trustworthy === true` AND every point has strict YYYY-MM-DD date + finite `equity_usd` AND curve non-empty (B2: empty degrades to legacy).
- `derivePhase07Fields(apiKeys, equitySnapshots, snapshotCount, holdingsRows, equityBaselineUnknown, derivedEquityRow)` (:2422) — the ONE producer: `equityDailyPoints = derivedCurve ?? equitySnapshotsToDailyPoints(...)`; `equityCurveSource = derivedCurve !== null ? "derived" : "legacy"` (:2515); `derivedEquityRow` param shape `{ payload: unknown; computed_at: string | null } | null`.

From analytics-service/services/allocator_equity_compose.py: `is_trustworthy` written into the payload at :238 (self-assessed from classified degradation reasons) and UPSERTed by run_derive_allocator_equity_job (job_worker.py:6552).

From supabase/migrations/20260717233529 STEP 4/5: the enqueue fn + the exact `cron.schedule('derive-allocator-key-dailies','30 5 * * *', SELECT enqueue_derive_broker_dailies_for_allocator_keys();)` text for the runbook. The cron is live-UNSCHEDULED (v1.11 recovery); repo/live diverge intentionally.

Rollback executed at v1.11 close (STATE.md + RESEARCH §4 — reuse verbatim): delete pending/running jobs of kinds derive_broker_dailies + derive_allocator_equity; DELETE FROM allocator_equity_derived; cron.unschedule('derive-allocator-key-dailies').
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: E2-gate fixture pair — anchor verdict (pytest) + display flip wiring (vitest)</name>
  <files>analytics-service/tests/test_e2_ground_truth_harness.py, src/lib/queries.test.ts</files>
  <read_first>analytics-service/tests/test_e2_ground_truth_harness.py (existing coverage — extend, never duplicate); analytics-service/scripts/e2_allocator_ground_truth.py :90-241; the existing extractTrustworthyDerivedCurve/derivePhase07Fields tests (grep src/lib for equityCurveSource in *.test.ts — the derived-curve tests may live in a dedicated file; extend the file that already covers derivePhase07Fields); 123-RESEARCH.md §3 + Pitfall 4</read_first>
  <behavior>
    - pytest PASS fixture: hand-derived numbers (e.g. derived_terminal 10000.0, live 10150.0, tol 0.02 → drift +1.5%) → `within_same_day_tolerance` True. The expected drift is computed BY HAND in the test, never by calling the module to produce its own expectation (P115).
    - pytest FAIL-on-drift fixture: derived 10000.0, live 10500.0, tol 0.02 → drift +5% → `within_same_day_tolerance` False while `trustworthy` True (the two signals surface separately).
    - pytest FAIL-on-degradation fixture: zero drift but `trustworthy=False` → `within_same_day_tolerance` False (a blocking degradation can never be outvoted by a clean anchor).
    - vitest wiring PASS: `derivePhase07Fields(..., derivedEquityRow={payload: {is_trustworthy: true, curve: [valid dense points]}, computed_at})` → `equityCurveSource === "derived"`, `equityDailyPoints` maps the payload directly, `derivedCurveComputedAt` set.
    - vitest wiring FAIL: byte-identical curve with `is_trustworthy: false` → `equityCurveSource === "legacy"`, `equityDailyPoints` falls back to the snapshot render, `derivedCurveComputedAt` null. (Test the WIRING through derivePhase07Fields, not only the extract helper — the call-site-invokes-it rule.)
  </behavior>
  <action>
    Add only the fixtures that do not already exist (grep the two test surfaces first; RESEARCH says helpers are covered but the explicit pass/fail PAIR is the gap). Skip any that are already pinned verbatim and record that in the SUMMARY. Keep pytest fixtures on the PURE helpers (`compute_anchor_consistency`, `derive_terminal_equity` if a curve-level fixture is cheap) — no network, no env; the LIVE run stays founder-gated in Task 3. For vitest, reuse the existing derivePhase07Fields fixture scaffolding (apiKeys/equitySnapshots args) from the file you extend; run touched TS files with --no-file-parallelism locally (flake rule).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_e2_ground_truth_harness.py -q && cd .. && npx vitest run src/lib/queries.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - The three pytest verdict fixtures pass with HAND-derived expectations (no self-referential oracle — reviewer greps the test for calls back into compose/derive as the expectation source: none).
    - The vitest pair proves the derived↔legacy flip at the derivePhase07Fields level; deleting the `is_trustworthy !== true` guard in extractTrustworthyDerivedCurve would fail the FAIL fixture (neuter-proof).
    - No existing tests weakened or duplicated.
  </acceptance_criteria>
  <done>CI permanently pins: anchor drift/degradation → E2 verdict, and is_trustworthy → derived/legacy display, without any live dependency.</done>
</task>

<task type="auto">
  <name>Task 2: go-live + rollback runbook (the FLIPRETRY-04 document)</name>
  <files>docs/runbooks/flipretry-derived-equity-go-live.md</files>
  <read_first>docs/runbooks/railway-worker.md + docs/runbooks/deploy-rollback.md (house runbook style); 123-RESEARCH.md §2 cron subsection + §4 (healthz mechanics, idempotency proof, verbatim rollback); supabase/migrations/20260717233529 STEP 5 (exact cron SQL); the plan-02 SUMMARY (final env/role names)</read_first>
  <action>
    Write the founder runbook with these ordered sections (every step has a verify command and an abort path back to the rollback):

    1. Preconditions: plans 01+02 merged; migration applied on PROD by the auto-apply run (verify objects); Railway prod worker deployed at the merge commit (commitHash + /health — Railway silently skips deploys on red main).
    2. Deploy the dedicated backfill worker: second Railway service from the same repo, env WORKER_CLAIM_ROLE=backfill + the standard worker env (SUPABASE_SERVICE_KEY — the exact Railway var name); verify its /health and logs show it claims nothing while the queue has no backfill jobs.
    3. Cut the prod worker over: set WORKER_CLAIM_ROLE=interactive on the existing service; verify healthz 200 and that new-key connects still process (broker-dailies onboarding now rides the dedicated worker — from this step the backfill worker is PROD-CRITICAL for deribit/sfox key connects; say so in bold).
    4. Pilot enqueue (bound verification, research A1): enqueue ONE known-heavy key (deribit-inception or the bybit-19k key) via a single `enqueue_compute_job(p_kind:='derive_broker_dailies', p_api_key_id:=..., p_idempotency_key:='derive-dailies-<key>-<date>')`; watch (a) the dedicated worker's crawl duration vs the 300s BROKER_CRAWL_TIMEOUT_S bound — if a healthy heavy crawl exceeds it, raise the env bound BEFORE the full enqueue rather than letting every heavy key loop transient (the F5 failure mode); (b) the PROD worker's healthz stays 200/fresh the entire time — the FLIPRETRY-04 live proof.
    5. LIVE E2 gate: set E2_GROUND_TRUTH_* (read-only key) on Railway; run `railway ssh "cd /app && python -m scripts.e2_allocator_ground_truth ..."` with the pilot key's members/anchors; REQUIRE exit 0 (exit 3 SKIP is NOT a pass; a material divergence FAILS LOUD → rollback, investigate — never widen the tolerance to pass).
    6. Full enqueue: `SELECT enqueue_derive_broker_dailies_for_allocator_keys();` — safe to re-run (advisory lock + per-(key,UTC-date) idempotency + one-inflight index, pinned by supabase/tests/test_claim_kind_filter.sql); watch prod healthz + the dedicated worker burn-down.
    7. Cron re-schedule (LAST): the exact SQL from STEP 5 — `SELECT cron.schedule('derive-allocator-key-dailies','30 5 * * *', $$SELECT enqueue_derive_broker_dailies_for_allocator_keys();$$);`. State explicitly WHY this is a live founder op and NOT a repo migration (deviation from the phase-scope parenthetical, per the CONTEXT safety ordering "ONLY once 01/02/04 make it safe"): a committed migration auto-applies to PROD at merge, and if the Railway worker deploy is skipped (red-main silent-skip) the OLD unfiltered, unwrapped worker would claim the cron's jobs inline — recreating the v1.11 wedge verbatim. The cron may only exist once steps 2-6 are verified.
    8. ROLLBACK (verbatim v1.11 recovery — executable at any step): `DELETE FROM compute_jobs WHERE kind IN ('derive_broker_dailies','derive_allocator_equity') AND status IN ('pending','running');` then `DELETE FROM allocator_equity_derived;` (the TS flip degrades to legacy on empty — zero user impact) then `SELECT cron.unschedule('derive-allocator-key-dailies');` and, if needed, WORKER_CLAIM_ROLE back to "all" on prod. Result: derived path DORMANT on legacy.
    9. sFOX-F5 note: the sFOX active-account transactions crawl rides this worker for free (same derive_broker_dailies kind); an active sfox account that outruns even the dedicated worker's bound gets its bound raised via SFOX_CRAWL_TIMEOUT_S on the backfill service only.
  </action>
  <verify>
    <automated>bash -c 'f=docs/runbooks/flipretry-derived-equity-go-live.md; test -f "$f" && grep -q "cron.unschedule" "$f" && grep -q "DELETE FROM allocator_equity_derived" "$f" && grep -q "WORKER_CLAIM_ROLE" "$f" && grep -qi "exit 3" "$f"'</automated>
  </verify>
  <acceptance_criteria>
    - Every go-live step has a verify + abort; rollback is the verbatim v1.11 recovery and is reachable from any step.
    - The cron-as-live-op deviation is documented with the wedge-recreation rationale.
    - The E2 gate is stated as exit-0-required, SKIP-is-not-a-pass, tolerance-never-widened-to-pass.
    - The prod-critical-for-onboarding consequence of the interactive role is stated in bold.
  </acceptance_criteria>
  <done>A founder can execute go-live end-to-end from this document alone, and can roll back from any step to the dormant-legacy state.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: founder ops — dedicated-worker deploy, role cutover, pilot + LIVE E2, full enqueue, cron re-schedule</name>
  <what-built>Plans 01-02 landed the wait_for bounds, the kind-filtered claim, the role-aware workers, and the idempotent-enqueue proof; Task 2 wrote the runbook. The remaining legs have NO CLI path from this repo session: minting/setting the read-only E2_GROUND_TRUTH_* key, creating the second Railway service, flipping prod env, running the live E2 against a real exchange account, and executing prod SQL for the enqueue + cron. Per 123-CONTEXT these are FOUNDER-gated and must never be faked.</what-built>
  <how-to-verify>
    Follow docs/runbooks/flipretry-derived-equity-go-live.md steps 1-7 in order. Hard gates:
    1. Prod worker healthz stays 200/fresh THROUGHOUT the step-4 pilot and step-6 full backfill (FLIPRETRY-04 live proof).
    2. `scripts/e2_allocator_ground_truth.py` exits 0 on the pilot (exit 3 = SKIP = NOT a pass).
    3. Only after both: full enqueue, then `cron.schedule('derive-allocator-key-dailies','30 5 * * *', ...)`.
    4. Spot-check one allocator dashboard: `equityCurveSource` shows derived only for a backfilled trustworthy key; everything else renders legacy unchanged.
    If ANY gate fails: execute the runbook rollback (step 8) and report the failure — do not proceed.
  </how-to-verify>
  <action>PAUSE here. Present the runbook path (docs/runbooks/flipretry-derived-equity-go-live.md) and the gate list above to the founder; do not execute, simulate, or fake any live leg (E2 run, Railway deploy, prod SQL). The executor's only action is to surface this checkpoint and wait.</action>
  <verify>Founder-reported: E2 exit code 0, prod healthz fresh throughout pilot + backfill, cron scheduled, derived curve spot-checked on one allocator.</verify>
  <done>Founder replied "flip-live" with the E2 exit code and a healthz observation — or the checkpoint remains open (human_needed) and is recorded as such, never as passed.</done>
  <resume-signal>Type "flip-live" when all gates passed (with the E2 exit code + a healthz observation note), or describe which gate failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| live exchange → E2 harness | untrusted live equity read; credentials in env |
| DB payload → SSR | worker-written JSONB treated as untrusted (existing extract guard) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-08 | Info disclosure | E2 evidence output | mitigate | preserved assert_sanitized/sanitize_evidence contract (existing; fixtures must not print raw creds/USD in CI) |
| T-123-09 | Elevation | E2_GROUND_TRUTH_* key | transfer | founder provisions a READ-ONLY exchange key (runbook states it) |
| T-123-10 | Spoofing/integrity | unvalidated curve shown | mitigate | staged gate: full enqueue + cron only after E2 exit 0; TS extract guard already fails closed to legacy |
| T-123-11 | Tampering | none — no package installs | accept | no install tasks in this plan |
</threat_model>

<verification>
- pytest harness + vitest wiring fixtures green in CI.
- Runbook exists and passes its grep gates.
- Checkpoint resolved by the founder with the E2 exit code + healthz observation (never auto-approved).
</verification>

<success_criteria>
FLIPRETRY-03: the ground-truth gate is CI-pinned (independent oracle, P115) and the live leg is founder-executed per the staged gate — no derived curve shows before E2 passes. FLIPRETRY-04: healthz-fresh-during-backfill proven live, enqueue idempotency pinned, rollback documented verbatim and executable.
</success_criteria>

<output>
Create `.planning/phases/123-flipretry-derived-equity/123-03-SUMMARY.md` when done (after the founder checkpoint resolves; if the founder defers go-live, record the checkpoint as human_needed-open, never as passed).
</output>
