---
phase: 120-sfox-equity-reconstruction-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/ingestion/sfox.py
  - analytics-service/services/ingestion/adapter.py
  - analytics-service/services/ingestion/__init__.py
  - analytics-service/tests/test_ingestion_sfox.py
  - analytics-service/tests/test_boundary_literals_parity.py
  - analytics-service/tests/test_ingestion_protocol.py
autonomous: true
requirements: [SFOX-05]
must_haves:
  truths:
    - "get_adapter('sfox') resolves an SfoxAdapter (lazily, cached) and unknown sources are still rejected"
    - "SfoxAdapter.compute_metrics RAISES — a fill-based sfox metrics snapshot can never be produced (BYB-02 corruption class blocked)"
    - "The ingestion Source Literal and SUPPORTED_SOURCES admit 'sfox' TOGETHER — both parity tests green in the same commit (the 119 deferral resolved, never re-split)"
    - "SfoxAdapter.validate proves auth via a real read and reports read_only=True STRUCTURALLY (never a probed-scope claim)"
  artifacts:
    - path: "analytics-service/services/ingestion/sfox.py"
      provides: "SfoxAdapter — 5-method IngestionAdapter, compute_metrics/fetch_raw fail-loud, validate via SfoxClient.get_balances"
      min_lines: 60
    - path: "analytics-service/tests/test_ingestion_sfox.py"
      provides: "adapter unit tests incl. fail-loud pins and registration/caching"
      min_lines: 60
    - path: "analytics-service/services/ingestion/__init__.py"
      provides: "SUPPORTED_SOURCES + _FACTORIES admit sfox"
      contains: "_make_sfox_adapter"
  key_links:
    - from: "analytics-service/services/ingestion/__init__.py"
      to: "analytics-service/services/ingestion/sfox.py"
      via: "_FACTORIES['sfox'] lazy factory"
      pattern: "_make_sfox_adapter"
    - from: "analytics-service/services/ingestion/sfox.py"
      to: "analytics-service/services/sfox_client.py"
      via: "validate auth probe"
      pattern: "get_balances"
---

<objective>
Register sfox as an ingestion CAPABILITY: ship `SfoxAdapter` (compute_metrics fail-loud, mirroring
`DeribitAdapter`) and land `'sfox'` in the ingestion `Source` Literal + `SUPPORTED_SOURCES` +
`_FACTORIES` in ONE lockstep commit, flipping the two boundary tests that pinned sfox OUT until
Phase 120. This resolves the phase-119 deferral (`b82d4d79` pinned sfox out of `Source` precisely
because the factory did not exist) and is the registration half of the F2/F7 seam resolution
(`get_adapter("sfox")` now resolves; the worker/router halves are plan 120-03).

Purpose: SFOX-05 — the reconstructed sfox strategy can only exist once the ingestion registry
resolves sfox; the fail-loud `compute_metrics` is the load-bearing corruption guard that forces
all sfox returns through the broker-dailies ONE-path.
Output: `services/ingestion/sfox.py`, registry/Literal edits, flipped parity tests, new adapter suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/120-sfox-equity-reconstruction-backbone/120-CONTEXT.md
@.planning/phases/120-sfox-equity-reconstruction-backbone/120-RESEARCH.md
@analytics-service/services/ingestion/deribit.py
@analytics-service/services/ingestion/__init__.py
</context>

<interfaces>
<!-- Verified from codebase 2026-07-18. Executor uses these directly. -->

From services/ingestion/__init__.py (the Protocol the adapter must satisfy; @runtime_checkable, mypy --strict enforced):
```python
class IngestionAdapter(Protocol):
    async def validate(self, req: KeySubmissionRequest) -> ValidationResult: ...
    async def fetch_raw(self, creds_or_file: dict[str, object]) -> list[Trade]: ...
    def compute_metrics(self, trades: list[Trade]) -> MetricsSnapshot: ...
    def compute_fingerprint(self, trades: list[Trade], metrics: MetricsSnapshot) -> Fingerprint: ...
    async def reconstruct_positions(self, trades: list[Trade]) -> list[Position]: ...

SUPPORTED_SOURCES: tuple[str, ...] = ("okx", "binance", "bybit", "csv", "deribit")  # :98 — add "sfox"
_FACTORIES: dict[str, Callable[[], IngestionAdapter]]  # :151 — add "sfox": _make_sfox_adapter
```

From services/ingestion/adapter.py:40 (the Literal to widen; the stale comment block at :30-39 says sfox joins "in 120" — that is NOW):
```python
Source = Literal["okx", "binance", "bybit", "csv", "deribit"]  # add "sfox"
```

From services/sfox_client.py (118; Bearer-token, GET-only):
```python
class SfoxClient:  # SfoxClient(api_key, ...) — single Bearer token, empty api_secret is the Q1 contract
    async def get_balances(self) -> list[dict]      # bare array; the validate auth probe
    async def aclose(self) -> None                  # bounded, idempotent
class SfoxApiError(Exception)  # .status: 401/403 auth; 0 = shape/contract violation; 429/5xx transient
```

From services/exchange.py (119-02 hoisted constant, shared with the ccxt AUTH arm — reuse, do not restate the copy):
```python
AUTH_FAILED_DETAIL: str
```

Parity tests to flip (this plan) — verified line-accurate:
- tests/test_boundary_literals_parity.py:118 `test_source_literal_excludes_sfox_until_phase_120` — asserts "sfox" NOT in Source; FLIP to include.
- tests/test_ingestion_protocol.py:198 `test_literal_types` — hardcoded expected Source set; ADD "sfox".
- tests/test_ingestion_deribit.py:262 `test_source_literal_and_registry_agree` — pure set-equality `set(get_args(Source)) == set(SUPPORTED_SOURCES)`; needs NO edit and MUST stay green untouched (if it reds, the lockstep is broken).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SfoxAdapter — 5-method adapter, compute_metrics fail-loud (D: reconstruction-shape locked decision)</name>
  <files>analytics-service/services/ingestion/sfox.py, analytics-service/tests/test_ingestion_sfox.py</files>
  <read_first>
    - analytics-service/services/ingestion/deribit.py (all 139 lines — THE analog; especially compute_metrics :108 and the fingerprint/positions delegation)
    - analytics-service/services/ingestion/adapter.py:1-100 (KeySubmissionRequest/ValidationResult dataclasses)
    - analytics-service/routers/exchange.py — `_validate_sfox_key` (the 119-02 auth-probe + classification pattern to mirror)
    - analytics-service/services/sfox_client.py:240-370 (get_balances, SfoxApiError semantics, aclose)
    - .planning/phases/120-sfox-equity-reconstruction-backbone/120-RESEARCH.md §Architecture Patterns / Pattern 1
  </read_first>
  <behavior>
    - compute_metrics(anything) raises NotImplementedError whose message names the broker-dailies ONE-path (chain_linked_twr → derive_basis_series) — pinned by substring assert
    - fetch_raw(anything) raises NotImplementedError (fail-loud; no sync flow admits sfox — see action rationale) — pinned
    - validate: happy path (mocked SfoxClient.get_balances returns a list) → ValidationResult(valid=True, read_only=True); client aclose awaited on EVERY path (assert via mock)
    - validate: SfoxApiError(401) and (403) → ValidationResult(valid=False, error_code="AUTH_FAILED", human_message=AUTH_FAILED_DETAIL) — never a false-verified
    - validate: SfoxApiError(status 0 / 429 / 500) PROPAGATES untouched (transient/contract failures must never read as auth-failed OR as valid — the 119 F4 honesty rule)
    - compute_fingerprint([], metrics-sentinel) / reconstruct_positions([]) delegate to the shared impls and return without error (deribit precedent: only the RETURNS axis is guarded)
    - isinstance(SfoxAdapter(), IngestionAdapter) is True (runtime_checkable presence check)
  </behavior>
  <action>
    Create `services/ingestion/sfox.py::SfoxAdapter` (SOURCE = "sfox") mirroring DeribitAdapter's
    structure, per the locked reconstruction-shape decision (CONTEXT §Implementation Decisions):

    - `validate(req)`: construct `SfoxClient` from `req.context["api_key"]` (`.strip()` per the
      v1.11 credential-trim chokepoint convention; empty `api_secret` is the Q1 single-Bearer
      contract — never require it). Auth-prove via `await client.get_balances()`. Success →
      `ValidationResult(valid=True, read_only=True)` with a comment stating read-only is
      STRUCTURAL (GET-only adapter, no scope endpoint — the 119 A1 posture), never a probed scope.
      `SfoxApiError` with status 401/403 → `ValidationResult(valid=False, error_code="AUTH_FAILED",
      human_message=AUTH_FAILED_DETAIL)` importing the hoisted constant from `services.exchange`
      (zero-TS-edit KEY_AUTH_FAILED mapping, 119-02). ANY other SfoxApiError propagates untouched.
      `await client.aclose()` in a `finally`.
    - `compute_metrics`: RAISE NotImplementedError, message mirroring deribit.py:108 verbatim in
      structure: sfox returns come from the balance-history usd_value series via the
      broker-dailies ONE-path (chain_linked_twr → derive_basis_series), never fill metrics; a
      fill-based snapshot would be the BYB-02 silently-empty/wrong-track-record class.
    - `fetch_raw`: RAISE NotImplementedError (fail-loud). Rationale in the docstring: H-11 never
      routes sfox to the synchronous pipeline (onboard/resync are long-fetch → the worker
      broker-dailies branch), so there is NO consumer; a bespoke sfox→Trade normalization with no
      consumer would be unverifiable invented mapping — and reusing the ccxt `_normalize_trade` on
      sFOX's non-ccxt row shape would silently mis-map. A future teaser admit must implement an
      honest normalization FIRST (this raise is the tripwire). This is Claude's-discretion per
      RESEARCH Pattern 1 ("the LOAD-BEARING requirement is only that compute_metrics fails loud").
    - `compute_fingerprint` / `reconstruct_positions`: delegate to `compute_fingerprint_v1` /
      `EquityCurveBuilder` via lazy imports, exactly like deribit.py:124-139 (execution-detail
      axis, unguarded).
    - Full type annotations; `mypy --strict services/ingestion/` must stay clean (the MC-3 gate).

    Tests in `tests/test_ingestion_sfox.py` (mock/patch `SfoxClient` at the sfox-module import
    seam; NO network). Write the behavior list above as failing tests first (RED commit), then
    implement (GREEN commit).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_ingestion_sfox.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_ingestion_sfox.py exits 0 with ≥8 tests
    - grep -v '^\s*#' analytics-service/services/ingestion/sfox.py | grep -c "NotImplementedError" returns ≥2 (compute_metrics + fetch_raw fail-loud)
    - grep -v '^\s*#' analytics-service/services/ingestion/sfox.py | grep -c "EquityCurveBuilder\|compute_fingerprint_v1" returns 2 (delegation, not re-implementation)
    - cd analytics-service && mypy --strict services/ingestion/ exits 0 (if the Makefile lint target covers it, run that)
  </acceptance_criteria>
  <done>SfoxAdapter exists, satisfies the Protocol, fails loud on both metrics axes, validates via a real read with honest 401/403 vs transient classification; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Lockstep registration — Source Literal + SUPPORTED_SOURCES + _FACTORIES + parity-test flips (D: registration locked decision; resolves the 119 deferral)</name>
  <files>analytics-service/services/ingestion/adapter.py, analytics-service/services/ingestion/__init__.py, analytics-service/tests/test_boundary_literals_parity.py, analytics-service/tests/test_ingestion_protocol.py, analytics-service/tests/test_ingestion_sfox.py</files>
  <read_first>
    - analytics-service/services/ingestion/adapter.py:20-45 (the Literal + the stale phase-119 comment block that must be rewritten)
    - analytics-service/services/ingestion/__init__.py:87-176 (registry, factories, get_adapter docstring)
    - analytics-service/tests/test_boundary_literals_parity.py:118-129 (the exclusion test to flip)
    - analytics-service/tests/test_ingestion_protocol.py:198-224 (test_literal_types hardcoded set)
    - .planning/phases/119-sfox-read-adapter-key-validation/deferred-items.md (the RESOLVED Source-Literal record — why lockstep is mandatory)
  </read_first>
  <action>
    In ONE commit (the lockstep IS the point — the 119 regression was exactly a
    Literal-without-registry split):
    1. adapter.py:40 — add "sfox" to the `Source` Literal. Rewrite the phase-119 comment block
       above it (:30-39): it currently promises sfox joins in Phase 120 — replace with a landed
       note mirroring the deribit Phase-70 style (ingestion capability shipped; sfox returns flow
       through the broker-dailies ONE-path via the balance-history usd_value series, never
       process_key fill metrics; the deribit precedent test
       `test_adapter_comment_reflects_phase_70_ships` pins against exactly this kind of stale note).
    2. __init__.py — add "sfox" to `SUPPORTED_SOURCES` (:98), add `_make_sfox_adapter()` (lazy
       import of SfoxAdapter, matching `_make_deribit_adapter`) and `_FACTORIES["sfox"]` (:151);
       update the `get_adapter` docstring allowlist and the registry comment (note: onboarding UI
       is Phase 122; live parity egress is Phase 121).
    3. tests/test_boundary_literals_parity.py:118 — flip `test_source_literal_excludes_sfox_until_phase_120`
       to `test_source_literal_includes_sfox` asserting "sfox" IN get_args(Source), with the
       rationale comment updated (Phase 120 shipped the factory; parity with SUPPORTED_SOURCES holds).
    4. tests/test_ingestion_protocol.py:211 — add "sfox" to the hardcoded expected Source set.
    5. tests/test_ingestion_sfox.py — add registration tests: get_adapter("sfox") resolves an
       SfoxAdapter AND caches (second call `is` first); get_adapter("kraken") still raises
       ValueError.
    Do NOT edit tests/test_ingestion_deribit.py — `test_source_literal_and_registry_agree` is
    set-equality over both sides and MUST pass untouched; if it fails, the lockstep is broken —
    fix the registration, never the test.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_boundary_literals_parity.py tests/test_ingestion_protocol.py tests/test_ingestion_deribit.py tests/test_ingestion_sfox.py tests/test_sfox_validate.py tests/test_sfox_read.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The verify selection exits 0, INCLUDING test_source_literal_and_registry_agree with zero edits to test_ingestion_deribit.py (confirm via git diff --name-only)
    - grep -v '^\s*#' analytics-service/services/ingestion/adapter.py | grep -c '"sfox"' returns ≥1 AND the stale "Phase 119 … does NOT add 'sfox'" wording is gone (grep -ci "does not add 'sfox'" services/ingestion/adapter.py == 0)
    - python -c "from services.ingestion import get_adapter; a=get_adapter('sfox'); assert type(a).__name__=='SfoxAdapter'" exits 0 (run from analytics-service with .venv)
  </acceptance_criteria>
  <done>Source Literal, SUPPORTED_SOURCES and _FACTORIES admit sfox together; both 119 parity pins flipped/green; get_adapter('sfox') resolves; 119 deferral closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → sFOX API | Bearer credential leaves the process; responses are untrusted JSON |
| registry dispatch | `get_adapter(source)` — an unregistered/unknown source must keep failing loud |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01 | Tampering (economic) | SfoxAdapter.compute_metrics | mitigate | fail-loud NotImplementedError; test pins the raise + message (BYB-02 class blocked) |
| T-120-02 | Spoofing | SfoxAdapter.validate | mitigate | auth proven by a real GET read; 401/403 → AUTH_FAILED, transient propagates (never false-verified) |
| T-120-03 | Info disclosure | validate error paths | mitigate | SfoxApiError messages already redact the bearer (118 scrub); human_message is the shared AUTH_FAILED_DETAIL constant, never the raw error |
| T-120-04 | Elevation | write-capable object smuggling | mitigate | adapter constructs its own GET-only SfoxClient; no caller-supplied client accepted |
| T-120-SC | Tampering | package installs | accept | zero new packages this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- Registration parity: `pytest tests/test_ingestion_deribit.py::test_source_literal_and_registry_agree tests/test_ingestion_protocol.py::test_literal_types -q` green.
- Fail-loud: `pytest tests/test_ingestion_sfox.py -q` green.
- No regression: `pytest tests/test_sfox_validate.py tests/test_sfox_read.py -q` green (119 surfaces untouched).
</verification>

<success_criteria>
- get_adapter("sfox") resolves + caches; compute_metrics and fetch_raw raise; validate classifies honestly.
- Both former sfox-exclusion pins flipped in the SAME commit as the registration; set-parity test green with zero edits.
- mypy --strict services/ingestion/ clean.
</success_criteria>

<output>
Create `.planning/phases/120-sfox-equity-reconstruction-backbone/120-01-SUMMARY.md` when done.
</output>
