---
phase: 120-sfox-equity-reconstruction-backbone
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/broker_dailies.py
  - analytics-service/services/sfox_read.py
  - analytics-service/tests/test_sfox_reconstruct.py
autonomous: true
requirements: [SFOX-05]
must_haves:
  truths:
    - "A known NAV+flows fixture produces cashflow-neutral TWR returns matching a HAND-DERIVED oracle (P115) — a deposit day books only its real PnL, never the deposit"
    - "An interior missing balance-history day yields NaN breaks on that day AND the next — never a bridged multi-day return, never a fabricated 0.0"
    - "DQ-01 guards (dust/negative/flow-dominated/pnl-dominated NAV) fire for sfox inputs and surface in meta — inherited from chain_linked_twr, not reimplemented"
    - "A truncated/under-fetched crawl RAISES a typed error — a complete-but-short series can never be returned silently"
    - "A flow row that cannot be USD-valued from its own fields RAISES — never guessed, never dropped"
  artifacts:
    - path: "analytics-service/services/broker_dailies.py"
      provides: "combine_sfox_balance_history — usd_value+flows → cashflow-neutral TWR via chain_linked_twr, sibling of combine_native_ledger"
      contains: "def combine_sfox_balance_history"
    - path: "analytics-service/services/sfox_read.py"
      provides: "crawl_sfox_balance_history / crawl_sfox_transactions (bounded, typed-truncation) + sfox_flows_by_day"
      contains: "def crawl_sfox_balance_history"
    - path: "analytics-service/tests/test_sfox_reconstruct.py"
      provides: "P115 independent-oracle unit suite (Wave-0 gap closed)"
      min_lines: 150
  key_links:
    - from: "analytics-service/services/broker_dailies.py"
      to: "analytics-service/services/nav_twr.py"
      via: "combine_sfox_balance_history → chain_linked_twr (REUSE, never a bespoke TWR loop)"
      pattern: "chain_linked_twr\\("
    - from: "analytics-service/services/sfox_read.py"
      to: "analytics-service/services/sfox_client.py"
      via: "GET-only crawl composition"
      pattern: "get_balance_history|get_transactions"
---

<objective>
Build the sfox reconstruction math on the EXISTING primitives: `combine_sfox_balance_history`
(sibling of `combine_native_ledger`) turning sFOX's daily `usd_value` NAV series + typed
deposit/withdraw flows into a cashflow-neutral daily TWR series via the EXISTING
`nav_twr.chain_linked_twr` (flow-in-numerator, full DQ-01 guard set), plus the bounded crawl +
flow-extraction helpers in `sfox_read.py`. Proven against a hand-derived independent oracle (P115).

Purpose: SFOX-05 — the returns math whose output plugs into the ONE backbone; deposits must never
book as return (perf-curve ≠ equity-curve, the v1.11 carry-forward).
Output: two service functions + helpers + `tests/test_sfox_reconstruct.py` (a VALIDATION.md Wave-0 gap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-sfox-equity-reconstruction-backbone/120-CONTEXT.md
@.planning/phases/120-sfox-equity-reconstruction-backbone/120-RESEARCH.md
@analytics-service/services/broker_dailies.py
@analytics-service/services/sfox_read.py
</context>

<interfaces>
<!-- Verified from codebase 2026-07-18. -->

From services/nav_twr.py:351 (REUSE — the TWR engine; do NOT fork):
```python
def chain_linked_twr(
    nav: pd.Series, daily_pnl: pd.Series, flows_by_day: pd.Series, *, prev0: float | None = None,
) -> tuple[pd.Series, dict[str, bool]]:
    # r_t = (NAV_t − NAV_{t-1} − F_t) / NAV_{t-1}; flow in the NUMERATOR.
    # Guards: negative_nav_guard / dust_nav_guard (DUST_NAV_FLOOR) / flow_dominated_guard
    # (_guard_denominator:437) + pnl_dominated_guard at the call site. Broken day → NaN, flag=True.
    # prev0 set ⇒ day-0 prev is the supplied inception capital (additive kwarg, App A #2).
```
`_build_nav_meta(flags)` at nav_twr.py:529 shapes the meta dict (carries the DQ-01 flag keys).

From services/broker_dailies.py (the siblings whose (returns, meta) shape must be matched exactly):
```python
def gap_fill_daily_returns(returns: pd.Series) -> pd.Series  # :123 — reindex [first,last] daily
    # freq="D").as_unit("us"), fill_value=0.0 for MISSING INDEX positions ONLY (existing NaN survives)
def combine_native_ledger(...) -> tuple[pd.Series, dict[str, Any]]  # :173 — the deribit sibling
```

From services/sfox_client.py (118; single-page reads, cursors exposed):
```python
async def get_balance_history(self, start_date_ms: int, end_date_ms: int | None = None,
                              interval: int = 86400) -> list[dict]   # :313 {timestamp, usd_value} rows
async def get_transactions(self, from_ms=None, to_ms=None, limit=250, after=None,
                           offset=None, types=None) -> list[dict]    # :268 limit max 1000; 1-req/10s rate gate
```
sFOX transactions row (118-RESEARCH, docs-cited): `{id, order_id, trade_id, day, action, currency,
amount, net_proceeds, price, fees, status, account_balance, symbol, timestamp}`; `action` ∈
charge/deposit/withdraw/credit/buy/sell.

From services/external_flows.py:45: `ExternalFlow(utc_day_iso: str, usd_signed: float)` (NamedTuple)
— the DQ-02 evidence shape plan 120-03 threads downstream; produce it here alongside the Series.

From services/sfox_read.py: `read_sfox_account(client)` must stay BYTE-UNTOUCHED — tests/test_sfox_read.py
pins its single-page/no-cursor contract AND runs a write-surface grep gate over this module's source
(docstrings+comments stripped): every new function must remain a GET-only composition.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: combine_sfox_balance_history — cashflow-neutral TWR from usd_value + flows (D: cashflow-separation + degenerate locked decisions)</name>
  <files>analytics-service/services/broker_dailies.py, analytics-service/tests/test_sfox_reconstruct.py</files>
  <read_first>
    - analytics-service/services/broker_dailies.py:123-226 (gap_fill_daily_returns + combine_native_ledger — the sibling contract)
    - analytics-service/services/nav_twr.py:351-470 (chain_linked_twr + _guard_denominator) and :529 (_build_nav_meta)
    - analytics-service/services/basis_series.py:181-250 (derive_basis_series contract — what downstream expects; DO NOT call it here)
    - .planning/phases/120-sfox-equity-reconstruction-backbone/120-RESEARCH.md §The sFOX Mirror + §Pattern 2 + Open Question 1 (day-0/A3)
  </read_first>
  <behavior>
    - HAND-DERIVED oracle (P115 — arithmetic computed BY HAND in test comments, never by calling the module or nav_twr as its own oracle):
      NAV = [1000.0, 1010.0, 1515.0, 1500.15] on 4 consecutive days, +500.0 deposit on day 2 →
      expected returns ≈ [day0 = anchor (no return), 0.01, (1515−1010−500)/1010 = 0.0049504950495…, (1500.15−1515)/1515 = −0.0098019801980…].
      Assert to ~1e-12. The deposit day books ~0.495%, NOT ~50% — additionally assert the output on day 2 differs materially from usd_value.pct_change() (the Pitfall-1 anti-pin).
    - Withdrawal symmetric case (−300 flow): return reflects only real PnL.
    - Day-0 convention (A3, [ASSUMED]): prev0 = first OBSERVED usd_value → day-0 emits no return (anchor day); returns begin day 1. Test docstring flags: convention resolves empirically in the SFOX-06 founder evidence run — if the live run contradicts, amend HERE.
    - Interior missing NAV day (5-day range, day 2 absent): day 2 AND day 3 are ABSENT/NaN-broken in the output — a bridged 2-day return must never appear on day 3, and day 2 must never be fabricated as 0.0. Day 4 (consecutive observed pair 3→4) IS computed.
    - Guards inherited: prev NAV below DUST_NAV_FLOOR → that day breaks + dust_nav_guard in meta; |flow| ≥ FLOW_DOM_RATIO×prev → flow_dominated_guard; negative prev → negative_nav_guard. Assert flags surface through the returned meta dict.
    - Shape parity with combine_native_ledger: float64 Series, ascending daily DatetimeIndex unit [us], plain dict meta.
    - Degenerate: empty NAV → empty Series (honest; the <2-finite gate lives downstream in derive_basis_series); single-point NAV → no computable return (empty/all-NaN), never an invented row.
    - Non-finite usd_value point (NaN/Inf in the feed) → that day breaks, never propagates a fabricated number.
  </behavior>
  <action>
    RED first: write the behavior suite in tests/test_sfox_reconstruct.py (commit
    "test(120-02): add failing independent-oracle tests for combine_sfox_balance_history"), then
    implement (commit "feat(120-02): …"), refactor if needed.

    Implement `combine_sfox_balance_history(usd_value: pd.Series, flows_by_day: pd.Series) ->
    tuple[pd.Series, dict[str, Any]]` in services/broker_dailies.py, directly below
    combine_native_ledger (:226), as its sfox sibling:
    - Sort + coerce the NAV index to an ascending daily DatetimeIndex unit [us] (match the
      gap_fill idiom at :134 — pin the unit, #593 pandas-3.0 lesson).
    - Reindex the NAV to EVERY calendar day in [first, last] WITHOUT value-filling (missing
      observation → NaN). Rationale comment required: an UNOBSERVED sfox NAV day is unknown, not
      flat — 0.0-filling the NAV would fabricate data; contrast deribit where ledger-completeness
      makes an absent day genuinely flat. NaN NAV propagates through chain_linked_twr arithmetic
      as a NaN return on that day AND the following day (NaN prev) — verify in tests; if any
      guard/coercion in nav_twr chokes on NaN NAV instead of propagating, pre-mask non-finite NAV
      days to explicit breaks in THIS function — never patch nav_twr (it has byte-identity
      consumers).
    - Call chain_linked_twr(nav=reindexed_nav, daily_pnl=implied pnl (nav.diff() aligned,
      fillna 0.0 — only iloc[0] would matter and prev0 overrides it; comment this),
      flows_by_day=flows aligned to the same index, prev0=float(first OBSERVED usd_value)).
    - gap_fill_daily_returns(returns) to keep the sibling contract (index already dense → fills
      nothing; existing NaN breaks survive — comment why this is a shape no-op, not a bridge).
    - meta = dict(_build_nav_meta(flags)) so DQ-01 flag keys ride to the DQ pipeline exactly as
      the siblings' meta does.
    - PROHIBITED in this function (ONE-path): compute_all_metrics, derive_basis_series,
      reconstruct_native_nav_and_twr, reconstruct_nav, any bespoke r_t loop. The backbone call
      stays at the job_worker seam (plan 120-03).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_sfox_reconstruct.py -q -k "combine or oracle or guard or degenerate or gap"</automated>
  </verify>
  <acceptance_criteria>
    - RED commit exists before the GREEN commit (git log shows test(120-02) then feat(120-02))
    - pytest selection exits 0; the hand-derived fixture asserts numeric values written literally in the test (grep the test file for "0.004950" — the oracle is not computed by the impl)
    - grep -v '^\s*#' analytics-service/services/broker_dailies.py | grep -c "chain_linked_twr(" returns exactly 1 (the reuse call; no bespoke TWR loop)
    - grep -v '^\s*#' analytics-service/services/broker_dailies.py | grep -c "compute_all_metrics\|derive_basis_series" returns 0
  </acceptance_criteria>
  <done>combine_sfox_balance_history returns (returns, meta) in the sibling shape; deposit-neutrality, gap honesty, guard inheritance and degenerate honesty all pinned by independent-oracle tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounded crawls + typed-flow extraction in sfox_read.py (Claude's-discretion crawl, CONTEXT-mandated wait_for readiness)</name>
  <files>analytics-service/services/sfox_read.py, analytics-service/tests/test_sfox_reconstruct.py</files>
  <read_first>
    - analytics-service/services/sfox_read.py (all 59 lines — read_sfox_account stays untouched; mirror its isinstance boundary guard + fail-loud doctrine)
    - analytics-service/services/sfox_client.py:268-345 (get_transactions params/cursor, get_balance_history window, the 1-req/10s transactions rate gate at _rate_gate)
    - analytics-service/tests/test_sfox_read.py (the write-surface grep gate + single-page pins that must stay green)
    - .planning/phases/120-sfox-equity-reconstruction-backbone/120-RESEARCH.md §Crawl Safety + Pitfall 4 + Assumptions A1
    - analytics-service/services/external_flows.py:1-100 (ExternalFlow shape + validate_flow_shape)
  </read_first>
  <behavior>
    - crawl_sfox_transactions: mocked client returning 3 cursor pages → all rows returned in order, cursor (`after` id) followed to exhaustion, requests SERIAL; page/request budget exhausted before exhaustion → typed SfoxCrawlTruncatedError (never a silent partial)
    - crawl_sfox_balance_history: full requested window returned → rows; response whose latest point stops materially short of the requested recent edge → SfoxCrawlTruncatedError (Pitfall 4: a truncation must never render as a complete-but-short series); earliest returned point AFTER the requested start is NOT an error (A1: docs-silent depth — the earliest point is the empirical inception) and is surfaced to the caller
    - sfox_flows_by_day: deposit +amount, withdraw −amount, credit +, charge − (document the action→sign map in the docstring); same-UTC-day flows aggregate; buy/sell rows EXCLUDED (internal rotations, not external flows); returns BOTH the pd.Series (daily index, signed USD) and the list[ExternalFlow] evidence rows
    - a deposit/withdraw row whose USD value is not derivable from its OWN fields (non-USD currency with no usable USD field) → typed error raised (never guessed/dropped — a mis-valued flow silently corrupts the TWR)
    - non-SfoxClient object passed to either crawl → TypeError before any read (the read_sfox_account boundary-guard precedent)
    - empty account → honest empties everywhere
  </behavior>
  <action>
    RED then GREEN, same commit discipline as Task 1. Add to services/sfox_read.py, leaving
    `read_sfox_account` byte-untouched:
    1. `SfoxCrawlTruncatedError(Exception)` — the typed truncation signal (module-level; plan
       120-03 dispositions it permanent — the assert_ledger_complete analog).
    2. `async def crawl_sfox_balance_history(client, start_date_ms, end_date_ms=None)` — windowed
       pulls of client.get_balance_history(interval=86400), chunking the window when a response
       is capped (detect via the returned span vs requested), under a module-constant hard request
       budget (`_SFOX_CRAWL_MAX_REQUESTS`, pick ~50 with a rationale comment). Recent-edge
       shortfall or budget exhaustion → raise SfoxCrawlTruncatedError. Return the rows plus the
       observed earliest timestamp (empirical inception, A1).
    3. `async def crawl_sfox_transactions(client, from_ms, to_ms=None)` — cursor crawl of
       get_transactions(limit capped at the client's _TRANSACTIONS_MAX_LIMIT constant — read it,
       never hardcode 1000), following the `after` id cursor to exhaustion, SERIAL awaits only (the client's
       1-req/10s transactions rate gate makes parallel fanning a 429 trap — 118 pitfall; comment
       it), same hard budget + typed truncation error.
    4. `def sfox_flows_by_day(transactions) -> tuple[pd.Series, list[ExternalFlow]]` — pure
       function building the signed USD daily flow Series + the ExternalFlow evidence rows from
       typed rows per the behavior spec; fail-loud typed error on an unvaluable flow (docstring:
       LedgerValuationError posture — evidence first, never a guess; the founder ground-truth run
       is where a non-USD-flow convention would be resolved).
    Both crawls: no asyncio.wait_for INSIDE (the hard bound wraps at the derive_broker_dailies
    call seam in plan 120-03, where deribit's budget lives — comment the seam). All functions are
    GET-only compositions and must keep the module's write-surface grep gate green.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_sfox_reconstruct.py tests/test_sfox_read.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_sfox_read.py exits 0 UNCHANGED (git diff shows zero edits to that file's pins beyond none)
    - pytest tests/test_sfox_reconstruct.py exits 0 with crawl + flows tests present (≥8 new tests)
    - grep -v '^\s*#' analytics-service/services/sfox_read.py | grep -c "wait_for" returns 0 (the bound lives at the worker seam)
    - grep -v '^\s*#' analytics-service/services/sfox_read.py | grep -cE "post|put|delete|create_order|withdraw\(" returns 0 (GET-only preserved)
  </acceptance_criteria>
  <done>Bounded, typed-truncation crawls + fail-loud flow extraction exist; read_sfox_account and its 119 pins untouched; sfox subset green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → sFOX API | untrusted JSON rows become money-math inputs |
| crawl completeness | a partial read must never become a "complete" series |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-05 | Tampering (economic) | combine_sfox_balance_history | mitigate | flow-in-numerator TWR via chain_linked_twr; deposit-day anti-pin test vs pct_change |
| T-120-06 | Tampering (integrity) | crawls | mitigate | SfoxCrawlTruncatedError on budget/recent-edge shortfall; never silent-partial |
| T-120-07 | Tampering (fabrication) | missing NAV days | mitigate | NaN breaks, never 0.0-filled NAV; pinned by the interior-gap test |
| T-120-08 | DoS (self-inflicted) | transactions crawl | mitigate | serial awaits + hard request budget; wait_for bound added at the worker seam (120-03) |
| T-120-09 | Info disclosure | crawl errors | mitigate | SfoxApiError messages pre-scrubbed by the 118 client; new errors carry counts/ids only, never credentials |
</threat_model>

<verification>
- `pytest tests/test_sfox_reconstruct.py tests/test_sfox_read.py -q` green.
- Full analytics suite at plan close: `cd analytics-service && .venv/bin/python -m pytest -q` — zero new failures.
</verification>

<success_criteria>
- The hand-derived oracle fixture passes to 1e-12; deposit day never books the deposit.
- Interior gaps break honestly; guards inherited; truncation and unvaluable flows raise typed.
- No bespoke TWR/derive math anywhere in the new code (grep gates).
</success_criteria>

<output>
Create `.planning/phases/120-sfox-equity-reconstruction-backbone/120-02-SUMMARY.md` when done.
</output>
