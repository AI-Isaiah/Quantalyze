---
phase: 120-sfox-equity-reconstruction-backbone
plan: 04
type: execute
wave: 2
depends_on: [120-02]
files_modified:
  - analytics-service/scripts/sfox_ground_truth.py
  - analytics-service/tests/test_sfox_ground_truth.py
  - analytics-service/tests/test_sfox_ground_truth_live.py
autonomous: false
requirements: [SFOX-06]
user_setup:
  - service: sfox
    why: "LIVE ground-truth parity run (SFOX-06 live leg) needs a real read-only sFOX API key; an IP-whitelisted key additionally needs the Phase-121 static egress"
    env_vars:
      - name: SFOX_GROUND_TRUTH_KEY
        source: "sFOX account → API settings → read-only token (founder-held; never committed)"
must_haves:
  truths:
    - "The parity oracle is economically INDEPENDENT: it reconstructs equity ONLY from transactions' running account_balance + typed cashflows and never reads usd_value/balance-history in any form (P115)"
    - "A materially divergent input RAISES with a nonzero exit — the wrong curve is never displayed"
    - "A tampered fixture (inflated usd_value day / dropped deposit) is PROVEN to fail — fail-loud demonstrated, not assumed"
    - "All emitted evidence passes assert_sanitized; the Bearer token can never reach stdout/stderr"
    - "The A2 (account_balance semantics) and A3 (day-0 convention) unknowns are SURFACED as reported residuals in the evidence — never silently guessed"
    - "The LIVE prod-key run is founder-gated (121 egress): skipIf-carried in CI, recorded human_needed — never faked"
  artifacts:
    - path: "analytics-service/scripts/sfox_ground_truth.py"
      provides: "committed parity harness mirroring deribit_ground_truth.py (env creds, sanitize, exit codes 0/1/2/3)"
      min_lines: 200
    - path: "analytics-service/tests/test_sfox_ground_truth.py"
      provides: "fixture parity suite incl. tamper-detection and oracle-independence pins (Wave-0 gap closed)"
      min_lines: 100
    - path: "analytics-service/tests/test_sfox_ground_truth_live.py"
      provides: "skipIf(no key) live parity leg (skip ≠ pass, documented)"
  key_links:
    - from: "analytics-service/scripts/sfox_ground_truth.py"
      to: "analytics-service/services/broker_dailies.py"
      via: "series-under-test = combine_sfox_balance_history output re-cumulated"
      pattern: "combine_sfox_balance_history"
    - from: "analytics-service/scripts/sfox_ground_truth.py"
      to: "analytics-service/scripts/deribit_ground_truth.py"
      via: "REUSED sanitize_evidence / assert_sanitized / _redact_secret_values"
      pattern: "from scripts.deribit_ground_truth import"
---

<objective>
The SFOX-06 parity harness: `scripts/sfox_ground_truth.py` mirroring `deribit_ground_truth.py`,
validating the reconstructed sfox curve (the `combine_sfox_balance_history` output) against a
P115-INDEPENDENT economic oracle built solely from the transactions ledger (running
`account_balance` anchors + typed cashflows). Material divergence fails loud. A fixture parity
test carries CI; the live prod-key run is founder-gated on Phase-121 egress (human_needed).

Purpose: SFOX-06 — the api_verified claim is only as strong as the ground-truth check behind it;
CSV is fabricatable, a live-API-anchored parity is not.
Output: the harness + fixture/live tests + the founder runbook checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-sfox-equity-reconstruction-backbone/120-CONTEXT.md
@.planning/phases/120-sfox-equity-reconstruction-backbone/120-RESEARCH.md
@.planning/phases/120-sfox-equity-reconstruction-backbone/120-02-SUMMARY.md
@analytics-service/scripts/deribit_ground_truth.py
</context>

<interfaces>
<!-- Verified from codebase 2026-07-18. -->

From scripts/deribit_ground_truth.py (the structural template — REUSE, do not re-implement):
```python
def _redact_secret_values(text: str, *secrets: str | None) -> str   # :63
def sanitize_evidence(obj: Any) -> Any                              # :359 — whitelist re-walk
def assert_sanitized(obj: Any, _path: str = "") -> None             # :390 — RAISES on unmasked email/token/deny-key
class ScopeViolationError(RuntimeError)                             # :430 — exit code 2 semantics
def main(argv) -> int                                               # :971 — exit 0 ok / 2 scope / 3 missing creds / 1 other (scrubbed)
```
Pattern: fully-paginated reads of whitelisted fields → ONE sanitized JSON to stdout;
`clean = sanitize_evidence(evidence); assert_sanitized(clean)` before print (:1027-1028).

From plan 120-02:
```python
combine_sfox_balance_history(usd_value, flows_by_day) -> (returns, meta)   # the series under test
crawl_sfox_balance_history / crawl_sfox_transactions / sfox_flows_by_day   # bounded reads
```

sFOX transactions row: `{id, order_id, trade_id, day, action, currency, amount, net_proceeds,
price, fees, status, account_balance, symbol, timestamp}` — `account_balance` is the RUNNING
balance sFOX computes independently of `balance/history`'s `usd_value` (assumption A2 — the
harness must PROBE this, not assume it).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: scripts/sfox_ground_truth.py — the P115-independent parity harness (D: parity locked decision)</name>
  <files>analytics-service/scripts/sfox_ground_truth.py</files>
  <read_first>
    - analytics-service/scripts/deribit_ground_truth.py:1-120 (module doctrine, redaction), :345-430 (deny-keys, sanitize_evidence, assert_sanitized, ScopeViolationError), :682-760 (run() orchestration head), :971-1036 (main + exit codes)
    - analytics-service/services/sfox_read.py (crawls + flows, plan 120-02)
    - analytics-service/services/broker_dailies.py::combine_sfox_balance_history
    - .planning/phases/120-sfox-equity-reconstruction-backbone/120-RESEARCH.md §Ground-Truth Parity + Assumptions A2/A3 + Open Questions
  </read_first>
  <action>
    Create the committed one-off `python -m scripts.sfox_ground_truth`, mirroring the deribit
    harness structure:

    - Credentials: env ONLY (`SFOX_GROUND_TRUTH_KEY`); missing → exit 3. Never a tracked file,
      never an argv secret. IMPORT the sanitization primitives from scripts.deribit_ground_truth
      (sanitize_evidence, assert_sanitized, _redact_secret_values) — do NOT re-implement; if an
      sFOX-specific deny-key is needed, extend via composition in this module.
    - Read-only assertion BEFORE any fetch: the structural guarantee (isinstance SfoxClient +
      the GET-only premise); a violation of the premise exits 2 (ScopeViolationError semantics).
    - Reads: bounded crawls (far-past start) of balance-history + transactions via the plan-02
      crawl functions. Serial, rate-gate-respecting.
    - SERIES UNDER TEST: `combine_sfox_balance_history(usd_value, flows)` returns, re-cumulated
      into an equity path anchored at the first observed usd_value.
    - INDEPENDENT ORACLE (the load-bearing P115 seam): a function, e.g.
      `reconstruct_equity_from_transactions(transactions) -> oracle path`, whose ONLY input is
      the transactions row list — running `account_balance` anchors + typed
      deposit/withdraw/credit/charge cashflows (+ buy/sell as internal rotations). Its signature
      and body must never receive or read usd_value / balance-history data — enforce BY
      CONSTRUCTION (module docstring states the invariant; the fixture test pins it).
    - CHECKS: (1) anchor-consistency at each cashflow event between the usd_value-side equity and
      the account_balance-side value, under a module-constant materiality threshold (relative +
      absolute floor — document the chosen values and why); (2) cumulative reconciliation across
      the full window; (3) A2 PROBE — compute residuals under BOTH interpretations of
      account_balance (USD cash-only running balance vs total portfolio MTM) and emit both in the
      evidence: the founder run RESOLVES A2 empirically — the harness must never silently pick
      one; (4) A3 PROBE — emit the day-0/inception residual (first observed usd_value vs the
      transactions-implied inception capital) so the prev0 convention is confirmed or refuted by
      evidence.
    - DISPOSITION: material divergence under the best-supported interpretation → RAISE → exit 1
      (the wrong curve is never displayed). A2/A3 interpretation-ambiguity where at least one
      interpretation reconciles → REPORTED in the evidence with an explicit
      "requires_founder_decision" flag, exit 0 — flagged, never guessed, never auto-failed.
    - OUTPUT: ONE sanitized JSON to stdout (whitelisted fields: dates, floats, counts, residuals,
      flags — no ids/emails/symbols beyond what the whitelist re-walk admits);
      `assert_sanitized` re-walk before print; ALL error paths scrub via _redact_secret_values;
      exit codes 0/1/2/3 mirroring deribit exactly.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -c "import scripts.sfox_ground_truth as g; assert hasattr(g,'main')" && (unset SFOX_GROUND_TRUTH_KEY; .venv/bin/python -m scripts.sfox_ground_truth; test $? -eq 3)</automated>
  </verify>
  <acceptance_criteria>
    - Module imports with no network side effects; missing-creds run exits 3
    - grep -c "from scripts.deribit_ground_truth import" analytics-service/scripts/sfox_ground_truth.py returns 1 (sanitization reused, not forked)
    - The oracle function's parameters contain no usd_value/balance_history input: comment-stripped grep of the oracle function body for "usd_value\|balance_history" returns 0
    - Exit-code contract documented in the module docstring (0/1/2/3)
  </acceptance_criteria>
  <done>A committed, sanitized, env-credentialed parity harness exists whose oracle is independent by construction and whose divergence path raises.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fixture parity suite + live skipIf leg (Wave-0 gaps)</name>
  <files>analytics-service/tests/test_sfox_ground_truth.py, analytics-service/tests/test_sfox_ground_truth_live.py</files>
  <read_first>
    - analytics-service/scripts/sfox_ground_truth.py (Task 1 output)
    - analytics-service/tests/test_sfox_client_live.py (the 118 skipIf-gated live-test pattern: skip keeps CI green, a skip is NOT a pass)
    - .planning/phases/120-sfox-equity-reconstruction-backbone/120-VALIDATION.md (per-task map rows ground-truth-parity + live-parity)
  </read_first>
  <behavior>
    - CONSISTENT fixture: a hand-built account (daily usd_value series + a transactions ledger with coherent running account_balance, containing ≥1 deposit, ≥1 withdrawal, buys/sells, and PnL between events — the two streams reconcile BY CONSTRUCTION with hand-checked arithmetic in comments) → the parity check PASSES (exit-0 path)
    - TAMPERED fixture A: one usd_value point inflated by 5% → parity RAISES (anchor-consistency catches valuation tampering)
    - TAMPERED fixture B: a deposit row dropped from the transactions ledger → parity RAISES (a hidden deposit would otherwise book as fake return)
    - Oracle-independence pin: inspect.signature of the oracle function has no usd_value-carrying parameter AND a comment-stripped source scan of the oracle function for usd_value|balance_history returns 0 matches
    - Sanitization: fixture-run evidence passes assert_sanitized; planting a token-shaped string into the evidence makes assert_sanitized RAISE
    - Live leg (separate file): skipIf(no SFOX_GROUND_TRUTH_KEY) test invoking the harness end-to-end and asserting exit 0 / no material divergence; module docstring states the 121-egress gate and that a skip is NOT a pass
  </behavior>
  <action>
    RED first (fixtures + assertions against the Task-1 API), then make green (fixing the harness,
    not the tests, where they disagree — the fixtures' hand arithmetic is the oracle).
    Keep every fixture number hand-derived with the derivation in comments (P115 — never generate
    expected values by running the harness). The live file mirrors test_sfox_client_live.py's
    skipIf idiom and env naming.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_sfox_ground_truth.py tests/test_sfox_ground_truth_live.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest exits 0 with the live leg reported as SKIPPED locally (skip expected — not a pass)
    - Both tamper cases assert pytest.raises (fail-loud PROVEN)
    - grep -c "skipif\|skipIf" analytics-service/tests/test_sfox_ground_truth_live.py ≥ 1 and its docstring mentions the phase-121 egress gate
    - Full analytics suite green at plan close (cd analytics-service && .venv/bin/python -m pytest -q)
  </acceptance_criteria>
  <done>CI carries a real parity gate (consistent fixture passes, tampered fixtures fail loud, oracle independence pinned); the live leg exists, skip-gated, never faked.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: FOUNDER — live prod-key parity run (SFOX-06 live leg; gates on Phase-121 egress)</name>
  <what-built>The committed `scripts/sfox_ground_truth.py` harness + fixture-gated CI parity. The LIVE leg needs a real read-only sFOX key and — for an IP-whitelisted key — the Phase-121 Fly.io static egress, neither of which exists in-session.</what-built>
  <action>Present the founder runbook below verbatim, wait for the resume signal, and record the outcome (approved with evidence, or deferred → SFOX-06 live leg logged as human_needed in the SUMMARY). Never fabricate a pass; a skip is NOT a pass.</action>
  <how-to-verify>
    1. After Phase 121's egress IP is VERIFIED (== the dedicated v4, measured from the machine), or immediately with a non-whitelisted key:
       `export SFOX_GROUND_TRUTH_KEY=<read-only sFOX token>` (never paste it into a file or chat)
    2. `cd analytics-service && python -m scripts.sfox_ground_truth > /tmp/sfox_parity_evidence.json; echo "exit=$?"`
       - exit 0 → parity holds. exit 1 → MATERIAL DIVERGENCE: STOP — the curve must not ship; file the divergence with the evidence JSON. exit 3 → key not exported. exit 2 → read-only premise violated: revoke the key.
    3. Review the evidence JSON's `a2_*` residuals (account_balance semantics: cash vs total-MTM — which interpretation reconciles?) and `a3_*` day-0 residual (does prev0=first usd_value hold?). These RESOLVE assumptions A2/A3 with evidence.
    4. Record the outcome + resolved A2/A3 conventions in this phase's SUMMARY; if A2/A3 contradict the shipped conventions in `combine_sfox_balance_history` or the harness anchor logic, open a follow-up fix BEFORE Phase 122 badges anything.
  </how-to-verify>
  <resume-signal>Type "approved" with the exit code + A2/A3 verdicts, or "deferred" — the executor then records the SFOX-06 live leg as human_needed in the SUMMARY (the 118-02/119-04 precedent: skip keeps CI green, a skip is NOT a pass, never faked).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → stdout/logs | evidence JSON is founder-shareable; any credential leak is permanent |
| reconstructed curve → display surfaces | a divergent curve displayed = the exact fabrication class api_verified exists to prevent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-16 | Info disclosure | evidence output | mitigate | reuse sanitize_evidence + assert_sanitized re-walk (RAISES); _redact_secret_values on every error path; bearer never printed |
| T-120-17 | Tampering (economic) | parity oracle | mitigate | oracle independent by construction (transactions-only inputs); tamper fixtures PROVE divergence raises |
| T-120-18 | Repudiation | self-referential validation (P115) | mitigate | fixture expected values hand-derived in comments; independence pinned by signature + source-scan tests |
| T-120-19 | Spoofing | faked live pass | mitigate | live leg skipIf-gated + founder checkpoint; a skip is documented as NOT a pass; no code path can synthesize a green |
| T-120-20 | Elevation | write-capable key misuse | accept | key is read-only + structural GET-only client; residual risk is founder key-hygiene (runbook says revoke on exit 2) |
</threat_model>

<verification>
- `pytest tests/test_sfox_ground_truth.py tests/test_sfox_ground_truth_live.py -q` green (live SKIPPED locally).
- Harness missing-creds exit 3; import-clean.
- Full analytics suite green at plan close.
</verification>

<success_criteria>
- SFOX-06 code-complete: committed harness + fixture parity in CI; tampering provably fails loud; oracle provably independent.
- A2/A3 surfaced as evidence probes with an explicit founder-decision flag — never guessed.
- Live leg founder-gated and recorded human_needed if deferred — never faked.
</success_criteria>

<output>
Create `.planning/phases/120-sfox-equity-reconstruction-backbone/120-04-SUMMARY.md` when done.
</output>
