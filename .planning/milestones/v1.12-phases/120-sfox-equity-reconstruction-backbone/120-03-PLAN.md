---
phase: 120-sfox-equity-reconstruction-backbone
plan: 03
type: execute
wave: 2
depends_on: [120-01, 120-02]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/services/exchange.py
  - analytics-service/routers/process_key.py
  - analytics-service/services/closed_sets.py
  - analytics-service/routers/internal.py
  - analytics-service/routers/portfolio.py
  - analytics-service/tests/test_sfox_reconstruct.py
  - analytics-service/tests/test_process_key.py
autonomous: true
requirements: [SFOX-05]
must_haves:
  truths:
    - "An sfox derive_broker_dailies job produces daily returns that ride the UNCHANGED shared derive_basis_series/persist_basis_series seams — zero new backbone call sites (ONE-path, grep-proven)"
    - "Each sfox crawl at the worker is asyncio.wait_for-bounded; a hang becomes a classified RETRYABLE failure, never a wedged sequential worker (FLIPRETRY-01)"
    - "A materially-funded sfox account with <2 usable NAV days FAILS LOUD permanent with an analytics stamp — never a green-but-empty track record"
    - "process_key accepts source='sfox' on onboard/resync and the verification draft carries trust_tier='api_verified' (the free :828 stamp, proven by test)"
    - "The finalize permission probe resolves sfox with the structural read-only triple; verify_strategy rejects sfox with honest copy — no KEY_NETWORK_TIMEOUT misdirection (F2/F7 closed)"
    - "sfox is a CRYPTO venue everywhere the canonical closed set is consumed (√365 RISK basis; the known blend unknown-asset_class bug class cannot recur for sfox)"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "elif venue == 'sfox' branch + sfox-aware preflights + native-returns guard widen"
      contains: "elif venue == \"sfox\""
    - path: "analytics-service/services/closed_sets.py"
      provides: "CRYPTO_VENUES admits sfox (single canonical source, MD-01)"
      contains: "sfox"
    - path: "analytics-service/routers/process_key.py"
      provides: "H-11 onboard/resync admit sfox"
  key_links:
    - from: "analytics-service/services/job_worker.py (sfox branch)"
      to: "analytics-service/services/broker_dailies.py"
      via: "combine_sfox_balance_history"
      pattern: "combine_sfox_balance_history\\("
    - from: "analytics-service/services/job_worker.py (sfox branch)"
      to: "shared downstream derive/persist (:3182/:3348 region)"
      via: "(returns, meta) fall-through — NO new derive_basis_series call"
      pattern: "derive_basis_series\\( count unchanged (4 comment-stripped call sites)"
    - from: "analytics-service/services/exchange.py aclose_exchange"
      to: "SfoxClient.aclose"
      via: "isinstance chokepoint branch"
      pattern: "SfoxClient"
---

<objective>
Wire the sfox venue into the broker-dailies ONE-path: sfox-aware worker preflights (SfoxClient
instead of `create_exchange`, which raises for sfox today), the `elif venue == "sfox":` branch in
`derive_broker_dailies` (wait_for-bounded crawls → honesty gates → `combine_sfox_balance_history`
→ the UNCHANGED shared derive/persist), and the router seams that make finalize/process/verify
resolve sfox honestly (F2/F7) with the free `api_verified` stamp and crypto-venue classification.

Purpose: SFOX-05 end-to-end — an ingested sfox key becomes an api_verified daily-return series on
the ONE backbone, degenerate input gated honestly, worker never wedged.
Output: job_worker sfox branch + preflight/close seams, H-11 + CRYPTO_VENUES + probe/verify seams, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-sfox-equity-reconstruction-backbone/120-CONTEXT.md
@.planning/phases/120-sfox-equity-reconstruction-backbone/120-RESEARCH.md
@.planning/phases/119-sfox-read-adapter-key-validation/deferred-items.md
@.planning/phases/120-sfox-equity-reconstruction-backbone/120-02-SUMMARY.md
</context>

<interfaces>
<!-- Verified from codebase 2026-07-18. Line numbers current as of planning; re-locate by pattern if drifted. -->

job_worker.py load-bearing seams:
- `:708 / :790` — `_exchange_preflight` / `_allocator_key_preflight` both call `create_exchange(key_row["exchange"], …)`; `create_exchange` RAISES ValueError for sfox → the sfox preflight branch goes BEFORE these calls.
- `:666` `_ExchangeContext` dataclass — `exchange: ccxt.Exchange` annotation to widen.
- `:1922` `venue = ctx.key_row["exchange"]`; venue dispatch `if venue == "deribit":` at `:2114`; ccxt `else:` at `:2504-area`; the sfox `elif` goes between them.
- `:2225` C2 floor: `abs(equity) > _DERIBIT_EMPTY_LEDGER_FLOOR_USD` (`:178`, `Final[float] = 100.0`) `and _completeness.total_return_rows == 0` → `_stamp_strategy_analytics_failed` + `DispatchResult(FAILED, permanent)` — the material-balance posture to mirror.
- `:2010` `mtm_attempted: bool = False` — sfox never sets it; the MTM persist heal path then deletes stale rows correctly. Do not touch.
- `:2635` `finally: await aclose_exchange(ctx.exchange)` — the close chokepoint.
- `:2645` `if venue != "deribit":` — the ccxt USD-space combine block that would OVERWRITE sfox's returns with `combine_realized_and_funding` on empty streams. MUST exclude sfox.
- `:3182 / :3348 / :4489 / :4638` — the ONLY four (comment-stripped) `derive_basis_series(` call sites. The sfox branch adds ZERO new ones (baseline count 4 — the ONE-path grep gate).

From plan 120-02 (wave 1):
```python
# services/broker_dailies.py
def combine_sfox_balance_history(usd_value: pd.Series, flows_by_day: pd.Series) -> tuple[pd.Series, dict[str, Any]]
# services/sfox_read.py
class SfoxCrawlTruncatedError(Exception)
async def crawl_sfox_balance_history(client, start_date_ms, end_date_ms=None)   # rows + earliest-observed
async def crawl_sfox_transactions(client, from_ms, to_ms=None)
def sfox_flows_by_day(transactions) -> tuple[pd.Series, list[ExternalFlow]]
```

Router seams:
- routers/process_key.py:144-146 — H-11 per-flow sets: `"onboard": {"okx","binance","bybit","deribit"}`, `"resync": {…}` — add "sfox" to BOTH, nowhere else. `_is_long_fetch` (:408) already queues onboard/resync to the worker; `:828` `trust_tier = "csv_uploaded" if body.source == "csv" else "api_verified"` — NO edit needed (the free stamp).
- services/closed_sets.py:112 — `CRYPTO_VENUES = frozenset({"deribit","binance","okx","bybit"})` — the MD-01 canonical single source consumed by process_key `_resolve_asset_class` (√365) AND job_worker `_COMPOSITE_CRYPTO_VENUES` (blend basis).
- routers/internal.py:221 — the key-permission probe calls `create_exchange(exchange_name)` → ValueError→400 for sfox; finalize-wizard consumes this route (the F2 misleading-error seam).
- routers/portfolio.py:2148 — `verify_strategy(req: VerifyStrategyRequest)`; `VerifyStrategyRequest.exchange` admits sfox since 119 but the body routes through `create_exchange` (F7).
- routers/exchange.py `_validate_sfox_key` — the 119-02 auth-probe pattern (SfoxClient.get_balances, 401/403→AUTH, fail-closed otherwise, aclose in finally) to mirror in the internal probe.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sfox preflight/close seams + the elif venue=="sfox" branch (D: ONE-path + wait_for locked decisions)</name>
  <files>analytics-service/services/job_worker.py, analytics-service/services/exchange.py, analytics-service/tests/test_sfox_reconstruct.py</files>
  <read_first>
    - analytics-service/services/job_worker.py:640-830 (both preflights + _ExchangeContext), :1890-1935 (derive entry, venue resolution), :2090-2260 (deribit branch head: anchor read, honesty gates, C2 floor), :2500-2700 (ccxt else + finally-aclose + the venue != "deribit" combine guard), :2296-2345 (the deribit remaining-budget arithmetic — the timeout-sizing precedent)
    - analytics-service/services/exchange.py — aclose_exchange (the close chokepoint to widen)
    - analytics-service/services/sfox_read.py + services/broker_dailies.py (the plan-02 interfaces above)
    - .planning/phases/120-sfox-equity-reconstruction-backbone/120-RESEARCH.md §Crawl Safety + §Degenerate Gates + the architecture diagram
  </read_first>
  <behavior>
    - preflight with key_row.exchange=='sfox' yields an _ExchangeContext whose .exchange is an SfoxClient (both preflight paths), constructed from the decrypted trimmed api_key; no create_exchange call for sfox
    - aclose_exchange(SfoxClient) awaits its bounded .aclose() (and stays byte-identical for ccxt objects)
    - sfox branch, crawl hang (mocked crawl that sleeps past the bound) → asyncio.TimeoutError converted to a RETRYABLE transient DispatchResult/raise — asserted NOT permanent, and the test completes quickly (the bound fired)
    - SfoxCrawlTruncatedError from either crawl → permanent FAILED + _stamp_strategy_analytics_failed called (message names the truncation)
    - unvaluable-flow typed error from sfox_flows_by_day → permanent FAILED + stamp (LedgerValuationError disposition parity)
    - material floor: terminal |usd_value| > floor AND <2 usable observed NAV days → permanent FAILED + stamp; a genuinely tiny/empty account instead flows to the honest downstream gates (no invented rows)
    - happy path (fixture NAV+flows via mocked crawls): the branch yields (returns, meta) from combine_sfox_balance_history and the shared downstream variables (equity/balance_error/open_unrealized_usd/upnl_unreadable/funding/realized/external_flows) are set per the action spec — asserted by monkeypatching the crawl seam and letting the branch section run
    - ONE-path source gate: comment-stripped job_worker.py contains exactly 4 `derive_basis_series(` call sites (pre-phase baseline), and comment-stripped services/ingestion/sfox.py + the combine function contain zero `compute_all_metrics|derive_basis_series|quantstats` tokens
  </behavior>
  <action>
    RED tests first where feasible (the disposition/gate behaviors), then implement:

    (a) PREFLIGHTS — in `_exchange_preflight` and `_allocator_key_preflight`, immediately after
    `decrypt_credentials`, branch on `key_row["exchange"] == "sfox"`: construct
    `SfoxClient(api_key.strip())` (empty secret is the Q1 single-Bearer contract; never pass it)
    instead of `create_exchange`. Widen `_ExchangeContext.exchange` to
    `ccxt.Exchange | SfoxClient` (explicit union, not Any). The circuit breaker runs unchanged
    (per-exchange dict keying tolerates 'sfox' — verify `_check_circuit_breaker` and
    EXCHANGE_COOLDOWNS default handling; fix only if a KeyError is possible).

    (b) CLOSE — teach `services.exchange.aclose_exchange` to route an `SfoxClient` to
    `await client.aclose()` via an isinstance branch at the top (lazy-import SfoxClient inside
    the function to avoid an import cycle). ONE chokepoint: all job_worker call sites become
    sfox-safe with zero per-site edits. Keep the ccxt path byte-identical.

    (c) THE BRANCH — insert `elif venue == "sfox":` between the deribit branch and the ccxt
    `else:` inside the same `try` (so the `finally` aclose at :2635 covers it). Contents, in the
    deribit tier order:
      1. Hoist a module constant `_SFOX_CRAWL_TIMEOUT_S` (choose a value with a rationale comment
         tying it to the 15-min derive budget the deribit MTM pass fractions at :2296-2345 — e.g.
         a per-crawl bound of 300s leaves the second crawl + derive headroom; document the
         arithmetic). Wrap EACH crawl: `await asyncio.wait_for(crawl_sfox_balance_history(
         ctx.exchange, start_date_ms=<far-past epoch-ms constant, e.g. 2015-01-01, comment: A1
         depth resolves empirically — the earliest returned point is the empirical inception>),
         timeout=_SFOX_CRAWL_TIMEOUT_S)` and likewise `crawl_sfox_transactions(ctx.exchange,
         from_ms=<same far-past>, to_ms=now)`. On `asyncio.TimeoutError`: raise/convert to the
         RETRYABLE transient disposition (mirror the DeribitTransientReadError pattern at :2156 —
         a new `SfoxTransientReadError` or reuse of the generic transient classification; NEVER
         permanent, NEVER unbounded — this is the FLIPRETRY-01 wedge guard: a hang becomes a
         classified failure).
      2. `SfoxCrawlTruncatedError` → `_stamp_strategy_analytics_failed("sFOX history crawl could
         not be verified as complete…")` + `DispatchResult(FAILED, permanent)` — the
         assert_ledger_complete analog: no partial track record is ever written.
      3. Parse the balance-history rows into the `usd_value` pd.Series (timestamp→UTC day, float
         coercion fail-loud on garbage) and `flows_series, flow_evidence = sfox_flows_by_day(
         txn_rows)`. The typed unvaluable-flow error → permanent FAILED + stamp (mirror the
         LedgerValuationError disposition text pattern at :2470-area).
      4. Material-balance floor (C2 analog): terminal `abs(usd_value.iloc[-1])` >
         `_DERIBIT_EMPTY_LEDGER_FLOOR_USD` (reuse the constant; add a comment that the floor is
         venue-agnostic — rename only if trivial) AND fewer than 2 usable observed NAV days →
         `_stamp_strategy_analytics_failed` + permanent FAILED ("sFOX account holds material
         equity but produced no interpretable daily history").
      5. `returns, meta = combine_sfox_balance_history(usd_value, flows_series)`.
      6. Set the shared downstream variables EXACTLY as the deribit branch does so the code after
         the dispatch runs unchanged: `equity = float(usd_value.iloc[-1])`,
         `balance_error = False`, `open_unrealized_usd = 0.0` (usd_value IS total MTM — no
         separate wedge; comment it), `upnl_unreadable = False`, `funding = []`, `realized = []`,
         `external_flows = flow_evidence` (the list[ExternalFlow] — the DQ-02 evidence shape the
         shared gate consumes; verify against how `_completeness.dated_external_flows` is consumed
         downstream and match that type exactly).

    (d) GUARD AUDIT — grep the WHOLE function for `!= "deribit"` and audit each occurrence for
    sfox correctness. Confirmed required: the `:2645` USD-space combine block must NOT run for
    sfox (it would overwrite the native returns with combine_realized_and_funding over empty
    streams). Introduce a named module constant `_NATIVE_RETURNS_VENUES = frozenset({"deribit",
    "sfox"})` and change the guard to `if venue not in _NATIVE_RETURNS_VENUES:`, extending the
    existing native-switch comment. Audit (do not blindly edit) any other occurrence (e.g. :4262
    stitch path) — document each verdict in a code comment or the SUMMARY.

    (e) DO NOT touch the shared derive/persist section (:3182/:3348 mtm+cash derives, :3296
    persists) — sfox rides it byte-unchanged; `mtm_attempted` stays False.

    Tests: extend tests/test_sfox_reconstruct.py per the behavior list (mock/monkeypatch at the
    crawl + supabase seams; follow the existing job_worker test seam patterns — find them via
    grep for derive_broker_dailies in tests/). Include the ONE-path source-scan test using
    comment-stripped grep (strip lines matching '^\s*#' and docstrings before counting — the
    grep-gate hygiene rule; pin the derive_basis_series( call-site count at 4).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_sfox_reconstruct.py -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -n 'elif venue == "sfox"' analytics-service/services/job_worker.py returns exactly 1 hit
    - grep -v '^\s*#' analytics-service/services/job_worker.py | grep -c "derive_basis_series(" returns 4 (unchanged baseline — the ONE-path proof)
    - The sfox branch region contains ≥2 `asyncio.wait_for(` occurrences (both crawls bounded): sed -n '/elif venue == "sfox"/,/else:/p' piped to grep -c "asyncio.wait_for(" ≥ 2
    - grep -v '^\s*#' analytics-service/services/job_worker.py | grep -c '_NATIVE_RETURNS_VENUES' ≥ 2 (definition + the :2645 guard)
    - pytest tests/test_sfox_reconstruct.py exits 0 including the timeout-transient, truncation-permanent, material-floor and ONE-path-scan tests
  </acceptance_criteria>
  <done>An sfox key's derive job runs the full branch → shared backbone path; hangs are bounded+retryable; truncation/unvaluable/material-empty all fail loud with stamps; zero new backbone call sites.</done>
</task>

<task type="auto">
  <name>Task 2: F2/F7 router seams + crypto-venue class + api_verified proof + full-suite regression</name>
  <files>analytics-service/routers/process_key.py, analytics-service/services/closed_sets.py, analytics-service/routers/internal.py, analytics-service/routers/portfolio.py, analytics-service/tests/test_process_key.py, analytics-service/tests/test_sfox_reconstruct.py</files>
  <read_first>
    - analytics-service/routers/process_key.py:126-160 (H-11 validator + the P72 deribit comment), :400-415 (_is_long_fetch), :828-845 (the free trust_tier stamp)
    - analytics-service/services/closed_sets.py:100-120 (CRYPTO_VENUES MD-01 note) + grep its consumers and pin-tests (grep -rn CRYPTO_VENUES tests/)
    - analytics-service/routers/internal.py:190-260 (the permission probe + create_exchange seam)
    - analytics-service/routers/portfolio.py:2100-2200 (verify_strategy entry — find where exchange dispatch happens)
    - analytics-service/routers/exchange.py `_validate_sfox_key` (the auth-probe pattern to mirror)
    - .planning/phases/119-sfox-read-adapter-key-validation/deferred-items.md (F2/F7 exact wording — the honesty bar)
  </read_first>
  <action>
    (a) H-11 (process_key.py:144-146): add "sfox" to the `onboard` and `resync` sets ONLY —
    NOT teaser/internal_report/csv (SfoxAdapter.compute_metrics/fetch_raw fail loud by design;
    the sync pipeline cannot serve sfox). Extend the P72 comment: sfox routes through
    run_process_key_long_job → derive_broker_dailies (the plan-03 Task-1 branch), exactly like
    deribit. This kills the F2 422.

    (b) CRYPTO_VENUES (closed_sets.py:112): add "sfox" to the canonical frozenset. This
    propagates via MD-01 aliasing to `_resolve_asset_class` (asset_class="crypto" → √365 RISK
    basis, the #597 convention) AND `_COMPOSITE_CRYPTO_VENUES` (blend basis 365 if any leg
    crypto) — proactively closing the known unknown-asset_class √252 blend bug class for sfox.
    Update any set-pinning tests that hardcode the old membership (locate via
    grep -rn "CRYPTO_VENUES" analytics-service/tests/); add an assertion that "sfox" is a member.

    (c) internal.py permission probe: branch `exchange_name == "sfox"` BEFORE `create_exchange`,
    mirroring `_validate_sfox_key`: construct SfoxClient(api_key.strip()), auth-prove via
    get_balances() (aclose in finally). Success → return the STRUCTURAL triple
    {"read": True, "trade": False, "withdraw": False, "probe_error": False, "detected_at": …}
    with a comment: sFOX has no scope endpoint; read-only is structural (GET-only adapter) — the
    119 F3 honesty posture. SfoxApiError 401/403 → {"read": False, "trade": False,
    "withdraw": False, "probe_error": False, …} (an auth-dead key is honestly scopeless, not a
    probe error). Any other SfoxApiError → {"probe_error": True, read/trade/withdraw False}
    (fail-closed transient, mirroring the ccxt _FAIL_CLOSED semantics referenced at the return).
    This kills the F2 misleading 502/KEY_NETWORK_TIMEOUT at finalize.

    (d) portfolio.py verify_strategy: add an explicit early branch `req.exchange == "sfox"` →
    the standard error envelope with honest copy: sFOX strategy verification through the verify
    flow is not yet available; sFOX accounts connect via the add-key flow (fill-based
    verification does not apply to a NAV-backed venue). Use the route's existing error-shape
    helpers and a 4xx status; NEVER let sfox fall into create_exchange's generic ValueError or
    any network-timeout-shaped copy (the F2/F7 interim-honesty rule; the verify UI does not
    offer sfox until Phase 122).

    (e) trust tier: extend tests/test_process_key.py's trust-tier coverage with a
    source="sfox" case asserting the verification draft insert carries
    trust_tier == "api_verified" (name the test so `-k api_verified` selects it — proving the
    free :828 stamp with ZERO stamp-code edits). Add router tests for (a) (onboard/resync
    accept sfox + still-rejected teaser), (c) (structural triple + honest auth-dead + transient
    fail-closed), (d) (honest rejection copy, no timeout wording) in the routers' existing test
    homes (locate via grep; add to test_sfox_reconstruct.py only if no natural home exists).

    (f) REGRESSION: run the FULL analytics suite (the backbone touches many consumers):
    `cd analytics-service && .venv/bin/python -m pytest -q` — zero new failures vs the
    3875-passed baseline recorded in STATE.md. Any failure surfaced by the CRYPTO_VENUES widen
    or the guard audit is in-scope to fix here (root cause, not skip).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_process_key.py -k "sfox or api_verified" -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest -k "sfox or api_verified" green with ≥1 test proving trust_tier=='api_verified' for source='sfox'
    - grep -A3 '"onboard"' analytics-service/routers/process_key.py | grep -c sfox returns 1, and the teaser set does NOT contain sfox
    - grep -v '^\s*#' analytics-service/services/closed_sets.py | grep -c '"sfox"' returns 1
    - grep -in "network\|timeout" on the new verify_strategy sfox-rejection copy returns 0 (honest copy, no timeout misdirection)
    - FULL suite exits 0 (record the passed/skipped counts in the SUMMARY)
  </acceptance_criteria>
  <done>F2/F7 closed: process/finalize/verify all resolve sfox honestly; sfox is canonically crypto; api_verified proven; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker event loop ↔ live sFOX crawl | a hanging crawl is a self-DoS on the SEQUENTIAL prod worker (the v1.11 FLIP rollback class) |
| finalize/process routes → adapters | untrusted client input selects venue/flow |
| persisted track record | a partial/mis-valued series displayed as truth is economic tampering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-10 | DoS | sfox crawls on the worker loop | mitigate | asyncio.wait_for per crawl (_SFOX_CRAWL_TIMEOUT_S); timeout → classified transient; tested with a sleeping mock |
| T-120-11 | Tampering (integrity) | truncated crawl / unvaluable flow | mitigate | permanent FAILED + _stamp_strategy_analytics_failed; no partial track record written |
| T-120-12 | Tampering (economic) | green-but-empty funded account | mitigate | material-balance floor (C2 analog) fails loud |
| T-120-13 | Spoofing/honesty | finalize probe + verify copy | mitigate | structural read-only triple after live auth probe; honest rejection copy — no KEY_NETWORK_TIMEOUT misdirection |
| T-120-14 | Info disclosure | branch error messages → compute_jobs.error_message | mitigate | scrub_freeform_string on every stamped/raised message (T-74-03 discipline, mirrored from the deribit dispositions) |
| T-120-15 | Repudiation | silent basis drift | mitigate | ONE-path grep gate pins derive_basis_series call-site count; CRYPTO_VENUES single source (MD-01) |
</threat_model>

<verification>
- `pytest tests/test_sfox_reconstruct.py tests/test_process_key.py -q` green.
- FULL suite green; ONE-path grep gate green; wait_for grep gate green.
- Manual sanity: `python -c` resolve get_adapter('sfox') + import job_worker (no import cycles from the preflight widen).
</verification>

<success_criteria>
- An sfox onboard/resync submission flows: process_key (202/queued) → worker branch → shared backbone → persisted series + api_verified draft — with every failure mode dispositioned honestly.
- No parallel metrics path exists (grep-proven); the worker cannot be wedged by an sfox crawl.
- F2/F7 no longer reproduce: no 422-on-source, no misleading probe 502, no fake network-timeout copy.
</success_criteria>

<output>
Create `.planning/phases/120-sfox-equity-reconstruction-backbone/120-03-SUMMARY.md` when done.
</output>
