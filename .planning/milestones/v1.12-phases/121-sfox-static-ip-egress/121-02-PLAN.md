---
phase: 121-sfox-static-ip-egress
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/sfox_factory.py
  - analytics-service/services/exchange.py
  - analytics-service/routers/exchange.py
  - analytics-service/routers/internal.py
  - analytics-service/services/job_worker.py
  - analytics-service/services/ingestion/sfox.py
  - analytics-service/scripts/probe_exchange_egress.py
  - analytics-service/tests/test_egress_proxy_wiring.py
  - analytics-service/tests/test_egress_proxy_connect.py
  - analytics-service/tests/test_probe_egress_verify.py
autonomous: true
requirements: [SFOX-07]
must_haves:
  truths:
    - "WORKER_EGRESS_PROXY_URL set → every SfoxClient is constructed with proxy=<url> (all 4 sites) and, when the ccxt opt-in flag is also set, every ccxt exchange gets aiohttp_proxy=<url>"
    - "WORKER_EGRESS_PROXY_URL unset → behavior is BYTE-IDENTICAL to today (SfoxClient proxy=None, ccxt aiohttp_proxy stays the None class default) — proven by tests, safe to merge before the founder deploys"
    - "The proxy URL's BasicAuth userinfo lands as Proxy-Authorization on the HTTPS CONNECT request, and the sFOX Bearer token NEVER appears in what the proxy sees (tunnel is opaque)"
    - "probe_exchange_egress --expect <ip> fails loud (exit 1) when the realized egress IP != expected — the founder's pre-whitelist gate"
  artifacts:
    - path: "analytics-service/services/sfox_factory.py"
      provides: "worker_egress_proxy_url() + make_sfox_client() — the ONE place the env becomes an explicit proxy"
      exports: ["worker_egress_proxy_url", "make_sfox_client"]
    - path: "analytics-service/tests/test_egress_proxy_wiring.py"
      provides: "env set/unset threading tests + all-4-sites-route-through-factory source scan"
      min_lines: 60
    - path: "analytics-service/tests/test_egress_proxy_connect.py"
      provides: "mock CONNECT proxy asserting Proxy-Authorization lands and the Bearer never leaks (Pitfall-1 guard across aiohttp upgrades)"
    - path: "analytics-service/tests/test_probe_egress_verify.py"
      provides: "probe --expect compare logic: match exit 0, mismatch exit 1, ipinfo-failure exit 1"
  key_links:
    - from: "analytics-service/services/exchange.py"
      to: "WORKER_EGRESS_PROXY_URL"
      via: "create_exchange sets exchange.aiohttp_proxy behind the ccxt opt-in flag"
      pattern: "aiohttp_proxy"
    - from: "analytics-service/services/job_worker.py"
      to: "analytics-service/services/sfox_factory.py"
      via: "_make_exchange_client sfox branch constructs via make_sfox_client"
      pattern: "make_sfox_client\\("
    - from: "analytics-service/scripts/probe_exchange_egress.py"
      to: "WORKER_EGRESS_PROXY_URL"
      via: "urllib ProxyHandler routes the probe THROUGH the proxy so it measures realized egress"
      pattern: "ProxyHandler"
---

<objective>
Thread ONE env var — `WORKER_EGRESS_PROXY_URL` — into both worker egress transports: a
`make_sfox_client()` factory wired at all 4 SfoxClient construction sites (explicit `proxy=`,
never trust_env), and `exchange.aiohttp_proxy` in `create_exchange` behind a separate ccxt
opt-in flag `WORKER_EGRESS_PROXY_APPLIES_TO_CCXT` (default sFOX-only per CONTEXT discretion —
today's working ccxt egress is untouched unless the founder opts in). Extend
`probe_exchange_egress.py` with `--expect <ip>` so the founder's pre-whitelist gate is one
command. UNSET env = byte-identical to today; safe to merge before the founder deploys.

Purpose: SFOX-07 worker leg — an IP-whitelisted sFOX key authenticates only if the worker's
traffic actually rides the static-egress proxy; aiohttp ignores env proxies without trust_env
(the documented trap), so the proxy must be explicit at both chokepoints.
Output: factory + 6 edited files + 3 new test files; full analytics suite stays green
(baseline 3974 passed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/121-sfox-static-ip-egress/121-CONTEXT.md
@.planning/phases/121-sfox-static-ip-egress/121-RESEARCH.md
@analytics-service/services/sfox_client.py
</context>

<interfaces>
From analytics-service/services/sfox_client.py (existing seams — do NOT modify this file):
- `SfoxClient.__init__(api_key: str, base_url: str = SFOX_PROD_BASE_URL, proxy: str | None = None, *, _clock, _sleep)` — proxy stored on `self._proxy`, threaded per-request; session is `trust_env=False` by design.
- `SFOX_PROD_BASE_URL` constant; `SfoxApiError(status, detail)`.

The 4 construction sites (exact current expressions — preserve each site's argument shape):
- routers/exchange.py:66 — `SfoxClient(api_key=api_key, base_url=SFOX_PROD_BASE_URL)` (top-level import from services.sfox_client)
- routers/internal.py:231 — `SfoxClient(api_key.strip())` (LAZY in-branch import — keep lazy)
- services/job_worker.py:725 — `SfoxClient(api_key.strip())` inside `_make_exchange_client`
- services/ingestion/sfox.py:74 — `SfoxClient(api_key=api_key)`

From analytics-service/services/exchange.py:
- `create_exchange(exchange_name, api_key, api_secret, passphrase=None) -> ccxt.Exchange` at :817 — the SINGLE ccxt chokepoint; `os` already imported; ccxt base declares `aiohttp_proxy = None` as class default (unset = byte-identical).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: make_sfox_client factory + ccxt opt-in in create_exchange (tests first)</name>
  <files>analytics-service/tests/test_egress_proxy_wiring.py, analytics-service/services/sfox_factory.py, analytics-service/services/exchange.py</files>
  <read_first>121-RESEARCH.md §Architecture Patterns 1–3 (the factory + opt-in patterns, verified aiohttp/ccxt defaults) and §Common Pitfalls 4–5; services/sfox_client.py ctor (:99-131) for the proxy seam; services/exchange.py :817-845 (create_exchange body incl. the bybit fetchCurrencies branch)</read_first>
  <behavior>
    - env unset → make_sfox_client("k")._proxy is None (byte-identical)
    - env set to "" (empty string) → treated as unset → _proxy is None
    - WORKER_EGRESS_PROXY_URL="http://u:p@1.2.3.4:8888" → make_sfox_client("k")._proxy == that url
    - api_key passes through UNCHANGED (no strip in the factory — " k " stays " k "); base_url defaults to SFOX_PROD_BASE_URL and an explicit override is honored
    - create_exchange("bybit", ...) with env unset → exchange.aiohttp_proxy is None
    - env set but WORKER_EGRESS_PROXY_APPLIES_TO_CCXT unset → aiohttp_proxy is None (opt-in gating)
    - env set + flag in {"1","true","on"} (case-insensitive) → aiohttp_proxy == url
    - flag set but URL unset → aiohttp_proxy is None
  </behavior>
  <action>
    RED: write `tests/test_egress_proxy_wiring.py` covering the behaviors above using
    pytest `monkeypatch.setenv`/`delenv` (never mutate os.environ directly). ccxt instances
    created in tests must be closed (`asyncio.run(exchange.close())` or an async test) to avoid
    unclosed-session noise.

    GREEN: create `services/sfox_factory.py` per RESEARCH Pattern 2 with TWO deliberate
    deviations, documented in the module docstring: (a) NO `.strip()` inside the factory — the
    byte-identical mandate means each call site keeps its EXACT current credential expression
    (job_worker/internal already strip; routers/exchange + ingestion receive pre-trimmed input
    from the TS validate/encrypt chokepoint); (b) empty-string env coerces to None.
    `worker_egress_proxy_url() -> str | None` reads `WORKER_EGRESS_PROXY_URL`;
    `make_sfox_client(api_key, base_url=SFOX_PROD_BASE_URL) -> SfoxClient` passes
    `proxy=worker_egress_proxy_url()` explicitly — trust_env stays False inside SfoxClient; the
    env is NEVER read via aiohttp trust_env (the Pitfall-4 trap). Imports only os + sfox_client
    (no cycle: sfox_client imports nothing from exchange.py). NEVER log or repr the URL — it
    carries the BasicAuth secret (Pitfall 5).

    In `services/exchange.py::create_exchange`, immediately after `exchange = cls(config)`:
    read `WORKER_EGRESS_PROXY_URL`; if truthy AND `WORKER_EGRESS_PROXY_APPLIES_TO_CCXT`
    (lowercased) is in ("1","true","on"), set `exchange.aiohttp_proxy = url` — ccxt threads it
    into EVERY fetch including load_markets (verified ccxt 4.5.64). Otherwise touch NOTHING
    (aiohttp_proxy stays the None class default — byte-identical). Comment WHY the ccxt leg is
    opt-in: the founder's working ccxt Amsterdam egress must not silently change; aiohttp 3.14.1
    auto-derives Proxy-Authorization from the URL userinfo so no proxy_auth plumbing is added.
    The ccxt path for every venue stays otherwise untouched (the bybit branch unchanged).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_egress_proxy_wiring.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All 8 behavior assertions pass; the env-unset assertions pin proxy None / aiohttp_proxy None (the byte-identical proof).
    - sfox_factory.py contains no `.strip(` and no logging of the URL.
    - create_exchange's diff is confined to the post-construction opt-in block (bybit branch and everything else byte-identical).
  </acceptance_criteria>
  <done>test_egress_proxy_wiring.py green; factory exists; create_exchange gated opt-in in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire all 4 SfoxClient sites through the factory (source-scan + functional wiring tests)</name>
  <files>analytics-service/tests/test_egress_proxy_wiring.py, analytics-service/routers/exchange.py, analytics-service/routers/internal.py, analytics-service/services/job_worker.py, analytics-service/services/ingestion/sfox.py</files>
  <read_first>The 4 sites in &lt;interfaces&gt; (exact lines: routers/exchange.py:55-75, routers/internal.py:222-240, services/job_worker.py:715-728, services/ingestion/sfox.py:65-82); 121-RESEARCH.md §Validation Architecture row "All 4 sites route through the factory"</read_first>
  <behavior>
    - Source scan (P110 "test the wiring" lesson): read the 4 site files from disk; each contains `make_sfox_client(` and ZERO `SfoxClient(` construction tokens (import lines of SfoxApiError/SFOX_PROD_BASE_URL remain fine — the scan matches the exact token `SfoxClient(`); prove the scan fails when neutered by asserting services/sfox_factory.py itself DOES contain `SfoxClient(` (the only allowed constructor besides sfox_client.py/tests)
    - Functional: job_worker._make_exchange_client("sfox", " k \n", "") with env set → returned client `._proxy == url` AND `._api_key == "k"` (the site's .strip() preserved); env unset → `._proxy is None`
    - Functional: ingestion SfoxAdapter's client construction with env set → `._proxy == url` (construct via the module under monkeypatched env; if reaching the client requires a network call, monkeypatch `services.ingestion.sfox.make_sfox_client` to capture the call instead — assert it IS the imported name)
  </behavior>
  <action>
    RED: extend `tests/test_egress_proxy_wiring.py` with the source-scan + functional tests above.

    GREEN: replace each site's bare constructor with the factory, preserving the site's EXACT
    argument expression so env-unset behavior is byte-identical:
    - routers/exchange.py:66 → `make_sfox_client(api_key, base_url=SFOX_PROD_BASE_URL)`; add
      `from services.sfox_factory import make_sfox_client` at top (keep the existing
      SfoxApiError/SFOX_PROD_BASE_URL import from services.sfox_client; drop SfoxClient from it
      if now unused — surgical, no other import reshuffling).
    - routers/internal.py:231 → `make_sfox_client(api_key.strip())`; the import stays LAZY
      inside the `exchange_name == "sfox"` branch (it is lazy today to avoid import cost/cycles —
      match it: `from services.sfox_factory import make_sfox_client`).
    - services/job_worker.py:725 → `make_sfox_client(api_key.strip())` in `_make_exchange_client`;
      every ccxt venue's path through that function stays byte-identical.
    - services/ingestion/sfox.py:74 → `make_sfox_client(api_key)`.
    Do not touch services/sfox_client.py. Update each site's adjacent comment only where it
    names the constructor (surgical — no comment rewrites beyond that).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_egress_proxy_wiring.py tests/test_sfox_validate.py tests/test_ingestion_sfox.py tests/test_sfox_client.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source-scan test proves all 4 sites call make_sfox_client( and none construct SfoxClient( directly.
    - job_worker functional test: env set → ._proxy == url with the strip preserved; env unset → None.
    - The pre-existing sfox suites (test_sfox_validate 12, test_ingestion_sfox 15, test_sfox_client 25) pass UNMODIFIED — the behavioral proof that env-unset wiring is byte-identical.
  </acceptance_criteria>
  <done>All 4 sites route through the factory; wiring + all pre-existing sfox suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: probe --expect fail-loud gate + CONNECT Proxy-Authorization proof</name>
  <files>analytics-service/tests/test_probe_egress_verify.py, analytics-service/tests/test_egress_proxy_connect.py, analytics-service/scripts/probe_exchange_egress.py</files>
  <read_first>scripts/probe_exchange_egress.py (whole file, 114 lines — _get, main, the exit-code contract); 121-RESEARCH.md §Validation Architecture "Founder-gated live verification" + §Common Pitfalls 1 (the CONNECT-auth mechanism to pin) and 5 (never log the URL)</read_first>
  <behavior>
    - probe: `--expect 1.2.3.4` with ipinfo returning ip 1.2.3.4 → prints the verified egress ip, exit 0 (then the existing exchange probes still run)
    - probe: `--expect 1.2.3.4` with ipinfo returning 5.6.7.8 → prints a loud EGRESS-MISMATCH line naming observed vs expected, exit 1
    - probe: `--expect` given but the ipinfo fetch fails (status None/non-200 or unparseable) → exit 1 (a gate can NEVER false-pass on missing evidence)
    - probe: no `--expect` → today's behavior unchanged (geo-block exit contract intact)
    - CONNECT: make_sfox_client under WORKER_EGRESS_PROXY_URL="http://alice:s3cret@127.0.0.1:{port}" issuing get_balances() against a local mock proxy → the mock captures a request head whose first line starts "CONNECT api.sfox.com:443", whose Proxy-Authorization equals "Basic " + base64("alice:s3cret"), and in whose captured bytes the sFOX api_key appears NOWHERE (the tunnel-opacity design assertion: the proxy URL carries ONLY the proxy BasicAuth, never the Bearer)
  </behavior>
  <action>
    RED first for both test files.

    `tests/test_probe_egress_verify.py`: refactor-enabling tests that monkeypatch
    `scripts.probe_exchange_egress._get` to inject the ipinfo/exchange responses and call
    `main(argv=[...])` asserting the return codes above.

    `tests/test_egress_proxy_connect.py`: an `asyncio.start_server` mock proxy on 127.0.0.1
    that reads until the blank line, records the raw request head, replies
    "HTTP/1.1 407 Proxy Authentication Required" + CRLF CRLF, and closes. Under the
    monkeypatched env, construct via `make_sfox_client("SFOX-TEST-BEARER-TOKEN")`, expect
    `get_balances()` to raise (aiohttp surfaces the 407 as a client proxy error — assert
    pytest.raises on aiohttp.ClientError or SfoxApiError, whichever propagates), always
    `await client.aclose()` in a finally. Assertions per the behavior list — this test is the
    standing guard for RESEARCH Pitfall 1 (aiohttp 3.14.1 derives Proxy-Authorization from URL
    userinfo; an aiohttp upgrade that silently drops it turns this RED instead of silently
    breaking auth in prod with a 407).

    GREEN (probe): edit `scripts/probe_exchange_egress.py`, stdlib-only preserved:
    - `main(argv: list[str] | None = None)` + argparse with the single optional `--expect <ip>`.
    - When env `WORKER_EGRESS_PROXY_URL` is set, build a `urllib.request.ProxyHandler` with that
      url for both http and https and use the resulting opener for ALL probe fetches, so the
      script measures the REALIZED proxied egress (the SFOX-07 gate semantics). Print that the
      probe is proxy-routed WITHOUT printing the URL or any userinfo (it carries the secret —
      redact to scheme+host:port at most).
    - Parse and print the ipinfo `ip` field alongside `country`. Implement the --expect
      compare/exit contract from the behavior list; keep the geo-block exit path for the
      non-expect mode byte-identical.
    - Update the module docstring USAGE with the founder gate command
      (`railway ssh "cd /app && python -m scripts.probe_exchange_egress --expect <egress-ip>"`).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_probe_egress_verify.py tests/test_egress_proxy_connect.py -q && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Probe: match→0, mismatch→1, evidence-missing→1, no---expect unchanged; stdlib-only (no new imports beyond argparse/os/base-stdlib).
    - CONNECT test pins all three: CONNECT line, exact Basic credential, Bearer absent from the proxy-visible bytes.
    - The probe never prints the proxy URL/userinfo.
    - FULL analytics suite green (baseline 3974 passed / 0 failed) — the plan's closing gate.
  </acceptance_criteria>
  <done>Both new suites green and the full analytics-service suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → tinyproxy | The proxy URL (with BasicAuth secret) crosses from Railway env into aiohttp requests |
| worker → sFOX (through CONNECT) | Bearer token must stay inside the end-to-end TLS tunnel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-05 | Info Disclosure | sFOX Bearer visible to the proxy hop | mitigate | HTTPS CONNECT tunnel — pinned by test_egress_proxy_connect.py's Bearer-absent assertion |
| T-121-06 | Info Disclosure | Proxy secret in logs/probe output | mitigate | Factory/probe never log the URL; probe redacts to host:port; ccxt verbose stays off |
| T-121-07 | Spoofing | Silent proxy-auth regression on aiohttp upgrade | mitigate | CONNECT test asserts Proxy-Authorization lands (Pitfall-1 standing guard) |
| T-121-08 | Repudiation/DoS | Whitelisting on assumed (not realized) egress | mitigate | probe --expect fails loud incl. on missing evidence; gate ordered before whitelist in the runbook |
| T-121-SC | Tampering | Package installs | accept | Zero new packages — aiohttp/ccxt already installed (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
Full analytics suite green; `git diff --stat` confined to the 10 declared files; grep confirms
no remaining bare `SfoxClient(` constructor outside services/sfox_client.py, services/sfox_factory.py,
and tests.
</verification>

<success_criteria>
Merging this plan changes NOTHING in prod (env unset — proven by the unchanged pre-existing sfox
suites plus explicit None-pins), and the moment the founder sets WORKER_EGRESS_PROXY_URL, every
sFOX request and (opt-in) every ccxt request rides the authenticated proxy, with a one-command
fail-loud egress gate ready for plan 121-03.
</success_criteria>

<output>
Create `.planning/phases/121-sfox-static-ip-egress/121-02-SUMMARY.md` when done.
</output>
