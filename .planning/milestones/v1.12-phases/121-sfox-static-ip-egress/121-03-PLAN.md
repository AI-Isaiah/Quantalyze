---
phase: 121-sfox-static-ip-egress
plan: 03
type: execute
wave: 2
depends_on: [121-01, 121-02]
files_modified: []
autonomous: false
requirements: [SFOX-07]
user_setup:
  - service: fly.io
    why: "Static-egress proxy hosting (flyctl not installed locally; founder-run by design)"
    env_vars: []
    dashboard_config:
      - task: "fly deploy + fly ips allocate-v4 (dedicated inbound) + fly ips allocate-egress (static egress, the whitelisted IP) + fly secrets set PROXY_BASIC_AUTH"
        location: "flyctl, from fly-egress-proxy/ per README.md"
  - service: railway
    why: "Worker must present the proxy credential"
    env_vars:
      - name: WORKER_EGRESS_PROXY_URL
        source: "http://quantalyze:<secret>@<inbound-v4>:8888 — secret from the founder's openssl rand, inbound-v4 from fly ips allocate-v4"
    dashboard_config: []
  - service: sfox
    why: "IP whitelist on the founder's key"
    env_vars: []
    dashboard_config:
      - task: "Whitelist the STATIC EGRESS IPv4 (from fly ips allocate-egress — never the inbound v4), ONLY after the probe gate passes"
        location: "sFOX dashboard"
must_haves:
  truths:
    - "The worker's REALIZED egress through the proxy equals the allocated static egress IPv4, proven by the fail-loud probe from Railway BEFORE anything is whitelisted"
    - "The sFOX whitelist entry is the static EGRESS ip (never the inbound dedicated v4)"
  artifacts: []
  key_links:
    - from: "Railway worker (WORKER_EGRESS_PROXY_URL)"
      to: "Fly static egress IPv4"
      via: "railway ssh probe_exchange_egress --expect <egress-ip> exit 0"
      pattern: "--expect"
---

<objective>
Founder-run ops gate for SFOX-07: deploy the Fly proxy, allocate BOTH IPs, set the secrets/env,
verify the worker's realized egress through the proxy equals the static egress IPv4 (fail-loud),
and only then whitelist that egress IP at sFOX. flyctl is not installed locally — this is
human-only by design (the 118-02/119-04 human_needed precedent: a skip is never a pass, the
harness never fakes a live gate).

Purpose: the phase's success criterion 4 — an IP-whitelisted sFOX key authenticates through the
proxy path end-to-end. This also unblocks the founder-gated SFOX-06 live parity run (Phase 120)
and Phase 122's live e2e.
Output: a verified live proxy path + the whitelisted key; no repo files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/121-sfox-static-ip-egress/121-CONTEXT.md
@fly-egress-proxy/README.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Founder ops — fly deploy, dual-IP allocation, egress-verify gate, sFOX whitelist</name>
  <action>Founder follows fly-egress-proxy/README.md end-to-end: deploy to ams, allocate the dedicated inbound v4 AND the static egress IP, set PROXY_BASIC_AUTH + WORKER_EGRESS_PROXY_URL, run the probe gate from Railway, and whitelist the EGRESS ip at sFOX only after the gate exits 0. No repo files change.</action>
  <what-built>
    Plans 121-01/121-02 shipped everything automatable: the complete Fly deployable
    (fly-egress-proxy/ with the rendered-secret tinyproxy config and the corrected dual-IP
    runbook) and the worker wiring (WORKER_EGRESS_PROXY_URL threads an explicit proxy into all
    4 SfoxClient sites + opt-in ccxt; probe_exchange_egress --expect is the fail-loud gate).
    Nothing further can be automated: flyctl is not installed locally, and the Fly org, Railway
    env, and sFOX dashboard are founder-credentialed.
  </what-built>
  <how-to-verify>
    Follow fly-egress-proxy/README.md top-to-bottom. The load-bearing sequence:
    1. `fly deploy` from fly-egress-proxy/ (region ams).
    2. `fly ips allocate-v4` — dedicated INBOUND (~$2/mo). This is only the :8888 host.
    3. `fly ips allocate-egress --region ams` (~$3.60/mo) — record the egress IPv4. THIS is the
       ip sFOX whitelists (≈$5.60/mo total; the two allocations are different primitives —
       whitelisting the inbound v4 fails).
    4. `fly secrets set PROXY_BASIC_AUTH="quantalyze:<openssl rand -hex 32>"`.
    5. Machine check: `fly ssh console -C "curl -s https://ipinfo.io/json"` — ip == the step-3
       egress IPv4, country NL/EU.
    6. Railway env: `WORKER_EGRESS_PROXY_URL=http://quantalyze:<secret>@<inbound-v4>:8888`
       (ccxt opt-in `WORKER_EGRESS_PROXY_APPLIES_TO_CCXT=true` only if desired; default off).
    7. THE GATE: `railway ssh "cd /app && python -m scripts.probe_exchange_egress --expect <egress-ipv4>"`
       must exit 0. Exit 1 / EGRESS-MISMATCH / connection failure to :8888 → STOP, do not
       whitelist (a :8888 connect failure suggests Railway blocks non-443 outbound — the README's
       443-fronted fallback applies; report back instead).
    8. Only after step 7: whitelist the egress IPv4 in the sFOX dashboard, then re-validate the
       sFOX key from the app (KEY_AUTH_FAILED should clear).
  </how-to-verify>
  <resume-signal>
    Reply "egress verified + whitelisted" with the egress IPv4 and the probe output line, or
    describe the failing step (mismatch output / 407 / :8888 unreachable) for a fix phase.
  </resume-signal>
  <verify>
    <automated>MISSING — live founder gate by design (flyctl/Fly org/sFOX dashboard are founder-credentialed); the automated arm is the probe the founder runs: railway ssh "cd /app && python -m scripts.probe_exchange_egress --expect &lt;egress-ipv4&gt;" exit 0</automated>
    <human-check>Probe exit 0 with egress == the allocated static egress IPv4; sFOX key validates post-whitelist</human-check>
  </verify>
  <done>Probe gate exit 0 from Railway AND the whitelisted sFOX key authenticates end-to-end (or the checkpoint is honestly recorded human_needed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder ops | Secret generation + dashboard whitelisting happen outside the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-09 | Repudiation | Whitelisting on assumption | mitigate | Probe gate (step 7) is ordered before whitelist; exit 0 is the only pass |
| T-121-10 | Info Disclosure | Secret pasted into chat/logs | mitigate | Runbook instructs generation + fly secrets/Railway env only; resume-signal asks for the IP and probe line, never the URL |
| T-121-SC | Tampering | Package installs | accept | None in this plan |
</threat_model>

<verification>
The probe gate exit 0 from Railway with the proxied egress ip == the allocated static egress
IPv4, and a successful sFOX key validation post-whitelist. A skipped or assumed step is a FAIL —
record human_needed status honestly if the founder defers.
</verification>

<success_criteria>
SFOX-07 fully TRUE: sFOX worker traffic egresses from the verified static IP and the
IP-whitelisted key authenticates end-to-end.
</success_criteria>

<output>
Create `.planning/phases/121-sfox-static-ip-egress/121-03-SUMMARY.md` when done (or when the
checkpoint is recorded human_needed).
</output>
