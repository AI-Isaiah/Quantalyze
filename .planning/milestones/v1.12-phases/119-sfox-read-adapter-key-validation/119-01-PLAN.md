---
phase: 119-sfox-read-adapter-key-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260718HHMMSS_sfox_exchange_boundary_checks.sql
  - supabase/tests/test_sfox_exchange_boundary.sql
  - src/lib/closed-sets.ts
  - src/lib/strategy-sources.ts
  - analytics-service/models/schemas.py
  - analytics-service/routers/debug_key_flow.py
  - analytics-service/services/ingestion/adapter.py
  - analytics-service/tests/test_boundary_literals_parity.py
autonomous: false
requirements: [SFOX-04]

must_haves:
  truths:
    - "The database admits 'sfox' at all 4 key-save boundary CHECKs (api_keys.exchange, compute_jobs.exchange, strategies.source, strategy_verifications.source) while a bogus exchange value is STILL rejected (widened, not dropped)"
    - "Both vitest parity tests (check-zod-db-check-parity, strategy-sources-migration-parity) and the pytest boundary-literals parity test are green with 'sfox' on BOTH sides"
    - "The migration is MCP-applied to the TEST project qmnijlgmdhviwzwfyzlc BEFORE any merge to main (prod auto-applies on merge)"
  artifacts:
    - path: "supabase/migrations/20260718HHMMSS_sfox_exchange_boundary_checks.sql"
      provides: "4-constraint widen with self-verifying DO blocks (Phase 119)"
      contains: "sfox"
    - path: "supabase/tests/test_sfox_exchange_boundary.sql"
      provides: "RED guard: 'sfox' admitted, bogus value rejected, at all 4 constraints"
    - path: "src/lib/closed-sets.ts"
      provides: "SUPPORTED_EXCHANGES includes sfox; EXCHANGE_DISPLAY has sfox entry"
      contains: "sfox"
    - path: "analytics-service/tests/test_boundary_literals_parity.py"
      provides: "_KEY_SAVE_EXCHANGES fixture bumped to the 5-set + sfox assertions"
      contains: "sfox"
  key_links:
    - from: "src/lib/closed-sets.ts SUPPORTED_EXCHANGES"
      to: "supabase/migrations/20260718HHMMSS_sfox_exchange_boundary_checks.sql"
      via: "check-zod-db-check-parity.test.ts SPECs (api_keys.exchange / compute_jobs.exchange / strategy_verifications.source key off SUPPORTED_EXCHANGES)"
      pattern: "'sfox'"
    - from: "src/lib/strategy-sources.ts STRATEGY_SOURCES"
      to: "strategies_source_check (new migration)"
      via: "strategy-sources-migration-parity.test.ts set-equality"
      pattern: "sfox"
    - from: "analytics-service/models/schemas.py VerifyStrategyRequest.exchange"
      to: "analytics-service/tests/test_boundary_literals_parity.py _KEY_SAVE_EXCHANGES"
      via: "set-equality assertion (test_boundary_literals_parity.py:79)"
      pattern: "sfox"
---

<objective>
Widen the DB exchange-value boundary to admit `'sfox'` (SFOX-04): ONE constraint-widening
migration cloning the deribit precedent `20260704200446`, landing ATOMICALLY with every
lockstep allowlist edit (TS `SUPPORTED_EXCHANGES`/`EXCHANGE_DISPLAY`/`STRATEGY_SOURCES`,
the 3 pydantic Literals, the pytest parity fixture) so the parity contract tests never go
red between commits. Then a [BLOCKING] human-gated MCP-apply to the TEST project before merge.

Purpose: the SQL last-line-of-defense + code allowlists must admit `sfox` before any key
route or worker branch (Wave 2) can save a sFOX key. Implements CONTEXT locked decision
"DB constraint-widen — follow the deribit precedent EXACTLY".
Output: the migration, the RED SQL guard, 6 lockstep code edits + 1 fixture bump, TEST-project apply.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/119-sfox-read-adapter-key-validation/119-CONTEXT.md
@.planning/phases/119-sfox-read-adapter-key-validation/119-RESEARCH.md

Key verified facts (RESEARCH, 2026-07-18):
- No migration re-bases the 4 constraints after `20260704200446` — copy its definitions verbatim, append `'sfox'`.
- SKIP set (parity-pinned — touching any REDs a test or errors the migration): `funding_fees_exchange_check`, `position_snapshots_exchange_check`, the `verification_requests` VIEW + frozen Phase-19 legacy table, `finalize_terminal_status_param.sql:188` terminal-status allowlist (RESOLVED: NO — deribit itself is absent there; the `api_verified` write is phase 120), `UI_EXCHANGE_CODES`/`EXCHANGES`/`FUNDING_EXCHANGES` in closed-sets.ts (wizard-offered set = phase 122), `ingestion SUPPORTED_SOURCES`/`_FACTORIES` (phase 120).
- `.planning` is gitignored — never `git add` anything under `.planning/`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone the deribit boundary migration for 'sfox' + write the RED SQL guard</name>
  <files>supabase/migrations/20260718HHMMSS_sfox_exchange_boundary_checks.sql, supabase/tests/test_sfox_exchange_boundary.sql</files>
  <read_first>supabase/migrations/20260704200446_deribit_exchange_boundary_checks.sql (the exact template), an existing supabase/tests/test_*.sql for the test harness shape (e.g. supabase/tests/test_api_key_delete_atomicity.sql), 119-RESEARCH.md section "DB constraint-widen (SFOX-04) — exact current definitions to copy"</read_first>
  <action>
    Create the migration named `$(date -u +%Y%m%d%H%M%S)_sfox_exchange_boundary_checks.sql`
    (real execution-time UTC — the HHMMSS in this plan's frontmatter is a placeholder; newest
    existing migration is 20260717233529, the new timestamp must sort after it). Clone
    `20260704200446` structure verbatim (per CONTEXT locked decision D "follow the deribit
    precedent EXACTLY"): `BEGIN; SET lock_timeout = '3s';` then, for EACH of the 4 constraints,
    DROP CONSTRAINT IF EXISTS → ADD CONSTRAINT → self-verifying DO block, appending `'sfox'`:
    1. `api_keys_exchange_check` — `CHECK (exchange IN ('binance','okx','bybit','deribit','sfox'))`
    2. `compute_jobs_exchange_check` — PRESERVE the nullable form: `CHECK (exchange IS NULL OR exchange IN (...,'sfox'))`; keep the DO block's `position('IS NULL' IN def) = 0` nullable-form RAISE guard
    3. `strategies_source_check` — the 9-value set + `'sfox'` (10 values: legacy, wizard, admin_import, allocator_connected, csv, okx, binance, bybit, deribit, sfox)
    4. `strategy_verifications_source_check` — `CHECK (source IN ('okx','binance','bybit','csv','deribit','sfox'))`
    Each DO block's `FOREACH expected_value IN ARRAY ARRAY[...]` list includes 'sfox' AND every
    pre-existing value (fail-loud if any is lost). Update the RAISE prefixes from "Phase 68
    self-verify failed" to "Phase 119 self-verify failed". `COMMIT;` then refresh the
    `COMMENT ON COLUMN strategy_verifications.source` vocabulary line to append sfox
    (okx/binance/bybit/csv/deribit/sfox). Forward-only, NO down migration. Header comment
    explains the Phase-119 SFOX-04 purpose + the SKIP set (funding_fees, position_snapshots,
    verification_requests VIEW, terminal-status :188) with the RESEARCH rationale for each.

    Then write `supabase/tests/test_sfox_exchange_boundary.sql` following the existing
    supabase/tests harness conventions: for each of the 4 constraints, (a) an INSERT/UPDATE
    with `'sfox'` succeeds (RED without the migration applied), (b) an INSERT with a bogus
    value (e.g. `'notanexchange'`) is REJECTED (proves widened-not-dropped), (c) for
    compute_jobs, a NULL exchange is still admitted (nullable form preserved). Roll back all
    test writes (transaction/cleanup per the harness convention).
    NOTE: do NOT commit this task alone — it commits atomically with Task 2 (a lone SQL-side
    'sfox' REDs the set-equality parity tests).
  </action>
  <verify>
    <automated>test -f supabase/tests/test_sfox_exchange_boundary.sql && ls supabase/migrations/ | tail -1 | grep sfox_exchange_boundary_checks && grep -v '^--' supabase/migrations/*_sfox_exchange_boundary_checks.sql | grep -c "'sfox'" | awk '{exit ($1>=8)?0:1}'</automated>
  </verify>
  <done>Migration widens exactly the 4 named constraints with 'sfox' appended, each with a Phase-119 self-verify DO block (compute_jobs keeps the IS NULL guard); the SQL test asserts admit-sfox + reject-bogus + compute_jobs-NULL; nothing in the SKIP set is touched (grep the migration for funding_fees/position_snapshots/verification_requests → zero hits).</done>
</task>

<task type="auto">
  <name>Task 2: Lockstep allowlist edits (6 code sites + parity fixture) — atomic commit with Task 1</name>
  <files>src/lib/closed-sets.ts, src/lib/strategy-sources.ts, analytics-service/models/schemas.py, analytics-service/routers/debug_key_flow.py, analytics-service/services/ingestion/adapter.py, analytics-service/tests/test_boundary_literals_parity.py</files>
  <read_first>src/lib/closed-sets.ts (lines 30–110 — note the DELIBERATELY-decoupled UI_EXCHANGE_CODES/EXCHANGES/FUNDING_EXCHANGES comments), analytics-service/tests/test_boundary_literals_parity.py (the set-equality mechanics at :51/:79), 119-RESEARCH.md "LOCKSTEP INVENTORY" tables</read_first>
  <action>
    Apply the RESEARCH 13-site lockstep inventory's code edits (sites #1–#3, #9–#12; #4–#7 are
    the migration from Task 1; #8 is Wave 2):
    - `src/lib/closed-sets.ts:39` — append `"sfox"` to `SUPPORTED_EXCHANGES` (exchangeEnum z.enum derives automatically).
    - `src/lib/closed-sets.ts:48` — add `sfox: "sFOX"` to `EXCHANGE_DISPLAY` (compile-forced by `satisfies Record<SupportedExchange, string>`). Display string is exactly `sFOX` (brand casing).
    - `src/lib/strategy-sources.ts:17` — append `"sfox"` to `STRATEGY_SOURCES`.
    - `analytics-service/models/schemas.py:206` — `VerifyStrategyRequest.exchange` Literal gains `"sfox"`.
    - `analytics-service/routers/debug_key_flow.py:61` — `Broker` Literal gains `"sfox"`.
    - `analytics-service/services/ingestion/adapter.py:31` — `Source` Literal gains `"sfox"`. Do NOT touch `SUPPORTED_SOURCES`/`_FACTORIES` in `ingestion/__init__.py` (phase 120; safe because nothing calls `get_adapter("sfox")` in 119 — the validate path is the LEGACY worker route).
    - `analytics-service/tests/test_boundary_literals_parity.py:51` — bump `_KEY_SAVE_EXCHANGES` to the 5-set including `"sfox"`; add sfox membership assertions for the Broker + Source Literals; ADD an assertion that the NEW `*_sfox_exchange_boundary_checks.sql` migration file admits sfox at all 4 constraints (mirror the existing deribit migration-parity assertion pattern in that file).
    DO NOT touch: `UI_EXCHANGE_CODES`, `EXCHANGES`, `FUNDING_EXCHANGES` (closed-sets.ts:65/82/100 — wizard-offered set is phase 122), any wizard component exchange cards.
    Run `npx tsc --noEmit` — if OTHER exhaustive `Record<SupportedExchange, ...>` sites break,
    fill each with an honest sfox entry (that is lockstep, not scope creep); if any currently-green
    test pins the exact 4-set, update it to the 5-set. Surface (do not silently skip) anything
    that would require inventing data (e.g. a funding/derivative-specific record — sFOX is spot;
    stop and flag instead of fabricating).
    COMMIT Tasks 1+2 TOGETHER as one atomic commit (the parity pair must never be split across
    commits). Per feedback_never_bundle_commit_with_edits: finish all edits, verify, then commit.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/__tests__/contracts/check-zod-db-check-parity.test.ts src/__tests__/strategy-sources-migration-parity.test.ts --no-file-parallelism && cd analytics-service && .venv/bin/python -m pytest tests/test_boundary_literals_parity.py -q</automated>
  </verify>
  <done>Both vitest parity tests + the pytest boundary-literals test green with sfox on both sides; tsc clean; UI_EXCHANGE_CODES/EXCHANGES/FUNDING_EXCHANGES byte-identical (git diff shows no hunks there); one atomic commit contains the migration + SQL test + all 6 lockstep edits.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: [BLOCKING] MCP-apply the migration to the TEST project BEFORE merge</name>
  <action>HUMAN-GATED (do not automate): the human applies the committed migration to the TEST project via Supabase MCP, fixes the schema_migrations timestamp if MCP stamped now(), and runs the RED SQL guard on TEST. Execution PAUSES here until confirmed — the branch must NOT merge before this checkpoint resolves.</action>
  <done>TEST project qmnijlgmdhviwzwfyzlc has all 4 widened constraints (pg_get_constraintdef contains 'sfox'), schema_migrations row carries the file timestamp, and test_sfox_exchange_boundary.sql passes on TEST.</done>
  <what-built>The Phase-119 sfox constraint-widening migration (Task 1) is committed on the branch. Load-bearing carry-forward (STATE.md): RED-guarded SQL tests run against the TEST project — merging before the TEST apply REDs CI, and merging `supabase/migrations/**` to main AUTO-APPLIES to PROD.</what-built>
  <how-to-verify>
    1. Via Supabase MCP `apply_migration` on TEST project `qmnijlgmdhviwzwfyzlc`, apply
       `supabase/migrations/*_sfox_exchange_boundary_checks.sql` (the DO blocks self-verify —
       a silent no-op is impossible; any RAISE aborts the apply).
    2. If MCP stamps `now()` in `schema_migrations`, fix that row to the FILE timestamp
       (feedback_supabase_apply_migration_drift).
    3. Run `supabase/tests/test_sfox_exchange_boundary.sql` against the TEST project — must pass
       (sfox admitted at all 4, bogus rejected, compute_jobs NULL admitted).
    4. Verify the 4 constraint defs on TEST via MCP (pg_get_constraintdef contains 'sfox').
    Only after 1–4: the branch may merge. On merge, WATCH the prod auto-apply run + verify the
    4 objects on prod project `khslejtfbuezsmvmtsdn` via MCP.
  </how-to-verify>
  <resume-signal>Type "applied" once the TEST project apply + SQL test pass are confirmed, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API routes → Postgres | untrusted exchange/source values reach constrained columns |
| migration merge → PROD | supabase/migrations/** auto-applies to production on merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Tampering | 4 widened CHECK constraints | mitigate | Widen-not-drop: RED SQL test proves a bogus value is still rejected; DO blocks RAISE if any pre-existing value is lost |
| T-119-02 | Tampering/DoS | TS↔SQL↔pydantic vocab drift | mitigate | 2 vitest + 1 pytest parity tests pin both sides; atomic Task-1+2 commit |
| T-119-03 | DoS | prod auto-apply on merge | mitigate | [BLOCKING] Task 3 TEST-project apply + founder-watched prod apply; `SET lock_timeout='3s'` bounds apply blast radius |
</threat_model>

<verification>
- `npx vitest run src/__tests__/contracts/check-zod-db-check-parity.test.ts src/__tests__/strategy-sources-migration-parity.test.ts --no-file-parallelism` green.
- `cd analytics-service && .venv/bin/python -m pytest tests/test_boundary_literals_parity.py -q` exit 0.
- `npx tsc --noEmit` clean.
- Migration touches ONLY the 4 named constraints: `grep -iv '^--' supabase/migrations/*_sfox_exchange_boundary_checks.sql | grep -c 'funding_fees\|position_snapshots\|verification_requests'` outputs 0.
- Task 3 checkpoint confirmed (TEST project apply + SQL test pass) before any merge.
</verification>

<success_criteria>
SFOX-04: the DB admits 'sfox' at every constrained exchange boundary via ONE RED-guarded
migration (self-verify DO blocks), a still-invalid value is still rejected, all three parity
tests are green with sfox on both sides, and the migration is applied to the TEST project
before merge.
</success_criteria>

<output>
Create `.planning/phases/119-sfox-read-adapter-key-validation/119-01-SUMMARY.md` when done.
</output>
