---
phase: 119-sfox-read-adapter-key-validation
plan: 03
type: execute
wave: 2
depends_on: ["119-01"]
files_modified:
  - src/app/api/keys/validate-and-encrypt/route.ts
  - src/app/api/keys/validate-and-encrypt/route.test.ts
  - src/app/api/strategies/create-with-key/route.ts
  - src/app/api/strategies/create-with-key/route.test.ts
  - src/app/api/strategies/composite/add-key/route.ts
  - src/app/api/strategies/composite/add-key/route.test.ts
autonomous: true
requirements: [SFOX-03]

must_haves:
  truths:
    - "A token-only sfox request (api_key set, api_secret absent/empty) passes all 3 routes' credential-shape gates and reaches validateKey through the SHARED analytics-client trim chokepoint"
    - "For every ccxt exchange (binance/okx/bybit/deribit) the api_secret presence/length checks are UNCHANGED — a missing or <8-char secret is still rejected KEY_INVALID_FORMAT"
    - "The 2 gating routes admit exchange='sfox' through isSupportedExchange (via 119-01's SUPPORTED_EXCHANGES) and an sfox auth failure surfaces as KEY_AUTH_FAILED via the shared classifyKeyValidationError"
  artifacts:
    - path: "src/app/api/keys/validate-and-encrypt/route.ts"
      provides: "sfox carve-out at the :23 presence gate (api_secret optional for sfox only)"
      contains: "sfox"
    - path: "src/app/api/strategies/create-with-key/route.ts"
      provides: "sfox carve-out at the :61 length gate"
      contains: "sfox"
    - path: "src/app/api/strategies/composite/add-key/route.ts"
      provides: "sfox carve-out at the :81 length gate"
      contains: "sfox"
  key_links:
    - from: "3 key routes"
      to: "src/lib/analytics-client.ts validateKey/encryptKey"
      via: "the SHARED trimCredential chokepoint (analytics-client.ts:169) — sfox passes api_secret '' through the SAME funnel, no parallel path"
      pattern: "validateKey"
    - from: "3 key routes"
      to: "src/lib/wizardErrors.ts classifyKeyValidationError"
      via: "shared error mapping (worker 'Authentication failed…' → KEY_AUTH_FAILED)"
      pattern: "classifyKeyValidationError"
---

<objective>
The Q1 credential carve-out (SFOX-03, SECURITY-SENSITIVE): sFOX auth is a SINGLE Bearer token
(no api_secret — 118-RESEARCH confirmed), but all 3 key routes require a secret. For
`exchange === 'sfox'` ONLY, make `api_secret` optional (normalize to empty string), store the
token as `api_key`, and route through the SAME shared trim/validate/encrypt chokepoint — a
surgical relaxation that must NOT weaken ccxt-exchange validation by one bit.

Purpose: this is the single thing that blocks a sFOX key from connecting end-to-end
(RESEARCH Pitfall 1). Flagged for the code-review + red-team security pass per CONTEXT Q1.
Output: 3 route carve-outs + fail-closed route tests both directions (sfox admitted, ccxt unchanged).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-sfox-read-adapter-key-validation/119-CONTEXT.md
@.planning/phases/119-sfox-read-adapter-key-validation/119-RESEARCH.md

<interfaces>
Verified gate sites (2026-07-18):
- `validate-and-encrypt/route.ts:23` — `if (!exchange || !api_key || !api_secret)` presence gate; NO isSupportedExchange gate (delegates to the worker); handler passes through `validateKey(exchange, api_key, api_secret, passphrase)` then `encryptKey(...)` (:122/:135); rejects `!validation.read_only` at :123.
- `create-with-key/route.ts:47` — `isSupportedExchange(exchange)` gate → 400 KEY_INVALID_FORMAT; `:61` — `typeof api_secret !== "string" || api_secret.length < 8` → KEY_INVALID_FORMAT "api_secret is required"; `:85` — max-length `api_secret.length > 512`; `read_only` consumed at :179.
- `composite/add-key/route.ts:67` — same isSupportedExchange gate; `:81` — same length<8 gate; `:105` — same max-length; `read_only` at :169.
- Shared chokepoint: `src/lib/analytics-client.ts` `trimCredential` (:169) inside `validateKey`/`encryptKey` — trims api_key AND api_secret identically for validate and encrypt (the v1.11 DOGFOOD fix). `trimCredential("")` is `""` — an empty sfox secret flows through unchanged; do NOT bypass or fork this funnel.
- Worker contract (plan 119-02): validate-key accepts sfox with `api_secret: ""`; returns `{valid, read_only}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sfox secret carve-out in validate-and-encrypt (presence gate)</name>
  <files>src/app/api/keys/validate-and-encrypt/route.ts, src/app/api/keys/validate-and-encrypt/route.test.ts</files>
  <read_first>src/app/api/keys/validate-and-encrypt/route.ts (whole file — the legacy/flag-locked handler shape and NO_STORE_HEADERS conventions), src/app/api/keys/validate-and-encrypt/route.test.ts (existing mocking of validateKey/encryptKey)</read_first>
  <behavior>
    - sfox + api_key + NO api_secret → passes the presence gate; validateKey called with api_secret "" (assert the mock's args — proves the shared chokepoint carries it, no parallel path)
    - sfox + api_key + api_secret undefined AND null AND "" → all three normalize identically to ""
    - binance + api_key + NO api_secret → STILL rejected exactly as today (same status + code/copy as the pre-change behavior)
    - deribit + api_key + NO api_secret → STILL rejected (second ccxt witness)
    - sfox + NO api_key → rejected (the carve-out relaxes ONLY api_secret, never api_key)
    - sfox + worker 400 "Authentication failed…" → response surfaces the fail-closed error (no false-verified)
  </behavior>
  <action>
    Per CONTEXT Q1 (LOCKED): at the :23 presence gate, allow a missing/empty `api_secret` when
    `exchange === "sfox"` ONLY; normalize to `""` and pass through the EXISTING
    `validateKey`/`encryptKey` calls untouched (they trim at the shared chokepoint —
    analytics-client.ts:169). Keep `!exchange || !api_key` rejection universal. Do not add an
    isSupportedExchange gate here (this route deliberately delegates gating to the worker —
    match existing structure, Rule 3 surgical). Comment the carve-out with WHY (sFOX = single
    Bearer token, stored as api_key, empty secret; per 119-CONTEXT Q1) and the security flag
    ("relaxes credential presence for one exchange — ccxt unchanged; security-reviewed").
    TDD: failing route tests first, then the carve-out.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/keys/validate-and-encrypt/route.test.ts --no-file-parallelism</automated>
  </verify>
  <done>All behavior cases green; the ccxt rejection assertions use the SAME expected status/copy as the pre-change tests (proving zero weakening); diff shows no other logic touched in the route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sfox secret carve-out in create-with-key + composite/add-key (length gates)</name>
  <files>src/app/api/strategies/create-with-key/route.ts, src/app/api/strategies/create-with-key/route.test.ts, src/app/api/strategies/composite/add-key/route.ts, src/app/api/strategies/composite/add-key/route.test.ts</files>
  <read_first>src/app/api/strategies/create-with-key/route.ts (:47 gate, :61 length check, :85 max-length, okx-passphrase branch :69 — untouched, sfox has no passphrase), src/app/api/strategies/composite/add-key/route.ts (:67/:81/:105 — the same three), both route.test.ts files (existing KEY_INVALID_FORMAT / KEY_AUTH_FAILED test shapes)</read_first>
  <behavior>
    - sfox passes isSupportedExchange in BOTH routes (needs 119-01's SUPPORTED_EXCHANGES — regression-proves the wiring, not just the constant)
    - sfox + NO api_secret (undefined/null/"") → passes the :61/:81 gate; validateKey receives api_secret "" (assert mock args in both routes)
    - sfox + api_secret longer than 512 → still rejected by the max-length gate (bound kept even for the optional field)
    - binance/okx/bybit/deribit + 7-char api_secret → STILL rejected KEY_INVALID_FORMAT "api_secret is required" in both routes (exact pre-change copy)
    - sfox + worker "Authentication failed…" → classifyKeyValidationError → response code KEY_AUTH_FAILED (both routes; proves the shared mapping end-to-end with zero wizardErrors.ts edits)
    - create-with-key: a successful sfox connect stamps source='sfox' into the strategy RPC payload (the route stamps source=exchange — assert the mock RPC args; DB CHECK admits it per 119-01)
  </behavior>
  <action>
    In BOTH routes, change the `typeof api_secret !== "string" || api_secret.length < 8` gate
    (:61 create-with-key, :81 add-key) to exempt `exchange === "sfox"`: for sfox, accept
    absent/empty and normalize to `""`; keep the `length > 512` upper bound applied whenever a
    secret IS present (:85/:105). Every other exchange keeps the exact existing gate (per
    CONTEXT Q1: "Keep the requirement intact for all ccxt exchanges"). The okx passphrase
    branches are untouched. Downstream `validateKey(...)`/`encryptKey(...)` calls and the RPC
    insert paths are untouched (encrypt_credentials tolerates an empty secret —
    RESEARCH-verified; `api_secret_encrypted ?? null` handling already exists at :217/:204).
    Mirror the same explanatory + security-flag comment as Task 1. Prefer one small shared
    predicate per route file over copy-paste drift ONLY if it does not restructure existing
    code (surgical). TDD: failing tests first in both route.test.ts files, then the edits.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/create-with-key/route.test.ts src/app/api/strategies/composite/add-key/route.test.ts --no-file-parallelism</automated>
  </verify>
  <done>All behavior cases green in both routes; ccxt short-secret rejections byte-identical to pre-change; source='sfox' stamping asserted; no wizardErrors.ts diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/wizard → 3 key routes | untrusted credentials + exchange enter the system |
| routes → worker | validated shape forwarded to /api/validate-key + /api/encrypt-key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-08 | Elevation/Tampering | secret carve-out weakening ccxt validation | mitigate | Carve-out keyed on exchange==='sfox' literal only; explicit regression tests pin binance/okx/bybit/deribit rejections to pre-change copy; flagged for security review pass |
| T-119-09 | Spoofing | sfox bypassing route gating | mitigate | 2 gating routes keep isSupportedExchange; api_key presence still universal; worker branch (119-02) is the auth authority |
| T-119-10 | Info disclosure | credential echo in route errors | mitigate | Existing generic KEY_* envelope reused; no new error copy carries input values |
| T-119-11 | DoS | unbounded sfox token size | mitigate | 512-char max-length gate retained for api_key and any present api_secret |
</threat_model>

<verification>
- `npx vitest run src/app/api/keys/validate-and-encrypt/route.test.ts src/app/api/strategies/create-with-key/route.test.ts src/app/api/strategies/composite/add-key/route.test.ts --no-file-parallelism` exit 0.
- `npx tsc --noEmit` clean.
- `git diff --stat` touches ONLY the 6 files in files_modified (no wizardErrors.ts, no analytics-client.ts — the shared chokepoint is reused, not modified).
- `npm run lint` (pre-push discipline — react-hooks errors invisible to tsc/vitest).
</verification>

<success_criteria>
SFOX-03 (route leg): a token-only sfox key flows through all 3 routes into the shared
trim/validate/encrypt chokepoint; auth failure surfaces as KEY_AUTH_FAILED via the shared
classifier; every ccxt exchange's credential validation is provably unchanged.
</success_criteria>

<output>
Create `.planning/phases/119-sfox-read-adapter-key-validation/119-03-SUMMARY.md` when done.
</output>
