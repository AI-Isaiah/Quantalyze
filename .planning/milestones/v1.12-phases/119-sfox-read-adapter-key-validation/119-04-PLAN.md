---
phase: 119-sfox-read-adapter-key-validation
plan: 04
type: execute
wave: 3
depends_on: ["119-02"]
files_modified:
  - analytics-service/services/sfox_read.py
  - analytics-service/tests/test_sfox_read.py
autonomous: false
requirements: [SFOX-02]
user_setup:
  - service: sfox
    why: "Live prod-account read leg (SFOX-02) needs a real sFOX API key; an IP-whitelisted key additionally needs the phase-121 static egress"
    env_vars: []
    dashboard_config:
      - task: "Mint a READ-ONLY sFOX API key (permissions mirror the user role) and run the live-read leg per Task 2's runbook"
        location: "trading.sfox.com account settings (prod) — founder-held"

must_haves:
  truths:
    - "The worker can pull an sFOX account's balances + trades + transactions in one read call composed ONLY of SfoxClient's GET read methods — no order/withdraw/transfer capability exists or is exercised (read-only asserted at the ingestion boundary)"
    - "Any leg failing (balances OR trades OR transactions) fails the WHOLE read loud — never a silently-partial or fabricated result"
    - "The live prod-account read is either performed by the founder per the runbook or recorded human_needed — never faked with a mock passed off as live"
  artifacts:
    - path: "analytics-service/services/sfox_read.py"
      provides: "read_sfox_account orchestration: balances + trades + transactions via SfoxClient, single-page (cursor crawl = phase 120)"
      contains: "get_balances"
    - path: "analytics-service/tests/test_sfox_read.py"
      provides: "mocked unit suite: 3-leg pull, per-leg fail-loud, no-write-surface gate"
      min_lines: 50
  key_links:
    - from: "analytics-service/services/sfox_read.py"
      to: "analytics-service/services/sfox_client.py SfoxClient"
      via: "get_balances / get_trades / get_transactions (GET-only surface)"
      pattern: "get_(balances|trades|transactions)"
---

<objective>
The SFOX-02 read pull: a thin worker-side orchestration that reads an sFOX account's
balances + trades + transactions through the Phase-118 `SfoxClient`, with read-only asserted
at the ingestion boundary and honest fail-loud on any leg. Full daily-return reconstruction,
cursor crawl, and `get_adapter("sfox")` registration are PHASE 120 — this phase delivers the
read pull + read-only assertion + honest failure ONLY. The live prod-read leg is founder-gated
(Q3 LOCKED): the mocked unit test carries the phase; a live read is never faked.

Purpose: phase 120's reconstruction consumes this read surface; proving it now (offline) plus
the founder's live leg closes SFOX-02's "reads a real account" claim honestly.
Output: services/sfox_read.py + mocked suite + the founder live-read checkpoint (human_needed if no key in-session).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-sfox-read-adapter-key-validation/119-CONTEXT.md
@.planning/phases/119-sfox-read-adapter-key-validation/119-RESEARCH.md
@analytics-service/services/sfox_client.py

<interfaces>
From analytics-service/services/sfox_client.py (Phase 118):
- `SfoxClient.get_balances() -> list[dict]` (:261)
- `SfoxClient.get_transactions(...)` (:268) — single-page, cursor exposed (crawl = phase 120)
- `SfoxClient.get_trades(...)` (:303) — single-page, cursor exposed
- `SfoxClient.aclose()` (:347) — bounded, idempotent
- `SfoxApiError(status, detail)` — status==0 shape/transport, else HTTP status; token-scrubbed
- Structural read-only: HTTP verb hardcoded GET, only read methods exist (118 WR-03); a write-surface grep gate test already exists from 118 (see STATE.md: "SfoxClient write-surface grep gates").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: read_sfox_account — 3-leg read pull, read-only asserted, fail-loud</name>
  <files>analytics-service/services/sfox_read.py, analytics-service/tests/test_sfox_read.py</files>
  <read_first>analytics-service/services/sfox_client.py (the 4 read-method signatures + cursor semantics + SfoxApiError), analytics-service/tests/test_sfox_client.py (mocking conventions + the existing write-surface grep-gate pattern to mirror), 119-RESEARCH.md "Read pull (SFOX-02) — where it hooks in"</read_first>
  <behavior>
    - read_sfox_account(client) returns a dict with exactly the keys balances, trades, transactions, populated from the client's three GET reads (mocked; single-page — cursors surfaced as returned, NOT crawled)
    - empty account (all three legs []) → an honest empty result, never fabricated rows (no-invented-data rule)
    - SfoxApiError on ANY leg (test each of the three) → the exception propagates (fail-LOUD, whole read fails); no partial dict is returned
    - the module never calls a non-read method: a grep-gate style test asserts sfox_read.py references only get_balances/get_trades/get_transactions/aclose of the client surface and contains no order/withdraw/transfer/POST token (filter comment lines first — grep -v '^\s*#' before counting; a bare unfiltered gate is forbidden)
    - the ingestion-boundary read-only assertion: read_sfox_account raises TypeError/ValueError if handed an object that is not an SfoxClient (prevents a future caller smuggling a ccxt exchange with write methods through this boundary)
  </behavior>
  <action>
    Create `analytics-service/services/sfox_read.py` with an async `read_sfox_account(client:
    SfoxClient) -> dict` that awaits `get_balances()`, `get_trades()`, `get_transactions()`
    (single-page each; pass no cursor — phase 120 owns crawl orchestration) and returns
    `{"balances": ..., "trades": ..., "transactions": ...}`. Session lifecycle stays with the
    CALLER (do not aclose inside — mirrors how 119-02's validate branch owns its client).
    Read-only at the ingestion boundary (CONTEXT A1/SFOX-02): an explicit isinstance guard on
    SfoxClient + module docstring stating read-only is STRUCTURAL (GET-only adapter, no write
    surface) — never a probed-scope claim. No retry/backoff/silent-catch: SfoxApiError
    propagates untouched (fail loud; the caller decides). Keep it thin — no dailies, no
    normalization, no DB writes (all phase 120; do NOT register in ingestion _FACTORIES /
    SUPPORTED_SOURCES). TDD: failing tests first, then implement.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_sfox_read.py -q</automated>
  </verify>
  <done>All behavior cases green; sfox_read.py imports only from sfox_client (no services.exchange / ccxt / ingestion imports — assert via the grep-gate test); full sfox subset (`pytest tests/test_sfox_*.py -q`) green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Founder live prod-account read (Q3 — human_needed if no key in-session)</name>
  <action>FOUNDER-GATED (Q3 LOCKED — do not automate, never fake): the founder runs the live prod-account read per the runbook below, or the leg is explicitly recorded human_needed at /gsd:verify-work. The mocked suite (Task 1) carries the phase either way.</action>
  <done>Either a real sFOX account's balances/trades/transactions were read live via read_sfox_account (founder-confirmed), or the leg is recorded human_needed in the SUMMARY — never a mock presented as live evidence.</done>
  <what-built>The committed, mocked read pull (Task 1) + the 119-02 validate branch. The LIVE leg — reading a REAL sFOX account's balances/trades/transactions — requires a founder-held sFOX key, and an IP-whitelisted key additionally requires the phase-121 static egress (an IP-pinned key 401s from the wrong egress).</what-built>
  <how-to-verify>
    Founder runbook (mirrors the 118 SC-3 sandbox smoke):
    1. Mint/locate a READ-ONLY prod sFOX key (NOT IP-whitelisted, or wait for phase 121).
    2. From analytics-service: run a one-off exercising SfoxClient(api_key=&lt;key&gt;) →
       read_sfox_account — confirm real balances/trades/transactions return (empty lists are a
       VALID honest result for an empty account; an exception is a failure to investigate).
    3. Confirm no write endpoint was hit (the adapter cannot — structural; sFOX dashboard shows
       no orders placed).
    If NO un-pinned prod key exists in-session: record this leg as human_needed at
    /gsd:verify-work (exactly like the 118 SC-3 smoke) — a skip is NOT a pass, and the mocked
    suite carries the phase. NEVER fake a live read or substitute a mock as evidence (Q3 LOCKED).
  </how-to-verify>
  <resume-signal>Type "live-read-verified", or "defer-human-needed" to close the leg human_needed, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → api.sfox.com | live financial-account reads; responses untrusted |
| sfox_read → future phase-120 ingestion | the ingestion boundary where read-only is asserted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-12 | Elevation | write capability reaching the ingestion boundary | mitigate | isinstance(SfoxClient) guard + grep-gate test (no order/withdraw/transfer token, comment-filtered) + 118 structural GET-only adapter |
| T-119-13 | Tampering | partial/fabricated read masking an upstream failure | mitigate | any-leg SfoxApiError propagates; no partial dict; empty account returns honest empties (no invented data) |
| T-119-14 | Repudiation | mocked read passed off as the live SFOX-02 proof | mitigate | Q3 checkpoint: live leg founder-run or explicitly recorded human_needed; a skip is never a pass |
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_sfox_read.py -q` exit 0.
- `cd analytics-service && .venv/bin/python -m pytest tests/test_sfox_*.py -q` exit 0 (composed sfox surface: client + validate + read).
- Full suite before verify: `cd analytics-service && .venv/bin/python -m pytest -q` (coverage gate --cov-fail-under=80 in CI).
- Task 2 resolved as live-read-verified OR explicitly human_needed in the SUMMARY.
</verification>

<success_criteria>
SFOX-02: balances + trades + transactions readable through SfoxClient with read-only asserted
at the ingestion boundary and honest fail-loud on every path; the live-account leg is
founder-verified or honestly recorded human_needed — never fabricated.
</success_criteria>

<output>
Create `.planning/phases/119-sfox-read-adapter-key-validation/119-04-SUMMARY.md` when done.
</output>
