---
phase: 119-sfox-read-adapter-key-validation
plan: 02
type: execute
wave: 2
depends_on: ["119-01"]
files_modified:
  - analytics-service/routers/exchange.py
  - analytics-service/tests/test_sfox_validate.py
autonomous: true
requirements: [SFOX-03]

must_haves:
  truths:
    - "POST /api/validate-key with exchange='sfox' and a working Bearer token returns {valid:true, read_only:true} WITHOUT ever entering create_exchange/EXCHANGE_CLASSES"
    - "A 401/403 sFOX auth failure returns HTTP 400 with the EXACT detail 'Authentication failed. Check your API key and secret.' so the TS classifyKeyValidationError maps it to KEY_AUTH_FAILED with zero TS edits"
    - "Any other sFOX failure (transport/shape status==0, 429, 5xx) fails CLOSED (non-2xx, never valid:true) with a generic non-auth message"
    - "ccxt exchanges (binance/okx/bybit/deribit) flow through the UNCHANGED create_exchange → validate_key_permissions path"
  artifacts:
    - path: "analytics-service/routers/exchange.py"
      provides: "is_sfox branch in validate_key BEFORE create_exchange"
      contains: "sfox"
    - path: "analytics-service/tests/test_sfox_validate.py"
      provides: "unit suite: auth-ok / 401 / 403 / status==0 / 5xx / aclose-always / ccxt-regression / empty-secret"
      min_lines: 60
  key_links:
    - from: "analytics-service/routers/exchange.py validate_key"
      to: "analytics-service/services/sfox_client.py SfoxClient.get_balances"
      via: "is_sfox branch (auth + read proof)"
      pattern: "get_balances"
    - from: "analytics-service/routers/exchange.py (sfox 401/403 detail)"
      to: "src/lib/wizardErrors.ts classifyKeyValidationError → KEY_AUTH_FAILED"
      via: "the exact string 'Authentication failed. Check your API key and secret.' (classifier matches lower.includes('authentication failed'))"
      pattern: "Authentication failed"
---

<objective>
Add the non-ccxt sFOX branch to the worker's legacy `validate_key` path (SFOX-03): when
`exchange == 'sfox'`, validate via the Phase-118 `SfoxClient` (auth + read proof through
`get_balances()`), never via `create_exchange`/`EXCHANGE_CLASSES` (ccxt-typed — sfox
ValueErrors there). Honest A1 shape: `read_only=True` is asserted STRUCTURALLY (the adapter
has no write surface); auth failure maps to the exact existing AUTH_FAILED string so the TS
classifier needs no edit. `encrypt_key` needs NO branch (encrypt_credentials is
exchange-agnostic and tolerates an empty secret — RESEARCH-verified).

Purpose: without this branch every sfox key 400s "Unsupported exchange" — this is the single
validation surface all 3 Vercel key routes delegate to.
Output: the branch + a fail-closed unit suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-sfox-read-adapter-key-validation/119-CONTEXT.md
@.planning/phases/119-sfox-read-adapter-key-validation/119-RESEARCH.md
@analytics-service/routers/exchange.py
@analytics-service/services/sfox_client.py

<interfaces>
From analytics-service/services/sfox_client.py (Phase 118, verified):
- `SFOX_PROD_BASE_URL = "https://api.sfox.com"` (:51), `SFOX_SANDBOX_BASE_URL` (:52)
- `class SfoxApiError(RuntimeError)` (:82) — `__init__(self, status: int, detail: str)`; `status==0` = shape/transport violation, real int = HTTP status (401/403 = auth). Messages are token-scrubbed (118 T-118-01).
- `class SfoxClient` (:96) — `__init__(self, api_key, base_url=SFOX_PROD_BASE_URL, ...)` with an explicit per-request `proxy=` seam (do NOT wire the proxy here — phase 121); `async get_balances() -> list[dict]` (:261); `async aclose()` (:347, bounded + idempotent).

From analytics-service/services/exchange.py:
- `validate_key_permissions` (:895) — the ccxt path; its AUTH_FAILED arm emits the exact string `"Authentication failed. Check your API key and secret."` (:1012).

From analytics-service/models/schemas.py:
- `ValidateKeyRequest.exchange` is a plain `str` (NOT a Literal) — no schema change needed for the legacy validate/encrypt path.

From src/lib/wizardErrors.ts (READ-ONLY consumer — no edit in this plan):
- `classifyKeyValidationError` (:844) matches `lower.includes("authentication failed") || lower.includes("invalid_credentials")` → `{code:"KEY_AUTH_FAILED", status:400}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: is_sfox branch in validate_key — auth+read proof, fail-closed, honest shape</name>
  <files>analytics-service/routers/exchange.py, analytics-service/tests/test_sfox_validate.py</files>
  <read_first>analytics-service/routers/exchange.py (validate_key :27–:72 — the exact error-shape conventions: 400 for user-cause, 500 for unexpected, aclose in finally), analytics-service/services/sfox_client.py (SfoxClient init/get_balances/aclose + SfoxApiError semantics), analytics-service/tests/test_sfox_client.py (existing mocking conventions for SfoxClient), 119-RESEARCH.md "Worker validate branch (SFOX-03)"</read_first>
  <behavior>
    - sfox + get_balances succeeds (even []) → 200 {"valid": true, "read_only": true}; create_exchange never called (assert via monkeypatch sentinel)
    - sfox + SfoxApiError(status=401) → HTTPException 400, detail EXACTLY "Authentication failed. Check your API key and secret."
    - sfox + SfoxApiError(status=403) → same 400 + exact string
    - sfox + SfoxApiError(status=0) (shape/transport) → 500 fail-closed, detail does NOT contain "authentication failed" (must not mislabel a transport blip as a bad key)
    - sfox + SfoxApiError(status=429 or 500) → 500 fail-closed, never {"valid": true}
    - client.aclose() awaited on EVERY path above (success + all failures) — assert on the mock
    - exchange="binance" still calls create_exchange → validate_key_permissions (ccxt path regression: branch placement does not perturb ccxt flow)
    - sfox request with api_secret="" is accepted by the branch (SfoxClient takes only api_key; secret ignored) — the Q1 carve-out's worker-side contract
  </behavior>
  <action>
    Per CONTEXT locked decision "Worker validate/encrypt — the non-ccxt branch (SFOX-03)" and
    RESEARCH's branch spec: in `validate_key` (routers/exchange.py:27), intercept BEFORE the
    `create_exchange` call: when `req.exchange == "sfox"`, construct
    `SfoxClient(api_key=req.api_key, base_url=SFOX_PROD_BASE_URL)` (no proxy — phase 121),
    await `get_balances()` as the auth + read proof, return `{"valid": True, "read_only": True}`.
    `read_only=True` is the A1 STRUCTURAL assertion (the adapter has no order/withdraw/transfer
    surface — 118 WR-03); document that in the branch docstring/comment and NEVER emit copy
    claiming an observed/probed read-only scope (sFOX has no per-key scope endpoint — no
    {read, trade, withdraw} triple, ever). Error mapping: `SfoxApiError` with status 401/403 →
    `HTTPException(400, ...)` reusing the EXACT string from services/exchange.py:1012
    ("Authentication failed. Check your API key and secret.") — Claude's discretion whether to
    hoist that literal into a module-level constant in services/exchange.py imported by the
    router (preferred: single source) or duplicate it with a test pinning both sites equal.
    Any other SfoxApiError (status==0 shape/transport, 429, 5xx) → `HTTPException(500,
    "Key validation failed. Please check your credentials.")` matching the existing unexpected-
    failure convention (:62) — fail CLOSED, log via `logger.exception` per the Phase-18
    observability pattern. `await client.aclose()` in a `finally`. Whether the branch is inline
    or a small `_validate_sfox_key` helper is Claude's discretion (CONTEXT). Do NOT touch
    `encrypt_key` (no branch needed — encrypt_credentials is exchange-agnostic) and do NOT
    modify the ccxt path below the branch. TDD: write tests/test_sfox_validate.py failing
    first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_sfox_validate.py -q</automated>
  </verify>
  <done>All behavior cases green; the branch sits above create_exchange; no EXCHANGE_CLASSES/create_exchange reference for sfox; the 401/403 detail is byte-identical to the :1012 string.</done>
</task>

<task type="auto">
  <name>Task 2: Composed-state regression sweep — ccxt path + encrypt path untouched</name>
  <files>analytics-service/tests/test_sfox_validate.py</files>
  <read_first>analytics-service/services/encryption.py (encrypt_credentials :57 — verify empty-secret tolerance), existing validate-key tests (grep tests/ for "validate-key" or "validate_key") to confirm none went red</read_first>
  <action>
    Add the composed-state assertions: (a) a test that `encrypt_credentials(api_key, "", None, kek)`
    round-trips (empty secret JSON-serializes — the sfox storage shape from Q1: token stored as
    api_key, empty secret); (b) if the AUTH string was hoisted to a constant, a test asserting
    the constant equals the literal the TS classifier matches (guards against a future rewording
    silently breaking KEY_AUTH_FAILED classification cross-language). Then run the FULL existing
    exchange-router/validate test surface to prove zero regression: `pytest tests/ -q -k
    "validate or exchange or encrypt"` and fix nothing silently — any red is investigated, not
    skipped (fail loud).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/ -q -k "sfox or validate or exchange or encrypt"</automated>
  </verify>
  <done>Empty-secret encrypt round-trip proven; AUTH-string pin test present; the touched-surface pytest selection exits 0 with no skipped-silently reds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel routes → worker /api/validate-key | untrusted exchange + credentials cross into the worker |
| worker → api.sfox.com | Bearer token leaves the boundary; responses are untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-04 | Spoofing | is_sfox branch auth proof | mitigate | get_balances() as live auth proof; 401/403 → fail-closed 400; never valid:true on any error |
| T-119-05 | Elevation | trade-enabled sfox key passes validate (no scope endpoint) | accept | A1 founder-accepted: structural no-write adapter + founder mints read-only key; copy never claims a probed scope |
| T-119-06 | Info disclosure | Bearer token in errors/logs | mitigate | SfoxApiError is scrub-by-value (118); branch reuses the generic AUTH string, never echoes credentials; logger.exception logs class+message only |
| T-119-07 | Tampering | ccxt path perturbed by the branch | mitigate | branch placement BEFORE create_exchange only for exchange=='sfox'; explicit binance regression test |
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_sfox_validate.py -q` exit 0.
- `cd analytics-service && .venv/bin/python -m pytest tests/ -q -k "sfox or validate or exchange or encrypt"` exit 0.
- `grep -n "sfox" analytics-service/routers/exchange.py` shows the branch ONLY in validate_key (not encrypt_key, not fetch_trades).
</verification>

<success_criteria>
SFOX-03 (worker leg): the worker validate/encrypt path accepts sfox via SfoxClient with the
honest structural read-only shape, auth failures map to KEY_AUTH_FAILED through the existing
classifier string with zero TS edits, every non-auth failure fails closed, and the ccxt path
is byte-for-byte behaviorally unchanged.
</success_criteria>

<output>
Create `.planning/phases/119-sfox-read-adapter-key-validation/119-02-SUMMARY.md` when done.
</output>
