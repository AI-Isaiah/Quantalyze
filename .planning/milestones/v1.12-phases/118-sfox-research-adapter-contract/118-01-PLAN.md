---
phase: 118-sfox-research-adapter-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/sfox_client.py
  - analytics-service/tests/test_sfox_client.py
autonomous: true
requirements: [SFOX-01]

must_haves:
  truths:
    - "SfoxClient constructs Bearer-auth requests (Authorization: Bearer <key>) against prod https://api.sfox.com or sandbox https://api.staging.sfox.com, selected by ctor base_url"
    - "An explicit optional proxy ctor arg is threaded into every aiohttp request call (never trust_env / HTTPS_PROXY pickup)"
    - "Exactly four read methods exist — get_balances, get_transactions, get_trades, get_balance_history — hitting the exact documented endpoint paths; no order/withdraw/transfer capability exists on the class"
    - "The mocked-aiohttp contract unit suite passes in CI with zero network and zero credentials"
  artifacts:
    - path: "analytics-service/services/sfox_client.py"
      provides: "Non-ccxt read-only sFOX adapter (aiohttp, Bearer, rate-gated, explicit-proxy seam, bounded aclose)"
      contains: "class SfoxClient"
      min_lines: 120
    - path: "analytics-service/tests/test_sfox_client.py"
      provides: "Pure-unit contract test (auth header shape, endpoint URLs, read-only surface, pagination cursors, proxy plumbing, rate gate, secret-scrubbed errors)"
      min_lines: 100
  key_links:
    - from: "analytics-service/services/sfox_client.py"
      to: "aiohttp.ClientSession.request"
      via: "single _request chokepoint passing proxy= kwarg explicitly"
      pattern: "proxy"
    - from: "analytics-service/tests/test_sfox_client.py"
      to: "analytics-service/services/sfox_client.py"
      via: "direct import + AsyncMock patch of the session request seam"
      pattern: "from services.sfox_client import"
---

<objective>
Implement the SFOX-01 adapter contract: a custom, non-ccxt, read-only async `SfoxClient`
in `analytics-service/services/sfox_client.py`, proven by a pure-unit contract test that
runs green in CI with no network or credentials.

Purpose: Phase 118's SC-2 — the adapter contract exists and coexists BESIDE the ccxt-typed
`EXCHANGE_CLASSES` seam without pretending sFOX is ccxt. Phases 119–121 build on this exact
surface (key validation, equity reconstruction, static-IP proxy wiring).
Output: `sfox_client.py` + `test_sfox_client.py`, both committed, unit suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/118-sfox-research-adapter-contract/118-RESEARCH.md
@.planning/phases/118-sfox-research-adapter-contract/118-CONTEXT.md
@analytics-service/services/exchange.py
@analytics-service/services/redact.py

<interfaces>
<!-- Extracted from the codebase 2026-07-18. Executor uses these directly — no exploration needed. -->

From analytics-service/services/exchange.py (the ccxt seam SfoxClient sits BESIDE, never inside):
- `EXCHANGE_CLASSES: dict[str, type]` at line 784 — ccxt classes only (binance/okx/bybit/deribit). DO NOT ADD sfox.
- `def create_exchange(exchange_name, api_key, api_secret, passphrase=None) -> ccxt.Exchange` at ~line 792 — returns ccxt.Exchange; SfoxClient must NOT route through it.
- `_ACLOSE_TIMEOUT_S = float(os.getenv("EXCHANGE_CLOSE_TIMEOUT_S", "10"))` + `async def aclose_exchange(exchange)` at line 830 — the bounded-teardown model: close is wrapped so a hung teardown degrades to a logged leak instead of wedging the sequential worker loop.

From analytics-service/services/redact.py:
- `def scrub_freeform_string(s: Any) -> Any` at line 198 — the secret-scrub helper for anything that reaches logs/exception messages.

From analytics-service/pytest.ini:
- `asyncio_mode = auto` — async test functions need no decorator.

From analytics-service/requirements.txt:
- `aiohttp==3.14.1` already vendored. `aioresponses` is NOT vendored — unit tests must mock via `unittest.mock.AsyncMock` / monkeypatch on the session request seam, NOT by installing a new package (RESEARCH Package Legitimacy Audit: no new install).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SfoxClient core — ctor, Bearer auth, proxy seam, get_balances, fail-loud errors + contract tests</name>
  <files>analytics-service/services/sfox_client.py, analytics-service/tests/test_sfox_client.py</files>
  <read_first>
    - .planning/phases/118-sfox-research-adapter-contract/118-RESEARCH.md — "The SfoxClient Adapter Contract" section (Auth, Base URLs, Endpoint Set, rate limits, aiohttp session/proxy seam, Dispatch seam) — every endpoint path, param name, and header format comes from here, not memory
    - analytics-service/services/exchange.py lines 770–870 — EXCHANGE_CLASSES, create_exchange, _ACLOSE_TIMEOUT_S, aclose_exchange (the bounded-close model)
    - analytics-service/services/redact.py line 198 — scrub_freeform_string signature
    - analytics-service/tests/test_deribit_ground_truth.py header docstring — the project's regression-gate test-documentation style (WHY each case matters, Rule 9)
    - analytics-service/pytest.ini — asyncio_mode
  </read_first>
  <behavior>
    - Test: ctor accepts (api_key: str, base_url: str = "https://api.sfox.com", proxy: str | None = None); base_url="https://api.staging.sfox.com" override is honored in built request URLs
    - Test: every outgoing request carries header exactly `Authorization: Bearer <api_key>` — asserted on the AsyncMock'd session request call kwargs (tests the WIRING into aiohttp, not a helper in isolation)
    - Test: proxy="http://1.2.3.4:8888" is passed as the `proxy=` kwarg on the session request call; proxy omitted → proxy=None passed (explicit seam; NEVER trust_env). This is the phase-121 seam — per RESEARCH Pitfall 2, aiohttp silently ignores HTTPS_PROXY without it
    - Test: `await client.get_balances()` issues GET to `{base_url}/v1/user/balance` and returns the parsed JSON list
    - Test: read-only surface — SfoxClient has NO attribute named create_order, place_order, cancel_order, withdraw, or transfer (read-only enforced by construction, per SFOX-01 / financial-action safety)
    - Test: non-2xx response raises a dedicated `SfoxApiError` carrying the HTTP status (401 must be distinguishable for phase 119's KEY_AUTH_FAILED mapping); `str(exc)` NEVER contains the api_key (assert the literal key is absent — T-118-01)
  </behavior>
  <action>
    Write the failing contract tests in analytics-service/tests/test_sfox_client.py first, then implement
    analytics-service/services/sfox_client.py to green them. Implementation directives (values per RESEARCH,
    cited to docs.sfox.com and live-401-confirmed 2026-07-18):

    - Module: a new standalone file in analytics-service/services/. It must NOT import from or modify
      exchange.py, and NOTHING is added to EXCHANGE_CLASSES (locked decision: sFOX is not ccxt; the
      is_sfox dispatch-seam selection function is phase 119, not here).
    - Class `SfoxClient` with module-level constants SFOX_PROD_BASE_URL = "https://api.sfox.com" and
      SFOX_SANDBOX_BASE_URL = "https://api.staging.sfox.com" so later phases import the canonical URLs.
    - Ctor stores api_key, base_url (strip a trailing slash), proxy. The aiohttp ClientSession is created
      lazily on first request and OWNED by the client (never trust_env=True).
    - ALL HTTP goes through one private `_request(method, path, params)` chokepoint that: builds
      f"{base_url}{path}", sets headers {"Authorization": f"Bearer {api_key}"}, passes proxy=self._proxy
      explicitly on session.request, awaits the rate gate (Task 2 wires the per-endpoint intervals; stub a
      no-op gate hook now so Task 2 extends rather than rewrites), and fail-loud parses: non-2xx raises
      SfoxApiError(status, scrubbed detail) — run any response text destined for the message through
      services.redact.scrub_freeform_string and never place the Authorization header or api_key in it.
      Non-JSON body on a 2xx also raises (no invented data).
    - `async def get_balances(self) -> list[dict]` → GET /v1/user/balance (no params; current snapshot).
      A non-list payload raises SfoxApiError (fail-loud, never coerce).
    - `async def aclose(self)` → bounded close of the owned session mirroring aclose_exchange discipline:
      wrap session.close() in asyncio.wait_for with a module-level SFOX_CLOSE_TIMEOUT_S (default 10, env
      override fine); on timeout log-and-degrade rather than hang (Sentry "Unclosed session" class; the
      sequential-worker wedge is the v1.11 FLIP failure mode). aclose must be idempotent.
    - NO order/withdraw/transfer/trade-placement methods of any kind exist on the class. Read-only by
      construction (T-118-02).
    - Unit tests patch aiohttp.ClientSession.request (unittest.mock.AsyncMock returning a stub response
      object with status + json()/text() awaitables) or monkeypatch the session attribute on the client —
      NO aioresponses install (not vendored; no new package surface per RESEARCH audit). Follow the
      test_deribit_ground_truth.py docstring style: each test states WHY it gates a real regression.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_sfox_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - analytics-service/services/sfox_client.py exists, defines class SfoxClient with exactly one auth
      scheme: header `Authorization: Bearer <key>`
    - Base URLs are exactly https://api.sfox.com (default) and https://api.staging.sfox.com (override)
    - get_balances hits exactly `/v1/user/balance`
    - proxy ctor arg lands as the proxy= kwarg on the aiohttp request call (asserted in a test)
    - grep -E '^\s*(async )?def (create_order|place_order|cancel_order|withdraw|transfer)' analytics-service/services/sfox_client.py exits 1 (no write surface)
    - `python -m pytest tests/test_sfox_client.py -x -q` exits 0 with no network access and no env credentials
  </acceptance_criteria>
  <done>Contract tests green in CI conditions (offline, credential-free); auth header, base-URL switch, proxy threading, read-only surface, and scrubbed fail-loud errors all pinned by tests that fail if the wiring is neutered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Paginated read methods (transactions/trades/balance_history) + per-endpoint rate gate + tests</name>
  <files>analytics-service/services/sfox_client.py, analytics-service/tests/test_sfox_client.py</files>
  <read_first>
    - .planning/phases/118-sfox-research-adapter-contract/118-RESEARCH.md — "Endpoint Set" table (exact param names: from/to/limit/after/offset/types; page_size/last_seen_id; start_date/end_date/interval) and "Rate limits (STRICT)" section (transactions 1 req/10s)
    - analytics-service/services/sfox_client.py — the Task 1 _request chokepoint and rate-gate hook being extended
    - analytics-service/tests/test_sfox_client.py — Task 1 test harness/fixtures to reuse
  </read_first>
  <behavior>
    - Test: `get_transactions(from_ms=None, to_ms=None, limit=250, after=None, offset=None, types=None)` issues GET `{base}/v1/account/transactions` with exactly the wire param names `from`,`to`,`limit`,`after`,`offset`,`types` (only those provided are sent); limit > 1000 raises ValueError (documented max 1000); returns parsed list
    - Test: `get_trades(page_size=100, last_seen_id=None)` issues GET `{base}/v1/account/trades` with `page_size`/`last_seen_id` params and unwraps the documented `{data:[...]}` envelope to the inner list; a missing `data` key raises SfoxApiError (fail-loud)
    - Test: `get_balance_history(start_date_ms, end_date_ms=None, interval=86400)` issues GET `{base}/v1/account/balance/history` with `start_date` REQUIRED (positional; the docs mark it required), `end_date` optional, `interval` in {3600, 86400} else ValueError; unwraps `{data:[{timestamp, usd_value}]}` to the inner list
    - Test: cursor plumbing — passing after="12345" / last_seen_id="67890" lands verbatim in the request params (phase 120 drives crawls with these; single-page reads only in this phase, NO auto-crawl loop)
    - Test: rate gate — two back-to-back get_transactions calls enforce a >=10s min-interval (transactions endpoint) and other endpoints enforce a smaller default interval; asserted by monkeypatching the time source and asyncio.sleep (capture requested sleep durations), so the test runs in milliseconds
  </behavior>
  <action>
    Extend SfoxClient with the three remaining read methods and the real rate gate. Directives:

    - Wire params exactly as sFOX documents them (RESEARCH Endpoint Set table): the Python signature may
      use from_ms/to_ms (from is a keyword) but the QUERY params sent must be `from`/`to`. Never invent
      param names.
    - Rate gate: replace the Task 1 no-op hook with a per-endpoint-path min-interval gate (token-bucket /
      last-monotonic-timestamp + asyncio.sleep of the remainder) inside _request. Intervals: a module-level
      map with "/v1/account/transactions" -> 10.0 seconds (docs: 1 req/10s) and a conservative default
      (1.0s) for the other authed endpoints ("treat all authed endpoints as tightly rate-limited",
      analogous to ccxt enableRateLimit=True). Serialize, never parallel-fan-out (RESEARCH Pitfall 3 /
      FLIPRETRY-01: an unbounded crawl on the sequential worker loop is the v1.11 wedge).
    - Deliberately NO auto-pagination/crawl-all loop in this phase — expose the cursors (after,
      last_seen_id) and let phase 120 own crawl orchestration with asyncio.wait_for bounds. Document this
      boundary in the module docstring so phase 120 doesn't misread single-page reads as a bug.
    - Envelope handling: trades and balance_history unwrap `data`; transactions and balances are bare
      arrays per docs. Absent/degenerate shapes raise SfoxApiError — no invented data, ever.
    - Tests reuse the Task 1 AsyncMock seam; inject a fake monotonic clock + captured asyncio.sleep so the
      10s gate is asserted without real sleeping. Each test docstring states the regression it pins
      (e.g. "429-wedge prevention: the transactions gate IS the FLIPRETRY lesson at the client layer").
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_sfox_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Endpoint paths in sfox_client.py are exactly /v1/account/transactions, /v1/account/trades, /v1/account/balance/history (plus Task 1's /v1/user/balance) — nothing else
    - Wire param names match the docs exactly: from, to, limit, after, offset, types / page_size, last_seen_id / start_date, end_date, interval
    - limit>1000 and interval not in {3600,86400} raise ValueError before any request is issued
    - Rate-gate test proves a second immediate transactions call requests a sleep >= ~10s (patched clock; suite still completes in seconds)
    - `python -m pytest tests/test_sfox_client.py -x -q` exits 0 offline; full file runtime < 10s
  </acceptance_criteria>
  <done>All four read methods implemented against the documented contract with pagination cursors threadable end-to-end, the strict transactions rate limit enforced in one place, and every behavior pinned by an offline unit test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → api.sfox.com / api.staging.sfox.com | Bearer credential leaves the worker; untrusted JSON payloads return |
| exception/log surface → Sentry/stdout | Anything in error messages can end up in logs or committed evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Information disclosure | SfoxApiError / logging | mitigate | Never place the Authorization header or api_key in any message; scrub response text via services.redact.scrub_freeform_string; unit test asserts key absent from str(exc) |
| T-118-02 | Tampering / Elevation | SfoxClient surface | mitigate | Read-only by construction — only the 4 read methods exist; unit test + grep gate assert no create_order/cancel_order/withdraw/transfer defs |
| T-118-03 | Denial of service | paginated pulls vs 1-req/10s limit | mitigate | Per-endpoint min-interval rate gate in _request (transactions 10s); single-page reads only — crawl orchestration deferred to phase 120 with wait_for bounds |
| T-118-04 | Spoofing (input) | untrusted sFOX JSON | mitigate | Fail-loud shape validation: non-list balances, missing `data` envelope, non-JSON 2xx all raise SfoxApiError — no invented data |
| T-118-SC | Tampering (supply chain) | package installs | accept | ZERO new packages this phase — aiohttp already vendored; RESEARCH Package Legitimacy Audit table is empty by design; unit mocking via stdlib unittest.mock |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_sfox_client.py -x -q` → exit 0, offline, no credentials
- `grep -E '^\s*(async )?def (create_order|place_order|cancel_order|withdraw|transfer)' analytics-service/services/sfox_client.py` → exit 1 (no write surface)
- `sed -n '/^EXCHANGE_CLASSES/,/^}/p' analytics-service/services/exchange.py | grep sfox` → exit 1 (ccxt dict untouched)
- `git diff --stat` shows ONLY the two files in files_modified (surgical change; exchange.py untouched)
</verification>

<success_criteria>
- SC-2 (adapter contract) satisfied: SfoxClient exists beside — not inside — the ccxt seam, with Bearer auth, both base URLs, all four documented read endpoints, explicit proxy seam, strict rate gate, bounded close, and a green offline contract suite proving each wire-level behavior.
</success_criteria>

<output>
Create `.planning/phases/118-sfox-research-adapter-contract/118-01-SUMMARY.md` when done
</output>
