---
phase: 118-sfox-research-adapter-contract
plan: 02
type: execute
wave: 2
depends_on: [118-01]
files_modified:
  - analytics-service/tests/test_sfox_client_live.py
autonomous: true
requirements: [SFOX-01]

must_haves:
  truths:
    - "A live sandbox smoke test exists that, given SFOX_SANDBOX_KEY, authenticates against https://api.staging.sfox.com and asserts get_balances() returns a real (possibly empty) list payload"
    - "Without SFOX_SANDBOX_KEY the test SKIPS with a verbose founder-gated reason — CI stays green and the skip is never a silent fake pass (SC-3 remains human_needed until the founder runs it)"
    - "The full analytics-service suite is green with the new sFOX files present, and EXCHANGE_CLASSES remains ccxt-only"
  artifacts:
    - path: "analytics-service/tests/test_sfox_client_live.py"
      provides: "skipIf-gated SC-3 empirical smoke test (founder-credential-gated)"
      contains: "skipif"
      min_lines: 30
  key_links:
    - from: "analytics-service/tests/test_sfox_client_live.py"
      to: "https://api.staging.sfox.com"
      via: "SfoxClient(base_url=SFOX_SANDBOX_BASE_URL) real network call when key present"
      pattern: "api\\.staging\\.sfox\\.com|SFOX_SANDBOX_BASE_URL"
    - from: "analytics-service/tests/test_sfox_client_live.py"
      to: "analytics-service/services/sfox_client.py"
      via: "imports the plan-01 adapter"
      pattern: "from services.sfox_client import"
---

<objective>
Commit the SC-3 empirical gate: a founder-gated live smoke test against the sFOX sandbox
(`api.staging.sfox.com`) that proves the plan-01 contract against reality — auth succeeds AND
at least one read endpoint returns a real payload — plus the phase-close regression sweep.

Purpose: CONTEXT locks "Docs-only is NOT green." The committed test + contract carry the phase;
the live green itself is FOUNDER-GATED (sandbox key minted at beta.sfox.com) and the code must
NEVER fake a pass. skipIf keeps CI green without the credential.
Output: `test_sfox_client_live.py` committed; full analytics-service suite green; phase closes
with SC-3 marked human_needed until the founder supplies `SFOX_SANDBOX_KEY` and runs it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/118-sfox-research-adapter-contract/118-RESEARCH.md
@.planning/phases/118-sfox-research-adapter-contract/118-VALIDATION.md
@analytics-service/tests/test_csv_daily_returns_dualaxis_live.py

<interfaces>
<!-- Contract created by plan 118-01 — use directly, no exploration needed. -->

From analytics-service/services/sfox_client.py (plan 118-01):
- `SfoxClient(api_key: str, base_url: str = SFOX_PROD_BASE_URL, proxy: str | None = None)`
- `SFOX_SANDBOX_BASE_URL = "https://api.staging.sfox.com"` (module constant)
- `await client.get_balances() -> list[dict]` — GET /v1/user/balance; raises SfoxApiError (carries HTTP status; 401 distinguishable) on auth failure
- `await client.aclose()` — bounded, idempotent session teardown

From analytics-service/tests/test_csv_daily_returns_dualaxis_live.py (the live-test convention):
- `pytestmark = pytest.mark.skipif(not os.environ.get("..."), reason="<verbose, Rule-12 fail-loud reason>")` — module-level gate, verbose reason mandatory ("silent skips are a fail-loud violation")

From analytics-service/pytest.ini:
- `asyncio_mode = auto` — async tests need no decorator
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: skipIf-gated live sandbox smoke test (SC-3, founder-gated)</name>
  <files>analytics-service/tests/test_sfox_client_live.py</files>
  <read_first>
    - .planning/phases/118-sfox-research-adapter-contract/118-RESEARCH.md — "Pattern: skipIf-gated live smoke test", "Base URLs" table, Open Question 3 (fresh sandbox may have zero balances → target get_balances, which returns an array even when empty), and the Security Domain V7 row (never print the token)
    - .planning/phases/118-sfox-research-adapter-contract/118-VALIDATION.md — Manual-Only Verifications row (env var name SFOX_SANDBOX_KEY; expected green = auth OK + non-error payload)
    - analytics-service/tests/test_csv_daily_returns_dualaxis_live.py lines 1–50 — the pytestmark skipif + verbose-reason convention to mirror
    - analytics-service/services/sfox_client.py — the plan-01 contract being smoked (import names, SfoxApiError semantics)
  </read_first>
  <action>
    Create analytics-service/tests/test_sfox_client_live.py. Directives:

    - Canonical env var: `SFOX_SANDBOX_KEY` (per 118-VALIDATION.md and the phase scope; note: 118-RESEARCH.md's
      sketch used SFOX_SANDBOX_API_KEY — standardize on SFOX_SANDBOX_KEY and say so in the module docstring
      so the founder runbook has one name).
    - Module-level gate mirroring the existing live-test convention:
      `pytestmark = pytest.mark.skipif(not os.environ.get("SFOX_SANDBOX_KEY"), reason=...)` with a VERBOSE
      reason: founder-gated SC-3 smoke; sandbox key minted at beta.sfox.com (separate from prod keys);
      support@sfox.com funds sandbox accounts; skipping keeps CI green but the phase's SC-3 stays
      human_needed until this runs green.
    - One async smoke test: construct `SfoxClient(api_key=os.environ["SFOX_SANDBOX_KEY"],
      base_url=SFOX_SANDBOX_BASE_URL, proxy=None)`; in try/finally awaiting `aclose()`, call
      `get_balances()` and assert `isinstance(balances, list)`. An EMPTY list is a PASS (auth succeeded +
      real payload returned — RESEARCH Open Question 3: a fresh sandbox account may hold nothing; the
      SC-3 bar is auth + real payload, not non-empty data). A raised SfoxApiError (e.g. 401) is a FAIL —
      let it propagate, never catch-and-pass.
    - Absolutely NO mocking, NO fixtures that stub the network, NO fallback path in this file — a real
      request or a skip, nothing in between (locked decision: the code must not fake a pass).
    - Never print/log the key or the raw Authorization header; on assertion messages include only the
      payload TYPE and length, not contents (mirror the deribit_ground_truth sanitization posture — this
      file's output may be pasted into phase evidence).
    - Optionally (still within SC-3 scope, cheap and useful for A1): after the balances assertion, add a
      second test — also skipif-gated by the same pytestmark — calling
      `get_balance_history(start_date_ms=<2015-01-01 in ms>)` and asserting it returns a list; docstring
      notes it doubles as the first empirical probe of historical depth (RESEARCH Assumption A1). It must
      not weaken the primary balances assertion.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_sfox_client_live.py -q -rs</automated>
  </verify>
  <acceptance_criteria>
    - Without SFOX_SANDBOX_KEY set: pytest exits 0 and reports the test(s) as SKIPPED with the verbose founder-gated reason visible under -rs (never pass, never fail, never silent)
    - File contains the exact sandbox base URL https://api.staging.sfox.com (via SFOX_SANDBOX_BASE_URL import or literal)
    - File contains zero mocking imports (no unittest.mock, no monkeypatch of the request seam)
    - `grep -n "SFOX_SANDBOX_KEY" analytics-service/tests/test_sfox_client_live.py` matches (canonical env name)
    - The key value never appears in any assert/log/print statement
  </acceptance_criteria>
  <done>SC-3's empirical gate is committed and honest: skips loudly without the founder credential, and with it performs a real Bearer-authed read against api.staging.sfox.com whose failure fails the test. Phase verification records SC-3 as human_needed (founder runbook: mint sandbox key at beta.sfox.com → export SFOX_SANDBOX_KEY → `cd analytics-service && python -m pytest tests/test_sfox_client_live.py -q`).</done>
</task>

<task type="auto">
  <name>Task 2: Phase-gate regression sweep — full suite green + seam-purity grep gates</name>
  <files>analytics-service/tests/test_sfox_client_live.py</files>
  <read_first>
    - .planning/phases/118-sfox-research-adapter-contract/118-VALIDATION.md — Sampling Rate (full suite per wave; coverage gate --cov-fail-under=80 already enforced by the existing pytest config)
    - analytics-service/services/exchange.py lines 780–800 — EXCHANGE_CLASSES dict literal (the purity gate target)
  </read_first>
  <action>
    Close the phase with the composed-state checks (P115 lesson: verify the FULLY-COMPOSED state, not just
    each task in isolation). Run and record:

    1. Full analytics-service suite: `cd analytics-service && python -m pytest -q` — must exit 0 with the
       new sfox files present (live test skipped, unit tests green, existing coverage gate honored).
    2. Seam purity: `sed -n '/^EXCHANGE_CLASSES/,/^}/p' analytics-service/services/exchange.py | grep sfox`
       must exit 1 — sFOX never entered the ccxt-typed dict (locked decision; grep scoped to the dict
       literal so header comments can't self-invalidate the gate).
    3. Read-only surface (repo-truth, belt-and-braces over the unit test):
       `grep -E '^\s*(async )?def (create_order|place_order|cancel_order|withdraw|transfer)' analytics-service/services/sfox_client.py`
       must exit 1.
    4. Skip honesty: `cd analytics-service && python -m pytest tests/test_sfox_client_live.py -q -rs` output
       shows SKIPPED with the founder-gated reason (proves CI-green does not mean SC-3 green).

    If any check fails, fix in the owning file (exchange.py must remain UNTOUCHED by this phase — a needed
    change there means a planning error; stop and surface it rather than editing). Record all four outcomes
    verbatim in the SUMMARY so /gsd:verify-work can confirm SC-3's human_needed status without re-deriving it.
    This task modifies test_sfox_client_live.py only if check 4 exposes a defect in Task 1's gate.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Full analytics-service pytest suite exits 0 (existing --cov-fail-under=80 gate included)
    - EXCHANGE_CLASSES grep gate exits 1 (dict is still binance/okx/bybit/deribit only)
    - Write-surface grep gate exits 1
    - The -rs run shows the live smoke test as SKIPPED with its verbose reason
    - git status shows exchange.py unmodified
  </acceptance_criteria>
  <done>Phase 118 composed state verified: unit contract green in CI conditions, live gate committed and honestly skipping, ccxt seam pristine, full suite + coverage gate green. SC-1 (RESEARCH.md GO verdict) and SC-2 (contract) closed; SC-3 recorded human_needed pending the founder's sandbox-key run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → api.staging.sfox.com | Real Bearer credential used when SFOX_SANDBOX_KEY is set |
| test output → CI logs / phase evidence | Assertion messages and skip reasons may be committed as evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-05 | Information disclosure | sandbox key in test output | mitigate | Env-only credential; never printed/asserted; assertion messages carry payload type/length only |
| T-118-06 | Repudiation (fake green) | skipIf gate | mitigate | Verbose Rule-12 skip reason; no mocking/fallback path in the live file; SfoxApiError propagates as FAIL; SUMMARY records SC-3 human_needed explicitly |
| T-118-07 | Information disclosure | committing the credential | mitigate | Key exists only as env var; no tracked file ever contains it (mirror deribit_ground_truth runbook) |
| T-118-SC | Tampering (supply chain) | package installs | accept | Zero new packages this phase (RESEARCH audit table empty) |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest -q` → exit 0 (full suite, coverage gate included)
- `cd analytics-service && python -m pytest tests/test_sfox_client_live.py -q -rs` → SKIPPED with verbose founder-gated reason when SFOX_SANDBOX_KEY unset
- `sed -n '/^EXCHANGE_CLASSES/,/^}/p' analytics-service/services/exchange.py | grep sfox` → exit 1
- SUMMARY explicitly records: SC-3 = human_needed; founder runbook (beta.sfox.com key → export SFOX_SANDBOX_KEY → pytest live file)
</verification>

<success_criteria>
- SC-3's empirical gate is committed, honest, and founder-runnable; CI is green via a loud skip, never a fake pass; the phase's composed state (contract + tests + pristine ccxt seam + full suite) is verified. Phase closes with SC-1/SC-2 done and SC-3 human_needed until the founder's sandbox-key run flips it green.
</success_criteria>

<output>
Create `.planning/phases/118-sfox-research-adapter-contract/118-02-SUMMARY.md` when done
</output>
