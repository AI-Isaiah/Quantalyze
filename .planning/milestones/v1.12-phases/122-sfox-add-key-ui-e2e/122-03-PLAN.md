---
phase: 122-sfox-add-key-ui-e2e
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/security/page.tsx
  - src/app/(marketing)/security/page.test.tsx
autonomous: true
requirements: [SFOX-08]
tags: [sfox, onboarding-copy, security-page, frontend]

must_haves:
  truths:
    - "/security#sfox-readonly resolves to a SubAnchor block titled sFOX inside the readonly-key section (deribit precedent)"
    - "The guide instructs minting a READ-ONLY sFOX API token and whitelisting the static egress IP — WITHOUT hardcoding any IP value"
    - "The copy states its own limits per F3: sFOX exposes no per-key scope endpoint (we cannot probe scope); the adapter is structurally read-only (no order/withdraw path)"
    - "No copy claims a verified read-only scope for sfox"
  artifacts:
    - path: "src/app/(marketing)/security/page.tsx"
      provides: "SubAnchor id sfox-readonly with the F3-honest setup steps"
      contains: "sfox-readonly"
    - path: "src/app/(marketing)/security/page.test.tsx"
      provides: "render tests pinning the anchor, the honest copy, and the absence of a false-verified claim"
      contains: "sfox-readonly"
  key_links:
    - from: "src/app/(marketing)/security/page.tsx #sfox-readonly"
      to: "ConnectKeyStep setup-guide link /security#${exchange}-readonly"
      via: "anchor id contract (plan 122-02 tests the link side; this plan provides the target)"
      pattern: "id=\"sfox-readonly\""
---

<objective>
Ship the sFOX onboarding/setup guide as a /security#sfox-readonly section mirroring the deribit precedent: how to mint a READ-ONLY sFOX API token and whitelist the static egress IP (phase-121 Fly proxy; the founder fills the actual IP after 121-03 — the copy points to the existing contact channel, never a hardcoded IP), with F3-honest limits.

Purpose: SFOX-08 onboarding-copy leg. Claude's-discretion call resolved: the /security page section (deribit precedent) over wizard-inline, because (a) ConnectKeyStep's setup-guide link already derives /security#sfox-readonly with zero edits, and (b) the founder needs this runbook for their own pre-flag key mint + IP whitelist ops. It ships UNCONDITIONALLY (content-only — the offer stays flag-gated in plan 122-02; a doc section is not an offer).
Output: The sfox-readonly SubAnchor + render tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-sfox-add-key-ui-e2e/122-CONTEXT.md
@DESIGN.md
@src/app/(marketing)/security/page.tsx
@src/app/(marketing)/security/page.test.tsx
</context>

<interfaces>
From src/app/(marketing)/security/page.tsx:
- Section id="readonly-key" (line 411) hosts per-exchange SubAnchor blocks: binance-readonly (420), okx-readonly (438), bybit-readonly (454), deribit-readonly (468) — each an ordered list of 3 concrete steps in DM Sans body voice.
- Section id="egress-ips" (line 498): the existing IP-allowlist contact pattern — email security@quantalyze.com for the current IP set. This is the no-hardcoded-IP precedent to reference.

From src/app/(marketing)/security/page.test.tsx:
- The deribit block tests (lines 144-172): renders #deribit-readonly titled Deribit; copy assertions against the block's textContent. Mirror this shape for sfox.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the sfox-readonly SubAnchor with F3-honest setup steps</name>
  <files>src/app/(marketing)/security/page.tsx, src/app/(marketing)/security/page.test.tsx</files>
  <behavior>
    - document.getElementById("sfox-readonly") exists, titled sFOX, inside the readonly-key section.
    - Block text includes: minting a READ-ONLY token, the no-scope-endpoint limit statement, and the egress-IP whitelist step referencing the security contact for the current IP.
    - Block text does NOT contain a false-verified claim (assert the absence of scope-verification phrasing for sfox, e.g. no "we verify"/"rejected before we store" wording inside the sfox block).
    - Existing exchange blocks byte-identical (surgical change).
  </behavior>
  <action>
    Read DESIGN.md Voice rules first (declarative sentence-case; state limitations with the reason attached; active voice; DM Sans body — the SubAnchor/ol markup already carries the right classes, reuse it verbatim). Insert a SubAnchor id "sfox-readonly" title "sFOX" directly after the deribit block (line ~485), with an ordered list of three steps: (1) generate an API token in the sFOX dashboard and select read-only permissions where offered — sFOX issues a single API token, no secret; (2) the honest limit (F3, LOCKED wording basis from 122-CONTEXT specifics): sFOX keys are used read-only by our adapter — no order or withdraw path exists — and sFOX does not expose a per-key scope endpoint, so we cannot verify the scope server-side; mint a READ-ONLY token; (3) if the key is IP-restricted (recommended), whitelist our static egress IP — email security@quantalyze.com for the current IP (mirror the egress-ips section's contact pattern; NEVER hardcode an IP — the founder provisions the actual static egress in 121-03 and the contact channel stays correct through rotation). Then paste the token into the wizard. Extend page.test.tsx mirroring the deribit test block (lines 144-172): anchor exists + titled sFOX; textContent includes "read-only" and the no-scope-endpoint limit; textContent includes the whitelist/egress step; a negative assertion that the sfox block does not claim scope rejection/verification. Do not modify any other section, link, or copy on the page (the six inline links carry the WCAG persistent-underline treatment — leave untouched).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(marketing)/security/page.test.tsx" --no-file-parallelism</automated>
  </verify>
  <done>#sfox-readonly renders with the three F3-honest steps; tests pin presence, honest copy, the no-hardcoded-IP contact step, and the absence of a false-verified claim; the rest of the page is byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public web → /security page | static public content; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-08 | Repudiation/Trust | false read-only-scope claim in public copy | mitigate | F3-honest wording + negative test asserting no scope-verification claim in the sfox block |
| T-122-09 | Information disclosure | publishing the static egress IP | mitigate | no IP in copy; the security@ contact channel gates disclosure (existing egress-ips precedent) |
</threat_model>

<verification>
- npx vitest run "src/app/(marketing)/security/page.test.tsx" --no-file-parallelism — green.
- npx tsc --noEmit && npm run lint — clean.
- grep -n "sfox-readonly" across src/ shows the anchor id in page.tsx (+ the plan-122-02 link tests once both land — no coordination required now; the id string is the contract).
</verification>

<success_criteria>
- /security#sfox-readonly exists, mirrors the deribit scope-guide pattern, and carries the honest F3 copy (mint READ-ONLY, no scope endpoint, whitelist egress IP via the contact channel, no hardcoded IP).
- ConnectKeyStep's derived setup-guide href has a live target once plan 122-02 flips the card on.
</success_criteria>

<output>
Create `.planning/phases/122-sfox-add-key-ui-e2e/122-03-SUMMARY.md` when done.
</output>
