---
phase: 122-sfox-add-key-ui-e2e
plan: 04
type: execute
wave: 2
depends_on: [122-01]
files_modified:
  - e2e/helpers/seed-test-project.ts
  - e2e/sfox-badge.spec.ts
autonomous: true
requirements: [SFOX-09]
tags: [sfox, e2e, playwright, provenance, seed]

must_haves:
  truths:
    - "A seed-gated e2e spec asserts a connected sfox strategy renders the SFOX exchange tag AND the api_verified badge, with the seed fixture OWNED BY the logged-in user"
    - "The spec sweeps the applicable roles: the owner ('both' role) on the manager surfaces, the allocator surface, and the admin surface; public-demo is documented N/A (demo runs on sentinel data, no sfox fixture exists there)"
    - "Without TEST_SUPABASE_URL/TEST_SUPABASE_SERVICE_ROLE_KEY the spec skips cleanly (CI stays green pre-secrets; with the wired GH secrets it RUNS in CI)"
    - "An axe pass on the badge-bearing strategy surface reports zero violations (wizard-axe precedent applied to the new surface)"
    - "A missing SFOX-04 constraint widening on the test project fails LOUD with a message naming the phase-119 migration — never a silent skip or a confusing 23514"
  artifacts:
    - path: "e2e/helpers/seed-test-project.ts"
      provides: "seedSfoxVerifiedStrategy — strategy + strategy_verifications(source sfox, trust_tier api_verified) + api_keys(exchange sfox) owned by the seeded user"
      contains: "sfox"
    - path: "e2e/sfox-badge.spec.ts"
      provides: "role-sweep badge assertions + axe pass, seed-env skip gate"
      contains: "api_verified"
  key_links:
    - from: "e2e/sfox-badge.spec.ts"
      to: "e2e/helpers/seed-test-project.ts seedSfoxVerifiedStrategy"
      via: "beforeAll seed + afterAll cleanup"
      pattern: "seedSfoxVerifiedStrategy"
    - from: "e2e/sfox-badge.spec.ts"
      to: "ApiKeyManager SFOX tag + VerifiedBadge/TrustTierLabel"
      via: "page assertions on the rendered tag text and badge"
      pattern: "SFOX"
---

<objective>
Prove SFOX-09 end-to-end: an e2e spec (seed-gated, all-applicable-roles) asserting a connected sFOX strategy renders the SFOX 3-letter exchange tag and the api_verified provenance badge, on seed fixtures owned by the logged-in user, plus an axe sweep of the badge-bearing surface.

Purpose: SFOX-09 e2e leg (ROADMAP SC3). Runs against the plan-122-01 badge surfaces — badge rendering is UNCONDITIONAL, so this spec needs no offer-flag manipulation and works against a default (flag-OFF) build.
Output: seedSfoxVerifiedStrategy helper + e2e/sfox-badge.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-sfox-add-key-ui-e2e/122-CONTEXT.md
@e2e/helpers/seed-test-project.ts
@e2e/wizard-axe.spec.ts
@e2e/composite-onboarding.spec.ts
@.planning/phases/122-sfox-add-key-ui-e2e/122-01-SUMMARY.md
</context>

<interfaces>
From e2e/helpers/seed-test-project.ts:
- seedTestAllocator(opts?: { role?: "allocator" | "manager" | "both" }) — fresh confirmed user + explicit profile upsert; role "both" owns BOTH the allocator surfaces AND the manager wizard/edit routes with no redirect (line ~111 comment).
- seedStrategyWithHistory(opts) — strategies + strategy_analytics history (withDailyReturns opt); NO trust_tier support today (strategy_verifications is never written by any seed helper — this plan adds it).
- The deribit api_keys seed precedent at line ~903 shows the encrypted-column placeholder shape for an api_keys insert (exchange "deribit").
- cleanupStrategiesByNamePrefix for teardown.

From e2e/wizard-axe.spec.ts:
- HAS_SEED_ENV = TEST_SUPABASE_URL && TEST_SUPABASE_SERVICE_ROLE_KEY; test.skip-when-absent pattern; buildAxe() from e2e/helpers/axe.ts is the ONLY sanctioned axe factory (never redeclare rule sets locally); inline loginViaForm idiom.

From the UI (plan 122-01 outputs):
- ApiKeyManager renders exchangeIcon[key.exchange] — "SFOX" for a sfox key — on /strategies/[id]/edit.
- VerifiedBadge renders the "Verified" chip iff trust_tier === "api_verified"; trust_tier is projected from strategy_verifications by getStrategyDetail(). TrustTierLabel renders the "API verified" provenance pill on factsheet/discovery surfaces.

DB precondition (SFOX-04, Phase 119 — currently an OPEN 119 merge leg per STATE.md):
- The constraint-widening migration must be applied to the TEST project (qmnijlgmdhviwzwfyzlc) for api_keys.exchange + strategy_verifications.source to admit 'sfox'. If absent, inserts 23514.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: seedSfoxVerifiedStrategy helper</name>
  <files>e2e/helpers/seed-test-project.ts</files>
  <behavior>
    - Returns a seeded user (role "both", email/password) + strategy id, where the strategy is OWNED by that user, has enough daily history to render a detail page, carries a strategy_verifications row with source "sfox" and trust_tier "api_verified", and an api_keys row with exchange "sfox" linked to the same user.
    - On a 23514 CHECK violation mentioning exchange/source, throws a descriptive error naming the phase-119 SFOX-04 constraint-widening migration as the missing precondition on the test project — fail loud, never a bare postgres error.
  </behavior>
  <action>
    Add seedSfoxVerifiedStrategy to seed-test-project.ts, composing the existing idioms rather than duplicating them: seedTestAllocator({ role: "both" }) for the owner (one user sweeps manager + allocator surfaces), the seedStrategyWithHistory internals (or a direct call if its opts suffice) for the strategies + strategy_analytics rows with withDailyReturns true, then two service-role inserts this file has never done for verifications: a strategy_verifications row { strategy_id, source: "sfox", trust_tier: "api_verified" } (read migration 20260501055202_strategy_verifications.sql for the NOT NULL column set before writing the insert — do not guess columns) and an api_keys row with exchange "sfox" mirroring the deribit seed's encrypted-placeholder column shape at line ~903. Ensure the strategy reaches whatever visibility/status the discovery/browse surfaces require (mirror how existing discovery-spec seeds mark strategies visible; read one such call site rather than inventing status values). Wrap the sfox-bearing inserts with the 23514 fail-loud translation described in behavior. Export a cleanup path reusing cleanupStrategiesByNamePrefix + key/user deletion consistent with the file's existing teardown idioms. Verification without live DB env: the helper is exercised by the spec (Task 2); statically, tsc + playwright --list must stay green.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx playwright test e2e/sfox-badge.spec.ts --list</automated>
  </verify>
  <done>seedSfoxVerifiedStrategy exists, composes existing idioms, seeds owner-owned sfox fixtures (strategy + verification + api_key), and fails loud on the missing SFOX-04 precondition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: e2e/sfox-badge.spec.ts — role sweep + axe</name>
  <files>e2e/sfox-badge.spec.ts</files>
  <behavior>
    - Skips entirely when HAS_SEED_ENV is false (authored-not-blocking, wizard-axe pattern).
    - Owner ("both" role, logged in): /strategies/[id]/edit shows the "SFOX" tag text in the API-key card (and never the "?" fallback); the strategy detail surface shows the api_verified badge (the VerifiedBadge "Verified" chip and/or the TrustTierLabel "API verified" pill — assert whichever the surface renders, discovered from the live DOM, not assumed).
    - Allocator surface (same "both" user): the strategy's discovery/browse presentation renders the api_verified provenance treatment on the seeded sfox strategy.
    - Admin surface: an admin-elevated session sees the sfox strategy with its api_verified tier on the admin review surface (mirror how existing admin-*-axe specs elevate/seed admin access; reuse that idiom verbatim).
    - Public-demo: documented N/A in a comment — the demo runs on sentinel fixtures with no sfox strategy; asserting there would require inventing data (no-invented-data rule).
    - One axe pass via buildAxe() on the badge-bearing strategy detail page: zero violations.
  </behavior>
  <action>
    Author e2e/sfox-badge.spec.ts mirroring wizard-axe.spec.ts structure: HAS_SEED_ENV gate, loginViaForm idiom, buildAxe from e2e/helpers/axe.ts (never a local rule set), seedSfoxVerifiedStrategy in beforeAll + cleanup in afterAll. Selectors: prefer role/text queries consistent with the sibling specs (the SFOX tag is plain text inside the key-card avatar span; the badge is the "Verified" text chip / "API verified" pill). Keep every assertion against fixtures OWNED by the logged-in user (the "both"-role owner) per the project e2e seed rule; the admin leg reads the same seeded strategy through the admin surface. Note in the spec header: (a) this spec exercises the UNCONDITIONAL badge surfaces and therefore does not touch NEXT_PUBLIC_SFOX_ENABLED (a default flag-OFF build renders them); (b) the live-key wizard connect flow is founder-gated on 121-03 and is NOT in scope here (ROADMAP SC4 is covered at component level in plan 122-02); (c) CI wiring: the spec auto-runs in the existing playwright job once the seed-gated GH secrets are present — no workflow edits (grep .github/workflows to confirm the playwright job globs e2e/*.spec.ts before claiming this; if it uses an explicit list, add the spec there). Local full run (when TEST_SUPABASE_* env present): npx playwright test e2e/sfox-badge.spec.ts.
  </action>
  <verify>
    <automated>npx playwright test e2e/sfox-badge.spec.ts --list && npm run lint</automated>
  </verify>
  <done>Spec registers and lints; with seed env it seeds, sweeps owner/allocator/admin surfaces asserting SFOX tag + api_verified badge, runs the axe pass, and cleans up; without seed env it skips cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e harness → TEST Supabase project (service role) | seed writes with the TEST service-role key only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-10 | Elevation | service-role seed against the wrong project | mitigate | seed-test-project.ts already carries WR-05 prod-URL/service-key probes (line ~57) — the new helper goes through the same getAdmin() gate, never a fresh client |
| T-122-11 | Tampering | seeded api_verified rows on shared test DB | accept | test project only; cleanup in afterAll; shared-testdb concurrency flakes are a known, documented re-run path |
</threat_model>

<verification>
- npx playwright test e2e/sfox-badge.spec.ts --list — spec registers.
- npx tsc --noEmit && npm run lint — clean.
- With TEST_SUPABASE_* env: npx playwright test e2e/sfox-badge.spec.ts — green (owner + allocator + admin assertions + axe zero-violations).
- Precondition check before the first live run: verify the phase-119 SFOX-04 migration is applied on the test project (qmnijlgmdhviwzwfyzlc) — the helper's fail-loud message covers the miss.
</verification>

<success_criteria>
- SFOX-09 e2e proven: SFOX tag + api_verified badge asserted on a connected sfox strategy across owner/allocator/admin surfaces, seed fixtures owned by the logged-in user, public-demo documented N/A.
- Spec is seed-gated (CI green without secrets, RUNS with them) and axe-clean on the badge surface.
</success_criteria>

<output>
Create `.planning/phases/122-sfox-add-key-ui-e2e/122-04-SUMMARY.md` when done.
</output>
