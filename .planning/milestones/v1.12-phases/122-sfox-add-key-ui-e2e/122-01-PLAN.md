---
phase: 122-sfox-add-key-ui-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/strategy/ApiKeyManager.tsx
  - src/components/strategy/ApiKeyManager.test.tsx
  - src/components/exchanges/AllocatorExchangeManager.tsx
  - src/components/exchanges/AllocatorExchangeManager.test.tsx
  - src/components/ui/VerifiedBadge.test.tsx
autonomous: true
requirements: [SFOX-09]
tags: [sfox, exchange-badge, provenance, frontend]

must_haves:
  truths:
    - "A connected sfox api_key row renders the 3-letter mono tag SFOX (never the '?' fallback) in the ApiKeyManager key card"
    - "A connected sfox key renders a SFOX tag chip in the AllocatorExchangeManager connected-key rows (both render sites)"
    - "A sfox strategy with trust_tier api_verified renders the VerifiedBadge; csv_uploaded/self_reported/null render nothing (unchanged)"
    - "A mixed-case exchange value (e.g. 'sFOX') from either add-key client can never reach the api_keys DB insert — both call sites insert canonical lowercase"
  artifacts:
    - path: "src/components/strategy/ApiKeyManager.tsx"
      provides: "exchangeIcon map with sfox: 'SFOX' entry + canonical-lowercase exchange in fetch body and insert"
      contains: "sfox"
    - path: "src/components/exchanges/AllocatorExchangeManager.tsx"
      provides: "EXCHANGE_TAGS sfox entry + canonical-lowercase exchange in fetch body and insert"
      contains: "sfox"
    - path: "src/components/ui/VerifiedBadge.test.tsx"
      provides: "api_verified dispatch coverage for a sfox-sourced strategy"
      contains: "api_verified"
  key_links:
    - from: "src/components/strategy/ApiKeyManager.tsx"
      to: "exchangeIcon[key.exchange]"
      via: "connected-key row avatar render (line ~352)"
      pattern: "exchangeIcon\\[key\\.exchange\\]"
    - from: "src/components/exchanges/AllocatorExchangeManager.tsx"
      to: "supabase.from('api_keys').insert"
      via: "canonicalized exchange variable (NOT raw data.exchange) in handleAddKey"
      pattern: "toLowerCase"
---

<objective>
Ship the unconditional SFOX-09 badge surfaces: the 3-letter mono exchange tag `SFOX` in both connected-key tag maps (a founder-connected sfox key must render correctly BEFORE the public offer flag ever flips), prove the VerifiedBadge api_verified dispatch covers a sfox strategy, and close the F6 raw-exchange-insert class (canonical lowercase at BOTH client insert sites) so a mixed-case value can never hit the DB lowercase-only CHECK (23514) after burning a live validation probe.

Purpose: SFOX-09 (badge leg) + phase-119 deferred F6 fold-in. These ship regardless of the SFOX-08 offer flag (plan 122-02) — zero dependency between the two plans.
Output: Updated ApiKeyManager + AllocatorExchangeManager (tag maps + canonicalization), regression tests proving tag render and lowercase wiring, VerifiedBadge sfox coverage test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/122-sfox-add-key-ui-e2e/122-CONTEXT.md
@DESIGN.md
@src/components/strategy/ApiKeyManager.tsx
@src/components/exchanges/AllocatorExchangeManager.tsx
@src/components/ui/VerifiedBadge.tsx
</context>

<interfaces>
From src/lib/closed-sets.ts (read-only here — plan 122-02 owns edits to this file):
- EXCHANGE_DISPLAY already contains sfox: "sFOX" (line 53); SUPPORTED_EXCHANGES already contains "sfox" (line 39).

From src/components/strategy/ApiKeyManager.tsx:
- exchangeIcon: Record<string, string> at line ~290 — binance:"B", okx:"O", bybit:"By", deribit:"DRB". Rendered at line ~352 as exchangeIcon[key.exchange] ?? "?".
- handleAddKey POSTs /api/keys/validate-and-encrypt with exchange: data.exchange (line ~147) then supabase.from("api_keys").insert({ exchange: data.exchange, ... }) (line ~170).

From src/components/exchanges/AllocatorExchangeManager.tsx:
- EXCHANGE_TAGS: Record<string, { label; bg; fg }> at line ~122 — binance BNB #FEF3C7/#92400E, okx OKX #1F2937/#F9FAFB, bybit BYB #FFE4D6/#C2410C, kraken KRK, deribit DRB #DBEAFE/#1E3A8A, coinbase CBS. Consumed at lines ~729 and ~816 with a "?" fallback.
- handleAddKey (line ~531) sends exchange: data.exchange in the fetch body (line ~548) and inserts raw data.exchange (line ~575).

From src/components/ui/VerifiedBadge.tsx:
- VerifiedBadge({ trustTier }) renders the green "Verified" chip iff trustTier === "api_verified"; every other value (csv_uploaded/self_reported/null/undefined) returns null. trust_tier is exchange-agnostic — NO code change is expected for sfox; only coverage.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the SFOX 3-letter mono tag to both connected-key tag maps</name>
  <files>src/components/strategy/ApiKeyManager.tsx, src/components/strategy/ApiKeyManager.test.tsx, src/components/exchanges/AllocatorExchangeManager.tsx, src/components/exchanges/AllocatorExchangeManager.test.tsx</files>
  <behavior>
    - ApiKeyManager: a key row with exchange "sfox" renders the avatar text "SFOX" (assert the "?" fallback is NOT rendered for sfox).
    - AllocatorExchangeManager: a connected key with exchange "sfox" renders a tag chip labeled "SFOX" at both EXCHANGE_TAGS consumer sites (~729 and ~816 — one test per reachable surface is sufficient if both routes flow through one row renderer).
    - Existing exchanges (binance/okx/bybit/deribit) render unchanged.
  </behavior>
  <action>
    Read DESIGN.md (3-letter mono, no emoji, per the deribit DRB precedent) and 122-CONTEXT.md first. In ApiKeyManager.tsx add sfox: "SFOX" to the exchangeIcon map at line ~290, keeping the existing lockstep comment accurate (the comment references the canonical EXCHANGE_TAGS map). In AllocatorExchangeManager.tsx add sfox: { label: "SFOX", bg, fg } to EXCHANGE_TAGS at line ~122. Color pair: sFOX has no bright brand color — use a neutral slate pairing distinct from every existing entry (e.g. bg "#F1F5F9" with fg "#0F172A", the track/near-black tokens already in the DESIGN.md palette family); verify the fg-on-bg contrast is >= 4.5:1 (WCAG AA for the micro-tier label) before committing to the pair. Do NOT introduce an emoji or any new decorative treatment (DESIGN.md AI-Slop Ban). This entry ships UNCONDITIONALLY — no reference to the offer flag anywhere in this plan (a founder-connected sfox key exists before the public flag flips, per 122-CONTEXT locked decision).
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/ApiKeyManager.test.tsx src/components/exchanges/AllocatorExchangeManager.test.tsx --no-file-parallelism</automated>
  </verify>
  <done>Both tag maps contain sfox: "SFOX"; tests prove a sfox key renders "SFOX" (not "?") in both components and existing exchange rows are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: F6 — canonical-lowercase exchange at BOTH client add-key sites</name>
  <files>src/components/exchanges/AllocatorExchangeManager.tsx, src/components/exchanges/AllocatorExchangeManager.test.tsx, src/components/strategy/ApiKeyManager.tsx, src/components/strategy/ApiKeyManager.test.tsx</files>
  <behavior>
    - Given the form submits exchange "sFOX" (mixed case), the /api/keys/validate-and-encrypt fetch body AND the supabase api_keys insert both receive exactly "sfox".
    - Given "binance" (already lowercase), both payloads are byte-identical to today.
    - The wiring is proven at the call site: the test asserts the mocked supabase insert received the canonical value — a test that only exercises a helper is insufficient (test-the-wiring rule).
  </behavior>
  <action>
    F6 fold-in (phase-119 deferred), widened to the whole class per the close-the-whole-batch rule: the CONTEXT names AllocatorExchangeManager.tsx:575, but ApiKeyManager.tsx has the IDENTICAL pattern (raw data.exchange in the fetch body at ~147 and the insert at ~170). In BOTH components' handleAddKey, compute a single canonical value at function entry — data.exchange trimmed and lowercased — and use it in the validate-and-encrypt fetch body AND the api_keys insert payload. Rationale to preserve in a short comment: the server route normalizes for validation (WR-01) but the CLIENT performs the DB insert directly, so a mixed-case value passes validation (burning a live probe) then 23514s on the lowercase-only CHECK; canonicalizing client-side closes that gap. Do not touch any other field's handling (label/apiKey/apiSecret credential expressions stay byte-identical — the .trim() chokepoint for credentials lives server-side per the v1.11 dogfood fix, do NOT re-trim credentials here). Regression tests: mock supabase and fetch, drive the form (ApiKeyForm accepts a defaultExchange prop — pass "sFOX" to simulate the mixed-case vector), assert both payloads carry "sfox"; assert the test FAILS when the canonicalization is neutered (temporarily revert locally to confirm, per the prove-it-fails discipline, then restore).
  </action>
  <verify>
    <automated>npx vitest run src/components/exchanges/AllocatorExchangeManager.test.tsx src/components/strategy/ApiKeyManager.test.tsx --no-file-parallelism</automated>
  </verify>
  <done>Both handleAddKey implementations send/insert canonical lowercase; regression tests pin the fetch body and the insert payload at both sites; lowercase inputs are byte-identical to today.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VerifiedBadge api_verified coverage for a sfox strategy</name>
  <files>src/components/ui/VerifiedBadge.test.tsx</files>
  <behavior>
    - A strategy-shaped input representing a sfox-sourced, api_verified strategy (trustTier "api_verified") renders the "Verified" chip.
    - trustTier csv_uploaded / self_reported / null / undefined render nothing (re-pin the existing fail-closed dispatch alongside the new case).
  </behavior>
  <action>
    Test-only task — VerifiedBadge dispatches on trustTier alone (exchange-agnostic), and the Python ingestion side already auto-stamps api_verified for sfox at process_key.py:828 (landed 120-01), so NO component change is expected. Add a coverage block to VerifiedBadge.test.tsx documenting the SFOX-09 claim: a sfox strategy whose strategy_verifications row carries trust_tier api_verified renders the badge. Name the test so the SFOX-09 traceability is greppable (mention sfox in the test name). If the existing test file already covers the api_verified branch generically, ADD the sfox-named case rather than renaming existing tests (surgical-changes rule).
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/VerifiedBadge.test.tsx --no-file-parallelism</automated>
  </verify>
  <done>A sfox-named test asserts the api_verified badge renders; non-api_verified tiers still render nothing; no production code changed by this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/keys/validate-and-encrypt | user-supplied exchange + credentials cross here |
| client → supabase api_keys insert (RLS) | client-composed row hits the DB CHECK constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | handleAddKey exchange value | mitigate | client-side lowercase canonicalization (this plan, Task 2) + server WR-01 normalization + DB lowercase CHECK remain the authoritative gates |
| T-122-02 | Information disclosure | credential fields in handleAddKey | accept | no change to credential handling in this plan; existing scrub/encrypt paths untouched (byte-identical mandate) |
| T-122-03 | Spoofing | VerifiedBadge rendering | accept | badge dispatches on server-projected trust_tier only; no client-writable path to api_verified |
</threat_model>

<verification>
- npx vitest run src/components/strategy/ApiKeyManager.test.tsx src/components/exchanges/AllocatorExchangeManager.test.tsx src/components/ui/VerifiedBadge.test.tsx --no-file-parallelism — all green.
- npx tsc --noEmit — clean.
- npm run lint — clean (react-hooks errors are lint-only, pre-push rule).
- grep confirms no offer-flag reference leaked into this plan's files (the badge is unconditional).
</verification>

<success_criteria>
- sfox: "SFOX" present in ApiKeyManager exchangeIcon AND AllocatorExchangeManager EXCHANGE_TAGS; a sfox key renders the tag, never "?" (SFOX-09 badge leg).
- Both add-key clients send/insert canonical lowercase exchange (F6 closed as a class, both sites).
- VerifiedBadge sfox api_verified coverage test exists and passes (SFOX-09 provenance leg, component level).
- Zero visual/behavioral change for existing exchanges.
</success_criteria>

<output>
Create `.planning/phases/122-sfox-add-key-ui-e2e/122-01-SUMMARY.md` when done.
</output>
