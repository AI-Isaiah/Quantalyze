---
phase: 122-sfox-add-key-ui-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/closed-sets.ts
  - src/lib/closed-sets.sfox-flag.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx
  - src/components/strategy/ApiKeyForm.tsx
  - src/components/strategy/ApiKeyForm.test.tsx
autonomous: true
requirements: [SFOX-08]
tags: [sfox, feature-flag, exchange-picker, wizard, frontend]

must_haves:
  truths:
    - "With NEXT_PUBLIC_SFOX_ENABLED unset (default), UI_EXCHANGE_CODES deep-equals the current 4-tuple and every picker surface is byte-identical to today — proven by tests"
    - "With the flag set to the exact string 'true', sfox is offered: UI_EXCHANGE_CODES includes 'sfox', EXCHANGES includes 'sFOX', and the wizard pickers render a sFOX card"
    - "The sfox picker card collects a SINGLE API token — no secret field is rendered and submit is not blocked on a missing secret; the payload carries api_secret as empty string"
    - "No surface shows a false 'we verify read-only scope' claim for sfox — the copy states the structural facts (no order/withdraw path; sFOX exposes no per-key scope endpoint) per F3"
    - "An invalid sfox key attempt renders the scripted KEY_AUTH_FAILED envelope — no false-verified path reachable from the UI"
  artifacts:
    - path: "src/lib/closed-sets.ts"
      provides: "SFOX_UI_ENABLED flag const + flag-conditional UI_EXCHANGE_CODES, satisfies readonly SupportedExchange[] intact"
      contains: "NEXT_PUBLIC_SFOX_ENABLED"
    - path: "src/lib/closed-sets.sfox-flag.test.ts"
      provides: "byte-identical-when-OFF + widened-when-ON proof"
      contains: "NEXT_PUBLIC_SFOX_ENABLED"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      provides: "flag-gated sfox ExchangeOption card with token-only credentials + honest trust-atom copy"
      contains: "sfox"
    - path: "src/components/strategy/ApiKeyForm.tsx"
      provides: "sfox branch: token-only field, honest read-only footer copy, empty-secret submit"
      contains: "sfox"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      to: "src/lib/closed-sets.ts SFOX_UI_ENABLED"
      via: "flag import gating the sfox ExchangeOption entry"
      pattern: "SFOX_UI_ENABLED"
    - from: "src/lib/closed-sets.ts UI_EXCHANGE_CODES"
      to: "EXCHANGES chip surfaces (MandateForm/StrategyFilters/PreferencesPanel/ApiKeyForm/StrategyForm/MetadataStep) + VerificationForm + RequestIntroButton"
      via: "EXCHANGES derives from UI_EXCHANGE_CODES (OQ4 — zero edits to those consumers)"
      pattern: "UI_EXCHANGE_CODES\\.map"
    - from: "ConnectKeyStep setup-guide link"
      to: "/security#sfox-readonly"
      via: "the existing derived href /security#${exchange}-readonly (zero edits)"
      pattern: "security#\\$\\{exchange\\}-readonly"
---

<objective>
Offer sFOX in the user-facing exchange pickers BEHIND the NEXT_PUBLIC_SFOX_ENABLED flag (default OFF — the founder flips it live only after the 121-03 egress deploy + SFOX-06 validation clear, per the 122-CONTEXT locked decision). Flag OFF: every surface byte-identical, proven by tests. Flag ON: sfox flows through UI_EXCHANGE_CODES (auto-widening every chip surface per OQ4) AND through the two wizard pickers' private ExchangeOption arrays, with token-only credential collection (sFOX is a single Bearer token — the routes already accept empty api_secret for sfox) and F3-honest copy.

Purpose: SFOX-08 offer leg. Build everything; expose nothing until the flag flips.
Output: Flag-conditional closed-sets, sfox wizard cards (ConnectKeyStep + MultiKeyConnectStep), sfox-aware ApiKeyForm, tests proving OFF=byte-identical and ON=honest token-only offer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/122-sfox-add-key-ui-e2e/122-CONTEXT.md
@DESIGN.md
@src/lib/closed-sets.ts
@src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
@src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
@src/components/strategy/ApiKeyForm.tsx
@src/app/api/keys/validate-and-encrypt/route.ts
</context>

<interfaces>
From src/lib/closed-sets.ts:
- SUPPORTED_EXCHANGES = ["binance","okx","bybit","deribit","sfox"] as const (line 39, already widened in 119).
- EXCHANGE_DISPLAY has sfox: "sFOX" (line 53) — the satisfies Record makes a missing label a compile error.
- UI_EXCHANGE_CODES (line 66) = 4-tuple, "as const satisfies readonly SupportedExchange[]". Deliberately decoupled from SUPPORTED_EXCHANGES (per-phase conscious widening, Phase-69 deribit precedent).
- EXCHANGES (line 101) = UI_EXCHANGE_CODES.map(code => EXCHANGE_DISPLAY[code]) — the OQ4 auto-widen seam.
- Module-header rule: closed-sets imports ONLY zod (process.env access is not an import — allowed).
- FUNDING_EXCHANGES stays 3-value — DO NOT TOUCH (Pitfall 2).

From src/app/api/keys/validate-and-encrypt/route.ts (landed 119, read-only here):
- isSfox = exchange.toLowerCase() === "sfox" (case-insensitive, WR-01); sfox carve-out: empty/absent api_secret normalized to "" and accepted; every ccxt exchange still requires a secret byte-identically. The create-with-key and composite/add-key siblings carry the same carve-out. 401/403 maps to KEY_AUTH_FAILED via classifyKeyValidationError with zero TS edits.

From ConnectKeyStep.tsx:
- Private EXCHANGES: ExchangeOption[] (line 42) — id/name/caption/requiresPassphrase + optional credentialLabels/credentialPlaceholders (deribit "Client ID"/"Client Secret" precedent, lines 61-71). NOT derived from UI_EXCHANGE_CODES.
- TRUST_ATOMS (line 74): the "What we reject" atom claims "Any key with trading or withdrawal scopes is rejected before we store it" — false for sfox (F3).
- Submit gate (line 384): disabled on !apiKey || !apiSecret || (requiresPassphrase && !passphrase). Secret input is a required raw input (lines 325-334).
- Setup-guide link (line 290): href={`/security#${exchange}-readonly`} — derives the sfox anchor automatically.
- data-testid convention: wizard-exchange-${ex.id} on picker cards.

From MultiKeyConnectStep.tsx:
- Its OWN private EXCHANGES: ExchangeOption[] (line 72) — deliberately not shared with ConnectKeyStep (comment at line 44). Panel validation consults requiresPassphrase at line ~583; per-panel testids key-${i}-exchange-${id}.

From ApiKeyForm.tsx:
- Select options from EXCHANGES (constants.ts re-export of closed-sets EXCHANGES) — auto-widens when the flag flips; option value is e.toLowerCase() ("sFOX" lowercases to "sfox" correctly).
- API Secret Input is required with a Show/Hide toggle (lines 111-131); footer copy (line 144) claims trading/withdrawal keys "will be rejected" — needs the sfox-honest variant.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flag-gate UI_EXCHANGE_CODES in closed-sets.ts</name>
  <files>src/lib/closed-sets.ts, src/lib/closed-sets.sfox-flag.test.ts</files>
  <behavior>
    - NEXT_PUBLIC_SFOX_ENABLED unset → UI_EXCHANGE_CODES deep-equals ["binance","okx","bybit","deribit"] and EXCHANGES deep-equals ["Binance","OKX","Bybit","Deribit"] (byte-identical proof).
    - Exact string "true" → UI_EXCHANGE_CODES is the 4-tuple plus "sfox" appended, EXCHANGES gains "sFOX".
    - Any other value ("1", "TRUE", "on", "") → OFF (strict equality; the fail-closed convention).
  </behavior>
  <action>
    Per the 122-CONTEXT locked flag decision (reuse the project's process.env.NEXT_PUBLIC_* convention — no dedicated flag lib exists; Claude's-discretion choice documented here): in closed-sets.ts, next to UI_EXCHANGE_CODES, export a const SFOX_UI_ENABLED computed as strict equality of process.env.NEXT_PUBLIC_SFOX_ENABLED against the exact string "true" (Next.js inlines the full static process.env.NEXT_PUBLIC_SFOX_ENABLED expression into client bundles at build time — keep the access as one static member expression, never dynamic indexing). Restructure UI_EXCHANGE_CODES as: a private base 4-tuple literal and a private widened 5-tuple literal (base plus "sfox"), EACH carrying "as const satisfies readonly SupportedExchange[]" so the compile-time set guarantee still holds; the exported UI_EXCHANGE_CODES, explicitly typed readonly SupportedExchange[], selects between them on SFOX_UI_ENABLED. Update the surrounding doc comment: the sfox offer is flag-gated because the backend is founder-gated end-to-end (121-03 egress deploy + SFOX-06 validation + phase-123 for active accounts); the founder flips NEXT_PUBLIC_SFOX_ENABLED=true in Vercel when those clear; note the badge surfaces ship unconditionally elsewhere. Do NOT touch FUNDING_EXCHANGES, SUPPORTED_EXCHANGES, or EXCHANGE_DISPLAY. Tests in a NEW file closed-sets.sfox-flag.test.ts using vi.stubEnv + vi.resetModules + dynamic import per case (module-scope const requires re-import), with vi.unstubAllEnvs in afterEach (the Node22 stub-leak lesson). Then run the full existing closed-sets-adjacent suites (check-zod-db-check-parity and any test importing UI_EXCHANGE_CODES/EXCHANGES) to prove flag-OFF byte-identity across consumers, and npx tsc --noEmit to prove literal-narrowing consumers (VerificationForm UI_EXCHANGE_CODES[0], RequestIntroButton map) still typecheck against the widened exported type.
  </action>
  <verify>
    <automated>npx vitest run src/lib/closed-sets.sfox-flag.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <done>SFOX_UI_ENABLED exported; UI_EXCHANGE_CODES flag-conditional with satisfies intact on both tuples; OFF deep-equals today's 4-tuple; strict "true" widens; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flag-gated sFOX card in both wizard pickers, token-only + F3-honest</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx</files>
  <behavior>
    - Flag OFF: exactly the four existing wizard-exchange-* testids render; no sfox card; DOM byte-identical (pin the card count and the absence of wizard-exchange-sfox).
    - Flag ON: wizard-exchange-sfox renders; selecting it shows ONE credential field labeled "API Token" (no secret input rendered), submit enables with token alone, and the POST body carries api_secret as empty string with exchange "sfox".
    - Flag ON + sfox selected: the setup-guide link resolves to /security#sfox-readonly (mirrors ConnectKeyStep.test.tsx:199 deribit precedent).
    - Flag ON + sfox selected: no rendered copy claims scope verification; the honest structural claim renders instead (F3).
    - Flag ON + server responds 400 KEY_AUTH_FAILED: the scripted WizardErrorEnvelope renders (ROADMAP SC4 — no false-verified path from the UI).
    - MultiKeyConnectStep: same flag gating for its own EXCHANGES array; a sfox panel validates with token-only credentials.
  </behavior>
  <action>
    Read DESIGN.md (voice: declarative sentence-case, no adjectives where a number/fact exists) and the deribit ExchangeOption precedent first. Extend the ExchangeOption interface (in BOTH files — they are deliberately unshared, keep them unshared per the line-44 comment) with an optional requiresSecret flag defaulting true when absent, mirroring how requiresPassphrase already works. Append the sfox entry to each private EXCHANGES array conditionally on SFOX_UI_ENABLED imported from closed-sets (spread a ternary — flag OFF leaves the array literal byte-identical). The sfox card: id "sfox", name "sFOX" (EXCHANGE_DISPLAY casing), requiresPassphrase false, requiresSecret false, credentialLabels key "API Token", credentialPlaceholders key directing the user to paste the read-only sFOX API token, and an honest caption stating spot account, single API token, no secret (per D-XX flag decision + F3; no marketing adjectives). In ConnectKeyStep: render the secret block only when the active exchange requiresSecret; relax the submit-disabled gate from !apiSecret to (requiresSecret && !apiSecret); send api_secret as empty string when requiresSecret is false (the validate route's sfox carve-out normalizes and accepts it — landed 119, zero server edits). Make the TRUST_ATOMS "What we reject" body exchange-aware: for sfox render the F3-honest structural claim (keys are used read-only by our adapter — no order or withdraw path exists; sFOX exposes no per-key scope endpoint, so mint a READ-ONLY token) instead of the scope-rejection claim; every other exchange renders today's copy byte-identically. The grid stays md:grid-cols-3 (5 cards wrap 3+2 — acceptable, matches the 4-card deribit wrap precedent). In MultiKeyConnectStep mirror the same option entry, requiresSecret handling in the panel fields/validation (line ~583 pattern), and empty-string secret in the composite/add-key POST (its sfox carve-out also landed in 119). Tests control the flag by stubbing env + resetModules (same idiom as Task 1) or vi.mock of closed-sets' SFOX_UI_ENABLED — pick ONE idiom and use it in both test files; always vi.unstubAllGlobals/unstubAllEnvs in afterEach.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx" --no-file-parallelism</automated>
  </verify>
  <done>Both pickers offer sfox only when the flag is ON; token-only collection proven (no secret field, empty-string secret in payload); OFF renders byte-identical card sets; setup-guide link resolves to #sfox-readonly; KEY_AUTH_FAILED envelope proven; F3 copy honest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: sfox-aware ApiKeyForm (token-only + honest footer)</name>
  <files>src/components/strategy/ApiKeyForm.tsx, src/components/strategy/ApiKeyForm.test.tsx</files>
  <behavior>
    - Flag OFF: the Select offers exactly today's four options; form byte-identical.
    - Flag ON + exchange "sfox" selected: the API Key input relabels to "API Token", the API Secret input (and its Show/Hide toggle) is not rendered, submit proceeds with apiSecret as empty string, and the footer renders the sfox-honest read-only copy.
    - Flag ON + any other exchange: secret required and footer copy byte-identical to today.
  </behavior>
  <action>
    ApiKeyForm's Select auto-widens via EXCHANGES (OQ4 — zero edits needed for the option to appear), but the form body must handle the token-only shape: derive an isSfox flag from the controlled exchange state (exact lowercase match — the Select values are already lowercased). When isSfox: relabel the key input to "API Token" with a matching placeholder, skip rendering the secret input block entirely (its required attribute would otherwise block submit on a field sfox does not have), pass empty string as apiSecret in onSubmit, and swap the footer paragraph (line 144) to the F3-honest variant: sFOX keys are used read-only by our adapter (no order or withdraw path exists); sFOX does not expose a per-key scope check, so mint a READ-ONLY token. Non-sfox exchanges keep the existing "will be rejected" copy byte-identically — the scope-probe claim is TRUE for ccxt exchanges. Preserve every existing scrub behavior (unmount cleanup, handleCancel, finally-block scrub) untouched; the scrubs already reset apiSecret which stays in state as empty string for sfox — no new scrub sites needed. This form is consumed by ApiKeyManager AND AllocatorExchangeManager (both fixed for lowercase in plan 122-01 — no coordination needed, different files). Tests follow the same flag-control idiom as Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/ApiKeyForm.test.tsx --no-file-parallelism</automated>
  </verify>
  <done>ApiKeyForm renders token-only + honest copy for sfox when the flag is ON, is byte-identical when OFF, and non-sfox exchanges are unchanged either way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → key-save routes (validate-and-encrypt / create-with-key / composite/add-key) | user-supplied exchange + single Bearer token cross here |
| build env → client bundle | NEXT_PUBLIC_SFOX_ENABLED is inlined into public JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-04 | Spoofing | flag bypass (user POSTs sfox with flag OFF) | accept | the flag gates the OFFER only; the server-side sfox carve-out (119, security-reviewed T-119-08/09/11) is the authoritative acceptance gate and already ships — a direct POST is the founder's own pre-flip path, fail-closed on invalid keys |
| T-122-05 | Information disclosure | sfox token in ConnectKeyStep/ApiKeyForm state | mitigate | token flows through the EXISTING api_key field + scrub lifecycle (unmount/cancel/finally scrubs untouched); no new plaintext residency introduced |
| T-122-06 | Repudiation/Trust | false read-only scope claim (F3) | mitigate | exchange-aware copy states structural facts only; tests assert the scope-probe claim never renders for sfox |
| T-122-07 | Elevation | client relaxing secret requirement for ccxt exchanges | mitigate | requiresSecret defaults true; tests pin that non-sfox exchanges still require a secret; server presence check unchanged |
</threat_model>

<verification>
- npx vitest run src/lib/closed-sets.sfox-flag.test.ts "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx" src/components/strategy/ApiKeyForm.test.tsx --no-file-parallelism — all green.
- npx tsc --noEmit — clean (satisfies + literal-narrowing consumers hold).
- npm run lint — clean.
- Repo-wide grep for NEXT_PUBLIC_SFOX_ENABLED confirms the env read lives ONLY in closed-sets.ts (single source; wizard steps import SFOX_UI_ENABLED).
</verification>

<success_criteria>
- Flag OFF (default): UI_EXCHANGE_CODES + both wizard pickers + ApiKeyForm byte-identical to today, proven by tests (safe merge; founder flips live).
- Flag ON: sfox offered across UI_EXCHANGE_CODES-derived surfaces (zero consumer edits, OQ4) AND both wizard pickers; token-only collection with empty-string secret; setup-guide link resolves to /security#sfox-readonly.
- F3 honesty: no surface claims verified read-only scope for sfox; structural claim renders instead.
- Invalid-key attempt renders the scripted KEY_AUTH_FAILED envelope (ROADMAP SC4 at component level; live-path e2e is founder-gated on 121-03).
</success_criteria>

<output>
Create `.planning/phases/122-sfox-add-key-ui-e2e/122-02-SUMMARY.md` when done.
</output>
