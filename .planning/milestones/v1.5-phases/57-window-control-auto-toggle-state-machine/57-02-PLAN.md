---
phase: 57-window-control-auto-toggle-state-machine
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [WINDOW-01, WINDOW-04, WINDOW-05, POLISH-01]
must_haves:
  truths:
    - "The scenario tab defaults its coverage window to the intersection of the selected+eligible strategy spans and PASSES it to computeScenario"
    - "member_count / KPIs actually change when the window moves (the window reaches the engine post-collapse)"
    - "A 'Common period (all in)' preset snaps to the intersection; a 'Full range (some drop out)' preset widens to the union"
    - "The coverage window is a distinct value from rollingWindow and never feeds the factsheet brush-zoom"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "ephemeral winStart/winEnd state, post-collapse window injection, CustomRangePicker window control, two preset buttons"
      contains: "deAliased.state"
  key_links:
    - from: "ScenarioComposer.tsx window injection"
      to: "computeScenario(deAliased.strategies, engineState, dateMapCache)"
      via: "engineState = win ? { ...deAliased.state, window: win } : deAliased.state"
      pattern: "\\.\\.\\.deAliased\\.state"
    - from: "ScenarioComposer.tsx window control"
      to: "src/lib/scenario-window.ts defaultWindowFor / unionOf"
      via: "preset click sets winStart/winEnd from the shared helpers"
      pattern: "defaultWindowFor|unionOf"
---

<objective>
Wire the coverage window into the scenario tab: add ephemeral `winStart`/`winEnd` composer
state (default = intersection via `defaultWindowFor`), INJECT the window onto
`deAliased.state` immediately before `computeScenario` (the critical hazard fix), mount a
`CustomRangePicker` as the window control, and add the two preset buttons ("Common period
(all in)" = intersection, "Full range (some drop out)" = union). This is the first phase
where the scenario tab passes an explicit `window` — the coverage-window blend becomes
user-visible. Covers WINDOW-01, WINDOW-04, WINDOW-05, and the POLISH-01 separation guard.

Purpose: give the allocator a precise, keyboard-accessible steering control over the
analytical window while keeping it provably distinct from the view axes (brush-zoom,
rollingWindow, startDates).

Output: composer wired to pass `state.window`; window control + presets rendered; a guard
test proving the window reaches the engine and stays distinct from the view axes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-window-control-auto-toggle-state-machine/57-CONTEXT.md
@.planning/phases/57-window-control-auto-toggle-state-machine/57-RESEARCH.md
@DESIGN.md
@.planning/phases/57-window-control-auto-toggle-state-machine/57-01-SUMMARY.md

<interfaces>
<!-- Consume from src/lib/scenario-window.ts (Plan 01 added unionOf/outlierIdsFor): -->
export function coverageSpanOf(dailyReturns: DailyPoint[]): CoverageSpan | null;
export function defaultWindowFor(spans: CoverageSpan[]): CoverageWindow | null;  // intersection [max(firsts), min(lasts)] | null
export function unionOf(spans: CoverageSpan[]): CoverageWindow | null;           // [min(firsts), max(lasts)] | null-on-empty (Plan 01)
export function covers(span: CoverageSpan, window: CoverageWindow): boolean;
export interface CoverageWindow { start: string; end: string; }

<!-- Engine — src/lib/scenario.ts (Phase 55, honours optional window; union-when-absent): -->
export interface ScenarioState { selected; weights; startDates; leverage?; window?: { start: string; end: string }; }
export function computeScenario(strategies: StrategyForBuilder[], state: ScenarioState, cache): ComputedMetrics;
// ComputedMetrics additively exposes: member_count?, member_ids?, effective_start, effective_end
// StrategyForBuilder carries daily_returns: DailyPoint[]  (DailyPoint = { date: string; value: number })

<!-- CustomRangePicker — src/app/(dashboard)/allocations/components/CustomRangePicker.tsx (reuse VERBATIM): -->
type Props = { isOpen; onClose: () => void; onApply: (r:{start:string;end:string}) => void; min: Date; max: Date; initialRange?: {start:string;end:string} | null; };
// Already a11y-hardened: role=dialog, Esc/outside-click dismiss, start<=end clamp + disabled Apply, aria-live clamp.
// Canonical mount pattern: EquityChart.tsx:1324-1336.

<!-- dateday.ts — ISO<->local-midnight Date (use these, never new Date(str)): -->
import { isoDayFromDate, localMidnight, parseIsoDay } from "@/lib/dateday";
</interfaces>

<hazard>
⚠️ CRITICAL (RESEARCH Pitfall 1): `collapseAliasedHoldingStrategies` (src/lib/scenario-dealias.ts:161-166)
reconstructs the ScenarioState and returns ONLY { selected, weights, startDates, leverage? }
— it SILENTLY DROPS `state.window`. Setting `window` on `projectionState` will NOT reach
the engine; the blend stays on the legacy union path and NOTHING changes (no error, no type
failure — window is optional). YOU MUST inject `window` onto `deAliased.state` AFTER the
collapse, in the `scenarioMetrics` memo at ScenarioComposer.tsx:1532-1535.

The TELL for getting this wrong: `scenarioMetrics.member_count` does NOT change when you
widen the window past an ended strategy. Task 1's acceptance criterion pins exactly this.
</hazard>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ephemeral window state + post-collapse engine injection (WINDOW-01 default; the hazard fix)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:698 (rollingWindow useState — the SEPARATE axis pattern), :1391 (activeAdapterOutput), :1463-1535 (projectionState → deAliased → dateMapCache → scenarioMetrics memos; :1532 is the computeScenario call to modify)
    - src/lib/scenario-dealias.ts:85-167 (the collapse that drops window — confirm it carries only selected/weights/startDates/leverage)
    - src/lib/scenario-window.ts (defaultWindowFor, coverageSpanOf)
    - src/lib/dateday.ts (isoDayFromDate, localMidnight, parseIsoDay)
  </read_first>
  <behavior>
    - Default window: on first composed render, winStart/winEnd seed from defaultWindowFor(spans of the currently selected+eligible deAliased strategies); if that intersection is null, no window is passed (leave engine on union path — the WINDOW-06 banner is Plan 03).
    - member_count changes with the window: with a 2-strategy set where strategy B ends before strategy A, widening winEnd past B's last data day makes computeScenario return member_count = 1 (B dropped); narrowing winEnd back within B's coverage returns member_count = 2. This proves the window reaches the engine post-collapse.
    - When win is null/untouched-empty-intersection, engineState === deAliased.state (own-book union path unchanged; no window key added).
    - The default seeds ONCE — narrowing the window does not snap it back to the intersection (research Pitfall 3): use a "user has touched the window" guard or a lazy initializer so defaultWindowFor is the initial seed + preset target only, not a controlled value.
  </behavior>
  <action>
    In ScenarioComposer.tsx, add composer-local `const [winStart, setWinStart]` and
    `const [winEnd, setWinEnd]` ephemeral useState near the rollingWindow state (:698) —
    NOT in useScenarioState / ScenarioDraft (persistence is Phase 59; NO
    SCENARIO_SCHEMA_VERSION bump). Seed them lazily from `defaultWindowFor` over the
    coverage spans of the selected+eligible deAliased strategies (spans via
    `coverageSpanOf(s.daily_returns)`), guarded by a `windowTouchedRef`/flag so a user
    narrow is never re-snapped (Pitfall 3). At the `scenarioMetrics` memo (:1532-1535),
    build `const win = (winStart && winEnd) ? { start: winStart, end: winEnd } : null;`
    then `const engineState = win ? { ...deAliased.state, window: win } : deAliased.state;`
    and call `computeScenario(deAliased.strategies, engineState, dateMapCache)`. Add `win`
    (or winStart/winEnd) to the memo deps. Only attach the window on this composed
    scenario-tab path — do not alter own-book callers. Keep winStart/winEnd as ISO
    "YYYY-MM-DD" strings (engine + helpers expect ISO); convert to/from Date only at the
    CustomRangePicker boundary (Task 2) via dateday helpers.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" -t "window"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\.\.\.deAliased\.state" src/app/\(dashboard\)/allocations/components/ScenarioComposer.tsx` shows the window injected onto the POST-collapse state (not projectionState)
    - A ScenarioComposer.test.tsx case renders a 2-strategy set (B ends before A), widens winEnd past B's last day, and asserts `scenarioMetrics.member_count === 1` (via a rendered member/divisor readout or a spy on computeScenario args); narrowing back asserts member_count === 2 — the MANDATORY "member_count changes when the window moves" proof
    - A case asserts that with no window set, computeScenario receives a state WITHOUT a `window` key (union path preserved) — assert via a computeScenario spy on the state arg
    - winStart/winEnd are NOT added to ScenarioDraft / scenario-state.ts (grep confirms no schema_version bump): `grep -c "SCENARIO_SCHEMA_VERSION\|schema_version" src/app/\(dashboard\)/allocations/lib/scenario-state.ts` unchanged from baseline
    - Full `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` stays green
  </acceptance_criteria>
  <done>The scenario tab passes an explicit window to computeScenario post-collapse; member_count provably changes with the window; window state is ephemeral (no schema bump).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CustomRangePicker window control + two preset buttons (WINDOW-04, WINDOW-05)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - DESIGN.md (Color: accent #1B6B5A, warning tokens; Motion; Buttons: Primary/Secondary/Ghost; Border radius; Spacing — MANDATORY for all visual choices)
    - src/app/(dashboard)/allocations/components/CustomRangePicker.tsx:46-53 (Props) — reuse verbatim, do NOT fork
    - src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx:1324-1336 (canonical open/apply mount pattern)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:2053-2094 (isEmptyState early-return — control mounts AFTER this, composed branch only), :2892-2936 (rollingWindow control layout — mirror placement/label style), :3018-3034 (CompositionList mount)
    - src/lib/scenario-window.ts (defaultWindowFor, unionOf)
    - src/lib/dateday.ts (isoDayFromDate, localMidnight, parseIsoDay)
  </read_first>
  <behavior>
    - "Common period (all in)" preset click → sets [winStart,winEnd] = defaultWindowFor(spans of selected) → every selected strategy covers it by construction (all in). Marks window as user-touched.
    - "Full range (some drop out)" preset click → sets [winStart,winEnd] = unionOf(spans of selected) → strategies whose span does not ⊇ the union become ineligible (dropped by covers, surfaced in Plan 03's group). Marks window as user-touched.
    - Opening the CustomRangePicker shows min = union earliest first over selected (as a local-midnight Date), max = today or union latest last (whichever is later), initialRange = current {winStart,winEnd}. onApply({start,end}) sets winStart/winEnd (ISO strings) and marks user-touched.
    - When the selected set has an empty intersection, "Common period (all in)" is disabled (defaultWindowFor === null) with an accessible explainer; "Full range" stays enabled (union always exists for a non-empty set).
  </behavior>
  <action>
    Mount the window control in the composed branch (after :2053), placed near the top of
    the composed surface / adjacent to the strategy controls per DESIGN.md and mirroring how
    the rollingWindow control sits above its graph (:2892). Reuse `CustomRangePicker`
    VERBATIM (a `pickerOpen` useState + the EquityChart mount pattern); convert winStart/
    winEnd ISO ↔ Date at this boundary ONLY via dateday helpers (`localMidnight(parseIsoDay(iso))`,
    `isoDayFromDate(date)`). Render two sibling preset buttons next to the picker trigger —
    label them exactly "Common period (all in)" and "Full range (some drop out)" — as
    Secondary/Ghost buttons per DESIGN.md (accent/border tokens, 6px radius, no raw px).
    "Common period" click → `setWin*` from `defaultWindowFor(selectedSpans)`; "Full range"
    click → from `unionOf(selectedSpans)`. Disable "Common period" (with an `aria-disabled`
    + visible explainer) when `defaultWindowFor` is null. Give every new control an explicit
    accessible label (the picker trigger, both presets). Do NOT wire the coverage window to
    the factsheet brush-zoom or rollingWindow — they are separate axes (POLISH-01, Task 3).
    Respect the 1100px content max / composer layout; follow DESIGN.md spacing tokens.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" -t "preset"</automated>
  </verify>
  <acceptance_criteria>
    - A test clicking "Common period (all in)" asserts winStart/winEnd become the intersection (defaultWindowFor) and all selected strategies are members (member_count === selected count)
    - A test clicking "Full range (some drop out)" asserts winStart/winEnd become the union (unionOf) and any non-covering selected strategy is dropped (member_count < selected count on a set with unequal spans)
    - A test asserts "Common period" is disabled/`aria-disabled` when the selected set has an empty intersection; "Full range" remains enabled
    - Both preset buttons and the picker trigger have accessible names (assert via getByRole("button", { name: /Common period|Full range/ }) and the picker trigger's label)
    - No raw px authored (project px-lint / CI stays green); `grep -n "CustomRangePicker" src/app/\(dashboard\)/allocations/components/ScenarioComposer.tsx` shows reuse (no new picker component file created)
  </acceptance_criteria>
  <done>The window control (reused CustomRangePicker) + two labeled preset buttons are mounted in the composed branch, wired to the shared helpers, DESIGN.md-conformant and accessible; the presets provably set the intersection/union windows.</done>
</task>

<task type="auto">
  <name>Task 3: POLISH-01 separation guard (coverage window ≠ view axes)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:698 (rollingWindow), :2474-2494 (ScenarioFactsheetChart + MasterBrush persist=false — the brush VIEW axis), :1501 (startDates — legacy include-from axis)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (existing guard/assertion style)
  </read_first>
  <action>
    Add a focused guard test in ScenarioComposer.test.tsx asserting the four axes stay
    distinct (POLISH-01, LOCKED): (a) changing rollingWindow (63/126/252) does NOT change
    winStart/winEnd or the `state.window` passed to computeScenario; (b) the coverage
    window value is never passed as a brush/`persist` prop to ScenarioFactsheetChart (assert
    the MasterBrush stays `persist=false` and receives no coverage-window prop); (c)
    changing the coverage window does not mutate rollingWindow or per-strategy startDates.
    Use a computeScenario spy (or the rendered readouts) to capture the state arg. Keep this
    a pure assertion test — no new production code beyond what Tasks 1-2 added.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" -t "POLISH-01"</automated>
  </verify>
  <acceptance_criteria>
    - A test named/tagged "POLISH-01" asserts rollingWindow changes leave the computeScenario `state.window` unchanged
    - The test asserts ScenarioFactsheetChart's brush remains `persist={false}` and receives no coverage-window value
    - The test asserts a coverage-window change leaves rollingWindow and startDates untouched
    - `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` green
  </acceptance_criteria>
  <done>A guard test proves the coverage `[winStart,winEnd]` axis is distinct from rollingWindow, the factsheet brush-zoom, and per-strategy startDates — none can leak into another.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user date input → CustomRangePicker | Untrusted date strings; already validated by dateday.parseIsoDay (strict, rejects rollover garbage) + start≤end clamp inside the picker. |
| composer client state → computeScenario | The window value crossing into the engine; must be the SAME value the UI derives eligibility from (Plan 03), never a fabricated window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03 | Tampering / Info-integrity | window silently not reaching the engine (collapse drops it) | mitigate | Post-collapse injection onto deAliased.state + the MANDATORY member_count-changes assertion (Task 1). |
| T-57-04 | Tampering | malformed date input (rollover garbage) | mitigate | Reuse CustomRangePicker's dateday.parseIsoDay strict parse + min/max clamp (no new input surface). |
| T-57-05 | Info-disclosure | coverage window leaking into the view brush-zoom, conflating analytical membership with a view (POLISH-01) | mitigate | Separation guard (Task 3): brush stays persist=false, receives no coverage-window prop; rollingWindow/startDates untouched. |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure internal wiring (RESEARCH: zero new deps). |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` green (window default, hazard/member_count, presets, POLISH-01 guard).
- `npx playwright test e2e/composer-axe.spec.ts` green — the new window control + presets mount in the composed surface the seeded axe spec already scans (RESEARCH Pitfall 6; no new CI wiring needed).
- Full frontend `npm test` stays green; coverage CI gate (lines 82/stmts 80/fns 74/branches 72) not regressed by the new code.
- No SCENARIO_SCHEMA_VERSION bump; no own-book caller behavior change (union-when-absent preserved).
</verification>

<success_criteria>
- The scenario tab defaults its window to the intersection and PASSES it to computeScenario post-collapse; member_count provably changes when the window moves (WINDOW-01 + hazard).
- "Common period (all in)" and "Full range (some drop out)" presets set the intersection/union windows via the shared helpers (WINDOW-04, WINDOW-05).
- The coverage window is a provably distinct axis from brush-zoom, rollingWindow, and startDates (POLISH-01).
- All new controls are DESIGN.md-conformant, accessible (labels), and pass the seeded composer-axe scan.
</success_criteria>

<output>
Create `.planning/phases/57-window-control-auto-toggle-state-machine/57-02-SUMMARY.md` when done.
</output>
