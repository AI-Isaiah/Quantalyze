---
phase: 57-window-control-auto-toggle-state-machine
plan: 03
type: execute
wave: 3
depends_on: ["57-01", "57-02"]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [WINDOW-02, WINDOW-03, WINDOW-06, POLISH-02]
must_haves:
  truths:
    - "Widening the window past a strategy's coverage moves it into a distinct 'auto-excluded (outside window)' group; narrowing restores it — only within the user's selected subset, never adding an unselected strategy"
    - "The dropped row animates (fade+slide) into the auto-excluded group, respecting prefers-reduced-motion"
    - "When the selected set shares no common window, a warning banner names the outlier(s) and offers a one-click deselect that restores a valid intersection"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "coverageEligible memo, auto-excluded group with inline reason + animation, empty-intersection warning banner with deselect action"
      contains: "coverageEligible"
  key_links:
    - from: "ScenarioComposer.tsx coverageEligible memo"
      to: "src/lib/scenario-window.ts covers / coverageSpanOf"
      via: "covers(coverageSpanOf(s.daily_returns), win) per selected strategy — SAME predicate as the engine"
      pattern: "covers\\(coverageSpanOf"
    - from: "ScenarioComposer.tsx empty-intersection banner deselect"
      to: "src/lib/scenario-window.ts outlierIdsFor + existing toggle/remove handlers"
      via: "outlierIdsFor(spansById) → deselect via scenario.toggleHolding / handleRemoveAdded"
      pattern: "outlierIdsFor"
---

<objective>
Deliver the auto-toggle state machine's VISIBLE behavior: a pure `coverageEligible` memo
(the SAME `covers` predicate the engine applies, so UI and divisor can never disagree), an
animated "auto-excluded (outside window)" group with an inline reason for coverage-dropped
rows, and the empty-intersection warning banner that names the outlier(s) and offers a
one-click deselect. Covers WINDOW-02 (widen→auto-off), WINDOW-03 (narrow→auto-on, only in
subset), WINDOW-06 (empty-intersection guided fix), POLISH-02 (animation + reduced-motion).

This is the visible proof of the whole milestone: an ended strategy no longer dilutes the
tail, and the allocator SEES it drop instead of it silently vanishing.

Purpose: make membership honest and legible at the mechanism level (the rich gantt /
three-state chips / blend header are Phase 58 — deliver the FUNCTIONAL group + minimal
honest label + animation only, do NOT over-build into 58).

Output: coverageEligible memo + auto-excluded group + inline reason + animation + empty-
intersection banner with deselect; tests for widen/narrow/subset-only/banner/motion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-window-control-auto-toggle-state-machine/57-CONTEXT.md
@.planning/phases/57-window-control-auto-toggle-state-machine/57-RESEARCH.md
@DESIGN.md
@.planning/phases/57-window-control-auto-toggle-state-machine/57-02-SUMMARY.md

<interfaces>
<!-- src/lib/scenario-window.ts (Plans 55 + 01): -->
export function coverageSpanOf(dailyReturns: DailyPoint[]): CoverageSpan | null;  // [first,last] | null
export function covers(span: CoverageSpan, window: CoverageWindow): boolean;      // INCLUSIVE-CLOSED
export function outlierIdsFor(spansById: Record<string, CoverageSpan>): string[]; // Plan 01 — WINDOW-06 outlier names
export interface CoverageSpan { first: string; last: string; }
export interface CoverageWindow { start: string; end: string; }

<!-- From Plan 02 (already in ScenarioComposer.tsx): winStart/winEnd ISO state, win = {start,end}|null, the deAliased.strategies set (StrategyForBuilder[] with daily_returns), computeScenario receiving state.window. -->

<!-- Engine cross-check — computeScenario output: member_ids?: string[] (who is in the blend, post-collapse). -->

<!-- Existing composer handlers for the deselect action (Open Question #2, RESEARCH): -->
//  added strategies (toggle-able unit; the outlier in practice): handleRemoveAdded(id)  (ScenarioComposer.tsx:1246)
//  live holdings (read-only book context): scenario.toggleHolding(scopeRef)  (:3023)
</interfaces>

<motion_tokens>
<!-- DESIGN.md Motion (MANDATORY): medium 250ms → Tailwind `duration-300` (duration-250 is NOT a valid Tailwind v4 token and silently drops). enter = ease-out. "No decorative animation" — the move must aid comprehension (row visibly relocating), nothing more. -->
<!-- Reduced-motion (codebase convention, CustomizeDrawer.tsx:158/166 + globals.css:182): apply `motion-reduce:transition-none` on EVERY element carrying a transition-* for the move (RESEARCH Pitfall 5). -->
<!-- DESIGN.md warning tokens (WCAG-AA verified): text --color-warning #B45309; bg --color-warning-bg #FEF3C7; border --color-warning-border #FDE68A. -->
</motion_tokens>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: coverageEligible memo + auto-toggle (WINDOW-02 widen→off, WINDOW-03 narrow→on, subset-only)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:1463-1535 (projectionState/deAliased/scenarioMetrics — winStart/winEnd + win from Plan 02 live here), :3018-3034 (CompositionList mount)
    - src/lib/scenario-window.ts (covers, coverageSpanOf)
    - src/lib/scenario.ts:263-295 (the engine's internal membership — the coverageEligible memo must mirror this exactly so UI group == engine member_ids)
  </read_first>
  <behavior>
    - coverageEligible: Record<strategyId, boolean> over the SELECTED deAliased strategies: `eligible[id] = span !== null && covers(span, win)` where span = coverageSpanOf(s.daily_returns) and win = {winStart,winEnd}. When win is null (untouched/empty-intersection), treat all selected as eligible (union path — no drops).
    - In-blend iff `selected && coverageEligible` (ADR §"UI state machine"). `selected` is NEVER mutated by a coverage change — auto-off only flips coverageEligible.
    - Widen past strategy B's coverage → coverageEligible[B] flips false → B is coverage-auto-excluded (WINDOW-02). Narrow until B regains full coverage → coverageEligible[B] flips true → B is in-blend again, IFF still selected (WINDOW-03).
    - Subset-only: an UNSELECTED strategy is never added by a coverage change — coverageEligible is only consulted for selected strategies; a narrow that would "cover" an unselected strategy leaves it out (WINDOW-03 guard).
    - Invariant (dev cross-check): the set { id : selected[id] && coverageEligible[id] } equals scenarioMetrics.member_ids for the passthrough (non-aliased) rows — assert this so the UI group and engine divisor can never desync (RESEARCH Pitfall 2).
  </behavior>
  <action>
    Add a pure `coverageEligible` useMemo in ScenarioComposer.tsx keyed on the deAliased
    strategy set + win (winStart/winEnd) + the selected map: for each SELECTED strategy,
    `covers(coverageSpanOf(s.daily_returns), win)` (null span or null win → the union/all-in
    default). Do NOT re-derive intersection/coverage math inline — call the
    scenario-window.ts helpers (RESEARCH anti-pattern). Use this memo to classify each
    CompositionList row as in-blend vs coverage-auto-excluded (Task 2 renders the group);
    `selected` stays the manual axis (manual-off = removed from subset, distinct). Do not
    mutate `selected` from coverage. Cross-check against `scenarioMetrics.member_ids` in a
    dev-mode assertion (passthrough rows align; the aliased-collapse seam is documented in a
    comment per RESEARCH Pitfall 2).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" -t "WINDOW-0[23]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "covers(coverageSpanOf" src/app/\(dashboard\)/allocations/components/ScenarioComposer.tsx` shows the shared predicate is used (no inline `span.first <= ... && span.last >= ...`)
    - A WINDOW-02 test: widen winEnd past B's last day → B is classified coverage-auto-excluded AND scenarioMetrics.member_count drops by one
    - A WINDOW-03 test: narrow winEnd back within B's coverage → B returns to in-blend; and a separate assertion that an UNSELECTED strategy remains excluded even when the window would "cover" it (subset-only guard)
    - A dev-invariant test: { selected && coverageEligible } === member_ids on a passthrough-only set
    - No mutation of `selected` on window change (assert the selected map is unchanged after a widen)
  </acceptance_criteria>
  <done>coverageEligible is a pure memo using the shared covers/coverageSpanOf predicate; widen→auto-off and narrow→auto-on work within the selected subset only, never adding an unselected strategy, and never desync from the engine divisor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-excluded group + inline reason + animation (POLISH-02)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - DESIGN.md (Color: warning tokens #B45309 / #FEF3C7 / #FDE68A; Motion: duration-300, ease-out, no decorative animation; Component Patterns; Border radius — MANDATORY)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:3018-3051 (CompositionList mount + "Add more strategies" block — the auto-excluded group renders adjacent to CompositionList)
    - src/components/strategy/CustomizeDrawer.tsx:158,166 (the transition + motion-reduce:transition-none convention)
    - src/app/globals.css:182-199 (reduced-motion block)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (className-match assertion style; RESEARCH cites AllocatorSyncStatus.test.tsx:540 as the pattern)
  </read_first>
  <behavior>
    - A coverage-auto-excluded strategy (selected && !coverageEligible) renders in a visually-distinct "Auto-excluded (outside window)" group, separate from the in-blend rows AND separate from manual-off.
    - Each auto-excluded row carries a minimal honest inline reason derived from its span vs the window, e.g. "ends {Mon YYYY} — outside window" (when span.last < win.end) or "starts {Mon YYYY} — outside window" (when span.first > win.start). Keep the reason MINIMAL — no rich gantt, no three-state chip system (that is Phase 58).
    - The row animates (fade + slide) as it moves into the group; the transition honours prefers-reduced-motion (no motion when reduced).
    - When no strategy is coverage-auto-excluded, the group is absent (not an empty shell).
  </behavior>
  <action>
    Render an "Auto-excluded (outside window)" group adjacent to CompositionList (near
    :3033), listing rows where `selected[id] && !coverageEligible[id]`. Style with DESIGN.md
    warning tokens (bg #FEF3C7 / border #FDE68A / text #B45309) — use the CSS custom-property
    tokens, no raw px, no raw hex. Give each row a minimal inline reason computed from the
    span vs the window (format the boundary month via existing date formatting; ISO→display
    only, no new date lib). Animate the move with Tailwind `transition-*` + `duration-300`
    + `ease-out` AND `motion-reduce:transition-none` on EVERY element carrying the transition
    (RESEARCH Pitfall 5 — do not leave a residual transition on the container). This is the
    functional group + minimal label + animation ONLY — do NOT build the Phase-58 mini-gantt,
    per-row three-state chips, or blend header. Ensure the group + rows are accessible
    (heading/label for the group; the reason text is real text, not color-only).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" -t "auto-excluded"</automated>
  </verify>
  <acceptance_criteria>
    - A test: with strategy B coverage-dropped, an "Auto-excluded (outside window)" group renders containing B with an inline reason string (assert the reason text, e.g. /outside window/)
    - A test asserts `motion-reduce:transition-none` is present on the animated element(s) (className match, mirroring AllocatorSyncStatus.test.tsx:540) and that the move uses `duration-300` (not duration-250)
    - A test asserts the group is ABSENT when no strategy is coverage-auto-excluded
    - The reason is real text (not color-only) — assert the reason node has textContent; group uses DESIGN.md warning tokens (grep shows `--color-warning`/token classes, no raw hex/px)
    - manual-off rows are NOT in the auto-excluded group (the two states stay distinct)
  </acceptance_criteria>
  <done>Coverage-dropped rows animate into a distinct, accessible, DESIGN.md-conformant "Auto-excluded (outside window)" group with a minimal honest inline reason, reduced-motion respected, and manual-off kept separate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Empty-intersection warning banner + one-click deselect (WINDOW-06)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - DESIGN.md (warning tokens; banner/alert styling; WCAG-AA)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:3053-3062 (existing role=alert / aria-live banner pattern), :1246-1265 (handleRemoveAdded), :3023 (scenario.toggleHolding), and the window control mount from Plan 02 (banner sits ABOVE the control)
    - src/lib/scenario-window.ts (outlierIdsFor from Plan 01, intersectionOf/defaultWindowFor)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  </read_first>
  <behavior>
    - When the selected set shares NO common window (defaultWindowFor(selectedSpans) === null), an inline warning banner renders ABOVE the window control (not a modal), naming the outlier strategy(ies) via outlierIdsFor.
    - The banner offers a one-click "Deselect {X}" action per outlier; clicking it removes that strategy from the selected subset (added strategy → handleRemoveAdded(id); holding → scenario.toggleHolding(scopeRef)), which restores a non-null intersection (guided fix, not a dead-end).
    - After deselecting the outlier, the banner disappears and a valid default window is available again.
    - When the intersection is non-null, the banner is absent.
    - The banner uses aria-live="polite" (mirror CustomRangePicker's clamp region) and DESIGN.md warning tokens; the deselect action(s) have accessible names naming the strategy.
  </behavior>
  <action>
    Detect the empty-intersection condition from the selected strategies' spans
    (`defaultWindowFor(selectedSpans) === null`) and render an inline warning banner above
    the Plan-02 window control. Use `outlierIdsFor` (Plan 01) to name the outlier(s); render
    a "Deselect {strategyName}" button per outlier. Map the outlier id to its display name
    via the existing name lookup (nameById at :1717 area) and wire the click: added strategy
    → `handleRemoveAdded(id)`; live holding → `scenario.toggleHolding(scopeRef)` (per
    RESEARCH Open Question #2 — in practice the outlier is an added strategy; if it is a
    holding, honestly say so and use toggleHolding). Style with DESIGN.md warning tokens,
    `role="alert"`/`aria-live="polite"` (mirror :3053-3062), accessible button names. Banner
    absent when the intersection is non-null. Do NOT block the rest of the composer — it is a
    guided fix, not a hard stop.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" -t "WINDOW-06"</automated>
  </verify>
  <acceptance_criteria>
    - A test with a disjoint selected set (empty intersection) asserts the banner renders, names the outlier (its display name appears), and offers a "Deselect {name}" button with an accessible name
    - Clicking "Deselect {name}" removes the outlier from the subset AND the banner then disappears AND defaultWindowFor(remaining) is non-null (intersection restored)
    - A test asserts the banner is ABSENT when the selected set has a common window
    - The banner uses `role`/`aria-live` and DESIGN.md warning tokens (grep shows token classes, no raw hex/px); `grep -n "outlierIdsFor" src/app/\(dashboard\)/allocations/components/ScenarioComposer.tsx` shows the shared helper is used (no inline outlier math)
  </acceptance_criteria>
  <done>An empty-intersection selected set renders an accessible warning banner naming the outlier(s) with a one-click deselect that restores a valid intersection — a guided fix, not a dead-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer coverageEligible (UI) ↔ engine member_ids (divisor) | The UI's derived membership must match the engine's divisor exactly, or the app hides/shows the wrong strategies vs the actual blend. |
| window state → auto-toggle | Coverage auto-off must operate only within the selected subset (WINDOW-03) — never silently add an unselected strategy to the blend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-06 | Info-integrity / Repudiation | a coverage-dropped strategy vanishing silently (hiding a divisor change) | mitigate | Auto-excluded group + inline reason makes every coverage drop VISIBLE (POLISH-02) — the milestone's honesty guarantee. |
| T-57-07 | Elevation / Tampering | an unselected strategy being auto-ADDED to the blend by a narrow | mitigate | coverageEligible consulted for selected strategies only; in-blend gate = selected && eligible; `selected` never mutated by coverage (WINDOW-03 subset-only test). |
| T-57-08 | Info-integrity | UI auto-excluded group desyncing from the engine divisor | mitigate | coverageEligible uses the SAME covers predicate on the SAME deAliased set + window; dev-invariant test { selected && eligible } === member_ids. |
| T-57-09 | Denial of service (UX) | empty-intersection window rendering a fabricated/dead-end blend | mitigate | WINDOW-06 banner + engine BLEND-05 honest empty-state; UI never fabricates a window (outlierIdsFor + deselect guided fix). |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure internal wiring (RESEARCH: zero new deps). |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` green (WINDOW-02/03 auto-toggle + subset-only, auto-excluded group + motion, WINDOW-06 banner + deselect, dev-invariant).
- `npx playwright test e2e/composer-axe.spec.ts` green — the auto-excluded group + banner mount in the composed surface the seeded axe spec already scans (RESEARCH Pitfall 6).
- Full `npm test` green; coverage CI gate not regressed; prefers-reduced-motion honoured on every animated element.
- No re-derived window/coverage math in the composer (helpers used); `selected` never mutated by a coverage change.
</verification>

<success_criteria>
- Widen→auto-off and narrow→auto-on work within the selected subset only, never adding an unselected strategy (WINDOW-02, WINDOW-03).
- Coverage-dropped rows animate into a distinct, accessible "Auto-excluded (outside window)" group with a minimal honest inline reason, reduced-motion respected (POLISH-02).
- Empty-intersection selected set shows a warning banner naming the outlier(s) + one-click deselect that restores a valid intersection (WINDOW-06).
- UI membership never desyncs from the engine divisor; the whole surface stays WCAG-AA (seeded composer-axe green).
</success_criteria>

<output>
Create `.planning/phases/57-window-control-auto-toggle-state-machine/57-03-SUMMARY.md` when done.
</output>
