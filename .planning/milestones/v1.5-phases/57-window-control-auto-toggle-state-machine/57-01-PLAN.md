---
phase: 57-window-control-auto-toggle-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scenario-window.ts
  - src/lib/scenario-window.test.ts
autonomous: true
requirements: [WINDOW-05, WINDOW-06]
must_haves:
  truths:
    - "The 'Full range' union window of a set of spans is derived by one shared helper, not re-derived in the composer"
    - "An empty-intersection set can be resolved to the specific outlier strategy id(s) that break the overlap"
  artifacts:
    - path: "src/lib/scenario-window.ts"
      provides: "unionOf(spans) + outlierIdsFor(spansById) pure helpers"
      contains: "export function unionOf"
    - path: "src/lib/scenario-window.test.ts"
      provides: "boundary-cell tests for unionOf + outlier detection"
      contains: "unionOf"
  key_links:
    - from: "src/lib/scenario-window.ts unionOf"
      to: "src/lib/scenario-window.ts intersectionOf"
      via: "same lexicographic min/max convention (symmetry)"
      pattern: "unionOf|intersectionOf"
---

<objective>
Add the two remaining pure window-derivation primitives to `scenario-window.ts` so the
composer (Plan 02/03) never hand-rolls interval math: `unionOf(spans)` (the "Full range
(some drop out)" preset target, WINDOW-05) and an outlier-detection helper that names the
strategy id(s) breaking an empty intersection (WINDOW-06). This is a leaf-file, zero-dep,
pure-TS addition mirroring the existing `intersectionOf`/`defaultWindowFor` contracts.

Purpose: keep ALL coverage-interval math in the single Phase-55 source of truth
(`scenario-window.ts`) — any window math authored in the composer is a smell (research
"Don't Hand-Roll"). Interface-first: these helpers are the contracts Plan 02/03 consume.

Output: two new exported functions + tests, no behavior change to existing exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-window-control-auto-toggle-state-machine/57-CONTEXT.md
@.planning/phases/57-window-control-auto-toggle-state-machine/57-RESEARCH.md

<interfaces>
<!-- Existing exports in src/lib/scenario-window.ts (Phase 55) — consume verbatim, mirror the style. -->

export interface CoverageSpan { first: string; last: string; }        // [first date WITH data, last date WITH data]
export interface CoverageWindow { start: string; end: string; }        // closed [start, end]

export function coverageSpanOf(dailyReturns: DailyPoint[]): CoverageSpan | null;
export function intersectionOf(spans: CoverageSpan[]): CoverageWindow | null;   // [max(firsts), min(lasts)] | null when start>end or empty
export function defaultWindowFor(spans: CoverageSpan[]): CoverageWindow | null; // delegates to intersectionOf
export function covers(span: CoverageSpan, window: CoverageWindow): boolean;    // INCLUSIVE-CLOSED: span.first <= window.start && span.last >= window.end

<!-- ALL comparison is LEXICOGRAPHIC "YYYY-MM-DD" string compare (dateday.ts convention). Never JS Date. Inputs never mutated. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unionOf(spans) helper (WINDOW-05 target)</name>
  <files>src/lib/scenario-window.ts, src/lib/scenario-window.test.ts</files>
  <read_first>
    - src/lib/scenario-window.ts (the full file — 106 lines; mirror intersectionOf at :73-83 exactly, opposite direction)
    - src/lib/scenario-window.test.ts (existing boundary-cell test style to extend)
  </read_first>
  <behavior>
    - unionOf([]) === null  (empty set → no window, mirrors intersectionOf empty case)
    - unionOf([{first:"2023-01-01",last:"2023-06-30"}]) === {start:"2023-01-01",end:"2023-06-30"}  (single span → itself)
    - unionOf([{first:"2023-01-01",last:"2023-06-30"},{first:"2023-03-01",last:"2023-12-31"}]) === {start:"2023-01-01",end:"2023-12-31"}  (widest bounds = [min(firsts), max(lasts)])
    - Two fully-disjoint spans still return a single spanning window (union never returns null for a non-empty set — the union of disjoint intervals is [earliest first, latest last])
    - Input array is not mutated
  </behavior>
  <action>
    Add `export function unionOf(spans: CoverageSpan[]): CoverageWindow | null` to
    src/lib/scenario-window.ts, directly below `intersectionOf`. It is the mirror of
    intersectionOf: `start = min over spans of span.first`, `end = max over spans of
    span.last`, lexicographic string compare, no JS Date. Return `null` ONLY when `spans`
    is empty (a non-empty union always has a valid window — unlike intersection there is no
    `start > end` degenerate). Write a doc-comment in the same voice as intersectionOf
    (:65-72) noting it is the "Full range (some drop out)" preset target and that
    non-covering members are dropped downstream by `covers`, not by this helper. Do NOT
    change any existing export.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario-window.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function unionOf" src/lib/scenario-window.ts` returns exactly one match
    - All new behavior cases above pass in scenario-window.test.ts
    - `npx tsc --noEmit` (or the project typecheck) reports no new errors in scenario-window.ts
    - No existing scenario-window export signature changed (git diff shows only additions)
  </acceptance_criteria>
  <done>unionOf exists, is pure (no mutation, lexicographic compare), returns null only on empty input, and its tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add outlier-detection helper (WINDOW-06 target)</name>
  <files>src/lib/scenario-window.ts, src/lib/scenario-window.test.ts</files>
  <read_first>
    - src/lib/scenario-window.ts (unionOf/intersectionOf just added — reuse them)
    - src/lib/scenario-window.test.ts
  </read_first>
  <behavior>
    - Given a map of {strategyId → CoverageSpan} whose overall intersection is non-null, the helper returns [] (no outlier — there is a common window)
    - Given a map whose intersection is null (empty), the helper returns the id(s) whose removal restores a non-null intersection over the remaining spans — i.e. the strategy(ies) breaking the overlap
    - Concrete: spans A=[2023-01..2023-12], B=[2024-06..2024-12] (disjoint) → intersection null → helper names the strategy that, when removed, restores overlap; if either removal restores it, name the one whose span is furthest out (latest first OR earliest last). Deterministic tie-break: prefer the id with the latest `first` (the strategy that starts after everyone else's data ends), else the earliest `last`.
    - Empty map → []  (no strategies, no outlier)
    - Single-strategy map → [] (a single span always intersects itself)
  </behavior>
  <action>
    Add `export function outlierIdsFor(spansById: Record<string, CoverageSpan>):
    string[]` to src/lib/scenario-window.ts below `covers`. Compute the intersection of
    all spans via `intersectionOf`; if non-null return []. If null, identify the
    breaking strategy(ies): the span with the maximum `first` (latest start) and the span
    with the minimum `last` (earliest end) are the two candidates whose bounds define the
    empty overlap; return the id(s) whose removal makes the remaining intersection
    non-null, deterministically (test-pinned tie-break above). Keep it O(n): find
    max-first id and min-last id, test removal of each. Pure, no mutation, lexicographic
    compare, no JS Date. Doc-comment it as the WINDOW-06 "name the outlier(s)" source.
    Do NOT re-implement intersection math — call `intersectionOf`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario-window.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function outlierIdsFor" src/lib/scenario-window.ts` returns exactly one match
    - `grep -c "intersectionOf" src/lib/scenario-window.ts` shows outlierIdsFor calls the shared helper (no re-derived `start > end` inline in outlierIdsFor)
    - All behavior cases above pass, including the empty-map → [] and single-strategy → [] cases
    - Removing every returned outlier id from the input yields a non-null `intersectionOf` (assert this invariant in a test)
  </acceptance_criteria>
  <done>outlierIdsFor exists, delegates intersection math to intersectionOf, and its removal-restores-overlap invariant is test-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-memory TS helpers over already-authorized in-memory returns; no network, no I/O, no auth, no persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | unionOf returning a fabricated window on empty input | mitigate | Return `null` on empty `spans` (test-pinned); never synthesize a window (`no-invented-data`). |
| T-57-02 | Information disclosure | outlierIdsFor mis-naming a non-outlier, misleading the WINDOW-06 fix | mitigate | Test the removal-restores-overlap invariant: every returned id, when removed, yields a non-null intersection. |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure internal TS. Legitimacy gate vacuous (RESEARCH: zero new deps). |
</threat_model>

<verification>
- `npx vitest run src/lib/scenario-window.test.ts` green (new + existing cases).
- Project typecheck clean; no existing export signature changed (git diff = additions only).
- `npm test` full frontend suite stays green (scenario-window.test.ts is a leaf; nothing else imports the new helpers yet).
</verification>

<success_criteria>
- `unionOf` and `outlierIdsFor` exist as pure exports in `src/lib/scenario-window.ts`, mirroring the existing helper contracts (null-on-empty, lexicographic, no mutation).
- Both are tested with boundary cells including the removal-restores-overlap invariant for outlier detection.
- Zero new dependencies; no change to existing `scenario-window.ts` exports.
</success_criteria>

<output>
Create `.planning/phases/57-window-control-auto-toggle-state-machine/57-01-SUMMARY.md` when done.
</output>
