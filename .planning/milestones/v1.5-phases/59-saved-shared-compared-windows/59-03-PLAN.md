---
phase: 59-saved-shared-compared-windows
plan: 03
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - src/app/(dashboard)/allocations/lib/scenario-compare.ts
  - src/app/(dashboard)/allocations/lib/scenario-compare.test.ts
  - src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx
  - src/app/(dashboard)/allocations/components/ScenarioCompareTable.test.tsx
autonomous: true
requirements: [PERSIST-03]
must_haves:
  truths:
    - "Each compared scenario column computes at its OWN persisted draft.window (heterogeneous windows are honest, not force-aligned)"
    - "The live-book compare column stays WINDOWLESS (Phase-55 own-book union lock) — no window is passed to its computeScenario"
    - "A windowless v2 draft in a compare set defaults to the intersection (same rule everywhere)"
    - "Each verdict.ok compare column shows a per-column effective-window label · {effective_start}–{effective_end} read from computeMetricsForDraft output (never re-derived)"
    - "The label is suppressed on undecodable (older-format) and below-sample-floor columns"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/scenario-compare.ts"
      provides: "post-collapse window injection onto deAliased.state per draft; live-book stays windowless"
      contains: "draft.window"
    - path: "src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx"
      provides: "augmented <tfoot> Window row — appends the effective {start}–{end} to the verdict.ok stamp"
      contains: "effective_start"
  key_links:
    - from: "computeMetricsForDraft (:145-155)"
      to: "computeScenario"
      via: "inject window onto deAliased.state POST-collapse (Pitfall 4)"
      pattern: "deAliased\\.state, window"
    - from: "ScenarioCompareTable <tfoot> verdict.ok branch (:266-269)"
      to: "c.metrics.effective_start / effective_end"
      via: "append · {start}–{end}, BlendHeader font-mono tabular-nums treatment"
      pattern: "effective_start|effective_end"
---

<objective>
Make compare (PERSIST-03) honor each scenario's OWN persisted window: inject `draft.window` into the
compare compute path POST-collapse (keeping the live-book column windowless per the Phase-55 lock), and
augment the compare table's `<tfoot>` "Window" row with a per-column effective `{start}–{end}` label so
heterogeneous windows are visible and honest.

Purpose: Plan 01 persisted the window in the draft; the compare engine currently passes NO window
(scenario-compare.ts:27-31 already flags this Phase-59 seam) and runs every column on the union path.
Compare must show 2+ scenarios each at its own saved window — force-aligning them would be dishonest.
This plan depends only on Plan 01's `window?` field (through the codec) and touches files disjoint from
Plan 02, so it runs parallel in Wave 2.

Output: window threaded per-draft in computeMetricsForDraft (live-book stays windowless); a per-column
window label in the compare table; extended compare and table tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-saved-shared-compared-windows/59-RESEARCH.md
@.planning/phases/59-saved-shared-compared-windows/59-PATTERNS.md
@.planning/phases/59-saved-shared-compared-windows/59-UI-SPEC.md
@.planning/phases/59-saved-shared-compared-windows/59-CONTEXT.md
@DESIGN.md
@src/app/(dashboard)/allocations/components/BlendHeader.tsx

<interfaces>
<!-- Seams the executor needs. Extracted from codebase at verified line anchors. -->

COMPARE COMPUTE (Threading Site 3 — POST-collapse injection) — scenario-compare.ts:
- :27-31 — the file's own comment already flags "Per-persisted-window compare alignment is Phase 59 (PERSIST-03), which will wire the saved window in through the same defaultWindowFor() helper."
- :120-143 — `computeMetricsForDraft(draft, liveInputs)` builds `projectionState: ScenarioState = { selected, weights, startDates }` (PRE-collapse; NO window here — Pitfall 4).
- :145-155 — `const deAliased = collapseAliasedHoldingStrategies(adapterOutput.strategies, projectionState, liveInputs.symbolByHoldingId); const dateMapCache = buildDateMapCache(deAliased.strategies); return computeScenario(deAliased.strategies, deAliased.state, dateMapCache);` — the POST-collapse call. `deAliased.state` is where `window: draft.window` must be spread (Pitfall 4: dealias reconstructs state and silently drops a window set on projectionState).
- :170-179 — `buildLiveBookDraft()` returns a synthetic draft with NO `window` field (schema_version 2). So `draft.window` is falsy for the live-book column → union path (Phase-55 own-book lock — the exact intended behavior). Do NOT inject a window here.

`computeScenario` emits `effective_start: string | null` and `effective_end: string | null` on
BOTH the windowed and union paths (verified src/lib/scenario.ts:154-155) — the label reads these.

COMPARE PANEL (no change expected) — ScenarioComparePanel.tsx:203-229 decodes each row via codec
(`decodeDraft` → `computeMetricsForDraft(draft, liveInputs)` at :211) and the live-book via
`computeMetricsForDraft(buildLiveBookDraft(), ...)` at :228-229. The window flows INSIDE
computeMetricsForDraft (it reads `draft.window`), so the panel needs no edit — the per-draft window
auto-flows through the decoded draft.

COMPARE TABLE <tfoot> (Contract 2 host) — ScenarioCompareTable.tsx:242-282. The "Window" row maps
`allColumns`; per column `const verdict = evaluateSampleFloor(c.metrics.n);` then three branches:
- :259 `c.undecodable` → OLDER_FORMAT_STAMP (SUPPRESS the date label here)
- :266-269 `verdict.ok` → `<span className="text-xs font-metric text-text-muted">{methodologyLine(c.metrics.n)}</span>` (APPEND the date label HERE only)
- :270-277 below-floor → SAMPLE_FLOOR_HEADING + body (SUPPRESS the date label here)

BlendHeader date treatment (Contract 2 typography source) — BlendHeader.tsx:64-77: dates in
`font-mono tabular-nums`, en-dash `–`, lexicographic `YYYY-MM-DD`. The <tfoot> cell is already
`text-fixed-11 text-text-muted` (:248).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: inject each draft's window POST-collapse in computeMetricsForDraft; keep the live-book column windowless</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-compare.ts, src/app/(dashboard)/allocations/lib/scenario-compare.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-compare.ts:27-31, :120-156, :170-179 (the compute path + live-book builder)
    - src/app/(dashboard)/allocations/lib/scenario-compare.test.ts (existing per-column assertions — the analog)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:1830-1836 (the canonical POST-collapse engineState idiom to replicate)
    - .planning/phases/59-saved-shared-compared-windows/59-PATTERNS.md §"scenario-compare.ts — inject window POST-collapse"
  </read_first>
  <behavior>
    - A draft with `window: { start, end }` computed via computeMetricsForDraft produces metrics whose `member_count` reflects membership under that window (smaller than the full selected set when a member does not cover the window) and whose `effective_start`/`effective_end` reflect the window — NOT the union.
    - Two drafts with DIFFERENT windows in the same compare set each compute at their own window (heterogeneous — the results are independent, not force-aligned).
    - The live-book draft (`buildLiveBookDraft()`, no window field) computes on the union path — its metrics are byte-identical to the current windowless behavior (Phase-55 own-book lock).
    - A windowless v2-shaped draft (no window) computes on the union/default path (no window key) — unchanged from today.
  </behavior>
  <action>
    In computeMetricsForDraft (:145-155), replace the direct `computeScenario(deAliased.strategies, deAliased.state, dateMapCache)` return with a window-injecting variant, matching the composer's engineState idiom (Pitfall 4 — inject POST-collapse on `deAliased.state`, NEVER on the pre-collapse projectionState at :139): `const engineState = draft.window ? { ...deAliased.state, window: draft.window } : deAliased.state; return computeScenario(deAliased.strategies, engineState, dateMapCache);`. Because `buildLiveBookDraft()` omits `window`, `draft.window` is falsy for the live-book column → it stays on the union path automatically (Phase-55 lock — verified intended). Optionally bump `buildLiveBookDraft`'s hardcoded `schema_version: 2` (:172) to `SCENARIO_SCHEMA_VERSION` for consistency (harmless — never decoded/persisted, research A3) ONLY IF no test asserts the literal 2 (grep scenario-compare.test.ts first); if any test asserts `schema_version: 2` on the live-book draft, leave it.

    Extend scenario-compare.test.ts: (a) a draft with a window produces windowed member_count/effective bounds distinct from the union; (b) two drafts with different windows compute independently (heterogeneous); (c) the live-book draft stays windowless (union path — assert its effective bounds match the union, unchanged); (d) a windowless draft is unchanged from today. These encode WHY (Rule 9): "compare shows each scenario at its own honest window, and the own-book column keeps union behavior".
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-compare.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "deAliased.state, window" src/app/(dashboard)/allocations/lib/scenario-compare.ts` matches (window injected POST-collapse).
    - The pre-collapse `projectionState` (:139) still has NO window key (Pitfall 4 respected).
    - A windowed draft's member_count/effective bounds differ from union when a member doesn't cover the window; the live-book column's metrics are unchanged (union path).
    - Two heterogeneous-window drafts compute independently.
  </acceptance_criteria>
  <done>Each compare column computes at its own persisted window; the live-book column stays windowless per the Phase-55 lock.</done>
</task>

<task type="auto">
  <name>Task 2: augment the compare table <tfoot> Window row with a per-column effective {start}–{end} label</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx, src/app/(dashboard)/allocations/components/ScenarioCompareTable.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx:242-282 (the <tfoot> Window row + the three branches)
    - src/app/(dashboard)/allocations/components/ScenarioCompareTable.test.tsx (existing table render assertions — the analog)
    - src/app/(dashboard)/allocations/components/BlendHeader.tsx:64-77 (the font-mono tabular-nums en-dash date treatment to mirror)
    - .planning/phases/59-saved-shared-compared-windows/59-UI-SPEC.md Contract 2 + §"Per-column window label placement (augment, don't duplicate)"
  </read_first>
  <action>
    AUGMENT (do NOT replace or duplicate — Pitfall 6) the `verdict.ok` branch ONLY (:266-269): append
    ` · {effective_start}–{effective_end}` to the existing `methodologyLine(c.metrics.n)` stamp, reading
    `c.metrics.effective_start` / `c.metrics.effective_end` (NEVER re-derive the window). Wrap the two dates
    in `font-mono tabular-nums` per BlendHeader's convention (:64-77), en-dash `–`, `YYYY-MM-DD` bounds. The
    label color stays `text-text-muted` (never accent/warning/winner — 59-UI-SPEC Color: a quiet honesty
    caption). If either effective bound is null (degenerate), omit the date range (show just the day-count
    stamp). Claude's-discretion: inline in the <tfoot> branch or extract a small `CompareWindowLabel` — either
    is acceptable per 59-UI-SPEC. SUPPRESS the date label on the `undecodable` branch (:259) and the
    below-sample-floor branch (:270) — no honest window to show there.

    Extend ScenarioCompareTable.test.tsx: (a) a verdict.ok column renders `· {start}–{end}` alongside the
    day-count stamp; (b) two columns with different windows render DIFFERENT date ranges (heterogeneous
    visible); (c) an undecodable column shows only OLDER_FORMAT_STAMP (no date range); (d) a below-floor
    column shows only the sample-floor copy (no date range).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioCompareTable.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "effective_start" src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx` matches inside the verdict.ok branch.
    - The date range is appended to (not replacing) `methodologyLine(c.metrics.n)`; the undecodable + below-floor branches carry NO date range.
    - Dates render in `font-mono tabular-nums` with an en-dash; color is `text-text-muted` (not accent/warning).
    - Tests assert per-column heterogeneous date ranges + suppression on undecodable/below-floor.
    - `ScenarioCompareTable.all-columns.test.tsx` still passes (no regression to the existing stamp).
  </acceptance_criteria>
  <done>Each verdict.ok compare column shows both how many days and over which window; heterogeneous windows read honestly; degenerate/older-format columns suppress the label.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decoded draft → compare engine | Each row's decoded draft (owner-authored, codec-validated) crosses into per-column recompute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-08 | Tampering (silent union fallback / window dropped) | computeMetricsForDraft window injection | mitigate | Inject the window POST-collapse on `deAliased.state`, NEVER on the pre-collapse projectionState — dealias reconstructs state and silently drops a window set pre-collapse (Pitfall 4). Pinned by the Task-1 windowed-vs-union member_count assertion. |
| T-59-09 | Tampering (own-book union lock broken) | live-book compare column | mitigate | `buildLiveBookDraft()` omits `window` → the live column runs the union path (Phase-55 own-book lock). Pinned by the Task-1 live-book-unchanged assertion. Never inject a window into the live column. |
| T-59-10 | Information Disclosure (mislabeled window) | <tfoot> per-column label | accept | The label reads engine-emitted `effective_start`/`effective_end` (never re-derives), suppressed on degenerate/older-format columns — a wrong label would be a display bug, not a data leak; low severity. |
| T-59-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed (first-party TS only). Legitimacy gate vacuously satisfied. |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/lib/scenario-compare.test.ts" "src/app/(dashboard)/allocations/components/ScenarioCompareTable.test.tsx"` — green.
- `ScenarioCompareTable.all-columns.test.tsx` + `ScenarioComparePanel.test.tsx` still green (no regression).
- Wave gate: `npm run test:coverage` full suite green + above the blocking ratchet; Phase-55 frozen-spine + BLEND-07 + PARITY-01 guards green.
- `e2e/composer-axe.spec.ts` still green (the label is static caption text in an already-labeled <tfoot> row — no new a11y violation).
</verification>

<success_criteria>
- Each compare column computes at its own persisted window; the live-book column stays windowless.
- The compare table shows a per-column effective {start}–{end} label on verdict.ok columns, suppressed on undecodable/below-floor columns, reading engine-emitted bounds only.
- `scenario.ts` and `scenario-window.ts` untouched (frozen); no engine change.
</success_criteria>

<output>
Create `.planning/phases/59-saved-shared-compared-windows/59-03-SUMMARY.md` when done.
</output>
