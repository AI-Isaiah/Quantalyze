---
phase: 55-coverage-window-compute-core
plan: 03
type: execute
wave: 3
depends_on: [55-01, 55-02]
files_modified:
  - src/lib/scenario-blend07.test.ts
  - src/lib/__fixtures__/blend07-six-series.json
  - .planning/phases/55-coverage-window-compute-core/BLEND-07-verification.md
autonomous: true
requirements: [BLEND-07]
must_haves:
  truths:
    - "A committed, deterministic 6-series fixture holds the raw date/daily_return series for mm/neon1/pokeokx/uc244 + OKX + Bybit (no prod pull at test time)"
    - "An independent from-scratch numpy computation of the equal-weight blend over the max-overlap window is recorded in a committed markdown artifact, with both OLD-convention and NEW-window-bounded numbers documented"
    - "A vitest gate asserts computeScenario matches the numpy blend to floating-point precision over that window, with divisor == live-member count"
    - "This gate is green BEFORE any golden re-bake (Phase 60) — the #1 defense against a re-bake masking a regression"
  artifacts:
    - path: ".planning/phases/55-coverage-window-compute-core/BLEND-07-verification.md"
      provides: "The from-scratch numpy script + the 6-strategy dataset numbers (total/CAGR/Sharpe/MaxDD/n) for both OLD union convention and NEW window-bounded convention"
      contains: "numpy"
    - path: "src/lib/__fixtures__/blend07-six-series.json"
      provides: "Deterministic committed fixture of the 6 real strategy series (or their overlap window) — no prod dependency"
      contains: "date"
    - path: "src/lib/scenario-blend07.test.ts"
      provides: "The CI-enforced gate: computeScenario over defaultWindowFor(6 series) matches the numpy numbers; member_count === live-member count"
      contains: "computeScenario"
  key_links:
    - from: "src/lib/scenario-blend07.test.ts"
      to: "src/lib/scenario.ts"
      via: "runs computeScenario with an explicit window = defaultWindowFor(...)"
      pattern: "computeScenario"
    - from: "src/lib/scenario-blend07.test.ts"
      to: "src/lib/scenario-window.ts"
      via: "derives the max-overlap window via defaultWindowFor"
      pattern: "defaultWindowFor"
---

<objective>
Record the BLEND-07 verification artifact: a committed deterministic 6-series fixture, a from-scratch numpy computation of the equal-weight blend over the max-overlap window (both OLD and NEW numbers), and a vitest gate asserting `computeScenario` matches the numpy result to floating-point precision with `divisor == live-member count`. This gate must be GREEN before Phase 60 re-bakes any golden — the milestone's highest-risk pitfall (a re-bake masking a math bug) is defended here.

Purpose: The engine rewrite (55-02) proved itself against hand-written unit cases. BLEND-07 proves it against an INDEPENDENT re-derivation on the real 6-strategy production dataset (mm/neon1/pokeokx/uc244 + OKX + Bybit) — the same dataset the goldens encode. This is the ADR's #1 parity checkpoint before any bake.

Output: `BLEND-07-verification.md` (numpy script + numbers), `src/lib/__fixtures__/blend07-six-series.json` (the deterministic fixture), `src/lib/scenario-blend07.test.ts` (the gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-coverage-window-compute-core/55-CONTEXT.md
@.planning/phases/55-coverage-window-compute-core/55-RESEARCH.md
@src/lib/scenario-window.ts
@src/lib/scenario.ts

<interfaces>
<!-- Contracts the gate builds against. From 55-01 + 55-02 (both landed). -->

From src/lib/scenario.ts (post-55-02):
```typescript
export function computeScenario(strategies, state, dateMapCache): ComputedMetrics;
// ComputedMetrics now includes: member_count: number, member_ids: string[],
// plus twr, cagr, sharpe, sortino, max_drawdown, volatility, n, effective_start, effective_end.
// Payload rounding (scenario.ts:451-456): twr/cagr/vol/maxDD .toFixed(5); sharpe/sortino .toFixed(3).
export function buildDateMapCache(strategies): Map<string, Map<string, number>>;
```

From src/lib/scenario-window.ts (55-01):
```typescript
export function defaultWindowFor(spans): { start: string; end: string } | null;
export function coverageSpanOf(dailyReturns): { first: string; last: string } | null;
```

BLEND-07 dataset facts (from CONTEXT §Specifics + 55-RESEARCH § BLEND-07 Harness):
- 6 strategies: mm / neon1 / pokeokx / uc244 + OKX + Bybit (real production series).
- OLD-convention empirical baseline (union, for CONTRAST in the artifact): equal-weight all 6 → total +586.86%, CAGR 51.82%, Sharpe 2.43, MaxDD −15.15%, n=1163.
- NEW window-bounded numbers WILL differ — documenting both is the whole point.
- numpy formulas (must MATCH scenario.ts:331-378): twr = ∏(1+r)−1; CAGR = (1+twr)^(252/n)−1; vol = std(ddof=1)·√252; Sharpe = mean·252/vol; MaxDD = min(equity/cummax−1). 252-day annualization.
- Fixture provenance (RESEARCH A2, recommended): commit a deterministic fixture of the 6 series (or their overlap window) into `src/lib/__fixtures__/` — NOT an authed prod pull at test time (non-deterministic, can't run in CI). numpy runs ONCE (dev) to produce the artifact numbers; the fixture is the durable input for both the numpy script and the vitest gate.
- "Floating-point precision" concretely (RESEARCH step 5): assert on the ROUNDED engine payload (toFixed 5/3) vs the numpy value rounded to the same decimals, OR `toBeCloseTo` at 5 decimals for the raw comparison — the raw series agree to within ~1e-10 before rounding.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the deterministic 6-series fixture and record the from-scratch numpy artifact</name>
  <files>src/lib/__fixtures__/blend07-six-series.json, .planning/phases/55-coverage-window-compute-core/BLEND-07-verification.md</files>
  <read_first>
    - src/lib/scenario.ts (:331-378 metrics block + :451-456 payload rounding — the numpy formulas must match these EXACTLY)
    - src/lib/scenario-window.ts (defaultWindowFor — the numpy script computes the same max-overlap window)
    - .planning/phases/55-coverage-window-compute-core/55-RESEARCH.md (§ BLEND-07 Verification Harness — the 6-step structure; Assumption A2 fixture-not-prod)
    - .planning/phases/55-coverage-window-compute-core/55-CONTEXT.md (§Specifics — the 6-strategy dataset + OLD-convention numbers to contrast)
    - .planning/SCENARIO-COVERAGE-WINDOW-ADR.md (:21 the "matched to fp precision" claim)
    - analytics-service/tests (the existing numpy golden-fixture precedent — mirror its fixture layout/format)
  </read_first>
  <action>
    Source the 6 real strategy series (mm/neon1/pokeokx/uc244 + OKX + Bybit) and commit them as a DETERMINISTIC fixture `src/lib/__fixtures__/blend07-six-series.json` — a map of strategy id → array of `{ date, value }` (daily returns), covering at least the max-overlap window plus enough head/tail that the coverage spans are meaningful (per RESEARCH A2: fixture, NOT a prod pull at test time). If the raw series are not already available in-repo, source them via the analytics-service Python venv / the ADR verification-session data and freeze them into the fixture (run-once; the committed JSON is the durable input thereafter). Write the from-scratch numpy script into `.planning/phases/55-coverage-window-compute-core/BLEND-07-verification.md`: the script loads the fixture, computes the intersection window (`max(firsts), min(lasts)`), keeps only members covering it, 0-fills interior gaps over the window, blends equal-weight (`Σ r / N`), then computes twr/CAGR/vol/Sharpe/MaxDD with the EXACT formulas from scenario.ts:331-378 (252-annualization, std ddof=1). Run it once and record in the markdown: (a) the NEW window-bounded numbers (total/CAGR/Sharpe/MaxDD/n + the derived window [winStart,winEnd] + the live member count), and (b) the OLD-convention union numbers for contrast (total +586.86%, CAGR 51.82%, Sharpe 2.43, MaxDD −15.15%, n=1163) with a one-line note that the difference is the intended effect of killing tail dilution. The markdown numbers are the assertion targets for the gate in Task 2.
  </action>
  <verify>
    <automated>test -f src/lib/__fixtures__/blend07-six-series.json && test -f .planning/phases/55-coverage-window-compute-core/BLEND-07-verification.md && node -e "const f=require('./src/lib/__fixtures__/blend07-six-series.json'); const ids=Object.keys(f); if(ids.length!==6) throw new Error('expected 6 series, got '+ids.length); for(const id of ids){ if(!Array.isArray(f[id])||f[id].length===0) throw new Error('empty series '+id); if(f[id][0].date===undefined||f[id][0].value===undefined) throw new Error('bad shape '+id);} console.log('fixture OK: 6 series');"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/__fixtures__/blend07-six-series.json` exists, is valid JSON, and has exactly 6 series each with `{ date, value }` entries (verify command above passes).
    - `.planning/phases/55-coverage-window-compute-core/BLEND-07-verification.md` exists and contains: the full from-scratch numpy script, the NEW window-bounded numbers (total/CAGR/Sharpe/MaxDD/n + window + member count), AND the OLD union numbers (+586.86% / 51.82% / 2.43 / −15.15% / 1163) for contrast.
    - `grep -qi "numpy" .planning/phases/55-coverage-window-compute-core/BLEND-07-verification.md` — the numpy script is present.
    - The numpy formulas in the artifact use 252-annualization and std ddof=1 (grep the artifact for `252` and `ddof`).
  </acceptance_criteria>
  <done>Deterministic 6-series fixture committed; numpy script + both OLD and NEW numbers recorded in the markdown artifact; formulas match scenario.ts (252, ddof=1).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the vitest gate asserting computeScenario == numpy over the max-overlap window</name>
  <files>src/lib/scenario-blend07.test.ts</files>
  <read_first>
    - src/lib/scenario.ts (computeScenario signature + the payload rounding at :451-456 — determines the precision target)
    - src/lib/scenario-window.ts (defaultWindowFor / coverageSpanOf — used to derive the window from the fixture)
    - src/lib/__fixtures__/blend07-six-series.json (the fixture committed in Task 1)
    - .planning/phases/55-coverage-window-compute-core/BLEND-07-verification.md (the numpy numbers = the assertion targets)
    - src/lib/scenario.test.ts (the existing test scaffolding — how strategies/ScenarioState/buildDateMapCache are constructed for computeScenario)
  </read_first>
  <behavior>
    - Loads the committed fixture, builds StrategyForBuilder[] + a ScenarioState with equal weights + an explicit `window = defaultWindowFor(coverage spans of the selected+enabled set)`.
    - Runs computeScenario and asserts twr/cagr/sharpe/max_drawdown match the numpy numbers recorded in BLEND-07-verification.md to floating-point precision (toBeCloseTo at 5 decimals for raw, or exact on the rounded payload per scenario.ts:451-456).
    - Asserts `metrics.member_count === <live member count>` and `metrics.n === <window N>` from the artifact — the divisor equals the visible live-member count, not the started-strategy count.
    - Asserts `metrics.effective_start === winStart` and `metrics.effective_end === winEnd`.
  </behavior>
  <action>
    Create `src/lib/scenario-blend07.test.ts`. Import the fixture, `computeScenario` + `buildDateMapCache` from `./scenario`, and `defaultWindowFor` + `coverageSpanOf` from `./scenario-window`. Build the 6 `StrategyForBuilder` objects from the fixture (mirror how `scenario.test.ts` constructs them), an equal-weight `ScenarioState` with all 6 selected, and set `state.window = defaultWindowFor(the 6 coverage spans)`. Run `computeScenario` and assert each metric equals the artifact number (use `toBeCloseTo(target, 5)` for raw twr/cagr/vol/maxDD and `toBeCloseTo(target, 3)` for sharpe/sortino, OR compare the toFixed-rounded payload to the artifact's rounded value — match scenario.ts:451-456 rounding). Assert `member_count === live-member count` and `n === window N` from the artifact. This is the CI-enforced gate; it MUST run in `npm test` (no seed-gating, no external network — the fixture is committed and deterministic).
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario-blend07.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/scenario-blend07.test.ts` passes.
    - The test asserts `member_count === <live member count>` (grep the test for `member_count`) — divisor == visible membership.
    - The test asserts twr/cagr/sharpe/max_drawdown against the numpy numbers with toBeCloseTo (or rounded-payload equality) — grep the test for `toBeCloseTo` or `toFixed`.
    - The test is NOT seed-gated and makes no network call (`grep -n "fetch\|supabase\|HAS_SEED" src/lib/scenario-blend07.test.ts` → no matches) — it runs hermetically in `npm test`.
    - `npm test` (full suite) remains green with this gate included.
  </acceptance_criteria>
  <done>The vitest gate loads the committed fixture, runs computeScenario over the max-overlap window, and matches the numpy numbers to fp precision with member_count == live-member count; runs hermetically in the full suite; green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture → test | The committed fixture is trusted deterministic input; no network/prod pull at test time (RESEARCH A2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03-01 | Repudiation / silent regression | A golden re-bake (Phase 60) canonizing a Phase-55 math bug | mitigate | This entire plan IS the mitigation: independent numpy re-derivation recorded + CI gate green BEFORE any bake (BLEND-07, Pitfall 8 — the milestone's highest risk) |
| T-55-03-02 | Tampering (non-determinism) | A non-deterministic prod pull making the gate flaky / un-runnable in CI | mitigate | Deterministic committed fixture; test makes no network/supabase call (grep-gated) |
| T-55-03-03 | Information disclosure (misleading data) | The gate passing on a divisor that disagrees with visible membership | mitigate | Explicit `member_count === live-member count` assertion (BLEND-06 tie-in) |
| T-55-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new npm deps; numpy is the already-provisioned analytics-service venv, run-once dev-only to produce the artifact — not a new project dependency. |
</threat_model>

<verification>
- `npx vitest run src/lib/scenario-blend07.test.ts` green.
- `BLEND-07-verification.md` records both OLD and NEW numbers + the numpy script.
- The gate runs hermetically in `npm test` (no network, no seed-gate).
- Fixture is valid 6-series JSON.
</verification>

<success_criteria>
The BLEND-07 correctness anchor exists and is green: a deterministic 6-series fixture, an independent from-scratch numpy computation recorded in a committed markdown artifact (OLD + NEW numbers), and a CI-enforced vitest gate asserting `computeScenario` matches the numpy blend over the max-overlap window to floating-point precision with `divisor == live-member count` — recorded BEFORE any Phase 60 golden re-bake (BLEND-07).
</success_criteria>

<output>
Create `.planning/phases/55-coverage-window-compute-core/55-03-SUMMARY.md` when done — note the derived window, the NEW numbers, and confirm the gate is green (the Phase 60 precondition).
</output>
