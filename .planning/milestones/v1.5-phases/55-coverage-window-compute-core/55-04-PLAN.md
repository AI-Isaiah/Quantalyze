---
phase: 55-coverage-window-compute-core
plan: 04
type: execute
wave: 4
depends_on: [55-02]
files_modified:
  - src/app/(dashboard)/allocations/lib/scenario-benchmark.ts
  - src/app/(dashboard)/allocations/lib/scenario-compare.ts
  - src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx
autonomous: true
requirements: [PARITY-02]
must_haves:
  truths:
    - "The stale 'zero-filled UNION date axis' comment in scenario-benchmark.ts is corrected to reflect coverage-window membership; the inner-join honesty is verified still correct on the shorter series"
    - "The stale 'heterogeneous windows … Phase 24' comments in scenario-compare.ts and ScenarioCompareTable.tsx are corrected to reflect the coverage-window reality (per-window compare is Phase 59)"
    - "stress/VaR, benchmark, Monte-Carlo, compare, and the KPI strip produce correct results on the coverage-window series with honest sample-floor behavior — a metric that used to clear 60 and now shows em-dash is CORRECT, not a regression"
    - "No sample-floor VALUE is changed (they are pinned single-sources); only the comments and the re-verification change"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts"
      provides: "Corrected header comment (was 'zero-filled UNION'); inner-join re-verified correct on the member-intersection series"
      contains: "coverage"
    - path: "src/app/(dashboard)/allocations/lib/scenario-compare.ts"
      provides: "Corrected heterogeneous-window comment reflecting coverage-window reality"
      contains: "coverage"
  key_links:
    - from: "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts"
      to: "src/lib/scenario.ts"
      via: "consumes portfolio_daily_returns (the now member-windowed series)"
      pattern: "portfolio_daily_returns"
---

<objective>
Clear the latent union-window assumptions and stale header comments in the three Category-B consumers (`scenario-benchmark.ts`, `scenario-compare.ts`, `ScenarioCompareTable.tsx`), and re-verify the full consumer blast radius (stress/VaR, benchmark, Monte-Carlo, compare, KPI strip) produces correct results on the now-shorter coverage-window series with honest sample-floor behavior.

Purpose: The engine now emits a member-intersection series for scenario-tab callers, but three consumer files still DOCUMENT a "zero-filled UNION" / "heterogeneous windows (Phase 24)" reality that is no longer true — a maintenance trap that will mislead the next reader (Rule 3: clean up the mess this change created). The re-verify confirms no consumer silently regressed: every sample-floor (`n<10`, the 60-day overlap floor, stress varN/betaN, MC block-length, correlation T>1) must gate HONESTLY on the shorter window. A metric that used to clear 60 days and now honestly shows an em-dash on a short intersection is CORRECT — this plan proves that, without changing any floor value.

Output: three corrected comments + a re-verification pass documented in the SUMMARY (which suites ran, which metrics honestly changed to em-dash on a short window). No math changes; no floor-value changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-coverage-window-compute-core/55-CONTEXT.md
@.planning/phases/55-coverage-window-compute-core/55-RESEARCH.md
@src/lib/scenario.ts

<interfaces>
<!-- The Category-A/B consumer map and the exact stale comments. From 55-RESEARCH § Consumer Blast Radius + § Sample-Floor Honesty. -->

Post-55-02, `computeScenario` scenario-tab output series is the MEMBER-INTERSECTION window (shorter), not the union. Every consumer below reads the engine's OUTPUT series and does NO bespoke blend math — the only work is (a) fixing stale comments and (b) verifying sample-floors gate honestly on the shorter series.

Stale comments to FIX (Category B):
- `scenario-benchmark.ts:8-15` — header says the scenario engine's date axis "is a zero-filled UNION (late strategies contribute 0 before their inception…)". This is now the member-intersection window on the scenario surface. The inner-join (`innerJoinByDate`, intersection ∩ smaller series) stays CORRECT; only the comment is wrong. Correct it to describe coverage-window membership; keep the "benchmark must NOT be zero-filled" honesty note.
- `scenario-compare.ts:27-29` — says "Heterogeneous windows. Each draft reports its OWN overlap n; the helper does not force a common window (common-window alignment is Phase 24)." Per-window compare is now Phase 59 (PERSIST-03). Correct the reference and the "does not force a common window" framing to the coverage-window reality.
- `ScenarioCompareTable.tsx:19-20` — says "there is NO single shared-window header (common-window alignment is Phase 24)." Correct to reflect coverage-window reality; Phase 24 reference is stale.

Category-A consumers to RE-VERIFY (read output series, no comment change unless a stale one surfaces):
- `scenario-stress.ts:41-44,111,124` — `varN = portfolioDaily.length` (now shorter), `betaN = bench.n` (shifts). Both gate on `evaluateSampleFloor(n, SAMPLE_FLOOR_OVERLAPPING_DAYS=60)`.
- `scenario-montecarlo.ts:13-16,63` — block-bootstrap; guard block-length ≤ window n; honest "insufficient history" not fabricated bands.
- `KpiStrip.tsx:101,405` — reads null-suppressed metrics; shorter window → different honest numbers; verify null-render.
- `MonteCarloSection.tsx:60`, `ScenarioBenchmarkSection.tsx:134`, `StressVarSection.tsx:135-139` — pass shorter series through, no math.

Sample-floors that MUST NOT change value (pinned single-sources — verify honesty only):
- Engine `n < 10` early-return (scenario.ts:210).
- `SAMPLE_FLOOR_OVERLAPPING_DAYS = 60` (sample-floor.ts:37, pinned by sample-floor.test.ts).
- Correlation `T > 1` (scenario.ts:420); a 1-member window → no pairs → avg null (scenario.ts:431).

DO NOT touch Category-C: `queries.ts:2208/2356`, `computeCompositeCurve`, `share-resolve.ts` (they run the union path — window absent — and stay byte-identical; Phase 59 owns share-resolve).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the three stale union-window comments</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-benchmark.ts, src/app/(dashboard)/allocations/lib/scenario-compare.ts, src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-benchmark.ts (the header comment :1-25, especially :8-15; the innerJoinByDate + computeScenarioBenchmark bodies to confirm the join logic is comment-only-stale)
    - src/app/(dashboard)/allocations/lib/scenario-compare.ts (the header comment :24-32; computeMetricsForDraft to confirm no bespoke window math)
    - src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx (the header comment :16-22)
    - .planning/phases/55-coverage-window-compute-core/55-RESEARCH.md (§ Consumer Blast Radius — Category-B rows 2/5/6)
    - .planning/phases/55-coverage-window-compute-core/55-CONTEXT.md (Deferred: per-window compare is Phase 59, NOT this phase)
  </read_first>
  <action>
    Correct ONLY the stale header comments (no logic changes). In `scenario-benchmark.ts:8-15`: replace the "the scenario engine's own date axis is a zero-filled UNION (late strategies contribute 0 before their inception…)" clause with an accurate description — the scenario-surface engine now blends over an explicit coverage window (members whose span ⊇ the window; constant divisor), so the portfolio series is the member-intersection window; the inner-join against the benchmark stays load-bearing (intersection ∩ the smaller series) and the benchmark must still NOT be zero-filled (keep that honesty note verbatim). In `scenario-compare.ts:27-29`: replace "common-window alignment is Phase 24" with the coverage-window reality — each draft defaults to its OWN intersection window; per-persisted-window compare alignment is Phase 59 (PERSIST-03); this helper still does not force a shared window. In `ScenarioCompareTable.tsx:19-20`: replace "there is NO single shared-window header (common-window alignment is Phase 24)" with the coverage-window reality — each column stamps its own methodology caption over its own coverage window; Phase 59 owns shared-window compare. Do NOT change any inner-join, formatting, null-render, or divisor logic in these files — comments only. Match the surrounding comment style.
  </action>
  <verify>
    <automated>grep -riq "zero-filled UNION\|zero-filled union" "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts" && echo "STALE COMMENT STILL PRESENT" && exit 1; grep -rq "Phase 24" "src/app/(dashboard)/allocations/lib/scenario-compare.ts" "src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx" && echo "STALE Phase-24 REF STILL PRESENT" && exit 1; echo "stale comments cleared"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ri "zero-filled union" "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts"` returns NO matches (stale union comment cleared).
    - `grep -r "Phase 24" "src/app/(dashboard)/allocations/lib/scenario-compare.ts" "src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx"` returns NO matches (stale Phase-24 references cleared).
    - `grep -riq "coverage window\|coverage-window\|member" "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts"` — the corrected comment describes the coverage-window reality.
    - `git diff --stat` on these three files shows ONLY comment-line changes (no logic churn) — the diff touches comment lines only.
    - The benchmark "must NOT be zero-filled" honesty note is preserved (grep the file for "NOT be zero-filled" or equivalent).
  </acceptance_criteria>
  <done>All three stale union/Phase-24 comments corrected to the coverage-window reality; no logic changed; benchmark honesty note preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify the consumer blast radius gates honestly on the shorter window</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-stress.ts, src/app/(dashboard)/allocations/lib/scenario-montecarlo.ts, src/app/(dashboard)/allocations/components/KpiStrip.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-stress.ts (:41-44 the two-N distinction, :111 varN, :124 betaN, and the evaluateSampleFloor gates)
    - src/app/(dashboard)/allocations/lib/scenario-montecarlo.ts (:13-16, :63 block-length + floor)
    - src/app/(dashboard)/allocations/components/KpiStrip.tsx (:101, :405 null-suppressed metric render)
    - src/lib/sample-floor.ts (:37 SAMPLE_FLOOR_OVERLAPPING_DAYS=60 — the pinned single source; DO NOT change)
    - .planning/phases/55-coverage-window-compute-core/55-RESEARCH.md (§ Sample-Floor Honesty on the Shorter Window — the full floor inventory)
    - .planning/phases/55-coverage-window-compute-core/55-VALIDATION.md (the PARITY-02 row + sampling rate)
  </read_first>
  <action>
    Re-verify — do NOT change floor VALUES (they are pinned single-sources; changing them fails their pin tests). Run the full existing consumer test suites (benchmark, stress, Monte-Carlo, compare, KPI strip) with the rewritten engine and confirm each passes on the shorter member-window series. For each sample-floor in the RESEARCH inventory (`n<10`, the 60-day overlap floor, stress `varN`/`betaN`, MC block-length ≤ n, correlation `T>1`), confirm the gate fires HONESTLY on a short intersection window: a metric that used to clear 60 days and now shows an em-dash on a shorter intersection is CORRECT behavior, not a regression — verify it null-renders (em-dash) rather than fabricating a value or dividing by zero. If a consumer TEST fails, trace to root: it is either (a) a genuine consumer bug the shorter window exposed (fix at root), or (b) a test that pinned the OLD union-window numbers and legitimately needs re-baselining on the new series (re-baseline with an annotation, enumerate in SUMMARY) — decide per failure, never blanket `--update-snapshots`. Document in the SUMMARY: which suites ran, which (if any) metrics honestly changed to em-dash on a short window, and confirmation that no floor VALUE changed and `sample-floor.test.ts` stays green.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` (full suite) is green — all consumer suites (benchmark, stress, MC, compare, KPI) pass on the coverage-window series.
    - `npx vitest run src/lib/sample-floor.test.ts` passes — the 60-day floor single-source is UNCHANGED (`grep -n "SAMPLE_FLOOR_OVERLAPPING_DAYS = 60" src/lib/sample-floor.ts` still returns the pinned value).
    - `git diff src/lib/sample-floor.ts` is EMPTY (no floor-value change).
    - `npm run test:coverage` still clears the blocking gate (lines 82 / stmts 80 / fns 74 / br 72).
    - The SUMMARY documents the re-verify: suites run, any honest em-dash changes on short windows, and that no floor value changed.
    - Category-C files (`queries.ts`, `computeCompositeCurve`, `share-resolve.ts`) are NOT in `git diff` (`git diff --name-only` shows none of them) — own-book/composite/share untouched.
  </acceptance_criteria>
  <done>Full suite green on the coverage-window series; every sample-floor gates honestly (em-dash where the window is too short, no fabrication, no ÷0); no floor value changed; Category-C untouched; re-verify documented in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Comment corrections + a re-verification pass on pure client-side consumers. No new auth/network/persistence/input boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04-01 | Information disclosure (misleading data) | A consumer fabricating a value (or dividing by zero) where the shorter window drops below a sample floor | mitigate | Re-verify each floor gates honestly to em-dash on the short window (PARITY-02); test-pinned; no floor value changed |
| T-55-04-02 | Repudiation (stale documentation) | The stale "zero-filled UNION / Phase 24" comments misleading the next maintainer into wrong assumptions | mitigate | Comments corrected to the coverage-window reality; grep-gated absent |
| T-55-04-03 | Tampering (silent regression) | Blanket `--update-snapshots` masking a genuine consumer regression | mitigate | Per-failure root-cause triage; annotated re-baselines enumerated in SUMMARY; sample-floor.test.ts stays green |
| T-55-04-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (locked). No install task. |
</threat_model>

<verification>
- `npm test` full suite green.
- `git diff src/lib/sample-floor.ts` empty (no floor-value change); `sample-floor.test.ts` green.
- Stale comments grep-gated absent; corrected comments describe coverage-window reality.
- `npm run test:coverage` clears the blocking gate.
- Category-C files absent from `git diff --name-only`.
</verification>

<success_criteria>
The three stale union-window comments are corrected to the coverage-window reality, and stress/VaR, benchmark, Monte-Carlo, compare, and the KPI strip are re-verified correct on the shorter coverage-window series with every sample-floor gating honestly (em-dash where too short, never a fabricated value or ÷0) — no floor value changed, Category-C callers untouched (PARITY-02).
</success_criteria>

<output>
Create `.planning/phases/55-coverage-window-compute-core/55-04-SUMMARY.md` when done — document the re-verify (suites run, honest em-dash changes, no floor-value change) and the three corrected comments.
</output>
