---
phase: 55-coverage-window-compute-core
plan: 02
type: execute
wave: 2
depends_on: [55-01]
files_modified:
  - src/lib/scenario.ts
  - src/lib/scenario.test.ts
  - src/__tests__/phase-29-frozen-spine-guards.test.ts
  - src/__tests__/phase-30-frozen-spine-guards.test.ts
  - src/__tests__/phase-31-frozen-spine-guards.test.ts
  - src/__tests__/phase-32-frozen-spine-guards.test.ts
  - src/__tests__/phase-52-frozen-spine-guards.test.ts
autonomous: true
requirements: [BLEND-01, BLEND-02, BLEND-03, BLEND-04, BLEND-05, BLEND-06, PARITY-03]
must_haves:
  truths:
    - "computeScenario accepts an explicit ScenarioState.window and, when present, blends only members whose coverage span ⊇ the window with a constant divisor = member count"
    - "An ended strategy (span ends before winEnd) is NOT a member and no longer divides the mean toward zero"
    - "When state.window is ABSENT the engine runs the byte-identical UNION path (own-book callers + computeCompositeCurve + share-resolve untouched)"
    - "A zero-member window returns the honest empty-state shape (no divide-by-zero, no fabricated flat-zero curve)"
    - "The output exposes member_count and member_ids additively; effective_start/effective_end carry the window bounds and n carries N"
    - "The phase-{29,30,31,32,52} frozen-spine guards are re-baselined as a reviewed, annotated act — never blind --update-snapshots"
  artifacts:
    - path: "src/lib/scenario.ts"
      provides: "computeScenario coverage-window path (window present) + preserved union path (window absent) + additive member_count/member_ids output"
      contains: "member_count"
    - path: "src/lib/scenario.test.ts"
      provides: "Additive coverage-window cases (ended-tail no-dilution, divisor==member count, single-member, interior-gap 0-fill, empty-intersection empty-state, weighted renorm-after-drop, narrow-back typed-weight restore) + preserved union pin"
      contains: "member_count"
  key_links:
    - from: "src/lib/scenario.ts"
      to: "src/lib/scenario-window.ts"
      via: "import coverageSpanOf / covers for member derivation"
      pattern: "scenario-window"
    - from: "src/lib/scenario.ts"
      to: "state.window"
      via: "gate the coverage path on state.window being present"
      pattern: "state\\.window"
---

<objective>
Rewrite `computeScenario` to add a coverage-window membership path (gated on `state.window` present) with a constant, honest divisor, while preserving the existing UNION path byte-identically when `window` is absent. Add `window?` to `ScenarioState` and `member_count`/`member_ids` to `ComputedMetrics` (both additive). Land the additive `scenario.test.ts` coverage-window cases. Re-baseline the five frozen-spine guards as a reviewed, annotated act — IN THE SAME logical change, because those guards fire the moment `scenario.ts` is touched.

Purpose: This is the core of the milestone — the ONE deliberate edit to the frozen engine. It kills the tail dilution where an ended strategy keeps dividing the blend toward zero (the whole reason for v1.5). The union-when-absent design (LOAD-BEARING, CONTEXT §Decisions) isolates the new behavior to the scenario tab and keeps every Category-C own-book caller (`queries.ts:2208/2356`, `computeCompositeCurve`, `share-resolve`) byte-identical.

Output: modified `src/lib/scenario.ts` (window path + additive fields), `src/lib/scenario.test.ts` (new cases), and five re-baselined guard files with the old→new enumerated in the SUMMARY (Q4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-coverage-window-compute-core/55-CONTEXT.md
@.planning/phases/55-coverage-window-compute-core/55-RESEARCH.md
@.planning/SCENARIO-COVERAGE-WINDOW-ADR.md
@src/lib/scenario-window.ts

<interfaces>
<!-- Contracts the executor must build against. From 55-01 (just landed) + scenario.ts at HEAD. -->

From src/lib/scenario-window.ts (landed in 55-01 — import these, do NOT re-derive):
```typescript
export function coverageSpanOf(dailyReturns: DailyPoint[]): { first: string; last: string } | null;
export function covers(span: { first: string; last: string }, window: { start: string; end: string }): boolean;
export function defaultWindowFor(spans): { start: string; end: string } | null; // (scenario tab uses this to build the window; 55-02 does NOT call it — it receives window pre-built)
```

From src/lib/scenario.ts at HEAD (the surfaces to extend):
- ScenarioState (:64-83): { selected; weights; startDates; leverage? }. Add `window?: { start: string; end: string }` mirroring the `leverage?` additive doc-comment (:68-82). Positional signature `computeScenario(strategies, state, dateMapCache)` (:149-153) UNCHANGED.
- ComputedMetrics (:85-129): add `member_count: number` and `member_ids: string[]`. Reuse existing `effective_start`/`effective_end` for [winStart,winEnd] and `n` for N. External construction sites read new fields with defaults — see below.
- The two existing degenerate early-returns to MODEL the empty-state on: `activeIds.length === 0` (:157-174) and `n < 10` (:210-227). Both return the full null-metrics shape.
- The current UNION/tail-dilution axis + blend loop (:199-255) — this is what runs when `window` is ABSENT (keep byte-identical) and what the PRESENT-window path replaces (member-only axis, constant divisor).
- The existing `normWeight` renorm over the active set (:177-182) — the PRESENT-window path renormalizes over the MEMBER set instead (BLEND-04), preserving typed `state.weights`.
- The metrics block (:331-378, twr/CAGR/vol/sharpe/sortino/maxDD, 252-annualization) — UNCHANGED. Only the SERIES feeding it changes.

External ComputedMetrics construction sites that must NOT need editing (they read new fields with defaults):
- `liveBaselineToComputedMetrics` (ScenarioComposer.tsx) — leaves member_count/member_ids at defaults.
- `NULL_METRICS` (ScenarioComparePanel.tsx).
Declare `member_count`/`member_ids` so these compile unchanged (either required-with-engine-always-sets like `n`, or optional-with-default like `portfolio_daily_returns?`). Verify both sites still compile.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add window? + member_count/member_ids and the coverage-window blend path to computeScenario</name>
  <files>src/lib/scenario.ts, src/lib/scenario.test.ts</files>
  <read_first>
    - src/lib/scenario.ts (FULL FILE — the engine; especially :64-83 ScenarioState, :85-129 ComputedMetrics, :149-255 the axis+blend, :331-378 metrics block, :157-174 + :210-227 early-returns)
    - src/lib/scenario-window.ts (the helpers landed in 55-01 — import coverageSpanOf + covers)
    - src/lib/scenario.test.ts (existing pins — especially the "never shrinks to the overlap" union pin at :351-369, which STAYS GREEN under union-when-absent)
    - .planning/phases/55-coverage-window-compute-core/55-CONTEXT.md (§Decisions — the LOAD-BEARING union-when-absent rule)
    - .planning/phases/55-coverage-window-compute-core/55-RESEARCH.md (§ Architecture Patterns 1/2/3; § Common Pitfalls 1-4; § Code Examples — the defect at :199-255)
    - .planning/SCENARIO-COVERAGE-WINDOW-ADR.md (:85-101 target algorithm)
  </read_first>
  <behavior>
    - BLEND-01: with an explicit `state.window`, the merged axis is exactly the dates in [winStart,winEnd]; effective_start === winStart, effective_end === winEnd, n === trading-day count in the window.
    - BLEND-02 ended-tail no-dilution: given member A (full window) + strategy B whose span ENDS mid-window, B is NOT a member; portfolio return over the post-B-end tail equals A's return (B contributes nothing, divisor === 1). member_count === 1, member_ids === [A].
    - BLEND-02 divisor: two full-window members equal-weight → each contributes exactly 1/2; divisor constant across the window; member_count === 2.
    - BLEND-03 interior gap: a member missing ONE mid-window day stays a member (divisor unchanged); that day contributes 0 to the numerator; member_count unchanged.
    - BLEND-04 weighted renorm: members A(weight 0.6) + B(weight 0.2), C(weight 0.2) auto-dropped for non-coverage → surviving A,B renormalize to 0.75/0.25 (sum-to-1) WITHOUT mutating state.weights; a narrow-back that re-admits C restores C's typed 0.2 then re-renormalizes to 0.6/0.2/0.2.
    - BLEND-05 empty-intersection: a window no selected strategy fully covers → member_count === 0, the honest empty-state shape (twr null, equity_curve [], portfolio_daily_returns []) — NEVER a flat-zero curve, NEVER a divide-by-zero.
    - BLEND-06: member_count (number), member_ids (string[]), effective_start/end === window bounds, n === N present on every non-empty-state return.
    - Union preserved (BLEND-01 absent-window): a state WITHOUT window produces byte-identical output to HEAD — the "never shrinks to overlap" pin (scenario.test.ts:351-369) stays green (n===60).
  </behavior>
  <action>
    In `src/lib/scenario.ts`: (1) Add `window?: { start: string; end: string }` to `ScenarioState` with an additive doc-comment mirroring the `leverage?` comment (:68-82) — note the byte-compat claim is CONDITIONAL: a state without `window` runs the union path. (2) Add `member_count: number` and `member_ids: string[]` to `ComputedMetrics` (declare so `liveBaselineToComputedMetrics` and `NULL_METRICS` compile unchanged — either `member_count` defaults in the empty-state returns like `n`, and both are read with `?? []`/`?? 0` by external sites; prefer required-fields-always-set-by-engine + external sites already spread — verify by compiling both sites). (3) Gate the NEW path on `state.window` being present. When ABSENT: run the existing union axis + blend loop (:199-255) BYTE-IDENTICALLY (set member_count/member_ids from the active set for output completeness, but the axis/divisor math is unchanged — the union pin must stay green). When PRESENT: derive each active strategy's coverage span via `coverageSpanOf(s.daily_returns)` (from scenario-window.ts, NOT from `start_date`/`"2022-01-01"`); members = active strategies where `covers(span, window)`; if `members.length === 0` return the empty-state shape (model on the :157-174 early-return) with `member_count: 0, member_ids: []` BEFORE the day loop (Pitfall 4 — never reach the `activeWeightSum > 0 ? r/… : 0` fabrication); else build the axis as the dates in [winStart,winEnd] (union of members' dates within the closed window — members bracket it by definition), 0-fill ONLY interior missing days for members (Pitfall 3 — never 0-fill outside the window or for non-members), renormalize weights over the MEMBER set only (extend `normWeight` to divide by the member weight mass; do NOT mutate `state.weights` — Pattern 3 / BLEND-04), apply leverage via the existing `lev(id)`, and feed the resulting series to the UNCHANGED metrics block (:331-378, 252-annualization preserved). Set `effective_start = winStart`, `effective_end = winEnd`, `member_count = members.length`, `member_ids = members.map(m => m.id)`. Preserve the existing `n < 10` guard behavior on the windowed axis. Document the CLOSED-window membership invariant in the file header behavior-notes block (where the 5 behaviors are pinned, :15-44). Do NOT touch `computeCompositeCurve` (:551) — it keeps union. Do NOT place any implementation code in this plan; the executor writes it against this directive.

    In `src/lib/scenario.test.ts`: ADD (do not remove existing pins) coverage-window cases matching every <behavior> bullet, each with an explicit `window` in the state. Keep the union pin at :351-369 unchanged and add a comment that it documents the preserved absent-window path. Name tests so the research's per-req filters hit: include the substrings "window", "interior gap", "renorm", "empty", "member_count" so `-t` filters in 55-VALIDATION resolve.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario.test.ts src/lib/scenario-window.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/scenario.test.ts` passes, including new cases matched by `-t "window"`, `-t "interior gap"`, `-t "renorm"`, `-t "empty"`, `-t "member_count"`.
    - The existing union pin `scenario.test.ts` "never 'shrinks' the scenario window to the overlap" still asserts `metrics.n === 60` and passes (union-when-absent preserved).
    - `grep -n "2022-01-01" src/lib/scenario.ts` shows the sentinel ONLY on the existing union/startDates path (:195), NOT inside the new window-present coverage branch (`grep` the coverage branch region for `2022-01-01` → none).
    - `grep -q "member_count" src/lib/scenario.ts` and `grep -q "member_ids" src/lib/scenario.ts` — both additive fields present in output.
    - `grep -q "state.window\|state\\.window" src/lib/scenario.ts` — the coverage path is gated on window presence.
    - The empty-member case returns `member_count: 0` with `equity_curve: []` and `portfolio_daily_returns: []` (no fabricated zeros) — asserted by a test.
    - Typecheck clean: `liveBaselineToComputedMetrics` (ScenarioComposer.tsx) and `NULL_METRICS` (ScenarioComparePanel.tsx) compile UNCHANGED (`npx tsc --noEmit` passes).
  </acceptance_criteria>
  <done>Engine accepts window, blends members only with a constant divisor when present, preserves union when absent, exposes member_count/member_ids, honest empty-state on zero members; all new + existing scenario.test.ts cases green; external construction sites compile unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Re-baseline the five frozen-spine guards as an annotated reviewed act</name>
  <files>src/__tests__/phase-29-frozen-spine-guards.test.ts, src/__tests__/phase-30-frozen-spine-guards.test.ts, src/__tests__/phase-31-frozen-spine-guards.test.ts, src/__tests__/phase-32-frozen-spine-guards.test.ts, src/__tests__/phase-52-frozen-spine-guards.test.ts</files>
  <read_first>
    - src/__tests__/phase-29-frozen-spine-guards.test.ts (FROZEN_ENGINE at :141; the zero-diff assertion at :172-181)
    - src/__tests__/phase-30-frozen-spine-guards.test.ts (FROZEN_ENGINE :137, FROZEN_ENGINE_TEST :138; both zero-diff assertions at :149-170)
    - src/__tests__/phase-31-frozen-spine-guards.test.ts (FROZEN_ENGINE :156, FROZEN_ENGINE_TEST :157; frozen-engine assertions :174-195 — LEAVE the LAYOUT-02 CompositionList grep assertions UNCHANGED)
    - src/__tests__/phase-32-frozen-spine-guards.test.ts (FROZEN_ENGINE :176; frozen-engine assertion :237-247 — LEAVE the FLOW-01/02/03 route assertions UNCHANGED)
    - src/__tests__/phase-52-frozen-spine-guards.test.ts (FROZEN_ISLANDS array :154, `src/lib/scenario.ts` at :155, the per-island loop :184-198 — LEAVE the other ~10 islands frozen)
    - .planning/phases/55-coverage-window-compute-core/55-RESEARCH.md (§ Frozen-Spine Guards — the git-DELTA mechanics + per-guard re-baseline table; Assumption A3)
    - .planning/phases/55-coverage-window-compute-core/55-CONTEXT.md (Q4 — annotate + enumerate in SUMMARY, never blind --update-snapshots)
  </read_first>
  <action>
    These are git-DELTA guards: each asserts `src/lib/scenario.ts` is NOT in the changed-files set vs a per-phase baseline SHA, so they fire the moment scenario.ts is edited (by design). Re-baseline by editing the frozen-`scenario.ts` ASSERTION in each guard to permit the v1.5 deliberate edit (per RESEARCH Assumption A3, option b — the assertion edit, NOT advancing baseline SHAs, since merge-base is HEAD-relative and still sees scenario.ts). For phase-29: edit the exit-gate assertion (:172-181) that expects `changedFiles` to NOT contain FROZEN_ENGINE so it acknowledges the v1.5 coverage-window edit; annotate the change `// v1.5 coverage-window re-baseline (ADR-001)`. For phase-30: same for BOTH the scenario.ts (:149-160) AND scenario.test.ts (:161-170) exit-gate assertions (both legitimately change now); annotate both. For phase-31: re-baseline the TWO frozen-engine assertions (:174-185 scenario.ts, :186-195 scenario.test.ts) with the annotation, and LEAVE the LAYOUT-02 CompositionList hide-don't-unmount grep assertions UNCHANGED (Phase 55 touches no composer JSX). For phase-32: re-baseline the ONE frozen-engine assertion (:237-247) with the annotation, and LEAVE all FLOW-01/02/03 route/redirect/delete assertions UNCHANGED. For phase-52: remove (or annotate-and-skip) `"src/lib/scenario.ts"` from the `FROZEN_ISLANDS` array (:155) with the `// v1.5 coverage-window re-baseline (ADR-001)` annotation, and LEAVE the other ~10 islands (compute.ts, factsheet-context, useBreakpoint, MC worker, EquityChart, TouchTooltip, useTapPin, 3 factsheet SVGs) FROZEN. Do NOT run `vitest --update-snapshots`. Enumerate every guard file + line + old→new in the plan SUMMARY (Q4). Keep each guard LIVE (still asserting its migration/RLS/route/other-island pins) — only the scenario.ts frozen assertion is re-baselined.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-29-frozen-spine-guards.test.ts src/__tests__/phase-30-frozen-spine-guards.test.ts src/__tests__/phase-31-frozen-spine-guards.test.ts src/__tests__/phase-32-frozen-spine-guards.test.ts src/__tests__/phase-52-frozen-spine-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five `phase-{29,30,31,32,52}-frozen-spine-guards.test.ts` suites pass with scenario.ts edited (they no longer fail on the scenario.ts delta).
    - `grep -rc "v1.5 coverage-window re-baseline (ADR-001)" src/__tests__/phase-29-frozen-spine-guards.test.ts src/__tests__/phase-30-frozen-spine-guards.test.ts src/__tests__/phase-31-frozen-spine-guards.test.ts src/__tests__/phase-32-frozen-spine-guards.test.ts src/__tests__/phase-52-frozen-spine-guards.test.ts` — the annotation appears in every one of the five files.
    - `git diff` on the guard files shows NO changes to phase-31 LAYOUT-02 assertions, NO changes to phase-32 FLOW route assertions, and the phase-52 FROZEN_ISLANDS array still contains the other ~10 islands (only `src/lib/scenario.ts` removed/annotated).
    - No `--update-snapshots` was run (the git diff shows only hand-edited, annotated assertion lines — verify no snapshot file churn).
    - The SUMMARY enumerates each guard file, the line edited, and old→new (Q4 requirement).
  </acceptance_criteria>
  <done>All five guards green with scenario.ts changed; every edit annotated; non-scenario assertions (LAYOUT-02, FLOW routes, other 10 islands) untouched; enumeration in SUMMARY; no blind snapshot update.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side compute rewrite. No new auth, network, persistence, or input boundary. The only integrity surface is the honesty of the emitted series. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02-01 | Information disclosure (misleading data) | Zero-member window emitting a plausible flat-0% curve read as real performance | mitigate | member-count-0 guard BEFORE the day loop returns the honest empty-state (BLEND-05, Pitfall 4); test-pinned — never reaches `activeWeightSum > 0 ? r/… : 0` |
| T-55-02-02 | Tampering (data integrity) | 0-fill leaking outside the member window reintroducing tail dilution | mitigate | Per-strategy vector built ONLY over the closed member window; members bracket the window; interior-gap-only 0-fill (BLEND-03, Pitfall 3); test-pinned |
| T-55-02-03 | Information disclosure (misleading data) | Coverage span sourced from `start_date`/`"2022-01-01"` sentinel mislabeling a ragged-head strategy as a member | mitigate | Coverage derived via `coverageSpanOf` (returns-map only); grep-gate the sentinel absent from the window-present branch (Pitfall 2) |
| T-55-02-04 | Repudiation / silent regression | Frozen-spine guard bypassed by blind `--update-snapshots` masking an unreviewed change | mitigate | Guards re-baselined by hand-edited annotated assertions, enumerated in SUMMARY; no `--update-snapshots`; guards stay LIVE (PARITY-03, Pitfall 8-adjacent) |
| T-55-02-05 | Tampering (scope creep) | Silently changing own-book / composite / share-resolve metrics by defaulting to intersection | mitigate | Union-when-absent gate; Category-C callers pass no window → byte-identical; union pin scenario.test.ts:351-369 stays green |
| T-55-02-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (locked). No install task. |
</threat_model>

<verification>
- `npm test` full suite green (includes the 12 consumers + all 5 guards) — no regression from the engine change.
- `npx vitest run src/lib/scenario.test.ts` green with all new coverage-window cases.
- `npm run test:coverage` still clears the blocking gate (lines 82 / stmts 80 / fns 74 / br 72).
- Union pin (`scenario.test.ts:351-369`) unchanged and green.
- All five guard suites green with scenario.ts edited; annotation present in each.
</verification>

<success_criteria>
computeScenario blends over an explicit coverage window with a constant member-count divisor (ended strategy no longer dilutes), preserves the union path when window is absent, exposes member_count/member_ids + effective window + N, and returns an honest empty-state on a zero-member window — with the five frozen-spine guards re-baselined as an annotated reviewed act and every LOCKED invariant (no-invented-data, 252-annualization, WCAG-AA) still green (BLEND-01…06, PARITY-03).
</success_criteria>

<output>
Create `.planning/phases/55-coverage-window-compute-core/55-02-SUMMARY.md` when done — MUST enumerate each re-baselined guard file, the line(s) edited, and old→new (Q4).
</output>
