---
phase: 58-coverage-legibility-disclosure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/components/BlendHeader.tsx
  - src/app/(dashboard)/allocations/components/BlendHeader.test.tsx
  - src/app/(dashboard)/allocations/components/CoverageStateChip.tsx
  - src/app/(dashboard)/allocations/components/CoverageStateChip.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/lib/storage-namespaces.ts
  - src/components/auth/SignOutButton.test.tsx
autonomous: true
requirements: [COVERAGE-02, COVERAGE-03]
must_haves:
  truths:
    - "An always-visible blend header states 'Mean of {N} strategies · {effStart}–{effEnd}' above the coverage-window control, reading the engine's member_count / effective_start / effective_end"
    - "The header degrades honestly: N=1 → '1 strategy — not a blend', N=0 → 'No strategies span the selected window', and appends '· window truncated from full range' when the effective window is narrower than the selected-set union"
    - "Each strategy row can render a three-state chip (In blend / Excluded / Outside window) whose state is derived from selected + coverageEligible — never a locally recomputed covers()"
    - "As the window moves so a member drops, the header's N and the engine's member_count fall together (single-source membership)"
    - "The composer.coverageDefaultChangeNoteDismissed localStorage prefix is registered so the sign-out purge reaches it"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/BlendHeader.tsx"
      provides: "Honest blend header reading ComputedMetrics"
      min_lines: 30
    - path: "src/app/(dashboard)/allocations/components/CoverageStateChip.tsx"
      provides: "Three-state coverage chip (in-blend / manually-excluded / auto-excluded)"
      min_lines: 20
    - path: "src/app/(dashboard)/allocations/components/BlendHeader.test.tsx"
      provides: "Four degrade-branch coverage for BlendHeader"
    - path: "src/app/(dashboard)/allocations/components/CoverageStateChip.test.tsx"
      provides: "Three-state × token-class × label coverage for the chip"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/BlendHeader.tsx"
      to: "scenarioMetrics.member_count / effective_start / effective_end"
      via: "props threaded from ScenarioComposer (never re-derived)"
      pattern: "member_count"
    - from: "src/app/(dashboard)/allocations/components/CoverageStateChip.tsx"
      to: "coverageEligible + selected"
      via: "props threaded from ScenarioComposer"
      pattern: "coverageEligible"
---

<objective>
Deliver the two read-only membership-display surfaces of Phase 58: the always-visible
honest **blend header** (COVERAGE-03) and the reusable **three-state coverage chip**
(COVERAGE-02, in-blend + manually-excluded states; the amber auto-excluded state is wired
by Plan 02). Both READ the engine's single membership axis — `scenarioMetrics.member_count`
/ `effective_start` / `effective_end` for the header, `selected` + `coverageEligible` for
the chip — and never re-derive the blend. This plan also registers the POLISH-03
localStorage prefix (a cheap two-edit infra change with no composer conflict) so Plan 03 is
unblocked.

Purpose: The blend header is the phase's PRIMARY visual anchor (UI-SPEC §Interaction) — the
allocator reads the honest member count · effective window first. The chip gives per-row
legibility. Getting both to source membership from the same axis the dev desync guard
(`ScenarioComposer.tsx:1813`) reconciles is the #1 correctness rule.

Output: `BlendHeader.tsx` + `CoverageStateChip.tsx` (+ colocated tests), wired into
`ScenarioComposer.tsx`; `storage-namespaces.ts` + `SignOutButton.test.tsx` prefix
registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-coverage-legibility-disclosure/58-CONTEXT.md
@.planning/phases/58-coverage-legibility-disclosure/58-UI-SPEC.md
@.planning/phases/58-coverage-legibility-disclosure/58-PATTERNS.md
@DESIGN.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase; use directly, no exploration. -->

From src/lib/scenario.ts (ComputedMetrics — read these fields ONLY for the header):
  n: number;                        // trading days
  effective_start: string | null;  // window start ("YYYY-MM-DD") or null
  effective_end: string | null;    // window end or null
  member_count?: number;           // the honest divisor N (read with ?? 0)
  member_ids?: string[];           // member ids in strategy order

From src/lib/scenario-window.ts:
  export interface CoverageWindow { start: string; end: string; }
  export function unionOf(spans: CoverageSpan[]): CoverageWindow | null;

ScenarioComposer.tsx existing memos to thread as props (do NOT recompute):
  scenarioMetrics           // ComputedMetrics (line ~1741) — engine output
  fullRangeWindow           // unionOf(selectedSpans) (line 1722) — the union span for truncation test
  coverageEligible          // Record<string, boolean> (line 1762) — in-blend axis
  deAliased.state.selected  // Record<string, boolean> — manual on/off
  coverageWindow            // CoverageWindow | null (line 1684)

Existing coverage-window control container to render the header ABOVE (line 2828-2887):
  <div className="mt-6 flex flex-wrap items-center gap-3 rounded-md border border-border bg-surface px-4 py-3" data-testid="scenario-coverage-window"> ...

storage-namespaces.ts APP_NAMESPACED_PREFIXES (line 20-31) — a readonly string[] already
containing "composer-collapse:" at line 30.

SignOutButton.test.tsx KNOWN_APP_KEYS array (line 124) — already lists
"composer-collapse:controls" (line 135).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BlendHeader component (COVERAGE-03) + colocated test</name>
  <files>src/app/(dashboard)/allocations/components/BlendHeader.tsx, src/app/(dashboard)/allocations/components/BlendHeader.test.tsx</files>
  <read_first>
    - src/lib/scenario.ts (lines 129-195 — ComputedMetrics shape: member_count, member_ids, effective_start, effective_end, n)
    - src/app/(dashboard)/allocations/components/KpiStrip.scenario.test.tsx (lines 25-63 — the EMPTY_METRICS-base fixture-spread test idiom to copy)
    - .planning/phases/58-coverage-legibility-disclosure/58-UI-SPEC.md (§Copywriting Contract — the 4 verbatim degrade strings; §Typography — text-fixed-13, font-mono tabular-nums for numbers)
    - .planning/phases/58-coverage-legibility-disclosure/58-PATTERNS.md (BlendHeader.tsx section — read-only fields + LOCKED degrade order)
    - DESIGN.md (color tokens; "all numbers use Geist Mono"; role=status vs role=alert rule)
  </read_first>
  <behavior>
    - Props: { metrics: ComputedMetrics; unionSpan: CoverageWindow | null }.
    - N=0 (member_count 0 or undefined) → renders exactly "No strategies span the selected window".
    - N=1 → renders exactly "1 strategy — not a blend".
    - N>=2, not truncated → renders "Mean of {N} strategies · {effStart}–{effEnd}" with {N} and the dates in font-mono tabular-nums.
    - Truncated (unionSpan != null && effStart != null && effEnd != null && (effStart > unionSpan.start || effEnd < unionSpan.end), lexicographic string compare) → appends " · window truncated from full range" in the quieter text-fixed-11 text-text-muted tier.
    - Root element carries role="status" aria-live="polite" (NEVER role="alert").
  </behavior>
  <action>
    Create a pure presentational `BlendHeader` component under
    `src/app/(dashboard)/allocations/components/BlendHeader.tsx`. Props exactly
    `{ metrics: ComputedMetrics; unionSpan: CoverageWindow | null }` (import both types from
    `@/lib/scenario` and `@/lib/scenario-window`). Read N via `metrics.member_count ?? 0`,
    `effStart = metrics.effective_start`, `effEnd = metrics.effective_end`. Never count
    coverageEligible — member_count is the honest divisor (BLEND-06 / Pitfall 1). Implement
    the LOCKED degrade order from 58-UI-SPEC §Copywriting Contract: N===0 → "No strategies
    span the selected window"; N===1 → "1 strategy — not a blend"; else `Mean of ${N}
    strategies · ${effStart}–${effEnd}`. Compute `truncated` with the lexicographic compare
    from 58-PATTERNS BlendHeader section and append " · window truncated from full range"
    only in the N>=2 branch. Root element: `role="status" aria-live="polite"`. Wrap the {N}
    count and the {start}–{end} dates in `font-mono tabular-nums` spans; body label in
    `text-fixed-13` medium, degrade note in `text-fixed-11 text-text-muted`. Use only
    DESIGN.md tokens; NO raw `text-[Npx]` (no-raw-font-px lint is error). NO icons.
    Then create the colocated `BlendHeader.test.tsx` copying the EMPTY_METRICS-base
    fixture-spread idiom from `KpiStrip.scenario.test.tsx:25-63`: one `render`+`screen.getByText`
    assertion per branch — N=0, N=1, N=2-normal, N=2-truncated — plus assert the root has
    `role="status"`.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/BlendHeader.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `BlendHeader.test.tsx` passes with 4 explicit branch cases (N=0 empty, N=1 not-a-blend, N>=2 normal, N>=2 truncated) plus a role="status" assertion.
    - `grep -n 'role="alert"' src/app/(dashboard)/allocations/components/BlendHeader.tsx` returns nothing.
    - `grep -nE 'text-\[[0-9]+px\]' src/app/(dashboard)/allocations/components/BlendHeader.tsx` returns nothing.
    - `grep -c 'coverageEligible\|covers(' src/app/(dashboard)/allocations/components/BlendHeader.tsx` returns 0 (header reads member_count, never re-derives membership).
  </acceptance_criteria>
  <done>BlendHeader renders all four honest degrade cases from member_count/effective_* with role="status"; four-branch colocated test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CoverageStateChip component (COVERAGE-02, in-blend + manual states) + colocated test</name>
  <files>src/app/(dashboard)/allocations/components/CoverageStateChip.tsx, src/app/(dashboard)/allocations/components/CoverageStateChip.test.tsx</files>
  <read_first>
    - src/components/ui/Badge.tsx (lines 43-57 — the cn(...) base + Record<state,string> token-map chip shape to match)
    - src/app/(dashboard)/allocations/components/HoldingsTable.tsx (lines 52-60 — AMBER_CHIP_STYLE: the exact warning tokens for the auto-excluded state)
    - .planning/phases/58-coverage-legibility-disclosure/58-UI-SPEC.md (§Color — the LOCKED 3-state mapping table; §Copywriting Contract — chip labels "In blend"/"Excluded"/"Outside window"; §Typography — text-fixed-11 font-medium uppercase tracking-wide)
    - .planning/phases/58-coverage-legibility-disclosure/58-PATTERNS.md (CoverageStateChip.tsx section — state derivation + never-import-covers rule)
    - DESIGN.md (warning-amber reservation; badge radius/ladder; negative/red is NOT used)
  </read_first>
  <behavior>
    - Exports type CoverageState = "in-blend" | "manually-excluded" | "auto-excluded".
    - state="in-blend" → label "In blend", classes text-accent bg-accent/10.
    - state="manually-excluded" → label "Excluded", classes text-text-muted bg-track.
    - state="auto-excluded" → label "Outside window", classes text-warning bg-warning-bg border border-warning-border (NEVER text-negative/red).
    - Base shape: inline-flex items-center rounded-sm px-2 py-0.5 text-fixed-11 font-medium uppercase tracking-wide (Badge ladder).
    - The text label always carries the meaning (color is never the sole signal — WCAG-AA).
  </behavior>
  <action>
    Create `CoverageStateChip.tsx` under the same components dir. Export
    `type CoverageState = "in-blend" | "manually-excluded" | "auto-excluded"` and a component
    taking `{ state: CoverageState; className?: string }`. Model it on the Badge chip shape
    (`Badge.tsx:51-57`): a `cn(...)` base string `"inline-flex items-center rounded-sm px-2
    py-0.5 text-fixed-11 font-medium uppercase tracking-wide"` plus a
    `Record<CoverageState, { label: string; cls: string }>` lookup exactly matching the
    LOCKED 58-UI-SPEC §Color mapping: in-blend → `{ label: "In blend", cls: "text-accent
    bg-accent/10" }`; manually-excluded → `{ label: "Excluded", cls: "text-text-muted
    bg-track" }`; auto-excluded → `{ label: "Outside window", cls: "text-warning
    bg-warning-bg border border-warning-border" }` (the exact HoldingsTable AMBER_CHIP_STYLE
    tokens). NEVER use `text-negative` / red — auto-excluded is transient-recoverable. NO
    icons. Do NOT import `covers` or `coverageSpanOf` — the chip receives its `state` as a
    prop; membership derivation lives in the composer (Task 3). Then create
    `CoverageStateChip.test.tsx`: render each of the three states, assert the label text and
    that the auto-excluded variant className contains `bg-warning-bg` and does NOT contain
    any red/negative token.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/CoverageStateChip.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `CoverageStateChip.test.tsx` asserts all three state → label mappings ("In blend" / "Excluded" / "Outside window") and the amber classes on auto-excluded.
    - `grep -c 'text-negative\|#DC2626\|bg-red\|text-red' src/app/(dashboard)/allocations/components/CoverageStateChip.tsx` returns 0.
    - `grep -c 'from "@/lib/scenario-window"\|covers(\|coverageSpanOf(' src/app/(dashboard)/allocations/components/CoverageStateChip.tsx` returns 0 (chip never re-derives membership).
    - `grep -nE 'text-\[[0-9]+px\]' src/app/(dashboard)/allocations/components/CoverageStateChip.tsx` returns nothing.
  </acceptance_criteria>
  <done>The three-state chip renders correct label + DESIGN token classes per state; auto-excluded is amber, never red; colocated test green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire BlendHeader + in-blend/manual chips into ScenarioComposer; register the POLISH-03 storage prefix</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx, src/lib/storage-namespaces.ts, src/components/auth/SignOutButton.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (lines 1684-1832 — coverageWindow/applyWindow/coverageEligible/scenarioMetrics/fullRangeWindow memos + the dev desync guard at 1813 that the new surfaces must keep quiet; lines 2828-2887 — the coverage-window control container to render the header ABOVE)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (lines 185-215, 5138-5340 — the Phase-57 window block: CustomRangePicker capture mock + REAL computeScenario member_count oracle rig to extend)
    - src/lib/storage-namespaces.ts (lines 20-31 — APP_NAMESPACED_PREFIXES)
    - src/components/auth/SignOutButton.test.tsx (lines 124-138 — KNOWN_APP_KEYS purge-coverage inventory)
    - .planning/phases/58-coverage-legibility-disclosure/58-PATTERNS.md (Shared Patterns — membership single-source + localStorage prefix registration)
  </read_first>
  <action>
    In `ScenarioComposer.tsx`, import `BlendHeader` and `CoverageStateChip`. Render
    `<BlendHeader metrics={scenarioMetrics} unionSpan={fullRangeWindow} />` ABOVE the existing
    coverage-window control container (the `data-testid="scenario-coverage-window"` div at
    line 2828), so it is the primary anchor per UI-SPEC §Interaction — use the existing `mt-6`
    vertical rhythm. Wire `CoverageStateChip` onto each in-blend / manually-excluded strategy
    row: derive the state INLINE at the render site from the existing memos —
    `selected === false → "manually-excluded"`, `selected && coverageEligible[id] →
    "in-blend"` (per 58-PATTERNS state-derivation block). Do NOT touch the auto-excluded group
    or `AutoExcludedRow` (that is Plan 02). Do NOT recompute `covers()` anywhere — read
    `coverageEligible` / `deAliased.state.selected` so the `:1813` desync guard stays quiet.
    Then register the POLISH-03 prefix: add `"composer."` to `APP_NAMESPACED_PREFIXES` in
    `storage-namespaces.ts` (distinct from the existing `"composer-collapse:"`), and add
    `"composer.coverageDefaultChangeNoteDismissed"` to `KNOWN_APP_KEYS` in
    `SignOutButton.test.tsx` with a comment noting it is the Phase-58 DefaultChangeNote key.
    Finally, extend the Phase-57 window block in `ScenarioComposer.test.tsx` with a
    COVERAGE-03 integration assertion: mount an unequal-span book, assert the BlendHeader text
    reads "Mean of {N} strategies · …" where {N} equals the REAL computeScenario
    `member_count`; drive `applyWindow` (via the captured picker `onApply`) past a strategy's
    last day and assert the header degrades to "1 strategy — not a blend" AND that
    member_count fell in lockstep (the oracle).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" "src/components/auth/SignOutButton.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - ScenarioComposer.test.tsx Phase-57 window block, extended, asserts BlendHeader text N === REAL computeScenario member_count and the header+divisor degrade together when the window widens past a member.
    - SignOutButton.test.tsx passes with "composer.coverageDefaultChangeNoteDismissed" in KNOWN_APP_KEYS.
    - `grep -c '"composer\."' src/lib/storage-namespaces.ts` returns >= 1 (new prefix registered, distinct from composer-collapse:).
    - No new `covers(` call added to ScenarioComposer.tsx render sites (state derived from existing memos): `git diff src/app/(dashboard)/allocations/components/ScenarioComposer.tsx | grep -E '^\+' | grep -c 'covers('` returns 0.
    - Full composer suite green (dev desync guard does not warn — the new surfaces read the same axis).
  </acceptance_criteria>
  <done>BlendHeader renders above the window control sourcing member_count/effective_*; in-blend/manual chips render on rows from existing memos; storage prefix registered in both sites; composer integration test asserts header==engine divisor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine output → UI display | `scenarioMetrics` (ComputedMetrics) is trusted in-memory client state; the header/chip only READ it |
| strategy name/date strings → DOM | strings rendered as React text children (auto-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Information Disclosure | BlendHeader divisor desync | mitigate | Header reads `member_count` (the engine's honest divisor), never counts `coverageEligible` — a desync would misreport N; the `:1813` dev guard + the integration test (header N === engine member_count) enforce agreement |
| T-58-02 | Tampering / XSS | strategy name / date strings in header + chip | accept | React auto-escapes all text nodes; no `dangerouslySetInnerHTML`; only strings (dates, N, labels) rendered — no injection vector |
| T-58-03 | Information Disclosure | stale localStorage prefix survives sign-out | mitigate | Register `composer.` in `APP_NAMESPACED_PREFIXES` so `purgeAppNamespacedStorage()` clears the note-dismissal flag on sign-out; `SignOutButton.test.tsx` asserts coverage |
| T-58-SC | Tampering | npm/pip/cargo installs | accept | No package install in this phase (zero new runtime dependency — REQUIREMENTS/ADR locked); no registry lookup |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/BlendHeader.test.tsx" "src/app/(dashboard)/allocations/components/CoverageStateChip.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" "src/components/auth/SignOutButton.test.tsx"` — all green.
- `npm run test` — full frontend suite green (frozen-spine + BLEND-07 + parity guards untouched: this plan edits no engine file).
- Dev-mode: mount composer, move window, confirm no `[ScenarioComposer] coverageEligible desync` console.warn (the header/chips read the same axis).
</verification>

<success_criteria>
- COVERAGE-03 blend header renders the four honest degrade cases from `member_count`/`effective_*`, above the window control, as the primary anchor.
- COVERAGE-02 in-blend + manually-excluded chips render on rows, state derived from existing memos (auto-excluded amber state completed in Plan 02).
- POLISH-03 storage prefix registered in both `storage-namespaces.ts` and `SignOutButton.test.tsx` (unblocks Plan 03).
- Header N proven equal to the real engine `member_count` by the composer integration test; no numeric change anywhere.
</success_criteria>

<output>
Create `.planning/phases/58-coverage-legibility-disclosure/58-01-SUMMARY.md` when done.
</output>
