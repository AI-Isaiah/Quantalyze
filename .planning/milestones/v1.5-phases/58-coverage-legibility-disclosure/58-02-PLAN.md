---
phase: 58-coverage-legibility-disclosure
plan: 02
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [COVERAGE-02, COVERAGE-04]
must_haves:
  truths:
    - "Each auto-excluded row carries the amber 'Outside window' CoverageStateChip alongside its existing coverageDropReason text (the third visually-distinct state)"
    - "Each auto-excluded row offers a one-click 'Include → shortens window to {date} (−{N} mo)' text-button whose cost is visible in the label before applying"
    - "Clicking Include narrows the window (via the existing applyWindow path) to the intersection that includes this strategy, so the strategy becomes a member and member_count rises"
    - "Include never reselects a manually-excluded (selected===false) strategy — manual-off stays sticky; the click only moves the window"
    - "The include target bound math is delegated to scenario-window.ts intersectionOf — no hand-rolled interval math"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "Extended AutoExcludedRow (amber chip + include-cost button) + onInclude handler reusing applyWindow"
      contains: "Include → shortens window to"
  key_links:
    - from: "AutoExcludedRow include button"
      to: "applyWindow(intersectionOf([currentWindow-as-span, strategySpan]))"
      via: "onInclude callback in ScenarioComposer"
      pattern: "applyWindow"
    - from: "AutoExcludedRow"
      to: "CoverageStateChip state=\"auto-excluded\""
      via: "amber chip render"
      pattern: "CoverageStateChip"
---

<objective>
Complete the three-state per-row legibility (COVERAGE-02 auto-excluded state) and the
include-cost affordance (COVERAGE-04) by extending the Phase-57 `AutoExcludedRow` and its
group render **in place** (Rule 3, surgical). Each auto-excluded row gains: (1) the amber
`CoverageStateChip` (state="auto-excluded", the "Outside window" label) alongside its existing
`coverageDropReason` text, and (2) a one-click text-button **"Include → shortens window to
{date} (−{N} mo)"** that discloses the cost in the label and, on click, narrows the window via
the existing `applyWindow` path so the strategy becomes a member — reusing the Phase-57
auto-toggle state machine downstream.

Purpose: This makes "why did X drop?" carry a rich chip and "how much history to keep it?"
answerable with one reversible click — the cost is disclosed before applying, no modal.

Output: extended `AutoExcludedRow` + a new `onInclude` handler in `ScenarioComposer.tsx`
(delegating bound math to `scenario-window.ts` `intersectionOf`), plus the COVERAGE-04
integration assertions in `ScenarioComposer.test.tsx`.

Depends on Plan 01: reuses `CoverageStateChip` for the amber chip, and serializes the
`ScenarioComposer.tsx` edits (single shared file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-coverage-legibility-disclosure/58-CONTEXT.md
@.planning/phases/58-coverage-legibility-disclosure/58-UI-SPEC.md
@.planning/phases/58-coverage-legibility-disclosure/58-PATTERNS.md
@DESIGN.md
@.planning/phases/58-coverage-legibility-disclosure/58-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase; use directly, no exploration. -->

Plan-01 output (already merged):
  CoverageStateChip.tsx — export type CoverageState = "in-blend" | "manually-excluded" | "auto-excluded";
    <CoverageStateChip state="auto-excluded" /> renders the amber "Outside window" chip.

Existing AutoExcludedRow (ScenarioComposer.tsx:3678) — current signature to EXTEND additively:
  function AutoExcludedRow({ id, name, reason }: { id: string; name: string; reason: string })
  — renders <li> with name + reason (text-warning), fade/slide enter transition
    (transition-all duration-300 ease-out motion-reduce:transition-none).

Existing auto-excluded group render (ScenarioComposer.tsx:3518-3547) — maps autoExcluded
rows; passes {id, name, reason}. `autoExcluded` memo shape (line 1787): { id, name, reason }[].

Existing window-set path (ScenarioComposer.tsx:1705):
  const applyWindow = useCallback((range: { start: string; end: string }) => {
    windowTouchedRef.current = true; setWinStart(range.start); setWinEnd(range.end);
  }, []);

Existing memos to reuse:
  coverageWindow            // CoverageWindow | null (line 1684) — current [winStart,winEnd]
  selectedSpanById          // Map<id, CoverageSpan | null> (line 1639) — per-strategy span

From src/lib/scenario-window.ts (delegate ALL interval math here — do not hand-roll):
  export interface CoverageSpan { first: string; last: string; }
  export interface CoverageWindow { start: string; end: string; }
  export function intersectionOf(spans: CoverageSpan[]): CoverageWindow | null;

From src/lib/dateday.ts (timezone-free month-delta; never new Date(iso)):
  export function parseIsoDay(s: string): IsoDay | null;
  export function diffDays(a: IsoDay, b: IsoDay): number;

Existing preset-button styling to match (ScenarioComposer.tsx:2862/2883) — accent-tinted
button focus ring: focus-visible:ring-2 focus-visible:ring-accent/50 motion-reduce:transition-none.
Include-cost is a text-button: text-accent hover:text-accent-hover.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AutoExcludedRow with the amber chip + include-cost button; add onInclude handler</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (lines 3678-3712 — AutoExcludedRow to extend additively; lines 3518-3547 — the group render that passes props; lines 1684-1722 — coverageWindow/applyWindow/commonPeriodWindow/fullRangeWindow; lines 1639-1656 — selectedSpanById; lines 1787-1805 — autoExcluded memo; lines 2850-2886 — preset button styling idiom)
    - src/lib/scenario-window.ts (lines 36-52 — CoverageSpan {first,last} / CoverageWindow {start,end}; lines 75-98 — intersectionOf semantics: inclusive-closed, null-on-empty)
    - src/lib/dateday.ts (lines 47-52 parseIsoDay, 118-130 diffDays — timezone-free month-delta)
    - .planning/phases/58-coverage-legibility-disclosure/58-PATTERNS.md (AutoExcludedRow include-cost button section — apply path + LOCKED cost label + button styling; Shared Patterns — coverage math delegation)
    - .planning/phases/58-coverage-legibility-disclosure/58-UI-SPEC.md (§Copywriting Contract — the verbatim "Include → shortens window to {date} (−{N} mo)"; §Color — accent text-button; §Interaction — no modal, reversible, does NOT reselect manual-off)
    - DESIGN.md (accent text-button convention; font-mono tabular-nums for dates/numbers)
  </read_first>
  <action>
    Extend `AutoExcludedRow` (ScenarioComposer.tsx:3678) additively — do NOT rebuild it (Rule 3).
    Add two props: `includeCost: { date: string; months: number }` and `onInclude: () => void`.
    Inside the row, render `<CoverageStateChip state="auto-excluded" />` (imported in Plan 01)
    next to the existing `reason` text (keep the existing reason span). Below/after the reason,
    render a real `<button type="button">` with the LOCKED verbatim label `Include → shortens
    window to {date} (−{months} mo)` where `{date}` and `−{months} mo` render in `font-mono
    tabular-nums`; style it as an accent text-button (`text-accent hover:text-accent-hover
    text-fixed-11 font-medium focus-visible:ring-2 focus-visible:ring-accent/50
    motion-reduce:transition-none`). onClick → `onInclude`. No modal.
    In the group render (line 3518-3547), for each `autoExcluded` row compute the include
    target and cost, then pass `includeCost` + `onInclude`. Add a memoized/inline helper (near
    the existing window memos) that, GIVEN the strategy's span from `selectedSpanById` and the
    current `coverageWindow`, computes the include target window as
    `intersectionOf([{ first: coverageWindow.start, last: coverageWindow.end }, strategySpan])`
    — DELEGATE the bound math to `scenario-window.ts` `intersectionOf` (never hand-roll min/max
    interval math, Rule 2). Derive the label `{date}` as the single moved bound (whichever of
    start/end differs from the current window) and `{months}` as `diffDays(parseIsoDay(oldBound),
    parseIsoDay(newBound))` folded to whole months (round to nearest; floor at 1 mo when the
    delta is > 0 but < 1 — the A3 discretion). `onInclude` = `() => applyWindow(includeTarget)`.
    Critically: `onInclude` MUST ONLY call `applyWindow` — it must NEVER touch
    `deAliased.state.selected` (a manually-off strategy stays sticky; the include only moves
    the window). Reversibility is already provided by the existing Common-period / Full-range
    presets — add nothing for it.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Include → shortens window to' src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` returns >= 1 (verbatim label present).
    - `grep -c 'intersectionOf' src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` shows the include-target uses intersectionOf (no hand-rolled Math.min/Math.max over date strings for the include bound): `git diff | grep -E '^\+' | grep -c 'Math.max.*first\|Math.min.*last'` returns 0.
    - The onInclude path calls `applyWindow` and does NOT mutate selected: `git diff | grep -E '^\+' | grep 'onInclude' -A3 | grep -c 'setSelected\|state.selected\|onToggle'` returns 0.
    - AutoExcludedRow still renders the existing `data-testid="auto-excluded-reason"` span (extension is additive, not a rebuild).
    - Existing composer suite (Phase-57 auto-excluded / auto-toggle blocks) stays green.
  </acceptance_criteria>
  <done>Auto-excluded rows carry the amber chip + a cost-disclosing Include text-button; clicking it narrows the window via applyWindow to the intersectionOf target; manual-off is never reselected.</done>
</task>

<task type="auto">
  <name>Task 2: COVERAGE-04 integration assertions in ScenarioComposer.test.tsx</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (lines 185-215 — CustomRangePicker capture mock; lines 198-215 — the computeScenario engine-arg recorder / REAL member_count oracle; lines 5138-5340 — the Phase-57 window block to extend)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (the extended AutoExcludedRow + onInclude from Task 1)
    - .planning/phases/58-coverage-legibility-disclosure/58-PATTERNS.md (Integration-test rig section — reuse the picker-capture + REAL computeScenario oracle; extend the existing block, no new spec)
  </read_first>
  <action>
    Extend the EXISTING Phase-57 window block in `ScenarioComposer.test.tsx` (do NOT add a new
    spec file, no new HAS_SEED_ENV const, no ci.yml entry — the rig is already wired). Mount an
    unequal-span book where a widened window auto-excludes strategy B. Assert: (1) B's
    auto-excluded row renders the amber "Outside window" chip AND its include text-button whose
    label contains "Include → shortens window to " and a "−" month delta; (2) clicking the
    include button narrows the window so B becomes a member — assert the REAL computeScenario
    `member_count` RISES (the load-bearing oracle) and B's row leaves the auto-excluded group;
    (3) if B was instead manually-off, the include click does NOT reselect it (selected stays
    false). Reuse the picker-capture `onApply` + REAL `@/lib/scenario` rig at lines 185-215.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The extended block asserts the include label text ("Include → shortens window to " + "−...mo") and that clicking it raises the REAL computeScenario member_count.
    - The block asserts the include click on a manually-off strategy does NOT set selected true.
    - No new spec file created: `git status --porcelain "src/app/(dashboard)/allocations/components/" | grep -c '?? .*\.spec\.'` returns 0.
    - Full composer suite green.
  </acceptance_criteria>
  <done>Integration test proves the include button's cost label, that applying it raises the engine divisor (B becomes a member), and that a manual-off strategy is not reselected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| include-button click → window state | the only new "input"; sets winStart/winEnd from scenario-window-derived bounds, never free-form user text |
| strategy name / date strings → DOM | rendered as React text children (auto-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04 | Tampering | include-cost apply sets an unbounded window | mitigate | The include target is `intersectionOf([currentWindow, strategySpan])` — always within existing data bounds; no free-form input; the picker path already validates via parseIsoDay |
| T-58-05 | Information Disclosure | include reselects a manual-off strategy (leaks it into the blend against user intent) | mitigate | `onInclude` calls ONLY `applyWindow`; the acceptance criteria + integration test assert `selected` is never mutated (manual-off stays sticky) |
| T-58-06 | Tampering / divisor desync | amber chip + row membership drifts from engine member_ids | mitigate | The row set is the existing `autoExcluded` memo (same axis the `:1813` guard reconciles); the include-raises-member_count integration test is the oracle |
| T-58-SC | Tampering | npm/pip/cargo installs | accept | No package install in this phase (zero new runtime dependency); no registry lookup |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` — green, including the new COVERAGE-04 assertions.
- `npm run test` — full frontend suite green (no engine file touched; frozen-spine / BLEND-07 / parity guards unaffected).
- Dev-mode: widen window to auto-exclude a strategy, click Include, confirm it becomes a member and no desync console.warn fires.
</verification>

<success_criteria>
- COVERAGE-02 completed: auto-excluded rows carry the amber "Outside window" chip + existing reason text (all three states now render).
- COVERAGE-04: the include text-button discloses the exact cost ("shortens window to {date} (−{N} mo)") and applies it in one reversible click via `applyWindow`, raising the engine divisor; manual-off is never reselected.
- Interval math delegated to `scenario-window.ts` `intersectionOf`; no numeric/engine change.
</success_criteria>

<output>
Create `.planning/phases/58-coverage-legibility-disclosure/58-02-SUMMARY.md` when done.
</output>
