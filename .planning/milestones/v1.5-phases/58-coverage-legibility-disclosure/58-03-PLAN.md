---
phase: 58-coverage-legibility-disclosure
plan: 03
type: execute
wave: 3
depends_on: [58-01, 58-02]
files_modified:
  - src/app/(dashboard)/allocations/components/CoverageTimeline.tsx
  - src/app/(dashboard)/allocations/components/CoverageTimeline.test.tsx
  - src/app/(dashboard)/allocations/components/DefaultChangeNote.tsx
  - src/app/(dashboard)/allocations/components/DefaultChangeNote.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - e2e/composer-axe.spec.ts
autonomous: true
requirements: [COVERAGE-01, POLISH-03]
must_haves:
  truths:
    - "A collapsed-by-default 'Coverage timeline' panel shows one horizontal bar per selected strategy against the union date axis, with the active [winStart,winEnd] window drawn as a shaded band overlay"
    - "Each bar's in-window portion is accent, out-of-window is muted, and an auto-excluded strategy's bar is amber — agreeing with its row chip — with an aria-label restating coverage + membership as text"
    - "The date→x scale uses utcEpoch(parseIsoDay(...)) — no new Date(iso) anywhere in the timeline"
    - "A one-time union→intersection note ('Now showing the common period where all {N} overlap · Show full range') shows ONLY when the intersection truncates the union AND not dismissed; the × dismiss persists across sessions via useCrossTabStorage and the note is SSR-safe (no hydration flash)"
    - "'Show full range' triggers the existing Full-range preset (applyWindow(fullRangeWindow))"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/CoverageTimeline.tsx"
      provides: "Collapsed-by-default mini-gantt of selected-strategy spans vs the active window"
      min_lines: 40
    - path: "src/app/(dashboard)/allocations/components/DefaultChangeNote.tsx"
      provides: "One-time SSR-safe union→intersection default-change note with dismiss + escape hatch"
      min_lines: 30
    - path: "src/app/(dashboard)/allocations/components/CoverageTimeline.test.tsx"
      provides: "Bar encoding + window band + aria-label + collapsed-default coverage"
    - path: "src/app/(dashboard)/allocations/components/DefaultChangeNote.test.tsx"
      provides: "shown/hidden/dismissed/escape-hatch/a11y coverage"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/CoverageTimeline.tsx"
      to: "utcEpoch(parseIsoDay(...)) date→x scale"
      via: "dateday.ts import"
      pattern: "utcEpoch"
    - from: "src/app/(dashboard)/allocations/components/DefaultChangeNote.tsx"
      to: "composer.coverageDefaultChangeNoteDismissed localStorage key"
      via: "useCrossTabStorage + rawStringCodec<boolean>"
      pattern: "useCrossTabStorage"
    - from: "DefaultChangeNote 'Show full range'"
      to: "applyWindow(fullRangeWindow)"
      via: "onShowFullRange prop"
      pattern: "applyWindow"
---

<objective>
Deliver the last two disclosure surfaces: the collapsed-by-default **coverage timeline**
mini-gantt (COVERAGE-01) and the one-time union→intersection **default-change note**
(POLISH-03). Both are new pure/near-pure child components wired into `ScenarioComposer.tsx`;
the note persists its dismissal through the already-registered (Plan 01) `composer.`
localStorage prefix via `useCrossTabStorage`. This plan also extends the `composer-axe.spec.ts`
whole-`<main>` anchors so the new blend header + (expanded) gantt are gated by the WCAG-AA scan.

Purpose: The gantt answers "why did X drop?" and "how much history to keep it?" visually; the
note ensures returning users aren't surprised by the intersection default and gives them a
one-click escape hatch to the full range.

Output: `CoverageTimeline.tsx` + `DefaultChangeNote.tsx` (+ colocated tests), wired into
`ScenarioComposer.tsx`; extended `e2e/composer-axe.spec.ts` anchors.

Depends on Plans 01 + 02: the `composer.` storage prefix (Plan 01) and the serialized
`ScenarioComposer.tsx` edits (single shared file — this is the last writer).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-coverage-legibility-disclosure/58-CONTEXT.md
@.planning/phases/58-coverage-legibility-disclosure/58-UI-SPEC.md
@.planning/phases/58-coverage-legibility-disclosure/58-PATTERNS.md
@DESIGN.md
@.planning/phases/58-coverage-legibility-disclosure/58-01-SUMMARY.md
@.planning/phases/58-coverage-legibility-disclosure/58-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase; use directly, no exploration. -->

From src/lib/dateday.ts (timezone-stable date→x scale — NEVER new Date(iso)):
  export function parseIsoDay(s: string): IsoDay | null;
  export function utcEpoch(day: IsoDay): number;   // docstring: "chart x-axis scaling"

From src/lib/scenario-window.ts:
  export interface CoverageSpan { first: string; last: string; }
  export interface CoverageWindow { start: string; end: string; }
  export function unionOf(spans: CoverageSpan[]): CoverageWindow | null;

From src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (existing formatter):
  function formatIsoMonth(iso: string): string;   // line 418 — string-slice "YYYY-MM-DD" → "Mon YYYY", timezone-free

ScenarioComposer.tsx existing memos to thread as props (do NOT recompute):
  selectedSpanById          // Map<id, CoverageSpan | null> (line 1639)
  coverageEligible          // Record<string, boolean> (line 1762) — in-blend axis (bar color)
  coverageWindow            // CoverageWindow | null (line 1684) — active [winStart,winEnd] band
  fullRangeWindow           // unionOf(selectedSpans) (line 1722) — the x-axis span + escape-hatch target
  applyWindow               // (range:{start,end})=>void (line 1705)
  deAliased.state.selected  // Record<string, boolean>

CollapsibleSection host (src/components/ui/CollapsibleSection.tsx:33):
  <CollapsibleSection title={...} defaultOpen={false} storageKey?={...}>{children}</CollapsibleSection>
  — native <details>, keyboard-accessible, respects prefers-reduced-motion.

useCrossTabStorage (src/lib/storage/cross-tab.ts:216) + rawStringCodec (src/lib/storage/codecs.ts:146):
  const { value, setValue, isHydrated } = useCrossTabStorage<T>({ key, initial, codec, sentryArea });
  — deferred hydration: server HTML === first client render (both use `initial`), no mismatch.

Registered (Plan 01): "composer." prefix in APP_NAMESPACED_PREFIXES;
"composer.coverageDefaultChangeNoteDismissed" in SignOutButton.test.tsx KNOWN_APP_KEYS.

e2e/composer-axe.spec.ts — scans the whole composed <main> on /allocations?tab=scenario;
serious+critical gate. Already CI-wired (no new HAS_SEED_ENV const, no ci.yml entry needed).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CoverageTimeline mini-gantt (COVERAGE-01) + colocated test</name>
  <files>src/app/(dashboard)/allocations/components/CoverageTimeline.tsx, src/app/(dashboard)/allocations/components/CoverageTimeline.test.tsx</files>
  <read_first>
    - src/lib/dateday.ts (lines 47-52 parseIsoDay, 94-97 utcEpoch — the timezone-stable date→x scale; docstring "chart x-axis scaling")
    - src/lib/scenario-window.ts (lines 36-52 CoverageSpan/CoverageWindow; lines 99-115 unionOf)
    - src/components/ui/CollapsibleSection.tsx (lines 33-92 — defaultOpen / storageKey / native <details> host)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (lines 3696-3711 AutoExcludedRow — the transition-all duration-300 ease-out motion-reduce:transition-none motion idiom; line 418 formatIsoMonth)
    - .planning/phases/58-coverage-legibility-disclosure/58-PATTERNS.md (CoverageTimeline.tsx section — date→x scale + bar encoding + collapsed host + motion idiom)
    - .planning/phases/58-coverage-legibility-disclosure/58-UI-SPEC.md (§Color — gantt bar encoding, window band; §Typography — timeline row name text-fixed-12, axis endpoint dates text-fixed-11 font-mono tabular-nums; §Interaction — collapsed default, duration-300 not duration-250)
    - .planning/phases/58-coverage-legibility-disclosure/58-RESEARCH.md (lines 546-563 — the flex/div gantt skeleton with aria-label; Open Question 1 — render union start+end endpoint labels only, no interior ticks)
  </read_first>
  <behavior>
    - Props: rows of { id, name, span: CoverageSpan | null, state: "in-blend" | "auto-excluded" }, the union window (CoverageWindow | null for the axis), and the active window (CoverageWindow | null for the band).
    - Renders one bar per selected strategy; leftPct/widthPct computed via utcEpoch(parseIsoDay(...)) against the union axis, guarding a single-day union (span || 1) against div-by-zero.
    - In-blend bar: in-window portion bg-accent, out-of-window bg-track; auto-excluded bar: bg-warning-bg + border-warning-border (agrees with the row chip).
    - The active [winStart,winEnd] draws as a shaded vertical band overlay (bg-accent/5 or 1px border-accent).
    - Every bar carries an aria-label: "{name}: covers {first}–{last}, {in blend | auto-excluded — {reason-or-outside window}}".
    - Union start + union end endpoint date labels only (no interior ticks), text-fixed-11 font-mono tabular-nums.
    - Whole panel is collapsed by default inside CollapsibleSection (title "Coverage timeline").
  </behavior>
  <action>
    Create `CoverageTimeline.tsx`. Props: `{ rows: { id: string; name: string; span:
    CoverageSpan | null; inBlend: boolean }[]; unionWindow: CoverageWindow | null;
    activeWindow: CoverageWindow | null }`. Wrap the whole thing in `<CollapsibleSection
    title="Coverage timeline" defaultOpen={false}>` (collapsed by default per CONTEXT). Build
    the date→x scale with `dateday.ts` `utcEpoch(parseIsoDay(iso))` ONLY — NEVER `new
    Date(iso)` (Pitfall 2 / H-1224). Compute `x0 = utcEpoch(parseIsoDay(unionWindow.start))`,
    `x1 = utcEpoch(parseIsoDay(unionWindow.end))`, `span = x1 - x0 || 1`; per-row `leftPct`
    /`widthPct` as in the 58-PATTERNS scale block. Render each row as a `<li>` with a truncated
    name (`text-fixed-12`, `title={name}`) + a relative track (`bg-track`) holding the
    absolutely-positioned bar. Bar fill: in-blend → `bg-accent` for the in-window slice with
    `bg-track` for the out-of-window remainder; auto-excluded (inBlend false) → whole bar
    `bg-warning-bg border border-warning-border`. Draw the `activeWindow` as a shaded band
    overlay (`bg-accent/5` or a 1px `border-accent` frame) positioned by the same scale. Give
    every bar an `aria-label` restating coverage + membership as TEXT (color is never the sole
    signal — WCAG-AA). Render union start + union end endpoint date labels only (no interior
    ticks — Open Question 1) in `text-fixed-11 font-mono tabular-nums text-text-muted`; format
    via the existing `formatIsoMonth` (or the raw ISO) — timezone-free, never `new Date`. Any
    transition uses `duration-300 ease-out motion-reduce:transition-none` (NEVER `duration-250`
    — invalid Tailwind v4, Pitfall 5). NO icons. NO recharts (Pitfall 4). Then create
    `CoverageTimeline.test.tsx`: render a mixed book (one in-blend, one auto-excluded), assert
    (1) one bar per row, (2) the in-blend bar has an accent class and the auto-excluded bar has
    `bg-warning-bg`, (3) each bar's aria-label contains the coverage dates + membership word,
    (4) the panel is collapsed by default (the CollapsibleSection is not open), (5) no `new
    Date(` appears in the source (guard the timezone rule).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/CoverageTimeline.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'new Date(' src/app/(dashboard)/allocations/components/CoverageTimeline.tsx` returns 0 (timezone rule — use utcEpoch/parseIsoDay).
    - `grep -c 'utcEpoch' src/app/(dashboard)/allocations/components/CoverageTimeline.tsx` returns >= 1.
    - `grep -c 'recharts\|duration-250' src/app/(dashboard)/allocations/components/CoverageTimeline.tsx` returns 0.
    - `grep -c 'aria-label' src/app/(dashboard)/allocations/components/CoverageTimeline.tsx` returns >= 1 (bars carry text/aria, not color-only).
    - `CoverageTimeline.test.tsx` asserts bar-per-row, accent vs bg-warning-bg encoding, aria-label content, and collapsed-by-default.
  </acceptance_criteria>
  <done>The collapsed mini-gantt renders accent/muted/amber bars + a window band via a utcEpoch scale, each with an aria-label; colocated test covers encoding, aria, and collapsed default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DefaultChangeNote (POLISH-03) SSR-safe one-time note + colocated test</name>
  <files>src/app/(dashboard)/allocations/components/DefaultChangeNote.tsx, src/app/(dashboard)/allocations/components/DefaultChangeNote.test.tsx</files>
  <read_first>
    - src/components/ui/CollapsibleSection.tsx (lines 71-102 — the EXACT useCrossTabStorage + rawStringCodec deferred-hydration boolean-persistence pattern to copy, gating render on isHydrated)
    - src/lib/storage/cross-tab.ts (lines 216-340 — useCrossTabStorage return shape: value/setValue/isHydrated; deferred hydration)
    - src/lib/storage/codecs.ts (lines 146-157 — rawStringCodec<T>)
    - src/lib/storage-namespaces.ts (confirm the "composer." prefix registered by Plan 01)
    - .planning/phases/58-coverage-legibility-disclosure/58-PATTERNS.md (DefaultChangeNote.tsx section — codec + hook + visibility gate + escape hatch + a11y)
    - .planning/phases/58-coverage-legibility-disclosure/58-UI-SPEC.md (§Copywriting Contract — verbatim "Now showing the common period where all {N} overlap · Show full range"; §Interaction — role=status aria-live=polite, × dismiss, shown only when intersection truncates union)
    - .planning/phases/58-coverage-legibility-disclosure/58-RESEARCH.md (Pattern 3 + Pitfall 3 — no flash-of-note; gate on isHydrated && !dismissed && intersectionTruncatesUnion)
  </read_first>
  <behavior>
    - Props: { memberCount: number; intersectionTruncatesUnion: boolean; onShowFullRange: () => void }.
    - Persists dismissal at key "composer.coverageDefaultChangeNoteDismissed" via useCrossTabStorage + rawStringCodec<boolean> (initial: false, sentryArea "composer.default-change-note").
    - Renders the note ONLY when isHydrated && !dismissed && intersectionTruncatesUnion (no flash for a returning-dismissed user; no note when spans coincide).
    - Note copy verbatim: "Now showing the common period where all {memberCount} overlap · Show full range" where "Show full range" is an inline accent text-button calling onShowFullRange.
    - The × dismiss button sets dismissed = true (persisted).
    - Root: role="status" aria-live="polite" (NEVER role="alert").
  </behavior>
  <action>
    Create `DefaultChangeNote.tsx`. Copy the `CollapsibleSection.tsx:71-102` persistence idiom
    verbatim, swapping the codec to boolean: `rawStringCodec<boolean>({ parse: (raw) => raw ===
    "true", serialize: (v) => (v ? "true" : "false") })` built via `useMemo`. Call
    `useCrossTabStorage<boolean>({ key: "composer.coverageDefaultChangeNoteDismissed", initial:
    false, codec, sentryArea: "composer.default-change-note" })`. Props `{ memberCount: number;
    intersectionTruncatesUnion: boolean; onShowFullRange: () => void }`. Gate the render on
    `isHydrated && !dismissed && intersectionTruncatesUnion` (Pitfall 3 — no flash-of-note,
    never when spans coincide). Render the verbatim copy `Now showing the common period where
    all {memberCount} overlap · ` with an inline accent text-button "Show full range"
    (`text-accent hover:text-accent-hover focus-visible:ring-2 focus-visible:ring-accent/50`)
    calling `onShowFullRange`, plus an `×` dismiss `<button aria-label="Dismiss">` that calls
    `setValue(true)`. Root element `role="status" aria-live="polite"` styled as an
    informational (NOT warning) inline note (`text-fixed-13 text-text-secondary`,
    surface-subtle bg + hairline border). Use only DESIGN.md tokens; NO raw `text-[Npx]`; NO
    icons (the `×` is a text glyph). Do NOT use raw `localStorage.setItem` (B25 lint). Then
    create `DefaultChangeNote.test.tsx`: cover (1) hidden when intersectionTruncatesUnion is
    false, (2) shown when true + not dismissed (after hydration), (3) clicking × hides it and
    persists (assert setValue path — remount reads dismissed), (4) "Show full range" calls
    onShowFullRange, (5) root has role="status". Follow the hydrate idiom from the composer's
    existing `.hydrate.test.tsx` style so the SSR-safe gate is exercised.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/DefaultChangeNote.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useCrossTabStorage' src/app/(dashboard)/allocations/components/DefaultChangeNote.tsx` returns >= 1; `grep -c 'localStorage.setItem\|localStorage.getItem' src/app/(dashboard)/allocations/components/DefaultChangeNote.tsx` returns 0 (B25 lint — no raw localStorage).
    - `grep -c 'composer.coverageDefaultChangeNoteDismissed' src/app/(dashboard)/allocations/components/DefaultChangeNote.tsx` returns 1 (matches the registered key).
    - `grep -c 'role="alert"' src/app/(dashboard)/allocations/components/DefaultChangeNote.tsx` returns 0; `grep -c 'role="status"' ...` returns >= 1.
    - `grep -c 'Now showing the common period where all' src/app/(dashboard)/allocations/components/DefaultChangeNote.tsx` returns 1 (verbatim copy).
    - `DefaultChangeNote.test.tsx` covers shown/hidden/dismissed/escape-hatch/role=status (5 branches).
  </acceptance_criteria>
  <done>The note shows only when the intersection truncates the union and not dismissed, persists dismissal via useCrossTabStorage under the registered key, offers the Show-full-range escape hatch, is SSR-safe and role=status; colocated test green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CoverageTimeline + DefaultChangeNote into ScenarioComposer; extend composer-axe anchors</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, e2e/composer-axe.spec.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (lines 1684-1832 memos incl coverageWindow/coverageEligible/fullRangeWindow/scenarioMetrics/selectedSpanById/applyWindow; lines 2828-2887 the coverage-window control — the note attaches ABOVE it, the timeline within/after it, per UI-SPEC Reused-primitives table)
    - e2e/composer-axe.spec.ts (read in full — the whole-<main> analyze() + serious+critical gate; the composed-surface anchors to extend)
    - .planning/phases/58-coverage-legibility-disclosure/58-UI-SPEC.md (Reused existing primitives table — blend header + POLISH-03 note attach ABOVE the window control; timeline toggle within/after it; §Interaction — timeline tertiary, note above window control/header)
    - .planning/phases/58-coverage-legibility-disclosure/58-PATTERNS.md (Shared Patterns — membership single-source; DefaultChangeNote visibility gate = intersectionTruncatesUnion)
    - .planning/phases/58-coverage-legibility-disclosure/58-RESEARCH.md (Wave 0 Gaps — extend composer-axe anchors, NO new spec / HAS_SEED_ENV / ci.yml entry)
  </read_first>
  <action>
    In `ScenarioComposer.tsx`, import and render `CoverageTimeline` and `DefaultChangeNote`.
    Build the `CoverageTimeline` `rows` prop from `deAliased.strategies` filtered to
    `deAliased.state.selected[id]`, mapping each to `{ id, name, span: selectedSpanById.get(id)
    ?? null, inBlend: coverageEligible[id] === true }`; pass `unionWindow={fullRangeWindow}` and
    `activeWindow={coverageWindow}`. Place the timeline within/after the coverage-window control
    (tertiary surface — CONTEXT places it there, collapsed). Compute
    `intersectionTruncatesUnion` inline (the coverageWindow / effective window is narrower than
    `fullRangeWindow` via lexicographic compare — reuse the same truncation test shape as
    BlendHeader) and render `<DefaultChangeNote memberCount={scenarioMetrics.member_count ?? 0}
    intersectionTruncatesUnion={...} onShowFullRange={() => fullRangeWindow &&
    applyWindow(fullRangeWindow)} />` ABOVE the window control / blend header (UI-SPEC placement).
    Do NOT recompute membership — thread `coverageEligible` / `selectedSpanById` /
    `scenarioMetrics` (keep the `:1813` desync guard quiet). Then extend `e2e/composer-axe.spec.ts`:
    add composed-surface anchors so the scan waits for the new blend-header and (when expanded)
    the coverage-timeline to be present in the composed `<main>` before `analyze()`, so the
    WCAG-AA gate covers them. Do NOT create a new spec, do NOT add a HAS_SEED_ENV const, do NOT
    touch ci.yml — the spec is already CI-wired (FLOW-01 does not apply).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CoverageTimeline\|DefaultChangeNote' src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` returns >= 2 (both wired).
    - `git diff src/app/(dashboard)/allocations/components/ScenarioComposer.tsx | grep -E '^\+' | grep -c 'covers('` returns 0 (membership threaded, not re-derived).
    - `git status --porcelain e2e/ | grep -c '?? '` returns 0 (no NEW e2e spec file — the existing composer-axe.spec.ts is extended in place).
    - `git diff .github/workflows/ci.yml | wc -l` returns 0 (ci.yml untouched).
    - Full composer suite green; dev desync guard does not warn.
  </acceptance_criteria>
  <done>CoverageTimeline (collapsed) + DefaultChangeNote (gated on truncation, above the header) wired into the composer from existing memos; composer-axe scan anchors extended to cover the new surfaces; no new spec / ci.yml change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage read/write → note visibility | the POLISH-03 dismissal boolean; routed through the hardened useCrossTabStorage primitive |
| strategy name / date strings → DOM / SVG-or-div bars | rendered as React text children + aria-label text (auto-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-07 | Tampering | localStorage prototype-poison / corrupt read for the dismissal flag | mitigate | `useCrossTabStorage` strips poison keys at the parse boundary + folds corrupt reads to `initial: false` via rawStringCodec; inherited for free |
| T-58-08 | Information Disclosure | dismissal flag survives sign-out (minor — a non-sensitive UI note) | mitigate | The `composer.` prefix registered in Plan 01 means `purgeAppNamespacedStorage()` clears it on sign-out; SignOutButton.test.tsx asserts coverage |
| T-58-09 | Tampering / XSS | strategy names / dates in gantt aria-labels + note copy | accept | React auto-escapes all text nodes and aria-label string props; no `dangerouslySetInnerHTML` |
| T-58-10 | Tampering | timezone off-by-one shifts bars → misleading coverage display | mitigate | Scale uses `utcEpoch(parseIsoDay(...))` only; acceptance criteria assert `new Date(` count is 0 in CoverageTimeline.tsx |
| T-58-SC | Tampering | npm/pip/cargo installs | accept | No package install (zero new runtime dependency); no registry lookup |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/CoverageTimeline.test.tsx" "src/app/(dashboard)/allocations/components/DefaultChangeNote.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` — all green.
- `npx playwright test e2e/composer-axe.spec.ts` — zero serious/critical WCAG-AA violations on the composed surface incl the new header + gantt.
- `npm run test:coverage` — the blocking ratchet (lines 82 / functions 74 / branches 72) stays green with the four new tested child components.
- `npm run test` — frozen-spine + BLEND-07 + parity guards green (no engine file touched).
</verification>

<success_criteria>
- COVERAGE-01: collapsed-by-default coverage timeline shows each selected strategy's span vs the active window, accent/muted/amber bars agreeing with row chips, via a timezone-safe utcEpoch scale, each bar aria-labelled.
- POLISH-03: the one-time union→intersection note shows only on real truncation, dismisses persistently (SSR-safe, no flash), and offers the Show-full-range escape hatch via the existing Full-range preset.
- The composer-axe WCAG-AA scan covers the new header + gantt; no numeric/engine change; coverage ratchet green.
</success_criteria>

<output>
Create `.planning/phases/58-coverage-legibility-disclosure/58-03-SUMMARY.md` when done.
</output>
