# Plan 60-01 — Restore the Phase-58 e2e net + VERIFY-01 evidence

## Tasks

1. **Seed helper** (`e2e/helpers/seed-test-project.ts`): add optional
   `withDailyReturns?: boolean` to `seedStrategyWithHistory`. When true, write
   `strategy_analytics.daily_returns` as `[{date, value}]` (small finite daily
   returns, deterministic sin-based, same date axis as `returns_series`).
   Default absent → byte-identical behavior for every existing caller
   (svg-chart-parity / strategy-v2 golden fixtures untouched).
2. **composer-axe** (`e2e/composer-axe.spec.ts`): seed with
   `withDailyReturns: true`; replace the ced581e0 conditional block with
   unconditional anchors — (d) blend header visible, (e) timeline expands —
   plus a pin that `scenario-coverage-window-value` shows a real
   `YYYY-MM-DD – YYYY-MM-DD` range. Update the stale "not guaranteed
   non-degenerate" comments to the new deterministic contract.
3. **Evidence** (`.planning/phases/60-golden-e2e-rebake/60-VERIFY-01-EVIDENCE.md`):
   record the no-op-bake proof (what each golden spec renders, CI runs green
   with zero snapshot churn, BLEND-07/parity anchors green on main).

## Verification

- `tsc --noEmit` + eslint + full vitest suite green (no frontend code touched,
  but the gates run anyway).
- Seeded composer-axe run green — locally if TEST_SUPABASE env is available,
  otherwise the PR CI e2e job is the proof gate (must be PASS, not skip, and
  the anchors must execute unconditionally).
- Zero diffs under `e2e/*-snapshots/` and zero engine-file diffs.

## must_haves

- Anchors run UNCONDITIONALLY in CI (the console.warn escape hatch deleted).
- Seed option is opt-in; no other caller changes.
- No `--update-snapshots` run anywhere.
