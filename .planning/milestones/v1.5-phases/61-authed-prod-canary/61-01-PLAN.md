# Phase 61 — Authed Prod QA Canary (VERIFY-02) — PLAN 01

Goal: the whole coverage-window chain verified live on authed prod
`/allocations` — the honest behaviors an allocator will actually touch.

Prod = quantalyze.xyz @ main (app last changed by #567 bac2960f v0.35.0.28;
#568/#569 are CI-only). Account: qa-demo@quantalyze.app (role=both,
is_admin) — login via service-role magic-link `action_link` (no password
typed), interactive session in Playwright MCP browser.

## Checklist

### A. Core VERIFY-02 (roadmap SC1) — composer on /allocations
- [ ] A1 widen window → auto-drop: strategy whose span ⊉ window moves to the
      auto-excluded group with inline reason
- [ ] A2 narrow window back → auto-restore (animated toggle back to in-blend)
- [ ] A3 preset "Common period (all in)" → intersection window, all members in
- [ ] A4 preset "Full range (some drop)" → union window, short-coverage rows drop
- [ ] A5 empty-intersection guidance: banner names the outlier + 1-click deselect
- [ ] A6 divisor == visible membership: blend-header N == count of in-blend rows

### B. Deferred HUMAN-UAT (6)
- [ ] B1 (58) coverage mini-gantt bars visually proportional to real spans
- [ ] B2 (58) blend-header hierarchy reads correctly (members · window · N)
- [ ] B3 (59) save scenario → reopen: window round-trips
- [ ] B4 (59) share link → recipient view recomputes at owner's window
- [ ] B5 (59) provenance note visible on the shared view
- [ ] B6 (59) heterogeneous compare: per-column effective windows in <tfoot>

### C. Phase-60 a11y fixes live (2)
- [ ] C1 factsheet heatmap label contrast ≥4.5:1 (spot-check via JS
      getComputedStyle + contrast calc on a mid-intensity negative cell)
- [ ] C2 scroll regions keyboard-focusable (daily calendar stack, monthly
      table, correlation matrix: role=region + tabindex=0, focus visible)

Every step: note console errors. Output: 61-VERIFICATION.md with per-item
evidence. Cleanup: any scenarios/shares created get deleted; session
self-expires.
